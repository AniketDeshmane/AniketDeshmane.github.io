// Auto-generated by agent
const COURSE_DATA = [{"id": "Book_1", "title": "Book_1...", "bookTag": "Interview Book", "lessons": [{"id": "lesson-0", "title": "CLR Basics", "readTime": "5 min read", "subtitle": "", "content": "<ul style=\"list-style-type: none;\">\n<li><p>CHAPTER 1 The CLR's Execution Model . . . . . . . . . . . . . . . . . . . . .3</p></li>\n<li><p>CHAPTER 2 Building, Packaging, Deploying, and Administering Applications and Types. . . . . . . . . . .33</p></li>\n<li><p>CHAPTER 3 Shared Assemblies and Strongly Named Assemblies. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . .65</p></li>\n</ul><h6>C H A P T E R 1</h6><h2>The CLR\u2019s Execution Model</h2><h6>In this chapter:</h6><p>Compiling Source Code into Managed Modules . . . . . . . . . . . . . . 3 Combining Managed Modules into Assemblies . . . . . . . . . . . . . . . 6 Loading the Common Language Runtime . . . . . . . . . . . . . . . . . . . . 8 Executing Your Assembly\u2019s Code . . . . . . . . . . . . . . . . . . . . . . . . . . . 11 The Native Code Generator Tool: NGen.exe . . . . . . . . . . . . . . . . . 19 The Framework Class Library . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 22 The Common Type System. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 24 The Common Language Specification. . . . . . . . . . . . . . . . . . . . . . . 26 Interoperability with Unmanaged Code . . . . . . . . . . . . . . . . . . . . . 30</p><p>The Microsoft .NET Framework introduces many concepts, technologies, and terms. My goal in this chapter is to give you an overview of how the .NET Framework is designed, introduce you to some of the technologies the framework includes, and define many of the terms you\u2019ll be seeing when you start using it. I\u2019ll also take you through the process of building your source code into an application or a set of redistributable components (files) that contain types (classes, structures, etc.) and then explain how your application will execute.</p><h6>Compiling Source Code into Managed Modules</h6><p>OK, so you\u2019ve decided to use the .NET Framework as your development platform. Great! Your first step is to determine what type of application or component you intend to build. Let\u2019s just assume that you\u2019ve completed this minor detail; everything is designed, the specifications are written, and you\u2019re ready to start development.</p><p>Now you must decide which programming language to use. This task is usually difficult because different languages offer different capabilities. For example, in unmanaged C/C++, you have pretty low-level control of the system. You can manage memory exactly the way you want to, create threads easily if you need to, and so on. Microsoft Visual Basic 6.0, on the other hand, allows you to build UI applications very rapidly and makes it easy for you to control COM objects and databases.</p><p>The common language runtime (CLR) is just what its name says it is: a runtime that is usable by different and varied programming languages. The core features of the CLR (such as memory management, assembly loading, security, exception handling, and thread synchronization) are available to any and all programming languages that target it\u2014period. For example, the runtime uses excep-</p><p>tions to report errors, so all languages that target the runtime also get errors reported via exceptions. Another example is that the runtime also allows you to create a thread, so any language that targets the runtime can create a thread.</p><p>In fact, at runtime, the CLR has no idea which programming language the developer used for the source code. This means that you should choose whatever programming language allows you to express your intentions most easily. You can develop your code in any programming language you desire as long as the compiler you use to compile your code targets the CLR.</p><p>So, if what I say is true, what is the advantage of using one programming language over another? Well, I think of compilers as syntax checkers and \u201ccorrect code\u201d analyzers. They examine your source code, ensure that whatever you\u2019ve written makes some sense, and then output code that describes your intention. Different programming languages allow you to develop using different syntax. Don\u2019t underestimate the value of this choice. For mathematical or financial applications, expressing your intentions by using APL syntax can save many days of development time when compared to expressing the same intention by using Perl syntax, for example.</p><p>Microsoft has created several language compilers that target the runtime: C++/CLI, C# (pronounced \u201cC sharp\u201d), Visual Basic, F# (pronounced \u201cF sharp\u201d), Iron Python, Iron Ruby, and an Intermediate Language (IL) Assembler. In addition to Microsoft, several other companies, colleges, and universities have created compilers that produce code to target the CLR. I\u2019m aware of compilers for Ada, APL, Caml, COBOL, Eiffel, Forth, Fortran, Haskell, Lexico, LISP, LOGO, Lua, Mercury, ML, Mondrian, Oberon, Pascal, Perl, PHP, Prolog, RPG, Scheme, Smalltalk, and Tcl/Tk.</p><figcaption>Figure 1-1 shows the process of compiling source code files. As the figure shows, you can create source code files written in any programming language that supports the CLR. Then you use the corresponding compiler to check the syntax and analyze the source code. Regardless of which compiler you use, the result is a managed module. A managed module is a standard 32-bit Windows portable executable (PE32) file or a standard 64-bit Windows portable executable (PE32+) file that requires the CLR to execute. By the way, managed assemblies always take advantage of Data Execution Prevention (DEP) and Address Space Layout Randomization (ASLR) in Windows; these two features improve the security of your whole system.</figcaption><table border=\"1\">\n<tr>\n<td colspan=\"2\"><p>C# source code file(s)</p>\n</td>\n</tr>\n<tr>\n<td></td>\n<td></td>\n</tr>\n</table><table border=\"1\">\n<tr>\n<td colspan=\"2\"><p>Basic source code file(s)</p>\n</td>\n</tr>\n<tr>\n<td></td>\n<td></td>\n</tr>\n</table><p>IL source code file(s)</p><p>C# compiler</p><p>Basic compiler</p><p>IL Assembler</p><table border=\"1\">\n<tr>\n<td><p>Managed module (IL and metadata)</p>\n</td>\n</tr>\n</table><table border=\"1\">\n<tr>\n<td><p>Managed module (IL and metadata)</p>\n</td>\n</tr>\n</table><table border=\"1\">\n<tr>\n<td><p>Managed module (IL and metadata)</p>\n</td>\n</tr>\n</table><p>FIGURE 1-1 Compiling source code into managed modules.</p><p>Table 1-1 describes the parts of a managed module.</p><p>TABLE 1-1 Parts of a Managed Module</p><table border=\"1\">\n<tr>\n<td><p>Part PE32 or PE32+ header</p>\n</td>\n<td><h6>Description</h6>\n<p>The standard Windows PE file header, which is similar to the Common Object File Format (COFF) header. If the header uses the PE32 format, the file can run on a 32-bit or 64-bit version of Windows. If the header uses the PE32+ format, the file requires a 64-bit version of Windows to run. This header also indicates the type of file: GUI, CUI, or DLL, and contains a time stamp indicating when the file was built. For modules that contain only IL code, the bulk of the information in the PE32(+) header is ignored. For modules that contain native CPU code, this header contains information about the native CPU code.</p>\n</td>\n</tr>\n<tr>\n<td><p>CLR header</p>\n</td>\n<td><p>Contains the information (interpreted by the CLR and utilities) that makes this a managed module. The header includes the version of the CLR required, some flags, the MethodDef metadata token of the managed module\u2019s entry point method (Main method), and the location/size of the module\u2019s metadata, resources, strong name, some flags, and other less interesting stuff.</p>\n</td>\n</tr>\n<tr>\n<td><p>Metadata</p>\n</td>\n<td><p>Every managed module contains metadata tables. There are two main types of tables: tables that describe the types and members defined in your source code and tables that describe the types and members referenced by your source code.</p>\n</td>\n</tr>\n<tr>\n<td><p>IL code</p>\n</td>\n<td><p>Code the compiler produced as it compiled the source code. At run time, the CLR compiles the IL into native CPU instructions.</p>\n</td>\n</tr>\n</table><figcaption>Native code compilers produce code targeted to a specific CPU architecture, such as x86, x64, or ARM. All CLR-compliant compilers produce IL code instead. (I\u2019ll go into more detail about IL code later in this chapter.) IL code is sometimes referred to as managed code because the CLR manages its execution.</figcaption><p>In addition to emitting IL, every compiler targeting the CLR is required to emit full metadata into every managed module. In brief, metadata is a set of data tables that describe what is defined in the module, such as types and their members. In addition, metadata also has tables indicating what the managed module references, such as imported types and their members. Metadata is a superset of older technologies such as COM\u2019s Type Libraries and Interface Definition Language (IDL) files. The important thing to note is that CLR metadata is far more complete. And, unlike Type Libraries and IDL, metadata is always associated with the file that contains the IL code. In fact, the metadata is always embedded in the same EXE/DLL as the code, making it impossible to separate the two. Because the compiler produces the metadata and the code at the same time and binds them into the resulting managed module, the metadata and the IL code it describes are never out of sync with one another.</p><p>Metadata has many uses. Here are some of them:</p><p>\u25a0\u25a0 Metadata removes the need for native C/C++ header and library files when compiling because all the information about the referenced types/members is contained in the file that has the IL that implements the type/members. Compilers can read metadata directly from managed modules.</p><p>\u25a0\u25a0 Microsoft Visual Studio uses metadata to help you write code. Its IntelliSense feature parses metadata to tell you what methods, properties, events, and fields a type offers, and in the case of a method, what parameters the method expects.</p><p>\u25a0\u25a0 The CLR\u2019s code verification process uses metadata to ensure that your code performs only</p><p>\u201ctype-safe\u201d operations. (I\u2019ll discuss verification shortly.)</p><p>\u25a0\u25a0 Metadata allows an object\u2019s fields to be serialized into a memory block, sent to another ma-</p><p>chine, and then deserialized, re-creating the object\u2019s state on the remote machine.</p><p>\u25a0\u25a0 Metadata allows the garbage collector to track the lifetime of objects. For any object, the garbage collector can determine the type of the object and, from the metadata, know which fields within that object refer to other objects.</p><p>In Chapter 2, \u201cBuilding, Packaging, Deploying, and Administering Applications and Types,\u201d I\u2019ll describe metadata in much more detail.</p><p>Microsoft\u2019s C#, Visual Basic, F#, and the IL Assembler always produce modules that contain managed code (IL) and managed data (garbage-collected data types). End users must have the CLR (presently shipping as part of the .NET Framework) installed on their machine in order to execute any modules that contain managed code and/or managed data in the same way that they must have the Microsoft Foundation Class (MFC) library or Visual Basic DLLs installed to run MFC or Visual Basic 6.0 applications.</p><p>By default, Microsoft\u2019s C++ compiler builds EXE/DLL modules that contain unmanaged (native) code and manipulate unmanaged data (native memory) at run time. These modules don\u2019t require the CLR to execute. However, by specifying the /CLR command-line switch, the C++ compiler produces modules that contain managed code, and of course, the CLR must then be installed to execute this code. Of all of the Microsoft compilers mentioned, C++ is unique in that it is the only compiler that allows the developer to write both managed and unmanaged code and have it emitted into a single module. It is also the only Microsoft compiler that allows developers to define both managed and unmanaged data types in their source code. The flexibility provided by Microsoft\u2019s C++ compiler is unparalleled by other compilers because it allows developers to use their existing native C/C++ code from managed code and to start integrating the use of managed types as they see fit.</p><h6>Combining Managed Modules into Assemblies</h6><p>The CLR doesn\u2019t actually work with modules, it works with assemblies. An assembly is an abstract concept that can be difficult to grasp initially. First, an assembly is a logical grouping of one or more modules or resource files. Second, an assembly is the smallest unit of reuse, security, and versioning. Depending on the choices you make with your compilers or tools, you can produce a single-file or a multifile assembly. In the CLR world, an assembly is what we would call a component.</p><p>In Chapter 2, I\u2019ll go over assemblies in great detail, so I don\u2019t want to spend a lot of time on them here. All I want to do now is make you aware that there is this extra conceptual notion that offers a way to treat a group of files as a single entity.</p><p>Figure 1-2 should help explain what assemblies are about. In this figure, some managed modules</p><p>and resource (or data) files are being processed by a tool. This tool produces a single PE32(+) file that represents the logical grouping of files. What happens is that this PE32(+) file contains a block of data</p><figcaption>called the manifest. The manifest is simply another set of metadata tables. These tables describe the files that make up the assembly, the publicly exported types implemented by the files in the assembly, and the resource or data files that are associated with the assembly.</figcaption><table border=\"1\">\n<tr>\n<td rowspan=\"2\"><p>Managed module (IL and metadata)</p>\n</td>\n<td></td>\n</tr>\n<tr>\n<td></td>\n</tr>\n</table><p>Assembly</p><p>(Manifest: describes the set of files in the assembly)</p><p>Tool combining multiple managed modules and resource files into an assembly C# compiler</p><table border=\"1\">\n<tr>\n<td rowspan=\"2\"><p>Managed module (IL and metadata)</p>\n</td>\n<td></td>\n</tr>\n<tr>\n<td></td>\n</tr>\n</table><p>Managed module (IL and metadata)</p><p>Managed module (IL and metadata)</p><p>(CSC.exe), Visual Basic compiler</p><p>Resource file (.jpeg, .gif, .html, etc.)</p><table border=\"1\">\n<tr>\n<td rowspan=\"2\"><p>Resource file (.jpeg, .gif, .html, etc.)</p>\n</td>\n<td></td>\n</tr>\n<tr>\n<td></td>\n</tr>\n</table><p>(VBC.exe), Assembly Linker (AL.exe)</p><p>Resource file (.jpeg, .gif, .html, etc.)</p><table border=\"1\">\n<tr>\n<td rowspan=\"2\"><p>Resource file (.jpeg, .gif, .html, etc.)</p>\n</td>\n<td></td>\n</tr>\n<tr>\n<td></td>\n</tr>\n</table><p>FIGURE 1-2 Combining managed modules into assemblies.</p><p>By default, compilers actually do the work of turning the emitted managed module into an assembly; that is, the C# compiler emits a managed module that contains a manifest. The manifest indicates that the assembly consists of just the one file. So, for projects that have just one managed module and no resource (or data) files, the assembly will be the managed module, and you don\u2019t have any additional steps to perform during your build process. If you want to group a set of files into an assembly, you\u2019ll have to be aware of more tools (such as the assembly linker, AL.exe) and their commandline options. I\u2019ll explain these tools and options in Chapter 2.</p><p>An assembly allows you to decouple the logical and physical notions of a reusable, securable, versionable component. How you partition your code and resources into different files is completely up to you. For example, you could put rarely used types or resources in separate files that are part of an assembly. The separate files could be downloaded on demand from the web as they are needed at run time. If the files are never needed, they\u2019re never downloaded, saving disk space and reducing installation time. Assemblies allow you to break up the deployment of the files while still treating all of the files as a single collection.</p><p>An assembly\u2019s modules also include information about referenced assemblies (including their version numbers). This information makes an assembly self-describing. In other words, the CLR can determine the assembly\u2019s immediate dependencies in order for code in the assembly to execute. No additional information is required in the registry or in Active Directory Domain Services (AD DS). Because no additional information is needed, deploying assemblies is much easier than deploying unmanaged components.</p><h6>Loading the Common Language Runtime</h6><p>Each assembly you build can be either an executable application or a DLL containing a set of types for use by an executable application. Of course, the CLR is responsible for managing the execution of code contained within these assemblies. This means that the .NET Framework must be installed on the host machine. Microsoft has created a redistribution package that you can freely ship to install the .NET Framework on your customers\u2019 machines. Some versions of Windows ship with the</p><p>.NET Framework already installed.</p><p>You can tell if the .NET Framework has been installed by looking for the MSCorEE.dll file in the %SystemRoot%\\System32 directory. The existence of this file tells you that the .NET Framework is installed. However, several versions of the .NET Framework can be installed on a single machine simultaneously. If you want to determine exactly which versions of the .NET Framework are installed, examine the subdirectories under the following directories.</p><p>%SystemRoot%\\Microsoft.NET\\Framework %SystemRoot%\\Microsoft.NET\\Framework64</p><p>The .NET Framework SDK includes a command-line utility called CLRVer.exe that shows all of the CLR versions installed on a machine. This utility can also show which version of the CLR is being used by processes currently running on the machine by using the \u2013all switch or passing the ID of the process you are interested in.</p><p>Before we start looking at how the CLR loads, we need to spend a moment discussing 32-bit and 64-bit versions of Windows. If your assembly files contain only type-safe managed code, you are writing code that should work on both 32-bit and 64-bit versions of Windows. No source code changes are required for your code to run on either version of Windows. In fact, the resulting EXE/DLL file produced by the compiler should work correctly when running on x86 and x64 versions of Windows. In addition, Windows Store applications or class libraries will run on Windows RT machines (which use an ARM CPU). In other words, the one file will run on any machine that has the corresponding version of the .NET Framework installed on it.</p><p>On extremely rare occasions, developers want to write code that works only on a specific version of Windows. Developers might do this when using unsafe code or when interoperating with unmanaged code that is targeted to a specific CPU architecture. To aid these developers, the C# compiler offers a /platform command-line switch. This switch allows you to specify whether the resulting assembly can run on x86 machines running 32-bit Windows versions only, x64 machines running 64bit Windows only, or ARM machines running 32-bit Windows RT only. If you don\u2019t specify a platform, the default is anycpu, which indicates that the resulting assembly can run on any version of Windows. Users of Visual Studio can set a project\u2019s target platform by displaying the project\u2019s property pages, clicking the Build tab, and then selecting an option in the Platform Target list (see Figure 1-3).</p><p>In Figure 1-3, you\u2019ll notice the Prefer 32-Bit check box. This check box is only enabled when Platform Target is set to Any CPU and if the project type produces an executable. If you select the Prefer 32-Bit check box, then Visual Studio spawns the C# compiler specifying the /platform: anycpu32bitpreferred compiler switch. This option indicates that the executable should run as a 32-bit</p><p>executable even when running on a 64-bit machine. If your application doesn\u2019t require the additional memory afforded to a 64-bit process, then this is typically a good way to go because Visual Studio does not support edit-and-continue of x64 applications. In addition, 32-bit applications can interoperate with 32-bit DLLs and COM components should your application require this.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile3.png\"/><figcaption>FIGURE 1-3 Setting the platform target by using Visual Studio.</figcaption><p>Depending on the platform switch, the C# compiler will emit an assembly that contains either a PE32 or PE32+ header, and the compiler will also emit the desired CPU architecture (or agnostic) into the header as well. Microsoft ships two SDK command-line utilities, DumpBin.exe and CorFlags.exe, that you can use to examine the header information emitted in a managed module by the compiler.</p><p>When running an executable file, Windows examines this EXE file\u2019s header to determine whether the application requires a 32-bit or 64-bit address space. A file with a PE32 header can run with a 32bit or 64-bit address space, and a file with a PE32+ header requires a 64-bit address space. Windows also checks the CPU architecture information embedded inside the header to ensure that it matches the CPU type in the computer. Lastly, 64-bit versions of Windows offer a technology that allows 32-bit Windows applications to run. This technology is called WoW64 (for Windows on Windows 64).</p><p>Table 1-2 shows two things. First, it shows what kind of managed module you get when you specify various /platform command-line switches to the C# compiler. Second, it shows how that application will run on various versions of Windows.</p><figcaption>TABLE 1-2 Effects of /platform on Resulting Module and at Run Time</figcaption><table border=\"1\">\n<tr>\n<td><h6>/platform Switch</h6>\n<p>anycpu (the default)</p>\n</td>\n<td><p>Resulting Managed Module</p>\n<p>PE32/agnostic</p>\n</td>\n<td><h6>x86 Windows</h6>\n<p>Runs as a 32-bit application</p>\n</td>\n<td><h6>x64 Windows</h6>\n<p>Runs as a 64-bit application</p>\n</td>\n<td><h6>ARM Windows RT</h6>\n<p>Runs as a 32-bit application</p>\n</td>\n</tr>\n<tr>\n<td><p>anycpu32bitpreferred</p>\n</td>\n<td><p>PE32/agnostic</p>\n</td>\n<td><p>Runs as a 32-bit application</p>\n</td>\n<td><p>Runs as a 32-bit application</p>\n</td>\n<td><p>Runs as a 32-bit application</p>\n</td>\n</tr>\n<tr>\n<td><p>x86</p>\n</td>\n<td><p>PE32/x86</p>\n</td>\n<td><p>Runs as a 32-bit application</p>\n</td>\n<td><p>Runs as a WoW64 application</p>\n</td>\n<td><p>Doesn\u2019t run</p>\n</td>\n</tr>\n<tr>\n<td><p>x64</p>\n</td>\n<td><p>PE32+/x64</p>\n</td>\n<td><p>Doesn\u2019t run</p>\n</td>\n<td><p>Runs as a 64-bit application</p>\n</td>\n<td><p>Doesn\u2019t run</p>\n</td>\n</tr>\n<tr>\n<td><p>ARM</p>\n</td>\n<td><p>PE32/ARM</p>\n</td>\n<td><p>Doesn\u2019t run</p>\n</td>\n<td><p>Doesn\u2019t run</p>\n</td>\n<td><p>Runs as a 32-bit application</p>\n</td>\n</tr>\n</table><p>After Windows has examined the EXE file\u2019s header to determine whether to create a 32-bit or 64bit process, Windows loads the x86, x64, or ARM version of MSCorEE.dll into the process\u2019s address space. On an x86 or ARM version of Windows, the 32-bit version of MSCorEE.dll can be found in the %SystemRoot%\\System32 directory. On an x64 version of Windows, the x86 version of MSCorEE.dll can be found in the %SystemRoot%\\SysWow64 directory, whereas the 64-bit version can be found in the %SystemRoot%\\System32 directory (for backward compatibility reasons). Then, the process\u2019s primary thread calls a method defined inside MSCorEE.dll. This method initializes the CLR, loads the EXE assembly, and then calls its entry point method (Main). At this point, the managed application is up and running.1</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile4.png\"/><p>Note Assemblies built by using version 1.0 or 1.1 of Microsoft\u2019s C# compiler contain a PE32 header and are CPU-architecture agnostic. However, at load time, the CLR considers these assemblies to be x86 only. For executable files, this improves the likelihood of the application actually working on a 64-bit system because the executable file will load in WoW64, giving the process an environment very similar to what it would have on a 32-bit x86 version of Windows.</p><p>If an unmanaged application calls the Win32 LoadLibrary function to load a managed assembly, Windows knows to load and initialize the CLR (if not already loaded) in order to process the code contained within the assembly. Of course, in this scenario, the process is already up and running, and this may limit the usability of the assembly. For example, a managed assembly compiled with the /platform:x86 switch will not be able to load into a 64-bit process at all, whereas an executable file compiled with this same switch would have loaded in WoW64 on a computer running a 64-bit version of Windows.</p><p>1 Your code can query Environment\u2019s Is64BitOperatingSystem property to determine if it is running on a 64-bit version of Windows. Your code can also query Environment\u2019s Is64BitProcess property to determine if it is running in a 64-bit address space.</p><h6>Executing Your Assembly\u2019s Code</h6><p>As mentioned earlier, managed assemblies contain both metadata and IL. IL is a CPU-independent machine language created by Microsoft after consultation with several external commercial and academic language/compiler writers. IL is a much higher-level language than most CPU machine languages. IL can access and manipulate object types and has instructions to create and initialize objects, call virtual methods on objects, and manipulate array elements directly. It even has instructions to throw and catch exceptions for error handling. You can think of IL as an object-oriented machine language.</p><p>Usually, developers will program in a high-level language, such as C#, Visual Basic, or F#. The compilers for these high-level languages produce IL. However, as any other machine language, IL can be written in assembly language, and Microsoft does provide an IL Assembler, ILAsm.exe. Microsoft also provides an IL Disassembler, ILDasm.exe.</p><p>Keep in mind that any high-level language will most likely expose only a subset of the facilities offered by the CLR. However, the IL assembly language allows a developer to access all of the CLR\u2019s facilities. So, should your programming language of choice hide a facility the CLR offers that you really want to take advantage of, you can choose to write that portion of your code in IL assembly or perhaps another programming language that exposes the CLR feature you seek.</p><p>The only way for you to know what facilities the CLR offers is to read documentation specific to the CLR itself. In this book, I try to concentrate on CLR features and how they are exposed or not exposed by the C# language. I suspect that most other books and articles will present the CLR via a language perspective, and that most developers will come to believe that the CLR offers only what the developer\u2019s chosen language exposes. As long as your language allows you to accomplish what you\u2019re trying to get done, this blurred perspective isn\u2019t a bad thing.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile5.png\"/><p>Important I think this ability to switch programming languages easily with rich integration between languages is an awesome feature of the CLR. Unfortunately, I also believe that developers will often overlook this feature. Programming languages such as C# and Visual Basic are excellent languages for performing I/O operations. APL is a great language for performing advanced engineering or financial calculations. Through the CLR, you can write the I/O portions of your application in C# and then write the engineering calculations part in APL. The CLR offers a level of integration between these languages that is unprecedented and really makes mixed-language programming worthy of consideration for many development projects.</p><p>To execute a method, its IL must first be converted to native CPU instructions. This is the job of the</p><p>CLR\u2019s JIT (just-in-time) compiler. Figure 1-4 shows what happens the first time a method is called. Just before the Main method executes, the CLR detects all of the types that are referenced by Main\u2019s code. This causes the CLR to allocate an internal data structure that is used to manage access</p><p>to the referenced types. In Figure 1-4, the Main method refers to a single type, Console, causing the CLR to allocate a single internal structure. This internal data structure contains an entry for each method defined by the Console type. Each entry holds the address where the method\u2019s implementation can be found. When initializing this structure, the CLR sets each entry to an internal, undocumented function contained inside the CLR itself. I call this function JITCompiler.</p><p>When Main makes its first call to WriteLine, the JITCompiler function is called. The JIT\u00ad</p><p>Compiler function is responsible for compiling a method\u2019s IL code into native CPU instructions. Because the IL is being compiled \u201cjust in time,\u201d this component of the CLR is frequently referred to as a JITter or a JIT compiler.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile6.png\"/><p>Note If the application is running on an x86 version of Windows or in WoW64, the JIT compiler produces x86 instructions. If your application is running as a 64-bit application on an x64 version of Windows, the JIT compiler produces x64 instructions. If the application is running on an ARM version of Windows, the JIT compiler produces ARM instructions.</p><table border=\"1\">\n<tr>\n<td><p>Console</p>\n<p>JITCompiler</p>\n<p>static void WriteLine()</p>\n<p>(remaining members) ...</p>\n<p>JITCompiler</p>\n<p>static void WriteLine(string)</p>\n</td>\n</tr>\n</table><table border=\"1\">\n<tr>\n<td><p>static void Main() { Console.WriteLine(\u201cHello\u201d); Console.WriteLine(\u201cGoodbye\u201d);</p>\n<p>}</p>\n<p>Managed EXE</p>\n</td>\n</tr>\n</table><table border=\"1\">\n<tr>\n<td></td>\n<td></td>\n</tr>\n<tr>\n<td colspan=\"2\"><p>Native CPU instructions</p>\n</td>\n</tr>\n</table><table border=\"1\">\n<tr>\n<td><p>JITCompiler function {</p>\n<p>}</p>\n<p>MSCorEE.dll</p>\n<ul style=\"list-style-type: none;\">\n<li><p>1. In the assembly that implements the type (Console), look up the method (WriteLine) being called in the metadata.</p></li>\n<li><p>2. From the metadata, get the IL for this method.</p></li>\n<li><p>3. Allocate a block of memory.</p></li>\n<li><p>4. Compile the IL into native CPU instructions; the native code is saved in the memory allocated in step 3.</p></li>\n<li><p>5. Modify the method\u2019s entry in the Type\u2019s table so that it now points to the memory block allocated in step 3.</p></li>\n<li><p>6. Jump to the native code contained inside the memory block.</p></li>\n</ul>\n</td>\n</tr>\n</table><p>FIGURE 1-4 Calling a method for the first time.</p><p>When called, the JITCompiler function knows what method is being called and what type defines this method. The JITCompiler function then searches the defining assembly\u2019s metadata for the called method\u2019s IL. JITCompiler next verifies and compiles the IL code into native CPU instructions. The native CPU instructions are saved in a dynamically allocated block of memory. Then, JITCompiler goes back to the entry for the called method in the type\u2019s internal data structure created by the CLR and replaces the reference that called it in the first place with the address of the block of memory containing the native CPU instructions it just compiled. Finally, the JITCompiler function jumps to the code in the memory block. This code is the implementation of the WriteLine method (the version that takes a String parameter). When this code returns, it returns to the code in Main, which continues execution as normal.</p><p>Main now calls WriteLine a second time. This time, the code for WriteLine has already been verified and compiled. So the call goes directly to the block of memory, skipping the JITCompiler function entirely. After the WriteLine method executes, it returns to Main. Figure 1-5 shows what the process looks like when WriteLine is called the second time.</p><p>Console</p><p>Managed EXE static void WriteLine()</p><p>JITCompiler</p><p>static void Main() { Console.WriteLine(\u201cHello\u201d); Console.WriteLine(\u201cGoodbye\u201d);</p><p>static void WriteLine(string)</p><p>}</p><p>Native</p><p>Native CPU instructions</p><p>(remaining members) ...</p><p>MSCorEE.dll</p><p>JITCompiler function {</p><ul style=\"list-style-type: none;\">\n<li><p>1. In the assembly that implements the type (Console), look up the method iteLine) being called in the metadata.</p></li>\n<li><p>2. From the metadata, get the IL for this method.</p></li>\n<li><p>3. Allocate a block memory.</p></li>\n<li><p>4. Compile the IL into native CPU instructions; the native code is saved in the memory</p><p>allocated in step 3.</p>\n</li>\n<li><p>5. Modify the method\u2019 s table so that it now points to the memory block allocated in step 3.</p></li>\n<li><p>6. Jump to the native code contained inside the memory block.</p></li>\n</ul><p>bly that implements t</p><p>ook up the method (WriteLin alled in the</p><p>the metadata, get the IL for this me ate a block of mem pile the IL into native CPU instruction</p><p>ative code is saved in the memory ated in</p><p>fy the method\u2019s entry in the Type\u2019s t now points to the memory block</p><p>e native code contained in k</p><p>}</p><p>FIGURE 1-5 Calling a method for the second time.</p><p>A performance hit is incurred only the first time a method is called. All subsequent calls to the method execute at the full speed of the native code because verification and compilation to native code don\u2019t need to be performed again.</p><p>The JIT compiler stores the native CPU instructions in dynamic memory. This means that the compiled code is discarded when the application terminates. So if you run the application again in the future or if you run two instances of the application simultaneously (in two different operating system processes), the JIT compiler will have to compile the IL to native instructions again. Depending on the application, this can increase memory consumption significantly compared to a native application whose read-only code pages can be shared by all instances of the running application.</p><p>For most applications, the performance hit incurred by JIT compiling isn\u2019t significant. Most applications tend to call the same methods over and over again. These methods will take the performance hit only once while the application executes. It\u2019s also likely that more time is spent inside the method than calling the method.</p><p>You should also be aware that the CLR\u2019s JIT compiler optimizes the native code just as the back end of an unmanaged C++ compiler does. Again, it may take more time to produce the optimized code, but the code will execute with much better performance than if it hadn\u2019t been optimized.</p><figcaption>There are two C# compiler switches that impact code optimization: /optimize and /debug. The following table shows the impact these switches have on the quality of the IL code generated by the C# compiler and the quality of the native code generated by the JIT compiler.</figcaption><table border=\"1\">\n<tr>\n<td><p>Compiler Switch Settings</p>\n<p>/optimize\u00ad /debug\u00ad</p>\n<p>(this is the default)</p>\n</td>\n<td><p>C# IL Code Quality Unoptimized</p>\n</td>\n<td><p>JIT Native Code Quality Optimized</p>\n</td>\n</tr>\n<tr>\n<td><p>/optimize\u00ad /debug(+/full/pdbonly)</p>\n</td>\n<td><p>Unoptimized</p>\n</td>\n<td><p>Unoptimized</p>\n</td>\n</tr>\n<tr>\n<td><p>/optimize+ /debug(\u00ad/+/full/pdbonly)</p>\n</td>\n<td><p>Optimized</p>\n</td>\n<td><p>Optimized</p>\n</td>\n</tr>\n</table><p>With /optimize\u00ad, the unoptimized IL code produced by the C# compiler contains many nooperation (NOP) instructions and also branches that jump to the next line of code. These instructions are emitted to enable the edit-and-continue feature of Visual Studio while debugging and the extra instructions also make code easier to debug by allowing breakpoints to be set on control flow instructions such as for, while, do, if, else, try, catch, and finally statement blocks. When producing optimized IL code, the C# compiler will remove these extraneous NOP and branch instructions, making the code harder to single-step through in a debugger because control flow will be optimized. Also, some function evaluations may not work when performed inside the debugger. However, the IL code is smaller, making the resulting EXE/DLL file smaller, and the IL tends to be easier to read for those of you (like me) who enjoy examining the IL to understand what the compiler is producing.</p><p>Furthermore, the compiler produces a Program Database (PDB) file only if you specify the /debug(+/full/pdbonly) switch. The PDB file helps the debugger find local variables and map the IL instructions to source code. The /debug:full switch tells the JIT compiler that you intend to debug the assembly, and the JIT compiler will track what native code came from each IL instruction. This allows you to use the just-in-time debugger feature of Visual Studio to connect a debugger to an already-running process and debug the code easily. Without the /debug:full switch, the JIT compiler does not, by default, track the IL to native code information, which makes the JIT compiler run a little faster and also uses a little less memory. If you start a process with the Visual Studio debugger, it forces the JIT compiler to track the IL to native code information (regardless of the /debug switch) unless you turn off the Suppress JIT Optimization On Module Load (Managed Only) option in Visual Studio.</p><p>When you create a new C# project in Visual Studio, the Debug configuration of the project has /optimize\u00ad and /debug:full switches, and the Release configuration has /optimize+ and /debug:pdbonly switches specified.</p><p>For those developers coming from an unmanaged C or C++ background, you\u2019re probably thinking about the performance ramifications of all this. After all, unmanaged code is compiled for a specific CPU platform, and, when invoked, the code can simply execute. In this managed environment, compiling the code is accomplished in two phases. First, the compiler passes over the source code, doing as much work as possible in producing IL. But to execute the code, the IL itself must be compiled into native CPU instructions at run time, requiring more non-shareable memory to be allocated and requiring additional CPU time to do the work.</p><p>Believe me, because I approached the CLR from a C/C++ background myself, I was quite skeptical and concerned about this additional overhead. The truth is that this second compilation stage that occurs at run time does hurt performance, and it does allocate dynamic memory. However, Microsoft has done a lot of performance work to keep this additional overhead to a minimum.</p><p>If you too are skeptical, you should certainly build some applications and test the performance for yourself. In addition, you should run some nontrivial managed applications Microsoft or others have produced, and measure their performance. I think you\u2019ll be surprised at how good the performance actually is.</p><p>You\u2019ll probably find this hard to believe, but many people (including me) think that managed applications could actually outperform unmanaged applications. There are many reasons to believe this. For example, when the JIT compiler compiles the IL code into native code at run time, the compiler knows more about the execution environment than an unmanaged compiler would know. Here are some ways that managed code can outperform unmanaged code:</p><p>\u25a0\u25a0 A JIT compiler can determine if the application is running on an Intel Pentium 4 CPU and produce native code that takes advantage of any special instructions offered by the Pentium 4. Usually, unmanaged applications are compiled for the lowest-common-denominator CPU and avoid using special instructions that would give the application a performance boost.</p><p>\u25a0\u25a0 A JIT compiler can determine when a certain test is always false on the machine that it is run-</p><p>ning on. For example, consider a method that contains the following code.</p><p>if (numberOfCPUs &gt; 1) {</p><p>... }</p><p>This code could cause the JIT compiler to not generate any CPU instructions if the host machine has only one CPU. In this case, the native code would be fine-tuned for the host machine; the resulting code is smaller and executes faster.</p><p>\u25a0\u25a0 The CLR could profile the code\u2019s execution and recompile the IL into native code while the application runs. The recompiled code could be reorganized to reduce incorrect branch predictions depending on the observed execution patterns. Current versions of the CLR do not do this, but future versions might.</p><p>These are only a few of the reasons why you should expect future managed code to execute better than today\u2019s unmanaged code. As I said, the performance is currently quite good for most applications, and it promises to improve as time goes on.</p><p>If your experiments show that the CLR\u2019s JIT compiler doesn\u2019t offer your application the kind of performance it requires, you may want to take advantage of the NGen.exe tool that ships with the</p><p>.NET Framework SDK. This tool compiles all of an assembly\u2019s IL code into native code and saves the resulting native code to a file on disk. At run time, when an assembly is loaded, the CLR automatically checks to see whether a precompiled version of the assembly also exists, and if it does, the CLR loads the precompiled code so that no compilation is required at run time. Note that NGen.exe must be conservative about the assumptions it makes regarding the actual execution environment, and for this reason, the code produced by NGen.exe will not be as highly optimized as the JIT compiler\u2013produced code. I\u2019ll discuss NGen.exe in more detail later in this chapter.</p><p>In addition, you may want to consider using the System.Runtime.ProfileOptimization class. This class causes the CLR to record (to a file) what methods get JIT compiled while your application is running. Then, on a future startup of your application, the JIT compiler will concurrently compile these methods by using other threads if your application is running on a machine with multiple CPUs. The end result is that your application runs faster because multiple methods get compiled concurrently, and during application initialization instead of compiling the methods just in time as the user is interacting with your application.</p><h6>IL and Verification</h6><p>IL is stack-based, which means that all of its instructions push operands onto an execution stack and pop results off the stack. Because IL offers no instructions to manipulate registers, it is easy for people to create new languages and compilers that produce code targeting the CLR.</p><p>IL instructions are also typeless. For example, IL offers an add instruction that adds the last two operands pushed on the stack. There are no separate 32-bit and 64-bit versions of the add instruction. When the add instruction executes, it determines the types of the operands on the stack and performs the appropriate operation.</p><p>In my opinion, the biggest benefit of IL isn\u2019t that it abstracts away the underlying CPU. The biggest benefit IL provides is application robustness and security. While compiling IL into native CPU instructions, the CLR performs a process called verification. Verification examines the high-level IL code and ensures that everything the code does is safe. For example, verification checks that every method is called with the correct number of parameters, that each parameter passed to every method is of the correct type, that every method\u2019s return value is used properly, that every method has a return statement, and so on. The managed module\u2019s metadata includes all of the method and type information used by the verification process.</p><p>In Windows, each process has its own virtual address space. Separate address spaces are necessary because you can\u2019t trust an application\u2019s code. It is entirely possible (and unfortunately, all too common) that an application will read from or write to an invalid memory address. By placing each Windows process in a separate address space, you gain robustness and stability; one process can\u2019t adversely affect another process.</p><p>By verifying the managed code, however, you know that the code doesn\u2019t improperly access memory and can\u2019t adversely affect another application\u2019s code. This means that you can run multiple managed applications in a single Windows virtual address space.</p><p>Because Windows processes require a lot of operating system resources, having many of them can hurt performance and limit available resources. Reducing the number of processes by running multiple applications in a single operating system process can improve performance, require fewer resources, and be just as robust as if each application had its own process. This is another benefit of managed code as compared to unmanaged code.</p><p>The CLR does, in fact, offer the ability to execute multiple managed applications in a single operating system process. Each managed application executes in an AppDomain. By default, every managed EXE file will run in its own separate address space that has just one AppDomain. However, a process hosting the CLR (such as Internet Information Services [IIS] or Microsoft SQL Server) can decide to run AppDomains in a single operating system process. I\u2019ll devote part of Chapter 22, \u201cCLR Hosting and AppDomains,\u201d to a discussion of AppDomains.</p><h6>Unsafe Code</h6><p>By default, Microsoft\u2019s C# compiler produces safe code. Safe code is code that is verifiably safe. However, Microsoft\u2019s C# compiler allows developers to write unsafe code. Unsafe code is allowed to work directly with memory addresses and can manipulate bytes at these addresses. This is a very powerful feature and is typically useful when interoperating with unmanaged code or when you want to improve the performance of a time-critical algorithm.</p><p>However, using unsafe code introduces a significant risk: unsafe code can corrupt data structures and exploit or even open up security vulnerabilities. For this reason, the C# compiler requires that all methods that contain unsafe code be marked with the unsafe keyword. In addition, the C# compiler requires you to compile the source code by using the /unsafe compiler switch.</p><p>When the JIT compiler attempts to compile an unsafe method, it checks to see if the assembly containing the method has been granted the System.Security.Permissions.Security Permission with the System.Security.Permissions.SecurityPermissionFlag\u2019s SkipVerification flag set. If this flag is set, the JIT compiler will compile the unsafe code and allow it to execute. The CLR is trusting this code and is hoping the direct address and byte manipulations do not cause any harm. If the flag is not set, the JIT compiler throws either a System.InvalidProgramException or a System.Security.VerificationException, preventing the method from executing. In fact, the whole application will probably terminate at this point, but at least no harm can be done.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile7.png\"/><p>Note By default, assemblies that load from the local machine or via network shares are granted full trust, meaning that they can do anything, which includes executing unsafe code. However, by default, assemblies executed via the Internet are not granted the permission to execute unsafe code. If they contain unsafe code, one of the aforementioned exceptions is thrown. An administrator/end user can change these defaults; however, the administrator is taking full responsibility for the code\u2019s behavior.</p><p>Microsoft supplies a utility called PEVerify.exe, which examines all of an assembly\u2019s methods and notifies you of any methods that contain unsafe code. You may want to consider running PEVerify.exe on assemblies that you are referencing; this will let you know if there may be problems running your application via the intranet or Internet.</p><p>You should be aware that verification requires access to the metadata contained in any dependent assemblies. So when you use PEVerify to check an assembly, it must be able to locate and load all referenced assemblies. Because PEVerify uses the CLR to locate the dependent assemblies, the assemblies are located using the same binding and probing rules that would normally be used when executing the assembly. I\u2019ll discuss these binding and probing rules in Chapter 2 and Chapter 3, \u201cShared Assemblies and Strongly Named Assemblies.\u201d</p><h6>IL and Protecting Your Intellectual Property</h6><p>Some people are concerned that IL doesn\u2019t offer enough intellectual property protection for their algorithms. In other words, they think that you could build a managed module and that someone else could use a tool, such as an IL Disassembler, to easily reverse engineer exactly what your application\u2019s code does.</p><p>Yes, it\u2019s true that IL code is higher-level than most other assembly languages, and, in general, reverse engineering IL code is relatively simple. However, when implementing server-side code (such as a web service, web form, or stored procedure), your assembly resides on your server. Because no one outside of your company can access the assembly, no one outside of your company can use any tool to see the IL\u2014your intellectual property is completely safe.</p><p>If you\u2019re concerned about any of the assemblies you do distribute, you can obtain an obfuscator utility from a third-party vendor. These utilities scramble the names of all of the private symbols in your assembly\u2019s metadata. It will be difficult for someone to unscramble the names and understand the purpose of each method. Note that these obfuscators can provide only a little protection because the IL must be available at some point for the CLR to JIT compile it.</p><p>If you don\u2019t feel that an obfuscator offers the kind of intellectual property protection you desire, you can consider implementing your more sensitive algorithms in some unmanaged module that will contain native CPU instructions instead of IL and metadata. Then you can use the CLR\u2019s interoperability features (assuming that you have ample permissions) to communicate between the managed and unmanaged portions of your application. Of course, this assumes that you\u2019re not worried about people reverse engineering the native CPU instructions in your unmanaged code.</p><h6>The Native Code Generator Tool: NGen.exe</h6><p>The NGen.exe tool that ships with the .NET Framework can be used to compile IL code to native code when an application is installed on a user\u2019s machine. Because the code is compiled at install time, the CLR\u2019s JIT compiler does not have to compile the IL code at run time, and this can improve the application\u2019s performance. The NGen.exe tool is interesting in two scenarios:</p><p>\u25a0\u25a0 Improving an application\u2019s startup time Running NGen.exe can improve startup time because the code will already be compiled into native code so that compilation doesn\u2019t have to occur at run time.</p><p>\u25a0\u25a0 Reducing an application\u2019s working set If you believe that an assembly will be loaded into multiple processes simultaneously, running NGen.exe on that assembly can reduce the applications\u2019 working set. The reason is because the NGen.exe tool compiles the IL to native code and saves the output in a separate file. This file can be memory-mapped into multiple-process address spaces simultaneously, allowing the code to be shared; not every process needs its own copy of the code.</p><p>When a setup program invokes NGen.exe on an application or a single assembly, all of the assemblies for that application or the one specified assembly have their IL code compiled into native code. A new assembly file containing only this native code instead of IL code is created by NGen.exe. This new file is placed in a folder under the directory with a name like %SystemRoot%\\Assembly\\ NativeImages_v4.0.#####_64. The directory name includes the version of the CLR and information denoting whether the native code is compiled for 32-bit or 64-bit versions of Windows.</p><p>Now, whenever the CLR loads an assembly file, the CLR looks to see if a corresponding NGen\u2019d native file exists. If a native file cannot be found, the CLR JIT compiles the IL code as usual. However, if a corresponding native file does exist, the CLR will use the compiled code contained in the native file, and the file\u2019s methods will not have to be compiled at run time.</p><p>On the surface, this sounds great! It sounds as if you get all of the benefits of managed code (garbage collection, verification, type safety, and so on) without all of the performance problems of managed code (JIT compilation). However, the reality of the situation is not as rosy as it would first seem. There are several potential problems with respect to NGen\u2019d files:</p><p>\u25a0\u25a0 No intellectual property protection Many people believe that it might be possible to ship NGen\u2019d files without shipping the files containing the original IL code, thereby keeping their intellectual property a secret. Unfortunately, this is not possible. At run time, the CLR requires access to the assembly\u2019s metadata (for functions such as reflection and serialization); this requires that the assemblies that contain IL and metadata be shipped. In addition, if the CLR can\u2019t use the NGen\u2019d file for some reason (described next), the CLR gracefully goes back to JIT compiling the assembly\u2019s IL code, which must be available.</p><p>\u25a0\u25a0 NGen\u2019d files can get out of sync When the CLR loads an NGen\u2019d file, it compares a number of characteristics about the previously compiled code and the current execution environment. If any of the characteristics don\u2019t match, the NGen\u2019d file cannot be used, and the normal JIT compiler process is used instead. Here is a partial list of characteristics that must match:</p><ul style=\"list-style-type: none;\">\n<li><p>\u2022 CLR version: This changes with patches or service packs.</p></li>\n<li><p>\u2022 CPU type: This changes if you upgrade your processor hardware.</p></li>\n<li><p>\u2022 Windows operating system version: This changes with a new service pack update.</p></li>\n<li><p>\u2022 Assembly\u2019s identity module version ID (MVID): This changes when recompiling.</p></li>\n<li><p>\u2022 Referenced assembly\u2019s version IDs: This changes when you recompile a referenced</p><p>assembly.</p>\n</li>\n<li><p>\u2022 Security: This changes when you revoke permissions (such as declarative inheritance, de-</p></li>\n</ul><p>clarative link-time, SkipVerification, or UnmanagedCode permissions), that were once granted.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile8.png\"/><p>Note It is possible to run NGen.exe in update mode. This tells the tool to run NGen.exe on all of the assemblies that had previously been NGen\u2019d. Whenever an end user installs a new service pack of the .NET Framework, the service pack\u2019s installation program will run NGen.exe in update mode automatically so that NGen\u2019d files are kept in sync with the version of the CLR installed.</p><p>\u25a0\u25a0 Inferior execution-time performance When compiling code, NGen can\u2019t make as many assumptions about the execution environment as the JIT compiler can. This causes NGen.exe to produce inferior code. For example, NGen won\u2019t optimize the use of certain CPU instructions; it adds indirections for static field access because the actual address of the static fields isn\u2019t known until run time. NGen inserts code to call class constructors everywhere because it doesn\u2019t know the order in which the code will execute and if a class constructor has already been called. (See Chapter 8, \u201cMethods,\u201d for more about class constructors.) Some NGen\u2019d applications actually perform about 5 percent slower when compared to their JIT-compiled counterpart. So, if you\u2019re considering using NGen.exe to improve the performance of your application, you should compare NGen\u2019d and non-NGen\u2019d versions to be sure that the NGen\u2019d version doesn\u2019t actually run slower! For some applications, the reduction in working set size improves performance, so using NGen can be a net win.</p><p>Due to all of the issues just listed, you should be very cautious when considering the use of NGen.exe. For server-side applications, NGen.exe makes little or no sense because only the first client request experiences a performance hit; future client requests run at high speed. In addition, for most server applications, only one instance of the code is required, so there is no working set benefit.</p><p>For client applications, NGen.exe might make sense to improve startup time or to reduce working set if an assembly is used by multiple applications simultaneously. Even in a case in which an assembly is not used by multiple applications, NGen\u2019ing an assembly could improve working set. Moreover, if NGen.exe is used for all of a client application\u2019s assemblies, the CLR will not need to load the JIT compiler at all, reducing working set even further. Of course, if just one assembly isn\u2019t NGen\u2019d or if an assembly\u2019s NGen\u2019d file can\u2019t be used, the JIT compiler will load, and the application\u2019s working set increases.</p><p>For large client applications that experience very long startup times, Microsoft provides a Managed Profile Guided Optimization tool (MPGO.exe). This tool analyzes the execution of your application to see what it needs at startup. This information is then fed to the NGen.exe tool in order to better optimize the resulting native image. This allows your application to start faster and with a reduced working set. When you\u2019re ready to ship your application, launch it via the MPGO tool and then exercise your application\u2019s common tasks. Information about the parts of your code that executed is written to a profile, which is embedded within your assembly file. The NGen.exe tool uses this profile data to better optimize the native image it produces.</p><h6>The Framework Class Library</h6><p>The .NET Framework includes the Framework Class Library (FCL). The FCL is a set of DLL assemblies that contain several thousand type definitions in which each type exposes some functionality. Microsoft is producing additional libraries such as the Windows Azure SDK and the DirectX SDK. These additional libraries provide even more types, exposing even more functionality for your use. In fact, Microsoft is producing many libraries at a phenomenal rate, making it easier than ever for developers to use various Microsoft technologies.</p><p>Here are just some of the kinds of applications developers can create by using these assemblies: \u25a0\u25a0 Web services Methods that can process messages sent over the Internet very easily using</p><p>Microsoft\u2019s ASP.NET XML Web Service technology or Microsoft\u2019s Windows Communication Foundation (WCF) technology.</p><p>\u25a0\u25a0 Web Forms/MVC HTML-based applications (websites) Typically, ASP.NET applications will make database queries and web service calls, combine and filter the returned information, and then present that information in a browser by using a rich HTML-based user interface.</p><p>\u25a0\u25a0 Rich Windows GUI applications Instead of using a webpage to create your application\u2019s UI, you can use the more powerful, higher-performance functionality offered by Windows Store, Windows Presentation Foundation (WPF), or Windows Forms technologies. GUI applications can take advantage of controls, menus, and touch, mouse, stylus, and keyboard events, and they can exchange information directly with the underlying operating system. Rich Windows applications can also make database queries and consume web services.</p><p>\u25a0\u25a0 Windows console applications For applications with very simple UI demands, a console application provides a quick and easy way to build an application. Compilers, utilities, and tools are typically implemented as console applications.</p><p>\u25a0\u25a0 Windows services Yes, it is possible to build service applications that are controllable via the</p><p>Windows Service Control Manager (SCM) by using the .NET Framework.</p><p>\u25a0\u25a0 Database stored procedures Microsoft\u2019s SQL Server, IBM\u2019s DB2, and Oracle\u2019s database</p><p>servers allow developers to write their stored procedures by using the .NET Framework.</p><p>\u25a0\u25a0 Component library The .NET Framework allows you to build stand-alone assemblies (components) containing types that can be easily incorporated into any of the previously mentioned application types.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile9.png\"/><p>Important Visual Studio allows you to create a Portable Class Library project. This project type lets you create a single class library assembly that works with various application types, including the .NET Framework proper, Silverlight, Windows Phone, Windows Store apps, and Xbox 360.</p><p>Because the FCL contains literally thousands of types, a set of related types is presented to the developer within a single namespace. For example, the System namespace (which you should become</p><p>most familiar with) contains the Object base type, from which all other types ultimately derive. In addition, the System namespace contains types for integers, characters, strings, exception handling, and console I/O as well as a bunch of utility types that convert safely between data types, format data types, generate random numbers, and perform various math functions. All applications will use types from the System namespace.</p><p>To access any of the framework\u2019s features, you need to know which namespace contains the types that expose the facilities you\u2019re after. A lot of types allow you to customize their behavior; you do so by simply deriving your own type from the desired FCL type. The object-oriented nature of the platform is how the .NET Framework presents a consistent programming paradigm to software developers. Also, developers can easily create their own namespaces containing their own types. These namespaces and types merge seamlessly into the programming paradigm. Compared to Win32 programming paradigms, this new approach greatly simplifies software development.</p><p>Most of the namespaces in the FCL present types that can be used for any kind of application. Table 1-3 lists some of the more general namespaces and briefly describes what the types in that namespace are used for. This is a very small sampling of the namespaces available. Please see the documentation that accompanies the various Microsoft SDKs to gain familiarity with the ever-growing set of namespaces that Microsoft is producing.</p><figcaption>TABLE 1-3 Some General FCL Namespaces</figcaption><table border=\"1\">\n<tr>\n<td><p>Namespace</p>\n<p>System</p>\n</td>\n<td><p>Description of Contents All of the basic types used by every application</p>\n</td>\n</tr>\n<tr>\n<td><p>System.Data</p>\n</td>\n<td><p>Types for communicating with a database and processing data</p>\n</td>\n</tr>\n<tr>\n<td><p>System.IO</p>\n</td>\n<td><p>Types for doing stream I/O and walking directories and files</p>\n</td>\n</tr>\n<tr>\n<td><p>System.Net</p>\n</td>\n<td><p>Types that allow for low-level network communications and working with some common Internet protocols</p>\n</td>\n</tr>\n<tr>\n<td><p>System.Runtime. InteropServices</p>\n</td>\n<td><p>Types that allow managed code to access unmanaged operating system platform facilities such as COM components and functions in Win32 or custom DLLs</p>\n</td>\n</tr>\n<tr>\n<td><p>System.Security</p>\n</td>\n<td><p>Types used for protecting data and resources</p>\n</td>\n</tr>\n<tr>\n<td><p>System.Text</p>\n</td>\n<td><p>Types to work with text in different encodings, such as ASCII and Unicode</p>\n</td>\n</tr>\n<tr>\n<td><p>System.Threading</p>\n</td>\n<td><p>Types used for asynchronous operations and synchronizing access to resources</p>\n</td>\n</tr>\n<tr>\n<td><p>System.Xml</p>\n</td>\n<td><p>Types used for processing Extensible Markup Language (XML) schemas and data</p>\n</td>\n</tr>\n</table><p>This book is about the CLR and about the general types that interact closely with the CLR. So the content of this book is applicable to all programmers writing applications or components that target the CLR. Many other good books exist that cover specific application types such as Web Services, Web Forms/MVC, Windows Presentation Foundation, etc. These other books will give you an excellent start at helping you build your application. I tend to think of these application-specific books as helping you learn from the top down because they concentrate on the application type and not on the development platform. In this book, I\u2019ll offer information that will help you learn from the bottom up. After reading this book and an application-specific book, you should be able to easily and proficiently build any kind of application you desire.</p><h6>The Common Type System</h6><p>By now, it should be obvious to you that the CLR is all about types. Types expose functionality to your applications and other types. Types are the mechanism by which code written in one programming language can talk to code written in a different programming language. Because types are at the root of the CLR, Microsoft created a formal specification\u2014the Common Type System (CTS)\u2014that describes how types are defined and how they behave.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile10.png\"/><p>Note In fact, Microsoft has been submitting the CTS as well as other parts of the .NET Framework, including file formats, metadata, IL, and access to the underlying platform (P/Invoke) to ECMA for the purpose of standardization. The standard is called the Common Language Infrastructure (CLI) and is the ECMA-335 specification. In addition, Microsoft has also submitted portions of the FCL, the C# programming language (ECMA-334), and the C++/CLI programming language. For information about these industry standards, go to the ECMA website that pertains to Technical Committee 39: http://www.ecma-international.org. You can also refer to Microsoft\u2019s own website:</p><p>http://msdn.microsoft.com/en-us/netframework/aa569283.aspx. In addition, Microsoft has applied their Community Promise to the ECMA-334 and ECMA-335 specifications. For more information about this, see http://www.microsoft.com/openspecifications/en</p><p>/us/programs/community-promise/default.aspx.</p><p>The CTS specification states that a type can contain zero or more members. In Part II, \u201cDesigning Types,\u201d I\u2019ll cover all of these members in great detail. For now, I just want to give you a brief introduction to them:</p><p>\u25a0\u25a0 Field A data variable that is part of the object\u2019s state. Fields are identified by their name</p><p>and type.</p><p>\u25a0\u25a0 Method A function that performs an operation on the object, often changing the object\u2019s state. Methods have a name, a signature, and modifiers. The signature specifies the number of parameters (and their sequence), the types of the parameters, whether a value is returned by the method, and if so, the type of the value returned by the method.</p><p>\u25a0\u25a0 Property To the caller, this member looks like a field. But to the type implementer, it looks like a method (or two). Properties allow an implementer to validate input parameters and object state before accessing the value and/or calculating a value only when necessary. They also allow a user of the type to have simplified syntax. Finally, properties allow you to create read-only or write-only \u201cfields.\"</p><p>\u25a0\u25a0 Event An event allows a notification mechanism between an object and other interested objects. For example, a button could offer an event that notifies other objects when the button is clicked.</p><p>The CTS also specifies the rules for type visibility and access to the members of a type. For example, marking a type as public (called public) exports the type, making it visible and accessible to any assembly. On the other hand, marking a type as assembly (called internal in C#) makes the type visible and accessible to code within the same assembly only. Thus, the CTS establishes the rules by which assemblies form a boundary of visibility for a type, and the CLR enforces the visibility rules.</p><p>A type that is visible to a caller can further restrict the ability of the caller to access the type\u2019s members. The following list shows the valid options for controlling access to a member:</p><p>\u25a0\u25a0 Private The member is accessible only by other members in the same class type.</p><p>\u25a0\u25a0 Family The member is accessible by derived types, regardless of whether they are within the same assembly. Note that many languages (such as C++ and C#) refer to family as protected.</p><p>\u25a0\u25a0 Family and assembly The member is accessible by derived types, but only if the derived type is defined in the same assembly. Many languages (such as C# and Visual Basic) don\u2019t offer this access control. Of course, IL Assembly language makes it available.</p><p>\u25a0\u25a0 Assembly The member is accessible by any code in the same assembly. Many languages</p><p>refer to assembly as internal.</p><p>\u25a0\u25a0 Family or assembly The member is accessible by derived types in any assembly. The member is also accessible by any types in the same assembly. C# refers to family or assembly as protected internal.</p><p>\u25a0\u25a0 Public The member is accessible by any code in any assembly. In addition, the CTS defines the rules governing type inheritance, virtual methods, object lifetime,</p><p>and so on. These rules have been designed to accommodate the semantics expressible in modernday programming languages. In fact, you won\u2019t even need to learn the CTS rules per se because the language you choose will expose its own language syntax and type rules in the same way that you\u2019re familiar with today. And it will map the language-specific syntax into IL, the \u201clanguage\u201d of the CLR, when it emits the assembly during compilation.</p><p>When I first started working with the CLR, I soon realized that it is best to think of the language and the behavior of your code as two separate and distinct things. Using C++/CLI, you can define your own types with their own members. Of course, you could have used C# or Visual Basic to define the same type with the same members. Sure, the syntax you use for defining the type is different depending on the language you choose, but the behavior of the type will be identical regardless of the language because the CLR\u2019s CTS defines the behavior of the type.</p><p>To help clarify this idea, let me give you an example. The CTS allows a type to derive from only one base class. So, although the C++ language supports types that can inherit from multiple base types, the CTS can\u2019t accept and operate on any such type. To help the developer, Microsoft\u2019s C++/CLI compiler reports an error if it detects that you\u2019re attempting to create managed code that includes a type deriving from multiple base types.</p><p>Here\u2019s another CTS rule. All types must (ultimately) inherit from a predefined type: System.Object. As you can see, Object is the name of a type defined in the System namespace. This Object is the root of all other types and therefore guarantees that every type instance has a minimum set of behaviors. Specifically, the System.Object type allows you to do the following:</p><p>\u25a0\u25a0 Compare two instances for equality. \u25a0\u25a0 Obtain a hash code for the instance. \u25a0\u25a0 Query the true type of an instance.</p><p>\u25a0\u25a0 Perform a shallow (bitwise) copy of the instance.</p><p>\u25a0\u25a0 Obtain a string representation of the instance object\u2019s current state.</p><h6>The Common Language Specification</h6><p>COM allows objects created in different languages to communicate with one another. On the other hand, the CLR now integrates all languages and allows objects created in one language to be treated as equal citizens by code written in a completely different language. This integration is possible because of the CLR\u2019s standard set of types, metadata (self-describing type information), and common execution environment.</p><p>Although this language integration is a fantastic goal, the truth of the matter is that programming</p><p>languages are very different from one another. For example, some languages don\u2019t treat symbols with case-sensitivity, and some don\u2019t offer unsigned integers, operator overloading, or methods to support a variable number of arguments.</p><p>If you intend to create types that are easily accessible from other programming languages, you need to use only features of your programming language that are guaranteed to be available in all other languages. To help you with this, Microsoft has defined a Common Language Specification (CLS) that details for compiler vendors the minimum set of features their compilers must support if these compilers are to generate types compatible with other components written by other CLS-compliant languages on top of the CLR.</p><p>The CLR/CTS supports a lot more features than the subset defined by the CLS, so if you don\u2019t care about interlanguage operability, you can develop very rich types limited only by the language\u2019s feature set. Specifically, the CLS defines rules that externally visible types and methods must adhere to if they are to be accessible from any CLS-compliant programming language. Note that the CLS rules don\u2019t apply to code that is accessible only within the defining assembly. Figure 1-6 summarizes the ideas expressed in this paragraph.</p><p>As Figure 1-6 shows, the CLR/CTS offers a set of features. Some languages expose a large subset of the CLR/CTS. A programmer willing to write in IL assembly language, for example, is able to use all of the features the CLR/CTS offers. Most other languages, such as C#, Visual Basic, and Fortran, expose a subset of the CLR/CTS features to the programmer. The CLS defines the minimum set of features that all languages must support.</p><p>CLR/CTS</p><p>Visual C# Basic</p><p>CLS</p><p>Fortran</p><p>FIGURE 1-6 Languages offer a subset of the CLR/CTS and a superset of the CLS (but not necessarily the same superset).</p><p>If you\u2019re designing a type in one language, and you expect that type to be used by another lan-</p><p>guage, you shouldn\u2019t take advantage of any features that are outside of the CLS in its public and protected members. Doing so would mean that your type\u2019s members might not be accessible by programmers writing code in other programming languages.</p><p>In the following code, a CLS-compliant type is being defined in C#. However, the type has a few non\u2013CLS-compliant constructs causing the C# compiler to complain about the code.</p><p>using System;</p><p>// Tell compiler to check for CLS compliance [assembly: CLSCompliant(true)]</p><p>namespace SomeLibrary { // Warnings appear because the class is public public sealed class SomeLibraryType {</p><p>// Warning: Return type of 'SomeLibrary.SomeLibraryType.Abc()' // is not CLS\u00adcompliant public UInt32 Abc() { return 0; }</p><p>// Warning: Identifier 'SomeLibrary.SomeLibraryType.abc()' // differing only in case is not CLS\u00adcompliant public void abc() { }</p><p>} }</p><p>// No warning: this method is private private UInt32 ABC() { return 0; }</p><p>In this code, the [assembly:CLSCompliant(true)] attribute is applied to the assembly. This attribute tells the compiler to ensure that any publicly exposed type doesn\u2019t have any construct that would prevent the type from being accessed from any other programming language. When this code is compiled, the C# compiler emits two warnings. The first warning is reported because the method Abc returns an unsigned integer; some other programming languages can\u2019t manipulate unsigned integer values. The second warning is because this type exposes two public methods that differ only by case and return type: Abc and abc. Visual Basic and some other languages can\u2019t call both of these methods.</p><p>Interestingly, if you were to delete public from in front of 'sealed class SomeLibraryType' and recompile, both warnings would go away. The reason is that the SomeLibraryType type would default to internal and would therefore no longer be exposed outside of the assembly. For a complete list of CLS rules, refer to the \u201cCross-Language Interoperability\u201d section in the .NET Framework SDK documentation (http://msdn.microsoft.com/en-us/library/730f1wy3.aspx).</p><p>Let me distill the CLS rules to something very simple. In the CLR, every member of a type is either a field (data) or a method (behavior). This means that every programming language must be able to access fields and call methods. Certain fields and certain methods are used in special and common ways. To ease programming, languages typically offer additional abstractions to make coding these common programming patterns easier. For example, languages expose concepts such as enums, arrays, properties, indexers, delegates, events, constructors, finalizers, operator overloads, conversion operators, and so on. When a compiler comes across any of these things in your source code, it must translate these constructs into fields and methods so that the CLR and any other programming language can access the construct.</p><p>Consider the following type definition, which contains a constructor, a finalizer, some overloaded operators, a property, an indexer, and an event. Note that the code shown is there just to make the code compile; it doesn\u2019t show the correct way to implement a type.</p><p>using System;</p><p>internal sealed class Test { // Constructor public Test() {}</p><p>// Finalizer ~Test() {}</p><p>// Operator overload public static Boolean operator == (Test t1, Test t2) {</p><p>return true;</p><p>} public static Boolean operator != (Test t1, Test t2) {</p><p>return false; }</p><p>// An operator overload public static Test operator + (Test t1, Test t2) { return null; }</p><p>// A property public String AProperty {</p><p>get { return null; } set { }</p><p>}</p><p>// An indexer public String this[Int32 x] {</p><p>get { return null; } set { }</p><p>}</p><p>}</p><p>// An event public event EventHandler AnEvent;</p><p>When the compiler compiles this code, the result is a type that has a number of fields and methods defined in it. You can easily see this by using the IL Disassembler tool (ILDasm.exe) provided with the</p><p>.NET Framework SDK to examine the resulting managed module, which is shown in Figure 1-7.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile11.png\"/><figcaption>FIGURE 1-7 ILDasm showing Test type\u2019s fields and methods (obtained from metadata).</figcaption><p>Table 1-4 shows how the programming language constructs got mapped to the equivalent CLR fields and methods.</p><figcaption>TABLE 1-4 Test Type\u2019s Fields and Methods (Obtained from Metadata)</figcaption><table border=\"1\">\n<tr>\n<td><p>Type Member</p>\n<p>AnEvent</p>\n</td>\n<td><h6>Member Type</h6>\n<p>Field</p>\n</td>\n<td><p>Equivalent Programming Language Construct</p>\n<p>Event; the name of the field is AnEvent and its type is System.EventHandler.</p>\n</td>\n</tr>\n<tr>\n<td><p>.ctor</p>\n</td>\n<td><p>Method</p>\n</td>\n<td><p>Constructor.</p>\n</td>\n</tr>\n<tr>\n<td><p>Finalize</p>\n</td>\n<td><p>Method</p>\n</td>\n<td><p>Finalizer.</p>\n</td>\n</tr>\n<tr>\n<td><p>add_AnEvent</p>\n</td>\n<td><p>Method</p>\n</td>\n<td><p>Event add accessor method.</p>\n</td>\n</tr>\n</table><table border=\"1\">\n<tr>\n<td><p>Type Member</p>\n<p>get_AProperty</p>\n</td>\n<td><h6>Member Type</h6>\n<p>Method</p>\n</td>\n<td><p>Equivalent Programming Language Construct</p>\n<p>Property get accessor method.</p>\n</td>\n</tr>\n<tr>\n<td><p>get_Item</p>\n</td>\n<td><p>Method</p>\n</td>\n<td><p>Indexer get accessor method.</p>\n</td>\n</tr>\n<tr>\n<td><p>op_Addition</p>\n</td>\n<td><p>Method</p>\n</td>\n<td><p>+ operator.</p>\n</td>\n</tr>\n<tr>\n<td><p>op_Equality</p>\n</td>\n<td><p>Method</p>\n</td>\n<td><p>== operator.</p>\n</td>\n</tr>\n<tr>\n<td><p>op_Inequality</p>\n</td>\n<td><p>Method</p>\n</td>\n<td><p>!= operator.</p>\n</td>\n</tr>\n<tr>\n<td><p>remove_AnEvent</p>\n</td>\n<td><p>Method</p>\n</td>\n<td><p>Event remove accessor method.</p>\n</td>\n</tr>\n<tr>\n<td><p>set_AProperty</p>\n</td>\n<td><p>Method</p>\n</td>\n<td><p>Property set accessor method.</p>\n</td>\n</tr>\n<tr>\n<td><p>set_Item</p>\n</td>\n<td><p>Method</p>\n</td>\n<td><p>Indexer set accessor method.</p>\n</td>\n</tr>\n</table><figcaption>The additional nodes under the Test type that aren\u2019t mentioned in Table 1-4\u2014.class, .custom, AnEvent, AProperty, and Item\u2014identify additional metadata about the type. These nodes don\u2019t map to fields or methods; they just offer some additional information about the type that the CLR, programming languages, or tools can get access to. For example, a tool can see that the Test type offers an event, called AnEvent, which is exposed via the two methods (add_AnEvent and remove_ AnEvent).</figcaption><h6>Interoperability with Unmanaged Code</h6><p>The .NET Framework offers a ton of advantages over other development platforms. However, very few companies can afford to redesign and re-implement all of their existing code. Microsoft realizes this and has constructed the CLR so that it offers mechanisms that allow an application to consist of both managed and unmanaged parts. Specifically, the CLR supports three interoperability scenarios:</p><p>\u25a0\u25a0 Managed code can call an unmanaged function in a DLL Managed code can easily call functions contained in DLLs by using a mechanism called P/Invoke (for Platform Invoke). After all, many of the types defined in the FCL internally call functions exported from Kernel32.dll, User32.dll, and so on. Many programming languages will expose a mechanism that makes it easy for managed code to call out to unmanaged functions contained in DLLs. For example, a C# application can call the CreateSemaphore function exported from Kernel32.dll.</p><p>\u25a0\u25a0 Managed code can use an existing COM component (server) Many companies have already implemented a number of unmanaged COM components. Using the type library from these components, a managed assembly can be created that describes the COM component. Managed code can access the type in the managed assembly just as any other managed type. See the TlbImp.exe tool that ships with the .NET Framework SDK for more information. At times, you might not have a type library or you might want to have more control over what TlbImp.exe produces. In these cases, you can manually build a type in source code that the CLR can use to achieve the proper interoperability. For example, you could use DirectX COM components from a C# application.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile12.png\"/><p>\u25a0\u25a0 Unmanaged code can use a managed type (server) A lot of existing unmanaged code requires that you supply a COM component for the code to work correctly. It\u2019s much easier to implement these components by using managed code so that you can avoid all of the code having to do with reference counting and interfaces. For example, you could create an ActiveX control or a shell extension in C#. See the TlbExp.exe and RegAsm.exe tools that ship with the</p><p>.NET Framework SDK for more information.</p><p>Note Microsoft now makes available the source code for the Type Library Importer tool and a P/Invoke Interop Assistant tool to help developers needing to interact with native code. These tools and their source code can be downloaded from http:// CLRInterop.CodePlex.com/.</p><p>With Windows 8, Microsoft has introduced a new Windows API called the Windows Runtime (WinRT). This API is implemented internally via COM components. But, instead of using type library files, the COM components describe their API via the metadata ECMA standard created by the .NET Framework team. The beauty of this is that code written via a .NET language can (for the most part) seamlessly communicate with WinRT APIs. Underneath the covers, the CLR is performing all of the COM interop for you without you having to use any additional tools at all\u2014it just works! Chapter 25, \u201cInteroperating with WinRT Components\u201d goes into all the details.</p><h6>C H A P T E R 2</h6><h2>Building, Packaging, Deploying, and Administering Applications and Types</h2><h6>In this chapter:</h6><p>.NET Framework Deployment Goals . . . . . . . . . . . . . . . . . . . . . . . . 34 Building Types into a Module . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 35 A Brief Look at Metadata . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 38 Combining Modules to Form an Assembly . . . . . . . . . . . . . . . . . . 45 Assembly Version Resource Information . . . . . . . . . . . . . . . . . . . . 54 Culture. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 59 Simple Application Deployment (Privately Deployed Assemblies). . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 60 Simple Administrative Control (Configuration). . . . . . . . . . . . . . . 62</p><p>Before we get into the chapters that explain how to develop programs for the Microsoft .NET Framework, let\u2019s discuss the steps required to build, package, and deploy your applications and their types. In this chapter, I\u2019ll focus on the basics of how to build assemblies that are for your application\u2019s sole use. In Chapter 3, \u201cShared Assemblies and Strongly Named Assemblies,\u201d I\u2019ll cover the more advanced concepts you\u2019ll need to understand, including how to build and use assemblies containing types that will be shared by multiple applications. In both chapters, I\u2019ll also talk about the ways an administrator can affect the execution of an application and its types.</p><p>Today, applications consist of several types, which are typically created by you and Microsoft. In addition, there are many component vendors creating and selling types that other companies can use to reduce a software project\u2019s development time. If these types are developed using any language that targets the common language runtime (CLR), they can all work together seamlessly; a type written in one language can use another type as its base class without concern for the language the base type was developed in.</p><p>In this chapter, I\u2019ll also explain how these types are built and packaged into files for deployment. In the process, I\u2019ll take you on a brief historical tour of some of the problems that the .NET Framework is solving.</p><p>33</p><h6>.NET Framework Deployment Goals</h6><p>Over the years, Windows has gotten a reputation for being unstable and complicated. This reputation, whether deserved or not, is the result of many different factors. First, all applications use dynamic-link libraries (DLLs) from Microsoft or other vendors. Because an application executes code from various vendors, the developer of any one piece of code can\u2019t be 100 percent sure how someone else is going to use it. Although this kind of interaction can potentially cause all kinds of trouble, in practice, these problems don\u2019t typically arise because applications are tested and debugged before they are deployed.</p><p>Users, however, frequently run into problems when one company decides to update its code and ships new files to them. These new files are supposed to be backward-compatible with the previous files, but who knows for sure? In fact, when one vendor updates its code, it usually finds it impossible to retest and debug all of the already-shipped applications to ensure that the changes will have no undesirable effect.</p><p>I\u2019m sure that everyone reading this book has experienced some variation of this problem: when installing a new application, you discover that it has somehow corrupted an already-installed application. This predicament is known as \u201cDLL hell.\u201d This type of instability puts fear into the hearts and minds of the typical computer user. The end result is that users have to carefully consider whether to install new software on their machines. Personally, I\u2019ve decided not to try out certain applications out of fear that it might adversely affect some application I really rely on.</p><p>The second reason that contributed to the aforementioned reputation of Windows is installation complexities. Today, when most applications are installed, they affect all parts of the system. For example, installing an application causes files to be copied to various directories, updates registry settings, and installs shortcuts on your desktop and Start menu/screen. The problem with this is that the application isn\u2019t isolated as a single entity. You can\u2019t easily back up the application because you must copy the application\u2019s files and also the relevant parts of the registry. In addition, you can\u2019t easily move the application from one machine to another; you must run the installation program again so that all files and registry settings are set properly. Finally, you can\u2019t easily uninstall or remove the application without having this nasty feeling that some part of the application is still lurking on your machine.</p><p>The third reason has to do with security. When applications are installed, they come with all kinds of files, many of them written by different companies. In addition, web applications frequently have code (like ActiveX controls) that is downloaded in such a way that users don\u2019t even realize that code is being installed on their machine. Today, this code can perform any operation, including deleting files or sending email. Users are right to be terrified of installing new applications because of the potential damage they can cause. To make users comfortable, security must be built into the system so that the users can explicitly allow or disallow code developed by various companies to access their system\u2019s resources.</p><p>The .NET Framework addresses the DLL hell issue in a big way, as you\u2019ll see while reading this chapter and Chapter 3. It also goes a long way toward fixing the problem of having an application\u2019s state scattered all over a user\u2019s hard disk. For example, unlike COM, types no longer require settings in the registry. Unfortunately, applications still require shortcut links. As for security, the .NET Framework</p><p>includes a security model called code access security. Whereas Windows security is based on a user\u2019s identity, code access security allows hosts to set permissions, thereby controlling what the loaded components can do. A host application like Microsoft SQL Server can grant just a few permissions to code, whereas a locally installed (self-hosting) application could run with full trust (all permissions). As you\u2019ll see, the .NET Framework enables users to control what gets installed and what runs, and in general, to control their machines, more than Windows ever did.</p><h6>Building Types into a Module</h6><p>In this section, I\u2019ll show you how to turn your source file, containing various types, into a file that can be deployed. Let\u2019s start by examining the following simple application.</p><p>public sealed class Program { public static void Main() {</p><p>System.Console.WriteLine(\"Hi\"); }</p><p>}</p><p>This application defines a type, called Program. This type has a single public, static method called Main. Inside Main is a reference to another type called System.Console. System.Console is a type implemented by Microsoft, and the Intermediate Language (IL) code that implements this type\u2019s methods is in the MSCorLib.dll file. So our application defines a type and also uses another company\u2019s type.</p><p>To build this sample application, put the preceding code into a source code file, say, Program.cs, and then execute the following command line.</p><p>csc.exe /out:Program.exe /t:exe /r:MSCorLib.dll Program.cs</p><p>This command line tells the C# compiler to emit an executable file called Program.exe (/out:Program.exe). The type of file produced is a Win32 console application (/t[arget]:exe).</p><p>When the C# compiler processes the source file, it sees that the code references the System.Console type\u2019s WriteLine method. At this point, the compiler wants to ensure that this type exists somewhere, that it has a WriteLine method, and that the argument being passed to this method matches the parameter the method expects. Because this type is not defined in the C# source code, to make the C# compiler happy, you must give it a set of assemblies that it can use to resolve references to external types. In the preceding command line, I\u2019ve included the /r[eference]:MSCorLib.dll switch, which tells the compiler to look for external types in the assembly identified by the MSCorLib.dll file.</p><p>MSCorLib.dll is a special file in that it contains all the core types: Byte, Char, String, Int32, and many more. In fact, these types are so frequently used that the C# compiler automatically references the MSCorLib.dll assembly. In other words, the following command line (with the /r switch omitted) gives the same results as the line shown earlier.</p><p>csc.exe /out:Program.exe /t:exe Program.cs</p><p>Furthermore, because the /out:Program.exe and the /t:exe command-line switches also match what the C# compiler would choose as defaults, the following command line gives the same results too.</p><p>csc.exe Program.cs</p><p>If, for some reason, you really don\u2019t want the C# compiler to reference the MSCorLib.dll assembly, you can use the /nostdlib switch. Microsoft uses this switch when building the MSCorLib.dll assembly itself. For example, the following command line will generate an error when CSC.exe attempts to compile the Program.cs file because the System.Console type is defined in MSCorLib.dll.</p><p>csc.exe /out:Program.exe /t:exe /nostdlib Program.cs</p><p>Now, let\u2019s take a closer look at the Program.exe file produced by the C# compiler. What exactly is this file? Well, for starters, it is a standard portable executable (PE) file. This means that a machine running 32-bit or 64-bit versions of Windows should be able to load this file and do something with it. Windows supports three types of applications. To build a console user interface (CUI) application, specify the /t:exe switch; to build a graphical user interface (GUI) application, specify the /t:winexe switch; and to build a Windows Store app, specify the /t:appcontainerexe switch.</p><h6>Response Files</h6><p>Before leaving the discussion about compiler switches, I\u2019d like to spend a moment talking about response files. A response file is a text file that contains a set of compiler command-line switches. When you execute CSC.exe, the compiler opens response files and uses any switches that are specified in them as though the switches were passed to CSC.exe on the command line. You instruct the compiler to use a response file by specifying its name on the command line prepended by an @ sign. For example, you could have a response file called MyProject.rsp that contains the following text.</p><p>/out:MyProject.exe /target:winexe</p><p>To cause CSC.exe to use these settings, you\u2019d invoke it as follows.</p><p>csc.exe @MyProject.rsp CodeFile1.cs CodeFile2.cs</p><p>This tells the C# compiler what to name the output file and what kind of target to create. As you can see, response files are very convenient because you don\u2019t have to manually express the desired command-line arguments each time you want to compile your project.</p><p>The C# compiler supports multiple response files. In addition to the files you explicitly specify on the command line, the compiler automatically looks for files called CSC.rsp. When you run CSC.exe, it looks in the directory containing the CSC.exe file for a global CSC.rsp file. Settings that you want applied to all of your projects should go in this file. The compiler aggregates and uses the settings in all of these response files. If you have conflicting settings in the local and global response files, the settings in the local file override the settings in the global file. Likewise, any settings explicitly passed on the command line override the settings taken from a local response file.</p><p>When you install the .NET Framework, it installs a default global CSC.rsp file in the %SystemRoot%\\ Microsoft.NET\\Framework(64)\\vX.X.X directory (where X.X.X is the version of the .NET Framework you have installed). The latest version of this file contains the following switches.</p><p># This file contains command\u00adline options that the C# # command line compiler (CSC) will process as part # of every compilation, unless the \"/noconfig\" option # is specified.</p><p># Reference the common Framework libraries /r:Accessibility.dll /r:Microsoft.CSharp.dll /r:System.Configuration.dll /r:System.Configuration.Install.dll /r:System.Core.dll /r:System.Data.dll /r:System.Data.DataSetExtensions.dll /r:System.Data.Linq.dll /r:System.Data.OracleClient.dll /r:System.Deployment.dll /r:System.Design.dll /r:System.DirectoryServices.dll /r:System.dll /r:System.Drawing.Design.dll /r:System.Drawing.dll /r:System.EnterpriseServices.dll /r:System.Management.dll /r:System.Messaging.dll /r:System.Runtime.Remoting.dll /r:System.Runtime.Serialization.dll /r:System.Runtime.Serialization.Formatters.Soap.dll /r:System.Security.dll /r:System.ServiceModel.dll /r:System.ServiceModel.Web.dll /r:System.ServiceProcess.dll /r:System.Transactions.dll /r:System.Web.dll /r:System.Web.Extensions.Design.dll /r:System.Web.Extensions.dll /r:System.Web.Mobile.dll /r:System.Web.RegularExpressions.dll /r:System.Web.Services.dll /r:System.Windows.Forms.Dll /r:System.Workflow.Activities.dll /r:System.Workflow.ComponentModel.dll /r:System.Workflow.Runtime.dll /r:System.Xml.dll /r:System.Xml.Linq.dll</p><p>Because the global CSC.rsp file references all of the assemblies listed, you do not need to explicitly reference these assemblies by using the C# compiler\u2019s /reference switch. This response file is a big convenience for developers because it allows them to use types and namespaces defined in various Microsoft-published assemblies without having to specify a /reference compiler switch for each when compiling.</p><p>Referencing all of these assemblies could slow the compiler down a bit. But if your source code doesn\u2019t refer to a type or member defined by any of these assemblies, there is no impact to the resulting assembly file, nor to run-time execution performance.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile13.png\"/><p>Note When you use the /reference compiler switch to reference an assembly, you can specify a complete path to a particular file. However, if you do not specify a path, the compiler will search for the file in the following places (in the order listed):</p><p>\u25a0\u25a0 Working directory.</p><p>\u25a0\u25a0 The directory that contains the CSC.exe file itself. MSCorLib.dll is always obtained from this directory. The path looks something like this: %SystemRoot%\\Microsoft.NET\\Framework\\ v4.0.#####.</p><p>\u25a0\u25a0 Any directories specified using the /lib compiler switch.</p><p>\u25a0\u25a0 Any directories specified using the LIB environment variable.</p><p>Of course, you\u2019re welcome to add your own switches to the global CSC.rsp file if you want to make your life even easier, but this makes it more difficult to replicate the build environment on different machines\u2014you have to remember to update the CSC.rsp the same way on each build machine. Also, you can tell the compiler to ignore both local and global CSC.rsp files by specifying the /noconfig command-line switch.</p><h6>A Brief Look at Metadata</h6><p>Now we know what kind of PE file we\u2019ve created. But what exactly is in the Program.exe file? A managed PE file has four main parts: the PE32(+) header, the CLR header, the metadata, and the IL. The PE32(+) header is the standard information that Windows expects. The CLR header is a small block of information that is specific to modules that require the CLR (managed modules). The header includes the major and minor version number of the CLR that the module was built for: some flags, a MethodDef token (described later) indicating the module\u2019s entry point method if this module is a CUI, GUI, or Windows Store executable, and an optional strong-name digital signature (discussed in Chapter 3). Finally, the header contains the size and offsets of certain metadata tables contained within the module. You can see the exact format of the CLR header by examining the IMAGE_COR20_HEADER defined in the CorHdr.h header file.</p><p>The metadata is a block of binary data that consists of several tables. There are three categories of tables: definition tables, reference tables, and manifest tables. Table 2-1 describes some of the more common definition tables that exist in a module\u2019s metadata block.</p><h6>TABLE 2-1 Common Definition Metadata Tables</h6><table border=\"1\">\n<tr>\n<td><p>Metadata Definition Table Name ModuleDef</p>\n</td>\n<td><h6>Description</h6>\n<p>Always contains one entry that identifies the module. The entry includes the module\u2019s file name and extension (without path) and a module version ID (in the form of a GUID created by the compiler). This allows the file to be renamed while keeping a record of its original name. However, renaming a file is strongly discouraged and can prevent the CLR from locating an assembly at run time, so don\u2019t do this.</p>\n</td>\n</tr>\n<tr>\n<td><p>TypeDef</p>\n</td>\n<td><p>Contains one entry for each type defined in the module. Each entry includes the type\u2019s name, base type, and flags (public, private, etc.) and contains indexes to the methods it owns in the MethodDef table, the fields it owns in the FieldDef table, the properties it owns in the PropertyDef table, and the events it owns in the EventDef table.</p>\n</td>\n</tr>\n<tr>\n<td><p>MethodDef</p>\n</td>\n<td><p>Contains one entry for each method defined in the module. Each entry includes the method\u2019s name, flags (private, public, virtual, abstract, static, final, etc.), signature, and offset within the module where its IL code can be found. Each entry can also refer to a ParamDef table entry in which more information about the method\u2019s parameters can be found.</p>\n</td>\n</tr>\n<tr>\n<td><p>FieldDef</p>\n</td>\n<td><p>Contains one entry for every field defined in the module. Each entry includes flags (private, public, etc.), type, and name.</p>\n</td>\n</tr>\n<tr>\n<td><p>ParamDef</p>\n</td>\n<td><p>Contains one entry for each parameter defined in the module. Each entry includes flags (in, out, retval, etc.), type, and name.</p>\n</td>\n</tr>\n<tr>\n<td><p>PropertyDef</p>\n</td>\n<td><p>Contains one entry for each property defined in the module. Each entry includes flags, type, and name.</p>\n</td>\n</tr>\n<tr>\n<td><p>EventDef</p>\n</td>\n<td><p>Contains one entry for each event defined in the module. Each entry includes flags and name.</p>\n</td>\n</tr>\n</table><figcaption>As the compiler compiles your source code, everything your code defines causes an entry to be created in one of the tables described in Table 2-1. Metadata table entries are also created as the compiler detects the types, fields, methods, properties, and events that the source code references. The metadata created includes a set of reference tables that keep a record of the referenced items. Table 2-2 shows some of the more common reference metadata tables.</figcaption><h6>TABLE 2-2 Common Reference Metadata Tables</h6><table border=\"1\">\n<tr>\n<td><p>Metadata Reference Table Name AssemblyRef</p>\n</td>\n<td><h6>Description</h6>\n<p>Contains one entry for each assembly referenced by the module. Each entry includes the information necessary to bind to the assembly: the assembly\u2019s name (without path and extension), version number, culture, and public key token (normally a small hash value generated from the publisher\u2019s public key, identifying the referenced assembly\u2019s publisher). Each entry also contains some flags and a hash value. This hash value was intended to be a checksum of the referenced assembly\u2019s bits. The CLR completely ignores this hash value and will probably continue to do so in the future.</p>\n</td>\n</tr>\n<tr>\n<td><p>ModuleRef</p>\n</td>\n<td><p>Contains one entry for each PE module that implements types referenced by this module. Each entry includes the module\u2019s file name and extension (without path). This table is used to bind to types that are implemented in different modules of the calling assembly\u2019s module.</p>\n</td>\n</tr>\n</table><table border=\"1\">\n<tr>\n<td><p>Metadata Reference Table Name TypeRef</p>\n</td>\n<td><h6>Description</h6>\n<p>Contains one entry for each type referenced by the module. Each entry includes the type\u2019s name and a reference to where the type can be found. If the type is implemented within another type, the reference will indicate a TypeRef entry. If the type is implemented in the same module, the reference will indicate a ModuleDef entry. If the type is implemented in another module within the calling assembly, the reference will indicate a ModuleRef entry. If the type is implemented in a different assembly, the reference will indicate an AssemblyRef entry.</p>\n</td>\n</tr>\n<tr>\n<td><p>MemberRef</p>\n</td>\n<td><p>Contains one entry for each member (fields and methods, as well as property and event methods) referenced by the module. Each entry includes the member\u2019s name and signature and points to the TypeRef entry for the type that defines the member.</p>\n</td>\n</tr>\n</table><figcaption>There are many more tables than what I listed in Tables 2-1 and 2-2, but I just wanted to give you a sense of the kind of information that the compiler emits to produce the metadata information. Earlier I mentioned that there is also a set of manifest metadata tables; I\u2019ll discuss these a little later in the chapter.</figcaption><p>Various tools allow you to examine the metadata within a managed PE file. One that I still use frequently is ILDasm.exe, the IL Disassembler. To see the metadata tables, execute the following command line.</p><p>ILDasm Program.exe</p><p>This causes ILDasm.exe to run, loading the Program.exe assembly. To see the metadata in a nice, human-readable form, select the View/MetaInfo/Show! menu item (or press Ctrl+M). This causes the following information to appear.</p><p>=========================================================== ScopeName : Program.exe MVID : {CA73FFE8\u00ad0D42\u00ad4610\u00adA8D3\u00ad9276195C35AA}</p><p>=========================================================== Global functions \u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad</p><p>Global fields \u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad</p><p>Global MemberRefs \u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad</p><p>TypeDef #1 (02000002) \u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad</p><p>TypDefName: Program (02000002) Flags : [Public] [AutoLayout] [Class] [Sealed] [AnsiClass]</p><p>[BeforeFieldInit] (00100101) Extends : 01000001 [TypeRef] System.Object Method #1 (06000001) [ENTRYPOINT]</p><p>\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00adMethodName: Main (06000001) Flags : [Public] [Static] [HideBySig] [ReuseSlot] (00000096) RVA : 0x00002050 ImplFlags : [IL] [Managed] (00000000) CallCnvntn: [DEFAULT] ReturnType: Void No arguments.</p><p>Method #2 (06000002) \u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad</p><p>MethodName: .ctor (06000002) Flags : [Public] [HideBySig] [ReuseSlot] [SpecialName]</p><p>[RTSpecialName] [.ctor] (00001886) RVA : 0x0000205c ImplFlags : [IL] [Managed] (00000000) CallCnvntn: [DEFAULT] hasThis ReturnType: Void No arguments.</p><ul style=\"list-style-type: none;\">\n<li><p>TypeRef #1 (01000001) \u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad</p><ul style=\"list-style-type: none;\">\n<li><p>Token: 0x01000001 ResolutionScope: 0x23000001 TypeRefName: System.Object</p><p>MemberRef #1 (0a000004) \u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad</p>\n<p>Member: (0a000004) .ctor: CallCnvntn: [DEFAULT] hasThis ReturnType: Void No arguments.</p>\n<p>TypeRef #2 (01000002) \u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad</p>\n</li>\n<li><p>Token: 0x01000002 ResolutionScope: 0x23000001 TypeRefName: System.Runtime.CompilerServices.CompilationRelaxationsAttribute</p></li>\n</ul>\n</li>\n</ul><p>MemberRef #1 (0a000001) \u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad</p><p>Member: (0a000001) .ctor: CallCnvntn: [DEFAULT] hasThis ReturnType: Void 1 Arguments</p><p>Argument #1: I4</p><ul style=\"list-style-type: none;\">\n<li><p>TypeRef #3 (01000003) \u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00adToken: 0x01000003 ResolutionScope: 0x23000001 TypeRefName: System.Runtime.CompilerServices.RuntimeCompatibilityAttribute</p><p>MemberRef #1 (0a000002) \u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad</p>\n<p>Member: (0a000002) .ctor: CallCnvntn: [DEFAULT] hasThis ReturnType: Void No arguments.</p>\n</li>\n<li><p>TypeRef #4 (01000004) \u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00adToken: 0x01000004 ResolutionScope: 0x23000001 TypeRefName: System.Console</p></li>\n</ul><p>MemberRef #1 (0a000003) \u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad</p><p>Member: (0a000003) WriteLine: CallCnvntn: [DEFAULT] ReturnType: Void 1 Arguments</p><p>Argument #1: String</p><p>Assembly \u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad</p><p>Token: 0x20000001 Name : Program Public Key : Hash Algorithm : 0x00008004 Version: 0.0.0.0 Major Version: 0x00000000 Minor Version: 0x00000000 Build Number: 0x00000000 Revision Number: 0x00000000 Locale: &lt;null&gt; Flags : [none] (00000000)</p><ul style=\"list-style-type: none;\">\n<li><p>CustomAttribute #1 (0c000001) \u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad</p><p>CustomAttribute Type: 0a000001 CustomAttributeName:</p>\n<p>System.Runtime.CompilerServices.CompilationRelaxationsAttribute ::</p>\n<p>instance void .ctor(int32) Length: 8 Value : 01 00 08 00 00 00 00 00 &gt; &lt; ctor args: (8)</p>\n</li>\n<li><p>CustomAttribute #2 (0c000002) \u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad</p></li>\n</ul><p>CustomAttribute Type: 0a000002 CustomAttributeName: System.Runtime.CompilerServices.RuntimeCompatibilityAttribute ::</p><p>instance void .ctor() Length: 30 Value : 01 00 01 00 54 02 16 57 72 61 70 4e 6f 6e 45 78 &gt; T WrapNonEx&lt;</p><p>: 63 65 70 74 69 6f 6e 54 68 72 6f 77 73 01 &gt;ceptionThrows &lt; ctor args: ()</p><p>AssemblyRef #1 (23000001) \u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad</p><p>Token: 0x23000001 Public Key or Token: b7 7a 5c 56 19 34 e0 89 Name: mscorlib Version: 4.0.0.0 Major Version: 0x00000004 Minor Version: 0x00000000 Build Number: 0x00000000 Revision Number: 0x00000000 Locale: &lt;null&gt; HashValue Blob: Flags: [none] (00000000)</p><p>User Strings \u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad70000001 : ( 2) L\"Hi\"</p><p>Coff symbol name overhead: 0</p><p>=========================================================== =========================================================== ===========================================================</p><p>Fortunately, ILDasm processes the metadata tables and combines information where appropriate so that you don\u2019t have to parse the raw table information. For example, in the preceding dump, you see that when ILDasm shows a TypeDef entry, the corresponding member definition information is shown with it before the first TypeRef entry is displayed.</p><p>You don\u2019t need to fully understand everything you see here. The important thing to remember is that Program.exe contains a TypeDef whose name is Program. This type identifies a public sealed class that is derived from System.Object (a type referenced from another assembly). The Program type also defines two methods: Main and .ctor (a constructor).</p><p>Main is a public, static method whose code is IL (as opposed to native CPU code, such as x86). Main has a void return type and takes no arguments. The constructor method (always shown with a name of .ctor) is public, and its code is also IL. The constructor has a void return type, has no arguments, and has a this pointer, which refers to the object\u2019s memory that is to be constructed when the method is called.</p><p>I strongly encourage you to experiment with using ILDasm. It can show you a wealth of information, and the more you understand what you\u2019re seeing, the better you\u2019ll understand the CLR and its capabilities. As you\u2019ll see, I\u2019ll use ILDasm quite a bit more in this book.</p><p>Just for fun, let\u2019s look at some statistics about the Program.exe assembly. When you select the ILDasm\u2019s View/Statistics menu item, the following information is displayed.</p><p>File size : 3584 PE header size : 512 (496 used) (14.29%) PE additional info : 1411 (39.37%) Num.of PE sections : 3 CLR header size : 72 ( 2.01%) CLR meta\u00addata size : 612 (17.08%) CLR additional info : 0 ( 0.00%) CLR method headers : 2 ( 0.06%) Managed code : 20 ( 0.56%) Data : 2048 (57.14%) Unaccounted : \u00ad1093 (\u00ad30.50%)</p><p>Num.of PE sections : 3</p><p>.text \u00ad 1024 .rsrc \u00ad 1536 .reloc \u00ad 512</p><p>CLR meta\u00addata size : 612 Module \u00ad 1 (10 bytes) TypeDef \u00ad 2 (28 bytes) 0 interfaces, 0 explicit layout TypeRef \u00ad 4 (24 bytes) MethodDef \u00ad 2 (28 bytes) 0 abstract, 0 native, 2 bodies MemberRef \u00ad 4 (24 bytes) CustomAttribute\u00ad 2 (12 bytes) Assembly \u00ad 1 (22 bytes) AssemblyRef \u00ad 1 (20 bytes) Strings \u00ad 184 bytes Blobs \u00ad 68 bytes UserStrings \u00ad 8 bytes Guids \u00ad 16 bytes Uncategorized \u00ad 168 bytes</p><p>CLR method headers : 2</p><p>Num.of method bodies \u00ad 2 Num.of fat headers \u00ad 0 Num.of tiny headers \u00ad 2</p><p>Managed code : 20 Ave method size \u00ad 10</p><p>Here you can see the size (in bytes) of the file and the size (in bytes and percentages) of the various parts that make up the file. For this very small Program.cs application, the PE header and the metadata occupy the bulk of the file\u2019s size. In fact, the IL code occupies just 20 bytes. Of course, as an application grows, it will reuse most of its types and references to other types and assemblies, causing the metadata and header information to shrink considerably as compared to the overall size of the file.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile14.png\"/><p>Note By the way, ILDasm.exe does have a bug in it that affects the file size information shown. In particular, you cannot trust the Unaccounted information.</p><h6>Combining Modules to Form an Assembly</h6><p>The Program.exe file discussed in the previous section is more than just a PE file with metadata; it is also an assembly. An assembly is a collection of one or more files containing type definitions and resource files. One of the assembly\u2019s files is chosen to hold a manifest. The manifest is another set of metadata tables that basically contain the names of the files that are part of the assembly. They also describe the assembly\u2019s version, culture, publisher, publicly exported types, and all of the files that comprise the assembly.</p><p>The CLR operates on assemblies; that is, the CLR always loads the file that contains the manifest metadata tables first and then uses the manifest to get the names of the other files that are in the assembly. Here are some characteristics of assemblies that you should remember:</p><p>\u25a0\u25a0 An assembly defines the reusable types.</p><p>\u25a0\u25a0 An assembly is marked with a version number.</p><p>\u25a0\u25a0 An assembly can have security information associated with it. An assembly\u2019s individual files don\u2019t have these attributes\u2014except for the file that contains the</p><p>manifest metadata tables.</p><p>To package, version, secure, and use types, you must place them in modules that are part of an assembly. In most cases, an assembly consists of a single file, as the preceding Program.exe example does. However, an assembly can also consist of multiple files: some PE files with metadata and some resource files such as .gif or .jpg files. It might help you to think of an assembly as a logical EXE or a DLL.</p><p>I\u2019m sure that many of you reading this are wondering why Microsoft has introduced this assembly concept. The reason is that an assembly allows you to decouple the logical and physical notions of reusable types. For example, an assembly can consist of several types. You could put the frequently used types in one file and the less frequently used types in another file. If your assembly is deployed by downloading it via the Internet, the file with the infrequently used types might not ever have to be downloaded to the client if the client never accesses the types. For example, an independent software vendor (ISV) specializing in UI controls might choose to implement Active Accessibility types in a separate module (to satisfy Microsoft\u2019s Logo requirements). Only users who require the additional accessibility features would require this module to be downloaded.</p><p>You configure an application to download assembly files by specifying a codeBase element (discussed in Chapter 3) in the application\u2019s configuration file. The codeBase element identifies a URL pointing to where all of an assembly\u2019s files can be found. When attempting to load an assembly\u2019s file, the CLR obtains the codeBase element\u2019s URL and checks the machine\u2019s download cache to see if the file is present. If it is, the file is loaded. If the file isn\u2019t in the cache, the CLR downloads the file into the cache from the location the URL points to. If the file can\u2019t be found, the CLR throws a FileNotFoundException exception at run time.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile15.png\"/><p>I\u2019ve identified three reasons to use multifile assemblies:</p><p>\u25a0\u25a0 You can partition your types among separate files, allowing for files to be incrementally downloaded as described in the Internet download scenario. Partitioning the types into separate files also allows for partial or piecemeal packaging and deployment for applications you purchase and install.</p><p>\u25a0\u25a0 You can add resource or data files to your assembly. For example, you could have a type that calculates some insurance information. This type might require access to some actuarial tables to make its computations. Instead of embedding the actuarial tables in your source code, you could use a tool (such as the Assembly Linker, AL.exe, discussed later) so that the data file is considered to be part of the assembly. By the way, this data file can be in any format\u2014a text file, a Microsoft Excel spreadsheet, a Microsoft Word table, or whatever you like\u2014as long as your application knows how to parse the file\u2019s contents.</p><p>\u25a0\u25a0 You can create assemblies consisting of types implemented in different programming languages. For example, you can implement some types in C#, some types in Microsoft Visual Basic, and other types in other languages. When you compile the types written with C# source code, the compiler produces a module. When you compile other types written with Visual Basic source code, the compiler produces a separate module. You can then use a tool to combine all of these modules into a single assembly. To developers using the assembly, the assembly appears to contain just a bunch of types; developers won\u2019t even know that different programming languages were used. By the way, if you prefer, you can run ILDasm.exe on each of the modules to obtain an IL source code file. Then you can run ILAsm.exe and pass it all of the IL source code files. ILAsm.exe will produce a single file containing all of the types. This technique requires your source code compiler to produce IL-only code.</p><p>Important To summarize, an assembly is a unit of reuse, versioning, and security. It allows you to partition your types and resources into separate files so that you, and consumers of your assembly, get to determine which files to package together and deploy. After the CLR loads the file containing the manifest, it can determine which of the assembly\u2019s other files contain the types and resources the application is referencing. Anyone consuming the assembly is required to know only the name of the file containing the manifest; the file partitioning is then abstracted away from the consumer and can change in the future without breaking the application\u2019s behavior.</p><p>If you have multiple types that can share a single version number and security settings, it is recommended that you place all of the types in a single file rather than spread the types out over separate files, let alone separate assemblies. The reason is performance. Loading a file/assembly takes the CLR and Windows time to find the assembly, load it, and initialize it. The fewer files/assemblies loaded the better, because loading fewer assemblies helps reduce working set and also reduces fragmentation of a process\u2019s address space. Finally, NGen.exe can perform better optimizations when processing larger files.</p><p>To build an assembly, you must select one of your PE files to be the keeper of the manifest. Or you can create a separate PE file that contains nothing but the manifest. Table 2-3 shows the manifest metadata tables that turn a managed module into an assembly.</p><p>TABLE 2-3 Manifest Metadata Tables</p><table border=\"1\">\n<tr>\n<td><p>Manifest Metadata Table Name AssemblyDef</p>\n</td>\n<td><h6>Description</h6>\n<p>Contains a single entry if this module identifies an assembly. The entry includes the assembly\u2019s name (without path and extension), version (major, minor, build, and revision), culture, flags, hash algorithm, and the publisher\u2019s public key (which can be null).</p>\n</td>\n</tr>\n<tr>\n<td><p>FileDef</p>\n</td>\n<td><p>Contains one entry for each PE and resource file that is part of the assembly (except the file containing the manifest because it appears as the single entry in the AssemblyDef table). The entry includes the file\u2019s name and extension (without path), hash value, and flags. If this assembly consists only of its own file, the FileDef table has no entries.</p>\n</td>\n</tr>\n<tr>\n<td><p>ManifestResourceDef</p>\n</td>\n<td><p>Contains one entry for each resource that is part of the assembly. The entry includes the resource\u2019s name, flags (public if visible outside the assembly and private otherwise), and an index into the FileDef table indicating the file that contains the resource file or stream. If the resource isn\u2019t a stand-alone file (such as a .jpg or a .gif), the resource is a stream contained within a PE file. For an embedded resource, the entry also includes an offset indicating the start of the resource stream within the PE file.</p>\n</td>\n</tr>\n<tr>\n<td><p>ExportedTypesDef</p>\n</td>\n<td><p>Contains one entry for each public type exported from all of the assembly\u2019s PE modules. The entry includes the type\u2019s name, an index into the FileDef table (indicating which of this assembly\u2019s files implements the type), and an index into the TypeDef table. Note: To save file space, types exported from the file containing the manifest are not repeated in this table because the type information is available using the metadata\u2019s TypeDef table.</p>\n</td>\n</tr>\n</table><figcaption>The existence of a manifest provides a level of indirection between consumers of the assembly and</figcaption><p>the partitioning details of the assembly and makes assemblies self-describing. Also, note that the file containing the manifest has metadata information that indicates which files are part of the assembly, but the individual files themselves do not have metadata information that specifies that they are part of the assembly.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile16.png\"/><p>Note The assembly file that contains the manifest also has an AssemblyRef table in it. This table contains an entry for all of the assemblies referenced by all of the assembly\u2019s files. This allows tools to open an assembly\u2019s manifest and see its set of referenced assemblies without having to open the assembly\u2019s other files. Again, the entries in the AssemblyRef table exist to make an assembly self-describing.</p><p>The C# compiler produces an assembly when you specify any of the following command-line switches: /t[arget]:exe, /t[arget]:winexe, /t[arget]: appcontainerexe, /t[arget]: library, or /t[arget]:winmdobj.1 All of these switches cause the compiler to generate a single PE file that contains the manifest metadata tables. The resulting file is either a CUI executable, a GUI executable, a Windows Store executable, a class library, or a WINMD library respectively.</p><p>1 When using /t[arget]:winmdobj, the resulting .winmdobj file must be passed to the WinMDExp.exe tool, which massages the metadata a bit in order to expose the assembly\u2019s public CLR types as Windows Runtime types. The WinMDExp.exe tool does not touch the IL code in any way.</p><p>In addition to these switches, the C# compiler supports the /t[arget]:module switch. This switch tells the compiler to produce a PE file that doesn\u2019t contain the manifest metadata tables. The PE file produced is always a DLL PE file, and this file must be added to an assembly before the CLR can access any types within it. When you use the /t:module switch, the C# compiler, by default, names the output file with an extension of .netmodule.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile17.png\"/><p>Important Unfortunately, the Microsoft Visual Studio integrated development environment (IDE) doesn\u2019t natively support the ability for you to create multifile assemblies. If you want to create multifile assemblies, you must resort to using command-line tools.</p><p>There are many ways to add a module to an assembly. If you\u2019re using the C# compiler to build a PE file with a manifest, you can use the /addmodule switch. To understand how to build a multifile assembly, let\u2019s assume that we have two source code files:</p><p>\u25a0\u25a0 RUT.cs, which contains rarely used types</p><p>\u25a0\u25a0 FUT.cs, which contains frequently used types Let\u2019s compile the rarely used types into their own module so that users of the assembly won\u2019t need</p><p>to deploy this module if they never access the rarely used types.</p><p>csc /t:module RUT.cs</p><p>This line causes the C# compiler to create a RUT.netmodule file. This file is a standard DLL PE file, but, by itself, the CLR can\u2019t load it.</p><p>Next let\u2019s compile the frequently used types into their own module. We\u2019ll make this module the keeper of the assembly\u2019s manifest because the types are used so often. In fact, because this module will now represent the entire assembly, I\u2019ll change the name of the output file to MultiFileLibrary.dll instead of calling it FUT.dll.</p><p>csc /out:MultiFileLibrary.dll /t:library /addmodule:RUT.netmodule FUT.cs</p><p>This line tells the C# compiler to compile the FUT.cs file to produce the MultiFileLibrary.dll file. Because /t:library is specified, a DLL PE file containing the manifest metadata tables is emitted into the MultiFileLibrary.dll file. The /addmodule:RUT.netmodule switch tells the compiler that RUT.netmodule is a file that should be considered part of the assembly. Specifically, the /addmodule switch tells the compiler to add the file to the FileDef manifest metadata table and to add RUT.netmodule\u2019s publicly exported types to the ExportedTypesDef manifest metadata table.</p><p>After the compiler has finished all of its processing, the two files shown in Figure 2-1 are created. The module on the right contains the manifest.</p><p>RUT.netmodule MultiFileLibrary.dll</p><table border=\"1\">\n<tr>\n<td><p>IL compiled from RUT.cs Metadata Types, methods, and so on defined by RUT.cs Types, methods, and so on referenced by RUT.cs</p>\n</td>\n</tr>\n</table><table border=\"1\">\n<tr>\n<td><p>IL compiled from FUT.cs Metadata Types, methods, and so on defined by FUT.cs Types, methods, and so on referenced by FUT.cs</p>\n<p>Assembly files (self and RUT.netmodule)</p>\n<p>Public assembly types (self and RUT.netmodule)</p>\n<p>Manifest</p>\n</td>\n</tr>\n</table><p>FIGURE 2-1 A multifile assembly consisting of two managed modules, one with a manifest.</p><p>The RUT.netmodule file contains the IL code generated by compiling RUT.cs. This file also contains metadata tables that describe the types, methods, fields, properties, events, and so on that are defined by RUT.cs. The metadata tables also describe the types, methods, and so on that are referenced by RUT.cs. The MultiFileLibrary.dll is a separate file. Like RUT.netmodule, this file includes the IL code generated by compiling FUT.cs and also includes similar definition and reference metadata tables. However, MultiFileLibrary.dll contains the additional manifest metadata tables, making MultiFileLibrary.dll an assembly. The additional manifest metadata tables describe all of the files that make up the assembly (the MultiFileLibrary.dll file itself and the RUT.netmodule file). The manifest metadata tables also include all of the public types exported from MultiFileLibrary.dll and RUT.netmodule.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile18.png\"/><p>Note In reality, the manifest metadata tables don\u2019t actually include the types that are exported from the PE file that contains the manifest. The purpose of this optimization is to reduce the number of bytes required by the manifest information in the PE file. So statements like \u201cThe manifest metadata tables also include all the public types exported from MultiFileLibrary.dll and RUT.netmodule\u201d aren\u2019t 100 percent accurate. However, this statement does accurately reflect what the manifest is logically exposing.</p><p>After the MultiFileLibrary.dll assembly is built, you can use ILDasm.exe to examine the metadata\u2019s manifest tables to verify that the assembly file does in fact have references to the RUT.netmodule file\u2019s types. Here is what the FileDef and ExportedTypesDef metadata tables look like.</p><p>File #1 (26000001) \u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad</p><p>Token: 0x26000001 Name : RUT.netmodule HashValue Blob : e6 e6 df 62 2c a1 2c 59 97 65 0f 21 44 10 15 96 f2 7e db c2 Flags : [ContainsMetaData] (00000000)</p><p>ExportedType #1 (27000001) \u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad</p><p>Token: 0x27000001 Name: ARarelyUsedType Implementation token: 0x26000001 TypeDef token: 0x02000002 Flags : [Public] [AutoLayout] [Class] [Sealed] [AnsiClass]</p><p>[BeforeFieldInit](00100101)</p><p>From this, you can see that RUT.netmodule is a file considered to be part of the assembly with the token 0x26000001. From the ExportedTypesDef table, you can see that there is a publicly exported type, ARarelyUsedType. The implementation token for this type is 0x26000001, which indicates that the type\u2019s IL code is contained in the RUT.netmodule file.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile19.png\"/><p>Note For the curious, metadata tokens are 4-byte values. The high byte indicates the type of token (0x01=TypeRef, 0x02=TypeDef, 0x23=AssemblyRef, 0x26=File (file definition), 0x27=ExportedType). For the complete list, see the CorTokenType enumerated type in the CorHdr.h file included with the .NET Framework SDK. The three lower bytes of the token simply identify the row in the corresponding metadata table. For example, the implementation token 0x26000001 refers to the first row of the File table. For most tables, rows are numbered starting with 1, not 0. For the TypeDef table, rows actually start with 2.</p><p>Any client code that consumes the MultiFileLibrary.dll assembly\u2019s types must be built using the /r[eference]: MultiFileLibrary.dll compiler switch. This switch tells the compiler to load the Multi-</p><p>FileLibrary.dll assembly and all of the files listed in its FileDef table when searching for an external type. The compiler requires all of the assembly\u2019s files to be installed and accessible. If you were to delete the RUT.netmodule file, the C# compiler would produce the following error: fatal error</p><p>CS0009: Metadata file 'C:\\ MultiFileLibrary.dll' could not be opened\u2014'Error importing module 'RUT.netmodule' of assembly 'C:\\ MultiFileLibrary.dll'\u2014The system cannot find the file specified'. This means that to build a new assembly, all of the</p><p>files from a referenced assembly must be present.</p><p>As the client code executes, it calls methods. When a method is called for the first time, the CLR detects the types that the method references as a parameter, a return type, or as a local variable. The CLR then attempts to load the referenced assembly\u2019s file that contains the manifest. If the type being accessed is in this file, the CLR performs its internal bookkeeping, allowing the type to be used. If the manifest indicates that the referenced type is in a different file, the CLR attempts to load the necessary file, performs its internal bookkeeping, and allows the type to be accessed. The CLR loads assembly files only when a method referencing a type in an unloaded assembly is called. This means that to run an application, all of the files from a referenced assembly do not need to be present.</p><h6>Adding Assemblies to a Project by Using the Visual Studio IDE</h6><p>If you\u2019re using the Visual Studio IDE to build your project, you\u2019ll have to add any assemblies that you want to reference to your project. To do so, open Solution Explorer, right-click the project you want to add a reference to, and then select the Add Reference menu item. This causes the Reference Manager dialog box, shown in Figure 2-2, to appear.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile20.png\"/><figcaption>FIGURE 2-2 The Reference Manager dialog box in Visual Studio.</figcaption><p>To have your project reference an assembly, select the desired assembly from the list. If the assembly you want isn\u2019t in the list, click the Browse button to navigate to the desired assembly (file containing a manifest) to add the assembly reference. The Solution option allows the current project to reference an assembly that is created by another project in the same solution. The COM option in the Reference Manager dialog box allows an unmanaged COM server to be accessed from within managed source code via a managed proxy class automatically generated by Visual Studio. The Browse option allows you to select an assembly that you recently added to another project.</p><p>To make your own assemblies appear in the Reference Manager\u2019s dialog box, follow the instructions at http://msdn.microsoft.com/en-us/library/wkze6zky(v=vs.110).aspx.</p><h6>Using the Assembly Linker</h6><p>Instead of using the C# compiler, you might want to create assemblies by using the Assembly Linker utility, AL.exe. The Assembly Linker is useful if you want to create an assembly consisting of modules built from different compilers (if your compiler doesn\u2019t support the equivalent of C#\u2019s /addmodule switch) or perhaps if you just don\u2019t know your assembly packaging requirements at build time. You can also use AL.exe to build resource-only assemblies, called satellite assemblies, which are typically used for localization purposes. I\u2019ll talk about satellite assemblies later in the chapter.</p><p>The AL.exe utility can produce an EXE or a DLL PE file that contains only a manifest describing the types in other modules. To understand how AL.exe works, let\u2019s change the way the MultiFileLibrary.dll assembly is built.</p><p>csc /t:module RUT.cs csc /t:module FUT.cs al /out: MultiFileLibrary.dll /t:library FUT.netmodule RUT.netmodule</p><p>Figure 2-3 shows the files that result from executing these statements.</p><p>RUT.netmodule</p><table border=\"1\">\n<tr>\n<td><p>IL compiled from RUT.cs Metadata Types, methods, and so on defined by RUT.cs Types, methods, and so on referenced by RUT.cs</p>\n</td>\n</tr>\n</table><p>FUT.netmodule</p><table border=\"1\">\n<tr>\n<td><p>IL compiled from FUT.cs Metadata Types, methods, and so on defined by FUT.cs Types, methods, and so on referenced by FUT.cs</p>\n</td>\n</tr>\n</table><p>MultiFileLibrary.dll</p><table border=\"1\">\n<tr>\n<td><p>(no IL) Metadata (No definition or reference tables) Manifest Assembly files (self, RUT.netmodule, and FUT.netmodule) Public assembly types (RUT.netmodule and FUT.netmodule)</p>\n</td>\n</tr>\n</table><p>FIGURE 2-3 A multifile assembly consisting of three managed modules, one with a manifest.</p><p>In this example, two separate modules, RUT.netmodule and FUT.netmodule, are created. Neither module is an assembly because they don\u2019t contain manifest metadata tables. Then a third file is produced: MultiFileLibrary.dll, which is a small DLL PE file (because of the /t[arget]:library switch) that contains no IL code but has manifest metadata tables indicating that RUT.netmodule and FUT.net module are part of the assembly. The resulting assembly consists of three files: MultiFileLibrary.dll,</p><p>RUT.netmodule, and FUT.netmodule. The Assembly Linker has no way to combine multiple files into a single file.</p><p>The AL.exe utility can also produce CUI, GUI, and Windows Store app PE files by using the /t[arget]:exe, /t[arget]:winexe, or /t[arget]:appcontainerexe command-line switches. But this is very unusual because it would mean that you\u2019d have an EXE PE file with just enough IL code in it to call a method in another module. You can specify which method in a module should be used as an entry point by adding the /main command-line switch when invoking AL.exe. The following is an example of how to call the Assembly Linker, AL.exe, by using the /main command-line switch.</p><p>csc /t:module /r:MultiFileLibrary.dll Program.cs al /out:Program.exe /t:exe /main:Program.Main Program.netmodule</p><p>Here the first line builds the Program.cs file into a Program.netmodule file. The second line produces a small Program.exe PE file that contains the manifest metadata tables. In addition, there is a small global function named __EntryPoint that is emitted by AL.exe because of the /main:Program.Main command-line switch. This function, __EntryPoint, contains the following IL code.</p><p>.method privatescope static void __EntryPoint$PST06000001() cil managed {</p><p>.entrypoint // Code size 8 (0x8)</p><p>.maxstack 8 IL_0000: tail. IL_0002: call void [.module 'Program.netmodule']Program::Main() IL_0007: ret</p><p>} // end of method 'Global Functions'::__EntryPoint</p><p>As you can see, this code simply calls the Main method contained in the Program type defined in the Program.netmodule file. The /main switch in AL.exe isn\u2019t that useful because it\u2019s unlikely that you\u2019d ever create an assembly for an application that didn\u2019t have its entry point in the PE file that contains the manifest metadata tables. I mention the switch here only to make you aware of its existence.</p><p>With the code that accompanies this book, I have created a Ch02-3-BuildMultiFileLibrary.bat file that encapsulates all the steps required to build a multifile assembly. The Ch02-4-AppUsingMultiFileLibrary project in Visual Studio invokes this batch file as a prebuild command-line step. You can examine this project to see how to integrate building and referencing a multifile assembly from within Visual Studio.</p><h6>Adding Resource Files to an Assembly</h6><p>When using AL.exe to create an assembly, you can add a file as a resource to the assembly by using the /embed[resource] switch. This switch takes a file (any file) and embeds the file\u2019s contents into the resulting PE file. The manifest\u2019s ManifestResourceDef table is updated to reflect the existence of the resources.</p><p>AL.exe also supports a /link[resource] switch, which also takes a file containing resources. However, the /link[resource] switch updates the manifest\u2019s ManifestResourceDef and FileDef tables, indicating that the resource exists and identifying which of the assembly\u2019s files contains it. The resource file is not embedded into the assembly PE file; it remains separate and must be packaged and deployed with the other assembly files.</p><p>Like AL.exe, CSC.exe also allows you to combine resources into an assembly produced by the C# compiler. The C# compiler\u2019s /resource switch embeds the specified resource file into the resulting assembly PE file, updating the ManifestResourceDef table. The compiler\u2019s /linkresource switch adds an entry to the ManifestResourceDef and the FileDef manifest tables to refer to a stand-alone resource file.</p><p>One last note about resources: it\u2019s possible to embed standard Win32 resources into an assembly. You can do this easily by specifying the path of a .res file with the /win32res switch when using either AL.exe or CSC.exe. In addition, you can quickly and easily embed a standard Win32 icon resource into an assembly file by specifying the path of the .ico file with the /win32icon switch when using either AL.exe or CSC.exe. Within Visual Studio, you can add resource files to your assembly by displaying your project\u2019s properties and then clicking the Application tab. The typical reason an icon is embedded is so that Windows Explorer can show an icon for a managed executable file.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile21.png\"/><p>Note Managed assembly files also contain Win32 manifest resource information in them. By default, the C# compiler automatically produces this manifest information but you can tell it not to by using the /nowin32manifest switch. The default manifest produced by the C# compiler looks like this:</p><p>&lt;?xml version=\"1.0\" encoding=\"UTF\u00ad8\" standalone=\"yes\"?&gt; &lt;assembly xmlns=\"urn:schemas\u00admicrosoft\u00adcom:asm.v1\" manifestVersion=\"1.0\"&gt;</p><p>&lt;assemblyIdentity version=\"1.0.0.0\" name=\"MyApplication.app\" /&gt; &lt;trustInfo xmlns=\"urn:schemas\u00admicrosoft\u00adcom:asm.v2\"&gt; &lt;security&gt; &lt;requestedPrivileges xmlns=\"urn:schemas\u00admicrosoft\u00adcom:asm.v3\"&gt;</p><p>&lt;requestedExecutionLevel level=\"asInvoker\" uiAccess=\"false\"/&gt; &lt;/requestedPrivileges&gt;</p><p>&lt;/security&gt; &lt;/trustInfo&gt;</p><p>&lt;/assembly&gt;</p><h6>Assembly Version Resource Information</h6><p>When AL.exe or CSC.exe produces a PE file assembly, it also embeds into the PE file a standard Win32 version resource. Users can examine this resource by viewing the file\u2019s properties. Application code can also acquire and examine this information at run time by calling System.Diagnostics.FileVersionInfo\u2019s static GetVersionInfo method with the assembly path as parameter. Figure 2-4 shows the Details tab of the Ch02-3-MultiFileLibrary.dll Properties dialog box.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile22.png\"/><figcaption>FIGURE 2-4 The Details tab of the Ch02-3-MultiFileLibrary.dll Properties dialog box.</figcaption><p>When building an assembly, you should set the version resource fields by using custom attributes that you apply at the assembly level in your source code. Here\u2019s what the code that produced the version information in Figure 2-4 looks like.</p><p>using System.Reflection;</p><p>// FileDescription version information: [assembly: AssemblyTitle(\"MultiFileLibrary.dll\")]</p><p>// Comments version information: [assembly: AssemblyDescription(\"This assembly contains MultiFileLibrary's types\")]</p><p>// CompanyName version information: [assembly: AssemblyCompany(\"Wintellect\")]</p><p>// ProductName version information: [assembly: AssemblyProduct(\"Wintellect (R) MultiFileLibrary's Type Library\")]</p><p>// LegalCopyright version information: [assembly: AssemblyCopyright(\"Copyright (c) Wintellect 2013\")]</p><p>// LegalTrademarks version information: [assembly:AssemblyTrademark(\"MultiFileLibrary is a registered trademark of Wintellect\")]</p><p>// AssemblyVersion version information: [assembly: AssemblyVersion(\"3.0.0.0\")]</p><p>// FILEVERSION/FileVersion version information: [assembly: AssemblyFileVersion(\"1.0.0.0\")]</p><p>// PRODUCTVERSION/ProductVersion version information: [assembly: AssemblyInformationalVersion(\"2.0.0.0\")]</p><p>// Set the Language field (discussed later in the \"Culture\" section) [assembly:AssemblyCulture(\"\")]</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile23.png\"/><p>Important Unfortunately, the Windows Explorer Properties dialog box is missing entries for some of the attributes. In particular, it would be great if the value of the AssemblyVersion attribute were shown because the CLR uses this value when loading assemblies, as we\u2019ll discuss in Chapter 3.</p><p>Table 2-4 shows the version resource fields and the custom attributes that correspond to them. If you\u2019re using AL.exe to build your assembly, you can use command-line switches to set this information instead of using the custom attributes. The second column in Table 2-4 shows the AL.exe command-line switch that corresponds to each version resource field. Note that the C# compiler doesn\u2019t offer these command-line switches and that, in general, using custom attributes is the preferred way to set this information.</p><ul style=\"list-style-type: none;\">\n<li><p>TABLE 2-4 Version Resource Fields and Their Corresponding AL.exe Switches and Custom Attributes</p></li>\n</ul><table border=\"1\">\n<tr>\n<td><p>Version Resource FILEVERSION</p>\n</td>\n<td><p>AL.exe Switch</p>\n<p>/fileversion</p>\n</td>\n<td><p>Custom Attribute/Comment System.Reflection.AssemblyFileVersionAttribute.</p>\n</td>\n</tr>\n<tr>\n<td><p>PRODUCTVERSION</p>\n</td>\n<td><p>/productversion</p>\n</td>\n<td><p>System.Reflection. AssemblyInformationalVersionAttribute.</p>\n</td>\n</tr>\n<tr>\n<td><p>FILEFLAGSMASK</p>\n</td>\n<td><p>(none)</p>\n</td>\n<td><p>Always set to VS_FFI_FILEFLAGSMASK (defined in WinVer.h as 0x0000003F).</p>\n</td>\n</tr>\n<tr>\n<td><p>FILEFLAGS</p>\n</td>\n<td><p>(none)</p>\n</td>\n<td><p>Always 0.</p>\n</td>\n</tr>\n<tr>\n<td><p>FILEOS</p>\n</td>\n<td><p>(none)</p>\n</td>\n<td><p>Currently always VOS__WINDOWS32.</p>\n</td>\n</tr>\n<tr>\n<td><p>FILETYPE</p>\n</td>\n<td><p>/target</p>\n</td>\n<td><p>Set to VFT_APP if /target:exe or /target:winexe is specified; set to VFT_DLL if /target:library is specified.</p>\n</td>\n</tr>\n<tr>\n<td><p>FILESUBTYPE</p>\n</td>\n<td><p>(none)</p>\n</td>\n<td><p>Always set to VFT2_UNKNOWN. (This field has no meaning for VFT_APP and VFT_DLL.)</p>\n</td>\n</tr>\n<tr>\n<td><p>AssemblyVersion</p>\n</td>\n<td><p>/version</p>\n</td>\n<td><p>System.Reflection.AssemblyVersionAttribute.</p>\n</td>\n</tr>\n<tr>\n<td><p>Comments</p>\n</td>\n<td><p>/description</p>\n</td>\n<td><p>System.Reflection.AssemblyDescriptionAttribute.</p>\n</td>\n</tr>\n<tr>\n<td><p>CompanyName</p>\n</td>\n<td><p>/company</p>\n</td>\n<td><p>System.Reflection.AssemblyCompanyAttribute.</p>\n</td>\n</tr>\n<tr>\n<td><p>FileDescription</p>\n</td>\n<td><p>/title</p>\n</td>\n<td><p>System.Reflection.AssemblyTitleAttribute.</p>\n</td>\n</tr>\n<tr>\n<td><p>FileVersion</p>\n</td>\n<td><p>/version</p>\n</td>\n<td><p>System.Reflection.AssemblyFileVersionAttribute.</p>\n</td>\n</tr>\n</table><table border=\"1\">\n<tr>\n<td><p>Version Resource InternalName</p>\n</td>\n<td><p>AL.exe Switch</p>\n<p>/out</p>\n</td>\n<td><p>Custom Attribute/Comment Set to the name of the output file specified (without the extension).</p>\n</td>\n</tr>\n<tr>\n<td><p>LegalCopyright</p>\n</td>\n<td><p>/copyright</p>\n</td>\n<td><p>System.Reflection.AssemblyCopyrightAttribute.</p>\n</td>\n</tr>\n<tr>\n<td><p>LegalTrademarks</p>\n</td>\n<td><p>/trademark</p>\n</td>\n<td><p>System.Reflection.AssemblyTrademarkAttribute.</p>\n</td>\n</tr>\n<tr>\n<td><p>OriginalFilename</p>\n</td>\n<td><p>/out</p>\n</td>\n<td><p>Set to the name of the output file (without a path).</p>\n</td>\n</tr>\n<tr>\n<td><p>PrivateBuild</p>\n</td>\n<td><p>(none)</p>\n</td>\n<td><p>Always blank.</p>\n</td>\n</tr>\n<tr>\n<td><p>ProductName</p>\n</td>\n<td><p>/product</p>\n</td>\n<td><p>System.Reflection.AssemblyProductAttribute.</p>\n</td>\n</tr>\n<tr>\n<td><p>ProductVersion</p>\n</td>\n<td><p>/productversion</p>\n</td>\n<td><p>System.Reflection. AssemblyInformationalVersionAttribute.</p>\n</td>\n</tr>\n<tr>\n<td><p>SpecialBuild</p>\n</td>\n<td><p>(none)</p>\n</td>\n<td><p>Always blank.</p>\n</td>\n</tr>\n</table><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile24.png\"/><figcaption>Important When you create a new C# project in Visual Studio, an AssemblyInfo.cs file is created automatically for you in a Properties folder. This file contains all of the assembly version attributes described in this section, plus a few additional attributes that I\u2019ll cover in Chapter 3. You can simply open the AssemblyInfo.cs file and modify your assembly-specific information. Visual Studio also provides a dialog box that you can use to edit the assembly version information in this file. To see this dialog box, in Solution Explorer, double-click your project\u2019s Properties entry, and on the Application tab, click Assembly Information; you\u2019ll see a dialog box like the one shown in Figure 2-5.</figcaption><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile25.png\"/><figcaption>FIGURE 2-5 Visual Studio\u2019s Assembly Information dialog box.</figcaption><h6>Version Numbers</h6><p>In the previous section, you saw that several version numbers can be applied to an assembly. All of these version numbers have the same format: each consists of four period-separated parts, as shown in Table 2-5.</p><ul style=\"list-style-type: none;\">\n<li><p>TABLE 2-5 Format of Version Numbers Major Number Minor Number Build Number Revision Number</p></li>\n</ul><p>Example: 2 5 719 2</p><p>Table 2-5 shows an example of a version number: 2.5.719.2. The first two numbers make up the public perception of the version. The public will think of this example as version 2.5 of the assembly. The third number, 719, indicates the build of the assembly. If your company builds its assembly every day, you should increment the build number each day as well. The last number, 2, indicates the revision of the build. If for some reason your company has to build an assembly twice in one day, maybe to resolve a hot bug that is halting other work, the revision number should be incremented. Microsoft uses this version-numbering scheme, and it\u2019s highly recommended that you use this scheme as well.</p><p>You\u2019ll notice that an assembly has three version numbers associated with it. This is very unfortunate and leads to a lot of confusion. Let me explain each version number\u2019s purpose and how it is expected to be used:</p><p>\u25a0\u25a0 AssemblyFileVersion This version number is stored in the Win32 version resource. This number is for information purposes only; the CLR doesn\u2019t examine this version number in any way. Typically, you set the major and minor parts to represent the version you want the public to see. Then you increment the build and revision parts each time a build is performed. Ideally, Microsoft\u2019s tool (such as CSC.exe or AL.exe) would automatically update the build and revision numbers for you (based on the date and time when the build was performed), but unfortunately, they don\u2019t. This version number can be seen when using Windows Explorer and is typically used to identify a specific version of an assembly when troubleshooting a customer\u2019s system.</p><p>\u25a0\u25a0 AssemblyInformationalVersion This version number is also stored in the Win32 version resource, and again, this number is for information purposes only; the CLR doesn\u2019t examine or care about it in any way. This version number exists to indicate the version of the product that includes this assembly. For example, version 2.0 of a product might contain several assemblies; one of these assemblies is marked as version 1.0 because it\u2019s a new assembly that didn\u2019t ship in version 1.0 of the same product. Typically, you set the major and minor parts of this version number to represent the public version of your product. Then you increment the build and revision parts each time you package a complete product with all its assemblies.</p><p>\u25a0\u25a0 AssemblyVersion This version number is stored in the AssemblyDef manifest metadata table. The CLR uses this version number when binding to strongly named assemblies (discussed in Chapter 3). This number is extremely important and is used to uniquely identify an assembly. When starting to develop an assembly, you should set the major, minor, build, and revision numbers and shouldn\u2019t change them until you\u2019re ready to begin work on the next deployable version of your assembly. When Assembly-A references a strongly named Assembly-B, Assembly-B\u2019s version is embedded inside Assembly-A\u2019s AssemblyRef table\u2019s entry. This way, when the CLR needs to load Assembly-B, it knows exactly which version Assembly-A was built and tested with. It is possible to have the CLR load a different version by using a binding redirect, which is discussed in Chapter 3.</p><h6>Culture</h6><p>Like version numbers, assemblies also have a culture as part of their identity. For example, I could have an assembly that is strictly for German, another assembly for Swiss German, another assembly for US English, and so on. Cultures are identified via a string that contains a primary and a secondary tag (as described in RFC 1766). Table 2-6 shows some examples.</p><p>TABLE 2-6 Examples of Assembly Culture Tags</p><table border=\"1\">\n<tr>\n<td><p>Primary Tag De</p>\n</td>\n<td><p>Secondary Tag (none)</p>\n</td>\n<td><p>Culture German</p>\n</td>\n</tr>\n<tr>\n<td><p>De</p>\n</td>\n<td><p>AT</p>\n</td>\n<td><p>Austrian German</p>\n</td>\n</tr>\n<tr>\n<td><p>De</p>\n</td>\n<td><p>CH</p>\n</td>\n<td><p>Swiss German</p>\n</td>\n</tr>\n<tr>\n<td><p>En</p>\n</td>\n<td><p>(none)</p>\n</td>\n<td><p>English</p>\n</td>\n</tr>\n<tr>\n<td><p>En</p>\n</td>\n<td><p>GB</p>\n</td>\n<td><p>British English</p>\n</td>\n</tr>\n<tr>\n<td><p>En</p>\n</td>\n<td><p>US</p>\n</td>\n<td><p>US English</p>\n</td>\n</tr>\n</table><figcaption>In general, if you create an assembly that contains code, you don\u2019t assign a culture to it. This is because code doesn\u2019t usually have any culture-specific assumptions built into it. An assembly that isn\u2019t assigned a culture is referred to as being culture neutral.</figcaption><p>If you\u2019re designing an application that has some culture-specific resources to it, Microsoft highly recommends that you create one assembly that contains your code and your application\u2019s default (or fallback) resources. When building this assembly, don\u2019t specify a culture. This is the assembly that other assemblies will reference when they create and manipulate types it publicly exposes.</p><p>Now you can create one or more separate assemblies that contain only culture-specific resourcesno code at all. Assemblies that are marked with a culture are called satellite assemblies. For these satellite assemblies, assign a culture that accurately reflects the culture of the resources placed in the assembly. You should create one satellite assembly for each culture you intend to support.</p><p>You\u2019ll usually use the AL.exe tool to build a satellite assembly. You won\u2019t use a compiler because the satellite assembly should have no code contained within it. When using AL.exe, you specify the desired culture by using the /c[ulture]:text switch, where text is a string such as \u201cen-US,\u201d representing US English. When you deploy a satellite assembly, you should place it in a subdirectory whose name matches the culture text. For example, if the application\u2019s base directory is C:\\MyApp, the US English satellite assembly should be placed in the C:\\MyApp\\en-US subdirectory. At run time, you access a satellite assembly\u2019s resources by using the System.Resources.ResourceManager class.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile26.png\"/><p>Note It is possible to create a satellite assembly that contains code, though this practice is discouraged. If you prefer, you can specify the culture by using the System.Reflection. AssemblyCultureAttribute custom attribute instead of using AL.exe\u2019s /culture switch, for example, as shown here.</p><p>// Set assembly's culture to Swiss German [assembly:AssemblyCulture(\"de\u00adCH\")]</p><p>Normally, you shouldn\u2019t build an assembly that references a satellite assembly. In other words, an assembly\u2019s AssemblyRef entries should all refer to culture-neutral assemblies. If you want to access types or members contained in a satellite assembly, you should use reflection techniques as discussed in Chapter 23, \u201cAssembly Loading and Reflection.\u201d</p><h6>Simple Application Deployment (Privately Deployed Assemblies)</h6><p>Throughout this chapter, I\u2019ve explained how you build modules and how you combine those modules into an assembly. At this point, I\u2019m ready to explain how to package and deploy all of the assemblies so that users can run the application.</p><p>Windows Store apps have very strict rules about packaging assemblies, and Visual Studio will package all of an application\u2019s required assemblies together into a single .appx file, which is either uploaded to the Windows Store or can be side-loaded onto a machine. When a user installs an .appx file, all the assemblies it contains are placed in a directory where the CLR will load them and Windows adds an application tile to the user's Start screen. If other users install the same .appx file, the previously installed assemblies are used and the new user simply gets a tile added to their Start screen. When a user uninstalls a Windows Store app, the system removes the tile from the user's Start screen. If no other users have the app installed, then Windows destroys the directory along with all the assemblies. Note that different users can install different versions of the same Windows Store app. To accommodate this, Windows installs the assemblies into different directories so that multiple versions of a single app can reside on a single machine simultaneously.</p><p>For desktop (non-Windows Store) applications, assemblies don\u2019t dictate or require any special means of packaging. The easiest way to package a set of assemblies is simply to copy all of the files directly. For example, you could put all of the assembly files on a CD-ROM and ship it to the user</p><p>with a batch file setup program that just copies the files from the CD to a directory on the user\u2019s hard drive. Because the assemblies include all of the dependent assembly references and types, the user can just run the application and the runtime will look for referenced assemblies in the application\u2019s directory. No modifications to the registry are necessary for the application to run. To uninstall the application, just delete all the files\u2014that\u2019s it!</p><p>Of course, you can package and install the assembly files by using other mechanisms, such as .cab files (typically used for Internet download scenarios to compress files and reduce download times). You can also package the assembly files into an MSI file for use by the Windows Installer service (MSIExec.exe). Using MSI files allows assemblies to be installed on demand the first time the CLR attempts to load the assembly. This feature isn\u2019t new to MSI; it can perform the same demand-load functionality for unmanaged EXE and DLL files as well.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile27.png\"/><p>Note Using a batch file or some other simple \u201cinstallation software\u201d will get an application onto the user\u2019s machine; however, you\u2019ll need more sophisticated installation software to create shortcut links on the user\u2019s desktop and Start menu/screen. Also, you can easily back up and restore the application or move it from one machine to another, but the various shortcut links will require special handling.</p><p>Of course, Visual Studio has a built-in mechanism that you can use to publish an application by displaying a project\u2019s Properties pages and clicking the Publish tab. You can use the options available on the Publish tab to cause Visual Studio to produce an MSI file and copy the resulting MSI file to a website, FTP server, or file path. The MSI file can also install any prerequisite components such as the .NET Framework or Microsoft SQL Server Express Edition. Finally, the application can automatically check for updates and install them on the user\u2019s machine by taking advantage of ClickOnce technology.</p><p>Assemblies deployed to the same directory as the application are called privately deployed assemblies because the assembly files aren\u2019t shared with any other application (unless the other application is also deployed to the same directory). Privately deployed assemblies are a big win for developers, end users, and administrators because they can simply be copied to an application\u2019s base directory, and the CLR will load them and execute the code in them. In addition, an application can be uninstalled by simply deleting the assemblies in its directory. This allows simple backup and restore as well.</p><p>This simple install/move/uninstall scenario is possible because each assembly has metadata indicating which referenced assembly should be loaded; no registry settings are required. In addition, the referencing assembly scopes every type. This means that an application always binds to the same type it was built and tested with; the CLR can\u2019t load a different assembly that just happens to provide a type with the same name. This is different from COM, in which types are recorded in the registry, making them available to any application running on the machine.</p><p>In Chapter 3, I\u2019ll discuss how to deploy shared assemblies that are accessible by multiple applications.</p><h6>Simple Administrative Control (Configuration)</h6><p>The user or the administrator can best determine some aspects of an application\u2019s execution. For example, an administrator might decide to move an assembly\u2019s files on the user\u2019s hard disk or to override information contained in the assembly\u2019s manifest. Other scenarios also exist related to versioning; I\u2019ll talk about some of these in Chapter 3.</p><p>To allow administrative control over an application, a configuration file can be placed in the application\u2019s directory. An application\u2019s publisher can create and package this file. The setup program would then install this configuration file in the application\u2019s base directory. In addition, the machine\u2019s administrator or an end user could create or modify this file. The CLR interprets the content of this file to alter its policies for locating and loading assembly files.</p><p>These configuration files contain Extensible Markup Language (XML) and can be associated with an application or with the machine. Using a separate file (versus registry settings) allows the file to be easily backed up and also allows the administrator to copy the application to another machine\u2014just copy the necessary files and the administrative policy is copied too.</p><p>In Chapter 3, we\u2019ll explore this configuration file in more detail. But I want to give you a taste of it</p><p>now. Let\u2019s say that the publisher of an application wants its application deployed with the MultiFileLibrary assembly files in a different directory than the application\u2019s assembly file. The desired directory structure looks like the following.</p><p>AppDir directory (contains the application\u2019s assembly files) Program.exe Program.exe.config (discussed below)</p><p>AuxFiles subdirectory (contains MultiFileLibrary\u2019s assembly files) MultiFileLibrary.dll FUT.netmodule RUT.netmodule</p><p>Because the MultiFileLibrary files are no longer in the application\u2019s base directory, the CLR won\u2019t be able to locate and load these files; running the application will cause a System.IO.FileNotFoundException exception to be thrown. To fix this, the publisher creates an XML configuration file and deploys it to the application\u2019s base directory. The name of this file must be the name of the application\u2019s main assembly file with a .config extension: Program.exe.config, for this example. The configuration file should look like the following.</p><p>&lt;configuration&gt; &lt;runtime&gt;</p><p>&lt;assemblyBinding xmlns=\"urn:schemas\u00admicrosoft\u00adcom:asm.v1\"&gt;</p><p>&lt;probing privatePath=\"AuxFiles\" /&gt; &lt;/assemblyBinding&gt;</p><p>&lt;/runtime&gt; &lt;/configuration&gt;</p><p>Whenever the CLR attempts to locate an assembly file, it always looks in the application\u2019s directory first, and if it can\u2019t find the file there, it looks in the AuxFiles subdirectory. You can specify multiple semicolon-delimited paths for the probing element\u2019s privatePath attribute. Each path is considered relative to the application\u2019s base directory. You can\u2019t specify an absolute or a relative path identifying a directory that is outside of the application\u2019s base directory. The idea is that an application can control its directory and its subdirectories but has no control over other directories.</p><h6>Probing for Assembly Files</h6><p>When the CLR needs to locate an assembly, it scans several subdirectories. Here is the order in which directories are probed for a culture-neutral assembly (where firstPrivatePath and secondPrivatePath are specified via the config file\u2019s privatePath attribute).</p><p>AppDir\\AsmName.dll AppDir\\AsmName\\AsmName.dll AppDir\\firstPrivatePath\\AsmName.dll AppDir\\firstPrivatePath\\AsmName\\AsmName.dll AppDir\\secondPrivatePath\\AsmName.dll AppDir\\secondPrivatePath\\AsmName\\AsmName.dll</p><p>...</p><p>In this example, no configuration file would be needed if the MultiFileLibrary assembly files were deployed to a subdirectory called MultiFileLibrary, because the CLR would automatically scan for a subdirectory whose name matches the name of the assembly being searched for.</p><p>If the assembly can\u2019t be found in any of the preceding subdirectories, the CLR starts all over, using an .exe extension instead of a .dll extension. If the assembly still can\u2019t be found, a FileNotFoundException is thrown.</p><p>For satellite assemblies, similar rules are followed except that the assembly is expected to be in a subdirectory, whose name matches the culture, of the application\u2019s base directory. For example, if AsmName.dll has a culture of \u201cen-US\u201d applied to it, the following directories are probed.</p><p>C:\\AppDir\\en\u00adUS\\AsmName.dll C:\\AppDir\\en\u00adUS\\AsmName\\AsmName.dll C:\\AppDir\\firstPrivatePath\\en\u00adUS\\AsmName.dll C:\\AppDir\\firstPrivatePath\\en\u00adUS\\AsmName\\AsmName.dll C:\\AppDir\\secondPrivatePath\\en\u00adUS\\AsmName.dll C:\\AppDir\\secondPrivatePath\\en\u00adUS\\AsmName\\AsmName.dll</p><p>C:\\AppDir\\en\u00adUS\\AsmName.exe C:\\AppDir\\en\u00adUS\\AsmName\\AsmName.exe C:\\AppDir\\firstPrivatePath\\en\u00adUS\\AsmName.exe C:\\AppDir\\firstPrivatePath\\en\u00adUS\\AsmName\\AsmName.exe C:\\AppDir\\secondPrivatePath\\en\u00adUS\\AsmName.exe C:\\AppDir\\secondPrivatePath\\en\u00adUS\\AsmName\\AsmName.exe</p><p>C:\\AppDir\\en\\AsmName.dll C:\\AppDir\\en\\AsmName\\AsmName.dll C:\\AppDir\\firstPrivatePath\\en\\AsmName.dll C:\\AppDir\\firstPrivatePath\\en\\AsmName\\AsmName.dll C:\\AppDir\\secondPrivatePath\\en\\AsmName.dll C:\\AppDir\\secondPrivatePath\\en\\AsmName\\AsmName.dll</p><p>C:\\AppDir\\en\\AsmName.exe C:\\AppDir\\en\\AsmName\\AsmName.exe C:\\AppDir\\firstPrivatePath\\en\\AsmName.exe C:\\AppDir\\firstPrivatePath\\en\\AsmName\\AsmName.exe C:\\AppDir\\secondPrivatePath\\en\\AsmName.exe C:\\AppDir\\secondPrivatePath\\en\\AsmName\\AsmName.exe</p><p>As you can see, the CLR probes for files with either an .exe or .dll file extension. Because probing can be very time-consuming (especially when the CLR is looking for files over a network), in the XML configuration file, you can specify one or more culture elements to limit the probing that the CLR performs when looking for satellite assemblies. Microsoft provides a FusLogVw.exe tool that you can use to see how the CLR is binding to assemblies at run time. For more information about it, see http://msdn.microsoft.com/en-us/library/e74a18c4(v=vs.110).aspx.</p><p>The name and location of this XML configuration file is different depending on the application type:</p><p>\u25a0\u25a0 For executable applications (EXEs), the configuration file must be in the application\u2019s base</p><p>directory, and it must be the name of the EXE file with \u201c.config\u201d appended to it.</p><p>\u25a0\u25a0 For Microsoft ASP.NET Web Form applications, the file must be in the Web application\u2019s virtual root directory and is always named Web.config. In addition, subdirectories can also contain their own Web.config file, and the configuration settings are inherited. For example, a Web application located at http://Wintellect.com/Training would use the settings in the Web.config files contained in the virtual root directory and in its Training subdirectory.</p><p>As mentioned at the beginning of this section, configuration settings apply to a particular application and to the machine. When you install the .NET Framework, it creates a Machine.config file. There is one Machine.config file per version of the CLR you have installed on the machine.</p><p>The Machine.config file is located in the following directory: %SystemRoot%\\Microsoft.NET\\Framework\\version\\CONFIG</p><p>Of course, %SystemRoot% identifies your Windows directory (usually C:\\WINDOWS), and version is a version number identifying a specific version of the .NET Framework (something like v4.0.#####).</p><p>Settings in the Machine.config file represent default settings that affect all applications running on the machine. An administrator can create a machine-wide policy by modifying the single Machine.config file. However, administrators and users should avoid modifying this file because it contains many settings related to various things, making it much more difficult to navigate. Plus, you want the application\u2019s settings to be backed up and restored, and keeping an application\u2019s settings in the application-specific configuration file enables this.</p><h6>C H A P T E R 3</h6><h2>Shared Assemblies and Strongly Named Assemblies</h2><h6>In this chapter:</h6><p>Two Kinds of Assemblies, Two Kinds of Deployment . . . . . . . . . 66 Giving an Assembly a Strong Name. . . . . . . . . . . . . . . . . . . . . . . . . 67 The Global Assembly Cache. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 72 Building an Assembly That References a Strongly Named Assembly . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 74 Strongly Named Assemblies Are Tamper-Resistant. . . . . . . . . . . 75 Delayed Signing . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 76 Privately Deploying Strongly Named Assemblies . . . . . . . . . . . . 79 How the Runtime Resolves Type References . . . . . . . . . . . . . . . . . 80 Advanced Administrative Control (Configuration). . . . . . . . . . . . 83</p><p>In Chapter 2, \u201cBuilding, Packaging, Deploying, and Administering Applications and Types,\u201d I talked about the steps required to build, package, and deploy an assembly. I focused on what\u2019s called private deployment, in which assemblies are placed in the application\u2019s base directory (or a subdirectory thereof) for the application\u2019s sole use. Deploying assemblies privately gives a company a large degree of control over the naming, versioning, and behavior of the assembly.</p><p>In this chapter, I\u2019ll concentrate on creating assemblies that can be accessed by multiple applications. The assemblies that ship with the Microsoft .NET Framework are an excellent example of globally deployed assemblies, because all managed applications use types defined by Microsoft in the</p><p>.NET Framework Class Library (FCL).</p><p>As I mentioned in Chapter 2, Windows has a reputation for being unstable. The main reason for this reputation is the fact that applications are built and tested using code implemented by someone else. After all, when you write an application for Windows, your application is calling into code written by Microsoft developers. Also, a large number of companies make controls that application developers can incorporate into their own applications. In fact, the .NET Framework encourages this, and many control vendors have appeared over time.</p><p>As time marches on, Microsoft developers and control developers modify their code: they fix bugs, make security updates, add features, and so on. Eventually, the new code makes its way onto the user\u2019s machine. The user\u2019s applications that were previously installed and working fine are no longer using the same code that the applications were built and tested with. As a result, the applications\u2019 behavior is no longer predictable, which contributes to the instability of Windows.</p><p>65</p><p>File versioning is a very difficult problem to solve. In fact, I assert that if you take a file that is used by other code files and change just one bit in the file\u2014change a 0 to a 1 or a 1 to a 0\u2014there\u2019s absolutely no way to guarantee that code that used the file before it was changed will now work just as well if it uses the new version of the file. One of the reasons why this statement is true is that a lot of applications exploit bugs, either knowingly or unknowingly. If a later version of a file fixes a bug, the application no longer runs as expected.</p><p>So here\u2019s the problem: How do you fix bugs and add features to a file and also guarantee that you don\u2019t break some application? I\u2019ve given this question a lot of thought and have come to one conclusion: It\u2019s just not possible. But, obviously, this answer isn\u2019t good enough. Files will ship with bugs, and companies will always want to provide new features. There must be a way to distribute new files with the hope that the applications will work just fine. And if the application doesn\u2019t work fine, there has to be an easy way to restore the application to its last-known good state.</p><p>In this chapter, I\u2019ll explain the infrastructure that the .NET Framework has in place to deal with versioning problems. Let me warn you: What I\u2019m about to describe is complicated. I\u2019m going to talk about a lot of algorithms, rules, and policies that are built into the common language runtime (CLR). I\u2019m also going to mention a lot of tools and utilities that the application developer must use. This stuff is complicated because, as I\u2019ve mentioned, the versioning problem is difficult to address and to solve.</p><h6>Two Kinds of Assemblies, Two Kinds of Deployment</h6><p>The CLR supports two kinds of assemblies: weakly named assemblies and strongly named assemblies.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile28.png\"/><p>Important By the way, you won\u2019t find the term weakly named assembly in any of the .NET Framework documentation. Why? Because I made it up. In fact, the documentation has no term to identify a weakly named assembly. I decided to coin the term so that I can talk about assemblies without any ambiguity as to what kind of assembly I\u2019m referring to.</p><p>Weakly named assemblies and strongly named assemblies are structurally identical\u2014that is, they use the same portable executable (PE) file format, PE32(+) header, CLR header, metadata, manifest tables, and Intermediate Language (IL) that we examined in Chapter 1, \u201cThe CLR\u2019s Execution Model,\u201d and Chapter 2. And you use the same tools, such as the C# compiler and AL.exe, to build both kinds of assemblies. The real difference between weakly named and strongly named assemblies is that a strongly named assembly is signed with a publisher\u2019s public/private key pair that uniquely identifies the assembly\u2019s publisher. This key pair allows the assembly to be uniquely identified, secured, and versioned, and it allows the assembly to be deployed anywhere on the user\u2019s machine or even on the Internet. This ability to uniquely identify an assembly allows the CLR to enforce certain known-to-besafe policies when an application tries to bind to a strongly named assembly. This chapter is dedicated to explaining what strongly named assemblies are and what policies the CLR applies to them.</p><p>An assembly can be deployed in two ways: privately or globally. A privately deployed assembly is an assembly that is deployed in the application\u2019s base directory or one of its subdirectories. A weakly named assembly can be deployed only privately. I talked about privately deployed assemblies in Chapter 2. A globally deployed assembly is an assembly that is deployed into some well-known location that the CLR looks in when it\u2019s searching for the assembly. A strongly named assembly can be deployed privately or globally. I\u2019ll explain how to create and deploy strongly named assemblies in this chapter. Table 3-1 summarizes the kinds of assemblies and the ways that they can be deployed.</p><figcaption>TABLE 3-1 How Weakly and Strongly Named Assemblies Can Be Deployed</figcaption><table border=\"1\">\n<tr>\n<td><p>Kind of Assembly Weakly named</p>\n</td>\n<td><p>Can Be Privately Deployed Yes</p>\n</td>\n<td><p>Can Be Globally Deployed No</p>\n</td>\n</tr>\n<tr>\n<td><p>Strongly named</p>\n</td>\n<td><p>Yes</p>\n</td>\n<td><p>Yes</p>\n</td>\n</tr>\n</table><h6>Giving an Assembly a Strong Name</h6><p>If multiple applications are going to access an assembly, the assembly must be placed in a well-known directory, and the CLR must know to look in this directory automatically when a reference to the assembly is detected. However, we have a problem: Two (or more) companies could produce assemblies that have the same file name. Then, if both of these assemblies get copied into the same well-known directory, the last one installed wins, and all of the applications that were using the old assembly no longer function as desired. (This is exactly why DLL hell exists today in Windows, in which shared DLLs are all just copied into the System32 directory.)</p><p>Obviously, differentiating assemblies simply by using a file name isn\u2019t good enough. The CLR needs to support some mechanism that allows assemblies to be uniquely identified. This is what the term strongly named assembly refers to. A strongly named assembly consists of four attributes that uniquely identify the assembly: a file name (without an extension), a version number, a culture identity, and a public key. Because public keys are very large numbers, we frequently use a small hash value derived from a public key. This hash value is called a public key token. The following assembly identity strings (sometimes called an assembly display name) identify four completely different assembly files.</p><p>\"MyTypes, Version=1.0.8123.0, Culture=neutral, PublicKeyToken=b77a5c561934e089\"</p><ul style=\"list-style-type: none;\">\n<li><p>\"MyTypes, Version=1.0.8123.0, Culture=\"en\u00adUS\", PublicKeyToken=b77a5c561934e089\"</p></li>\n<li><p>\"MyTypes, Version=2.0.1234.0, Culture=neutral, PublicKeyToken=b77a5c561934e089\"</p></li>\n</ul><p>\"MyTypes, Version=1.0.8123.0, Culture=neutral, PublicKeyToken=b03f5f7f11d50a3a\"</p><p>The first string identifies an assembly file called MyTypes.exe or MyTypes.dll (you can\u2019t actually determine the file extension from an assembly identity string). The company producing the assembly is creating version 1.0.8123.0 of this assembly, and nothing in the assembly is sensitive to any one culture because Culture is set to neutral. Of course, any company could produce a MyTypes.dll (or MyTypes.exe) assembly file that is marked with a version number of 1.0.8123.0 and a neutral culture.</p><p>There must be a way to distinguish this company\u2019s assembly from another company\u2019s assembly that happens to have the same attributes. For several reasons, Microsoft chose to use standard public/ private key cryptographic technologies instead of any other unique identification technique such as GUIDs, URLs, or URNs. Specifically, cryptographic techniques provide a way to check the integrity of the assembly\u2019s bits as they are installed on a machine, and they also allow permissions to be granted on a per-publisher basis. I\u2019ll discuss these techniques later in this chapter. So a company that wants to uniquely mark its assemblies must create a public/private key pair. Then the public key can be associated with the assembly. No two companies should have the same public/private key pair, and this distinction is what allows two companies to create assemblies that have the same name, version, and culture without causing any conflict.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile29.png\"/><p>Note The System.Reflection.AssemblyName class is a helper class that makes it easy for you to build an assembly name and to obtain the various parts of an assembly\u2019s name. The class offers several public instance properties, such as CultureInfo, FullName, KeyPair, Name, and Version. The class also offers a few public instance methods, such as GetPublicKey, GetPublicKeyToken, SetPublicKey, and SetPublicKeyToken.</p><p>In Chapter 2, I showed you how to name an assembly file and how to apply an assembly version number and a culture. A weakly named assembly can have assembly version and culture attributes embedded in the manifest metadata; however, the CLR always ignores the version number and uses only the culture information when it\u2019s probing subdirectories looking for the satellite assembly. Because weakly named assemblies are always privately deployed, the CLR simply uses the name of the assembly (tacking on a .dll or an .exe extension) when searching for the assembly\u2019s file in the application\u2019s base directory or in any of the application\u2019s subdirectories specified in the Extensible Markup Language (XML) configuration file\u2019s probing element\u2019s privatePath XML attribute.</p><p>A strongly named assembly has a file name, an assembly version, and a culture. In addition, a strongly named assembly is signed with the publisher\u2019s private key.</p><p>The first step in creating a strongly named assembly is to obtain a key by using the Strong Name utility, SN.exe, that ships with the .NET Framework SDK and Microsoft Visual Studio. This utility offers a whole slew of features depending on the command-line switch you specify. Note that all SN.exe\u2019s command-line switches are case-sensitive. To generate a public/private key pair, you run SN.exe as follows.</p><p>SN \u2013k MyCompany.snk</p><p>This line tells SN.exe to create a file called MyCompany.snk. This file will contain the public and private key numbers persisted in a binary format.</p><p>Public key numbers are very big. If you want to, after creating the file that contains the public and private key, you can use the SN.exe utility again to see the actual public key. To do this, you must execute the SN.exe utility twice. First, you invoke SN.exe with the \u2013p switch to create a file that contains only the public key (MyCompany.PublicKey).1</p><p>SN \u2013p MyCompany.snk MyCompany.PublicKey sha256</p><p>Then, you invoke SN.exe, passing it the \u2013tp switch and the file that contains just the public key.</p><p>SN \u2013tp MyCompany.PublicKey</p><p>When I execute this line, I get the following output.</p><p>Microsoft (R) .NET Framework Strong Name Utility Version 4.0.30319.17929 Copyright (c) Microsoft Corporation. All rights reserved.</p><p>Public key (hash algorithm: sha256): 00240000048000009400000006020000002400005253413100040000010001003f9d621b702111 850be453b92bd6a58c020eb7b804f75d67ab302047fc786ffa3797b669215afb4d814a6f294010 b233bac0b8c8098ba809855da256d964c0d07f16463d918d651a4846a62317328cac893626a550 69f21a125bc03193261176dd629eace6c90d36858de3fcb781bfc8b817936a567cad608ae672b6 1fb80eb0</p><p>Public key token is 3db32f38c8b42c9a</p><p>The SN.exe utility doesn\u2019t offer any way for you to display the private key. The size of public keys makes them difficult to work with. To make things easier for the developer</p><p>(and for end users too), public key tokens were created. A public key token is a 64-bit hash of the public key. SN.exe\u2019s \u2013tp switch shows the public key token that corresponds to the complete public key at the end of its output.</p><p>Now that you know how to create a public/private key pair, creating a strongly named assembly is simple. When you compile your assembly, you use the /keyfile:&lt;file&gt; compiler switch.</p><p>csc /keyfile:MyCompany.snk Program.cs</p><p>When the C# compiler sees this switch, the compiler opens the specified file (MyCompany.snk), signs the assembly with the private key, and embeds the public key in the manifest. Note that you sign only the assembly file that contains the manifest; the assembly\u2019s other files can\u2019t be signed explicitly.</p><p>If you are using Visual Studio, you can create a new public/private key file by displaying the properties for your project, clicking the Signing tab, selecting the Sign The Assembly check box, and then choosing the &lt;New\u2026&gt; option from the Choose A Strong Name Key File combo box.</p><p>1 In this example, I am using Enhanced Strong Naming, which was introduced in .NET Framework 4.5. If you need to</p><p>produce an assembly that is compatible with previous versions of the .NET Framework, then you will also have to create a counter-signature by using the AssemblySignatureKeyAttribute. For details about this, see http://msdn.microsoft.com/ en-us/library/hh415055(v=vs.110).aspx.</p><p>Here\u2019s what it means to sign a file: When you build a strongly named assembly, the assembly\u2019s FileDef manifest metadata table includes the list of all the files that make up the assembly. As each file\u2019s name is added to the manifest, the file\u2019s contents are hashed, and this hash value is stored along with the file\u2019s name in the FileDef table. You can override the default hash algorithm used with AL.exe\u2019s /algid switch or apply the assembly-level System.Reflection.AssemblyAlgorithmIdAttribute custom attribute in one of the assembly\u2019s source code files. By default, a SHA-1 algorithm is used.</p><p>After the PE file containing the manifest is built, the PE file\u2019s entire contents (except for any Authenticode Signature, the assembly\u2019s strong name data, and the PE header checksum) are hashed, as shown in Figure 3-1. This hash value is signed with the publisher\u2019s private key, and the resulting RSA digital signature is stored in a reserved section (not included in the hash) within the PE file. The CLR header of the PE file is updated to reflect where the digital signature is embedded within the file.</p><p>Calculus.dll</p><p>IL Metadata Manifest Assembly files (self and RUT.netmodule) Exported types (self and RUT.netmodule)</p><p>RSA digital Hash signature</p><p>Hash value</p><p>Signed with private key</p><p>PE file</p><p>Public key</p><p>Embedded in PE file</p><p>CLR header RSA digital signature</p><p>Embedded in PE file</p><p>Public key</p><p>FIGURE 3-1 Signing an assembly.</p><p>The publisher\u2019s public key is also embedded into the AssemblyDef manifest metadata table in this PE file. The combination of the file name, the assembly version, the culture, and the public key gives this assembly a strong name, which is guaranteed to be unique. There is no way that two companies could each produce an assembly named OurLibrary with the same public/private keys unless the companies share this key pair with each other.</p><p>At this point, the assembly and all of its files are ready to be packaged and distributed. As described in Chapter 2, when you compile your source code, the compiler detects the types and members that your code references. You must specify the referenced assemblies to the compiler. For the C# compiler, you use the /reference compiler switch. Part of the compiler\u2019s job is to emit an AssemblyRef metadata table inside the resulting managed module. Each entry in the AssemblyRef metadata table indicates the referenced assembly\u2019s name (without path and extension), version number, culture, and public key information.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile30.png\"/><p>Important Because public keys are such large numbers, and a single assembly might reference many assemblies, a large percentage of the resulting file\u2019s total size would be occupied with public key information. To conserve storage space, Microsoft hashes the public key and takes the last 8 bytes of the hashed value. These reduced public key values\u2014known as public key tokens\u2014are what are actually stored in an AssemblyRef table. In general, developers and end users will see public key token values much more frequently than full public key values.</p><p>Note, however, that the CLR never uses public key tokens when making security or trust decisions because it is possible that several public keys could hash to a single public key token.</p><p>The AssemblyRef metadata information (obtained by using ILDasm.exe) for a simple class library DLL file is shown here.</p><p>AssemblyRef #1 (23000001) \u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00adToken: 0x23000001 Public Key or Token: b7 7a 5c 56 19 34 e0 89 Name: mscorlib Version: 4.0.0.0 Major Version: 0x00000004 Minor Version: 0x00000000 Build Number: 0x00000000 Revision Number: 0x00000000 Locale: &lt;null&gt; HashValue Blob: Flags: [none] (00000000)</p><p>From this, you can see that the DLL assembly references a type that is contained in an assembly matching the following attributes.</p><p>\"MSCorLib, Version=4.0.0.0, Culture=neutral, PublicKeyToken=b77a5c561934e089\"</p><p>Unfortunately, ILDasm.exe uses the term Locale when it really should be using Culture. If you look at the DLL assembly\u2019s AssemblyDef metadata table, you see the following.</p><p>Assembly \u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\u00adToken: 0x20000001 Name : SomeClassLibrary Public Key : Hash Algorithm : 0x00008004 Version: 3.0.0.0 Major Version: 0x00000003 Minor Version: 0x00000000 Build Number: 0x00000000 Revision Number: 0x00000000 Locale: &lt;null&gt; Flags : [none] (00000000)</p><p>This is equivalent to the following.</p><p>\"SomeClassLibrary, Version=3.0.0.0, Culture=neutral, PublicKeyToken=null\"</p><p>In this line, no public key token is specified because the DLL assembly wasn\u2019t signed with a public/ private key pair, making it a weakly named assembly. If I had used SN.exe to create a key file compiled with the /keyfile compiler switch, the resulting assembly would have been signed. If I had then used ILDasm.exe to explore the new assembly\u2019s metadata, the AssemblyDef entry would have bytes appearing after the Public Key field, and the assembly would be strongly named. By the way, the AssemblyDef entry always stores the full public key, not the public key token. The full public key is necessary to ensure that the file hasn\u2019t been tampered with. I\u2019ll explain the tamper resistance of strongly named assemblies later in this chapter.</p><h6>The Global Assembly Cache</h6><p>Now that you know how to create a strongly named assembly, it\u2019s time to learn how to deploy this assembly and how the CLR uses the information to locate and load the assembly.</p><p>If an assembly is to be accessed by multiple applications, the assembly must be placed into a wellknown directory, and the CLR must know to look in this directory automatically when a reference to the assembly is detected. This well-known location is called the global assembly cache (GAC). The exact location of the GAC is an implementation detail that is subject to change with different versions of the .NET Framework. However, you can typically find it in the following directory:</p><p>%SystemRoot%\\Microsoft.NET\\Assembly The GAC directory is structured. It contains many subdirectories, and an algorithm is used to gen-</p><p>erate the names of these subdirectories. You should never manually copy assembly files into the GAC; instead, you should use tools to accomplish this task. These tools know the GAC\u2019s internal structure and how to generate the proper subdirectory names.</p><p>While developing and testing, you can use the most common tool for installing a strongly named assembly into the GAC, which is GACUtil.exe. Running this tool without any command-line arguments yields the following usage.</p><p>Microsoft (R) .NET Global Assembly Cache Utility. Version 4.0.30319.17929 Copyright (c) Microsoft Corporation. All rights reserved.</p><p>Usage: Gacutil &lt;command&gt; [ &lt;options&gt; ] Commands:</p><p>/i &lt;assembly_path&gt; [ /r &lt;...&gt; ] [ /f ] Installs an assembly to the global assembly cache.</p><p>/il &lt;assembly_path_list_file&gt; [ /r &lt;...&gt; ] [ /f ] Installs one or more assemblies to the global assembly cache.</p><p>/u &lt;assembly_display_name&gt; [ /r &lt;...&gt; ] Uninstalls an assembly from the global assembly cache.</p><p>/ul &lt;assembly_display_name_list_file&gt; [ /r &lt;...&gt; ] Uninstalls one or more assemblies from the global assembly cache.</p><p>/l [ &lt;assembly_name&gt; ] List the global assembly cache filtered by &lt;assembly_name&gt;</p><p>/lr [ &lt;assembly_name&gt; ] List the global assembly cache with all traced references.</p><p>/cdl Deletes the contents of the download cache</p><p>/ldl Lists the contents of the download cache</p><p>/?</p><p>Displays a detailed help screen</p><p>Options: /r &lt;reference_scheme&gt; &lt;reference_id&gt; &lt;description&gt; Specifies a traced reference to install (/i, /il) or uninstall (/u, /ul).</p><p>/f</p><p>Forces reinstall of an assembly.</p><p>/nologo Suppresses display of the logo banner</p><p>/silent Suppresses display of all output</p><p>As you can see, you can invoke GACUtil.exe, specifying the /i switch to install an assembly into the GAC, and you can use GACUtil.exe\u2019s /u switch to uninstall an assembly from the GAC. Note that you can\u2019t ever place a weakly named assembly into the GAC. If you pass the file name of a weakly named assembly to GACUtil.exe, it displays the following error message: Failure adding assembly to the cache: Attempt to install an assembly without a strong name.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile31.png\"/><p>Note By default, the GAC can be manipulated only by a user belonging to the Windows Administrators group. GACUtil.exe will fail to install or uninstall an assembly if the user invoking the execution of the utility isn\u2019t a member of this group.</p><p>Using GACUtil.exe\u2019s /i switch is very convenient for developer testing. However, if you use GACUtil.exe to deploy an assembly in a production environment, it\u2019s recommended that you use GACUtil.exe\u2019s /r switch in addition to specifying the /i or /u switch to install or uninstall the assembly. The /r switch integrates the assembly with the Windows install and uninstall engine. Basically, it tells the system which application requires the assembly and then ties the application and the assembly together.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile32.png\"/><p>Note If a strongly named assembly is packaged in a cabinet (.cab) file or is compressed in some way, the assembly\u2019s file must first be decompressed to temporary file(s) before you use GACUtil.exe to install the assembly\u2019s files into the GAC. After the assembly\u2019s files have been installed, the temporary file(s) can be deleted.</p><p>The GACUtil.exe tool doesn\u2019t ship with the end-user .NET Framework redistributable package. If</p><p>your application includes some assemblies that you want deployed into the GAC, you should use the Windows Installer (MSI), because MSI is the only tool that is guaranteed to be on end-user machines and capable of installing assemblies into the GAC.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile33.png\"/><p>Important Globally deploying assembly files into the GAC is a form of registering the assembly, although the actual Windows registry isn\u2019t affected in any way. Installing assemblies into the GAC breaks the goal of simple application installation, backup, restore, moving, and uninstall. So it is recommended that you avoid global deployment and use private deployment whenever possible.</p><p>What is the purpose of \u201cregistering\u201d an assembly in the GAC? Well, say two companies each produce an OurLibrary assembly consisting of one file: OurLibrary.dll. Obviously, both of these files can\u2019t go in the same directory because the last one installed would overwrite the first one, surely breaking some application. When you install an assembly into the GAC, dedicated subdirectories are created under the %SystemRoot%\\Microsoft.NET\\Assembly directory, and the assembly files are copied into one of these subdirectories.</p><p>Normally, no one examines the GAC\u2019s subdirectories, so the structure of the GAC shouldn\u2019t really matter to you. As long as the tools and the CLR know the structure, all is good.</p><h6>Building an Assembly That References a Strongly Named Assembly</h6><p>Whenever you build an assembly, the assembly will have references to other strongly named assemblies. This is true because System.Object is defined in MSCorLib.dll, which is strongly named. However, it\u2019s likely that an assembly will reference types in other strongly named assemblies published either by Microsoft, a third party, or your own organization. In Chapter 2, I showed you how to use CSC.exe\u2019s /reference compiler switch to specify the assembly file names you want to reference. If the file name is a full path, CSC.exe loads the specified file and uses its metadata information to build the assembly. As mentioned in Chapter 2, if you specify a file name without a path, CSC.exe attempts to find the assembly by looking in the following directories (in order of their presentation here):</p><ul style=\"list-style-type: none;\">\n<li><p>1. Working directory.</p></li>\n<li><p>2. The directory that contains the CSC.exe file itself. This directory also contains the CLR DLLs.</p></li>\n</ul><ul style=\"list-style-type: none;\">\n<li><p>3. Any directories specified using the /lib compiler switch.</p></li>\n<li><p>4. Any directories specified using the LIB environment variable.</p></li>\n</ul><p>So if you\u2019re building an assembly that references Microsoft\u2019s System.Drawing.dll, you can specify the /reference:System.Drawing.dll switch when invoking CSC.exe. The compiler will examine the directories shown earlier and will find the System.Drawing.dll file in the directory that contains the CSC.exe file itself, which is the same directory that contains the DLLs for the version of the CLR the compiler is tied to. Even though this is the directory where the assembly is found at compile time, this isn\u2019t the directory where the assembly will be loaded from at run time.</p><p>You see, when you install the .NET Framework, two copies of Microsoft\u2019s assembly files are actually</p><p>installed. One set is installed into the compiler/CLR directory, and another set is installed into a GAC subdirectory. The files in the compiler/CLR directory exist so that you can easily build your assembly, whereas the copies in the GAC exist so that they can be loaded at run time.</p><p>The reason that CSC.exe doesn\u2019t look in the GAC for referenced assemblies is that you\u2019d have to know the path to the assembly file and the structure of the GAC is undocumented. Alternatively, CSC.exe could allow you to specify a still long but slightly nicer-looking string, such as \u201cSystem.Drawing, Version=4.0.0.0, Culture=neutral, PublicKeyToken=b03f5f7f11d50a3a.\u201d Both of these solutions were deemed worse than having the assembly files installed twice on the user\u2019s hard drive.</p><p>In addition, the assemblies in the compiler/CLR directory are machine agnostic. That is, these assemblies contain only metadata in them. Because the IL code is not required at build time, this directory does not have to contain x86, x64, and ARM versions of an assembly. The assemblies in the GAC contain metadata and IL code because the code is needed only at run time. And, because the code can be fine-tuned for a specific CPU architecture, the GAC allows multiple copies of an assembly to reside within it; each copy is located under a different subdirectory for each CPU architecture.</p><h6>Strongly Named Assemblies Are Tamper-Resistant</h6><p>Signing an assembly with a private key and embedding the signature and public key within an assembly allows the CLR to verify that the assembly has not been modified or corrupted. When an assembly is installed into the GAC, the system hashes the contents of the file containing the manifest and compares the hash value with the RSA digital signature value embedded within the PE file (after unsigning it with the public key). If the values are identical, the file\u2019s contents haven\u2019t been tampered with. In addition, the system hashes the contents of the assembly\u2019s other files and compares the hash values with the hash values stored in the manifest file\u2019s FileDef table. If any of the hash values don\u2019t match, at least one of the assembly\u2019s files has been tampered with, and the assembly will fail to install into the GAC.</p><p>When an application needs to bind to an assembly, the CLR uses the referenced assembly\u2019s properties (name, version, culture, and public key) to locate the assembly in the GAC. If the referenced assembly can be found, its containing subdirectory is returned, and the file holding the manifest is loaded. Finding the assembly this way assures the caller that the assembly loaded at run time came</p><p>from the same publisher that built the assembly the code was compiled against. This assurance is possible because the public key token in the referencing assembly\u2019s AssemblyRef table corresponds to the public key in the referenced assembly\u2019s AssemblyDef table. If the referenced assembly isn\u2019t in the GAC, the CLR looks in the application\u2019s base directory and then in any of the private paths identified in the application\u2019s configuration file; then, if the application was installed using MSI, the CLR asks MSI to locate the assembly. If the assembly can\u2019t be found in any of these locations, the bind fails, and a System.IO.FileNotFoundException is thrown.</p><p>When strongly named assembly files are loaded from a location other than the GAC (via the application\u2019s base directory or via a codeBase element in a configuration file), the CLR compares hash values when the assembly is loaded. In other words, a hash of the file is performed every time an application executes and loads the assembly. This performance hit is a tradeoff for being certain that the assembly file\u2019s content hasn\u2019t been tampered with. When the CLR detects mismatched hash values at run time, it throws a System.IO.FileLoadException.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile34.png\"/><p>Note When a strongly named assembly is installed in the GAC, the system ensures that the file containing the manifest hasn\u2019t been tampered with. This check occurs only once, at installation time. In addition, to improve performance, the CLR does not check if a strongly named assembly has been tampered with if the assembly is fully trusted and is being loaded into a fully trusted AppDomain. On the other hand, when a strongly named assembly is loaded from a directory other than the GAC, the CLR verifies the assembly\u2019s manifest file to ensure that the file\u2019s contents have not been tampered with, causing an additional performance hit every time this file is loaded.</p><h6>Delayed Signing</h6><p>Earlier in this chapter, I discussed how the SN.exe tool can produce public/private key pairs. This tool generates the keys by making calls into the Crypto API provided by Windows. These keys can be stored in files or other storage devices. For example, large organizations (such as Microsoft) will maintain the returned private key in a hardware device that stays locked in a vault; only a few people in the company have access to the private key. This precaution prevents the private key from being compromised and ensures the key\u2019s integrity. The public key is, well, public and freely distributed.</p><p>When you\u2019re ready to package your strongly named assembly, you\u2019ll have to use the secure private key to sign it. However, while developing and testing your assembly, gaining access to the secure private key can be a hassle. For this reason, the .NET Framework supports delayed signing, sometimes referred to as partial signing. Delayed signing allows you to build an assembly by using only your company\u2019s public key; the private key isn\u2019t necessary. Using the public key allows assemblies that reference your assembly to embed the correct public key value in their AssemblyRef metadata entries. It also allows the assembly to be placed in the GAC appropriately. If you don\u2019t sign the file with your company\u2019s private key, you lose all of the tampering protection afforded to you because the assembly\u2019s files won\u2019t be hashed, and a digital signature won\u2019t be embedded in the file. This loss of protection shouldn\u2019t be</p><p>a problem, however, because you use delayed signing only while developing your own assembly, not when you\u2019re ready to package and deploy the assembly.</p><p>Basically, you get your company\u2019s public key value in a file and pass the file name to whatever utility you use to build the assembly. (As I have shown earlier in this chapter, you can use SN.exe\u2019s \u2013p switch to extract a public key from a file that contains a public/private key pair.) You must also tell the tool that you want the assembly to be delay signed, meaning that you\u2019re not supplying a private key. For the C# compiler, you do this by specifying the /delaysign compiler switch. In Visual Studio, you display the properties for your project, click the Signing tab, and then select the Delay Sign Only check box. If you\u2019re using AL.exe, you can specify the /delay[sign] commandline switch.</p><p>When the compiler or AL.exe detects that you\u2019re delay signing an assembly, it will emit the assembly\u2019s AssemblyDef manifest entry, which will contain the assembly\u2019s public key. Again, the presence of the public key allows the assembly to be placed in the GAC. It also allows you to build other assemblies that reference this assembly; the referencing assemblies will have the correct public key in their AssemblyRef metadata table entries. When creating the resulting assembly, space is left in the resulting PE file for the RSA digital signature. (The utility can determine how much space is necessary from the size of the public key.) Note that the file\u2019s contents won\u2019t be hashed at this time either.</p><p>At this point, the resulting assembly doesn\u2019t have a valid signature. Attempting to install the assembly into the GAC will fail because a hash of the file\u2019s contents hasn\u2019t been done\u2014the file appears to have been tampered with. On every machine on which the assembly needs to be installed into the GAC, you must prevent the system from verifying the integrity of the assembly\u2019s files. To do this, you use the SN.exe utility, specifying the \u2013Vr command-line switch. Executing SN.exe with this switch also tells the CLR to skip checking hash values for any of the assembly\u2019s files when loaded at run time. Internally, SN\u2019s \u2013Vr switch adds the assembly\u2019s identity under the following registry subkey: HKEY_LOCAL_MACHINE\\SOFTWARE\\Microsoft\\StrongName\\Verification.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile35.png\"/><p>Important When using any utility that manipulates the registry, make sure that you run the 64-bit version of the utility on a 64-bit machine. By default, the 32-bit utilities are installed in C:\\Program Files (x86)\\Microsoft SDKs\\Windows\\v8.0A\\bin\\NETFX 4.0 Tools, and the 64-bit utilities are installed in C:\\Program Files (x86)\\Microsoft SDKs\\Windows\\v8.0A\\ bin\\NETFX 4.0 Tools\\x64.</p><p>When you\u2019re finished developing and testing the assembly, you need to officially sign it so that you can package and deploy it. To sign the assembly, use the SN.exe utility again, this time with the \u2013R switch and the name of the file that contains the actual private key. The \u2013R switch causes SN.exe to hash the file\u2019s contents, sign it with the private key, and embed the RSA digital signature in the file where the space for it had previously been reserved. After this step, you can deploy the fully signed assembly. On the developing and testing machines, don\u2019t forget to turn verification of this assembly back on by using SN.exe\u2019s \u2013Vu or \u2013Vx command-line switch.</p><p>The following list summarizes the steps discussed in this section to develop your assembly by using the delayed signing technique:</p><ul style=\"list-style-type: none;\">\n<li><p>1. While developing an assembly, obtain a file that contains only your company\u2019s public key, and compile your assembly by using the /keyfile and /delaysign compiler switches.</p><p>csc /keyfile:MyCompany.PublicKey /delaysign MyAssembly.cs</p>\n</li>\n<li><p>2. After building the assembly, execute the following line so that the CLR will trust the assembly\u2019s bytes without performing the hash and comparison. This allows you to install the assembly in the GAC (if you desire). Now, you can build other assemblies that reference the assembly, and you can test the assembly. Note that you have to execute the following command line only once per machine; it\u2019s not necessary to perform this step each time you build your assembly.</p><p>SN.exe \u2013Vr MyAssembly.dll</p>\n</li>\n<li><p>3. When ready to package and deploy the assembly, obtain your company\u2019s private key, and then execute the following line. You can install this new version in the GAC if you desire, but don\u2019t attempt to install it in the GAC until executing step 4.</p><p>SN.exe \u00adRa MyAssembly.dll MyCompany.PrivateKey</p>\n</li>\n<li><p>4. To test in real conditions, turn verification back on by executing the following command line.</p></li>\n</ul><p>SN.exe \u2013Vu MyAssembly.dll</p><p>At the beginning of this section, I mentioned how organizations keep their key pairs in a hardware device such as a smart card. To keep these keys secure, you must make sure that the key values are never persisted in a disk file. Cryptographic service providers (CSPs) offer containers that abstract the location of these keys. Microsoft, for example, uses a CSP that has a container that, when accessed, obtains the private key from a hardware device.</p><p>If your public/private key pair is in a CSP container, you\u2019ll have to specify different switches to the CSC.exe, AL.exe, and SN.exe programs: When compiling (CSC.exe), specify the /keycontainer switch instead of the /keyfile switch; when linking (AL.exe), specify its /keyname switch instead of its /keyfile switch; and when using the Strong Name program (SN.exe) to add a private key to a delay-signed assembly, specify the \u2013Rc switch instead of the \u2013R switch. SN.exe offers additional switches that allow you to perform operations with a CSP.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile36.png\"/><p>Important Delayed signing is also useful whenever you want to perform some other operation to an assembly before you package it. For example, you may want to run an obfuscator over your assembly. You can\u2019t obfuscate an assembly after it\u2019s been fully signed because the hash value will be incorrect. So, if you want to obfuscate an assembly file or perform any other type of post-build operation, you should use delayed signing, perform the post-build operation, and then run SN.exe with the \u2013R or \u2013Rc switch to complete the signing process of the assembly with all of its hashing.</p><h6>Privately Deploying Strongly Named Assemblies</h6><p>Installing assemblies into the GAC offers several benefits. The GAC enables many applications to share assemblies, reducing physical memory usage on the whole. In addition, it\u2019s easy to deploy a new version of the assembly into the GAC and have all applications use the new version via a publisher policy (described later in this chapter). The GAC also provides side-by-side management for an assembly\u2019s different versions. However, the GAC is usually secured so that only an administrator can install an assembly into it. Also, installing into the GAC breaks the simple copy deployment story.</p><p>Although strongly named assemblies can be installed into the GAC, they certainly don\u2019t have to be. In fact, it\u2019s recommended that you deploy assemblies into the GAC only if the assembly is intended to be shared by many applications. If an assembly isn\u2019t intended to be shared, it should be deployed privately. Deploying privately preserves the simple copy install deployment story and better isolates the application and its assemblies. Also, the GAC isn\u2019t intended to be the new C:\\Windows\\System32 dumping ground for common files. The reason is because new versions of assemblies don\u2019t overwrite each other; they are installed side by side, eating up disk space.</p><p>In addition to deploying a strongly named assembly in the GAC or privately, a strongly named assembly can be deployed to some arbitrary directory that a small set of applications know about. For example, you might be producing three applications, all of which want to share a strongly named assembly. Upon installation, you can create four directories: one for each application and an additional directory for the assembly you want shared. When you install each application into its directory, also install an XML configuration file, and have the shared assembly\u2019s codeBase element indicate the path of the shared assembly. Now at run time, the CLR will know to look in the strongly named assembly\u2019s directory for the shared assembly. For the record, this technique is rarely used and is somewhat discouraged because no single application controls when the assembly\u2019s files should be uninstalled.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile37.png\"/><p>Note The configuration file\u2019s codeBase element actually identifies a URL. This URL can refer to any directory on the user\u2019s machine or to a Web address. In the case of a Web address, the CLR will automatically download the file and store it in the user\u2019s download cache (a subdirectory under C:\\Users\\UserName\\Local Settings\\Application Data\\Assembly, where UserName is the name of the Windows user account currently signed on). When referenced in the future, the CLR will compare the time stamp of the downloaded file with the time stamp of the file at the specified URL. If the time stamp of the file at the URL is newer, the CLR will download the new version of the file and load it. If the previously downloaded file is newer, the CLR will load this file and will not download the file again (improving performance). An example of a configuration file containing a codeBase element is shown later in this chapter.</p><h6>How the Runtime Resolves Type References</h6><p>At the beginning of Chapter 2, we saw the following source code.</p><p>public sealed class Program { public static void Main() {</p><p>System.Console.WriteLine(\"Hi\"); }</p><p>}</p><p>This code is compiled and built into an assembly, say Program.exe. When you run this application, the CLR loads and initializes. Then the CLR reads the assembly\u2019s CLR header, looking for the MethodDefToken that identifies the application\u2019s entry point method (Main). From the MethodDef metadata table, the offset within the file for the method\u2019s IL code is located and JIT-compiled into native code, which includes having the code verified for type safety. The native code then starts executing. Following is the IL code for the Main method. To obtain this output, I ran ILDasm.exe, chose the View menu\u2019s Show Bytes menu item, and then double-clicked the Main method in the tree view.</p><p>.method public hidebysig static void Main() cil managed // SIG: 00 00 01 {</p><p>.entrypoint // Method begins at RVA 0x2050 // Code size 11 (0xb)</p><p>.maxstack 8 IL_0000: /* 72 | (70)000001 */</p><p>ldstr \"Hi\" IL_0005: /* 28 | (0A)000003 */</p><p>call void [mscorlib]System.Console::WriteLine(string) IL_000a: /* 2A | */</p><p>ret } // end of method Program::Main</p><p>When JIT-compiling this code, the CLR detects all references to types and members and loads their defining assemblies (if not already loaded). As you can see, the preceding IL code has a reference to System.Console.WriteLine. Specifically, the IL call instruction references metadata token 0A000003. This token identifies entry 3 in the MemberRef metadata table (table 0A). The CLR looks up this MemberRef entry and sees that one of its fields refers to an entry in a TypeRef table (the System.Console type). From the TypeRef entry, the CLR is directed to an AssemblyRef entry: \u201cmscorlib, Version=4.0.0.0, Culture=neutral, PublicKeyToken=b77a5c561934e089\u201d. At this point, the CLR knows which assembly it needs. Now the CLR must locate the assembly in order to load it.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile38.png\"/><p>When resolving a referenced type, the CLR can find the type in one of three places:</p><p>\u25a0\u25a0 Same file Access to a type that is in the same file is determined at compile time (sometimes</p><p>referred to as early bound). The type is loaded out of the file directly, and execution continues.</p><p>\u25a0\u25a0 Different file, same assembly The runtime ensures that the file being referenced is, in fact, in the assembly\u2019s ModuleRef table of the current assembly\u2019s manifest. The runtime then looks in the directory where the assembly\u2019s manifest file was loaded. The file is loaded, its hash value is checked to ensure the file\u2019s integrity, the type\u2019s member is found, and execution continues.</p><p>\u25a0\u25a0 Different file, different assembly When a referenced type is in a different assembly\u2019s file, the runtime loads the file that contains the referenced assembly\u2019s manifest. If this file doesn\u2019t contain the type, the appropriate file is loaded. The type\u2019s member is found, and execution continues.</p><p>Note The ModuleDef, ModuleRef, and FileDef metadata tables refer to files using the file\u2019s name and its extension. However, the AssemblyRef metadata table refers to assemblies by file name without an extension. When binding to an assembly, the system automatically appends .dll and .exe file extensions while attempting to locate the file by probing the directories as mentioned in the \u201cSimple Administrative Control (Configuration)\u201d section in Chapter 2.</p><p>If any errors occur while resolving a type reference\u2014file can\u2019t be found, file can\u2019t be loaded, hash mismatch, and so on\u2014an appropriate exception is thrown.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile39.png\"/><p>Note If you want, your code can register callback methods with System.AppDomain\u2019s AssemblyResolve, ReflectionOnlyAssemblyResolve, and TypeResolve events. In your callback methods, you can execute code that resolves the binding problem and allows the application to continue running without throwing an exception.</p><p>In the previous example, the CLR determines that System.Console is implemented in a different assembly than the caller. The CLR must search for the assembly and load the PE file that contains the assembly\u2019s manifest. The manifest is then scanned to determine the PE file that implements the type. If the manifest file contains the referenced type, all is well. If the type is in another of the assembly\u2019s files, the CLR loads the other file and scans its metadata to locate the type. The CLR then creates its internal data structures to represent the type, and the JIT compiler completes the compilation for the Main method. Finally, the Main method can start executing.</p><h6>Figure 3-2 illustrates how type binding occurs.</h6><p>IL refers to a type</p><p>IL refers to a member</p><p>AssemblyRef: Type is in different file, different assembly</p><p>Strongly named assembly Search for</p><p>What does TypeRef entry indicate?</p><p>What does AssemblyRef entry indicate?</p><p>assembly in GAC and then AppBase</p><p>ModuleRef: Type is in different file, same assembly</p><p>Weakly named assembly</p><p>Search for assembly in AppBase</p><p>Load file with manifest</p><p>Examine ModuleRef table and load appropriate file</p><p>ModuleDef: Type is in same file, same assembly</p><p>Type in manifest file</p><p>What does ExportedTypesDef entry indicate?</p><p>Create internal type structure</p><p>Type not in manifest file</p><p>Load file</p><p>Note: If any operation fails, an appropriate exception is thrown.</p><p>FIGURE 3-2 Flowchart showing how, given IL code that refers to a method or type, the CLR uses metadata to locate the proper assembly file that defines a type.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile40.png\"/><p>Important Strictly speaking, the example just described isn\u2019t 100 percent correct. For references to methods and types defined in an assembly that does not ship with the .NET Framework, the discussion is correct. However, the .NET Framework assemblies (including MSCorLib.dll) are closely tied to the version of the CLR that\u2019s running. Any assembly that references .NET Framework assemblies always binds to the version that matches the CLR\u2019s version. This is called unification, and Microsoft does this because they test all of the .NET Framework assemblies with a particular version of the CLR; therefore, unifying the code stack helps ensure that applications will work correctly.</p><p>So in the previous example, the reference to System.Console\u2019s WriteLine method binds to whatever version of MSCorLib.dll matches the version of the CLR, regardless of what version of MSCorLib.dll is referenced in the assembly\u2019s AssemblyRef metadata table.</p><p>There is one more twist to this story: to the CLR, all assemblies are identified by name, version, culture, and public key. However, the GAC identifies assemblies by using name, version, culture, public key, and CPU architecture. When searching the GAC for an assembly, the CLR figures out what type of process the application is currently running in: 32-bit x86 (possibly using the WoW64 technology), 64-bit x64, or 32-bit ARM. Then, when searching the GAC for an assembly, the CLR first searches for a CPU architecture\u2013specific version of the assembly. If it does not find a matching assembly, it then searches for a CPU-agnostic version of the assembly.</p><p>In this section, you saw how the CLR locates an assembly when using a default policy. However, an administrator or the publisher of an assembly can override the default policy. In the next two sections, I\u2019ll describe how to alter the CLR\u2019s default binding policy.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile41.png\"/><p>Note The CLR supports the ability to move a type (class, structure, enum, interface, or delegate) from one assembly to another. For example, in the .NET Framework 3.5, the System.TimeZoneInfo class is defined in the System.Core.dll assembly. But in the .NET Framework 4, Microsoft moved this class to the MSCorLib.dll assembly. Normally, moving a type from one assembly to another would break applications. However, the CLR offers a System.Runtime.CompilerServices.TypeForwardedToAttribute attribute, which can be applied to the original assembly (such as System.Core.dll). The parameter that you pass to this attribute\u2019s constructor is of type System.Type and it indicates the new type (that is now defined in MSCorLib.dll) that applications should now use. The CLR\u2019s binder uses this information. Because the TypeForwardedToAttribute\u2019s constructor takes a Type, the assembly containing this attribute will be dependent on the new assembly defining the type.</p><p>If you take advantage of this feature, then you should also apply the attribute System.Runtime.CompilerServices.TypeForwardedFromAttribute to the type in the new assembly and pass to this attribute\u2019s constructor a string with the full name of the assembly that used to define the type. This attribute typically is used for tools, utilities, and serialization. Because the TypeForwardedFromAttribute\u2019s constructor takes a String, the assembly containing this attribute is not dependent on the assembly that used to define the type.</p><h6>Advanced Administrative Control (Configuration)</h6><p>In the section \u201cSimple Administrative Control (Configuration)\u201d in Chapter 2, I gave a brief introduction to how an administrator can affect the way the CLR searches and binds to assemblies. In that section, I demonstrated how a referenced assembly\u2019s files can be moved to a subdirectory of the application\u2019s base directory and how the CLR uses the application\u2019s XML configuration file to locate the moved files.</p><p>Having discussed only the probing element\u2019s privatePath attribute in Chapter 2, I\u2019m going to discuss the other XML configuration file elements in this section. Following is an XML configuration file.</p><p>&lt;?xml version=\"1.0\"?&gt; &lt;configuration&gt; &lt;runtime&gt;</p><p>&lt;assemblyBinding xmlns=\"urn:schemas\u00admicrosoft\u00adcom:asm.v1\"&gt; &lt;probing privatePath=\"AuxFiles;bin\\subdir\" /&gt;</p><p>&lt;dependentAssembly&gt;</p><p>&lt;assemblyIdentity name=\"SomeClassLibrary\" publicKeyToken=\"32ab4ba45e0a69a1\" culture=\"neutral\"/&gt;</p><p>&lt;bindingRedirect oldVersion=\"1.0.0.0\" newVersion=\"2.0.0.0\" /&gt;</p><p>&lt;codeBase version=\"2.0.0.0\" href=\"http://www.Wintellect.com/SomeClassLibrary.dll\" /&gt;</p><p>&lt;/dependentAssembly&gt;</p><p>&lt;dependentAssembly&gt;</p><p>&lt;assemblyIdentity name=\"TypeLib\" publicKeyToken=\"1f2e74e897abbcfe\" culture=\"neutral\"/&gt;</p><p>&lt;bindingRedirect oldVersion=\"3.0.0.0\u00ad3.5.0.0\" newVersion=\"4.0.0.0\" /&gt;</p><p>&lt;publisherPolicy apply=\"no\" /&gt;</p><p>&lt;/dependentAssembly&gt;</p><p>&lt;/assemblyBinding&gt;</p><p>&lt;/runtime&gt; &lt;/configuration&gt;</p><p>This XML file gives a wealth of information to the CLR. Here\u2019s what it says:</p><p>\u25a0\u25a0 probing element Look in the application base directory\u2019s AuxFiles and bin\\subdir subdirectories when trying to find a weakly named assembly. For strongly named assemblies, the CLR looks in the GAC or in the URL specified by the codeBase element. The CLR looks in the application\u2019s private paths for a strongly named assembly only if no codeBase element is specified.</p><p>\u25a0\u25a0 First dependentAssembly, assemblyIdentity, and bindingRedirect elements When attempting to locate version 1.0.0.0 of the culture-neutral SomeClassLibrary assembly published by the organization that controls the 32ab4ba45e0a69a1 public key token, locate version 2.0.0.0 of the same assembly instead.</p><p>\u25a0\u25a0 codeBase element When attempting to locate version 2.0.0.0 of the culture-neutral SomeClassLibrary assembly published by the organization that controls the 32ab4ba45e0a69a1</p><p>public key token, try to find it at the following URL: www.Wintellect.com/SomeClassLibrary.dll. Although I didn\u2019t mention it in Chapter 2, a codeBase element can also be used with weakly named assemblies. In this case, the assembly\u2019s version number is ignored and should be omitted from the XML\u2019s codeBase element. Also, the codeBase URL must refer to a directory under the application\u2019s base directory.</p><p>\u25a0\u25a0 Second dependentAssembly, assemblyIdentity, and bindingRedirect elements When attempting to locate version 3.0.0.0 through version 3.5.0.0 inclusive of the culture-neutral TypeLib assembly published by the organization that controls the 1f2e74e897abbcfe public key token, locate version 4.0.0.0 of the same assembly instead.</p><p>\u25a0\u25a0 publisherPolicy element If the organization that produces the TypeLib assembly has de-</p><p>ployed a publisher policy file (described in the next section), the CLR should ignore this file.</p><p>When compiling a method, the CLR determines the types and members being referenced. Using this information, the runtime determines, by looking in the referencing assembly\u2019s AssemblyRef table, the assembly that was originally referenced when the calling assembly was built. The CLR then looks up the assembly/version in the application\u2019s configuration file and applies any version number redirections; the CLR is now looking for this assembly/version.</p><p>If the publisherPolicy element's apply attribute is set to yes\u2014or if the element is omittedthe CLR examines the GAC for the new assembly/version and applies any version number redirections that the publisher of the assembly feels is necessary; the CLR is now looking for this assembly/version. I\u2019ll talk more about publisher policy in the next section. Finally, the CLR looks up the new assembly/ version in the machine\u2019s Machine.config file and applies any version number redirections there.</p><p>At this point, the CLR knows the version of the assembly that it should load, and it attempts to load the assembly from the GAC. If the assembly isn\u2019t in the GAC, and if there is no codeBase element, the CLR probes for the assembly as I described in Chapter 2. If the configuration file that performs the last redirection also contains a codeBase element, the CLR attempts to load the assembly from the codeBase element\u2019s specified URL.</p><p>Using these configuration files, an administrator can really control what assembly the CLR decides to load. If an application is experiencing a bug, the administrator can contact the publisher of the errant assembly. The publisher can send the administrator a new assembly that the administrator can install. By default, the CLR won\u2019t load this new assembly because the already-built assemblies don\u2019t reference the new version. However, the administrator can modify the application\u2019s XML configuration file to instruct the CLR to load the new assembly.</p><p>If the administrator wants all applications on the machine to pick up the new assembly, the administrator can modify the machine\u2019s Machine.config file instead, and the CLR will load the new assembly whenever an application refers to the old assembly.</p><p>If the new assembly doesn\u2019t fix the original bug, the administrator can delete the binding redirection lines from the configuration file, and the application will behave as it did before. It\u2019s important to note that the system allows the use of an assembly that doesn\u2019t exactly match the assembly version recorded in the metadata. This extra flexibility is very handy.</p><h6>Publisher Policy Control</h6><p>In the scenario described in the previous section, the publisher of an assembly simply sent a new version of the assembly to the administrator, who installed the assembly and manually edited the application\u2019s or machine\u2019s XML configuration files. In general, when a publisher fixes a bug in an assembly, the publisher would like an easy way to package and distribute the new assembly to all of the users. But the publisher also needs a way to tell each user\u2019s CLR to use the new assembly version instead of the old assembly version. Sure, each user could modify his or her application\u2019s or machine\u2019s XML configuration file, but this is terribly inconvenient and error prone. What the publisher needs is a way to create policy information that is installed on the user\u2019s computer when the new assembly is installed. In this section, I\u2019ll show how an assembly\u2019s publisher can create this policy information.</p><p>Let\u2019s say that you\u2019re a publisher of an assembly and that you\u2019ve just created a new version of your assembly that fixes some bugs. When you package your new assembly to send out to all of your users, you should also create an XML configuration file. This configuration file looks just like the configuration files we\u2019ve been talking about. Here\u2019s an example file (called SomeClassLibrary.config) for the SomeClassLibrary.dll assembly.</p><p>&lt;configuration&gt; &lt;runtime&gt;</p><p>&lt;assemblyBinding xmlns=\"urn:schemas\u00admicrosoft\u00adcom:asm.v1\"&gt; &lt;dependentAssembly&gt;</p><p>&lt;assemblyIdentity name=\"SomeClassLibrary\" publicKeyToken=\"32ab4ba45e0a69a1\" culture=\"neutral\"/&gt;</p><p>&lt;bindingRedirect oldVersion=\"1.0.0.0\" newVersion=\"2.0.0.0\" /&gt;</p><p>&lt;codeBase version=\"2.0.0.0\" href=\"http://www.Wintellect.com/SomeClassLibrary.dll\"/&gt;</p><p>&lt;/dependentAssembly&gt; &lt;/assemblyBinding&gt;</p><p>&lt;/runtime&gt; &lt;/configuration&gt;</p><p>Of course, publishers can set policies only for the assemblies that they themselves create. In addition, the elements shown here are the only elements that can be specified in a publisher policy configuration file; you can\u2019t specify the probing or publisherPolicy elements, for example.</p><p>This configuration file tells the CLR to load version 2.0.0.0 of the SomeClassLibrary assembly whenever version 1.0.0.0 of the assembly is referenced. Now you, the publisher, can create an assembly that contains this publisher policy configuration file. You create the publisher policy assembly by running AL.exe as follows.</p><p>AL.exe /out:Policy.1.0.SomeClassLibrary.dll /version:1.0.0.0 /keyfile:MyCompany.snk /linkresource:SomeClassLibrary.config</p><p>Let me explain the meaning of AL.exe\u2019s command-line switches:</p><p>\u25a0\u25a0 /out This switch tells AL.exe to create a new PE file, called Policy.1.0.SomeClassLibrary.dll, which contains nothing but a manifest. The name of this assembly is very important. The first part of the name, Policy, tells the CLR that this assembly contains publisher policy information. The second and third parts of the name, 1.0, tell the CLR that this publisher policy assembly is for any version of the SomeClassLibrary assembly that has a major and minor version of 1.0. Publisher policies apply to the major and minor version numbers of an assembly only; you can\u2019t create a publisher policy that is specific to individual builds or revisions of an assembly. The fourth part of the name, SomeClassLibrary, indicates the name of the assembly that this publisher policy corresponds to. The fifth and last part of the name, dll, is simply the extension given to the resulting assembly file.</p><p>\u25a0\u25a0 /version This switch identifies the version of the publisher policy assembly; this version number has nothing to do with the SomeClassLibrary assembly itself. You see, publisher policy assemblies can also be versioned. Today, the publisher might create a publisher policy redirecting version 1.0.0.0 of SomeClassLibrary to version 2.0.0.0. In the future, the publisher might want to direct version 1.0.0.0 of SomeClassLibrary to version 2.5.0.0. The CLR uses this version number so that it knows to pick up the latest version of the publisher policy assembly.</p><p>\u25a0\u25a0 /keyfile This switch causes AL.exe to sign the publisher policy assembly by using the</p><p>publisher\u2019s public/private key pair. This key pair must also match the key pair used for all versions of the SomeClassLibrary assembly. After all, this is how the CLR knows that the same publisher created both the SomeClassLibrary assembly and this publisher policy file.</p><p>\u25a0\u25a0 /linkresource This switch tells AL.exe that the XML configuration file is to be considered a separate file of the assembly. The resulting assembly consists of two files, both of which must be packaged and deployed to the users along with the new version of the SomeClassLibrary assembly. By the way, you can\u2019t use AL.exe\u2019s /embedresource switch to embed the XML configuration file into the assembly file, making a single file assembly, because the CLR requires the XML file to be contained in its own separate file.</p><p>After this publisher policy assembly is built, it can be packaged together with the new SomeClassLibrary.dll assembly file and deployed to users. The publisher policy assembly must be installed into the GAC. Although the SomeClassLibrary assembly can also be installed into the GAC, it doesn\u2019t have to be. It could be deployed into an application\u2019s base directory or some other directory identified by a codeBase URL.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile42.png\"/><p>Important A publisher should create a publisher policy assembly only when deploying an update or a service pack version of an assembly. When doing a fresh install of an application, no publisher policy assemblies should be installed.</p><p>I want to make one last point about publisher policy. Say that a publisher distributes a publisher policy assembly, and for some reason, the new assembly introduces more bugs than it fixes. If this happens, the administrator would like to tell the CLR to ignore the publisher policy assembly. To have the runtime do this, the administrator can edit the application\u2019s configuration file and add the following publisherPolicy element.</p><p>&lt;publisherPolicy apply=\"no\"/&gt;</p><p>This element can be placed as a child element of the &lt;assemblyBinding&gt; element in the application\u2019s configuration file so that it applies to all assemblies, or as a child element of the &lt;dependentAssembly&gt; element in the application\u2019s configuration file to have it apply to a specific assembly. When the CLR processes the application\u2019s configuration file, it will see that the GAC shouldn\u2019t be examined for the publisher policy assembly. So the CLR will continue to operate using the older version of the assembly. Note, however, that the CLR will still examine and apply any policy specified in the Machine.config file.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile43.png\"/><p>Important A publisher policy assembly is a way for a publisher to make a statement about the compatibility of different versions of an assembly. If a new version of an assembly isn\u2019t intended to be compatible with an earlier version, the publisher shouldn\u2019t create a publisher policy assembly. In general, use a publisher policy assembly when you build a new version of your assembly that fixes a bug. You should test the new version of the assembly for backward compatibility. On the other hand, if you\u2019re adding new features to your assembly, you should consider the assembly to have no relationship to a previous version, and you shouldn\u2019t ship a publisher policy assembly. In addition, there\u2019s no need to do any backward compatibility testing with such an assembly.</p><h6>PART II</h6>"}, {"id": "lesson-1", "title": "Designing Types", "readTime": "5 min read", "subtitle": "", "content": "<ul style=\"list-style-type: none;\">\n<li><p>CHAPTER 4 Type Fundamentals . . . . . . . . . . . . . . . . . . . . . . . . . . .91</p></li>\n<li><p>CHAPTER 5 Primitive, Reference, and Value Types . . . . . . . . . .111</p></li>\n<li><p>CHAPTER 6 Type and Member Basics . . . . . . . . . . . . . . . . . . . . .151</p></li>\n<li><p>CHAPTER 7 Constants and Fields . . . . . . . . . . . . . . . . . . . . . . . . .175</p></li>\n<li><p>CHAPTER 8 Methods. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . .181</p></li>\n<li><p>CHAPTER 9 Parameters . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . .209</p></li>\n<li><p>CHAPTER 10 Properties . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . .227</p></li>\n<li><p>CHAPTER 11 Events. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . .249</p></li>\n<li><p>CHAPTER 12 Generics. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . .265</p></li>\n<li><p>CHAPTER 13 Interfaces. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . .295</p></li>\n</ul><h6>C H A P T E R 4</h6><h2>Type Fundamentals</h2><h6>In this chapter:</h6><p>All Types Are Derived from System.Object. . . . . . . . . . . . . . . . . 91 Casting Between Types . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 93 Namespaces and Assemblies. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 97 How Things Relate at Run Time . . . . . . . . . . . . . . . . . . . . . . . . . . . 101</p><p>In this chapter, I will introduce information that is fundamental to working with types and the common language runtime (CLR). In particular, I\u2019ll discuss the minimum set of behaviors that you can expect every type to have. I\u2019ll also describe type safety, namespaces, assemblies, and the various ways you can cast objects from one type to another. Finally, I\u2019ll conclude this chapter with an explanation of how types, objects, thread stacks, and the managed heap all relate to one another at run time.</p><h6>All Types Are Derived from System.Object</h6><p>The runtime requires every type to ultimately be derived from the System.Object type. This means that the following two type definitions are identical.</p><p>// Implicitly derived from Object // Explicitly derived from Object class Employee { class Employee : System.Object {</p><p>... ... } }</p><p>Because all types are ultimately derived from System.Object, you are guaranteed that every object of every type has a minimum set of methods. Specifically, the System.Object class offers the public instance methods listed in Table 4-1.</p><p>In addition, types that derive from System.Object have access to the protected methods listed in Table 4-2.</p><ul style=\"list-style-type: none;\">\n<li><p>TABLE 4-1 Public Methods of System.Object</p><table border=\"1\">\n<tr>\n<td><p>Public Method</p>\n<p>Equals</p>\n</td>\n<td><h6>Description</h6>\n<p>Returns true if two objects have the same value. For more information about this method, see the \u201cObject Equality and Identity\u201d section in Chapter 5, \u201cPrimitive, Reference, and Value Types.\u201d</p>\n</td>\n</tr>\n<tr>\n<td><p>GetHashCode</p>\n</td>\n<td><p>Returns a hash code for this object\u2019s value. A type should override this method if its objects are to be used as a key in a hash table collection, like Dictionary. The method should provide a good distribution for its objects. It is unfortunate that this method is defined in Object because most types are never used as keys in a hash table; this method should have been defined in an interface. For more information about this method, see the \u201cObject Hash Codes\u201d section in Chapter 5.</p>\n</td>\n</tr>\n<tr>\n<td><p>ToString</p>\n</td>\n<td><p>The default implementation returns the full name of the type (this.GetType().FullName). However, it is common to override this method so that it returns a String object containing a representation of the object\u2019s state. For example, the core types, such as Boolean and Int32, override this method to return a string representation of their values. It is also common to override this method for debugging purposes; you can call it and get a string showing the values of the object\u2019s fields. In fact, Microsoft Visual Studio\u2019s debugger calls this function automatically to show you a string representation of an object. Note that ToString is expected to be aware of the CultureInfo associated with the calling thread. Chapter 14, \u201cChars, Strings, and Working with Text,\u201d discusses ToString in greater detail.</p>\n</td>\n</tr>\n<tr>\n<td><p>GetType</p>\n</td>\n<td><p>Returns an instance of a Type-derived object that identifies the type of the object used to call GetType. The returned Type object can be used with the reflection classes to obtain metadata information about the object\u2019s type. Reflection is discussed in Chapter 23, \u201cAssembly Loading and Reflection.\u201d The GetType method is nonvirtual, which prevents a class from overriding this method and lying about its type, violating type safety.</p>\n</td>\n</tr>\n</table>\n</li>\n<li><p>TABLE 4-2 Protected Methods of System.Object</p></li>\n</ul><table border=\"1\">\n<tr>\n<td><p>Protected Method</p>\n<p>MemberwiseClone</p>\n</td>\n<td><h6>Description</h6>\n<p>This nonvirtual method creates a new instance of the type and sets the new object\u2019s instance fields to be identical to the this object\u2019s instance fields. A reference to the new instance is returned.</p>\n</td>\n</tr>\n<tr>\n<td><p>Finalize</p>\n</td>\n<td><p>This virtual method is called when the garbage collector determines that the object is garbage and before the memory for the object is reclaimed. Types that require cleanup when collected should override this method. I\u2019ll talk about this important method in much more detail in Chapter 21, \u201cThe Managed Heap and Garbage Collection.\u201d</p>\n</td>\n</tr>\n</table><figcaption>The CLR requires all objects to be created using the new operator. The following line shows how to create an Employee object.</figcaption><p>Employee e = new Employee(\"ConstructorParam1\");</p><p>Here\u2019s what the new operator does:</p><p>1. It calculates the number of bytes required by all instance fields defined in the type and all of its base types up to and including System.Object (which defines no instance fields of its own). Every object on the heap requires some additional members\u2014called the type object pointer and the sync block index\u2014used by the CLR to manage the object. The bytes for these additional members are added to the size of the object.</p><ul style=\"list-style-type: none;\">\n<li><p>2. It allocates memory for the object by allocating the number of bytes required for the specified type from the managed heap; all of these bytes are then set to zero (0).</p></li>\n<li><p>3. It initializes the object\u2019s type object pointer and sync block index members.</p></li>\n<li><p>4. The type\u2019s instance constructor is called, passing it any arguments (the string \"ConstructorParam1\" in the preceding example) specified in the call to new. Most compilers automatically emit code in a constructor to call a base class\u2019s constructor. Each constructor is responsible for initializing the instance fields defined by the type whose constructor is being called. Eventually, System.Object\u2019s constructor is called, and this constructor method does nothing but return.</p></li>\n</ul><p>After new has performed all of these operations, it returns a reference (or pointer) to the newly created object. In the preceding code example, this reference is saved in the variable e, which is of type Employee.</p><p>By the way, the new operator has no complementary delete operator; that is, there is no way to</p><p>explicitly free the memory allocated for an object. The CLR uses a garbage-collected environment (described in Chapter 21) that automatically detects when objects are no longer being used or accessed and frees the object\u2019s memory automatically.</p><h6>Casting Between Types</h6><p>One of the most important features of the CLR is type safety. At run time, the CLR always knows what type an object is. You can always discover an object\u2019s exact type by calling the GetType method. Because this method is nonvirtual, it is impossible for a type to spoof another type. For example, the Employee type can\u2019t override the GetType method and have it return a type of SuperHero.</p><p>Developers frequently find it necessary to cast an object to various types. The CLR allows you to cast an object to its type or to any of its base types. Your choice of programming language dictates how to expose casting operations to the developer. For example, C# doesn\u2019t require any special syntax to cast an object to any of its base types, because casts to base types are considered safe implicit conversions. However, C# does require the developer to explicitly cast an object to any of its derived types because such a cast could fail at run time. The following code demonstrates casting to base and derived types.</p><p>// This type is implicitly derived from System.Object. internal class Employee {</p><p>... }</p><p>public sealed class Program {</p><p>public static void Main() { // No cast needed since new returns an Employee object // and Object is a base type of Employee. Object o = new Employee();</p><p>} }</p><p>// Cast required since Employee is derived from Object. // Other languages (such as Visual Basic) might not require // this cast to compile. Employee e = (Employee) o;</p><p>This example shows what is necessary for your compiler to compile your code. Now I\u2019ll explain what happens at run time. At run time, the CLR checks casting operations to ensure that casts are always to the object\u2019s actual type or any of its base types. For example, the following code will compile, but at run time, an InvalidCastException will be thrown.</p><p>internal class Employee {</p><p>...</p><p>} internal class Manager : Employee {</p><p>... }</p><p>public sealed class Program {</p><p>public static void Main() { // Construct a Manager object and pass it to PromoteEmployee. // A Manager IS\u00adA Object: PromoteEmployee runs OK. Manager m = new Manager(); PromoteEmployee(m);</p><p>}</p><p>// Construct a DateTime object and pass it to PromoteEmployee. // A DateTime is NOT derived from Employee. PromoteEmployee // throws a System.InvalidCastException exception. DateTime newYears = new DateTime(2013, 1, 1); PromoteEmployee(newYears);</p><p>}</p><p>public static void PromoteEmployee(Object o) { // At this point, the compiler doesn't know exactly what // type of object o refers to. So the compiler allows the // code to compile. However, at run time, the CLR does know // what type o refers to (each time the cast is performed) and // it checks whether the object's type is Employee or any type // that is derived from Employee. Employee e = (Employee) o;</p><p>... }</p><p>In the Main method, a Manager object is constructed and passed to PromoteEmployee. This code compiles and executes because Manager is ultimately derived from Object, which is what PromoteEmployee expects. Once inside PromoteEmployee, the CLR confirms that o refers to an object that is either an Employee or a type that is derived from Employee. Because Manager is derived from Employee, the CLR performs the cast and allows PromoteEmployee to continue executing.</p><p>After PromoteEmployee returns, Main constructs a DateTime object and passes it to PromoteEmployee. Again, DateTime is derived from Object, and the compiler compiles the code that calls PromoteEmployee with no problem. However, inside PromoteEmployee, the CLR checks the cast and detects that o refers to a DateTime object and is therefore not an Employee or any type derived from Employee. At this point, the CLR can\u2019t allow the cast and throws a System.InvalidCastException.</p><p>If the CLR allowed the cast, there would be no type safety, and the results would be unpredictable, including the possibility of application crashes and security breaches caused by the ability of types to easily spoof other types. Type spoofing is the cause of many security breaches and compromises an application\u2019s stability and robustness. Type safety is therefore an extremely important part of the CLR.</p><p>By the way, the proper way to declare the PromoteEmployee method would be to specify an Employee type instead of an Object type as its parameter so that the compiler produces a compiletime error, saving the developer from waiting until a runtime exception occurs to discover a problem. I used Object so that I could demonstrate how the C# compiler and the CLR deal with casting and type-safety.</p><h6>Casting with the C# is and as Operators</h6><p>Another way to cast in the C# language is to use the is operator. The is operator checks whether an object is compatible with a given type, and the result of the evaluation is a Boolean: true or false. The is operator will never throw an exception. The following code demonstrates.</p><p>Object o = new Object();</p><ul style=\"list-style-type: none;\">\n<li><p>Boolean b1 = (o is Object); // b1 is true.</p></li>\n<li><p>Boolean b2 = (o is Employee); // b2 is false.</p></li>\n</ul><p>If the object reference is null, the is operator always returns false because there is no object available to check its type.</p><p>The is operator is typically used as follows.</p><p>if (o is Employee) { Employee e = (Employee) o; // Use e within the remainder of the 'if' statement.</p><p>}</p><p>In this code, the CLR is actually checking the object\u2019s type twice: The is operator first checks to see if o is compatible with the Employee type. If it is, inside the if statement, the CLR again verifies that o refers to an Employee when performing the cast. The CLR\u2019s type checking improves security, but it certainly comes at a performance cost, because the CLR must determine the actual type of the object referred to by the variable (o), and then the CLR must walk the inheritance hierarchy, checking each base type against the specified type (Employee). Because this programming paradigm is quite common, C# offers a way to simplify this code and improve its performance by providing an as operator.</p><p>Employee e = o as Employee; if (e != null) {</p><p>// Use e within the 'if' statement. }</p><p>In this code, the CLR checks if o is compatible with the Employee type, and if it is, as returns a non-null reference to the same object. If o is not compatible with the Employee type, the as operator returns null. Notice that the as operator causes the CLR to verify an object\u2019s type just once. The if statement simply checks whether e is null; this check can be performed faster than verifying an object\u2019s type.</p><p>The as operator works just as casting does except that the as operator will never throw an exception. Instead, if the object can\u2019t be cast, the result is null. You\u2019ll want to check to see whether the resulting reference is null, or attempting to use the resulting reference will cause a System.NullReferenceException to be thrown. The following code demonstrates.</p><p>Object o = new Object(); // Creates a new Object object Employee e = o as Employee; // Casts o to an Employee // The cast above fails: no exception is thrown, but e is set to null.</p><p>e.ToString(); // Accessing e throws a NullReferenceException.</p><p>To make sure you understand everything just presented, take the following quiz. Assume that these two class definitions exist.</p><p>internal class B { // Base class }</p><p>internal class D : B { // Derived class }</p><p>Now examine the lines of C# code in Table 4-3. For each line, decide whether the line would compile and execute successfully (marked OK in Table 4-3), cause a compile-time error (CTE), or cause a run-time error (RTE).</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile44.png\"/><figcaption>TABLE 4-3 Type-Safety Quiz</figcaption><table border=\"1\">\n<tr>\n<td><p>Statement</p>\n<p>Object o1 = new Object();</p>\n</td>\n<td><h6>OK</h6>\n<p>3</p>\n</td>\n<td><p>CTE</p>\n</td>\n<td><p>RTE</p>\n</td>\n</tr>\n<tr>\n<td><p>Object o2 = new B();</p>\n</td>\n<td><p>3</p>\n</td>\n<td></td>\n<td></td>\n</tr>\n<tr>\n<td><p>Object o3 = new D();</p>\n</td>\n<td><p>3</p>\n</td>\n<td></td>\n<td></td>\n</tr>\n<tr>\n<td><p>Object o4 = o3;</p>\n</td>\n<td><p>3</p>\n</td>\n<td></td>\n<td></td>\n</tr>\n<tr>\n<td><p>B b1 = new B();</p>\n</td>\n<td><p>3</p>\n</td>\n<td></td>\n<td></td>\n</tr>\n<tr>\n<td><p>B b2 = new D();</p>\n</td>\n<td><p>3</p>\n</td>\n<td></td>\n<td></td>\n</tr>\n<tr>\n<td><p>D d1 = new D();</p>\n</td>\n<td><p>3</p>\n</td>\n<td></td>\n<td></td>\n</tr>\n<tr>\n<td><p>B b3 = new Object();</p>\n</td>\n<td></td>\n<td><p>3</p>\n</td>\n<td></td>\n</tr>\n<tr>\n<td><p>D d2 = new Object();</p>\n</td>\n<td></td>\n<td><p>3</p>\n</td>\n<td></td>\n</tr>\n<tr>\n<td><p>B b4 = d1;</p>\n</td>\n<td><p>3</p>\n</td>\n<td></td>\n<td></td>\n</tr>\n<tr>\n<td><p>D d3 = b2;</p>\n</td>\n<td></td>\n<td><p>3</p>\n</td>\n<td></td>\n</tr>\n<tr>\n<td><p>D d4 = (D) d1;</p>\n</td>\n<td><p>3</p>\n</td>\n<td></td>\n<td></td>\n</tr>\n<tr>\n<td><p>D d5 = (D) b2;</p>\n</td>\n<td><p>3</p>\n</td>\n<td></td>\n<td></td>\n</tr>\n<tr>\n<td><p>D d6 = (D) b1;</p>\n</td>\n<td></td>\n<td></td>\n<td><p>3</p>\n</td>\n</tr>\n<tr>\n<td><p>B b5 = (B) o1;</p>\n</td>\n<td></td>\n<td></td>\n<td><p>3</p>\n</td>\n</tr>\n<tr>\n<td><p>B b6 = (D) b2;</p>\n</td>\n<td><p>3</p>\n</td>\n<td></td>\n<td></td>\n</tr>\n</table><p>Note C# allows a type to define conversion operator methods, as discussed in the \u201cConversion Operator Methods\u201d section of Chapter 8, \u201cMethods.\u201d These methods are invoked only when using a cast expression; they are never invoked when using C#'s as or is operator.</p><h6>Namespaces and Assemblies</h6><p>Namespaces allow for the logical grouping of related types, and developers typically use them to make it easier to locate a particular type. For example, the System.Text namespace defines a bunch of types for performing string manipulations, and the System.IO namespace defines a bunch of types for performing I/O operations. Here\u2019s some code that constructs a System.IO.FileStream object and a System.Text.StringBuilder object.</p><p>public sealed class Program {</p><p>public static void Main() { System.IO.FileStream fs = new System.IO.FileStream(...); System.Text.StringBuilder sb = new System.Text.StringBuilder();</p><p>} }</p><p>As you can see, the code is pretty verbose; it would be nice if there were some shorthand way to refer to the FileStream and StringBuilder types to reduce typing. Fortunately, many compilers do offer mechanisms to reduce programmer typing. The C# compiler provides this mechanism via the using directive. The following code is identical to the previous example.</p><p>using System.IO; // Try prepending \"System.IO.\" using System.Text; // Try prepending \"System.Text.\"</p><p>public sealed class Program {</p><p>public static void Main() { FileStream fs = new FileStream(...); StringBuilder sb = new StringBuilder();</p><p>} }</p><p>To the compiler, a namespace is simply an easy way of making a type\u2019s name longer and more likely to be unique by preceding the name with some symbols separated by dots. So the compiler interprets the reference to FileStream in this example to mean System.IO.FileStream. Similarly, the compiler interprets the reference to StringBuilder to mean System.Text.StringBuilder.</p><p>Using the C# using directive is entirely optional; you\u2019re always welcome to type out the fully qualified name of a type if you prefer. The C# using directive instructs the compiler to try prepending different prefixes to a type name until a match is found.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile45.png\"/><p>Important The CLR doesn\u2019t know anything about namespaces. When you access a type,</p><p>the CLR needs to know the full name of the type (which can be a really long name containing periods) and which assembly contains the definition of the type so that the runtime can load the proper assembly, find the type, and manipulate it.</p><p>In the previous code example, the compiler needs to ensure that every type referenced exists and that my code is using that type in the correct way: calling methods that exist, passing the right number of arguments to these methods, ensuring that the arguments are the right type, using the method\u2019s return value correctly, and so on. If the compiler can\u2019t find a type with the specified name in the source files or in any referenced assemblies, it prepends System.IO. to the type name and checks if the generated name matches an existing type. If the compiler still can\u2019t find a match, it prepends System.Text. to the type\u2019s name. The two using directives shown earlier allow me to simply type FileStream and StringBuilder in my code\u2014the compiler automatically expands the references to System.IO.FileStream and System.Text.StringBuilder. I\u2019m sure you can easily imagine how much typing this saves, as well as how much cleaner your code is to read.</p><p>When checking for a type\u2019s definition, the compiler must be told which assemblies to examine by using the /reference compiler switch as discussed in Chapter 2, \u201cBuilding, Packaging, Deploying, and Administering Applications and Types,\u201d and Chapter 3, \u201cShared Assemblies and Strongly Named Assemblies.\u201d The compiler will scan all of the referenced assemblies looking for the type\u2019s definition.</p><p>After the compiler finds the proper assembly, the assembly information and the type information is emitted into the resulting managed module\u2019s metadata. To get the assembly information, you must pass the assembly that defines any referenced types to the compiler. The C# compiler, by default, automatically looks in the MSCorLib.dll assembly even if you don\u2019t explicitly tell it to. The MSCorLib.dll assembly contains the definitions of all of the core Framework Class Library (FCL) types, such as Object, Int32, String, and so on.</p><p>As you might imagine, there are some potential problems with the way that compilers treat namespaces: it\u2019s possible to have two (or more) types with the same name in different namespaces. Microsoft strongly recommends that you define unique names for types. However, in some cases, it\u2019s simply not possible. The runtime encourages the reuse of components. Your application might take advantage of a component that Microsoft created and another component that Wintellect created. These two companies might both offer a type called Widget\u2014Microsoft\u2019s Widget does one thing, and Wintellect\u2019s Widget does something entirely different. In this scenario, you had no control over the naming of the types, so you can differentiate between the two widgets by using their fully qualified names when referencing them. To reference Microsoft\u2019s Widget, you would use Microsoft.Widget, and to reference Wintellect\u2019s Widget, you would use Wintellect.Widget. In the following code, the reference to Widget is ambiguous, so the C# compiler generates the following message: error CS0104: 'Widget' is an ambiguous reference between 'Microsoft.Widget' and 'Wintellect.Widget'.</p><p>using Microsoft; // Try prepending \"Microsoft.\" using Wintellect; // Try prepending \"Wintellect.\"</p><p>public sealed class Program { public static void Main() {</p><p>Widget w = new Widget();// An ambiguous reference }</p><p>}</p><p>To remove the ambiguity, you must explicitly tell the compiler which Widget you want to create.</p><p>using Microsoft; // Try prepending \"Microsoft.\" using Wintellect; // Try prepending \"Wintellect.\"</p><p>public sealed class Program { public static void Main() {</p><p>Wintellect.Widget w = new Wintellect.Widget(); // Not ambiguous }</p><p>}</p><p>There\u2019s another form of the C# using directive that allows you to create an alias for a single type</p><p>or namespace. This is handy if you have just a few types that you use from a namespace and don\u2019t want to pollute the global namespace with all of a namespace\u2019s types. The following code demonstrates another way to solve the ambiguity problem shown in the preceding code.</p><p>using Microsoft; // Try prepending \"Microsoft.\" using Wintellect; // Try prepending \"Wintellect.\"</p><p>// Define WintellectWidget symbol as an alias to Wintellect.Widget using WintellectWidget = Wintellect.Widget;</p><p>public sealed class Program { public static void Main() {</p><p>WintellectWidget w = new WintellectWidget(); // No error now }</p><p>}</p><p>These methods of disambiguating a type are useful, but in some scenarios, you need to go further.</p><p>Imagine that the Australian Boomerang Company (ABC) and the Alaskan Boat Corporation (ABC) are each creating a type, called BuyProduct, which they intend to ship in their respective assemblies. It\u2019s likely that both companies would create a namespace called ABC that contains a type called Buy\u00ad</p><p>Product. Anyone who tries to develop an application that needs to buy both boomerangs and boats would be in for some trouble unless the programming language provides a way to programmatically distinguish between the assemblies, not just between the namespaces. Fortunately, the C# compiler offers a feature called extern aliases that gives you a way to work around this rarely occurring problem. Extern aliases also give you a way to access a single type from two (or more) different versions of the same assembly. For more information about extern aliases, see the C# Language Specification.</p><p>In your library, when you\u2019re designing types that you expect third parties to use, you should define these types in a namespace so that compilers can easily disambiguate them. In fact, to reduce the likelihood of conflict, you should use your full company name (not an acronym or abbreviation) to be your top-level namespace name. Referring to the Microsoft .NET Framework SDK documentation, you can see that Microsoft uses a namespace of \u201cMicrosoft\u201d for Microsoft-specific types. (See the Microsoft.CSharp, Microsoft.VisualBasic, and Microsoft.Win32 namespaces as examples.)</p><p>Creating a namespace is simply a matter of writing a namespace declaration into your code as follows (in C#).</p><p>namespace CompanyName { public sealed class A { // TypeDef: CompanyName.A }</p><p>}</p><p>namespace X {</p><p>public sealed class B { ... } // TypeDef: CompanyName.X.B }</p><p>The comment on the right of the preceding class definitions indicates the real name of the type the compiler will emit into the type definition metadata table; this is the real name of the type from the CLR\u2019s perspective.</p><p>Some compilers don\u2019t support namespaces at all, and other compilers are free to define what</p><p>\u201cnamespace\u201d means to a particular language. In C#, the namespace directive simply tells the compiler to prefix each type name that appears in source code with the namespace name so that programmers can do less typing.</p><h6>How Namespaces and Assemblies Relate</h6><p>Be aware that a namespace and an assembly (the file that implements a type) aren\u2019t necessarily related. In particular, the various types belonging to a single namespace might be implemented in multiple assemblies. For example, the System.IO.FileStream type is implemented in the MSCorLib.dll assembly, and the System.IO.FileSystemWatcher type is implemented in the System.dll assembly.</p><p>A single assembly can contain types in different namespaces. For example, the System.Int32 and System.Text.StringBuilder types are both in the MSCorLib.dll assembly.</p><p>When you look up a type in the .NET Framework SDK documentation, the documentation will clearly indicate the namespace that the type belongs to and also the assembly that the type is implemented in. In Figure 4-1, you can clearly see (right above the Syntax section) that the ResXFileRef type is part of the System.Resources namespace and that the type is implemented in the System.Windows.Forms.dll assembly. To compile code that references the ResXFileRef type, you\u2019d add a using System.Resources; directive to your source code, and you\u2019d use the /r:System.Windows.Forms.dll compiler switch.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile46.png\"/><figcaption>FIGURE 4-1 SDK documentation showing namespace and assembly information for a type.</figcaption><h6>How Things Relate at Run Time</h6><p>In this section, I\u2019m going to explain the relationship at run time between types, objects, a thread\u2019s stack, and the managed heap. Furthermore, I will also explain the difference between calling static methods, instance methods, and virtual methods. Let\u2019s start off with some fundamentals of computers.</p><p>What I\u2019m about to describe is not specific to the CLR at all, but I\u2019m going to describe it so that we have a working foundation, and then I\u2019ll modify the discussion to incorporate CLR-specific information.</p><p>Figure 4-2 shows a single Windows process that has the CLR loaded into it. In this process there may be many threads. When a thread is created, it is allocated a 1-MB stack. This stack space is used for passing arguments to a method and for local variables defined within a method. In Figure 4-2, the memory for one thread\u2019s stack is shown (on the right). Stacks build from high-memory addresses to low-memory addresses. In the figure, this thread has been executing some code, and its stack has some data on it already (shown as the shaded area at the top of the stack). Now, imagine that the thread has executed some code that calls the M1 method.</p><figcaption>Thread Stack</figcaption><table border=\"1\">\n<tr>\n<td></td>\n</tr>\n<tr>\n<td></td>\n</tr>\n</table><p>void M1() { String name = \"Joe\"; M2(name);</p><p>\u2022\u2022\u2022 return;</p><p>}</p><ul style=\"list-style-type: none;\">\n<li><p>FIGURE 4-2 A thread\u2019s stack with the M1 method about to be called.</p><p>All but the simplest of methods contain some prologue code, which initializes a method before it can start doing its work. These methods also contain epilogue code, which cleans up a method after it has performed its work so that it can return to its caller. When the M1 method starts to execute, its prologue code allocates memory for the local name variable from the thread\u2019s stack (see Figure 4-3).</p>\n<table border=\"1\">\n<tr>\n<td><p>name (String)</p>\n</td>\n</tr>\n</table>\n<p>Thread Stack</p>\n<p>void M1() { String name = \"Joe\"; M2(name);</p>\n<p>\u2022\u2022\u2022 return;</p>\n<p>}</p>\n<p>} M1 Locals</p>\n</li>\n<li><p>FIGURE 4-3 Allocating M1\u2019s local variable on the thread\u2019s stack.</p></li>\n</ul><p>Then, M1 calls the M2 method, passing in the name local variable as an argument. This causes the address in the name local variable to be pushed on the stack (see Figure 4-4). Inside the M2 method, the stack location will be identified using the parameter variable named s. (Note that some architectures pass arguments via registers to improve performance, but this distinction is not important for</p><p>this discussion.) Also, when a method is called, the address indicating where the called method should return to in the calling method is pushed on the stack (also shown in Figure 4-4).</p><p>Thread Stack</p><p>void M1() { String name = \"Joe\"; M2(name);</p><table border=\"1\">\n<tr>\n<td><p>name (String) s (String)</p>\n</td>\n</tr>\n<tr>\n<td><p>[return address]</p>\n</td>\n</tr>\n</table><p>} M1 Locals } M2 Params</p><p>\u2022\u2022\u2022 return;</p><p>}</p><p>void M2(String s) { Int32 length = s.Length; Int32 tally;</p><p>\u2022\u2022\u2022 return;</p><p>}</p><ul style=\"list-style-type: none;\">\n<li><p>FIGURE 4-4 M1 pushes arguments and the return address on the thread\u2019s stack when calling M2.</p><p>When the M2 method starts to execute, its prologue code allocates memory for the local length and tally variables from the thread\u2019s stack (see Figure 4-5). Then the code inside method M2 executes. Eventually, M2 gets to its return statement, which causes the CPU\u2019s instruction pointer to be set to the return address in the stack, and M2\u2019s stack frame is unwound so that it looks the way it did in Figure 4-3. At this point, M1 is continuing to execute its code that immediately follows the call to M2, and its stack frame accurately reflects the state needed by M1.</p>\n<p>Eventually, M1 will return back to its caller by setting the CPU\u2019s instruction pointer to be set to the return address (not shown on the figures, but it would be just above the name argument on the stack), and M1\u2019s stack frame is unwound so that it looks the way it did in Figure 4-2. At this point, the method that called M1 continues to execute its code that immediately follows the call to M1, and its stack frame accurately reflects the state needed by that method.</p>\n<table border=\"1\">\n<tr>\n<td><p>name (String)</p>\n</td>\n</tr>\n<tr>\n<td><p>s (String)</p>\n</td>\n</tr>\n<tr>\n<td><p>[return address]</p>\n</td>\n</tr>\n<tr>\n<td><p>tally (Int32)</p>\n<p>length (Int32)</p>\n</td>\n</tr>\n</table>\n<p>Thread Stack</p>\n<p>void M1() { String name = \"Joe\"; M2(name);</p>\n<p>\u2022\u2022\u2022 return;</p>\n<p>}</p>\n<p>} M1 Locals } M2 Params</p>\n<p>void M2(String s) { Int32 length = s.Length; Int32 tally;</p>\n<p>\u2022\u2022\u2022 return;</p>\n<p>}</p>\n<p>M2 Locals</p>\n</li>\n<li><p>FIGURE 4-5 Allocating M2\u2019s local variables on the thread\u2019s stack.</p></li>\n</ul><p>Now, let\u2019s start gearing the discussion toward the CLR. Let\u2019s say that we have these two class definitions.</p><p>internal class Employee {</p><p>public Int32 GetYearsEmployed() { ... } public virtual String GetProgressReport() { ... } public static Employee Lookup(String name) { ... }</p><p>}</p><p>internal sealed class Manager : Employee {</p><p>public override String GetProgressReport() { ... } }</p><p>Our Windows process has started, the CLR is loaded into it, the managed heap is initialized, and a thread has been created (along with its 1 MB of stack space). This thread has already executed some code, and this code has decided to call the M3 method. All of this is shown in Figure 4-6. The M3 method contains code that demonstrates how the CLR works; this is not code that you would normally write, because it doesn\u2019t actually do anything useful.</p><p>Thread Stack</p><p>Heap</p><p>void M3() { Employee e; Int32 year; e = new Manager(); e = Employee.Lookup(\"Joe\"); year = e.GetYearsEmployed(); e.GetProgressReport();</p><p>}</p><p>FIGURE 4-6 The CLR loaded in a process, its heap initialized, and a thread\u2019s stack with the M3 method about to be called.</p><p>As the just-in-time (JIT) compiler converts M3\u2019s Intermediate Language (IL) code into native CPU instructions, it notices all of the types that are referred to inside M3: Employee, Int32, Manager, and String (because of \"Joe\"). At this time, the CLR ensures that the assemblies that define these types are loaded. Then, using the assembly\u2019s metadata, the CLR extracts information about these types and creates some data structures to represent the types themselves. The data structures for the Employee</p><p>and Manager type objects are shown in Figure 4-7. Because this thread already executed some code prior to calling M3, let\u2019s assume that the Int32 and String type objects have already been created (which is likely because these are commonly used types), and so I won\u2019t show them in the figure.</p><p>Thread Stack</p><p>void M3() { Employee e; Int32 year; e = new Manager(); e = Employee.Lookup(\"Joe\"); year = e.GetYearsEmployed(); e.GetProgressReport();</p><p>}</p><p>Heap</p><p>Manager Type Object</p><p>Type object ptr Sync block index Static fields GetProgressReport</p><p>Employee Type Object</p><p>Type object ptr Sync block index Static fields GetYearsEmployed GetProgressReport Lookup</p><p>FIGURE 4-7 The Employee and Manager type objects are created just as M3 is being called.</p><p>Let\u2019s take a moment to discuss these type objects. As discussed earlier in this chapter, all objects on the heap contain two overhead members: the type object pointer and the sync block index. As you can see, the Employee and Manager type objects have both of these members. When you define a type, you can define static data fields within it. The bytes that back these static data fields are allocated within the type objects themselves. Finally, inside each type object is a method table with one entry per method defined within the type. This is the method table that was discussed in Chapter 1, \u201cThe CLR\u2019s Execution Model.\u201d Because the Employee type defines three methods (GetYearsEmployed, GetProgressReport, and Lookup), there are three entries in Employee\u2019s method table. Because the Manager type defines one method (an override of GetProgressReport), there is just one entry in Manager\u2019s method table.</p><p>Now, after the CLR has ensured that all of the type objects required by the method are created and</p><p>the code for M3 has been compiled, the CLR allows the thread to execute M3\u2019s native code. When M3\u2019s prologue code executes, memory for the local variables must be allocated from the thread\u2019s stack, as shown in Figure 4-8. By the way, the CLR automatically initializes all local variables to null or 0 (zero) as part of the method\u2019s prologue code. However, the C# compiler issues a Use of unassigned local</p><p>variable error message if you write code that attempts to read from a local variable that you have not explicitly initialized in your source code.</p><p>Manager Type Object</p><p>e (Employee) year (int32) = 0 null</p><p>Type object ptr Sync block index Static fields GetProgressReport</p><p>Employee Type Object</p><p>Type object ptr Sync block index Static fields GetYearsEmployed GetProgressReport Lookup</p><p>void M3() { Employee e; Int32 year; e = new Manager(); e = Employee.Lookup(\"Joe\"); year = e.GetYearsEmployed(); e.GetProgressReport();</p><p>}</p><p>FIGURE 4-8 Allocating M3\u2019s local variables on the thread\u2019s stack.</p><p>Then, M3 executes its code to construct a Manager object. This causes an instance of the Manager type, a Manager object, to be created in the managed heap, as shown in Figure 4-9. As you can see, the Manager object\u2014as do all objects\u2014has a type object pointer and sync block index. This object also contains the bytes necessary to hold all of the instance data fields defined by the Manager type, as well as any instance fields defined by any base classes of the Manager type (in this case, Employee and Object). Whenever a new object is created on the heap, the CLR automatically initializes the internal type object pointer member to refer to the object\u2019s corresponding type object (in this case, the Manager type object). Furthermore, the CLR initializes the sync block index and sets all of the object\u2019s instance fields to null or 0 (zero) prior to calling the type\u2019s constructor, a method that will likely modify some of the instance data fields. The new operator returns the memory address of the Manager object, which is saved in the variable e (on the thread\u2019s stack).</p><p>The next line of code in M3 calls Employee\u2019s static Lookup method. When calling a static method, the JIT compiler locates the type object that corresponds to the type that defines the static method. Then, the JIT compiler locates the entry in the type object\u2019s method table that refers to the method being called, JITs the method (if necessary), and calls the JITted code. For our discussion, let\u2019s say that Employee\u2019s Lookup method queries a database to find Joe. Let\u2019s also say that the database indicates that Joe is a manager at the company, and therefore, internally, the Lookup method constructs a new Manager object on the heap, initializes it for Joe, and returns the address of this object. The address is saved in the local variable e. The result of this operation is shown in Figure 4-10.</p><p>Manager Object Type object ptr Sync block index Instance fields</p><p>Manager Type Object</p><p>e (Employee) year (int32) = 0</p><p>Type object ptr Sync block index Static fields GetProgressReport</p><p>Employee Type Object</p><p>Type object ptr Sync block index Static fields GetYearsEmployed GetProgressReport Lookup</p><p>void M3() { Employee e; Int32 year; e = new Manager(); e = Employee.Lookup(\"Joe\"); year = e.GetYearsEmployed(); e.GetProgressReport();</p><p>}</p><ul style=\"list-style-type: none;\">\n<li><p>FIGURE 4-9 Allocating and initializing a Manager object.</p><p>Thread Stack</p>\n<p>void M3() { Employee e; Int32 year; e = new Manager(); e = Employee.Lookup(\"Joe\"); year = e.GetYearsEmployed(); e.GetProgressReport();</p>\n<p>}</p>\n<p>Heap</p>\n<p>Manager Type Object</p>\n<p>Employee Type Object</p>\n<p>Type object ptr Sync block index Static fields GetProgressReport</p>\n<p>Type object ptr Sync block index Static fields GetYearsEmployed GetProgressReport Lookup</p>\n<p>e (Employee) year (int32) = 0</p>\n<p>Manager Object Type object ptr Sync block index Instance fields</p>\n<p>Type object ptr Sync block index Instance fields</p>\n<p>Manager Object</p>\n<p>JITted code</p>\n</li>\n<li><p>FIGURE 4-10 Employee\u2019s static Lookup method allocates and initializes a Manager object for Joe.</p></li>\n</ul><p>Note that e no longer refers to the first Manager object that was created. In fact, because no variable refers to this object, it is a prime candidate for being garbage collected in the future, which will reclaim (free) the memory used by this object.</p><p>The next line of code in M3 calls Employee\u2019s nonvirtual instance GetYearsEmployed method. When calling a nonvirtual instance method, the JIT compiler locates the type object that corresponds to the type of the variable being used to make the call. In this case, the variable e is defined as an Employee. (If the Employee type didn\u2019t define the method being called, the JIT compiler walks down the class hierarchy toward Object looking for this method. It can do this because each type object has a field in it that refers to its base type; this information is not shown in the figures.) Then, the JIT compiler locates the entry in the type object\u2019s method table that refers to the method being called, JITs the method (if necessary), and then calls the JITted code. For our discussion, let\u2019s say that Employee\u2019s GetYearsEmployed method returns 5 because Joe has been employed at the company for five years. The integer is saved in the local variable year. The result of this operation is shown in Figure 4-11.</p><p>Thread Stack</p><p>Heap</p><p>Manager Object Type object ptr Sync block index Instance fields</p><p>Manager Type Object</p><p>e (Employee) year (int32) = 5</p><p>Type object ptr Sync block index Static fields GetProgressReport</p><p>Manager Object</p><p>Type object ptr Sync block index Instance fields</p><p>Employee Type Object</p><p>Type object ptr Sync block index Static fields GetYearsEmployed GetProgressReport Lookup</p><p>void M3() { Employee e; Int32 year; e = new Manager(); e = Employee.Lookup(\"Joe\"); year = e.GetYearsEmployed(); e.GetProgressReport();</p><p>JITted code</p><p>JITted code</p><p>}</p><p>FIGURE 4-11 Employee\u2019s nonvirtual instance GetYearsEmployed method is called, returning 5.</p><p>The next line of code in M3 calls Employee\u2019s virtual instance GetProgressReport method. When calling a virtual instance method, the JIT compiler produces some additional code in the method, which will be executed each time the method is invoked. This code will first look in the variable being used to make the call and then follow the address to the calling object. In this case, the variable e points to the Manager object representing \u201cJoe.\u201d Then, the code will examine the object\u2019s internal type object pointer member; this member refers to the actual type of the object. The code then locates the entry in the type object\u2019s method table that refers to the method being called, JITs the method (if necessary), and calls the JITted code. For our discussion, Manager\u2019s GetProgressReport</p><p>implementation is called because e refers to a Manager object. The result of this operation is shown</p><ul style=\"list-style-type: none;\">\n<li><p>in Figure 4-12.</p><p>Note that if Employee\u2019s Lookup method had discovered that Joe was just an Employee and not a Manager, Lookup would have internally constructed an Employee object whose type object pointer member would have referred to the Employee type object, causing Employee\u2019s implementation of GetProgressReport to execute instead of Manager\u2019s implementation.</p>\n<p>Thread Stack</p>\n<p>void M3() { Employee e; Int32 year; e = new Manager(); e = Employee.Lookup(\"Joe\"); year = e.GetYearsEmployed(); e.GetProgressReport();</p>\n<p>}</p>\n<p>Heap</p>\n<p>Manager Type Object</p>\n<p>Employee Type Object</p>\n<p>Type object ptr Sync block index Static fields GetProgressReport</p>\n<p>Type object ptr Sync block index Static fields GetYearsEmployed GetProgressReport Lookup</p>\n<p>e (Employee) year (int32) = 5</p>\n<p>Manager Object Type object ptr Sync block index Instance fields</p>\n<p>Type object ptr Sync block index Instance fields</p>\n<p>Manager Object</p>\n<table border=\"1\">\n<tr>\n<td><p>JITted code</p>\n</td>\n</tr>\n</table>\n<table border=\"1\">\n<tr>\n<td><p>JITted code</p>\n</td>\n</tr>\n</table>\n<table border=\"1\">\n<tr>\n<td><p>JITted code</p>\n</td>\n</tr>\n</table>\n<p>FIGURE 4-12 Employee\u2019s virtual instance GetProgressReport method is called, causing Manager\u2019s override of this method to execute.</p>\n<p>At this point, we have discussed the relationship between source code, IL, and JITted code. We have also discussed the thread\u2019s stack, arguments, local variables, and how these arguments and variables refer to objects on the managed heap. You also see how objects contain a pointer to their type object (containing the static fields and method table). We have also discussed how the JIT compiler determines how to call static methods, nonvirtual instance methods, and virtual instance methods. All of this should give you great insight into how the CLR works, and this insight should help you when architecting and implementing your types, components, and applications. Before ending this chapter, I\u2019d like to give you just a little more insight as to what is going on inside the CLR.</p>\n<p>You\u2019ll notice that the Employee and Manager type objects both contain type object pointer members. This is because type objects are actually objects themselves. When the CLR creates type objects, the CLR must initialize these members. \u201cTo what?\u201d you might ask. Well, when the CLR starts running in a process, it immediately creates a special type object for the System.Type type (defined in MSCorLib.dll). The Employee and Manager type objects are \u201cinstances\u201d of this type, and therefore, their type object pointer members are initialized to refer to the System.Type type object, as shown</p>\n</li>\n<li><p>in Figure 4-13.</p></li>\n</ul><p>Thread Stack</p><p>Heap</p><p>Manager Object Type object ptr Sync block index Instance fields</p><p>Manager Type Object</p><p>e (Employee) year (int32) = 5</p><p>Type object ptr Sync block index Static fields GetProgressReport</p><table border=\"1\">\n<tr>\n<td rowspan=\"2\"></td>\n<td></td>\n</tr>\n<tr>\n<td></td>\n</tr>\n</table><p>JITted code</p><p>Manager Object</p><p>Type object ptr Sync block index Instance fields</p><p>Employee Type Object</p><p>Type object ptr Sync block index Static fields GetYearsEmployed GetProgressReport Lookup</p><p>void M3() { Employee e; Int32 year; e = new Manager(); e = Employee.Lookup(\"Joe\"); year = e.GetYearsEmployed(); e.GetProgressReport();</p><p>Type Type Object</p><p>JITted code</p><p>Type object ptr Sync block index Static fields</p><p>JITted code</p><p>\u2022\u2022\u2022</p><p>}</p><p>FIGURE 4-13 The Employee and Manager type objects are instances of the System.Type type.</p><p>Of course, the System.Type type object is an object itself and therefore also has a type object pointer member in it, and it is logical to ask what this member refers to. It refers to itself because the</p><p>System.Type type object is itself an \u201cinstance\u201d of a type object. And now you should understand the CLR\u2019s complete type system and how it works. By the way, System.Object\u2019s GetType method simply returns the address stored in the specified object\u2019s type object pointer member. In other words, the</p><p>GetType method returns a pointer to an object\u2019s type object, and this is how you can determine the true type of any object in the system (including type objects).</p><h6>C H A P T E R 5</h6><h2>Primitive, Reference, and Value Types</h2><h6>In this chapter:</h6><p>Programming Language Primitive Types . . . . . . . . . . . . . . . . . . . 111 Reference Types and Value Types. . . . . . . . . . . . . . . . . . . . . . . . . . 118 Boxing and Unboxing Value Types. . . . . . . . . . . . . . . . . . . . . . . . . 124 Object Hash Codes. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 142 The dynamic Primitive Type . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 144</p><p>In this chapter, I\u2019ll discuss the different kinds of types you\u2019ll run into as a Microsoft .NET Framework developer. It is crucial for all developers to be familiar with the different behaviors that these types exhibit. When I was first learning the .NET Framework, I didn\u2019t fully understand the difference between primitive, reference, and value types. This lack of clarity led me to unwittingly introduce subtle bugs and performance issues into my code. By explaining the differences between the types here, I\u2019m hoping to save you some of the headaches that I experienced while getting up to speed.</p><h6>Programming Language Primitive Types</h6><p>Certain data types are so commonly used that many compilers allow code to manipulate them using simplified syntax. For example, you could allocate an integer by using the following syntax.</p><p>System.Int32 a = new System.Int32();</p><p>But I\u2019m sure you\u2019d agree that declaring and initializing an integer by using this syntax is rather cumbersome. Fortunately, many compilers (including C#) allow you to use syntax similar to the following instead.</p><p>int a = 0;</p><p>This syntax certainly makes the code more readable and generates identical Intermediate Language (IL) to that which is generated when System.Int32 is used. Any data types the compiler directly supports are called primitive types. Primitive types map directly to types existing in the</p><p>111</p><p>Framework Class Library (FCL). For example, in C#, an int maps directly to the System.Int32 type. Because of this, the following four lines of code all compile correctly and produce exactly the same IL.</p><p>int a = 0; // Most convenient syntax System.Int32 a = 0; // Convenient syntax int a = new int(); // Inconvenient syntax System.Int32 a = new System.Int32(); // Most inconvenient syntax</p><p>Table 5-1 shows the FCL types that have corresponding primitives in C#. For the types that are compliant with the Common Language Specification (CLS), other languages will offer similar primitive types. However, languages aren\u2019t required to offer any support for the non\u2013CLS-compliant types.</p><p>TABLE 5-1 C# Primitives with Corresponding FCL Types</p><table border=\"1\">\n<tr>\n<td><p>Primitive Type</p>\n<p>sbyte</p>\n</td>\n<td><p>FCL Type</p>\n<p>System.SByte</p>\n</td>\n<td><p>CLS-Compliant No</p>\n</td>\n<td><p>Description Signed 8-bit value</p>\n</td>\n</tr>\n<tr>\n<td><p>byte</p>\n</td>\n<td><p>System.Byte</p>\n</td>\n<td><p>Yes</p>\n</td>\n<td><p>Unsigned 8-bit value</p>\n</td>\n</tr>\n<tr>\n<td><p>short</p>\n</td>\n<td><p>System.Int16</p>\n</td>\n<td><p>Yes</p>\n</td>\n<td><p>Signed 16-bit value</p>\n</td>\n</tr>\n<tr>\n<td><p>ushort</p>\n</td>\n<td><p>System.UInt16</p>\n</td>\n<td><p>No</p>\n</td>\n<td><p>Unsigned 16-bit value</p>\n</td>\n</tr>\n<tr>\n<td><p>int</p>\n</td>\n<td><p>System.Int32</p>\n</td>\n<td><p>Yes</p>\n</td>\n<td><p>Signed 32-bit value</p>\n</td>\n</tr>\n<tr>\n<td><p>uint</p>\n</td>\n<td><p>System.UInt32</p>\n</td>\n<td><p>No</p>\n</td>\n<td><p>Unsigned 32-bit value</p>\n</td>\n</tr>\n<tr>\n<td><p>long</p>\n</td>\n<td><p>System.Int64</p>\n</td>\n<td><p>Yes</p>\n</td>\n<td><p>Signed 64-bit value</p>\n</td>\n</tr>\n<tr>\n<td><p>ulong</p>\n</td>\n<td><p>System.UInt64</p>\n</td>\n<td><p>No</p>\n</td>\n<td><p>Unsigned 64-bit value</p>\n</td>\n</tr>\n<tr>\n<td><p>char</p>\n</td>\n<td><p>System.Char</p>\n</td>\n<td><p>Yes</p>\n</td>\n<td><p>16-bit Unicode character (char never represents an 8-bit value as it would in unmanaged C++.)</p>\n</td>\n</tr>\n<tr>\n<td><p>float</p>\n</td>\n<td><p>System.Single</p>\n</td>\n<td><p>Yes</p>\n</td>\n<td><p>IEEE 32-bit floating point value</p>\n</td>\n</tr>\n<tr>\n<td><p>double</p>\n</td>\n<td><p>System.Double</p>\n</td>\n<td><p>Yes</p>\n</td>\n<td><p>IEEE 64-bit floating point value</p>\n</td>\n</tr>\n<tr>\n<td><p>bool</p>\n</td>\n<td><p>System.Boolean</p>\n</td>\n<td><p>Yes</p>\n</td>\n<td><p>A true/false value</p>\n</td>\n</tr>\n<tr>\n<td><p>decimal</p>\n</td>\n<td><p>System.Decimal</p>\n</td>\n<td><p>Yes</p>\n</td>\n<td><p>A 128-bit high-precision floating-point value commonly used for financial calculations in which rounding errors can\u2019t be tolerated. Of the 128 bits, 1 bit represents the sign of the value, 96 bits represent the value itself, and 8 bits represent the power of 10 to divide the 96-bit value by (can be anywhere from 0 to 28). The remaining bits are unused.</p>\n</td>\n</tr>\n<tr>\n<td><p>string</p>\n</td>\n<td><p>System.String</p>\n</td>\n<td><p>Yes</p>\n</td>\n<td><p>An array of characters</p>\n</td>\n</tr>\n<tr>\n<td><p>object</p>\n</td>\n<td><p>System.Object</p>\n</td>\n<td><p>Yes</p>\n</td>\n<td><p>Base type of all types</p>\n</td>\n</tr>\n<tr>\n<td><p>dynamic</p>\n</td>\n<td><p>System.Object</p>\n</td>\n<td><p>Yes</p>\n</td>\n<td><p>To the common language runtime (CLR), dynamic is identical to object. However, the C# compiler allows dynamic variables to participate in dynamic dispatch by using a simplified syntax. For more information, see \u201cThe dynamic Primitive Type\u201d section at the end of this chapter.</p>\n</td>\n</tr>\n</table><figcaption>Another way to think of this is that the C# compiler automatically assumes that you have the following using directives (as discussed in Chapter 4, \u201cType Fundamentals\u201d) in all of your source code files.</figcaption><p>using sbyte = System.SByte; using byte = System.Byte; using short = System.Int16; using ushort = System.UInt16; using int = System.Int32; using uint = System.UInt32;</p><p>...</p><p>The C# language specification states, \u201cAs a matter of style, use of the keyword is favored over use of the complete system type name.\u201d I disagree with the language specification; I prefer to use the FCL type names and completely avoid the primitive type names. In fact, I wish that compilers didn\u2019t even offer the primitive type names and forced developers to use the FCL type names instead. Here are my reasons:</p><p>\u25a0\u25a0 I\u2019ve seen a number of developers confused, not knowing whether to use string or String in their code. Because in C# string (a keyword) maps exactly to System.String (an FCL type), there is no difference and either can be used. Similarly, I\u2019ve heard some developers say that int represents a 32-bit integer when the application is running on a 32-bit operating system and that it represents a 64-bit integer when the application is running on a 64-bit operating system. This statement is absolutely false: in C#, an int always maps to System.Int32, and therefore it represents a 32-bit integer regardless of the operating system the code is running on. If programmers would use Int32 in their code, then this potential confusion is also eliminated.</p><p>\u25a0\u25a0 In C#, long maps to System.Int64, but in a different programming language, long could map to an Int16 or Int32. In fact, C++/CLI does treat long as an Int32. Someone reading source code in one language could easily misinterpret the code\u2019s intention if he or she were used to programming in a different programming language. In fact, most languages won\u2019t even treat long as a keyword and won\u2019t compile code that uses it.</p><p>\u25a0\u25a0 The FCL has many methods that have type names as part of their method names. For example, the BinaryReader type offers methods such as ReadBoolean, ReadInt32, ReadSingle, and so on, and the System.Convert type offers methods such as ToBoolean, ToInt32, ToSingle, and so on. Although it\u2019s legal to write the following code, the line with float feels very unnatural to me, and it\u2019s not obvious that the line is correct.</p><p>BinaryReader br = new BinaryReader(...); float val = br.ReadSingle(); // OK, but feels unnatural Single val = br.ReadSingle(); // OK and feels good</p><p>\u25a0\u25a0 Many programmers that use C# exclusively tend to forget that other programming languages can be used against the CLR, and because of this, C#-isms creep into the class library code. For example, Microsoft\u2019s FCL is almost exclusively written in C# and developers on the FCL team have now introduced methods into the library such as Array\u2019s GetLongLength, which returns an Int64 value that is a long in C# but not in other languages (like C++/CLI). Another example is System.Linq.Enumerable\u2019s LongCount method.</p><p>For all of these reasons, I\u2019ll use the FCL type names throughout this book.</p><p>In many programming languages, you would expect the following code to compile and execute correctly.</p><p>Int32 i = 5; // A 32\u00adbit value Int64 l = i; // Implicit cast to a 64\u00adbit value</p><p>However, based on the casting discussion presented in Chapter 4, you wouldn\u2019t expect this code to compile. After all, System.Int32 and System.Int64 are different types, and neither one is derived from the other. Well, you\u2019ll be happy to know that the C# compiler does compile this code correctly, and it runs as expected. Why? The reason is that the C# compiler has intimate knowledge of primitive types and applies its own special rules when compiling the code. In other words, the compiler recognizes common programming patterns and produces the necessary IL to make the written code work as expected. Specifically, the C# compiler supports patterns related to casting, literals, and operators,</p><ul style=\"list-style-type: none;\">\n<li><p>as shown in the following examples.</p><p>First, the compiler is able to perform implicit or explicit casts between primitive types such as the following.</p>\n<p>Int32 i = 5; // Implicit cast from Int32 to Int32 Int64 l = i; // Implicit cast from Int32 to Int64 Single s = i; // Implicit cast from Int32 to Single Byte b = (Byte) i; // Explicit cast from Int32 to Byte Int16 v = (Int16) s; // Explicit cast from Single to Int16</p>\n<p>C# allows implicit casts if the conversion is \u201csafe,\u201d that is, no loss of data is possible, such as converting an Int32 to an Int64. But C# requires explicit casts if the conversion is potentially unsafe. For numeric types, \u201cunsafe\u201d means that you could lose precision or magnitude as a result of the conversion. For example, converting from Int32 to Byte requires an explicit cast because precision might be lost from large Int32 numbers; converting from Single to Int16 requires a cast because Single can represent numbers of a larger magnitude than Int16 can.</p>\n<p>Be aware that different compilers can generate different code to handle these cast operations. For example, when casting a Single with a value of 6.8 to an Int32, some compilers could generate code to put a 6 in the Int32, and others could perform the cast by rounding the result up to 7. By the way, C# always truncates the result. For the exact rules that C# follows for casting primitive types, see the \u201cConversions\u201d section in the C# language specification.</p>\n<p>In addition to casting, primitive types can be written as literals. A literal is considered to be an instance of the type itself, and therefore, you can call instance methods by using the instance as shown here.</p>\n<p>Console.WriteLine(123.ToString() + 456.ToString()); // \"123456\"</p>\n<p>Also, if you have an expression consisting of literals, the compiler is able to evaluate the expression</p>\n</li>\n<li><p>at compile time, improving the application\u2019s performance.</p></li>\n</ul><p>Boolean found = false; // Generated code sets found to 0 Int32 x = 100 + 20 + 3; // Generated code sets x to 123 String s = \"a \" + \"bc\"; // Generated code sets s to \"a bc\"</p><p>Finally, the compiler automatically knows how and in what order to interpret operators (such as +, \u00ad, *, /, %, &amp;, ^, |, ==, !=, &gt;, &lt;, &gt;=, &lt;=, &lt;&lt;, &gt;&gt;, ~, !, ++, \u00ad\u00ad, and so on) when used in code.</p><ul style=\"list-style-type: none;\">\n<li><p>Int32 x = 100; // Assignment operator</p></li>\n<li><p>Int32 y = x + 23; // Addition and assignment operators Boolean lessThanFifty = (y &lt; 50); // Less\u00adthan and assignment operators</p></li>\n</ul><h6>Checked and Unchecked Primitive Type Operations</h6><p>Programmers are well aware that many arithmetic operations on primitives could result in an overflow.</p><p>Byte b = 100; b = (Byte) (b + 200); // b now contains 44 (or 2C in Hex).</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile47.png\"/><p>Important When performing the preceding arithmetic operation, the first step requires that all operand values be expanded to 32-bit values (or 64-bit values if any operand requires more than 32 bits). So b and 200 (values requiring less than 32 bits) are first converted to 32-bit values and then added together. The result is a 32-bit value (300 in decimal, or 12C in hexadecimal) that must be cast to a Byte before the result can be stored back in the variable b. C# doesn\u2019t perform this cast for you implicitly, which is why the Byte cast on the second line of the preceding code is required.</p><p>In most programming scenarios, this silent overflow is undesirable and if not detected causes the application to behave in strange and unusual ways. In some rare programming scenarios (such as calculating a hash value or a checksum), however, this overflow is not only acceptable but is also desired.</p><p>Different languages handle overflows in different ways. C and C++ don\u2019t consider overflows to be an error and allow the value to wrap; the application continues running. Microsoft Visual Basic, on the other hand, always considers overflows to be errors and throws an exception when it detects one.</p><p>The CLR offers IL instructions that allow the compiler to choose the desired behavior. The CLR has an instruction called add that adds two values together. The add instruction performs no overflow checking. The CLR also has an instruction called add.ovf that also adds two values together. However, add.ovf throws a System.OverflowException if an overflow occurs. In addition to these two IL instructions for the add operation, the CLR also has similar IL instructions for subtraction (sub/ sub.ovf), multiplication (mul/mul.ovf), and data conversions (conv/conv.ovf).</p><p>C# allows the programmer to decide how overflows should be handled. By default, overflow checking is turned off. This means that the compiler generates IL code by using the versions of the add, subtract, multiply, and conversion instructions that don\u2019t include overflow checking. As a result, the code runs faster\u2014but developers must be assured that overflows won\u2019t occur or that their code is designed to anticipate these overflows.</p><p>One way to get the C# compiler to control overflows is to use the /checked+ compiler switch. This switch tells the compiler to generate code that has the overflow-checking versions of the add, subtract, multiply, and conversion IL instructions. The code executes a little slower because the CLR is checking these operations to determine whether an overflow occurred. If an overflow occurs, the CLR throws an OverflowException.</p><p>In addition to having overflow checking turned on or off globally, programmers can control overflow checking in specific regions of their code. C# allows this flexibility by offering checked and unchecked operators. Here\u2019s an example that uses the unchecked operator.</p><p>UInt32 invalid = unchecked((UInt32) (\u00ad1)); // OK</p><p>And here is an example that uses the checked operator.</p><p>Byte b = 100; b = checked((Byte) (b + 200)); // OverflowException is thrown</p><p>In this example, b and 200 are first converted to 32-bit values and are then added together; the result is 300. Then 300 is converted to a Byte due to the explicit cast; this generates the OverflowException. If the Byte were cast outside the checked operator, the exception wouldn\u2019t occur.</p><p>b = (Byte) checked(b + 200); // b contains 44; no OverflowException</p><p>In addition to the checked and unchecked operators, C# also offers checked and unchecked statements. The statements cause all expressions within a block to be checked or unchecked.</p><p>checked { // Start of checked block Byte b = 100; b = (Byte) (b + 200); // This expression is checked for overflow.</p><p>} // End of checked block</p><p>In fact, if you use a checked statement block, you can now use the += operator with the Byte, which simplifies the code a bit.</p><p>checked { // Start of checked block Byte b = 100; b += 200; // This expression is checked for overflow.</p><p>} // End of checked block</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile48.png\"/><p>Important Because the only effect that the checked operator and statement have is to determine which versions of the add, subtract, multiply, and data conversion IL instructions are produced, calling a method within a checked operator or statement has no impact on that method, as the following code demonstrates.</p><p>checked { // Assume SomeMethod tries to load 400 into a Byte. SomeMethod(400); // SomeMethod might or might not throw an OverflowException. // It would if SomeMethod were compiled with checked instructions.</p><p>}</p><p>In my experience, I've seen a lot of calculations produce surprising results. Typically, this is due to invalid user input, but it can also be due to values returned from parts of the system that a programmer just doesn't expect. And so, I now recommend that programmers do the following:</p><p>\u25a0\u25a0 Use signed data types (such as Int32 and Int64) instead of unsigned numeric types (such as UInt32 and UInt64) wherever possible. This allows the compiler to detect more overflow/ underflow errors. In addition, various parts of the class library (such as Array's and String's Length properties) are hard-coded to return signed values, and less casting is required as you move these values around in your code. Fewer casts make source code cleaner and easier to maintain. In addition, unsigned numeric types are not CLS-compliant.</p><p>\u25a0\u25a0 As you write your code, explicitly use checked around blocks where an unwanted overflow might occur due to invalid input data, such as processing a request with data supplied from an end user or a client machine. You might want to catch OverflowException as well, so that your application can gracefully recover from these failures.</p><p>\u25a0\u25a0 As you write your code, explicitly use unchecked around blocks where an overflow is OK, such</p><p>as calculating a checksum.</p><p>\u25a0\u25a0 For any code that doesn\u2019t use checked or unchecked, the assumption is that you do want an exception to occur on overflow, for example, calculating something (such as prime numbers) where the inputs are known, and overflows are bugs.</p><p>Now, as you develop your application, turn on the compiler\u2019s /checked+ switch for debug builds. Your application will run more slowly because the system will be checking for overflows on any code that you didn\u2019t explicitly mark as checked or unchecked. If an exception occurs, you\u2019ll easily detect it and be able to fix the bug in your code. For the release build of your application, use the compiler\u2019s /checked\u00adswitch so that the code runs faster and overflow exceptions won\u2019t be generated. To change the Checked setting in Microsoft Visual Studio, display the properties for your project, select the Build tab, click Advanced, and then select the Check For Arithmetic Overflow/Underflow option, as shown in Figure 5-1.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile49.png\"/><figcaption>FIGURE 5-1 Changing the compiler\u2019s default setting for performing checked arithmetic by using Visual Studio\u2019s Advanced Build Settings dialog box.</figcaption><p>If your application can tolerate the slight performance hit of always doing checked operations, then I recommend that you compile with the /checked command-line option even for a release build because this can prevent your application from continuing to run with corrupted data and possible security holes. For example, you might perform a multiplication to calculate an index into an array; it is much better to get an OverflowException as opposed to accessing an incorrect array element due to the math wrapping around.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile50.png\"/><p>Important The System.Decimal type is a very special type. Although many programming languages (C# and Visual Basic included) consider Decimal a primitive type, the CLR does not. This means that the CLR doesn\u2019t have IL instructions that know how to manipulate a Decimal value. If you look up the Decimal type in the .NET Framework SDK documentation, you\u2019ll see that it has public static methods called Add, Subtract, Multiply, Divide, and so on. In addition, the Decimal type provides operator overload methods for +, \u00ad, *, /, and so on.</p><p>When you compile code that uses Decimal values, the compiler generates code to call Decimal\u2019s members to perform the actual operation. This means that manipulating Decimal values is slower than manipulating CLR primitive values. Also, because there are no IL instructions for manipulating Decimal values, the checked and unchecked operators, statements, and compiler switches have no effect. Operations on Decimal values always throw an OverflowException if the operation can\u2019t be performed safely.</p><p>Similarly, the System.Numerics.BigInteger type is also special in that it internally uses an array of UInt32s to represent an arbitrarily large integer whose value has no upper or lower bound. Therefore, operations on a BigInteger never result in an OverflowException. However, a BigInteger operation may throw an OutOfMemoryException if the value gets too large and there is insufficient available memory to resize the array.</p><h6>Reference Types and Value Types</h6><p>The CLR supports two kinds of types: reference types and value types. Although most types in the FCL are reference types, the types that programmers use most often are value types. Reference types are always allocated from the managed heap, and the C# new operator returns the memory address of the object\u2014the memory address refers to the object\u2019s bits. You need to bear in mind some performance considerations when you\u2019re working with reference types. First, consider these facts:</p><p>\u25a0\u25a0 The memory must be allocated from the managed heap.</p><p>\u25a0\u25a0 Each object allocated on the heap has some additional overhead members associated with it</p><p>that must be initialized.</p><p>\u25a0\u25a0 The other bytes in the object (for the fields) are always set to zero.</p><p>\u25a0\u25a0 Allocating an object from the managed heap could force a garbage collection to occur.</p><p>If every type were a reference type, an application\u2019s performance would suffer greatly. Imagine how poor performance would be if every time you used an Int32 value, a memory allocation occurred! To improve performance for simple, frequently used types, the CLR offers lightweight types called value types. Value type instances are usually allocated on a thread\u2019s stack (although they can also be embedded as a field in a reference type object). The variable representing the instance doesn\u2019t contain a pointer to an instance; the variable contains the fields of the instance itself. Because the variable contains the instance\u2019s fields, a pointer doesn\u2019t have to be dereferenced to manipulate the instance\u2019s fields. Value type instances don\u2019t come under the control of the garbage collector, so their use reduces pressure in the managed heap and reduces the number of collections an application requires over its lifetime.</p><p>The .NET Framework SDK documentation clearly indicates which types are reference types and which are value types. When looking up a type in the documentation, any type called a class is a reference type. For example, the System.Exception class, the System.IO.FileStream class, and the System.Random class are all reference types. On the other hand, the documentation refers to each value type as a structure or an enumeration. For example, the System.Int32 structure, the System.Boolean structure, the System.Decimal structure, the System.TimeSpan structure, the System.DayOfWeek enumeration, the System.IO.FileAttributes enumeration, and the System.Drawing.FontStyle enumeration are all value types.</p><p>All of the structures are immediately derived from the System.ValueType abstract type. System.ValueType is itself immediately derived from the System.Object type. By definition, all value types must be derived from System.ValueType. All enumerations are derived from the System.Enum abstract type, which is itself derived from System.ValueType. The CLR and all programming languages give enumerations special treatment. For more information about enumerated types, refer to Chapter 15, \u201cEnumerated Types and Bit Flags.\u201d</p><p>Even though you can\u2019t choose a base type when defining your own value type, a value type can implement one or more interfaces if you choose. In addition, all value types are sealed, which prevents a value type from being used as a base type for any other reference type or value type. So, for example, it\u2019s not possible to define any new types using Boolean, Char, Int32, UInt64, Single, Double, Decimal, and so on as base types.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile51.png\"/><p>Important For many developers (such as unmanaged C/C++ developers), reference types and value types will seem strange at first. In unmanaged C/C++, you declare a type, and then the code that uses the type gets to decide if an instance of the type should be allocated on the thread\u2019s stack or in the application\u2019s heap. In managed code, the developer defining the type indicates where instances of the type are allocated; the developer using the type has no control over this.</p><p>The following code and Figure 5-2 demonstrate how reference types and value types differ.</p><p>// Reference type (because of 'class') class SomeRef { public Int32 x; }</p><p>// Value type (because of 'struct') struct SomeVal { public Int32 x; }</p><p>static void ValueTypeDemo() {</p><ul style=\"list-style-type: none;\">\n<li><p>SomeRef r1 = new SomeRef(); // Allocated in heap SomeVal v1 = new SomeVal(); // Allocated on stack r1.x = 5; // Pointer dereference v1.x = 5; // Changed on stack Console.WriteLine(r1.x); // Displays \"5\" Console.WriteLine(v1.x); // Also displays \"5\" // The left side of Figure 5\u00ad2 reflects the situation // after the lines above have executed.</p></li>\n<li><p>SomeRef r2 = r1; // Copies reference (pointer) only SomeVal v2 = v1; // Allocate on stack &amp; copies members r1.x = 8; // Changes r1.x and r2.x v1.x = 9; // Changes v1.x, not v2.x</p></li>\n</ul><ul style=\"list-style-type: none;\">\n<li><p>Console.WriteLine(r1.x); // Displays \"8\"</p></li>\n<li><p>Console.WriteLine(r2.x); // Displays \"8\" Console.WriteLine(v1.x); // Displays \"9\" Console.WriteLine(v2.x); // Displays \"5\" // The right side of Figure 5\u00ad2 reflects the situation // after ALL of the lines above have executed.</p></li>\n</ul><p>}</p><p>In this code, the SomeVal type is declared using struct instead of the more common class. In C#, types declared using struct are value types, and types declared using class are reference types. As you can see, the behavior of reference types and value types differs quite a bit. As you use types in your code, you must be aware of whether the type is a reference type or a value type because it can greatly affect how you express your intentions in the code.</p><p>Situation after the first half of the ValueTypeDemo method executes</p><p>Situation after the ValueTypeDemo method completely executes</p><h6>Thread Stack</h6><h6>Thread Stack</h6><h6>Managed Heap</h6><h6>Managed Heap</h6><p>r1</p><p>r1</p><p>Type object ptr Sync block index</p><p>Type object ptr Sync block index</p><p>v1 x=5</p><p>v1 x=9</p><p>x=5</p><p>x=8</p><p>r2</p><p>v2 x=5</p><p>FIGURE 5-2 Visualizing the memory as the code executes.</p><p>In the preceding code, you saw this line.</p><p>SomeVal v1 = new SomeVal(); // Allocated on stack</p><p>The way this line is written makes it look as if a SomeVal instance will be allocated on the managed heap. However, the C# compiler knows that SomeVal is a value type and produces code that allocates the SomeVal instance on the thread\u2019s stack. C# also ensures that all of the fields in the value type instance are zeroed.</p><p>The preceding line could have been written like this instead.</p><p>SomeVal v1; // Allocated on stack</p><p>This line also produces IL that allocates the instance on the thread\u2019s stack and zeroes the fields. The only difference is that C# \u201cthinks\u201d that the instance is initialized if you use the new operator. The following code will make this point clear.</p><p>// These two lines compile because C# thinks that // v1's fields have been initialized to 0. SomeVal v1 = new SomeVal(); Int32 a = v1.x;</p><p>// These two lines don't compile because C# doesn't think that // v1's fields have been initialized to 0. SomeVal v1; Int32 a = v1.x; // error CS0170: Use of possibly unassigned field 'x'</p><p>When designing your own types, consider carefully whether to define your types as value types instead of reference types. In some situations, value types can give better performance. In particular, you should declare a type as a value type if all the following statements are true:</p><p>\u25a0\u25a0 The type acts as a primitive type. Specifically, this means that it is a fairly simple type that has no members that modify any of its instance fields. When a type offers no members that alter its fields, we say that the type is immutable. In fact, it is recommended that many value types mark all their fields as readonly (discussed in Chapter 7, \"Constants and Fields\").</p><p>\u25a0\u25a0 The type doesn\u2019t need to inherit from any other type.</p><p>\u25a0\u25a0 The type won\u2019t have any other types derived from it. The size of instances of your type is also a condition to take into account because by default,</p><p>arguments are passed by value, which causes the fields in value type instances to be copied, hurting performance. Again, a method that returns a value type causes the fields in the instance to be copied into the memory allocated by the caller when the method returns, hurting performance. So, in addition to the previous conditions, you should declare a type as a value type if one of the following statements is true:</p><p>\u25a0\u25a0 Instances of the type are small (approximately 16 bytes or less).</p><p>\u25a0\u25a0 Instances of the type are large (greater than 16 bytes) and are not passed as method param-</p><p>eters or returned from methods.</p><p>The main advantage of value types is that they\u2019re not allocated as objects in the managed heap. Of course, value types have several limitations of their own when compared to reference types. Here are some of the ways in which value types and reference types differ:</p><p>\u25a0\u25a0 Value type objects have two representations: an unboxed form and a boxed form (discussed in</p><p>the next section). Reference types are always in a boxed form.</p><p>\u25a0\u25a0 Value types are derived from System.ValueType. This type offers the same methods as defined by System.Object. However, System.ValueType overrides the Equals method so that it returns true if the values of the two objects\u2019 fields match. In addition, System.ValueType overrides the GetHashCode method to produce a hash code value by using an algorithm</p><p>that takes into account the values in the object\u2019s instance fields. Due to performance issues with this default implementation, when defining your own value types, you should override and provide explicit implementations for the Equals and GetHashCode methods. I\u2019ll cover the Equals and GetHashCode methods at the end of this chapter.</p><p>\u25a0\u25a0 Because you can\u2019t define a new value type or a new reference type by using a value type as a base class, you shouldn\u2019t introduce any new virtual methods into a value type. No methods can be abstract, and all methods are implicitly sealed (can\u2019t be overridden).</p><p>\u25a0\u25a0 Reference type variables contain the memory address of objects in the heap. By default, when a reference type variable is created, it is initialized to null, indicating that the reference type variable doesn\u2019t currently point to a valid object. Attempting to use a null reference type variable causes a NullReferenceException to be thrown. By contrast, value type variables always contain a value of the underlying type, and all members of the value type are initialized to 0. Because a value type variable isn\u2019t a pointer, it\u2019s not possible to generate a NullReferenceException when accessing a value type. The CLR does offer a special feature that adds the notion of nullability to a value type. This feature, called nullable types, is discussed in Chapter 19, \u201cNullable Value Types.\u201d</p><p>\u25a0\u25a0 When you assign a value type variable to another value type variable, a field-by-field copy is made. When you assign a reference type variable to another reference type variable, only the memory address is copied.</p><p>\u25a0\u25a0 Because of the previous point, two or more reference type variables can refer to a single object</p><p>in the heap, allowing operations on one variable to affect the object referenced by the other variable. On the other hand, value type variables are distinct objects, and it\u2019s not possible for operations on one value type variable to affect another.</p><p>\u25a0\u25a0 Because unboxed value types aren\u2019t allocated on the heap, the storage allocated for them is freed as soon as the method that defines an instance of the type is no longer active as opposed to waiting for a garbage collection.</p><h6>How the CLR Controls the Layout of a Type\u2019s Fields</h6><p>To improve performance, the CLR is capable of arranging the fields of a type any way it chooses. For example, the CLR might reorder fields in memory so that object references are grouped together and data fields are properly aligned and packed. However, when you define a type, you can tell the CLR whether it must keep the type\u2019s fields in the same order as the developer specified them or whether it can reorder them as it sees fit.</p><p>You tell the CLR what to do by applying the System.Runtime.InteropServices.StructLayoutAttribute attribute on the class or structure you\u2019re defining. To this attribute\u2019s</p><p>constructor, you can pass LayoutKind.Auto to have the CLR arrange the fields, LayoutKind.Sequential to have the CLR preserve your field layout, or LayoutKind.Explicit to explicitly arrange the fields in memory by using offsets. If you don\u2019t explicitly specify the</p><p>StructLayoutAttribute on a type that you\u2019re defining, your compiler selects whatever layout it determines is best.</p><p>You should be aware that Microsoft\u2019s C# compiler selects LayoutKind.Auto for reference types (classes) and LayoutKind.Sequential for value types (structures). It is obvious that the C# compiler team believes that structures are commonly used when interoperating with unmanaged code, and for this to work, the fields must stay in the order defined by the programmer. However, if you\u2019re creating a value type that has nothing to do with interoperability with unmanaged code, you could override the C# compiler\u2019s default. Here\u2019s an example.</p><p>using System; using System.Runtime.InteropServices;</p><p>// Let the CLR arrange the fields to improve // performance for this value type. [StructLayout(LayoutKind.Auto)] internal struct SomeValType {</p><p>private readonly Byte m_b; private readonly Int16 m_x;</p><p>... }</p><p>The StructLayoutAttribute also allows you to explicitly indicate the offset of each field by passing LayoutKind.Explicit to its constructor. Then you apply an instance of the System.Runtime.InteropServices.FieldOffsetAttribute attribute to each field passing to this attribute\u2019s constructor an Int32 indicating the offset (in bytes) of the field\u2019s first byte from the beginning of the instance. Explicit layout is typically used to simulate what would be a union in unmanaged C/C++ because you can have multiple fields starting at the same offset in memory.</p><p>Here is an example.</p><p>using System; using System.Runtime.InteropServices;</p><p>// The developer explicitly arranges the fields of this value type. [StructLayout(LayoutKind.Explicit)] internal struct SomeValType {</p><p>[FieldOffset(0)] private readonly Byte m_b; // The m_b and m_x fields overlap each</p><p>}</p><p>[FieldOffset(0)] private readonly Int16 m_x; // other in instances of this type</p><p>It should be noted that it is illegal to define a type in which a reference type and a value type overlap. It is possible to define a type in which multiple reference types overlap at the same starting offset; however, this is unverifiable. It is legal to define a type in which multiple value types overlap; however, all of the overlapping bytes must be accessible via public fields for the type to be verifiable.</p><h6>Boxing and Unboxing Value Types</h6><p>Value types are lighter weight than reference types because they are not allocated as objects in the managed heap, not garbage collected, and not referred to by pointers. However, in many cases, you must get a reference to an instance of a value type. For example, let\u2019s say that you wanted to create an ArrayList object (a type defined in the System.Collections namespace) to hold a set of Point structures. The code might look like this.</p><p>// Declare a value type. struct Point {</p><p>public Int32 x, y; }</p><p>public sealed class Program {</p><p>public static void Main() { ArrayList a = new ArrayList(); Point p; // Allocate a Point (not in the heap). for (Int32 i = 0; i &lt; 10; i++) {</p><p>p.x = p.y = i; // Initialize the members in the value type. a.Add(p); // Box the value type and add the</p><p>// reference to the Arraylist. }</p><p>... }</p><p>}</p><p>With each iteration of the loop, a Point\u2019s value type fields are initialized. Then the Point is stored in the ArrayList. But let\u2019s think about this for a moment. What is actually being stored in the ArrayList? Is it the Point structure, the address of the Point structure, or something else entirely?</p><p>To get the answer, you must look up ArrayList\u2019s Add method and see what type its parameter is defined as. In this case, the Add method is prototyped as follows.</p><p>public virtual Int32 Add(Object value);</p><p>From this, you can plainly see that Add takes an Object as a parameter, indicating that Add requires a reference (or pointer) to an object on the managed heap as a parameter. But in the preceding code, I\u2019m passing p, a Point, which is a value type. For this code to work, the Point value type must be converted into a true heap-managed object, and a reference to this object must be obtained.</p><p>It\u2019s possible to convert a value type to a reference type by using a mechanism called boxing. Internally, here\u2019s what happens when an instance of a value type is boxed:</p><ul style=\"list-style-type: none;\">\n<li><p>1. Memory is allocated from the managed heap. The amount of memory allocated is the size required by the value type\u2019s fields plus the two additional overhead members (the type object pointer and the sync block index) required by all objects on the managed heap.</p></li>\n<li><p>2. The value type\u2019s fields are copied to the newly allocated heap memory.</p></li>\n<li><p>3. The address of the object is returned. This address is now a reference to an object; the value type is now a reference type.</p></li>\n</ul><p>The C# compiler automatically produces the IL code necessary to box a value type instance, but you still need to understand what\u2019s going on internally so that you\u2019re aware of code size and performance issues.</p><p>In the preceding code, the C# compiler detected that I was passing a value type to a method that requires a reference type, and it automatically emitted code to box the object. So at run time, the fields currently residing in the Point value type instance p are copied into the newly allocated Point object. The address of the boxed Point object (now a reference type) is returned and is then passed to the Add method. The Point object will remain in the heap until it is garbage collected. The Point value type variable (p) can be reused because the ArrayList never knows anything about it. Note that the lifetime of the boxed value type extends beyond the lifetime of the unboxed value type.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile52.png\"/><p>Note It should be noted that the FCL now includes a new set of generic collection classes that make the non-generic collection classes obsolete. For example, you should use the System.Collections.Generic.List&lt;T&gt; class instead of the System.Collections.ArrayList class. The generic collection classes offer many improvements over the non-generic equivalents. For example, the API has been cleaned up and improved, and the performance of the collection classes has been greatly improved as well. But one of the biggest improvements is that the generic collection classes allow you to work with collections of value types without requiring that items in the collection be boxed/unboxed. This in itself greatly improves performance because far fewer objects will be created on the managed heap, thereby reducing the number of garbage collections required by your application. Furthermore, you will get compile-time type safety, and your source code will be cleaner due to fewer casts. This will all be explained in further detail in Chapter 12, \u201cGenerics.\u201d</p><p>Now that you know how boxing works, let\u2019s talk about unboxing. Let\u2019s say that you want to grab the first element out of the ArrayList by using the following code.</p><p>Point p = (Point) a[0];</p><p>Here you\u2019re taking the reference (or pointer) contained in element 0 of the ArrayList and trying to put it into a Point value type instance, p. For this to work, all of the fields contained in the boxed Point object must be copied into the value type variable, p, which is on the thread\u2019s stack. The CLR accomplishes this copying in two steps. First, the address of the Point fields in the boxed Point object is obtained. This process is called unboxing. Then, the values of these fields are copied from the heap to the stack-based value type instance.</p><p>Unboxing is not the exact opposite of boxing. The unboxing operation is much less costly than boxing. Unboxing is really just the operation of obtaining a pointer to the raw value type (data fields) contained within an object. In effect, the pointer refers to the unboxed portion in the boxed instance. So, unlike boxing, unboxing doesn\u2019t involve the copying of any bytes in memory. Having made this important clarification, it is important to note that an unboxing operation is typically followed by copying the fields.</p><p>Obviously, boxing and unboxing/copy operations hurt your application\u2019s performance in terms of both speed and memory, so you should be aware of when the compiler generates code to perform these operations automatically and try to write code that minimizes this code generation.</p><p>Internally, here\u2019s exactly what happens when a boxed value type instance is unboxed:</p><ul style=\"list-style-type: none;\">\n<li><p>1. If the variable containing the reference to the boxed value type instance is null, a NullReferenceException is thrown.</p></li>\n<li><p>2. If the reference doesn\u2019t refer to an object that is a boxed instance of the desired value type, an InvalidCastException is thrown.1</p></li>\n</ul><p>The second item in the preceding list means that the following code will not work as you might expect.</p><p>public static void Main() { Int32 x = 5; Object o = x; // Box x; o refers to the boxed object Int16 y = (Int16) o; // Throws an InvalidCastException</p><p>}</p><p>Logically, it makes sense to take the boxed Int32 that o refers to and cast it to an Int16. However, when unboxing an object, the cast must be to the exact unboxed value type\u2014Int32 in this case. Here\u2019s the correct way to write this code.</p><p>public static void Main() { Int32 x = 5; Object o = x; // Box x; o refers to the boxed object Int16 y = (Int16)(Int32) o; // Unbox to the correct type and cast</p><p>}</p><p>1 The CLR also allows you to unbox a value type into a nullable version of the same value type. This is discussed in Chapter 19.</p><p>I mentioned earlier that an unboxing operation is frequently followed immediately by a field copy. Let\u2019s take a look at some C# code demonstrating that unbox and copy operations work together.</p><p>public static void Main() { Point p; p.x = p.y = 1; Object o = p; // Boxes p; o refers to the boxed instance</p><p>p = (Point) o; // Unboxes o AND copies fields from boxed</p><p>// instance to stack variable }</p><p>On the last line, the C# compiler emits an IL instruction to unbox o (get the address of the fields in the boxed instance) and another IL instruction to copy the fields from the heap to the stack-based variable p.</p><p>Now look at this code.</p><p>public static void Main() { Point p; p.x = p.y = 1; Object o = p; // Boxes p; o refers to the boxed instance</p><p>}</p><p>// Change Point's x field to 2 p = (Point) o; // Unboxes o AND copies fields from boxed</p><p>// instance to stack variable p.x = 2; // Changes the state of the stack variable o = p; // Boxes p; o refers to a new boxed instance</p><p>The code at the bottom of this fragment is intended only to change Point\u2019s x field from 1 to 2. To do this, an unbox operation must be performed, followed by a field copy, followed by changing the field (on the stack), followed by a boxing operation (which creates a whole new boxed instance in the managed heap). Hopefully, you see the impact that boxing and unboxing/copying operations have on your application\u2019s performance.</p><p>Some languages, such as C++/CLI, allow you to unbox a boxed value type without copying the fields. Unboxing returns the address of the unboxed portion of a boxed object (ignoring the object\u2019s type object pointer and sync block index overhead). You can now use this pointer to manipulate the unboxed instance\u2019s fields (which happen to be in a boxed object on the heap). For example, the previous code would be much more efficient if written in C++/CLI, because you could change the value of Point\u2019s x field within the already boxed Point instance. This would avoid both allocating a new object on the heap and copying all of the fields twice!</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile53.png\"/><p>Important If you\u2019re the least bit concerned about your application\u2019s performance, you must be aware of when the compiler produces the code that performs these operations. Unfortunately, many compilers implicitly emit code to box objects, so it is not obvious when you write code that boxing is occurring. If I am concerned about the performance of a particular algorithm, I always use a tool such as ILDasm.exe to view the IL code for my methods and see where the box IL instructions are.</p><p>Let\u2019s look at a few more examples that demonstrate boxing and unboxing.</p><p>public static void Main() { Int32 v = 5; // Create an unboxed value type variable. Object o = v; // o refers to a boxed Int32 containing 5. v = 123; // Changes the unboxed value to 123</p><p>Console.WriteLine(v + \", \" + (Int32) o); // Displays \"123, 5\" }</p><p>In this code, can you guess how many boxing operations occur? You might be surprised to discover that the answer is three! Let\u2019s analyze the code carefully to really understand what\u2019s going on. To help you understand, I\u2019ve included the IL code generated for the Main method shown in the preceding code. I\u2019ve commented the code so that you can easily see the individual operations.</p><p>.method public hidebysig static void Main() cil managed {</p><p>.entrypoint // Code size 45 (0x2d)</p><p>.maxstack 3</p><p>.locals init ([0]int32 v,</p><p>[1] object o) // Load 5 into v.</p><ul style=\"list-style-type: none;\">\n<li><p>IL_0000: ldc.i4.5</p></li>\n<li><p>IL_0001: stloc.0</p><p>// Box v and store the reference pointer in o.</p>\n</li>\n<li><p>IL_0002: ldloc.0</p></li>\n<li><p>IL_0003: box [mscorlib]System.Int32</p></li>\n</ul><ul style=\"list-style-type: none;\">\n<li><p>IL_0008: stloc.1</p><p>// Load 123 into v.</p>\n</li>\n<li><p>IL_0009: ldc.i4.s 123</p></li>\n</ul><ul style=\"list-style-type: none;\">\n<li><p>IL_000b: stloc.0</p><p>// Box v and leave the pointer on the stack for Concat.</p>\n</li>\n<li><p>IL_000c: ldloc.0</p></li>\n<li><p>IL_000d: box [mscorlib]System.Int32</p></li>\n</ul><p>// Load the string on the stack for Concat. IL_0012: ldstr \", \"</p><p>// Unbox o: Get the pointer to the In32's field on the stack.</p><ul style=\"list-style-type: none;\">\n<li><p>IL_0017: ldloc.1</p></li>\n<li><p>IL_0018: unbox.any [mscorlib]System.Int32</p></li>\n</ul><p>// Box the Int32 and leave the pointer on the stack for Concat. IL_001d: box [mscorlib]System.Int32</p><p>// Call Concat. IL_0022: call string [mscorlib]System.String::Concat(object, object, object)</p><p>// The string returned from Concat is passed to WriteLine. IL_0027: call void [mscorlib]System.Console::WriteLine(string)</p><p>// Return from Main terminating this application. IL_002c: ret</p><p>} // end of method App::Main</p><p>First, an Int32 unboxed value type instance (v) is created on the stack and initialized to 5. Then a variable (o) typed as Object is created, and is initialized to point to v. But because reference type variables must always point to objects in the heap, C# generated the proper IL code to box and store the address of the boxed copy of v in o. Now the value 123 is placed into the unboxed value type instance v; this has no effect on the boxed Int32 value, which keeps its value of 5.</p><p>Next is the call to the WriteLine method. WriteLine wants a String object passed to it, but there is no string object. Instead, these three items are available: an unboxed Int32 value type instance (v), a String (which is a reference type), and a reference to a boxed Int32 value type instance (o) that is being cast to an unboxed Int32. These must somehow be combined to create a String.</p><p>To create a String, the C# compiler generates code that calls String\u2019s static Concat method. There are several overloaded versions of the Concat method, all of which perform identically\u2014the only difference is in the number of parameters. Because a string is being created from the concatenation of three items, the compiler chooses the following version of the Concat method.</p><p>public static String Concat(Object arg0, Object arg1, Object arg2);</p><p>For the first parameter, arg0, v is passed. But v is an unboxed value parameter and arg0 is an Object, so v must be boxed and the address to the boxed v is passed for arg0. For the arg1 parameter, the \",\" string is passed as a reference to a String object. Finally, for the arg2 parameter,</p><ul style=\"list-style-type: none;\">\n<li><p>o (a reference to an Object) is cast to an Int32. This requires an unboxing operation (but no copy</p></li>\n<li><p>operation), which retrieves the address of the unboxed Int32 contained inside the boxed Int32. This unboxed Int32 instance must be boxed again and the new boxed instance\u2019s memory address passed for Concat\u2019s arg2 parameter.</p></li>\n</ul><p>The Concat method calls each of the specified objects\u2019 ToString method and concatenates each object\u2019s string representation. The String object returned from Concat is then passed to WriteLine to show the final result.</p><p>I should point out that the generated IL code is more efficient if the call to WriteLine is written as follows.</p><p>Console.WriteLine(v + \", \" + o);// Displays \"123, 5\"</p><p>This line is identical to the earlier version except that I\u2019ve removed the (Int32) cast that preceded the variable o. This code is more efficient because o is already a reference type to an Object and its address can simply be passed to the Concat method. So, removing the cast saved two operations: an unbox and a box. You can easily see this savings by rebuilding the application and examining the generated IL code, as shown in the following code.</p><p>.method public hidebysig static void Main() cil managed {</p><p>.entrypoint // Code size 35 (0x23)</p><p>.maxstack 3</p><p>.locals init ([0] int32 v, [1] object o)</p><p>// Load 5 into v.</p><ul style=\"list-style-type: none;\">\n<li><p>IL_0000: ldc.i4.5</p></li>\n<li><p>IL_0001: stloc.0</p><p>// Box v and store the reference pointer in o.</p>\n</li>\n<li><p>IL_0002: ldloc.0</p></li>\n<li><p>IL_0003: box [mscorlib]System.Int32</p></li>\n</ul><ul style=\"list-style-type: none;\">\n<li><p>IL_0008: stloc.1</p><p>// Load 123 into v.</p>\n</li>\n<li><p>IL_0009: ldc.i4.s 123</p></li>\n</ul><ul style=\"list-style-type: none;\">\n<li><p>IL_000b: stloc.0</p><p>// Box v and leave the pointer on the stack for Concat.</p>\n</li>\n<li><p>IL_000c: ldloc.0</p></li>\n<li><p>IL_000d: box [mscorlib]System.Int32</p></li>\n</ul><p>// Load the string on the stack for Concat. IL_0012: ldstr \", \"</p><p>// Load the address of the boxed Int32 on the stack for Concat.</p><ul style=\"list-style-type: none;\">\n<li><p>IL_0017: ldloc.1</p><p>// Call Concat.</p>\n</li>\n<li><p>IL_0018: call string [mscorlib]System.String::Concat(object, object, object)</p></li>\n</ul><p>// The string returned from Concat is passed to WriteLine. IL_001d: call void [mscorlib]System.Console::WriteLine(string)</p><p>// Return from Main terminating this application. IL_0022: ret</p><p>} // end of method App::Main</p><p>A quick comparison of the IL for these two versions of the Main method shows that the version without the (Int32) cast is 10 bytes smaller than the version with the cast. The extra unbox/box steps in the first version are obviously generating more code. An even bigger concern, however, is that the extra boxing step allocates an additional object from the managed heap that must be garbage collected in the future. Certainly, both versions give identical results, and the difference in speed isn\u2019t noticeable, but extra, unnecessary boxing operations occurring in a loop cause the performance and memory usage of your application to be seriously degraded.</p><p>You can improve the previous code even more by calling WriteLine like this.</p><p>Console.WriteLine(v.ToString() + \", \" + o); // Displays \"123, 5\"</p><p>Now ToString is called on the unboxed value type instance v, and a String is returned. String objects are already reference types and can simply be passed to the Concat method without requiring any boxing.</p><p>Let\u2019s look at yet another example that demonstrates boxing and unboxing.</p><p>public static void Main() { Int32 v = 5; // Create an unboxed value type variable. Object o = v; // o refers to the boxed version of v.</p><p>v = 123; // Changes the unboxed value type to 123 Console.WriteLine(v); // Displays \"123\"</p><p>}</p><p>v = (Int32) o; // Unboxes and copies o into v Console.WriteLine(v); // Displays \"5\"</p><p>How many boxing operations do you count in this code? The answer is one. The reason that there is only one boxing operation is that the System.Console class defines a WriteLine method that accepts an Int32 as a parameter.</p><p>public static void WriteLine(Int32 value);</p><p>In the two preceding calls to WriteLine, the variable v, an Int32 unboxed value type instance, is passed by value. Now it may be that WriteLine will box this Int32 internally, but you have no control over that. The important thing is that you\u2019ve done the best you could and have eliminated the boxing from your own code.</p><p>If you take a close look at the FCL, you\u2019ll notice many overloaded methods that differ based on their value type parameters. For example, the System.Console type offers several overloaded versions of the WriteLine method.</p><p>public static void WriteLine(Boolean); public static void WriteLine(Char); public static void WriteLine(Char[]); public static void WriteLine(Int32); public static void WriteLine(UInt32); public static void WriteLine(Int64); public static void WriteLine(UInt64); public static void WriteLine(Single); public static void WriteLine(Double); public static void WriteLine(Decimal); public static void WriteLine(Object); public static void WriteLine(String);</p><p>You\u2019ll also find a similar set of overloaded methods for System.Console\u2019s Write method, System.IO.BinaryWriter\u2019s Write method, System.IO.TextWriter\u2019s Write and WriteLine methods, System.Runtime.Serialization.SerializationInfo\u2019s AddValue method, System.Text.StringBuilder\u2019s Append and Insert methods, and so on. Most of these methods offer overloaded versions for the sole purpose of reducing the number of boxing operations for the common value types.</p><p>If you define your own value type, these FCL classes will not have overloads of these methods that accept your value type. Furthermore, there are a bunch of value types already defined in the FCL for which overloads of these methods do not exist. If you call a method that does not have an overload for the specific value type that you are passing to it, you will always end up calling the overload that takes an Object. Passing a value type instance as an Object will cause boxing to occur, which will adversely affect performance. If you are defining your own class, you can define the methods in the class to be generic (possibly constraining the type parameters to be value types). Generics give you a way to define a method that can take any kind of value type without having to box it. Generics are discussed in Chapter 12.</p><p>One last point about boxing: if you know that the code that you\u2019re writing is going to cause the compiler to box a single value type repeatedly, your code will be smaller and faster if you manually box the value type. Here\u2019s an example.</p><p>using System;</p><p>public sealed class Program { public static void Main() { Int32 v = 5; // Create an unboxed value type variable.</p><p>#if INEFFICIENT // When compiling the following line, v is boxed // three times, wasting time and memory. Console.WriteLine(\"{0}, {1}, {2}\", v, v, v);</p><p>#else</p><p>// The lines below have the same result, execute // much faster, and use less memory. Object o = v; // Manually box v (just once).</p><p>// No boxing occurs to compile the following line. Console.WriteLine(\"{0}, {1}, {2}\", o, o, o);</p><p>#endif</p><p>} }</p><p>If this code is compiled with the INEFFICIENT symbol defined, the compiler will generate code to box v three times, causing three objects to be allocated from the heap! This is extremely wasteful because each object will have exactly the same contents: 5. If the code is compiled without the INEFFICIENT symbol defined, v is boxed just once, so only one object is allocated from the heap. Then, in the call to Console.WriteLine, the reference to the single boxed object is passed three times. This second version executes much faster and allocates less memory from the heap.</p><p>In these examples, it\u2019s fairly easy to recognize when an instance of a value type requires boxing. Basically, if you want a reference to an instance of a value type, the instance must be boxed. Usually this happens because you have a value type instance and you want to pass it to a method that requires a reference type. However, this situation isn\u2019t the only one in which you\u2019ll need to box an instance of a value type.</p><p>Recall that unboxed value types are lighter-weight types than reference types for two reasons:</p><p>\u25a0\u25a0 They are not allocated on the managed heap.</p><p>\u25a0\u25a0 They don\u2019t have the additional overhead members that every object on the heap has: a type</p><p>object pointer and a sync block index.</p><p>Because unboxed value types don\u2019t have a sync block index, you can\u2019t have multiple threads synchronize their access to the instance by using the methods of the System.Threading.Monitor type (or by using C#\u2019s lock statement).</p><p>Even though unboxed value types don\u2019t have a type object pointer, you can still call virtual methods (such as Equals, GetHashCode, or ToString) inherited or overridden by the type. If your value type overrides one of these virtual methods, then the CLR can invoke the method nonvirtually because value types are implicitly sealed and cannot have any types derived from them. In addition, the value type instance being used to invoke the virtual method is not boxed. However, if your override of the virtual method calls into the base type's implementation of the method, then the value type instance does get boxed when calling the base type's implementation so that a reference to a heap object gets passed to the this pointer into the base method.</p><p>However, calling a nonvirtual inherited method (such as GetType or MemberwiseClone) always requires the value type to be boxed because these methods are defined by System.Object, so the methods expect the this argument to be a pointer that refers to an object on the heap.</p><p>In addition, casting an unboxed instance of a value type to one of the type\u2019s interfaces requires the instance to be boxed, because interface variables must always contain a reference to an object on the heap. (I\u2019ll talk about interfaces in Chapter 13, \u201cInterfaces.\u201d) The following code demonstrates.</p><p>using System;</p><p>internal struct Point : IComparable { private readonly Int32 m_x, m_y;</p><p>// Constructor to easily initialize the fields public Point(Int32 x, Int32 y) {</p><p>m_x = x; m_y = y;</p><p>}</p><p>// Override ToString method inherited from System.ValueType public override String ToString() {</p><p>// Return the point as a string. Note: calling ToString prevents boxing return String.Format(\"({0}, {1})\", m_x.ToString(), m_y.ToString());</p><p>}</p><p>// Implementation of type\u00adsafe CompareTo method public Int32 CompareTo(Point other) {</p><p>// Use the Pythagorean Theorem to calculate // which point is farther from the origin (0, 0) return Math.Sign(Math.Sqrt(m_x * m_x + m_y * m_y)</p><p>\u00ad Math.Sqrt(other.m_x * other.m_x + other.m_y * other.m_y)); }</p><p>// Implementation of IComparable's CompareTo method public Int32 CompareTo(Object o) {</p><p>if (GetType() != o.GetType()) { throw new ArgumentException(\"o is not a Point\");</p><p>} // Call type\u00adsafe CompareTo method return CompareTo((Point) o);</p><p>} }</p><p>public static class Program { public static void Main() { // Create two Point instances on the stack.</p><ul style=\"list-style-type: none;\">\n<li><p>Point p1 = new Point(10, 10);</p></li>\n<li><p>Point p2 = new Point(20, 20);</p></li>\n</ul><p>// p1 does NOT get boxed to call ToString (a virtual method). Console.WriteLine(p1.ToString());// \"(10, 10)\"</p><p>// p DOES get boxed to call GetType (a non\u00advirtual method). Console.WriteLine(p1.GetType());// \"Point\"</p><p>// p1 does NOT get boxed to call CompareTo. // p2 does NOT get boxed because CompareTo(Point) is called. Console.WriteLine(p1.CompareTo(p2));// \"\u00ad1\"</p><p>// p1 DOES get boxed, and the reference is placed in c. IComparable c = p1; Console.WriteLine(c.GetType());// \"Point\"</p><ul style=\"list-style-type: none;\">\n<li><p>// p1 does NOT get boxed to call CompareTo. // Because CompareTo is not being passed a Point variable, // CompareTo(Object) is called, which requires a reference to // a boxed Point. // c does NOT get boxed because it already refers to a boxed Point.</p><ul style=\"list-style-type: none;\">\n<li><p>Console.WriteLine(p1.CompareTo(c));// \"0\"</p><p>// c does NOT get boxed because it already refers to a boxed Point. // p2 does get boxed because CompareTo(Object) is called. Console.WriteLine(c.CompareTo(p2));// \"\u00ad1\"</p>\n<p>// c is unboxed, and fields are copied into p2. p2 = (Point) c;</p>\n<p>// Proves that the fields got copied into p2.</p>\n</li>\n<li><p>Console.WriteLine(p2.ToString());// \"(10, 10)\"</p></li>\n</ul>\n</li>\n</ul><p>} }</p><p>This code demonstrates several scenarios related to boxing and unboxing:</p><p>\u25a0\u25a0 Calling ToString In the call to ToString, p1 doesn\u2019t have to be boxed. At first, you\u2019d think that p1 would have to be boxed because ToString is a virtual method that is inherited from the base type, System.ValueType. Normally, to call a virtual method, the CLR needs to determine the object\u2019s type in order to locate the type\u2019s method table. Because p1 is an unboxed</p><p>value type, there\u2019s no type object pointer. However, the just-in-time (JIT) compiler sees that Point overrides the ToString method, and it emits code that calls ToString directly (non-</p><p>virtually) without having to do any boxing. The compiler knows that polymorphism can\u2019t come into play here because Point is a value type, and no type can derive from it to provide another implementation of this virtual method. Note that if Point's ToString method internally calls</p><p>base.ToString(), then the value type instance would be boxed when calling System.ValueType's ToString method.</p><p>\u25a0\u25a0 Calling GetType In the call to the nonvirtual GetType method, p1 does have to be boxed. The reason is that the Point type inherits GetType from System.Object. So to call GetType, the CLR must use a pointer to a type object, which can be obtained only by boxing p1.</p><p>\u25a0\u25a0 Calling CompareTo (first time) In the first call to CompareTo, p1 doesn\u2019t have to be boxed because Point implements the CompareTo method, and the compiler can just call it directly. Note that a Point variable (p2) is being passed to CompareTo, and therefore the compiler calls the overload of CompareTo that accepts a Point parameter. This means that p2 will be passed by value to CompareTo and no boxing is necessary.</p><p>\u25a0\u25a0 Casting to IComparable When casting p1 to a variable (c) that is of an interface type, p1 must be boxed because interfaces are reference types by definition. So p1 is boxed, and the pointer to this boxed object is stored in the variable c. The following call to GetType proves that c does refer to a boxed Point on the heap.</p><p>\u25a0\u25a0 Calling CompareTo (second time) In the second call to CompareTo, p1 doesn\u2019t have to be boxed because Point implements the CompareTo method, and the compiler can just call it directly. Note that an IComparable variable (c) is being passed to CompareTo, and therefore, the compiler calls the overload of CompareTo that accepts an Object parameter. This means that the argument passed must be a pointer that refers to an object on the heap. Fortunately, c does refer to a boxed Point, and therefore, that memory address in c can be passed to CompareTo, and no additional boxing is necessary.</p><p>\u25a0\u25a0 Calling CompareTo (third time) In the third call to CompareTo, c already refers to a boxed Point object on the heap. Because c is of the IComparable interface type, you can call only the interface\u2019s CompareTo method that requires an Object parameter. This means that the argument passed must be a pointer that refers to an object on the heap. So p2 is boxed, and the pointer to this boxed object is passed to CompareTo.</p><p>\u25a0\u25a0 Casting to Point When casting c to a Point, the object on the heap referred to by c is unboxed, and its fields are copied from the heap to p2, an instance of the Point type residing on the stack.</p><p>I realize that all of this information about reference types, value types, and boxing might be overwhelming at first. However, a solid understanding of these concepts is critical to any .NET Framework developer\u2019s long-term success. Trust me: having a solid grasp of these concepts will allow you to build efficient applications faster and easier.</p><h6>Changing Fields in a Boxed Value Type by Using Interfaces (and Why You Shouldn\u2019t Do This)</h6><p>Let\u2019s have some fun and see how well you understand value types, boxing, and unboxing. Examine the following code, and see whether you can figure out what it displays on the console.</p><p>using System;</p><p>// Point is a value type. internal struct Point {</p><p>private Int32 m_x, m_y;</p><p>public Point(Int32 x, Int32 y) {</p><ul style=\"list-style-type: none;\">\n<li><p>m_x = x;</p></li>\n<li><p>m_y = y;</p></li>\n</ul><p>}</p><p>public void Change(Int32 x, Int32 y) {</p><p>m_x = x; m_y = y; }</p><p>}</p><p>public override String ToString() {</p><p>return String.Format(\"({0}, {1})\", m_x.ToString(), m_y.ToString()); }</p><p>public sealed class Program { public static void Main() { Point p = new Point(1, 1);</p><p>Console.WriteLine(p);</p><p>p.Change(2, 2); Console.WriteLine(p);</p><p>Object o = p; Console.WriteLine(o);</p><p>} }</p><p>((Point) o).Change(3, 3); Console.WriteLine(o);</p><p>Very simply, Main creates an instance (p) of a Point value type on the stack and sets its m_x and m_y fields to 1. Then, p is boxed before the first call to WriteLine, which calls ToString on the boxed Point, and (1, 1) is displayed as expected. Then, p is used to call the Change method, which changes the values of p\u2019s m_x and m_y fields on the stack to 2. The second call to WriteLine requires p to be boxed again and displays (2, 2), as expected.</p><p>Now, p is boxed a third time, and o refers to the boxed Point object. The third call to WriteLine again shows (2, 2), which is also expected. Finally, I want to call the Change method to update the fields in the boxed Point object. However, Object (the type of the variable o) doesn\u2019t know anything about the Change method, so I must first cast o to a Point. Casting o to a Point unboxes o and</p><p>copies the fields in the boxed Point to a temporary Point on the thread\u2019s stack! The m_x and m_y fields of this temporary point are changed to 3 and 3, but the boxed Point isn\u2019t affected by this call to Change. When WriteLine is called the fourth time, (2, 2) is displayed again. Many developers do not expect this.</p><p>Some languages, such as C++/CLI, let you change the fields in a boxed value type, but C# does not. However, you can fool C# into allowing this by using an interface. The following code is a modified version of the previous code.</p><p>using System;</p><p>// Interface defining a Change method internal interface IChangeBoxedPoint {</p><p>void Change(Int32 x, Int32 y); }</p><p>// Point is a value type. internal struct Point : IChangeBoxedPoint {</p><p>private Int32 m_x, m_y;</p><p>public Point(Int32 x, Int32 y) {</p><ul style=\"list-style-type: none;\">\n<li><p>m_x = x;</p></li>\n<li><p>m_y = y;</p></li>\n</ul><p>}</p><p>public void Change(Int32 x, Int32 y) {</p><p>m_x = x; m_y = y; }</p><p>}</p><p>public override String ToString() {</p><p>return String.Format(\"({0}, {1})\", m_x.ToString(), m_y.ToString()); }</p><p>public sealed class Program { public static void Main() { Point p = new Point(1, 1);</p><p>Console.WriteLine(p);</p><p>p.Change(2, 2); Console.WriteLine(p);</p><p>Object o = p; Console.WriteLine(o);</p><p>((Point) o).Change(3, 3);</p><ul style=\"list-style-type: none;\">\n<li><p>Console.WriteLine(o);</p><p>// Boxes p, changes the boxed object and discards it ((IChangeBoxedPoint) p).Change(4, 4);</p>\n</li>\n<li><p>Console.WriteLine(p);</p></li>\n</ul><p>} }</p><p>// Changes the boxed object and shows it ((IChangeBoxedPoint) o).Change(5, 5); Console.WriteLine(o);</p><p>This code is almost identical to the previous version. The main difference is that the Change method is defined by the IChangeBoxedPoint interface, and the Point type now implements this interface. Inside Main, the first four calls to WriteLine are the same and produce the same results I had before (as expected). However, I\u2019ve added two more examples at the end of Main.</p><p>In the first example, the unboxed Point, p, is cast to an IChangeBoxedPoint. This cast causes the value in p to be boxed. Change is called on the boxed value, which does change its m_x and m_y fields to 4 and 4, but after Change returns, the boxed object is immediately ready to be garbage collected. So the fifth call to WriteLine displays (2, 2). Many developers won\u2019t expect this result.</p><p>In the last example, the boxed Point referred to by o is cast to an IChangeBoxedPoint. No boxing is necessary here because o is already a boxed Point. Then Change is called, which does change the boxed Point\u2019s m_x and m_y fields. The interface method Change has allowed me to change the fields in a boxed Point object! Now, when WriteLine is called, it displays (5, 5) as expected. The purpose of this whole example is to demonstrate how an interface method is able to modify the fields of a boxed value type. In C#, this isn\u2019t possible without using an interface method.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile54.png\"/><p>Important Earlier in this chapter, I mentioned that value types should be immutable: that is, they should not define any members that modify any of the type\u2019s instance fields. In fact, I recommended that value types have their fields marked as readonly so that the compiler will issue errors should you accidentally write a method that attempts to modify a field. The previous example should make it very clear to you why value types should be immutable. The unexpected behaviors shown in the previous example all occur when attempting to call a method that modifies the value type\u2019s instance fields. If after constructing a value type, you do not call any methods that modify its state, you will not get confused when all of the boxing and unboxing/field copying occurs. If the value type is immutable, you will end up just copying the same state around, and you will not be surprised by any of the behaviors you see.</p><p>A number of developers reviewed the chapters of this book. After reading through some of my code samples (such as the preceding one), these reviewers would tell me that they\u2019ve sworn off value types. I must say that these little value type nuances have cost me days of debugging time, which is why I spend time pointing them out in this book. I hope you\u2019ll remember some of these nuances and that you\u2019ll be prepared for them if and when they strike you and your code. Certainly, you shouldn\u2019t be scared of value types. They are useful, and they have their place. After all, a program needs a little Int32 love now and then. Just keep in mind that value types and reference types have very different behaviors depending on how they\u2019re used. In fact, you should take the preceding code and declare</p><p>the Point as a class instead of a struct to appreciate the different behavior that results. Finally, you'll be very happy to know that the core value types that ship in the FCL\u2014Byte, Int32, UInt32, Int64, UInt64, Single, Double, Decimal, BigInteger, Complex, all enums, and so on\u2014are all immutable, so you should experience no surprising behavior when using any of these types.</p><h6>Object Equality and Identity</h6><p>Frequently, developers write code to compare objects with one another. This is particularly true when placing objects in a collection and you\u2019re writing code to sort, search, or compare items in a collection. In this section, I'll discuss object equality and identity, and I\u2019ll also discuss how to define a type that properly implements object equality.</p><p>The System.Object type offers a virtual method named Equals, whose purpose is to return true if two objects contain the same value. The implementation of Object\u2019s Equals method looks like this.</p><p>public class Object { public virtual Boolean Equals(Object obj) {</p><p>// If both references point to the same object, // they must have the same value. if (this == obj) return true;</p><p>} }</p><p>// Assume that the objects do not have the same value. return false;</p><p>At first, this seems like a reasonable default implementation of Equals: it returns true if the this and obj arguments refer to the same exact object. This seems reasonable because Equals knows that an object must have the same value as itself. However, if the arguments refer to different objects, Equals can\u2019t be certain if the objects contain the same values, and therefore, false is returned. In other words, the default implementation of Object\u2019s Equals method really implements identity, not value equality.</p><p>Unfortunately, as it turns out, Object\u2019s Equals method is not a reasonable default, and it should have never been implemented this way. You immediately see the problem when you start thinking about class inheritance hierarchies and how to properly override Equals. Here is how to properly implement an Equals method internally:</p><ul style=\"list-style-type: none;\">\n<li><p>1. If the obj argument is null, return false because the current object identified by this is obviously not null when the nonstatic Equals method is called.</p></li>\n<li><p>2. If the this and obj arguments refer to the same object, return true. This step can improve performance when comparing objects with many fields.</p></li>\n</ul><ul style=\"list-style-type: none;\">\n<li><p>3. If the this and obj arguments refer to objects of different types, return false. Obviously, checking if a String object is equal to a FileStream object should result in a false result.</p></li>\n<li><p>4. For each instance field defined by the type, compare the value in the this object with the value in the obj object. If any fields are not equal, return false.</p></li>\n<li><p>5. Call the base class\u2019s Equals method so it can compare any fields defined by it. If the base class\u2019s Equals method returns false, return false; otherwise, return true.</p></li>\n</ul><p>So Microsoft should have implemented Object\u2019s Equals like this.</p><p>public class Object {</p><p>public virtual Boolean Equals(Object obj) { // The given object to compare to can't be null if (obj == null) return false;</p><p>// If objects are different types, they can't be equal. if (this.GetType() != obj.GetType()) return false;</p><p>} }</p><p>// If objects are same type, return true if all of their fields match // Because System.Object defines no fields, the fields match return true;</p><p>But, because Microsoft didn\u2019t implement Equals this way, the rules for how to implement Equals are significantly more complicated than you would think. When a type overrides Equals, the override should call its base class\u2019s implementation of Equals unless it would be calling Object\u2019s implementation. This also means that because a type can override Object\u2019s Equals method, this Equals method can no longer be called to test for identity. To fix this, Object offers a static ReferenceEquals method, which is implemented like this.</p><p>public class Object { public static Boolean ReferenceEquals(Object objA, Object objB) {</p><p>return (objA == objB); }</p><p>}</p><p>You should always call ReferenceEquals if you want to check for identity (if two references point to the same object). You shouldn\u2019t use the C# == operator (unless you cast both operands to Object first) because one of the operands\u2019 types could overload the == operator, giving it semantics other than identity.</p><p>As you can see, the .NET Framework has a very confusing story when it comes to object equality and identity. By the way, System.ValueType (the base class of all value types) does override Object\u2019s Equals method and is correctly implemented to perform a value equality check (not an identity check). Internally, ValueType\u2019s Equals is implemented this way:</p><ul style=\"list-style-type: none;\">\n<li><p>1. If the obj argument is null, return false.</p></li>\n<li><p>2. If the this and obj arguments refer to objects of different types, return false.</p></li>\n</ul><ul style=\"list-style-type: none;\">\n<li><p>3. For each instance field defined by the type, compare the value in the this object with the value in the obj object by calling the field\u2019s Equals method. If any fields are not equal, return false.</p></li>\n<li><p>4. Return true. Object\u2019s Equals method is not called by ValueType\u2019s Equals method.</p></li>\n</ul><p>Internally, ValueType\u2019s Equals method uses reflection (covered in Chapter 23, \u201cAssembly Loading and Reflection\u201d) to accomplish step 3. Because the CLR\u2019s reflection mechanism is slow, when defining your own value type, you should override Equals and provide your own implementation to improve the performance of value equality comparisons that use instances of your type. Of course, in your own implementation, do not call base.Equals.</p><p>When defining your own type, if you decide to override Equals, you must ensure that it adheres to the four properties of equality:</p><p>\u25a0\u25a0 Equals must be reflexive; that is, x.Equals(x) must return true.</p><p>\u25a0\u25a0 Equals must be symmetric; that is, x.Equals(y) must return the same value as</p><p>y.Equals(x).</p><p>\u25a0\u25a0 Equals must be transitive; that is, if x.Equals(y) returns true and y.Equals(z) returns</p><p>true, then x.Equals(z) must also return true.</p><p>\u25a0\u25a0 Equals must be consistent. Provided that there are no changes in the two values being com-</p><p>pared, Equals should consistently return true or false.</p><p>If your implementation of Equals fails to adhere to all of these rules, your application will behave in strange and unpredictable ways.</p><p>When overriding the Equals method, there are a couple more things that you\u2019ll probably want to do:</p><p>\u25a0\u25a0 Have the type implement the System.IEquatable&lt;T&gt; interface\u2019s Equals method This generic interface allows you to define a type-safe Equals method. Usually, you\u2019ll implement the Equals method that takes an Object parameter to internally call the type-safe Equals method.</p><p>\u25a0\u25a0 Overload the == and != operator methods Usually, you\u2019ll implement these operator meth-</p><p>ods to internally call the type-safe Equals method.</p><p>Furthermore, if you think that instances of your type will be compared for the purposes of sorting, you\u2019ll want your type to also implement System.IComparable\u2019s CompareTo method and</p><p>System.IComparable&lt;T&gt;\u2019s type-safe CompareTo method. If you implement these methods, you\u2019ll also want to overload the various comparison operator methods (&lt;, &lt;=, &gt;, &gt;=) and implement these methods internally to call the type-safe CompareTo method.</p><h6>Object Hash Codes</h6><p>The designers of the FCL decided that it would be incredibly useful if any instance of any object could be placed into a hash table collection. To this end, System.Object provides a virtual GetHashCode method so that an Int32 hash code can be obtained for any and all objects.</p><p>If you define a type and override the Equals method, you should also override the GetHashCode method. In fact, Microsoft\u2019s C# compiler emits a warning if you define a type that overrides Equals without also overriding GetHashCode. For example, compiling the following type yields this warning: warning CS0659: 'Program' overrides Object.Equals(object o) but does not override Object.GetHashCode().</p><p>public sealed class Program {</p><p>public override Boolean Equals(Object obj) { ... } }</p><p>The reason a type that defines Equals must also define GetHashCode is that the implementation of the System.Collections.Hashtable type, the System.Collections.Generic.Dictionary type, and some other collections require that any two objects that are equal must have the same hash code value. So if you override Equals, you should override GetHashCode to ensure that the algorithm you use for calculating equality corresponds to the algorithm you use for calculating the object\u2019s hash code.</p><p>Basically, when you add a key/value pair to a collection, a hash code for the key object is obtained first. This hash code indicates which \u201cbucket\u201d the key/value pair should be stored in. When the collection needs to look up a key, it gets the hash code for the specified key object. This code identifies the \u201cbucket\u201d that is now searched sequentially, looking for a stored key object that is equal to the specified key object. Using this algorithm of storing and looking up keys means that if you change a key object that is in a collection, the collection will no longer be able to find the object. If you intend to change a key object in a hash table, you should remove the original key/value pair, modify the key object, and then add the new key/value pair back into the hash table.</p><p>Defining a GetHashCode method can be easy and straightforward. But depending on your data types and the distribution of data, it can be tricky to come up with a hashing algorithm that returns a well-distributed range of values. Here\u2019s a simple example that will probably work just fine for Point objects.</p><p>internal sealed class Point { private readonly Int32 m_x, m_y; public override Int32 GetHashCode() {</p><p>return m_x ^ m_y; // m_x XOR'd with m_y }</p><p>... }</p><p>When selecting an algorithm for calculating hash codes for instances of your type, try to follow these guidelines:</p><p>\u25a0\u25a0 Use an algorithm that gives a good random distribution for the best performance of the hash</p><p>table.</p><p>\u25a0\u25a0 Your algorithm can also call the base type\u2019s GetHashCode method, including its return value. However, you don\u2019t generally want to call Object\u2019s or ValueType\u2019s GetHashCode method, because the implementation in either method doesn\u2019t lend itself to high-performance hashing algorithms.</p><p>\u25a0\u25a0 Your algorithm should use at least one instance field.</p><p>\u25a0\u25a0 Ideally, the fields you use in your algorithm should be immutable; that is, the fields should be initialized when the object is constructed, and they should never again change during the object\u2019s lifetime.</p><p>\u25a0\u25a0 Your algorithm should execute as quickly as possible.</p><p>\u25a0\u25a0 Objects with the same value should return the same code. For example, two String objects</p><p>with the same text should return the same hash code value.</p><p>System.Object\u2019s implementation of the GetHashCode method doesn\u2019t know anything about its derived type and any fields that are in the type. For this reason, Object\u2019s GetHashCode method returns a number that is guaranteed not to change for the lifetime of the object.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile55.png\"/><p>Important If you\u2019re implementing your own hash table collection for some reason, or you\u2019re implementing any piece of code in which you\u2019ll be calling GetHashCode, you should never, ever persist hash code values. The reason is that hash code values are subject to change. For example, a future version of a type might use a different algorithm for calculating the object\u2019s hash code.</p><p>There is a company that was not heeding this important warning. On their website, users could create new accounts by selecting a user name and a password. The website then took the password String, called GetHashCode, and persisted the hash code value in a database. When users logged back on to the website, they entered their password. The website would call GetHashCode again and compare the hash code value with the stored value in the database. If the hash codes matched, the user would be granted access. Unfortunately, when the company upgraded to a new version of the CLR, String\u2019s GetHashCode method had changed, and it now returned a different hash code value. The end result was that no user was able to log on to the website anymore!</p><h6>The dynamic Primitive Type</h6><p>C# is a type-safe programming language. This means that all expressions resolve into an instance of a type and the compiler will generate only code that is attempting to perform an operation that is valid for this type. The benefit of a type-safe programming language over a non\u2013type-safe programming language is that many programmer errors are detected at compile time, helping to ensure that the code is correct before you attempt to execute it. In addition, compile-time languages can typically produce smaller and faster code because they make more assumptions at compile time and bake those assumptions into the resulting IL and metadata.</p><p>However, there are also many occasions when a program has to act on information that it doesn\u2019t know about until it is running. Although you can use type-safe programming languages (like C#) to interact with this information, the syntax tends to be clumsy, especially because you tend to work a lot with strings, and performance is hampered as well. If you are writing a pure C# application, then the only occasion you have for working with runtime-determined information is when you are using reflection (discussed in Chapter 23). However, many developers also use C# to communicate with components that are not implemented in C#. Some of these components could be .NET-dynamic languages such as Python or Ruby, or COM objects that support the IDispatch interface (possibly implemented in native C or C++), or HTML Document Object Model (DOM) objects (implemented using various languages and technologies). Communicating with HTML DOM objects is particularly useful when building a Microsoft Silverlight application.</p><p>To make it easier for developers using reflection or communicating with other components, the C# compiler offers you a way to mark an expression\u2019s type as dynamic. You can also put the result of an expression into a variable and you can mark a variable\u2019s type as dynamic. This dynamic expression/ variable can then be used to invoke a member such as a field, a property/indexer, a method, delegate, and unary/binary/conversion operators. When your code invokes a member by using a dynamic expression/variable, the compiler generates special IL code that describes the desired operation. This special code is referred to as the payload. At run time, the payload code determines the exact operation to execute based on the actual type of the object now referenced by the dynamic expression/ variable.</p><p>Here is some code to demonstrate what I\u2019m talking about.</p><p>internal static class DynamicDemo {</p><p>public static void Main() { dynamic value; for (Int32 demo = 0; demo &lt; 2; demo++) {</p><p>value = (demo == 0) ? (dynamic) 5 : (dynamic) \"A\"; value = value + value; M(value);</p><p>} }</p><p>}</p><p>private static void M(Int32 n) { Console.WriteLine(\"M(Int32): \" + n); } private static void M(String s) { Console.WriteLine(\"M(String): \" + s); }</p><p>When I execute Main, I get the following output.</p><p>M(Int32): 10 M(String): AA</p><p>To understand what\u2019s happening, let\u2019s start by looking at the + operator. This operator has operands of the dynamic type. Because value is dynamic, the C# compiler emits payload code that will examine the actual type of value at run time and determine what the + operator should actually do.</p><p>The first time the + operator evaluates, value contains 5 (an Int32) and the result is 10 (also an Int32). This puts this result in the value variable. Then, the M method is called, passing it value. For the call to M, the compiler will emit payload code that will, at run time, examine the actual type of the argument being passed to M and determine which overload of the M method to call. When value contains an Int32, the overload of M that takes an Int32 parameter is called.</p><p>The second time the + operator evaluates, value contains \u201cA\u201d (a String) and the result is \u201cAA\u201d (the result of concatenating \u201cA\u201d with itself). Then, the M method is called again, passing it value. This time, the payload code determines that the actual type being passed to M is a String and calls the overload of M that takes a String parameter.</p><p>When the type of a field, method parameter, or method return type is specified as dynamic, the compiler converts this type to the System.Object type and applies an instance of System.Runtime.CompilerServices.DynamicAttribute to the field, parameter, or return type in metadata. If a local variable is specified as dynamic, then the variable\u2019s type will also be of type Object, but the DynamicAttribute is not applied to the local variable because its usage is self-contained within the method. Because dynamic is really the same as Object, you cannot write methods whose signature differs only by dynamic and Object.</p><p>It is also possible to use dynamic when specifying generic type arguments to a generic class (reference type), a structure (value type), an interface, a delegate, or a method. When you do this, the compiler converts dynamic to Object and applies DynamicAttribute to the various pieces of metadata where it makes sense. Note that the generic code that you are using has already been compiled and will consider the type to be Object; no dynamic dispatch will be performed because the compiler did not produce any payload code in the generic code.</p><p>Any expression can implicitly be cast to dynamic because all expressions result in a type that is derived from Object.2 Normally, the compiler does not allow you to write code that implicitly casts an expression from Object to another type; you must use explicit cast syntax. However, the compiler does allow you to cast an expression from dynamic to another type by using implicit cast syntax.</p><p>Object o1 = 123; // OK: Implicit cast from Int32 to Object (boxing)</p><ul style=\"list-style-type: none;\">\n<li><p>Int32 n1 = o; // Error: No implicit cast from Object to Int32</p></li>\n<li><p>Int32 n2 = (Int32) o; // OK: Explicit cast from Object to Int32 (unboxing)</p><p>dynamic d1 = 123; // OK: Implicit cast from Int32 to dynamic (boxing)</p>\n</li>\n<li><p>Int32 n3 = d1; // OK: Implicit cast from dynamic to Int32 (unboxing)</p></li>\n</ul><p>2 And, as always, value types will be boxed.</p><p>Although the compiler allows you to omit the explicit cast when casting from dynamic to some other type, the CLR will validate the cast at run time to ensure that type safety is maintained. If the object\u2019s type is not compatible with the cast, the CLR will throw an InvalidCastException exception.</p><p>Note that the result of evaluating a dynamic expression is a dynamic expression. Examine this code.</p><p>dynamic d = 123; var result = M(d); // Note: 'var result' is the same as 'dynamic result'</p><p>Here, the compiler allows the code to compile because it doesn\u2019t know at compile time which M method it will call. Therefore, it also does not know what type of result M will return. And so, the compiler assumes that the result variable is of type dynamic itself. You can verify this by placing your mouse over var in the Visual Studio editor; the IntelliSense window will indicate 'dynamic: Represents an object whose operations will be resolved at runtime.' If the M method invoked at run time has a return type of void, a Microsoft.CSharp.RuntimeBinder.RuntimeBinderException exception is thrown.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile56.png\"/><p>Important Do not confuse dynamic and var. Declaring a local variable using var is just a syntactical shortcut that has the compiler infer the specific data type from an expression. The var keyword can be used only for declaring local variables inside a method, whereas the dynamic keyword can be used for local variables, fields, and arguments. You cannot cast an expression to var but you can cast an expression to dynamic. You must explicitly initialize a variable declared using var, whereas you do not have to initialize a variable declared with dynamic. For more information about C#\u2019s var, see the \u201cImplicitly Typed Local Variables\u201d section in Chapter 9, \u201cParameters.\u201d</p><p>However, when converting from dynamic to another static type, the result\u2019s type is, of course, the static type. Similarly, when constructing a type by passing one or more dynamic arguments to its constructor, the result is the type of object you are constructing.</p><p>dynamic d = 123; var x = (Int32) d; // Conversion: 'var x' is the same as 'Int32 x' var dt = new DateTime(d); // Construction: 'var dt' is the same as 'DateTime dt'</p><p>If a dynamic expression is specified as the collection in a foreach statement or as a resource in a using statement, the compiler will generate code that attempts to cast the expression to the non-generic System.IEnumerable interface or to the System.IDisposable interface, respectively. If the cast succeeds, the expression is used and the code runs just fine. If the cast fails, a Microsoft.CSharp.RuntimeBinder.RuntimeBinderException exception is thrown.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile57.png\"/><p>Important A dynamic expression is really the same type as System.Object. The compiler assumes that whatever operation you attempt on the expression is legal, so the compiler will not generate any warnings or errors. However, exceptions will be thrown at run time if you attempt to execute an invalid operation. In addition, Visual Studio cannot offer any IntelliSense support to help you write code against a dynamic expression. You cannot define an extension method (discussed in Chapter 8, \u201cMethods\u201d) that extends dynamic, although you can define one that extends Object. And, you cannot pass a lambda expression or anonymous method (both discussed in Chapter 17, \u201cDelegates\u201d) as an argument to a dynamic method call because the compiler cannot infer the types being used.</p><p>Here is an example of some C# code that uses COM IDispatch to create a Microsoft Excel workbook and places a string in cell A1.</p><p>using Microsoft.Office.Interop.Excel;</p><p>... public static void Main() {</p><p>Application excel = new Application(); excel.Visible = true; excel.Workbooks.Add(Type.Missing); ((Range)excel.Cells[1, 1]).Value = \"Text in cell A1\"; // Put this string in cell A1</p><p>}</p><p>Without the dynamic type, the value returned from excel.Cells[1, 1] is of type Object, which must be cast to the Range type before its Value property can be accessed. However, when producing a runtime callable wrapper assembly for a COM object, any use of VARIANT in the COM method is really converted to dynamic; this is called dynamification. Therefore, because excel.Cells[1, 1] is of type dynamic, you do not have to explicitly cast it to the Range type before its Value property can be accessed. Dynamification can greatly simplify code that interoperates with COM objects. Here is the simpler code.</p><p>using Microsoft.Office.Interop.Excel;</p><p>... public static void Main() {</p><p>Application excel = new Application(); excel.Visible = true; excel.Workbooks.Add(Type.Missing); excel.Cells[1, 1].Value = \"Text in cell A1\"; // Put this string in cell A1</p><p>}</p><p>The following code shows how to use reflection to call a method (\u201cContains\u201d) on a String target (\u201cJeffrey Richter\u201d) passing it a String argument (\u201cff\u201d) and storing the Boolean result in a local variable (result).</p><p>Object target = \"Jeffrey Richter\"; Object arg = \"ff\";</p><p>// Find a method on the target that matches the desired argument types Type[] argTypes = new Type[] { arg.GetType() }; MethodInfo method = target.GetType().GetMethod(\"Contains\", argTypes);</p><p>// Invoke the method on the target passing the desired arguments Object[] arguments = new Object[] { arg }; Boolean result = Convert.ToBoolean(method.Invoke(target, arguments));</p><p>Using C#\u2019s dynamic type, this code can be rewritten with greatly improved syntax.</p><p>dynamic target = \"Jeffrey Richter\"; dynamic arg = \"ff\"; Boolean result = target.Contains(arg);</p><p>Earlier, I mentioned that the C# compiler emits payload code that, at run time, figures out what operation to perform based on the actual type of an object. This payload code uses a class known as a runtime binder. Different programming languages define their own runtime binders that encapsulate the rules of that language. The code for the C# runtime binder is in the Microsoft.CSharp.dll assembly, and you must reference this assembly when you build projects that use the dynamic keyword. This assembly is referenced in the compiler\u2019s default response file, CSC.rsp. It is the code in this assembly that knows to produce code (at run time) that performs addition when the + operator is applied to two Int32 objects and concatenation when applied to two String objects.</p><p>At run time, the Microsoft.CSharp.dll assembly will have to load into the AppDomain, which hurts your application\u2019s performance and increases memory consumption. Microsoft.CSharp.dll also loads System.dll and System.Core.dll. If you are using dynamic to help you interoperate with COM components, then System.Dynamic.dll will also load. And when the payload code executes, it generates dynamic code at run time; this code will be in an in-memory assembly called \u201cAnonymously Hosted DynamicMethods Assembly.\u201d The purpose of this code is to improve the performance of dynamic dispatch in scenarios where a particular call site is making many invocations using dynamic arguments that have the same runtime type.</p><p>Due to all the overhead associated with C#\u2019s built-in dynamic evaluation feature, you should consciously decide that you are getting sufficient syntax simplification from the dynamic feature to make it worth the extra performance hit of loading all these assemblies and the extra memory that they consume. If you have only a couple places in your program where you need dynamic behavior, it might be more efficient to just do it the old-fashioned way, by calling reflection methods (for managed objects) or with manual casting (for COM objects).</p><p>At run time, the C# runtime binder resolves a dynamic operation according to the runtime type of the object. The binder first checks to see if the type implements the IDynamicMetaObjectProvider interface. If the object does implement this interface, then the interface\u2019s GetMetaObject method is</p><p>called, which returns a DynamicMetaObject-derived type. This type can process all of the member, method, and operator bindings for the object. Both the IDynamicMetaObjectProvider interface and the DynamicMetaObject base class are defined in the System.Dynamic namespace, and both are in the System.Core.dll assembly.</p><p>Dynamic languages, such as Python and Ruby, endow their types with DynamicMetaObjectderived types so that they can be accessed in a way appropriate for them when manipulated from other programming languages (like C#). Similarly, when accessing a COM component, the C# runtime binder will use a DynamicMetaObject-derived type that knows how to communicate with a COM component. The COM DynamicMetaObject-derived type is defined in the System.Dynamic.dll assembly.</p><p>If the type of the object being used in the dynamic expression does not implement the IDynamicMetaObjectProvider interface, then the C# compiler treats the object like an instance of an ordinary C#-defined type and performs operations on the object using reflection.</p><p>One of the limitations of dynamic is that you can only use it to access an object\u2019s instance members because the dynamic variable must refer to an object. But, there are occasions when it would be useful to dynamically invoke static members of a type where the type is determined at run time. To accomplish this, I have created a StaticMemberDynamicWrapper class that derives from System.Dynamic.DynamicObject, which implements the IDynamicMetaObjectProvider interface. The class internally uses quite a bit of reflection (covered in Chapter 23, \u201cAssembly Loading and Reflection\u201d). Here is the code for my StaticMemberDynamicWrapper class.</p><p>internal sealed class StaticMemberDynamicWrapper : DynamicObject { private readonly TypeInfo m_type; public StaticMemberDynamicWrapper(Type type) { m_type = type.GetTypeInfo(); }</p><p>public override IEnumerable&lt;String&gt; GetDynamicMemberNames() {</p><p>return m_type.DeclaredMembers.Select(mi =&gt; mi.Name); }</p><p>public override Boolean TryGetMember(GetMemberBinder binder, out object result) { result = null; var field = FindField(binder.Name); if (field != null) { result = field.GetValue(null); return true; }</p><p>}</p><p>var prop = FindProperty(binder.Name, true); if (prop != null) { result = prop.GetValue(null, null); return true; } return false;</p><p>public override Boolean TrySetMember(SetMemberBinder binder, object value) { var field = FindField(binder.Name); if (field != null) { field.SetValue(null, value); return true; }</p><p>}</p><p>var prop = FindProperty(binder.Name, false); if (prop != null) { prop.SetValue(null, value, null); return true; } return false;</p><p>public override Boolean TryInvokeMember(InvokeMemberBinder binder, Object[] args, out Object result) { MethodInfo method = FindMethod(binder.Name); if (method == null) { result = null; return false; } result = method.Invoke(null, args); return true;</p><p>}</p><p>private MethodInfo FindMethod(String name, Type[] paramTypes) { return m_type.DeclaredMethods.FirstOrDefault(mi =&gt; mi.IsPublic &amp;&amp; mi.IsStatic &amp;&amp; mi.Name == name</p><p>&amp;&amp; ParametersMatch(mi.GetParameters(), paramTypes)); }</p><p>private Boolean ParametersMatch(ParameterInfo[] parameters, Type[] paramTypes) { if (parameters.Length != paramTypes.Length) return false; for (Int32 i = 0; i &lt; parameters.Length; i++)</p><p>if (parameters[i].ParameterType != paramTypes[i]) return false; return true;</p><p>}</p><p>private FieldInfo FindField(String name) { return m_type.DeclaredFields.FirstOrDefault(fi =&gt; fi.IsPublic &amp;&amp; fi.IsStatic</p><p>&amp;&amp; fi.Name == name); }</p><p>private PropertyInfo FindProperty(String name, Boolean get) { if (get) return m_type.DeclaredProperties.FirstOrDefault(</p><p>pi =&gt; pi.Name == name &amp;&amp; pi.GetMethod != null &amp;&amp; pi.GetMethod.IsPublic &amp;&amp; pi.GetMethod.IsStatic);</p><p>}</p><p>return m_type.DeclaredProperties.FirstOrDefault( pi =&gt; pi.Name == name &amp;&amp; pi.SetMethod != null &amp;&amp;</p><p>pi.SetMethod.IsPublic &amp;&amp; pi.SetMethod.IsStatic); }</p><p>To invoke a static member dynamically, construct an instance of this class by passing in the Type you want it to operate on and put the reference in a dynamic variable. Then, invoke the desired static member by using instance member syntax. Here is an example of how to invoke String\u2019s static Concat(String, String) method.</p><p>dynamic stringType = new StaticMemberDynamicWrapper(typeof(String)); var r = stringType.Concat(\"A\", \"B\"); // dynamically invoke String\u2019s static Concat method Console.WriteLine(r); // Displays \"AB\"</p><h6>C H A P T E R 6</h6><h2>Type and Member Basics</h2><h6>In this chapter:</h6><p>The Different Kinds of Type Members . . . . . . . . . . . . . . . . . . . . . 151 Type Visibility . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 154 Member Accessibility. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 156 Static Classes . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 158 Partial Classes, Structures, and Interfaces. . . . . . . . . . . . . . . . . . . 159 Components, Polymorphism, and Versioning. . . . . . . . . . . . . . . 160</p><p>In Chapters 4 and 5, I focused on types and what operations are guaranteed to exist on all instances of any type. I also explained how all types fall into one of two categories: reference types and value types. In this and the subsequent chapters in this part, I\u2019ll show how to design types by using the different kinds of members that can be defined within a type. In Chapters 7 through 11, I\u2019ll discuss the various members in detail.</p><h6>The Different Kinds of Type Members</h6><p>A type can define zero or more of the following kinds of members:</p><p>\u25a0\u25a0 Constants A constant is a symbol that identifies a never-changing data value. These symbols are typically used to make code more readable and maintainable. Constants are always associated with a type, not an instance of a type. Logically, constants are always static members. Discussed in Chapter 7, \u201cConstants and Fields.\u201d</p><p>\u25a0\u25a0 Fields A field represents a read-only or read/write data value. A field can be static, in which case the field is considered part of the type\u2019s state. A field can also be instance (nonstatic), in which case it\u2019s considered part of an object\u2019s state. I strongly encourage you to make fields private so that the state of the type or object can\u2019t be corrupted by code outside of the defining type. Discussed in Chapter 7.</p><p>\u25a0\u25a0 Instance constructors An instance constructor is a special method used to initialize a new</p><p>object\u2019s instance fields to a good initial state. Discussed in Chapter 8, \u201cMethods.\u201d</p><p>\u25a0\u25a0 Type constructors A type constructor is a special method used to initialize a type\u2019s static</p><p>fields to a good initial state. Discussed in Chapter 8.</p><p>151</p><p>\u25a0\u25a0 Methods A method is a function that performs operations that change or query the state of a type (static method) or an object (instance method). Methods typically read and write to the fields of the type or object. Discussed in Chapter 8.</p><p>\u25a0\u25a0 Operator overloads An operator overload is a method that defines how an object should be manipulated when certain operators are applied to the object. Because not all programming languages support operator overloading, operator overload methods are not part of the Common Language Specification (CLS). Discussed in Chapter 8.</p><p>\u25a0\u25a0 Conversion operators A conversion operator is a method that defines how to implicitly or explicitly cast or convert an object from one type to another type. As with operator overload methods, not all programming languages support conversion operators, so they\u2019re not part of the CLS. Discussed in Chapter 8.</p><p>\u25a0\u25a0 Properties A property is a mechanism that allows a simple, field-like syntax for setting or querying part of the logical state of a type (static property) or object (instance property) while ensuring that the state doesn\u2019t become corrupt. Properties can be parameterless (very common) or parameterful (fairly uncommon but used frequently with collection classes). Discussed in Chapter 10, \u201cProperties.\u201d</p><p>\u25a0\u25a0 Events A static event is a mechanism that allows a type to send a notification to one or more static or instance methods. An instance (nonstatic) event is a mechanism that allows an object to send a notification to one or more static or instance methods. Events are usually raised in response to a state change occurring in the type or object offering the event. An event consists of two methods that allow static or instance methods to register and unregister interest in the event. In addition to the two methods, events typically use a delegate field to maintain the set of registered methods. Discussed in Chapter 11, \u201cEvents.\u201d</p><p>\u25a0\u25a0 Types A type can define other types nested within it. This approach is typically used to break</p><p>a large, complex type down into smaller building blocks to simplify the implementation. Again, the purpose of this chapter isn\u2019t to describe these various members in detail but to set the</p><p>stage and explain what these various members all have in common.</p><p>Regardless of the programming language you\u2019re using, the corresponding compiler must process your source code and produce metadata and Intermediate Language (IL) code for each kind of member in the preceding list. The format of the metadata is identical regardless of the source programming language you use, and this feature is what makes the CLR a common language run time. The metadata is the common information that all languages produce and consume, enabling code in one programming language to seamlessly access code written in a completely different programming language.</p><p>This common metadata format is also used by the CLR, which determines how constants, fields,</p><p>constructors, methods, properties, and events all behave at run time. Simply stated, metadata is the key to the whole Microsoft .NET Framework development platform; it enables the seamless integration of languages, types, and objects.</p><p>The following C# code shows a type definition that contains an example of all the possible members. The code shown here will compile (with warnings), but it isn\u2019t representative of a type that you\u2019d normally create; most of the methods do nothing of any real value. Right now, I just want to show you how the compiler translates this type and its members into metadata. Once again, I\u2019ll discuss the individual members in the next few chapters.</p><p>using System;</p><p>public sealed class SomeType { // 1</p><p>// Nested class private class SomeNestedType { } // 2</p><p>// Constant, read\u00adonly, and static read/write field private const Int32 c_SomeConstant = 1; // 3 private readonly String m_SomeReadOnlyField = \"2\"; // 4 private static Int32 s_SomeReadWriteField = 3; // 5</p><p>// Type constructor static SomeType() { } // 6</p><p>// Instance constructors public SomeType(Int32 x) { } // 7 public SomeType() { } // 8</p><p>// Instance and static methods private String InstanceMethod() { return null; } // 9 public static void Main() {} // 10</p><p>// Instance property public Int32 SomeProp { // 11 get { return 0; } // 12 set { } // 13</p><p>}</p><p>// Instance parameterful property (indexer) public Int32 this[String s] { // 14 get { return 0; } // 15 set { } // 16</p><p>}</p><p>}</p><p>// Instance event public event EventHandler SomeEvent; // 17</p><p>If you were to compile the type just defined and examine the metadata in ILDasm.exe, you\u2019d see the output shown in Figure 6-1.</p><p>Notice that all the members defined in the source code cause the compiler to emit some metadata. In fact, some of the members cause the compiler to generate additional members as well as additional metadata. For example, the event member (17) causes the compiler to emit a field, two methods, and some additional metadata. I don\u2019t expect you to fully understand what you\u2019re seeing here now. But as you read the next few chapters, I encourage you to look back to this example to see how the member is defined and what effect it has on the metadata produced by the compiler.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile58.png\"/><figcaption>FIGURE 6-1 ILDasm.exe output showing metadata from preceding code.</figcaption><h6>Type Visibility</h6><p>When defining a type at file scope (versus defining a type nested within another type), you can specify the type\u2019s visibility as being either public or internal. A public type is visible to all code within the defining assembly as well as all code written in other assemblies. An internal type is visible to all code within the defining assembly, and the type is not visible to code written in other assemblies. If you do not explicitly specify either of these when you define a type, the C# compiler sets the type\u2019s visibility to internal (the more restrictive of the two). Here are some examples.</p><p>using System;</p><p>// The type below has public visibility and can be accessed by code // in this assembly as well as code written in other assemblies. public class ThisIsAPublicType { ... }</p><p>// The type below has internal visibility and can be accessed by code // in this assembly only. internal class ThisIsAnInternalType { ... }</p><p>// The type below is internal because public/internal // is not explicitly stated. class ThisIsAlsoAnInternalType { ... }</p><h6>Friend Assemblies</h6><p>Imagine the following scenario: a company has one team, TeamA, that is defining a bunch of utility types in one assembly, and they expect these types to be used by members in another team, TeamB. For various reasons such as time schedules or geographical location, or perhaps different cost centers or reporting structures, these two teams cannot build all of their types into a single assembly; instead, each team produces its own assembly file.</p><p>In order for TeamB\u2019s assembly to use TeamA\u2019s types, TeamA must define all of their utility types as public. However, this means that their types are publicly visible to any and all assemblies; developers in another company could write code that uses the public utility types, and this is not desirable. Maybe the utility types make certain assumptions that TeamB ensures when they write code that uses TeamA\u2019s types. What we\u2019d like to have is a way for TeamA to define their types as internal while still allowing TeamB to access the types. The CLR and C# support this via friend assemblies. This friend assembly feature is also useful when you want to have one assembly containing code that performs unit tests against the internal types within another assembly.</p><p>When an assembly is built, it can indicate other assemblies it considers \u201cfriends\u201d by using the InternalsVisibleTo attribute defined in the System.Runtime.CompilerServices namespace. The attribute has a string parameter that identifies the friend assembly\u2019s name and public key (the string you pass to the attribute must not include a version, culture, or processor architecture). Note that friend assemblies can access all of an assembly\u2019s internal types as well as these type\u2019s internal members. Here is an example of how an assembly can specify two other strongly named assemblies named \u201cWintellect\u201d and \u201cMicrosoft\u201d as its friend assemblies.</p><p>using System; using System.Runtime.CompilerServices; // For InternalsVisibleTo attribute</p><p>// This assembly's internal types can be accessed by any code written // in the following two assemblies (regardless of version or culture): [assembly:InternalsVisibleTo(\"Wintellect, PublicKey=12345678...90abcdef\")] [assembly:InternalsVisibleTo(\"Microsoft, PublicKey=b77a5c56...1934e089\")]</p><p>internal sealed class SomeInternalType { ... } internal sealed class AnotherInternalType { ... }</p><p>Accessing the above assembly\u2019s internal types from a friend assembly is trivial. For example, here\u2019s how a friend assembly called \u201cWintellect\u201d with a public key of \u201c12345678...90abcdef\u201d can access the internal type SomeInternalType in the preceding assembly.</p><p>using System;</p><p>internal sealed class Foo {</p><p>private static Object SomeMethod() { // This \"Wintellect\" assembly accesses the other assembly's // internal type as if it were a public type SomeInternalType sit = new SomeInternalType(); return sit;</p><p>} }</p><p>Because the internal members of the types in an assembly become accessible to friend assemblies, you should think carefully about what accessibility you specify for your type\u2019s members and which assemblies you declare as your friends. Note that the C# compiler requires you to use the /out:&lt;file&gt; compiler switch when compiling the friend assembly (the assembly that does not contain the InternalsVisibleTo attribute). The switch is required because the compiler needs to know the name of the assembly being compiled in order to determine if the resulting assembly should be</p><p>considered a friend assembly. You would think that the C# compiler could determine this on its own because it normally determines the output file name on its own; however, the compiler doesn\u2019t decide on an output file name until it is finished compiling the code. So requiring the /out:&lt;file&gt; compiler switch improves the performance of compiling significantly.</p><p>Also, if you are compiling a module (as opposed to an assembly) using C#\u2019s /t:module switch, and this module is going to become part of a friend assembly, you need to compile the module by using the C# compiler\u2019s /moduleassemblyname:&lt;string&gt; switch as well. This tells the compiler what assembly the module will be a part of so the compiler can allow code in the module to access the other assembly\u2019s internal types.</p><h6>Member Accessibility</h6><p>When defining a type\u2019s member (which includes nested types), you can specify the member\u2019s accessibility. A member\u2019s accessibility indicates which members can be legally accessed from referent code. The CLR defines the set of possible accessibility modifiers, but each programming language chooses the syntax and term it wants developers to use when applying the accessibility to a member. For example, the CLR uses the term Assembly to indicate that a member is accessible to any code within the same assembly, whereas the C# term for this is internal.</p><p>Table 6-1 shows the six accessibility modifiers that can be applied to a member. The rows of the table are in order from most restrictive (Private) to least restrictive (Public).</p><p>TABLE 6-1 Member Accessibility</p><table border=\"1\">\n<tr>\n<td><p>CLR Term Private</p>\n</td>\n<td><p>C# Term</p>\n<p>private</p>\n</td>\n<td><h6>Description</h6>\n<p>The member is accessible only by methods in the defining type or any nested type.</p>\n</td>\n</tr>\n<tr>\n<td><p>Family</p>\n</td>\n<td><p>protected</p>\n</td>\n<td><p>The member is accessible only by methods in the defining type, any nested type, or one of its derived types without regard to assembly.</p>\n</td>\n</tr>\n<tr>\n<td><p>Family and Assembly</p>\n</td>\n<td><p>(not supported)</p>\n</td>\n<td><p>The member is accessible only by methods in the defining type, any nested type, or by any derived types defined in the same assembly.</p>\n</td>\n</tr>\n<tr>\n<td><p>Assembly</p>\n</td>\n<td><p>internal</p>\n</td>\n<td><p>The member is accessible only by methods in the defining assembly.</p>\n</td>\n</tr>\n<tr>\n<td><p>Family or Assembly</p>\n</td>\n<td><p>protected internal</p>\n</td>\n<td><p>The member is accessible by any nested type, any derived type (regardless of assembly), or any methods in the defining assembly.</p>\n</td>\n</tr>\n<tr>\n<td><p>Public</p>\n</td>\n<td><p>public</p>\n</td>\n<td><p>The member is accessible to all methods in any assembly.</p>\n</td>\n</tr>\n</table><figcaption>Of course, for any member to be accessible, it must be defined in a type that is visible. For example, if AssemblyA defines an internal type with a public method, code in AssemblyB cannot call the public method because the internal type is not visible to AssemblyB.</figcaption><p>When compiling code, the language compiler is responsible for checking that the code is referencing types and members correctly. If the code references some type or member incorrectly, the compiler has the responsibility of emitting the appropriate error message. In addition, the just-in-time (JIT) compiler also ensures that references to fields and methods are legal when compiling IL code into native CPU instructions at run time. For example, if the JIT compiler detects code that is improperly attempting to access a private field or method, the JIT compiler throws a FieldAccessException or a MethodAccessException, respectively.</p><p>Verifying the IL code ensures that a referenced member\u2019s accessibility is properly honored at run time, even if a language compiler ignored checking the accessibility. Another, more likely, possibility is that the language compiler compiled code that accessed a public member in another type (in another assembly); but at run time, a different version of the assembly is loaded, and in this new version, the public member has changed and is now protected or private.</p><p>In C#, if you do not explicitly declare a member\u2019s accessibility, the compiler usually (but not always) defaults to selecting private (the most restrictive of them all). The CLR requires that all members of an interface type be public. The C# compiler knows this and forbids the programmer from explicitly specifying accessibility on interface members; the compiler just makes all the members public for you.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile59.png\"/><p>More Info See the \u201cDeclared Accessibility\u201d section in the C# Language Specification for the complete set of C# rules about what accessibilities can be applied to types and members and what default accessibilities C# selects based on the context in which the declaration takes place.</p><p>Furthermore, you\u2019ll notice the CLR offers an accessibility called Family and Assembly. However, C# doesn\u2019t expose this in the language. The C# team felt that this accessibility was for the most part useless and decided not to incorporate it into the C# language.</p><p>When a derived type is overriding a member defined in its base type, the C# compiler requires that the original member and the overriding member have the same accessibility. That is, if the member in the base class is protected, the overriding member in the derived class must also be protected. However, this is a C# restriction, not a CLR restriction. When deriving from a base class, the CLR allows a member\u2019s accessibility to become less restrictive but not more restrictive. For example, a class can override a protected method defined in its base class and make the overridden method public (more accessible). However, a class cannot override a protected method defined in its base class and make the overridden method private (less accessible). The reason a class cannot make a base class method more restricted is because a user of the derived class could always cast to the base type and gain access to the base class\u2019s method. If the CLR allowed the derived type\u2019s method to be less accessible, it would be making a claim that was not enforceable.</p><h6>Static Classes</h6><p>There are certain classes that are never intended to be instantiated, such as Console, Math, Environment, and ThreadPool. These classes have only static members and, in fact, the classes exist simply as a way to group a set of related members together. For example, the Math class defines a bunch of methods that do math-related operations. C# allows you to define non-instantiable classes by using the C# static keyword. This keyword can be applied only to classes, not structures (value types) because the CLR always allows value types to be instantiated and there is no way to stop or prevent this.</p><p>The compiler enforces many restrictions on a static class:</p><p>\u25a0\u25a0 The class must be derived directly from System.Object because deriving from any other base class makes no sense because inheritance applies only to objects, and you cannot create an instance of a static class.</p><p>\u25a0\u25a0 The class must not implement any interfaces because interface methods are callable only</p><p>when using an instance of a class.</p><p>\u25a0\u25a0 The class must define only static members (fields, methods, properties, and events). Any</p><p>instance members cause the compiler to generate an error.</p><p>\u25a0\u25a0 The class cannot be used as a field, method parameter, or local variable because all of these would indicate a variable that refers to an instance, and this is not allowed. If the compiler detects any of these uses, the compiler issues an error.</p><p>Here is an example of a static class that defines some static members; this code compiles (with a warning) but the class doesn\u2019t do anything interesting.</p><p>using System;</p><p>public static class AStaticClass { public static void AStaticMethod() { }</p><p>public static String AStaticProperty { get { return s_AStaticField; } set { s_AStaticField = value; }</p><p>}</p><p>private static String s_AStaticField;</p><p>public static event EventHandler AStaticEvent; }</p><p>If you compile the code above into a library (DLL) assembly and look at the result by using</p><p>ILDasm.exe, you\u2019ll see what is shown in Figure 6-2. As you can see in Figure 6-2, defining a class by using the static keyword causes the C# compiler to make the class both abstract and sealed. Furthermore, the compiler will not emit an instance constructor method into the type. Notice that there is no instance constructor (.ctor) method shown in Figure 6-2.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile60.png\"/><figcaption>FIGURE 6-2 ILDasm.exe showing the class as abstract sealed in metadata.</figcaption><h6>Partial Classes, Structures, and Interfaces</h6><p>In this section, I discuss partial classes, structures, and interfaces. The partial keyword tells the C# compiler that the source code for a single class, structure, or interface definition may span one or more source code files. It should be noted that the compiler combines all of a type\u2019s partials together at compile time; the CLR always works on complete type definitions. There are three main reasons why you might want to split the source code for a type across multiple files:</p><p>\u25a0\u25a0 Source control Suppose a type\u2019s definition consists of a lot of source code, and a programmer checks it out of source control to make changes. No other programmer will be able to modify the type at the same time without doing a merge later. Using the partial keyword allows you to split the code for the type across multiple source code files, each of which can be checked out individually so that multiple programmers can edit the type at the same time.</p><p>\u25a0\u25a0 Splitting a class, structure, or interface into distinct logical units within a single file I sometimes create a single type that provides multiple features so that the type can provide a complete solution. To simplify my implementation, I will sometimes declare the same partial type repeatedly within a single source code file. Then, in each part of the partial type, I implement one feature with all its fields, methods, properties, events, and so on. This allows me to easily see all the members that provide a single feature grouped together, which simplifies my coding. Also, I can easily comment out a part of the partial type to remove a whole feature from the type and replace it with another implementation (via a new part of the partial type).</p><p>\u25a0\u25a0 Code spitters In Microsoft Visual Studio, when you create a new project, some source code files are created automatically as part of the project. These source code files contain templates that give you a head start at building these kinds of projects. When you use the Visual Studio designers and drag and drop controls onto the design surface, Visual Studio writes source code for you automatically and spits this code into the source code files. This really improves your productivity. Historically, the generated code was emitted into the same source code file</p><p>that you were working on. The problem with this is that you might edit the generated code accidentally and cause the designers to stop functioning correctly. Starting with Visual Studio 2005, when you create a new form, user control, and so on, Visual Studio creates two source code files: one for your code and the other for the code generated by the designer. Because the designer code is in a separate file, you\u2019ll be far less likely to accidentally edit it.</p><p>The partial keyword is applied to the types in all files. When the files are compiled together, the compiler combines the code to produce one type that is in the resulting .exe or .dll assembly file (or</p><p>.netmodule module file). As I stated in the beginning of this section, the partial types feature is completely implemented by the C# compiler; the CLR knows nothing about partial types at all. This is why all of the source code files for the type must use the same programming language, and they must all be compiled together as a single compilation unit.</p><h6>Components, Polymorphism, and Versioning</h6><p>Object-oriented programming (OOP) has been around for many, many years. When it was first used in the late 1970s/early 1980s, applications were much smaller in size and all the code to make the application run was written by one company. Sure, there were operating systems back then and applications did make use of what they could out of those operating systems, but the operating systems offered very few features compared with the operating systems of today.</p><p>Today, software is much more complex and users demand that applications offer rich features such as GUIs, menu items, mouse input, tablet input, printer output, networking, and so on. For this reason, our operating systems and development platforms have grown substantially over recent years. Furthermore, it is no longer feasible or even cost effective for application developers to write all of the code necessary for their application to work the way users expect. Today, applications consist of code produced by many different companies. This code is stitched together using an object-oriented paradigm.</p><p>Component Software Programming (CSP) is OOP brought to this level. Here are some attributes of a component:</p><p>\u25a0\u25a0 A component (an assembly in the .NET Framework) has the feeling of being \u201cpublished.\u201d</p><p>\u25a0\u25a0 A component has an identity (a name, version, culture, and public key).</p><p>\u25a0\u25a0 A component forever maintains its identity (the code in an assembly is never statically linked</p><p>into another assembly; .NET always uses dynamic linking).</p><p>\u25a0\u25a0 A component clearly indicates the components it depends upon (reference metadata tables). \u25a0\u25a0 A component should document its classes and members. C# offers this by allowing in-source</p><p>Extensible Markup Language (XML) documentation along with the compiler\u2019s /doc commandline switch.</p><p>\u25a0\u25a0 A component must specify the security permissions it requires. The CLR\u2019s code access security</p><p>(CAS) facilities enable this.</p><p>\u25a0\u25a0 A component publishes an interface (object model) that won\u2019t change for any servicings. A servicing is a new version of a component whose intention is to be backward compatible with the original version of the component. Typically, a servicing version includes bug fixes, security patches, and possibly some small feature enhancements. But a servicing cannot require any new dependencies or any additional security permissions.</p><p>As indicated by the last bullet, a big part of CSP has to do with versioning. Components will change over time and components will ship on different time schedules. Versioning introduces a whole new level of complexity for CSP that didn\u2019t exist with OOP, with which all code was written, tested, and shipped as a single unit by a single company. In this section, I\u2019m going to focus on component versioning.</p><p>In the .NET Framework, a version number consists of four parts: a major part, a minor part, a build part, and a revision part. For example, an assembly whose version number is 1.2.3.4 has a major part of 1, a minor part of 2, a build part of 3, and a revision part of 4. The major/minor parts are typically used to represent a consistent and stable feature set for an assembly and the build/revision parts are typically used to represent a servicing of this assembly\u2019s feature set.</p><p>Let\u2019s say that a company ships an assembly with version 2.7.0.0. If the company later wants to fix a bug in this component, they would produce a new assembly in which only the build/revision parts of the version are changed, something like version 2.7.1.34. This indicates that the assembly is a servicing whose intention is to be backward compatible with the original component (version 2.7.0.0).</p><p>On the other hand, if the company wants to make a new version of the assembly that has significant changes to it and is therefore not intended to be backward compatible with the original assembly, the company is really creating a new component and the new assembly should be given a version number in which the major/minor parts are different from the existing component (version 3.0.0.0, for example).</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile61.png\"/><p>Note I have just described how you should think of version numbers. Unfortunately, the CLR doesn\u2019t treat version numbers this way. Today, the CLR treats a version number as an opaque value, and if an assembly depends on version 1.2.3.4 of another assembly, the CLR tries to load version 1.2.3.4 only (unless a binding redirection is in place).</p><p>Now that we\u2019ve looked at how we use version numbers to update a component\u2019s identity to reflect a new version, let\u2019s take a look at some of the features offered by the CLR and programming languages (such as C#) that allow developers to write code that is resilient to changes that may be occurring in components that they are using.</p><p>Versioning issues come into play when a type defined in a component (assembly) is used as the base class for a type in another component (assembly). Obviously, if the base class versions (changes) underneath the derived class, the behavior of the derived class changes as well, probably in a way that causes the class to behave improperly. This is particularly true in polymorphism scenarios in which a derived type overrides virtual methods defined by a base type.</p><p>C# offers five keywords that you can apply to types and/or type members that impact component versioning. These keywords map directly to features supported in the CLR to support component versioning. Table 6-2 contains the C# keywords related to component versioning and indicates how each keyword affects a type or type member definition.</p><figcaption>TABLE 6-2 C# Keywords and How They Affect Component Versioning</figcaption><table border=\"1\">\n<tr>\n<td><p>C# Keyword</p>\n<p>abstract</p>\n</td>\n<td><h6>Type</h6>\n<p>Indicates that no instances of the type can be constructed</p>\n</td>\n<td><h6>Method/Property/Event</h6>\n<p>Indicates that the derived type must override and implement this member before instances of the derived type can be constructed</p>\n</td>\n<td><p>Constant/Field (not allowed)</p>\n</td>\n</tr>\n<tr>\n<td><p>virtual</p>\n</td>\n<td><p>(not allowed)</p>\n</td>\n<td><p>Indicates that this member can be overridden by a derived type</p>\n</td>\n<td><p>(not allowed)</p>\n</td>\n</tr>\n<tr>\n<td><p>override</p>\n</td>\n<td><p>(not allowed)</p>\n</td>\n<td><p>Indicates that the derived type is overriding the base type\u2019s member</p>\n</td>\n<td><p>(not allowed)</p>\n</td>\n</tr>\n<tr>\n<td><p>sealed</p>\n</td>\n<td><p>Indicates that the type cannot be used as a base type</p>\n</td>\n<td><p>Indicates that the member cannot be overridden by a derived type. This keyword can be applied only to a method that is overriding a virtual method</p>\n</td>\n<td><p>(not allowed)</p>\n</td>\n</tr>\n<tr>\n<td><p>new</p>\n</td>\n<td><p>When applied to a nested type, method, property, event, constant, or field, indicates that the member has no relationship to a similar member that may exist in the base class</p>\n</td>\n<td></td>\n<td></td>\n</tr>\n</table><p>I will demonstrate the value and use of all these keywords in the upcoming section titled \u201cDealing with Virtual Methods When Versioning Types.\u201d But before we get to a versioning scenario, let\u2019s focus on how the CLR actually calls virtual methods.</p><h6>How the CLR Calls Virtual Methods, Properties, and Events</h6><p>In this section, I will be focusing on methods, but this discussion is relevant to virtual properties and virtual events as well. Properties and events are actually implemented as methods; this will be shown in their corresponding chapters.</p><p>Methods represent code that performs some operation on the type (static methods) or an instance of the type (nonstatic methods). All methods have a name, a signature, and a return type (that may be void). The CLR allows a type to define multiple methods with the same name as long as each method has a different set of parameters or a different return type. So it\u2019s possible to define two methods with the same name and same parameters as long as the methods have a different return type. However, except for IL assembly language, I\u2019m not aware of any language that takes advantage of this \u201cfeature\u201d; most languages (including C#) require that methods differ by parameters and ignore a method\u2019s return type when determining uniqueness. (C# actually relaxes this restriction when defining conversion operator methods; see Chapter 8 for details.)</p><p>The Employee class shown below defines three different kinds of methods.</p><p>internal class Employee { // A nonvirtual instance method public Int32 GetYearsEmployed() { ... }</p><p>// A virtual method (virtual implies instance) public virtual String GetProgressReport() { ... }</p><p>}</p><p>// A static method public static Employee Lookup(String name) { ... }</p><p>When the compiler compiles this code, the compiler emits three entries in the resulting assembly\u2019s method definition table. Each entry has flags set indicating if the method is instance, virtual, or static.</p><p>When code is written to call any of these methods, the compiler emitting the calling code examines the method definition\u2019s flags to determine how to emit the proper IL code so that the call is made correctly. The CLR offers two IL instructions for calling a method:</p><p>\u25a0\u25a0 The call IL instruction can be used to call static, instance, and virtual methods. When the call instruction is used to call a static method, you must specify the type that defines the method that the CLR should call. When the call instruction is used to call an instance or virtual method, you must specify a variable that refers to an object. The call instruction assumes that this variable is not null. In other words, the type of the variable itself indicates which type defines the method that the CLR should call. If the variable\u2019s type doesn\u2019t define the method, base types are checked for a matching method. The call instruction is frequently used to call a virtual method nonvirtually.</p><p>\u25a0\u25a0 The callvirt IL instruction can be used to call instance and virtual methods, not static methods. When the callvirt instruction is used to call an instance or virtual method, you must specify a variable that refers to an object. When the callvirt IL instruction is used to call a nonvirtual instance method, the type of the variable indicates which type defines the method that the CLR should call. When the callvirt IL instruction is used to call a virtual instance method, the CLR discovers the actual type of the object being used to make the call and then calls the method polymorphically. In order to determine the type, the variable being used to make the call must not be null. In other words, when compiling this call, the JIT compiler generates code that verifies that the variable\u2019s value is not null. If it is null, the callvirt instruction causes the CLR to throw a NullReferenceException. This additional check means that the callvirt IL instruction executes slightly more slowly than the call instruction. Note that this null check is performed even when the callvirt instruction is used to call a nonvirtual instance method.</p><p>So now, let\u2019s put this together to see how C# uses these different IL instructions.</p><p>using System;</p><p>public sealed class Program { public static void Main() { Console.WriteLine(); // Call a static method</p><p>} }</p><p>Object o = new Object();</p><ul style=\"list-style-type: none;\">\n<li><p>o.GetHashCode(); // Call a virtual instance method</p></li>\n<li><p>o.GetType(); // Call a nonvirtual instance method</p></li>\n</ul><p>If you were to compile the code above and look at the resulting IL, you\u2019d see the following.</p><p>.method public hidebysig static void Main() cil managed {</p><p>.entrypoint // Code size 26 (0x1a)</p><p>.maxstack 1</p><p>.locals init (object o) IL_0000: call void System.Console::WriteLine() IL_0005: newobj instance void System.Object::.ctor()</p><ul style=\"list-style-type: none;\">\n<li><p>IL_000a: stloc.0</p></li>\n<li><p>IL_000b: ldloc.0</p></li>\n<li><p>IL_000c: callvirt instance int32 System.Object::GetHashCode()</p></li>\n</ul><ul style=\"list-style-type: none;\">\n<li><p>IL_0011: pop</p></li>\n<li><p>IL_0012: ldloc.0</p></li>\n<li><p>IL_0013: callvirt instance class System.Type System.Object::GetType() IL_0018: pop IL_0019: ret</p></li>\n</ul><p>} // end of method Program::Main</p><p>Notice that the C# compiler uses the call IL instruction to call Console\u2019s WriteLine method. This is expected because WriteLine is a static method. Next, notice that the callvirt IL instruction is used to call GetHashCode. This is also expected, because GetHashCode is a virtual method. Finally, notice that the C# compiler also uses the callvirt IL instruction to call the GetType method. This is surprising because GetType is not a virtual method. However, this works because while JIT-compiling this code, the CLR will know that GetType is not a virtual method, and so the JIT-compiled code will simply call GetType nonvirtually.</p><p>Of course, the question is, why didn\u2019t the C# compiler simply emit the call instruction instead? The answer is because the C# team decided that the JIT compiler should generate code to verify that the object being used to make the call is not null. This means that calls to nonvirtual instance methods are a little slower than they could be. It also means that the following C# code will cause a NullReferenceException to be thrown. In some other programming languages, the intention of the following code would run just fine.</p><p>using System;</p><p>public sealed class Program { public Int32 GetFive() { return 5; } public static void Main() {</p><p>} }</p><p>Program p = null; Int32 x = p.GetFive(); // In C#, NullReferenceException is thrown</p><p>Theoretically, the preceding code is fine. Sure, the variable p is null, but when calling a nonvirtual method (GetFive), the CLR needs to know just the data type of p, which is Program. If GetFive did get called, the value of the this argument would be null. Because the argument is not used inside the GetFive method, no NullReferenceException would be thrown. However, because the C# compiler emits a callvirt instruction instead of a call instruction, the preceding code will end up throwing the NullReferenceException.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile62.png\"/><p>Important If you define a method as nonvirtual, you should never change the method to virtual in the future. The reason is because some compilers will call the nonvirtual method by using the call instruction instead of the callvirt instruction. If the method changes from nonvirtual to virtual and the referencing code is not recompiled, the virtual method will be called nonvirtually, causing the application to produce unpredictable behavior. If the referencing code is written in C#, this is not a problem, because C# calls all instance methods by using callvirt. But this could be a problem if the referencing code was written using a different programming language.</p><p>Sometimes, the compiler will use a call instruction to call a virtual method instead of using a callvirt instruction. At first, this may seem surprising, but the code below demonstrates why it is sometimes required.</p><p>internal class SomeClass { // ToString is a virtual method defined in the base class: Object. public override String ToString() {</p><p>// Compiler uses the 'call' IL instruction to call // Object\u2019s ToString method nonvirtually.</p><p>} }</p><p>// If the compiler were to use 'callvirt' instead of 'call', this // method would call itself recursively until the stack overflowed. return base.ToString();</p><p>When calling base.ToString (a virtual method), the C# compiler emits a call instruction to ensure that the ToString method in the base type is called nonvirtually. This is required because if ToString were called virtually, the call would execute recursively until the thread\u2019s stack overflowed, which obviously is not desired.</p><p>Compilers tend to use the call instruction when calling methods defined by a value type because value types are sealed. This implies that there can be no polymorphism even for their virtual methods, which causes the performance of the call to be faster. In addition, the nature of a value type instance guarantees it can never be null, so a NullReferenceException will never be thrown. Finally, if you</p><p>were to call a value type\u2019s virtual method virtually, the CLR would need to have a reference to the value type\u2019s type object in order to refer to the method table within it. This requires boxing the value type. Boxing puts more pressure on the heap, forcing more frequent garbage collections and hurting performance.</p><p>Regardless of whether call or callvirt is used to call an instance or virtual method, these methods always receive a hidden this argument as the method\u2019s first parameter. The this argument refers to the object being operated on.</p><p>When designing a type, you should try to minimize the number of virtual methods you define. First, calling a virtual method is slower than calling a nonvirtual method. Second, virtual methods cannot be inlined by the JIT compiler, which further hurts performance. Third, virtual methods make versioning of components more brittle, as described in the next section. Fourth, when defining a base type, it is common to offer a set of convenience overloaded methods. If you want these methods to be polymorphic, the best thing to do is to make the most complex method virtual and leave all of the convenience overloaded methods nonvirtual. By the way, following this guideline will also improve the ability to version a component without adversely affecting the derived types. Here is an example.</p><p>public class Set { private Int32 m_length = 0;</p><p>// This convenience overload is not virtual public Int32 Find(Object value) {</p><p>return Find(value, 0, m_length); }</p><p>// This convenience overload is not virtual public Int32 Find(Object value, Int32 startIndex) {</p><p>return Find(value, startIndex, m_length \u00ad startIndex); }</p><p>// The most feature\u00adrich method is virtual and can be overridden public virtual Int32 Find(Object value, Int32 startIndex, Int32 endIndex) {</p><p>// Actual implementation that can be overridden goes here... }</p><p>// Other methods go here }</p><h6>Using Type Visibility and Member Accessibility Intelligently</h6><p>With the .NET Framework, applications are composed of types defined in multiple assemblies produced by various companies. This means that the developer has little control over the components he or she is using and the types defined within those components. The developer typically doesn\u2019t have access to the source code (and probably doesn\u2019t even know what programming language was used to create the component), and components tend to version with different schedules. Furthermore, due to polymorphism and protected members, a base class developer must trust the code written by the derived class developer. And, of course, the developer of a derived class must trust the code that he</p><p>is inheriting from a base class. These are just some of the issues that you need to really think about when designing components and types.</p><p>In this section, I\u2019d like to say just a few words about how to design a type with these issues in mind. Specifically, I\u2019m going to focus on the proper way to set type visibility and member accessibility so that you\u2019ll be most successful.</p><p>First, when defining a new type, compilers should make the class sealed by default so that the class cannot be used as a base class. Instead, many compilers, including C#, default to unsealed classes and allow the programmer to explicitly mark a class as sealed by using the sealed keyword. Obviously, it is too late now, but I think that today\u2019s compilers have chosen the wrong default and it would be nice if this could change with future compilers. There are three reasons why a sealed class is better than an unsealed class:</p><p>\u25a0\u25a0 Versioning When a class is originally sealed, it can change to unsealed in the future without breaking compatibility. However, after a class is unsealed, you can never change it to sealed in the future as this would break all derived classes. In addition, if the unsealed class defines any unsealed virtual methods, ordering of the virtual method calls must be maintained with new versions or there is the potential of breaking derived types in the future.</p><p>\u25a0\u25a0 Performance As discussed in the previous section, calling a virtual method doesn\u2019t perform</p><ul style=\"list-style-type: none;\">\n<li><p>as well as calling a nonvirtual method because the CLR must look up the type of the object</p></li>\n<li><p>at run time in order to determine which type defines the method to call. However, if the JIT compiler sees a call to a virtual method using a sealed type, the JIT compiler can produce more efficient code by calling the method nonvirtually. It can do this because it knows there can\u2019t possibly be a derived class if the class is sealed. For example, in the code below, the JIT compiler can call the virtual ToString method nonvirtually.</p></li>\n</ul><p>using System; public sealed class Point {</p><p>private Int32 m_x, m_y;</p><p>public Point(Int32 x, Int32 y) { m_x = x; m_y = y; }</p><p>public override String ToString() {</p><p>return String.Format(\"({0}, {1})\", m_x, m_y); }</p><p>public static void Main() { Point p = new Point(3, 4);</p><p>} }</p><p>// The C# compiler emits the callvirt instruction here but the // JIT compiler will optimize this call and produce code that // calls ToString nonvirtually because p's type is Point, // which is a sealed class Console.WriteLine(p.ToString());</p><p>\u25a0\u25a0 Security and predictability A class must protect its own state and not allow itself to ever become corrupted. When a class is unsealed, a derived class can access and manipulate the base class\u2019s state if any data fields or methods that internally manipulate fields are accessible and not private. In addition, a virtual method can be overridden by a derived class, and the derived class can decide whether to call the base class\u2019s implementation. By making a method, property, or event virtual, the base class is giving up some control over its behavior and its state. Unless carefully thought out, this can cause the object to behave unpredictably, and it opens up potential security holes.</p><p>The problem with a sealed class is that it can be a big inconvenience to users of the type. Occasionally, developers want to create a class derived from an existing type in order to attach some additional fields or state information for their application\u2019s own use. In fact, they may even want to define some helper or convenience methods on the derived type to manipulate these additional fields. Although the CLR offers no mechanism to extend an already-built type with helper methods or fields, you can simulate helper methods by using C#\u2019s extension methods (discussed in Chapter 8) and you can simulate tacking state onto an object by using the ConditionalWeakTable class (discussed in Chapter 21, \u201cThe Managed Heap and Garbage Collection.\u201d</p><p>Here are the guidelines I follow when I define my own classes:</p><p>\u25a0\u25a0 When defining a class, I always explicitly make it sealed unless I truly intend for the class to be a base class that allows specialization by derived classes. As stated earlier, this is the opposite of what C# and many other compilers default to today. I also default to making the class internal unless I want the class to be publicly exposed outside of my assembly. Fortunately, if you do not explicitly indicate a type\u2019s visibility, the C# compiler defaults to internal. If I really feel that it is important to define a class that others can derive but I do not want to allow specialization, I will simulate creating a closed class by using the above technique of sealing the virtual methods that my class inherits.</p><p>\u25a0\u25a0 Inside the class, I always define my data fields as private and I never waver on this. Fortunately, C# does default to making fields private. I\u2019d actually prefer it if C# mandated that all fields be private and that you could not make fields protected, internal, public, and so on. Exposing state is the easiest way to get into problems, have your object behave unpredictably, and open potential security holes. This is true even if you just declare some fields as internal. Even within a single assembly, it is too hard to track all code that references a field, especially if several developers are writing code that gets compiled into the same assembly.</p><p>\u25a0\u25a0 Inside the class, I always define my methods, properties, and events as private and nonvirtual. Fortunately, C# defaults to this as well. Certainly, I\u2019ll make a method, property, or event public to expose some functionality from the type. I try to avoid making any of these members protected or internal, because this would be exposing my type to some potential vulnerability. However, I would sooner make a member protected or internal than I would make a member virtual because a virtual member gives up a lot of control and really relies on the proper behavior of the derived class.</p><p>\u25a0\u25a0 There is an old OOP adage that goes like this: when things get too complicated, make more types. When an implementation of some algorithm starts to get complicated, I define helper types that encapsulate discrete pieces of functionality. If I\u2019m defining these helper types for use by a single \u00fcber-type, I\u2019ll define the helper types nested within the \u00fcber-type. This allows for scoping and also allows the code in the nested, helper type to reference the private members defined in the \u00fcber-type. However, there is a design guideline rule, enforced by the Code Analysis tool (FxCopCmd.exe) in Visual Studio, which indicates that publicly exposed nested types should be defined at file or assembly scope and not be defined within another type. This rule exists because some developers find the syntax for referencing nested types cumbersome. I appreciate this rule, and I never define public nested types.</p><h6>Dealing with Virtual Methods When Versioning Types</h6><p>As was stated earlier, in a Component Software Programming environment, versioning is a very important issue. I talked about some of these versioning issues in Chapter 3, \u201cShared Assemblies and Strongly Named Assemblies,\u201d when I explained strongly named assemblies and discussed how an administrator can ensure that an application binds to the assemblies that it was built and tested with. However, other versioning issues cause source code compatibility problems. For example, you must be very careful when adding or modifying members of a type if that type is used as a base type. Let\u2019s look at some examples.</p><p>CompanyA has designed the following type, Phone.</p><ul style=\"list-style-type: none;\">\n<li><p>namespace CompanyA { public class Phone {</p><p>public void Dial() { Console.WriteLine(\"Phone.Dial\"); // Do work to dial the phone here.</p>\n<p>} }</p>\n<p>}</p>\n<p>Now imagine that CompanyB defines another type, BetterPhone, which uses CompanyA\u2019s Phone type as its base.</p>\n</li>\n<li><p>namespace CompanyB { public class BetterPhone : CompanyA.Phone {</p></li>\n</ul><p>public void Dial() { Console.WriteLine(\"BetterPhone.Dial\"); EstablishConnection(); base.Dial();</p><p>}</p><p>}</p><p>protected virtual void EstablishConnection() { Console.WriteLine(\"BetterPhone.EstablishConnection\"); // Do work to establish the connection.</p><p>} }</p><p>When CompanyB attempts to compile its code, the C# compiler issues the following message.</p><p>warning CS0108: 'CompanyB.BetterPhone.Dial()' hides inherited member 'CompanyA.Phone.Dial()'.</p><p>Use the new keyword if hiding was intended.\u201d This warning is notifying the developer that BetterPhone is defining a Dial method, which will hide the Dial method defined in Phone. This new method could change the semantic meaning of Dial (as defined by CompanyA when it originally created the Dial method).</p><p>It\u2019s a very nice feature of the compiler to warn you of this potential semantic mismatch. The compiler also tells you how to remove the warning by adding the new keyword before the definition of Dial in the BetterPhone class. Here\u2019s the fixed BetterPhone class.</p><p>namespace CompanyB { public class BetterPhone : CompanyA.Phone {</p><p>// This Dial method has nothing to do with Phone's Dial method. public new void Dial() {</p><p>Console.WriteLine(\"BetterPhone.Dial\"); EstablishConnection(); base.Dial();</p><p>}</p><p>}</p><p>protected virtual void EstablishConnection() { Console.WriteLine(\"BetterPhone.EstablishConnection\"); // Do work to establish the connection.</p><p>} }</p><p>At this point, CompanyB can use BetterPhone.Dial in its application. Here\u2019s some sample code that CompanyB might write.</p><p>public sealed class Program {</p><p>public static void Main() { CompanyB.BetterPhone phone = new CompanyB.BetterPhone(); phone.Dial();</p><p>} }</p><p>When this code runs, the following output is displayed.</p><p>BetterPhone.Dial BetterPhone.EstablishConnection Phone.Dial</p><p>This output shows that CompanyB is getting the behavior it desires. The call to Dial is calling the new Dial method defined by BetterPhone, which calls the virtual EstablishConnection method and then calls the Phone base type\u2019s Dial method.</p><p>Now let\u2019s imagine that several companies have decided to use CompanyA\u2019s Phone type. Let\u2019s</p><p>further imagine that these other companies have decided that the ability to establish a connection in the Dial method is a really useful feature. This feedback is given to CompanyA, which now revises its Phone class.</p><ul style=\"list-style-type: none;\">\n<li><p>namespace CompanyA { public class Phone {</p><p>public void Dial() { Console.WriteLine(\"Phone.Dial\"); EstablishConnection(); // Do work to dial the phone here.</p>\n<p>}</p>\n<p>protected virtual void EstablishConnection() { Console.WriteLine(\"Phone.EstablishConnection\"); // Do work to establish the connection.</p>\n<p>} }</p>\n<p>}</p>\n<p>Now when CompanyB compiles its BetterPhone type (derived from this new version of CompanyA\u2019s Phone), the compiler issues this message.</p>\n<p>warning CS0114: 'CompanyB.BetterPhone.EstablishConnection()' hides inherited member 'CompanyA. Phone.EstablishConnection()'. To make the current member override that implementation, add the override keyword. Otherwise, add the new keyword.</p>\n<p>The compiler is alerting you to the fact that both Phone and BetterPhone offer an Establish\u00ad</p>\n<p>Connection method and that the semantics of both might not be identical; simply recompiling BetterPhone can no longer give the same behavior as it did when using the first version of the Phone type.</p>\n<p>If CompanyB decides that the EstablishConnection methods are not semantically identical in both types, CompanyB can tell the compiler that the Dial and EstablishConnection method defined in BetterPhone is the correct method to use and that it has no relationship with the EstablishConnection method defined in the Phone base type. CompanyB informs the compiler of this by adding the new keyword to the EstablishConnection method.</p>\n</li>\n<li><p>namespace CompanyB { public class BetterPhone : CompanyA.Phone {</p></li>\n</ul><p>// Keep 'new' to mark this method as having no // relationship to the base type's Dial method. public new void Dial() {</p><p>Console.WriteLine(\"BetterPhone.Dial\"); EstablishConnection(); base.Dial();</p><p>}</p><p>}</p><p>// Add 'new' to mark this method as having no // relationship to the base type's EstablishConnection method. protected new virtual void EstablishConnection() {</p><p>Console.WriteLine(\"BetterPhone.EstablishConnection\"); // Do work to establish the connection.</p><p>} }</p><p>In this code, the new keyword tells the compiler to emit metadata, making it clear to the CLR that BetterPhone\u2019s EstablishConnection method is intended to be treated as a new function that is introduced by the BetterPhone type. The CLR will know that there is no relationship between Phone\u2019s and BetterPhone\u2019s methods.</p><p>When the same application code (in the Main method) executes, the output is as follows.</p><p>BetterPhone.Dial BetterPhone.EstablishConnection Phone.Dial Phone.EstablishConnection</p><p>This output shows that Main\u2019s call to Dial calls the new Dial method defined by BetterPhone.Dial, which in turn calls the virtual EstablishConnection method that is also defined by BetterPhone. When BetterPhone\u2019s EstablishConnection method returns, Phone\u2019s Dial method is called. Phone\u2019s Dial method calls EstablishConnection, but because BetterPhone\u2019s EstablishConnection is marked with new, BetterPhone\u2019s EstablishConnection method isn\u2019t considered an override of Phone\u2019s virtual EstablishConnection method. As a result, Phone\u2019s Dial method calls Phone\u2019s EstablishConnection method\u2014this is the expected behavior.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile63.png\"/><p>Note If the compiler treated methods as overrides by default (as a native C++ compiler does), the developer of BetterPhone couldn\u2019t use the method names Dial and EstablishConnection. This would most likely cause a ripple effect of changes through-</p><p>out the entire source code base, breaking source and binary compatibility. This type of pervasive change is undesirable, especially in any moderate-to-large project. However, if changing the method name causes only moderate updates in the source code, you should change the name of the methods so the two different meanings of Dial and EstablishConnection don\u2019t confuse other developers.</p><p>Alternatively, CompanyB could have gotten the new version of CompanyA\u2019s Phone type and decided that Phone\u2019s semantics of Dial and EstablishConnection are exactly what it\u2019s been looking for. In this case, CompanyB would modify its BetterPhone type by removing its Dial method entirely. In addition, because CompanyB now wants to tell the compiler that BetterPhone\u2019s EstablishConnection method is related to Phone\u2019s EstablishConnection method, the new keyword must be removed. Simply removing the new keyword isn\u2019t enough, though, because now the compiler can\u2019t tell exactly what the intention is of BetterPhone\u2019s EstablishConnection method. To express his intent exactly, the CompanyB developer must also change BetterPhone\u2019s EstablishConnection method from virtual to override. The following code shows the new version of BetterPhone.</p><p>namespace CompanyB { public class BetterPhone : CompanyA.Phone {</p><p>// Delete the Dial method (inherit Dial from base).</p><p>// Remove 'new' and change 'virtual' to 'override' to // mark this method as having a relationship to the base // type's EstablishConnection method. protected override void EstablishConnection() {</p><p>Console.WriteLine(\"BetterPhone.EstablishConnection\"); // Do work to establish the connection.</p><p>} }</p><p>}</p><p>Now when the same application code (in the Main method) executes, the output is as follows.</p><p>Phone.Dial BetterPhone.EstablishConnection</p><p>This output shows that Main\u2019s call to Dial calls the Dial method defined by Phone and inherited by BetterPhone. Then when Phone\u2019s Dial method calls the virtual EstablishConnection method, BetterPhone\u2019s EstablishConnection method is called because it overrides the virtual EstablishConnection method defined by Phone.</p><h6>C H A P T E R 7</h6><h2>Constants and Fields</h2><h6>In this chapter:</h6><p>Constants . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 175 Fields . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 177</p><p>In this chapter, I\u2019ll show you how to add data members to a type. Specifically, we\u2019ll look at constants and fields.</p><h6>Constants</h6><p>A constant is a symbol that has a never-changing value. When defining a constant symbol, its value must be determinable at compile time. The compiler then saves the constant\u2019s value in the assembly\u2019s metadata. This means that you can define a constant only for types that your compiler considers primitive types. In C#, the following types are primitives and can be used to define constants: Boolean, Char, Byte, SByte, Int16, UInt16, Int32, UInt32, Int64, UInt64, Single, Double, Decimal, and String. However, C# also allows you to define a constant variable of a non-primitive type if you set the value to null.</p><p>using System;</p><p>public sealed class SomeType { // SomeType is not a primitive type but C# does allow // a constant variable of this type to be set to 'null'. public const SomeType Empty = null;</p><p>}</p><p>Because a constant value never changes, constants are always considered to be part of the defining type. In other words, constants are always considered to be static members, not instance members. Defining a constant causes the creation of metadata.</p><p>When code refers to a constant symbol, compilers look up the symbol in the metadata of the assembly that defines the constant, extract the constant\u2019s value, and embed the value in the emitted Intermediate Language (IL) code. Because a constant\u2019s value is embedded directly in code, constants don\u2019t require any memory to be allocated for them at run time. In addition, you can\u2019t get the address of a constant and you can\u2019t pass a constant by reference. These constraints also mean that constants don\u2019t have a good cross-assembly versioning story, so you should use them only when you know that</p><p>175</p><p>the value of a symbol will never change. (Defining MaxInt16 as 32767 is a good example.) Let me demonstrate exactly what I mean. First, take the following code and compile it into a DLL assembly.</p><p>using System;</p><p>public sealed class SomeLibraryType { // NOTE: C# doesn't allow you to specify static for constants // because constants are always implicitly static. public const Int32 MaxEntriesInList = 50;</p><p>}</p><p>Then use the following code to build an application assembly.</p><p>using System;</p><p>public sealed class Program { public static void Main() { Console.WriteLine(\"Max entries supported in list: \"</p><p>+ SomeLibraryType.MaxEntriesInList); }</p><p>}</p><p>You\u2019ll notice that this application code references the MaxEntriesInList constant defined in the SomeLibraryType class. When the compiler builds the application code, it sees that MaxEntriesInList is a constant literal with a value of 50 and embeds the Int32 value of 50 right inside the application\u2019s IL code, as you can see in the following IL code. In fact, after building the application assembly, the DLL assembly isn\u2019t even loaded at run time and can be deleted from the disk because the compiler does not even add a reference to the DLL assembly in the application's metadata.</p><p>.method public hidebysig static void Main() cil managed {</p><p>.entrypoint // Code size 25 (0x19)</p><p>.maxstack 8</p><ul style=\"list-style-type: none;\">\n<li><p>IL_0000: nop</p></li>\n<li><p>IL_0001: ldstr \"Max entries supported in list: \" IL_0006: ldc.i4.s 50 IL_0008: box [mscorlib]System.Int32 IL_000d: call string [mscorlib]System.String::Concat(object, object) IL_0012: call void [mscorlib]System.Console::WriteLine(string)</p></li>\n</ul><ul style=\"list-style-type: none;\">\n<li><p>IL_0017: nop</p></li>\n<li><p>IL_0018: ret</p></li>\n</ul><p>} // end of method Program::Main</p><p>This example should make the versioning problem obvious to you. If the developer changes the MaxEntriesInList constant to 1000 and only rebuilds the DLL assembly, the application assembly</p><p>is not affected. For the application to pick up the new value, it will have to be recompiled as well. You can\u2019t use constants if you need to have a value in one assembly picked up by another assembly at run time (instead of compile time). Instead, you can use readonly fields, which I\u2019ll discuss next.</p><h6>Fields</h6><p>A field is a data member that holds an instance of a value type or a reference to a reference type. Table 7-1 shows the modifiers that can be applied to a field.</p><figcaption>TABLE 7-1 Field Modifiers</figcaption><table border=\"1\">\n<tr>\n<td><p>CLR Term Static</p>\n</td>\n<td><p>C# Term</p>\n<p>static</p>\n</td>\n<td><h6>Description</h6>\n<p>The field is part of the type\u2019s state, as opposed to being part of an object\u2019s state.</p>\n</td>\n</tr>\n<tr>\n<td><p>Instance</p>\n</td>\n<td><p>(default)</p>\n</td>\n<td><p>The field is associated with an instance of the type, not the type itself.</p>\n</td>\n</tr>\n<tr>\n<td><p>InitOnly</p>\n</td>\n<td><p>readonly</p>\n</td>\n<td><p>The field can be written to only by code contained in a constructor method.</p>\n</td>\n</tr>\n<tr>\n<td><p>Volatile</p>\n</td>\n<td><p>volatile</p>\n</td>\n<td><p>Code that accessed the field is not subject to some thread-unsafe optimizations that may be performed by the compiler, the CLR, or by hardware. Only the following types can be marked volatile: all reference types, Single, Boolean, Byte, SByte, Int16, UInt16, Int32, UInt32, Char, and all enumerated types with an underlying type of Byte, SByte, Int16, UInt16, Int32, or UInt32. Volatile fields are discussed in Chapter 29, \u201cPrimitive Thread Synchronization Constructs.\u201d</p>\n</td>\n</tr>\n</table><p>As Table 7-1 shows, the common language run time (CLR) supports both type (static) and instance (nonstatic) fields. For type fields, the dynamic memory required to hold the field\u2019s data is allocated inside the type object, which is created when the type is loaded into an AppDomain (see Chapter 22, \u201cCLR Hosting and AppDomains\u201d), which typically happens the first time any method that references the type is just-in-time (JIT)\u2013compiled. For instance fields, the dynamic memory to hold the field is allocated when an instance of the type is constructed.</p><p>Because fields are stored in dynamic memory, their value can be obtained at run time only. Fields also solve the versioning problem that exists with constants. In addition, a field can be of any data type, so you don\u2019t have to restrict yourself to your compiler\u2019s built-in primitive types (as you do for constants).</p><p>The CLR supports readonly fields and read/write fields. Most fields are read/write fields, meaning the field\u2019s value might change multiple times as the code executes. However, readonly fields can be written to only within a constructor method (which is called only once, when an object is first created). Compilers and verification ensure that readonly fields are not written to by any method other than a constructor. Note that reflection can be used to modify a readonly field.</p><p>Let\u2019s take the example from the \u201cConstants\u201d section and fix the versioning problem by using a static readonly field. Here\u2019s the new version of the DLL assembly\u2019s code.</p><p>using System;</p><p>public sealed class SomeLibraryType { // The static is required to associate the field with the type. public static readonly Int32 MaxEntriesInList = 50;</p><p>}</p><p>This is the only change you have to make; the application code doesn\u2019t have to change at all, although you must rebuild it to see the new behavior. Now when the application\u2019s Main method runs, the CLR will load the DLL assembly (so this assembly is now required at run time) and grab the value of the MaxEntriesInList field out of the dynamic memory allocated for it. Of course, the value will be 50.</p><p>Let\u2019s say that the developer of the DLL assembly changes the 50 to 1000 and rebuilds the assembly. When the application code is re-executed, it will automatically pick up the new value: 1000. In this case, the application code doesn\u2019t have to be rebuilt\u2014it just works (although its performance is adversely affected). A caveat: this scenario assumes that the new version of the DLL assembly is not strongly named and the versioning policy of the application is such that the CLR loads this new version.</p><p>The following example shows how to define a readonly static field that is associated with the type itself, as well as read/write static fields and readonly and read/write instance fields, as shown here.</p><p>public sealed class SomeType { // This is a static read\u00adonly field; its value is calculated and // stored in memory when this class is initialized at run time. public static readonly Random s_random = new Random();</p><p>// This is a static read/write field. private static Int32 s_numberOfWrites = 0;</p><p>// This is an instance read\u00adonly field. public readonly String Pathname = \"Untitled\";</p><p>// This is an instance read/write field. private System.IO.FileStream m_fs;</p><p>public SomeType(String pathname) { // This line changes a read\u00adonly field. // This is OK because the code is in a constructor. this.Pathname = pathname;</p><p>}</p><p>public String DoSomething() { // This line reads and writes to the static read/write field. s_numberOfWrites = s_numberOfWrites + 1;</p><p>} }</p><p>// This line reads the read\u00adonly instance field. return Pathname;</p><p>In this code, many of the fields are initialized inline. C# allows you to use this convenient inline initialization syntax to initialize a class\u2019s constants and read/write and readonly fields. As you\u2019ll see in Chapter 8, \u201cMethods,\u201d C# treats initializing a field inline as shorthand syntax for initializing the field in a constructor. Also, in C#, there are some performance issues to consider when initializing fields by using inline syntax versus assignment syntax in a constructor. These performance issues are discussed in Chapter 8 as well.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile64.png\"/><p>Important When a field is of a reference type and the field is marked as readonly, it is the reference that is immutable, not the object that the field refers to. The following code demonstrates.</p><p>public sealed class AType { // InvalidChars must always refer to the same array object public static readonly Char[] InvalidChars = new Char[] { 'A', 'B', 'C' };</p><p>}</p><p>public sealed class AnotherType {</p><p>public static void M() { // The lines below are legal, compile, and successfully // change the characters in the InvalidChars array</p><ul style=\"list-style-type: none;\">\n<li><p>AType.InvalidChars[0] = 'X';</p></li>\n<li><p>AType.InvalidChars[1] = 'Y';</p></li>\n<li><p>AType.InvalidChars[2] = 'Z';</p></li>\n</ul><p>} }</p><p>// The line below is illegal and will not compile because // what InvalidChars refers to cannot be changed AType.InvalidChars = new Char[] { 'X', 'Y', 'Z' };</p><h6>C H A P T E R 8</h6><h2>Methods</h2><h6>In this chapter:</h6><p>Instance Constructors and Classes (Reference Types) . . . . . . . . 181 Instance Constructors and Structures (Value Types) . . . . . . . . . 184 Type Constructors. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 187 Operator Overload Methods . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 191 Conversion Operator Methods . . . . . . . . . . . . . . . . . . . . . . . . . . . . 195 Extension Methods. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 198 Partial Methods. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 204</p><p>This chapter focuses on the various kinds of methods that you\u2019ll run into, including instance constructors and type constructors, as well as how to define methods to overload operators and type conversions (for implicit and explicit casting). We\u2019ll also talk about extension methods, which allow you to logically add your own instance methods to already existing types, and partial methods, which allow you to spread a type\u2019s implementation into multiple parts.</p><h6>Instance Constructors and Classes (Reference Types)</h6><p>Constructors are special methods that allow an instance of a type to be initialized to a good state. Constructor methods are always called .ctor (for constructor) in a method definition metadata table. When creating an instance of a reference type, memory is allocated for the instance\u2019s data fields, the object\u2019s overhead fields (type object pointer and sync block index) are initialized, and then the type\u2019s instance constructor is called to set the initial state of the object.</p><p>When constructing a reference type object, the memory allocated for the object is always zeroed out before the type\u2019s instance constructor is called. Any fields that the constructor doesn\u2019t explicitly overwrite are guaranteed to have a value of 0 or null.</p><p>Unlike other methods, instance constructors are never inherited. That is, a class has only the instance constructors that the class itself defines. Because instance constructors are never inherited, you cannot apply the following modifiers to an instance constructor: virtual, new, override, sealed, or abstract. If you define a class that does not explicitly define any constructors, the C# compiler defines a default (parameterless) constructor for you whose implementation simply calls the base class\u2019s parameterless constructor.</p><p>181</p><p>For example, if you define the following class.</p><p>public class SomeType { }</p><p>it is as though you wrote the code as follows.</p><p>public class SomeType {</p><p>public SomeType() : base() { } }</p><p>If the class is abstract, the compiler-produced default constructor has protected accessibility; otherwise, the constructor is given public accessibility. If the base class doesn\u2019t offer a parameterless constructor, the derived class must explicitly call a base class constructor or the compiler will issue an error. If the class is static (sealed and abstract), the compiler will not emit a default constructor at all into the class definition.</p><p>A type can define several instance constructors. Each constructor must have a different signature, and each can have different accessibility. For verifiable code, a class\u2019s instance constructor must call its base class\u2019s constructor before accessing any of the inherited fields of the base class. The C# compiler will generate a call to the default base class\u2019s constructor automatically if the derived class\u2019s constructor does not explicitly invoke one of the base class\u2019s constructors. Ultimately, System.Object\u2019s public, parameterless constructor gets called. This constructor does nothing\u2014it simply returns. This is because System.Object defines no instance data fields, and therefore its constructor has nothing to do.</p><p>In a few situations, an instance of a type can be created without an instance constructor being called. In particular, calling Object\u2019s MemberwiseClone method allocates memory, initializes the object\u2019s overhead fields, and then copies the source object\u2019s bytes to the new object. Also, a constructor is usually not called when deserializing an object with the runtime serializer. The deserialization code allocates memory for the object without calling a constructor by using the System.Runtime.Serialization.FormatterServices type's GetUninitializedObject or GetSafeUninitializedObject methods (as discussed in Chapter 24, \u201cRuntime Serialization\u201d).</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile65.png\"/><p>Important You should not call any virtual methods within a constructor that can affect the object being constructed. The reason is if the virtual method is overridden in the type being instantiated, the derived type\u2019s implementation of the overridden method will execute, but all of the fields in the hierarchy have not been fully initialized. Calling a virtual method would therefore result in unpredictable behavior.</p><p>C# offers a simple syntax that allows the initialization of fields defined within a reference type when an instance of the type is constructed.</p><p>internal sealed class SomeType {</p><p>private Int32 m_x = 5; }</p><p>When a SomeType object is constructed, its m_x field will be initialized to 5. How does this happen? Well, if you examine the Intermediate Language (IL) for SomeType\u2019s constructor method (also called .ctor), you\u2019ll see the code shown here.</p><p>.method public hidebysig specialname rtspecialname instance void .ctor() cil managed</p><p>{</p><p>// Code size 14 (0xe)</p><p>.maxstack 8</p><ul style=\"list-style-type: none;\">\n<li><p>IL_0000: ldarg.0</p></li>\n<li><p>IL_0001: ldc.i4.5</p></li>\n<li><p>IL_0002: stfld int32 SomeType::m_x</p></li>\n</ul><ul style=\"list-style-type: none;\">\n<li><p>IL_0007: ldarg.0</p></li>\n<li><p>IL_0008: call instance void [mscorlib]System.Object::.ctor() IL_000d: ret</p></li>\n</ul><p>} // end of method SomeType::.ctor</p><p>In this code, you see that SomeType\u2019s constructor contains code to store a 5 into m_x and then calls the base class\u2019s constructor. In other words, the C# compiler allows the convenient syntax that lets you initialize the instance fields inline and translates this to code in the constructor method to perform the initialization. This means that you should be aware of code explosion, as illustrated by the following class definition.</p><p>internal sealed class SomeType { private Int32 m_x = 5; private String m_s = \"Hi there\"; private Double m_d = 3.14159; private Byte m_b;</p><p>}</p><p>// Here are some constructors. public SomeType() { ... } public SomeType(Int32 x) { ... } public SomeType(String s) { ...; m_d = 10; }</p><p>When the compiler generates code for the three constructor methods, the beginning of each method includes the code to initialize m_x, m_s, and m_d. After this initialization code, the compiler inserts a call to the base class\u2019s constructor, and then the compiler appends to the method the code that appears in the constructor methods. For example, the code generated for the constructor that takes a String parameter includes the code to initialize m_x, m_s, and m_d, call the base class\u2019s (Object\u2019s) constructor, and then overwrite m_d with the value 10. Note that m_b is guaranteed to be initialized to 0 even though no code exists to explicitly initialize it.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile66.png\"/><p>Note The compiler initializes any fields by using the convenient syntax before calling a base class\u2019s constructor to maintain the impression that these fields always have a value as the source code appearance dictates. The potential problem occurs when a base class\u2019s constructor invokes a virtual method that calls back into a method defined by the derived class. If this happens, the fields initialized by using the convenient syntax have been initialized before the virtual method is called.</p><p>Because there are three constructors in the preceding class, the compiler generates the code to initialize m_x, m_s, and m_d three times\u2014once per constructor. If you have several initialized instance fields and a lot of overloaded constructor methods, you should consider defining the fields without the initialization, creating a single constructor that performs the common initialization, and having each constructor explicitly call the common initialization constructor. This approach will reduce the size of the generated code. Here is an example using C#\u2019s ability to explicitly have a constructor call another constructor by using the this keyword.</p><p>internal sealed class SomeType { // Do not explicitly initialize the fields here. private Int32 m_x; private String m_s; private Double m_d; private Byte m_b;</p><p>// This constructor sets all fields to their default. // All of the other constructors explicitly invoke this constructor. public SomeType() {</p><p>m_x = 5; m_s = \"Hi there\"; m_d = 3.14159; m_b = 0xff;</p><p>}</p><p>// This constructor sets all fields to their default, then changes m_x. public SomeType(Int32 x) : this() {</p><p>m_x = x; }</p><p>// This constructor sets all fields to their default, then changes m_s. public SomeType(String s) : this() {</p><p>m_s = s; }</p><p>// This constructor sets all fields to their default, then changes m_x &amp; m_s. public SomeType(Int32 x, String s) : this() {</p><p>m_x = x; m_s = s;</p><p>} }</p><h6>Instance Constructors and Structures (Value Types)</h6><p>Value type (struct) constructors work quite differently from reference type (class) constructors. The common language runtime (CLR) always allows the creation of value type instances, and there is no way to prevent a value type from being instantiated. For this reason, value types don\u2019t actually even need to have a constructor defined within them, and the C# compiler doesn't emit default parameterless constructors for value types. Examine the following code.</p><p>internal struct Point { public Int32 m_x, m_y;</p><p>} internal sealed class Rectangle {</p><p>public Point m_topLeft, m_bottomRight; }</p><p>To construct a Rectangle, the new operator must be used, and a constructor must be specified. In this case, the default constructor automatically generated by the C# compiler is called. When memory is allocated for the Rectangle, the memory includes the two instances of the Point value type. For performance reasons, the CLR doesn\u2019t attempt to call a constructor for each value type field contained within the reference type. But as I mentioned earlier, the fields of the value types are initialized to 0/null.</p><p>The CLR does allow you to define constructors on value types. The only way that these constructors will execute is if you write code to explicitly call one of them, as in Rectangle\u2019s constructor, shown here.</p><p>internal struct Point { public Int32 m_x, m_y;</p><p>public Point(Int32 x, Int32 y) {</p><p>m_x = x; m_y = y;</p><p>} }</p><p>internal sealed class Rectangle { public Point m_topLeft, m_bottomRight;</p><p>public Rectangle() { // In C#, new on a value type calls the constructor to // initialize the value type's fields. m_topLeft = new Point(1, 2); m_bottomRight = new Point(100, 200);</p><p>} }</p><p>A value type\u2019s instance constructor is executed only when explicitly called. So if Rectangle\u2019s constructor didn\u2019t initialize its m_topLeft and m_bottomRight fields by using the new operator to call Point\u2019s constructor, the m_x and m_y fields in both Point fields would be 0.</p><p>In the Point value type defined earlier, no default parameterless constructor is defined. However, let\u2019s rewrite that code as follows.</p><p>internal struct Point { public Int32 m_x, m_y;</p><p>}</p><p>public Point() {</p><p>m_x = m_y = 5; }</p><p>internal sealed class Rectangle { public Point m_topLeft, m_bottomRight;</p><p>}</p><p>public Rectangle() { }</p><p>Now when a new Rectangle is constructed, what do you think the m_x and m_y fields in the two Point fields, m_topLeft and m_bottomRight, would be initialized to: 0 or 5? (Hint: This is a trick question.)</p><p>Many developers (especially those with a C++ background) would expect the C# compiler to emit code in Rectangle\u2019s constructor that automatically calls Point\u2019s default parameterless constructor for the Rectangle\u2019s two fields. However, to improve the run-time performance of the application, the C# compiler doesn\u2019t automatically emit this code. In fact, many compilers will never emit code to call a value type\u2019s default constructor automatically, even if the value type offers a parameterless constructor. To have a value type\u2019s parameterless constructor execute, the developer must add explicit code to call a value type\u2019s constructor.</p><p>Based on the information in the preceding paragraph, you should expect the m_x and m_y fields in Rectangle\u2019s two Point fields to be initialized to 0 in the code shown earlier because there are no explicit calls to Point\u2019s constructor anywhere in the code.</p><p>However, I did say that my original question was a trick question. The trick part is that C# doesn\u2019t allow a value type to define a parameterless constructor. So the previous code won\u2019t actually compile. The C# compiler produces the following message when attempting to compile that code: error CS0568: Structs cannot contain explicit parameterless constructors.</p><p>C# purposely disallows value types from defining parameterless constructors to remove any confusion a developer might have about when that constructor gets called. If the constructor can\u2019t be defined, the compiler can never generate code to call it automatically. Without a parameterless constructor, a value type\u2019s fields are always initialized to 0/null.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile67.png\"/><p>Note Strictly speaking, value type fields are guaranteed to be 0/null when the value type is a field nested within a reference type. However, stack-based value type fields are not guaranteed to be 0/null. For verifiability, any stack-based value type field must be written to prior to being read. If code could read a value type\u2019s field prior to writing to the field, a security breach is possible. C# and other compilers that produce verifiable code ensure that all stack-based value types have their fields zeroed out or at least written to before being read so that a verification exception won\u2019t be thrown at run time. For the most part, this means that you can assume that your value types have their fields initialized to 0, and you can completely ignore everything in this note.</p><p>Keep in mind that although C# doesn\u2019t allow value types with parameterless constructors, the CLR does. So if the unobvious behavior described earlier doesn\u2019t bother you, you can use another programming language (such as IL assembly language) to define your value type with a parameterless constructor.</p><p>Because C# doesn\u2019t allow value types with parameterless constructors, compiling the following type produces the following message: error CS0573: 'SomeValType.m_x': cannot have instance field initializers in structs.</p><p>internal struct SomeValType { // You cannot do inline instance field initialization in a value type. private Int32 m_x = 5;</p><p>}</p><p>In addition, because verifiable code requires that every field of a value type be written to prior to any field being read, any constructors that you do have for a value type must initialize all of the type\u2019s fields. The following type defines a constructor for the value type but fails to initialize all of the fields.</p><p>internal struct SomeValType { private Int32 m_x, m_y;</p><p>// C# allows value types to have constructors that take parameters. public SomeValType(Int32 x) {</p><p>m_x = x; // Notice that m_y is not initialized here.</p><p>} }</p><p>When compiling this type, the C# compiler produces the following message: error CS0171: Field 'SomeValType.m_y' must be fully assigned before control leaves the constructor. To fix the problem, assign a value (usually 0) to y in the constructor.</p><p>As an alternative way to initialize all the fields of a value type, you can actually do the following.</p><p>// C# allows value types to have constructors that take parameters. public SomeValType(Int32 x) {</p><p>// Looks strange but compiles fine and initializes all fields to 0/null. this = new SomeValType();</p><p>}</p><p>m_x = x; // Overwrite m_x's 0 with x // Notice that m_y was initialized to 0.</p><p>In a value type\u2019s constructor, this represents an instance of the value type itself and you can actually assign to it the result of newing up an instance of the value type, which really just zeroes out all the fields. In a reference type\u2019s constructor, this is considered read-only, so you cannot assign to it at all.</p><h6>Type Constructors</h6><p>In addition to instance constructors, the CLR supports type constructors (also known as static constructors, class constructors, or type initializers). A type constructor can be applied to interfaces (although C# doesn\u2019t allow this), reference types, and value types. Just as instance constructors are used to set the initial state of an instance of a type, type constructors are used to set the initial state of a type. By default, types don\u2019t have a type constructor defined within them. If a type has a type</p><p>constructor, it can have no more than one. In addition, type constructors never have parameters. In C#, here\u2019s how to define a reference type and a value type that have type constructors.</p><p>internal sealed class SomeRefType { static SomeRefType() {</p><p>// This executes the first time a SomeRefType is accessed. }</p><p>}</p><p>internal struct SomeValType { // C# does allow value types to define parameterless type constructors. static SomeValType() {</p><p>// This executes the first time a SomeValType is accessed. }</p><p>}</p><p>You\u2019ll notice that you define type constructors just as you would parameterless instance constructors, except that you must mark them as static. Also, type constructors should always be private; C# makes them private for you automatically. In fact, if you explicitly mark a type constructor as private (or anything else) in your source code, the C# compiler issues the following error: error CS0515: 'SomeValType.SomeValType()': access modifiers are not allowed on static constructors. Type constructors should be private to prevent any developer-written code from calling them; the CLR is always capable of calling a type constructor.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile68.png\"/><p>Important Although you can define a type constructor within a value type, you should never actually do this because there are times when the CLR will not call a value type\u2019s static type constructor. Here is an example.</p><p>internal struct SomeValType { static SomeValType() { Console.WriteLine(\"This never gets displayed\");</p><p>} public Int32 m_x;</p><p>}</p><p>public sealed class Program {</p><p>public static void Main() { SomeValType[] a = new SomeValType[10]; a[0].m_x = 123; Console.WriteLine(a[0].m_x); // Displays 123</p><p>} }</p><p>The calling of a type constructor is a tricky thing. When the just-in-time (JIT) compiler is compiling a method, it sees what types are referenced in the code. If any of the types define a type constructor, the JIT compiler checks if the type\u2019s type constructor has already been executed for this AppDomain. If the constructor has never executed, the JIT compiler emits a call to the type constructor into the native code that the JIT compiler is emitting. If the type constructor for the type has already executed, the JIT compiler does not emit the call because it knows that the type is already initialized.</p><p>Now, after the method has been JIT-compiled, the thread starts to execute it and will eventually get to the code that calls the type constructor. In fact, it is possible that multiple threads will be executing the same method concurrently. The CLR wants to ensure that a type\u2019s constructor executes only once per AppDomain. To guarantee this, when a type constructor is called, the calling thread acquires a mutually exclusive thread synchronization lock. So if multiple threads attempt to simultaneously call a type\u2019s static constructor, only one thread will acquire the lock and the other threads will block. The first thread will execute the code in the static constructor. After the first thread leaves the constructor, the waiting threads will wake up and will see that the constructor\u2019s code has already been executed. These threads will not execute the code again; they will simply return from the constructor method. In addition, if any of these methods ever get called again, the CLR knows that the type constructor has already executed and will ensure that the constructor is not called again.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile69.png\"/><p>Note Because the CLR guarantees that a type constructor executes only once per AppDomain and is thread-safe, a type constructor is a great place to initialize any singleton objects required by the type.</p><p>Within a single thread, there is a potential problem that can occur if two type constructors contain code that reference each other. For example, ClassA has a type constructor containing code that references ClassB, and ClassB has a type constructor containing code that references ClassA. In this situation, the CLR still guarantees that each type constructor\u2019s code executes only once; however, it cannot guarantee that ClassA\u2019s type constructor code has run to completion before executing ClassB\u2019s type constructor. You should certainly try to avoid writing code that sets up this scenario. In fact, because the CLR is responsible for calling type constructors, you should always avoid writing any code that requires type constructors to be called in a specific order.</p><p>Finally, if a type constructor throws an unhandled exception, the CLR considers the type to be unusable. Attempting to access any fields or methods of the type will cause a System.TypeInitializationException to be thrown.</p><p>The code in a type constructor has access only to a type\u2019s static fields, and its usual purpose is to initialize those fields. As it does with instance fields, C# offers a simple syntax that allows you to initialize a type\u2019s static fields.</p><p>internal sealed class SomeType { private static Int32 s_x = 5;</p><p>}</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile70.png\"/><p>Note Although C# doesn\u2019t allow a value type to use inline field initialization syntax for instance fields, it does allow you to use it for static fields. In other words, if you change the SomeType type above from a class to a struct, the code will compile and work as expected.</p><p>When this code is built, the compiler automatically generates a type constructor for SomeType. It\u2019s as if the source code had originally been written as follows.</p><p>internal sealed class SomeType { private static Int32 s_x; static SomeType() { s_x = 5; }</p><p>}</p><p>Using ILDasm.exe, it\u2019s easy to verify what the compiler actually produced by examining the IL for the type constructor. Type constructor methods are always called .cctor (for class constructor) in a method definition metadata table.</p><p>In the code below, you see that the .cctor method is private and static. In addition, notice that the code in the method does in fact load a 5 into the static field s_x.</p><p>.method private hidebysig specialname rtspecialname static</p><p>void .cctor() cil managed {</p><p>// Code size 7 (0x7)</p><p>.maxstack 8 IL_0000: ldc.i4.5 IL_0001: stsfld int32 SomeType::s_x IL_0006: ret</p><p>} // end of method SomeType::.cctor</p><p>Type constructors shouldn\u2019t call a base type\u2019s type constructor. Such a call isn\u2019t necessary because none of a type\u2019s static fields are shared or inherited from its base type.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile71.png\"/><p>Note Some languages, such as Java, expect that accessing a type causes its type constructor and all of its base type\u2019s type constructors to be called. In addition, interfaces implemented by the types must also have their type constructors called. The CLR doesn\u2019t offer this behavior. However, the CLR does offer compilers and developers the ability to provide this behavior via the RunClassConstructor method offered by the System. Runtime.CompilerServices.RuntimeHelpers type. Any language that requires this behavior would have its compiler emit code into a type\u2019s type constructor that calls this method for all base types. When using the RunClassConstructor method to call a type constructor, the CLR knows if the type constructor has executed previously and, if it has, the CLR won\u2019t call it again.</p><p>Finally, assume that you have this code.</p><p>internal sealed class SomeType { private static Int32 s_x = 5;</p><p>}</p><p>static SomeType() {</p><p>s_x = 10; }</p><p>In this case, the C# compiler generates a single type constructor method. This constructor first initializes s_x to 5 and then initializes s_x to 10. In other words, when the C# compiler generates IL code for the type constructor, it first emits the code required to initialize the static fields followed by the explicit code contained in your type constructor method.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile72.png\"/><p>Important Developers occasionally ask me if there\u2019s a way to get some code to execute when a type is unloaded. You should first know that types are unloaded only when the AppDomain unloads. When the AppDomain unloads, the object that identifies the type becomes unreachable, and the garbage collector reclaims the type object\u2019s memory. This behavior leads many developers to believe that they could add a static Finalize method to the type, which will automatically get called when the type is unloaded. Unfortunately, the CLR doesn\u2019t support static Finalize methods. All is not lost, however. If you want some code to execute when an AppDomain unloads, you can register a callback method with the System.AppDomain type\u2019s DomainUnload event.</p><h6>Operator Overload Methods</h6><p>Some programming languages allow a type to define how operators should manipulate instances of the type. For example, a lot of types (such as System.String, System.Decimal, and System.DateTime) overload the equality (==) and inequality (!=) operators. The CLR doesn\u2019t know anything about operator overloading because it doesn\u2019t even know what an operator is. Your programming language defines what each operator symbol means and what code should be generated when these special symbols appear.</p><p>For example, in C#, applying the + symbol to primitive numbers causes the compiler to generate code that adds the two numbers together. When the + symbol is applied to String objects, the C# compiler generates code that concatenates the two strings together. For inequality, C# uses the != symbol, while Microsoft Visual Basic uses the &lt;&gt; symbol. Finally, the ^ symbol means exclusive OR (XOR) in C#, but it means exponent in Visual Basic.</p><p>Although the CLR doesn\u2019t know anything about operators, it does specify how languages should expose operator overloads so that they can be readily consumed by code written in a different programming language. Each programming language gets to decide for itself whether it will support operator overloads, and if it does, the syntax for expressing and using them. As far as the CLR is concerned, operator overloads are simply methods.</p><p>Your choice of programming language determines whether or not you get the support of operator overloading and what the syntax looks like. When you compile your source code, the compiler produces a method that identifies the behavior of the operator. The CLR specification mandates that operator overload methods be public and static methods. In addition, C# (and many other languages) requires that at least one of the operator method\u2019s parameters must be the same as the type</p><p>that the operator method is defined within. The reason for this restriction is that it enables the C# compiler to search for a possible operator method to bind to in a reasonable amount of time.</p><p>Here is an example of an operator overload method defined in a C# class definition.</p><p>public sealed class Complex {</p><p>public static Complex operator+(Complex c1, Complex c2) { ... } }</p><p>The compiler emits a metadata method definition entry for a method called op_Addition; the method definition entry also has the specialname flag set, indicating that this is a \u201cspecial\u201d method. When language compilers (including the C# compiler) see a + operator specified in source code, they look to see if one of the operand\u2019s types defines a specialname method called op_Addition whose parameters are compatible with the operand\u2019s types. If this method exists, the compiler emits code to call this method. If no such method exists, a compilation error occurs.</p><p>Tables 8-1 and 8-2 show the set of unary and binary operators that C# supports being overloaded, their symbols, and the corresponding Common Language Specification (CLS) method name that the compiler emits. I\u2019ll explain the tables\u2019 third columns in the next section.</p><ul style=\"list-style-type: none;\">\n<li><p>TABLE 8-1 C# Unary Operators and Their CLS-Compliant Method Names</p><table border=\"1\">\n<tr>\n<td><p>C# Operator Symbol</p>\n<p>+</p>\n</td>\n<td><p>Special Method Name</p>\n<p>op_UnaryPlus</p>\n</td>\n<td><p>Suggested CLS-Compliant Method Name</p>\n<p>Plus</p>\n</td>\n</tr>\n<tr>\n<td><p>\u00ad</p>\n</td>\n<td><p>op_UnaryNegation</p>\n</td>\n<td><p>Negate</p>\n</td>\n</tr>\n<tr>\n<td><p>!</p>\n</td>\n<td><p>op_LogicalNot</p>\n</td>\n<td><p>Not</p>\n</td>\n</tr>\n<tr>\n<td><p>~</p>\n</td>\n<td><p>op_OnesComplement</p>\n</td>\n<td><p>OnesComplement</p>\n</td>\n</tr>\n<tr>\n<td><p>++</p>\n</td>\n<td><p>op_Increment</p>\n</td>\n<td><p>Increment</p>\n</td>\n</tr>\n<tr>\n<td><p>\u00ad\u00ad</p>\n</td>\n<td><p>op_Decrement</p>\n</td>\n<td><p>Decrement</p>\n</td>\n</tr>\n<tr>\n<td><p>(none)</p>\n</td>\n<td><p>op_True</p>\n</td>\n<td><p>IsTrue { get; }</p>\n</td>\n</tr>\n<tr>\n<td><p>(none)</p>\n</td>\n<td><p>op_False</p>\n</td>\n<td><p>IsFalse { get; }</p>\n</td>\n</tr>\n</table>\n</li>\n<li><p>TABLE 8-2 C# Binary Operators and Their CLS-Compliant Method Names</p></li>\n</ul><table border=\"1\">\n<tr>\n<td><p>C# Operator Symbol</p>\n<p>+</p>\n</td>\n<td><p>Special Method Name</p>\n<p>op_Addition</p>\n</td>\n<td><p>Suggested CLS-Compliant Method Name</p>\n<p>Add</p>\n</td>\n</tr>\n<tr>\n<td><p>\u00ad</p>\n</td>\n<td><p>op_Subtraction</p>\n</td>\n<td><p>Subtract</p>\n</td>\n</tr>\n<tr>\n<td><p>*</p>\n</td>\n<td><p>op_Multiply</p>\n</td>\n<td><p>Multiply</p>\n</td>\n</tr>\n<tr>\n<td><p>/</p>\n</td>\n<td><p>op_Division</p>\n</td>\n<td><p>Divide</p>\n</td>\n</tr>\n<tr>\n<td><p>%</p>\n</td>\n<td><p>op_Modulus</p>\n</td>\n<td><p>Mod</p>\n</td>\n</tr>\n<tr>\n<td><p>&amp;</p>\n</td>\n<td><p>op_BitwiseAnd</p>\n</td>\n<td><p>BitwiseAnd</p>\n</td>\n</tr>\n<tr>\n<td><p>|</p>\n</td>\n<td><p>op_BitwiseOr</p>\n</td>\n<td><p>BitwiseOr</p>\n</td>\n</tr>\n<tr>\n<td><p>^</p>\n</td>\n<td><p>op_ExclusiveOr</p>\n</td>\n<td><p>Xor</p>\n</td>\n</tr>\n</table><table border=\"1\">\n<tr>\n<td><p>C# Operator Symbol</p>\n<p>&lt;&lt;</p>\n</td>\n<td><p>Special Method Name</p>\n<p>op_LeftShift</p>\n</td>\n<td><p>Suggested CLS-Compliant Method Name</p>\n<p>LeftShift</p>\n</td>\n</tr>\n<tr>\n<td><p>&gt;&gt;</p>\n</td>\n<td><p>op_RightShift</p>\n</td>\n<td><p>RightShift</p>\n</td>\n</tr>\n<tr>\n<td><p>==</p>\n</td>\n<td><p>op_Equality</p>\n</td>\n<td><p>Equals</p>\n</td>\n</tr>\n<tr>\n<td><p>!=</p>\n</td>\n<td><p>op_Inequality</p>\n</td>\n<td><p>Equals</p>\n</td>\n</tr>\n<tr>\n<td><p>&lt;</p>\n</td>\n<td><p>op_LessThan</p>\n</td>\n<td><p>Compare</p>\n</td>\n</tr>\n<tr>\n<td><p>&gt;</p>\n</td>\n<td><p>op_GreaterThan</p>\n</td>\n<td><p>Compare</p>\n</td>\n</tr>\n<tr>\n<td><p>&lt;=</p>\n</td>\n<td><p>op_LessThanOrEqual</p>\n</td>\n<td><p>Compare</p>\n</td>\n</tr>\n<tr>\n<td><p>&gt;=</p>\n</td>\n<td><p>op_GreaterThanOrEqual</p>\n</td>\n<td><p>Compare</p>\n</td>\n</tr>\n</table><figcaption>The CLR specification defines many additional operators that can be overloaded, but C# does not support these additional operators. Therefore, they are not in mainstream use, so I will not list them here. If you are interested in the complete list, please see the ECMA specifications (www.ecma-international.org/publications/standards/Ecma-335.htm) for the Common Language Infrastructure (CLI), Partition I, Concepts and Architecture, Sections 10.3.1 (unary operators) and 10.3.2 (binary operators).</figcaption><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile73.png\"/><p>Note If you examine the core numeric types (Int32, Int64, UInt32, and so on) in the Framework Class Library (FCL), you\u2019ll see that they don\u2019t define any operator overload methods. The reason they don\u2019t is that compilers look specifically for operations on these primitive types and emit IL instructions that directly manipulate instances of these types. If the types were to offer methods and if compilers were to emit code to call these methods, a run-time performance cost would be associated with the method call. Plus, the method would ultimately have to execute some IL instructions to perform the expected operation anyway. This is the reason why the core FCL types don\u2019t define any operator overload methods. Here\u2019s what this means to you: if the programming language you\u2019re using doesn\u2019t support one of the core FCL types, you won\u2019t be able to perform any operations on instances of that type.</p><h6>Operators and Programming Language Interoperability</h6><p>Operator overloading can be a very useful tool, allowing developers to express their thoughts with succinct code. However, not all programming languages support operator overloading. When using a language that doesn\u2019t support operator overloading, the language will not know how to interpret the</p><p>+ operator (unless the type is a primitive in that language), and the compiler will emit an error. When using languages that do not support operator overloading, the language should allow you to call the desired op_* method directly (such as op_Addition).</p><p>If you are using a language that doesn\u2019t support + operator overloading to be defined in a type, obviously, this type could still offer an op_Addition method. From C#, you might expect that you</p><p>could call this op_Addition method by using the + operator, but you cannot. When the C# compiler detects the + operator, it looks for an op_Addition method that has the specialname metadata flag associated with it so that the compiler knows for sure that the op_Addition method is intended to be an operator overload method. Because the op_Addition method is produced by a language that doesn\u2019t support operator overloads, the method won\u2019t have the specialname flag associated with it, and the C# compiler will produce a compilation error. Of course, code in any language can explicitly call a method that just happens to be named op_Addition, but the compilers won\u2019t translate a usage of the + symbol to call this method.</p><h6>Jeff\u2019s Opinion About Microsoft\u2019s Operator Method Name Rules</h6><p>I\u2019m sure that all of these rules about when you can and can\u2019t call an operator overload method seem very confusing and overly complicated. If compilers that supported operator overloading just didn\u2019t emit the specialname metadata flag, the rules would be a lot simpler, and programmers would have an easier time working with types that offer operator overload methods. Languages that support operator overloading would support the operator symbol syntax, and all languages would support calling the various op_ methods explicitly. I can\u2019t come up with any reason why Microsoft made this so difficult, and I hope that they\u2019ll loosen these rules in future versions of their compilers.</p><p>For a type that defines operator overload methods, Microsoft recommends that the type also define friendlier public static methods that call the operator overload methods internally. For example, a public-friendly named method called Add should be defined by a type that overloads the op_Addition method. The third column in Tables 8-1 and 8-2 lists the recommended friendly name for each operator. So the Complex type shown earlier should be defined in the following way.</p><p>public sealed class Complex { public static Complex operator+(Complex c1, Complex c2) { ... } public static Complex Add(Complex c1, Complex c2) { return(c1 + c2); }</p><p>}</p><p>Certainly, code written in any programming language can call any of the friendly operator methods, such as Add. Microsoft\u2019s guideline that types offer these friendly method names complicates the story even more. I feel that this additional complication is unnecessary, and that calling these friendly named methods would cause an additional performance hit unless the JIT compiler is able to inline the code in the friendly named method. Inlining the code would cause the JIT compiler to optimize the code, removing the additional method call and boosting runtime performance.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile74.png\"/><p>Note For an example of a type that overloads operators and uses the friendly method names as per Microsoft\u2019s design guidelines, see the System.Decimal class in the FCL.</p><h6>Conversion Operator Methods</h6><p>Occasionally, you need to convert an object from one type to an object of a different type. For example, I\u2019m sure you\u2019ve had to convert a Byte to an Int32 at some point in your life. When the source type and the target type are a compiler\u2019s primitive types, the compiler knows how to emit the necessary code to convert the object.</p><p>If the source type or target type is not a primitive, the compiler emits code that has the CLR perform the conversion (cast). In this case, the CLR just checks if the source object\u2019s type is the same type as the target type (or derived from the target type). However, it is sometimes natural to want to convert an object of one type to a completely different type. For example, the System.Xml.Linq.XElement class allows you to convert an Extensible Markup Language (XML) element to a Boolean, (U)Int32, (U)Int64, Single, Double, Decimal, String, DateTime, DateTimeOffset, TimeSpan, Guid, or the nullable equivalent of any of these types (except String). You could also imagine that the FCL included a Rational data type and that it might be convenient to convert an Int32 object or a Single object to a Rational object. Moreover, it also might be nice to convert a Rational object to an Int32 or a Single object.</p><p>To make these conversions, the Rational type should define public constructors that take a single parameter: an instance of the type that you\u2019re converting from. You should also define public instance ToXxx methods that take no parameters (just like the very popular ToString method). Each method will convert an instance of the defining type to the Xxx type. Here\u2019s how to correctly define conversion constructors and methods for a Rational type.</p><p>public sealed class Rational { // Constructs a Rational from an Int32 public Rational(Int32 num) { ... }</p><p>// Constructs a Rational from a Single public Rational(Single num) { ... }</p><p>// Converts a Rational to an Int32 public Int32 ToInt32() { ... }</p><p>}</p><p>// Converts a Rational to a Single public Single ToSingle() { ... }</p><p>By invoking these constructors and methods, a developer using any programming language can convert an Int32 or a Single object to a Rational object and convert a Rational object to an Int32 or a Single object. The ability to do these conversions can be quite handy, and when designing a type, you should seriously consider what conversion constructors and methods make sense for your type.</p><p>In the previous section, I discussed how some programming languages offer operator overloading. Well, some programming languages (such as C#) also offer conversion operator overloading. Conversion operators are methods that convert an object from one type to another type. You define a conversion operator method by using special syntax. The CLR specification mandates that conversion</p><p>overload methods be public and static methods. In addition, C# (and many other languages) requires that either the parameter or the return type must be the same as the type that the conversion method is defined within. The reason for this restriction is that it enables the C# compiler to search for a possible operator method to bind to in a reasonable amount of time. The following code adds four conversion operator methods to the Rational type.</p><p>public sealed class Rational { // Constructs a Rational from an Int32 public Rational(Int32 num) { ... }</p><p>// Constructs a Rational from a Single public Rational(Single num) { ... }</p><p>// Converts a Rational to an Int32 public Int32 ToInt32() { ... }</p><p>// Converts a Rational to a Single public Single ToSingle() { ... }</p><p>// Implicitly constructs and returns a Rational from an Int32 public static implicit operator Rational(Int32 num) {</p><p>return new Rational(num); }</p><p>// Implicitly constructs and returns a Rational from a Single public static implicit operator Rational(Single num) {</p><p>return new Rational(num); }</p><p>// Explicitly returns an Int32 from a Rational public static explicit operator Int32(Rational r) {</p><p>return r.ToInt32(); }</p><p>}</p><p>// Explicitly returns a Single from a Rational public static explicit operator Single(Rational r) {</p><p>return r.ToSingle(); }</p><p>For conversion operator methods, you must indicate whether a compiler can emit code to call a conversion operator method implicitly or whether the source code must explicitly indicate when the compiler is to emit code to call a conversion operator method. In C#, you use the implicit keyword to indicate to the compiler that an explicit cast doesn\u2019t have to appear in the source code in order to emit code that calls the method. The explicit keyword allows the compiler to call the method only when an explicit cast exists in the source code.</p><p>After the implicit or explicit keyword, you tell the compiler that the method is a conversion operator by specifying the operator keyword. After the operator keyword, you specify the type that an object is being cast to; in the parentheses, you specify the type that an object is being cast from.</p><p>Defining the conversion operators in the preceding Rational type allows you to write code like this (in C#).</p><p>public sealed class Program { public static void Main() {</p><ul style=\"list-style-type: none;\">\n<li><p>Rational r1 = 5; // Implicit cast from Int32 to Rational</p></li>\n<li><p>Rational r2 = 2.5F; // Implicit cast from Single to Rational</p></li>\n</ul><p>} }</p><p>Int32 x = (Int32) r1; // Explicit cast from Rational to Int32 Single s = (Single) r2; // Explicit cast from Rational to Single</p><p>Under the covers, the C# compiler detects the casts (type conversions) in the code and internally generates IL code that calls the conversion operator methods defined by the Rational type. But what are the names of these methods? Well, compiling the Rational type and examining its metadata shows that the compiler produces one method for each conversion operator defined. For the Rational type, the metadata for the four conversion operator methods looks like this.</p><p>public static Rational op_Implicit(Int32 num) public static Rational op_Implicit(Single num) public static Int32 op_Explicit(Rational r) public static Single op_Explicit(Rational r)</p><p>As you can see, methods that convert an object from one type to another are always named op_ Implicit or op_Explicit. You should define an implicit conversion operator only when precision or magnitude isn\u2019t lost during a conversion, such as when converting an Int32 to a Rational. However, you should define an explicit conversion operator if precision or magnitude is lost during the conversion, as when converting a Rational object to an Int32. If an explicit conversion fails, you should indicate this by having your explicit conversion operator method throw an OverflowException or an InvalidOperationException.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile75.png\"/><p>Note The two op_Explicit methods take the same parameter, a Rational. However, the methods differ by their return type, an Int32 and a Single. This is an example of two methods that differ only by their return type. The CLR fully supports the ability for a type to define multiple methods that differ only by return type. However, very few languages expose this ability. As you\u2019re probably aware, C++, C#, Visual Basic, and Java are all examples of languages that don\u2019t support the definition of multiple methods that differ only by their return type. A few languages (such as IL assembly language) allow the developer to explicitly select which of these methods to call. Of course, IL assembly language programmers shouldn\u2019t take advantage of this ability because the methods they define can\u2019t be callable from other programming languages. Even though C# doesn\u2019t expose this ability to the C# programmer, the compiler does take advantage of this ability internally when a type defines conversion operator methods.</p><p>C# has full support for conversion operators. When it detects code where you\u2019re using an object of one type and an object of a different type is expected, the compiler searches for an implicit conversion operator method capable of performing the conversion and generates code to call that method. If an implicit conversion operator method exists, the compiler emits a call to it in the resulting IL code. If the compiler sees source code that is explicitly casting an object from one type to another type, the compiler searches for an implicit or explicit conversion operator method. If one exists, the compiler emits the call to the method. If the compiler can\u2019t find an appropriate conversion operator method, it issues an error and doesn\u2019t compile the code.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile76.png\"/><p>Note C# generates code to invoke explicit conversion operators when using a cast expression; they are never invoked when using C#\u2019s as or is operators.</p><p>To really understand operator overload methods and conversion operator methods, I strongly encourage you to examine the System.Decimal type as a role model. Decimal defines several constructors that allow you to convert objects from various types to a Decimal. It also offers several ToXxx methods that let you convert a Decimal object to another type. Finally, the type defines several conversion operators and operator overload methods as well.</p><h6>Extension Methods</h6><p>The best way to understand C#\u2019s extension methods feature is by way of an example. In the \u201cStringBuilder Members\u201d section in Chapter 14, \u201cChars, Strings, and Working with Text,\u201d I mention how the StringBuilder class offers fewer methods than the String class for manipulating a string and how strange this is, considering that the StringBuilder class is the preferred way of manipulating a string because it is mutable. So, let\u2019s say that you would like to define some of these missing methods yourself to operate on a StringBuilder. For example, you might want to define your own IndexOf method as follows.</p><p>public static class StringBuilderExtensions { public static Int32 IndexOf(StringBuilder sb, Char value) { for (Int32 index = 0; index &lt; sb.Length; index++)</p><p>if (sb[index] == value) return index; return \u00ad1;</p><p>} }</p><p>Now that you have defined this method, you can use it as the following code demonstrates.</p><p>StringBuilder sb = new StringBuilder(\"Hello. My name is Jeff.\"); // The initial string</p><p>// Change period to exclamation and get # characters in 1st sentence (5). Int32 index = StringBuilderExtensions.IndexOf(sb.Replace('.', '!'), '!');</p><p>This code works just fine, but is it not ideal from a programmer\u2019s perspective. The first problem is that a programmer who wants to get the index of a character within a StringBuilder must know</p><p>that the StringBuilderExtensions class even exists. The second problem is that the code does not reflect the order of operations that are being performed on the StringBuilder object, making the code difficult to write, read, and maintain. The programmer wants to call Replace first and then call IndexOf; but when you read the last line of code from left to right, IndexOf appears first on the line and Replace appears second. Of course, you could alleviate this problem and make the code\u2019s behavior more understandable by rewriting it like this.</p><p>// First, change period to exclamation mark sb.Replace('.', '!');</p><p>// Now, get # characters in 1st sentence (5) Int32 index = StringBuilderExtensions.IndexOf(sb, '!');</p><p>However, a third problem exists with both versions of this code that affects understanding the</p><p>code\u2019s behavior. The use of StringBuilderExtensions is overpowering and detracts a programmer\u2019s mind from the operation that is being performed: IndexOf. If the StringBuilder class had defined its own IndexOf method, then we could rewrite the code above as follows.</p><p>// Change period to exclamation and get # characters in 1st sentence (5). Int32 index = sb.Replace('.', '!').IndexOf('!');</p><p>Wow, look how great this is in terms of code maintainability! In the StringBuilder object, we\u2019re going to replace a period with an exclamation mark and then find the index of the exclamation mark.</p><p>Now, I can explain what C#\u2019s extension methods feature does. It allows you to define a static method that you can invoke using instance method syntax. Or, in other words, we can now define our own IndexOf method and the three problems mentioned above go away. To turn the IndexOf method into an extension method, we simply add the this keyword before the first argument.</p><p>public static class StringBuilderExtensions { public static Int32 IndexOf(this StringBuilder sb, Char value) { for (Int32 index = 0; index &lt; sb.Length; index++)</p><p>if (sb[index] == value) return index; return \u00ad1;</p><p>} }</p><p>Now, when the compiler sees code like the following, the compiler first checks if the StringBuilder class or any of its base classes offers an instance method called IndexOf that takes a single Char parameter.</p><p>Int32 index = sb.IndexOf('X');</p><p>If an existing instance method exists, then the compiler produces IL code to call it. If no matching instance method exists, then the compiler will look at any static classes that define static methods called IndexOf that take as their first parameter a type matching the type of the expression being used to invoke the method. This type must also be marked with the this keyword. In this example, the expression is sb, which is of the StringBuilder type. In this case, the compiler is looking specifically for an</p><p>IndexOf method that takes two parameters: a StringBuilder (marked with the this keyword) and a Char. The compiler will find our IndexOf method and produce IL code that calls our static method.</p><p>OK\u2014so this now explains how the compiler improves the last two problems related to code understandability that I mentioned earlier. However, I haven\u2019t yet addressed the first problem: how does a programmer know that an IndexOf method even exists that can operate on a StringBuilder object? The answer to this question is found in Microsoft Visual Studio\u2019s IntelliSense feature. In the editor, when you type a period, Visual Studio\u2019s IntelliSense window opens to show you the list of instance methods that are available. Well, that IntelliSense window also shows you any extension methods that exist for the type of expression you have to the left of the period. Figure 8-1 shows Visual Studio\u2019s IntelliSense window; the icon for an extension method has a down arrow next to it, and the tooltip next to the method indicates that the method is really an extension method. This is truly awesome because it is now easy to define your own methods to operate on various types of objects and have other programmers discover your methods naturally when using objects of these types.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile77.png\"/><figcaption>FIGURE 8-1 Visual Studio\u2019s IntelliSense window, showing extension methods.</figcaption><h6>Rules and Guidelines</h6><p>There are some additional rules and guidelines that you should know about extension methods:</p><p>\u25a0\u25a0 C# supports extension methods only; it does not offer extension properties, extension events,</p><p>extension operators, and so on.</p><p>\u25a0\u25a0 Extension methods (methods with this before their first argument) must be declared in nongeneric, static classes. However, there is no restriction on the name of the class; you can call it whatever you want. Of course, an extension method must have at least one parameter, and only the first parameter can be marked with the this keyword.</p><p>\u25a0\u25a0 The C# compiler looks only for extension methods defined in static classes that are themselves defined at the file scope. In other words, if you define the static class nested within another class, the C# compiler will emit the following message: error CS1109: Extension method must be defined in a top\u00adlevel static class; StringBuilderExtensions is a nested class.</p><p>\u25a0\u25a0 Because the static classes can have any name you want, it takes the C# compiler time to find extension methods because it must look at all the file-scope static classes and scan their static methods for a match. To improve performance and also to avoid considering an extension method that you may not want, the C# compiler requires that you \u201cimport\u201d extension methods. For example, if someone has defined a StringBuilderExtensions class in a Win\u00ad</p><p>tellect namespace, then a programmer who wants to have access to this class\u2019s extension methods must put a using Wintellect; directive at the top of his or her source code file.</p><p>\u25a0\u25a0 It is possible that multiple static classes could define the same extension method. If the compiler detects that two or more extension methods exist, then the compiler issues the following message: error CS0121: The call is ambiguous between the following methods or properties: 'StringBuilderExtensions.IndexOf(string, char)' and 'AnotherStringBuilderExtensions.IndexOf(string, char)'. To fix this error, you must modify your source code. Specifically, you cannot use the instance method syntax to call this static method anymore; instead you must now use the static method syntax where you explicitly indicate the name of the static class to explicitly tell the compiler which method you want to invoke.</p><p>\u25a0\u25a0 You should use this feature sparingly, because not all programmers are familiar with it. For example, when you extend a type with an extension method, you are actually extending derived types with this method as well. Therefore, you should not define an extension method whose first parameter is System.Object, because this method will be callable for all expression types and this will really pollute Visual Studio\u2019s IntelliSense window.</p><p>\u25a0\u25a0 There is a potential versioning problem that exists with extension methods. If, in the future, Microsoft adds an IndexOf instance method to their StringBuilder class with the same prototype as my code is attempting to call, then when I recompile my code, the compiler will bind to Microsoft\u2019s IndexOf instance method instead of my static IndexOf method. Because of this, my program will experience different behavior. This versioning problem is another reason why this feature should be used sparingly.</p><h6>Extending Various Types with Extension Methods</h6><p>In this chapter, I demonstrated how to define an extension method for a class, StringBuilder. I\u2019d like to point out that because an extension method is really the invocation of a static method, the CLR does not emit code ensuring that the value of the expression used to invoke the method is not null.</p><p>// sb is null StringBuilder sb = null;</p><p>// Calling extension method: NullReferenceException will NOT be thrown when calling IndexOf // NullReferenceException will be thrown inside IndexOf\u2019s for loop sb.IndexOf('X');</p><p>// Calling instance method: NullReferenceException WILL be thrown when calling Replace sb.Replace('.', '!');</p><p>I\u2019d also like to point out that you can define extension methods for interface types, as the following code shows.</p><p>public static void ShowItems&lt;T&gt;(this IEnumerable&lt;T&gt; collection) { foreach (var item in collection)</p><p>Console.WriteLine(item); }</p><p>The extension method above can now be invoked using any expression that results in a type that implements the IEnumerable&lt;T&gt; interface.</p><p>public static void Main() { // Shows each Char on a separate line in the console \"Grant\".ShowItems();</p><p>// Shows each String on a separate line in the console new[] { \"Jeff\", \"Kristin\" }.ShowItems();</p><p>}</p><p>// Shows each Int32 value on a separate line in the console new List&lt;Int32&gt;() { 1, 2, 3 }.ShowItems();</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile78.png\"/><p>Important Extension methods are the cornerstone of Microsoft\u2019s Language Integrated Query (LINQ) technology. For a great example of a class that offers many extension methods, see the static System.Linq.Enumerable class and all its static extension methods in the Microsoft .NET Framework SDK documentation. Every extension method in this class extends either the IEnumerable or IEnumerable&lt;T&gt; interface.</p><p>You can define extension methods for delegate types, too. Here is an example.</p><p>public static void InvokeAndCatch&lt;TException&gt;(this Action&lt;Object&gt; d, Object o) where TException : Exception { try { d(o); } catch (TException) { }</p><p>}</p><p>And here is an example of how to invoke it.</p><p>Action&lt;Object&gt; action = o =&gt; Console.WriteLine(o.GetType()); // Throws NullReferenceException action.InvokeAndCatch&lt;NullReferenceException&gt;(null); // Swallows NullReferenceException</p><p>You can also add extension methods to enumerated types. I show an example of this in the \u201cAdding Methods to Enumerated Types\u201d section in Chapter 15, \u201cEnumerated Types and Bit Flags.\u201d</p><p>And last but not least, I want to point out that the C# compiler allows you to create a delegate (see Chapter 17, \u201cDelegates,\u201d for more information) that refers to an extension method over an object.</p><p>public static void Main () { // Create an Action delegate that refers to the static ShowItems extension method // and has the first argument initialized to reference the \"Jeff\" string. Action a = \"Jeff\".ShowItems;</p><p>. . . // Invoke the delegate that calls ShowItems passing it a reference to the \"Jeff\" string. a();</p><p>}</p><p>In the preceding code, the C# compiler generates IL code to construct an Action delegate. When creating a delegate, the constructor is passed the method that should be called and is also passed a reference to an object that should be passed to the method\u2019s hidden this parameter. Normally, when you create a delegate that refers to a static method, the object reference is null because static methods don\u2019t have a this parameter. However, in this example, the C# compiler generated some special code that creates a delegate that refers to a static method (ShowItems) and the target object of the static method is the reference to the \u201cJeff\u201d string. Later, when the delegate is invoked, the CLR will call the static method and will pass to it the reference to the \u201cJeff\u201d string. This is a little hacky, but it works great and it feels natural so long as you don\u2019t think about what is happening internally.</p><h6>The Extension Attribute</h6><p>It would be best if this concept of extension methods was not C#-specific. Specifically, we want programmers to define a set of extension methods in some programming language and for people in other programming languages to take advantage of them. For this to work, the compiler of choice must support searching static types and methods for potentially matching extension methods. And compilers need to do this quickly so that compilation time is kept to a minimum.</p><p>In C#, when you mark a static method\u2019s first parameter with the this keyword, the compiler internally applies a custom attribute to the method and this attribute is persisted in the resulting file\u2019s metadata. The attribute is defined in the System.Core.dll assembly, and it looks like this.</p><p>// Defined in the System.Runtime.CompilerServices namespace [AttributeUsage(AttributeTargets.Method | AttributeTargets.Class | AttributeTargets.Assembly)] public sealed class ExtensionAttribute : Attribute { }</p><p>In addition, this attribute is applied to the metadata for any static class that contains at least one extension method. And this attribute is also applied to the metadata for any assembly that contains at least one static class that contains an extension method. So now, when compiling code that invokes an instance method that doesn\u2019t exist, the compiler can quickly scan all the referenced assemblies to know which ones contain extension methods. Then it can scan only these assemblies for static classes that contain extension methods, and it can scan just the extension methods for potential matches to compile the code as quickly as possible.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile79.png\"/><p>Note The ExtensionAttribute class is defined in the System.Core.dll assembly. This means that the resulting assembly produced by the compiler will have a reference to System.Core.dll embedded in it even if I do not use any types from System.Core.dll and do not even reference System.Core.dll when compiling my code. However, this is not too bad a problem because the ExtensionAttribute is used only at compile time; at run time, System.Core.dll will not have to be loaded unless the application consumes something else in this assembly.</p><h6>Partial Methods</h6><p>Imagine that you use a tool that produces a C# source code file containing a type definition. The tool knows that there are potential places within the code it produces where you might want to customize the type\u2019s behavior. Normally, customization would be done by having the tool-produced code invoke virtual methods. The tool-produced code would also have to contain definitions for these virtual methods, and the way these methods would be implemented is to do nothing and simply return. Now, if you want to customize the behavior of the class, you\u2019d define your own class, derive it from the base class, and then override any virtual methods implementing it so that it has the behavior you desire. Here is an example.</p><p>// Tool\u00adproduced code in some source code file: internal class Base {</p><p>private String m_name;</p><p>// Called before changing the m_name field protected virtual void OnNameChanging(String value) { }</p><p>}</p><p>public String Name { get { return m_name; } set {</p><p>OnNameChanging(value.ToUpper()); // Inform class of potential change m_name = value; // Change the field</p><p>} }</p><p>// Developer\u00adproduced code in some other source code file: internal class Derived : Base {</p><p>protected override void OnNameChanging(string value) { if (String.IsNullOrEmpty(value))</p><p>throw new ArgumentNullException(\"value\"); }</p><p>}</p><p>Unfortunately, there are two problems with the preceding code:</p><p>\u25a0\u25a0 The type must be a class that is not sealed. You cannot use this technique for sealed classes or for value types (because value types are implicitly sealed). In addition, you cannot use this technique for static methods because they cannot be overridden.</p><p>\u25a0\u25a0 There are efficiency problems here. A type is being defined just to override a method; this wastes a small amount of system resources. And, even if you do not want to override the behavior of OnNameChanging, the base class code still invokes a virtual method that simply does nothing but return. Also, ToUpper is called whether OnNameChanging accesses the argument passed to it or not.</p><p>C#\u2019s partial methods feature allows you the option of overriding the behavior or a type while fixing the aforementioned problems. The code below uses partial methods to accomplish the same semantic as the previous code.</p><p>// Tool\u00adproduced code in some source code file: internal sealed partial class Base {</p><p>private String m_name;</p><p>// This defining\u00adpartial\u00admethod\u00addeclaration is called before changing the m_name field partial void OnNameChanging(String value);</p><p>}</p><p>public String Name { get { return m_name; } set {</p><p>OnNameChanging(value.ToUpper()); // Inform class of potential change m_name = value; // Change the field</p><p>} }</p><p>// Developer\u00adproduced code in some other source code file: internal sealed partial class Base {</p><p>}</p><p>// This implementing\u00adpartial\u00admethod\u00addeclaration is called before m_name is changed partial void OnNameChanging(String value) {</p><p>if (String.IsNullOrEmpty(value))</p><p>throw new ArgumentNullException(\"value\"); }</p><p>There are several things to notice about this new version of the code:</p><p>\u25a0\u25a0 The class is now sealed (although it doesn\u2019t have to be). In fact, the class could be a static class</p><p>or even a value type.</p><p>\u25a0\u25a0 The tool-produced code and the developer-produced code are really two partial definitions that ultimately make up one type definition. For more information about partial types, see the \u201cPartial Classes, Structures, and Interfaces\u201d section in Chapter 6, \u201cType and Member Basics.\u201d</p><p>\u25a0\u25a0 The tool-produced code defined a partial method declaration. This method is marked with the</p><p>partial token and it has no body.</p><p>\u25a0\u25a0 The developer-produced code implemented the partial method declaration. This method is</p><p>also marked with the partial token and it has a body.</p><p>Now, when you compile this code, you see the same effect as the original code I showed you. Again, the big benefit here is that you can rerun the tool and produce new code in a new source code file, but your code remains in a separate file and is unaffected. And, this technique works for sealed classes, static classes, and value types.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile80.png\"/><p>Note In Visual Studio\u2019s editor, if you type in partial and press the spacebar, the IntelliSense window shows you all the enclosing type\u2019s defined partial method declarations that do not yet have matching implementing partial method declarations. You can then easily select a partial method from the IntelliSense window and Visual Studio will produce the method prototype for you automatically. This is a very nice feature that enhances productivity.</p><p>But, there is another big improvement we get with partial methods. Let\u2019s say that you do not need to modify the behavior of the tool-produced type. In this case, you do not supply your source code file at all. If you just compile the tool-produced code by itself, the compiler produces IL code and metadata as if the tool-produced code looked like this.</p><p>// Logical equivalent of tool\u00adproduced code if there is no // implementing partial method declaration: internal sealed class Base { private String m_name;</p><p>public String Name { get { return m_name; } set {</p><p>m_name = value; // Change the field }</p><p>} }</p><p>That is, if there is no implementing partial method declaration, the compiler will not emit any metadata representing the partial method. In addition, the compiler will not emit any IL instructions to call the partial method. And the compiler will not emit code that evaluates any arguments that would have been passed to the partial method. In this example, the compiler will not emit code to call the ToUpper method. The result is that there is less metadata/IL, and the run-time performance is awesome!</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile81.png\"/><p>Note Partial methods work similarly to the System.Diagnostics.ConditionalAttribute attribute. However, partial methods work within a single type only while the ConditionalAttribute can be used to optionally invoke methods defined in another type.</p><h6>Rules and Guidelines</h6><p>There are some additional rules and guidelines that you should know about partial methods:</p><p>\u25a0\u25a0 They can only be declared within a partial class or struct.</p><p>\u25a0\u25a0 Partial methods must always have a return type of void, and they cannot have any parameters marked with the out modifier. These restrictions are in place because at run time, the method may not exist and so you can\u2019t initialize a variable to what the method might return because the method might not exist. Similarly, you can\u2019t have an out parameter because the method would have to initialize it and the method might not exist. A partial method may have ref parameters, may be generic, may be instance or static, and may be marked as unsafe.</p><p>\u25a0\u25a0 Of course, the defining partial method declaration and the implementing partial method declaration must have identical signatures. If both have custom attributes applied to them, then the compiler combines both methods\u2019 attributes together. Any attributes applied to a parameter are also combined.</p><p>\u25a0\u25a0 If there is no implementing partial method declaration, then you cannot have any code that attempts to create a delegate that refers to the partial method. Again, the reason is that the method doesn\u2019t exist at run time. The compiler produces this message: error CS0762: Cannot create delegate from method 'Base.OnNameChanging(string)' because it is a partial method without an implementing declaration.</p><p>\u25a0\u25a0 Partial methods are always considered to be private methods. However, the C# compiler for-</p><p>bids you from putting the private keyword before the partial method declaration.</p><h6>C H A P T E R 9</h6><h2>Parameters</h2><h6>In this chapter:</h6><p>Optional and Named Parameters. . . . . . . . . . . . . . . . . . . . . . . . . . 209 Implicitly Typed Local Variables . . . . . . . . . . . . . . . . . . . . . . . . . . . 212 Passing Parameters by Reference to a Method. . . . . . . . . . . . . . 214 Passing a Variable Number of Arguments to a Method . . . . . . 220 Parameter and Return Type Guidelines. . . . . . . . . . . . . . . . . . . . . 223 Const-ness. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 224</p><p>This chapter focuses on the various ways of passing parameters to a method, including how to optionally specify parameters, specify parameters by name, and pass parameters by reference, as well as how to define methods that accept a variable number of arguments.</p><h6>Optional and Named Parameters</h6><p>When designing a method\u2019s parameters, you can assign default values to some of or all the parameters. Then, code that calls these methods can optionally not specify some of the arguments, thereby accepting the default values. In addition, when you call a method, you can specify arguments by using the name of their parameters. Here is some code that demonstrates using both optional and named parameters.</p><p>public static class Program { private static Int32 s_n = 0;</p><p>private static void M(Int32 x = 9, String s = \"A\", DateTime dt = default(DateTime), Guid guid = new Guid()) {</p><p>Console.WriteLine(\"x={0}, s={1}, dt={2}, guid={3}\", x, s, dt, guid); }</p><p>public static void Main() {</p><ul style=\"list-style-type: none;\">\n<li><p>// 1. Same as: M(9, \"A\", default(DateTime), new Guid()); M();</p></li>\n<li><p>// 2. Same as: M(8, \"X\", default(DateTime), new Guid()); M(8, \"X\");</p></li>\n<li><p>// 3. Same as: M(5, \"A\", DateTime.Now, Guid.NewGuid()); M(5, guid: Guid.NewGuid(), dt: DateTime.Now);</p></li>\n</ul><p>209</p><p>} }</p><ul style=\"list-style-type: none;\">\n<li><p>// 4. Same as: M(0, \"1\", default(DateTime), new Guid()); M(s_n++, s_n++.ToString());</p></li>\n<li><p>// 5. Same as: String t1 = \"2\"; Int32 t2 = 3; // M(t2, t1, default(DateTime), new Guid()); M(s: (s_n++).ToString(), x: s_n++);</p></li>\n</ul><p>When I run this program, I get the following output.</p><p>x=9, s=A, dt=1/1/0001 12:00:00 AM, guid=00000000\u00ad0000\u00ad0000\u00ad0000\u00ad000000000000 x=8, s=X, dt=1/1/0001 12:00:00 AM, guid=00000000\u00ad0000\u00ad0000\u00ad0000\u00ad000000000000 x=5, s=A, dt=8/16/2012 10:14:25 PM, guid=d24a59da\u00ad6009\u00ad4aae\u00ad9295\u00ad839155811309 x=0, s=1, dt=1/1/0001 12:00:00 AM, guid=00000000\u00ad0000\u00ad0000\u00ad0000\u00ad000000000000 x=3, s=2, dt=1/1/0001 12:00:00 AM, guid=00000000\u00ad0000\u00ad0000\u00ad0000\u00ad000000000000</p><p>As you can see, whenever arguments are left out at the call site, the C# compiler embeds the parameter\u2019s default value. The third and fifth calls to M use C#\u2019s named parameter feature. In the two calls, I\u2019m explicitly passing a value for x and I\u2019m indicating that I want to pass an argument for the parameters named guid and dt.</p><p>When you pass arguments to a method, the compiler evaluates the arguments from left to right. In the fourth call to M, the value in s_n (0) is passed for x, then s_n is incremented, and s_n (1) is passed as a string for s and then s_n is incremented again to 2. When you pass arguments by using named parameters, the compiler still evaluates the arguments from left to right. In the fifth call to M, the value in s_n (2) is converted to a string and saved in a temporary variable (t1) that the compiler creates. Next, s_n is incremented to 3 and this value is saved in another temporary variable (t2) created by the compiler, and then s_n is incremented again to 4. Ultimately, M is invoked, passing it t2, t1, a default DateTime, and a new Guid.</p><h6>Rules and Guidelines</h6><p>There are some additional rules and guidelines that you should know about when defining a method that specifies default values for some of its parameters:</p><p>\u25a0\u25a0 You can specify default values for the parameters of methods, constructor methods, and parameterful properties (C# indexers). You can also specify default values for parameters that are part of a delegate definition. Then, when invoking a variable of this delegate type, you can omit the arguments and accept the default values.</p><p>\u25a0\u25a0 Parameters with default values must come after any parameters that do not have default values. That is, after you define a parameter as having a default value, then all parameters to the right of it must also have default values. For example, in the definition of my M method, I would get a compiler error if I removed the default value (\"A\") for s. There is one exception to this rule: a params array parameter (discussed later in this chapter) must come after all parameters (including those that have default values), and the array cannot have a default value itself.</p><p>\u25a0\u25a0 Default values must be constant values known at compile time. This means that you can set default values for parameters of types that C# considers to be primitive types, as shown in Table 5-1 in Chapter 5, \u201cPrimitive, Reference, and Value Types.\u201d This also includes enumerated types, and any reference type can be set to null. For a parameter of an arbitrary value type, you can set the default value to be an instance of the value type, with all its fields containing zeroes. You can use the default keyword or the new keyword to express this; both syntaxes produce identical Intermediate Language (IL) code. Examples of both syntaxes are used by my M method for setting the default value for the dt parameter and guid parameter, respectively.</p><p>\u25a0\u25a0 Be careful not to rename parameter variables because any callers who are passing arguments by parameter name will have to modify their code. For example, in the declaration of my M method, if I rename the dt variable to dateTime, then my third call to M in the earlier code will cause the compiler to produce the following message: error CS1739: The best overload for 'M' does not have a parameter named 'dt'.</p><p>\u25a0\u25a0 Be aware that changing a parameter\u2019s default value is potentially dangerous if the method is called from outside the module. A call site embeds the default value into its call. If you later change the parameter\u2019s default value and do not recompile the code containing the call site, then it will call your method passing the old default value. You might want to consider using a default value of 0/null as a sentinel to indicate default behavior; this allows you to change your default without having to recompile all the code with call sites. Here is an example.</p><p>// Don\u2019t do this: private static String MakePath(String filename = \"Untitled\") {</p><p>return String.Format(@\"C:\\{0}.txt\", filename); }</p><p>// Do this instead: private static String MakePath(String filename = null) {</p><p>// I am using the null\u00adcoalescing operator (??) here; see Chapter 19 return String.Format(@\"C:\\{0}.txt\", filename ?? \"Untitled\");</p><p>}</p><p>\u25a0\u25a0 You cannot set default values for parameters marked with either the ref or out keywords</p><p>because there is no way to pass a meaningful default value for these parameters.</p><p>There are some additional rules and guidelines that you should know about when calling a method by using optional or named parameters:</p><p>\u25a0\u25a0 Arguments can be passed in any order; however, named arguments must always appear at the</p><p>end of the argument list.</p><p>\u25a0\u25a0 You can pass arguments by name to parameters that do not have default values, but all required arguments must be passed (by position or by name) for the compiler to compile the code.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile82.png\"/><p>\u25a0\u25a0 C# doesn\u2019t allow you to omit arguments between commas, as in M(1, ,DateTime.Now), because this could lead to unreadable comma-counting code. Pass arguments by way of their parameter name if you want to omit some arguments for parameters with default values.</p><p>\u25a0\u25a0 To pass an argument by parameter name that requires ref/out, use syntax like the following.</p><p>// Method declaration: private static void M(ref Int32 x) { ... }</p><p>// Method invocation: Int32 a = 5; M(x: ref a);</p><p>Note C#\u2019s optional and named parameter features are really convenient when writing C# code that interoperates with the COM object model in Microsoft Office. And, when calling a COM component, C# also allows you to omit ref/out when passing an argument by reference to simplify the coding even more. When not calling a COM component, C# requires that the out/ref keyword be applied to the argument.</p><p>The DefaultParameterValue and Optional Attributes</p><p>It would be best if this concept of default and optional arguments was not C#-specific. Specifically, we want programmers to define a method indicating which parameters are optional and what their default value should be in a programming language and then give programmers working in other programming languages the ability to call them. For this to work, the compiler of choice must allow the caller to omit some arguments and have a way of determining what those arguments\u2019 default values should be.</p><p>In C#, when you give a parameter a default value, the compiler internally applies the System. Runtime.InteropServices.OptionalAttribute custom attribute to the parameter, and this attribute is persisted in the resulting file\u2019s metadata. In addition, the compiler applies System.Runtime.InteropServices.DefaultParameterValueAttribute to the parameter and persists this attribute in the resulting file\u2019s metadata. Then, DefaultParameterValueAttribute\u2019s constructor is passed the constant value that you specified in your source code.</p><p>Now, when a compiler sees that you have code calling a method that is missing some arguments, the compiler can ensure that you\u2019ve omitted optional arguments, grab their default values out of metadata, and embed the values in the call for you automatically.</p><h6>Implicitly Typed Local Variables</h6><p>C# supports the ability to infer the type of a method\u2019s local variable from the type of expression that is used to initialize it. The following shows some sample code demonstrating the use of this feature.</p><p>private static void ImplicitlyTypedLocalVariables() { var name = \"Jeff\"; ShowVariableType(name); // Displays: System.String</p><p>// var n = null; // Error: Cannot assign &lt;null&gt; to an implicitly\u00adtyped local</p><p>variable var x = (String)null; // OK, but not much value ShowVariableType(x); // Displays: System.String</p><p>var numbers = new Int32[] { 1, 2, 3, 4 }; ShowVariableType(numbers); // Displays: System.Int32[]</p><p>// Less typing for complex types var collection = new Dictionary&lt;String, Single&gt;() { { \"Grant\", 4.0f } };</p><p>// Displays: System.Collections.Generic.Dictionary`2[System.String,System.Single] ShowVariableType(collection);</p><p>foreach (var item in collection) { // Displays: System.Collections.Generic.KeyValuePair`2[System.String,System.Single] ShowVariableType(item);</p><p>} }</p><p>private static void ShowVariableType&lt;T&gt;(T t) {</p><p>Console.WriteLine(typeof(T)); }</p><p>The first line of code inside the ImplicitlyTypedLocalVariables method is introducing a new local variable by using the C# var token. To determine the type of the name variable, the compiler looks at the type of the expression on the right side of the assignment operator (=). Because \"Jeff\" is a string, the compiler infers that name\u2019s type must be String. To prove that the compiler is inferring the type correctly, I wrote the ShowVariableType method. This generic method infers the type of its argument, and then it shows the type that it inferred on the console. I added what ShowVariableType displayed as comments inside the ImplicitlyTypedLocalVariables method for easy reading.</p><p>The second assignment (commented out) inside the ImplicitlyTypedLocalVariables method would produce a compiler error (error CS0815: Cannot assign &lt;null&gt; to an implicitlytyped local variable) because null is implicitly castable to any reference type or nullable value type; therefore, the compiler cannot infer a distinct type for it. However, on the third assignment, I show that it is possible to initialize an implicitly typed local variable with null if you explicitly specify a type (String, in my example). Although this is possible, it is not that useful because you could also write String x = null; to get the same result.</p><p>In the fourth assignment, you see some real value of using C#\u2019s implicitly typed local variable feature. Without this feature, you\u2019d have to specify Dictionary&lt;String, Single&gt; on both sides of the assignment operator. Not only is this a lot of typing, but if you ever decide to change the collection type or any of the generic parameter types, then you would have to modify your code on both sides of the assignment operator, too.</p><p>In the foreach loop, I also use var to have the compiler automatically infer the type of the elements inside the collection. This demonstrates that it is possible and quite useful to use var with foreach, using, and for statements. It can also be useful when experimenting with code. For example, you initialize an implicitly typed local variable from the return type of a method, and as you develop your method, you might decide to change its return type. If you do this, the compiler will automatically figure out that the return type has changed and automatically change the type of the variable! This is great, but of course, other code in the method that uses that variable may no longer compile if the code accesses members by using the variable assuming that it was the old type.</p><p>In Microsoft Visual Studio, you can hold the mouse cursor over var in your source code and the editor will display a tooltip showing you the type that the compiler infers from the expression. C#\u2019s implicitly typed local variable feature must be used when working with anonymous types within a method; see Chapter 10, \u201cProperties,\u201d for more details.</p><p>You cannot declare a method\u2019s parameter type by using var. The reason for this should be obvious to you because the compiler would have to infer the parameter\u2019s type from the argument being passed at a callsite and there could be no call sites or many call sites. In addition, you cannot declare a type\u2019s field by using var. There are many reasons why C# has this restriction. One reason is that fields can be accessed by several methods and the C# team feels that this contract (the type of the variable) should be stated explicitly. Another reason is that allowing this would permit an anonymous type (discussed in Chapter 10) to leak outside of a single method.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile83.png\"/><p>Important Do not confuse dynamic and var. Declaring a local variable by using var is just a syntactical shortcut that has the compiler infer the specific data type from an expression. The var keyword can be used only for declaring local variables inside a method, whereas the dynamic keyword can be used for local variables, fields, and arguments. You cannot cast an expression to var, but you can cast an expression to dynamic. You must explicitly initialize a variable declared using var, whereas you do not have to initialize a variable declared with dynamic. For more information about C#\u2019s dynamic type, see the \u201cThe dynamic Primitive Type\u201d section in Chapter 5.</p><h6>Passing Parameters by Reference to a Method</h6><p>By default, the common language runtime (CLR) assumes that all method parameters are passed by value. When reference type objects are passed, the reference (or pointer) to the object is passed (by value) to the method. This means that the method can modify the object and the caller will see the change. For value type instances, a copy of the instance is passed to the method. This means that the method gets its own private copy of the value type and the instance in the caller isn\u2019t affected.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile84.png\"/><p>Important In a method, you must know whether each parameter passed is a reference type or a value type because the code you write to manipulate the parameter could be markedly different.</p><p>The CLR allows you to pass parameters by reference instead of by value. In C#, you do this by using the out and ref keywords. Both keywords tell the C# compiler to emit metadata indicating that this designated parameter is passed by reference, and the compiler uses this to generate code to pass the address of the parameter rather than the parameter itself.</p><p>From the CLR\u2019s perspective, out and ref are identical\u2014that is, the same IL is produced regardless of which keyword you use, and the metadata is also identical except for 1 bit, which is used to record whether you specified out or ref when declaring the method. However, the C# compiler treats the two keywords differently, and the difference has to do with which method is responsible for initializing the object being referred to. If a method\u2019s parameter is marked with out, the caller isn\u2019t expected to have initialized the object prior to calling the method. The called method can\u2019t read from the value, and the called method must write to the value before returning. If a method\u2019s parameter is marked with ref, the caller must initialize the parameter\u2019s value prior to calling the method. The called method can read from the value and/or write to the value.</p><p>Reference and value types behave very differently with out and ref. Let\u2019s look at using out and ref with value types first.</p><p>public sealed class Program {</p><p>public static void Main() { Int32 x; // x is uninitialized. GetVal(out x); // x doesn\u2019t have to be initialized. Console.WriteLine(x); // Displays \"10\"</p><p>}</p><p>}</p><p>private static void GetVal(out Int32 v) {</p><p>v = 10; // This method must initialize v. }</p><p>In this code, x is declared in Main\u2019s stack frame. The address of x is then passed to GetVal. GetVal\u2019s v is a pointer to the Int32 value in Main\u2019s stack frame. Inside GetVal, the Int32 that v points to is changed to 10. When GetVal returns, Main\u2019s x has a value of 10, and 10 is displayed on the console. Using out with large value types is efficient because it prevents instances of the value type\u2019s fields from being copied when making method calls.</p><p>Now let\u2019s look at an example that uses ref instead of out.</p><p>public sealed class Program {</p><p>public static void Main() { Int32 x = 5; // x is initialized. AddVal(ref x); // x must be initialized. Console.WriteLine(x); // Displays \"15\"</p><p>}</p><p>}</p><p>private static void AddVal(ref Int32 v) {</p><p>v += 10; // This method can use the initialized value in v. }</p><p>In this code, x is also declared in Main\u2019s stack frame and is initialized to 5. The address of x is then passed to AddVal. AddVal\u2019s v is a pointer to the Int32 value in Main\u2019s stack frame. Inside AddVal, the Int32 that v points to is required to have a value already. So, AddVal can use the initial value in any expression it desires. AddVal can also change the value, and the new value will be \u201creturned\u201d to the caller. In this example, AddVal adds 10 to the initial value. When AddVal returns, Main\u2019s x will contain 15, which is what gets displayed in the console.</p><p>To summarize, from an IL or a CLR perspective, out and ref do exactly the same thing: they both cause a pointer to the instance to be passed. The difference is that the compiler helps ensure that your code is correct. The following code that attempts to pass an uninitialized value to a method expecting a ref parameter produces the following message: error CS0165: Use of unassigned local variable 'x'.</p><p>public sealed class Program { public static void Main() { Int32 x; // x is not initialized.</p><p>// The following line fails to compile, producing // error CS0165: Use of unassigned local variable 'x'. AddVal(ref x);</p><p>Console.WriteLine(x); }</p><p>}</p><p>private static void AddVal(ref Int32 v) {</p><p>v += 10; // This method can use the initialized value in v. }</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile85.png\"/><p>Important I\u2019m frequently asked why C# requires that a call to a method must specify out or ref. After all, the compiler knows whether the method being called requires out or ref and should be able to compile the code correctly. It turns out that the compiler can indeed do the right thing automatically. However, the designers of the C# language felt that the caller should explicitly state its intention. This way, at the call site, it\u2019s obvious that the method being called is expected to change the value of the variable being passed.</p><p>In addition, the CLR allows you to overload methods based on their use of out and ref parameters. For example, in C#, the following code is legal and compiles just fine.</p><p>public sealed class Point { static void Add(Point p) { ... } static void Add(ref Point p) { ... }</p><p>}</p><p>It\u2019s not legal to overload methods that differ only by out and ref because the metadata representation of the method\u2019s signature for the methods would be identical. So I couldn\u2019t also define the following method in the preceding Point type.</p><p>static void Add(out Point p) { ... }</p><p>If you attempt to include the last Add method in the Point type, the C# compiler issues this message: error CS0663: 'Add' cannot define overloaded methods because it differs only on ref and out.</p><p>Using out and ref with value types gives you the same behavior that you already get when passing reference types by value. With value types, out and ref allow a method to manipulate a single value type instance. The caller must allocate the memory for the instance, and the callee manipulates that memory. With reference types, the caller allocates memory for a pointer to a reference object, and the callee manipulates this pointer. Because of this behavior, using out and ref with reference types is useful only when the method is going to \u201creturn\u201d a reference to an object that it knows about. The following code demonstrates.</p><p>using System; using System.IO;</p><p>public sealed class Program { public static void Main() { FileStream fs; // fs is uninitialized</p><p>// Open the first file to be processed. StartProcessingFiles(out fs);</p><p>// Continue while there are more files to process. for (; fs != null; ContinueProcessingFiles(ref fs)) {</p><p>} }</p><p>// Process a file. fs.Read(...);</p><p>private static void StartProcessingFiles(out FileStream fs) {</p><p>fs = new FileStream(...); // fs must be initialized in this method }</p><p>private static void ContinueProcessingFiles(ref FileStream fs) { fs.Close(); // Close the last file worked on.</p><p>} }</p><p>// Open the next file, or if no more files, \"return\" null. if (noMoreFilesToProcess) fs = null; else fs = new FileStream (...);</p><p>As you can see, the big difference with this code is that the methods that have out or ref reference type parameters are constructing an object, and the pointer to the new object is returned to the caller. You\u2019ll also notice that the ContinueProcessingFiles method can manipulate the object being passed into it before returning a new object. This is possible because the parameter is marked with the ref keyword. You can simplify the preceding code a bit, as shown here.</p><p>using System; using System.IO;</p><p>public sealed class Program { public static void Main() { FileStream fs = null; // Initialized to null (required)</p><p>// Open the first file to be processed. ProcessFiles(ref fs);</p><p>// Continue while there are more files to process. for (; fs != null; ProcessFiles(ref fs)) {</p><p>} }</p><p>// Process a file. fs.Read(...);</p><p>private static void ProcessFiles(ref FileStream fs) { // Close the previous file if one was open. if (fs != null) fs.Close(); // Close the last file worked on.</p><p>} }</p><p>// Open the next file, or if no more files, \"return\" null. if (noMoreFilesToProcess) fs = null; else fs = new FileStream (...);</p><p>Here\u2019s another example that demonstrates how to use the ref keyword to implement a method that swaps two reference types.</p><p>public static void Swap(ref Object a, ref Object b) { Object t = b; b = a; a = t;</p><p>}</p><p>To swap references to two String objects, you\u2019d probably think that you could write code like the following.</p><p>public static void SomeMethod() {</p><ul style=\"list-style-type: none;\">\n<li><p>String s1 = \"Jeffrey\";</p></li>\n<li><p>String s2 = \"Richter\";</p></li>\n</ul><p>}</p><p>Swap(ref s1, ref s2);</p><ul style=\"list-style-type: none;\">\n<li><p>Console.WriteLine(s1); // Displays \"Richter\"</p></li>\n<li><p>Console.WriteLine(s2); // Displays \"Jeffrey\"</p></li>\n</ul><p>However, this code won\u2019t compile. The problem is that variables passed by reference to a method must be of the same type as declared in the method signature. In other words, Swap expects two Object references, not two String references. To swap the two String references, you must do the following.</p><p>public static void SomeMethod() {</p><ul style=\"list-style-type: none;\">\n<li><p>String s1 = \"Jeffrey\";</p></li>\n<li><p>String s2 = \"Richter\";</p></li>\n</ul><p>// Variables that are passed by reference // must match what the method expects. Object o1 = s1, o2 = s2; Swap(ref o1, ref o2);</p><p>// Now cast the objects back to strings. s1 = (String) o1; s2 = (String) o2;</p><p>}</p><ul style=\"list-style-type: none;\">\n<li><p>Console.WriteLine(s1); // Displays \"Richter\"</p></li>\n<li><p>Console.WriteLine(s2); // Displays \"Jeffrey\"</p></li>\n</ul><p>This version of SomeMethod does compile and execute as expected. The reason why the parameters passed must match the parameters expected by the method is to ensure that type safety is preserved. The following code, which thankfully won\u2019t compile, shows how type safety could be compromised.</p><p>internal sealed class SomeType {</p><p>public Int32 m_val; }</p><p>public sealed class Program { public static void Main() { SomeType st;</p><p>// The following line generates error CS1503: Argument '1': // cannot convert from 'ref SomeType' to 'ref object'. GetAnObject(out st);</p><p>Console.WriteLine(st.m_val); }</p><p>}</p><p>private static void GetAnObject(out Object o) {</p><p>o = new String('X', 100); }</p><p>In this code, Main clearly expects GetAnObject to return a SomeType object. However, because GetAnObject\u2019s signature indicates a reference to an Object, GetAnObject is free to initialize o to an object of any type. In this example, when GetAnObject returned to Main, st would refer to a String, which is clearly not a SomeType object, and the call to Console.WriteLine would certainly fail. Fortunately, the C# compiler won\u2019t compile the preceding code because st is a reference to SomeType, but GetAnObject requires a reference to an Object.</p><p>You can use generics to fix these methods so that they work as you\u2019d expect. Here is how to fix the Swap method shown earlier.</p><p>public static void Swap&lt;T&gt;(ref T a, ref T b) { T t = b; b = a; a = t;</p><p>}</p><p>And now, with Swap rewritten as above, the following code (identical to that shown before) will compile and run perfectly.</p><p>public static void SomeMethod() {</p><p>String s1 = \"Jeffrey\"; String s2 = \"Richter\";</p><p>}</p><p>Swap(ref s1, ref s2); Console.WriteLine(s1); // Displays \"Richter\" Console.WriteLine(s2); // Displays \"Jeffrey\"</p><p>For some other examples that use generics to solve this problem, see System.Threading\u2019s Interlocked class with its CompareExchange and Exchange methods.</p><h6>Passing a Variable Number of Arguments to a Method</h6><p>It\u2019s sometimes convenient for the developer to define a method that can accept a variable number of arguments. For example, the System.String type offers methods allowing an arbitrary number of strings to be concatenated together and methods allowing the caller to specify a set of strings that are to be formatted together.</p><p>To declare a method that accepts a variable number of arguments, you declare the method as follows.</p><p>static Int32 Add(params Int32[] values) { // NOTE: it is possible to pass the 'values' // array to other methods if you want to.</p><p>}</p><p>Int32 sum = 0; if (values != null) {</p><p>for (Int32 x = 0; x &lt; values.Length; x++) sum += values[x];</p><p>} return sum;</p><p>Everything in this method should look very familiar to you except for the params keyword that is applied to the last parameter of the method signature. Ignoring the params keyword for the moment, it\u2019s obvious that this method accepts an array of Int32 values and iterates over the array, adding up all of the values. The resulting sum is returned to the caller.</p><p>Obviously, code can call this method as follows.</p><p>public static void Main() { // Displays \"15\" Console.WriteLine(Add(new Int32[] { 1, 2, 3, 4, 5 } ));</p><p>}</p><p>It\u2019s clear that the array can easily be initialized with an arbitrary number of elements and then</p><p>passed off to Add for processing. Although the preceding code would compile and work correctly, it is a little ugly. As developers, we would certainly prefer to have written the call to Add as follows.</p><p>public static void Main() { // Displays \"15\" Console.WriteLine(Add(1, 2, 3, 4, 5));</p><p>}</p><p>You\u2019ll be happy to know that we can do this because of the params keyword. The params keyword tells the compiler to apply an instance of the System.ParamArrayAttribute custom attribute to the parameter.</p><p>When the C# compiler detects a call to a method, the compiler checks all of the methods with the specified name, where no parameter has the ParamArray attribute applied. If a method exists that can accept the call, the compiler generates the code necessary to call the method. However, if the compiler can\u2019t find a match, it looks for methods that have a ParamArray attribute to see whether the call can be satisfied. If the compiler finds a match, it emits code that constructs an array and populates its elements before emitting the code that calls the selected method.</p><p>In the previous example, no Add method is defined that takes five Int32-compatible arguments; however, the compiler sees that the source code has a call to Add that is being passed a list of Int32 values and that there is an Add method whose array-of-Int32 parameter is marked with the ParamArray attribute. So the compiler considers this a match and generates code that coerces the parameters into an Int32 array and then calls the Add method. The end result is that you can write the code, easily passing a bunch of parameters to Add, but the compiler generates code as though you\u2019d written the first version that explicitly constructs and initializes the array.</p><p>Only the last parameter to a method can be marked with the params keyword (ParamArrayAttribute). This parameter must also identify a single-dimension array of any type. It\u2019s legal to pass null or a reference to an array of 0 entries as the last parameter to the method. The following call to Add compiles fine, runs fine, and produces a resulting sum of 0 (as expected).</p><p>public static void Main() { // Both of these lines display \"0\" Console.WriteLine(Add()); // passes new Int32[0] to Add Console.WriteLine(Add(null)); // passes null to Add: more efficient (no array allocated)</p><p>}</p><p>So far, all of the examples have shown how to write a method that takes an arbitrary number of Int32 parameters. How would you write a method that takes an arbitrary number of parameters where the parameters could be any type? The answer is very simple: just modify the method\u2019s prototype so that it takes an Object[] instead of an Int32[]. Here\u2019s a method that displays the Type of every object passed to it.</p><p>public sealed class Program { public static void Main() {</p><p>DisplayTypes(new Object(), new Random(), \"Jeff\", 5); }</p><p>private static void DisplayTypes(params Object[] objects) { if (objects != null) { foreach (Object o in objects)</p><p>Console.WriteLine(o.GetType()); }</p><p>} }</p><p>Running this code yields the following output.</p><p>System.Object System.Random System.String System.Int32</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile86.png\"/><p>Important Be aware that calling a method that takes a variable number of arguments incurs an additional performance hit unless you explicitly pass null. After all, an array object must be allocated on the heap, the array\u2019s elements must be initialized, and the array\u2019s memory must ultimately be garbage collected. To help reduce the performance hit associated with this, you may want to consider defining a few overloaded methods that do not use the params keyword. For some examples, look at the System.String class\u2019s Concat method, which has the following overloads.</p><p>public sealed class String : Object, ... { public static string Concat(object arg0); public static string Concat(object arg0, object arg1); public static string Concat(object arg0, object arg1, object arg2); public static string Concat(params object[] args);</p><p>}</p><p>public static string Concat(string str0, string str1); public static string Concat(string str0, string str1, string str2); public static string Concat(string str0, string str1, string str2, string str3); public static string Concat(params string[] values);</p><p>As you can see, the Concat method defines several overloads that do not use the params keyword. These versions of the Concat method are the most frequently called overloads, and these overloads exist in order to improve performance for the most common scenarios. The overloads that use the params keyword are there for the less common scenarios; these scenarios will suffer a performance hit, but fortunately, they are rare.</p><h6>Parameter and Return Type Guidelines</h6><p>When declaring a method\u2019s parameter types, you should specify the weakest type possible, preferring interfaces over base classes. For example, if you are writing a method that manipulates a collection of items, it would be best to declare the method\u2019s parameter by using an interface such as IEnumerable&lt;T&gt; rather than using a strong data type such as List&lt;T&gt; or even a stronger interface type such as ICollection&lt;T&gt; or IList&lt;T&gt;.</p><p>// Desired: This method uses a weak parameter type public void ManipulateItems&lt;T&gt;(IEnumerable&lt;T&gt; collection) { ... }</p><p>// Undesired: This method uses a strong parameter type public void ManipulateItems&lt;T&gt;(List&lt;T&gt; collection) { ... }</p><p>The reason, of course, is that someone can call the first method passing in an array object, a List&lt;T&gt; object, a String object, and so on\u2014any object whose type implements IEnumerable&lt;T&gt;. The second method allows only List&lt;T&gt; objects to be passed in; it will not accept an array or a String object. Obviously, the first method is better because it is much more flexible and can be used in a much wider range of scenarios.</p><p>Naturally, if you are writing a method that requires a list (not just any enumerable object), then you should declare the parameter type as an IList&lt;T&gt;. You should still avoid declaring the parameter type as List&lt;T&gt;. Using IList&lt;T&gt; allows the caller to pass arrays and any other objects whose type implements IList&lt;T&gt;.</p><p>Note that my examples talked about collections, which are designed using an interface architecture. If we were talking about classes designed using a base class architecture, the concept still applies. So, for example, if I were implementing a method that processed bytes from a stream, we\u2019d have the following.</p><p>// Desired: This method uses a weak parameter type public void ProcessBytes(Stream someStream) { ... }</p><p>// Undesired: This method uses a strong parameter type public void ProcessBytes(FileStream fileStream) { ... }</p><p>The first method can process bytes from any kind of stream: a FileStream, a NetworkStream, a MemoryStream, and so on. The second method can operate only on a FileStream, making it far more limited.</p><p>On the flip side, it is usually best to declare a method\u2019s return type by using the strongest type possible (trying not to commit yourself to a specific type). For example, it is better to declare a method that returns a FileStream object as opposed to returning a Stream object.</p><p>// Desired: This method uses a strong return type public FileStream OpenFile() { ... }</p><p>// Undesired: This method uses a weak return type public Stream OpenFile() { ... }</p><p>Here, the first method is preferred because it allows the method\u2019s caller the option of treating the returned object as either a FileStream object or as a Stream object. Meanwhile, the second method requires that the caller treat the returned object as a Stream object. Basically, it is best to let the caller have as much flexibility as possible when calling a method, allowing the method to be used in the widest range of scenarios.</p><p>Sometimes you want to retain the ability to change the internal implementation of a method without affecting the callers. In the example just shown, the OpenFile method is unlikely to ever change its internal implementation to return anything other than a FileStream object (or an object whose type is derived from FileStream). However, if you have a method that returns a List&lt;String&gt; object, you might very well want to change the internal implementation of this method in the future so that it would instead return a String[]. In the cases in which you want to leave yourself some flexibility to change what your method returns, choose a weaker return type. The following is an example.</p><p>// Flexible: This method uses a weaker return type public IList&lt;String&gt; GetStringCollection() { ... }</p><p>// Inflexible: This method uses a stronger return type public List&lt;String&gt; GetStringCollection() { ... }</p><p>In this example, even though the GetStringCollection method uses a List&lt;String&gt; object internally and returns it, it is better to prototype the method as returning an IList&lt;String&gt; instead. In the future, the GetStringCollection method could change its internal collection to use a String[], and callers of the method won\u2019t be required to change any of their source code. In fact, they won\u2019t even have to recompile their code. Notice in this example that I\u2019m using the strongest of the weakest types. For instance, I\u2019m not using an IEnumerable&lt;String&gt; or even ICollection&lt;String&gt;.</p><h6>Const-ness</h6><p>In some languages, such as unmanaged C++, it is possible to declare methods or parameters as a constant that forbids the code in an instance method from changing any of the object\u2019s fields or prevents the code from modifying any of the objects passed into the method. The CLR does not provide for this, and many programmers have been lamenting this missing feature. Because the CLR doesn\u2019t offer this feature, no language (including C#) can offer this feature.</p><p>First, you should note that in unmanaged C++, marking an instance method or parameter as const ensured only that the programmer could not write normal code that would modify the object or parameter. Inside the method, it was always possible to write code that could mutate the object/ parameter by either casting away the const-ness or by getting the address of the object/argument and then writing to the address. In a sense, unmanaged C++ lied to programmers, making them believe that their constant objects/arguments couldn\u2019t be written to even though they could.</p><p>When designing a type\u2019s implementation, the developer can just avoid writing code that manipulates the object/arguments. For example, strings are immutable because the String class doesn\u2019t offer any methods that can change a string object.</p><p>Also, it would be very difficult for Microsoft to endow the CLR with the ability to verify that a constant object/argument isn\u2019t being mutated. The CLR would have to verify at each write that the write was not occurring to a constant object, and this would hurt performance significantly. Of course, a detected violation would result in the CLR throwing an exception. Furthermore, constant support adds a lot of complexity for developers. For example, if a type is immutable, all derived types would have to respect this. In addition, an immutable type would probably have to consist of fields that are also of immutable types.</p><p>These are just some of the reasons why the CLR does not support constant objects/arguments.</p><h6>C H A P T E R 1 0</h6><h2>Properties</h2><h6>In this chapter:</h6><p>Parameterless Properties . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 227 Parameterful Properties . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 242 The Performance of Calling Property Accessor Methods. . . . . 247 Property Accessor Accessibility. . . . . . . . . . . . . . . . . . . . . . . . . . . . 248 Generic Property Accessor Methods . . . . . . . . . . . . . . . . . . . . . . . 248</p><p>In this chapter, I\u2019ll talk about properties. Properties allow source code to call a method by using a simplified syntax. The common language runtime (CLR) offers two kinds of properties: parameterless properties, which are simply called properties, and parameterful properties, which are called different names by different programming languages. For example, C# calls parameterful properties indexers, and Microsoft Visual Basic calls them default properties. I\u2019ll also talk about initializing properties by using object and collection initializers, in addition to ways to package a bunch of properties together by using C#\u2019s anonymous types and the System.Tuple type.</p><h6>Parameterless Properties</h6><p>Many types define state information that can be retrieved or altered. Frequently, this state information is implemented as field members of the type. For example, here\u2019s a type definition that contains two fields.</p><p>public sealed class Employee { public String Name; // The employee's name public Int32 Age; // The employee's age</p><p>}</p><p>If you were to create an instance of this type, you could easily get or set any of this state information with code similar to the following.</p><p>Employee e = new Employee(); e.Name = \"Jeffrey Richter\"; // Set the employee's Name. e.Age = 48; // Set the employee's Age.</p><p>Console.WriteLine(e.Name); // Displays \"Jeffrey Richter\"</p><p>227</p><p>Querying and setting an object\u2019s state information in the way I just demonstrated is very common. However, I would argue that the preceding code should never be implemented as shown. One of the hallmarks of object-oriented design and programming is data encapsulation. Data encapsulation means that your type\u2019s fields should never be publicly exposed because it\u2019s too easy to write code that improperly uses the fields, corrupting the object\u2019s state. For example, a developer could easily corrupt an Employee object with code like the following.</p><p>e.Age = \u00ad5; // How could someone be \u20135 years old?</p><p>There are additional reasons for encapsulating access to a type\u2019s data field. For example, you might want access to a field to execute some side effect, cache some value, or lazily create some internal object. You might also want access to the field to be thread-safe. Or perhaps the field is a logical field whose value isn\u2019t represented by bytes in memory but whose value is instead calculated using some algorithm.</p><p>For any of these reasons, when designing a type, I strongly suggest that all of your fields be private. Then, to allow a user of your type to get or set state information, you expose methods for that specific purpose. Methods that wrap access to a field are typically called accessor methods. These accessor methods can optionally perform sanity checking and ensure that the object\u2019s state is never corrupted. For example, I\u2019d rewrite the previous class as follows.</p><p>public sealed class Employee {</p><p>private String m_Name; // Field is now private private Int32 m_Age; // Field is now private</p><p>public String GetName() {</p><p>return(m_Name); }</p><p>public void SetName(String value) {</p><p>m_Name = value; }</p><p>public Int32 GetAge() {</p><p>return(m_Age); }</p><p>public void SetAge(Int32 value) { if (value &lt; 0) throw new ArgumentOutOfRangeException(\"value\", value.ToString(),</p><p>\"The value must be greater than or equal to 0\"); m_Age = value;</p><p>} }</p><p>Although this is a simple example, you should still be able to see the enormous benefit you get from encapsulating the data fields. You should also be able to see how easy it is to make read-only or write-only properties: just don\u2019t implement one of the accessor methods. Alternatively, you could allow only derived types to modify the value by marking the SetXxx method as protected.</p><p>Encapsulating the data as shown earlier has two disadvantages. First, you have to write more code because you now have to implement additional methods. Second, users of the type must now call methods rather than simply refer to a single field name.</p><p>e.SetName(\"Jeffrey Richter\"); // Updates the employee's name String EmployeeName = e.GetName(); // Retrieves the employee's name e.SetAge(48); // Updates the employee's age e.SetAge(\u00ad5); // Throws ArgumentOutOfRangeException Int32 EmployeeAge = e.GetAge(); // Retrieves the employee's age</p><p>Personally, I think these disadvantages are quite minor. Nevertheless, programming languages and the CLR offer a mechanism called properties that alleviates the first disadvantage a little and removes the second disadvantage entirely.</p><p>The class shown here uses properties and is functionally identical to the class shown earlier.</p><p>public sealed class Employee { private String m_Name; private Int32 m_Age;</p><p>public String Name { get { return(m_Name); } set { m_Name = value; } // The 'value' keyword always identifies the new value.</p><p>}</p><p>}</p><p>public Int32 Age { get { return(m_Age); } set {</p><p>if (value &lt; 0) // The 'value' keyword always identifies the new value. throw new ArgumentOutOfRangeException(\"value\", value.ToString(),</p><p>\"The value must be greater than or equal to 0\"); m_Age = value;</p><p>} }</p><p>As you can see, properties complicate the definition of the type slightly, but the fact that they allow you to write your code as follows more than compensates for the extra work.</p><p>e.Name = \"Jeffrey Richter\"; // \"Sets\" the employee name String EmployeeName = e.Name; // \"Gets\" the employee's name e.Age = 48; // \"Sets\" the employee's age e.Age = \u00ad5; // Throws ArgumentOutOfRangeException Int32 EmployeeAge = e.Age; // \"Gets\" the employee's age</p><p>You can think of properties as smart fields: fields with additional logic behind them. The CLR</p><p>supports static, instance, abstract, and virtual properties. In addition, properties can be marked with any accessibility modifier (discussed in Chapter 6, \u201cType and Member Basics\u201d) and defined within an interface (discussed in Chapter 13, \u201cInterfaces\u201d).</p><p>Each property has a name and a type (which can\u2019t be void). It isn\u2019t possible to overload properties</p><p>(that is, have two properties with the same name if their types are different). When you define a property, you typically specify both a get and a set method. However, you can leave out the set method to define a read-only property or leave out the get method to define a write-only property.</p><p>It\u2019s also quite common for the property\u2019s get/set methods to manipulate a private field defined within the type. This field is commonly referred to as the backing field. The get and set methods don\u2019t have to access a backing field, however. For example, the System.Threading.Thread type offers a Priority property that communicates directly with the operating system; the Thread object doesn\u2019t maintain a field for a thread\u2019s priority. Another example of properties without backing fields are those read-only properties calculated at run time\u2014for example, the length of a zero-terminated array or the area of a rectangle when you have its height and width.</p><p>When you define a property, depending on its definition, the compiler will emit either two or three of the following items into the resulting managed assembly:</p><p>\u25a0\u25a0 A method representing the property\u2019s get accessor method. This is emitted only if you define</p><p>a get accessor method for the property.</p><p>\u25a0\u25a0 A method representing the property\u2019s set accessor method. This is emitted only if you define</p><p>a set accessor method for the property.</p><p>\u25a0\u25a0 A property definition in the managed assembly\u2019s metadata. This is always emitted. Refer back to the Employee type shown earlier. As the compiler compiles this type, it comes across</p><p>the Name and Age properties. Because both properties have get and set accessor methods, the compiler emits four method definitions into the Employee type. It\u2019s as though the original source were written as follows.</p><p>public sealed class Employee { private String m_Name; private Int32 m_Age;</p><p>public String get_Name(){ return m_Name;</p><p>} public void set_Name(String value) {</p><p>m_Name = value; // The argument 'value' always identifies the new value. }</p><p>public Int32 get_Age() {</p><p>return m_Age; }</p><p>public void set_Age(Int32 value) {</p><p>if (value &lt; 0) // The 'value' always identifies the new value. throw new ArgumentOutOfRangeException(\"value\", value.ToString(),</p><p>\"The value must be greater than or equal to 0\"); m_Age = value;</p><p>} }</p><p>The compiler automatically generates names for these methods by prepending get_ or set_ to the property name specified by the developer.</p><p>C# has built-in support for properties. When the C# compiler sees code that\u2019s trying to get or set a property, the compiler actually emits a call to one of these methods. If you\u2019re using a programming language that doesn\u2019t directly support properties, you can still access properties by calling the desired accessor method. The effect is exactly the same; it\u2019s just that the source code doesn\u2019t look as pretty.</p><p>In addition to emitting the accessor methods, compilers also emit a property definition entry into the managed assembly\u2019s metadata for each property defined in the source code. This entry contains some flags and the type of the property, and it refers to the get and set accessor methods. This information exists simply to draw an association between the abstract concept of a \u201cproperty\u201d and its accessor methods. Compilers and other tools can use this metadata, which can be obtained by using the System.Reflection.PropertyInfo class. The CLR doesn\u2019t use this metadata information and requires only the accessor methods at run time.</p><h6>Automatically Implemented Properties</h6><p>If you are creating a property to simply encapsulate a backing field, then C# offers a simplified syntax known as automatically implemented properties (AIPs), as shown here for the Name property.</p><p>public sealed class Employee { // This property is an automatically implemented property public String Name { get; set; }</p><p>private Int32 m_Age;</p><p>}</p><p>public Int32 Age { get { return(m_Age); } set {</p><p>if (value &lt; 0) // The 'value' keyword always identifies the new value. throw new ArgumentOutOfRangeException(\"value\", value.ToString(),</p><p>\"The value must be greater than or equal to 0\"); m_Age = value;</p><p>} }</p><p>When you declare a property and do not provide an implementation for the get/set methods, then the C# compiler will automatically declare for you a private field. In this example, the field will be of type String, the type of the property. And, the compiler will automatically implement the get_Name and set_Name methods for you to return the value in the field and to set the field\u2019s value, respectively.</p><p>You might wonder what the value of doing this is, as opposed to just declaring a public String field called Name. Well, there is a big difference. Using the AIP syntax means that you have created a property. Any code that accesses this property is actually calling get and set methods. If you decide later to implement the get and/or set method yourself instead of accepting the compiler\u2019s default</p><p>implementation, then any code that accesses the property will not have to be recompiled. However, if you declared Name as a field and then you later change it to a property, then all code that accessed the field will have to be recompiled so that it now accesses the property methods.</p><p>\u25a0\u25a0 Personally, I do not like the compiler\u2019s AIP feature, so I usually avoid it for the following reason: The syntax for a field declaration can include initialization so that you are declaring and initializing the field in one line of code. However, there is no convenient syntax to set an AIP to an initial value. Therefore, you must explicitly initialize each AIP in each constructor method.</p><p>\u25a0\u25a0 The runtime serialization engines persist the name of the field in a serialized stream. The name of the backing field for an AIP is determined by the compiler, and it could actually change the name of this backing field every time you recompile your code, negating the ability to deserialize instances of any types that contain an AIP. Do not use the AIP feature with any type you intend to serialize or deserialize.</p><p>\u25a0\u25a0 When debugging, you cannot put a breakpoint on an AIP get or set method, so you cannot easily detect when an application is getting or setting this property. You can set breakpoints on manually implemented properties, which can be quite handy when tracking down bugs.</p><p>You should also know that when you use AIPs, the property must be readable and writable; that is, the compiler must produce both get and set methods. This makes sense because a write-only field is not useful without the ability to read its value; likewise, a read-only field would always have its default value. In addition, because you do not know the name of the compiler-generated backing field, your code must always access the property by using the property name. And, if you decide you want to explicitly implement one of the accessor methods, then you must explicitly implement both accessor methods and you are not using the AIP feature anymore. For a single property, the AIP feature is an all-or-nothing deal.</p><h6>Defining Properties Intelligently</h6><p>Personally, I don\u2019t like properties and I wish that they were not supported in the Microsoft .NET Framework and its programming languages. The reason is that properties look like fields, but they are methods. This has been known to cause a phenomenal amount of confusion. When a programmer sees code that appears to be accessing a field, there are many assumptions that the programmer makes that may not be true for a property. For example:</p><p>\u25a0\u25a0 A property may be read-only or write-only; field access is always readable and writable. If you</p><p>define a property, it is best to offer both get and set accessor methods.</p><p>\u25a0\u25a0 A property method may throw an exception; field access never throws an exception.</p><p>\u25a0\u25a0 A property cannot be passed as an out or ref parameter to a method; a field can. For ex-</p><p>ample, the following code will not compile.</p><p>using System;</p><p>public sealed class SomeType { private static String Name {</p><p>}</p><p>get { return null; } set {}</p><p>static void MethodWithOutParam(out String n) { n = null; }</p><p>public static void Main() { // For the line of code below, the C# compiler emits the following: // error CS0206: A property, indexer or dynamic member access may not // be passed as an out or ref parameter MethodWithOutParam(out Name);</p><p>} }</p><p>\u25a0\u25a0 A property method can take a long time to execute; field access always completes immediately. A common reason to use properties is to perform thread synchronization, which can stop the thread forever, and therefore, a property should not be used if thread synchronization is required. In that situation, a method is preferred. Also, if your class can be accessed remotely (for example, your class is derived from System.MarshalByRefObject), calling the property method will be very slow, and therefore, a method is preferred to a property. In my opinion, classes derived from MarshalByRefObject should never use properties.</p><p>\u25a0\u25a0 If called multiple times in a row, a property method may return a different value each time; a field returns the same value each time. The System.DateTime class has a read-only Now property that returns the current date and time. Each time you query this property, it will return a different value. This is a mistake, and Microsoft wishes that they could fix the class by making Now a method instead of a property. Environment\u2019s TickCount property is another example of this mistake.</p><p>\u25a0\u25a0 A property method may cause observable side effects; field access never does. In other words, a user of a type should be able to set various properties defined by a type in any order he or she chooses without noticing any different behavior in the type.</p><p>\u25a0\u25a0 A property method may require additional memory or return a reference to something that is not actually part of the object\u2019s state, so modifying the returned object has no effect on the original object; querying a field always returns a reference to an object that is guaranteed to be part of the original object\u2019s state. Working with a property that returns a copy can be very confusing to developers, and this characteristic is frequently not documented.</p><p>It has come to my attention that people use properties far more often than they should. If you examine this list of differences between properties and fields, you\u2019ll see that there are very few circumstances in which defining a property is actually useful and will not cause confusion for developers. The only thing that properties buy you is some simplified syntax; there is no performance benefit compared to calling a non-property method, and understandability of the code is reduced. If I had been involved in the design of the .NET Framework and compilers, I would have not offered properties at all; instead, I would have programmers actually implement GetXxx and SetXxx methods as desired. Then, if compilers wanted to offer some special, simplified syntax for calling these methods, so be it. But I\u2019d want the compiler to use syntax that is different from field access syntax so that programmers really understand what they are doing\u2014a method call.</p><h6>Properties and the Visual Studio Debugger</h6><p>Microsoft Visual Studio allows you to enter an object\u2019s property in the debugger\u2019s watch window. When you do this, every time you hit a breakpoint, the debugger calls into the property\u2019s get accessor method and displays the returned value. This can be quite helpful in tracking down bugs, but it can also cause bugs to occur and hurt your debugging performance. For example, let\u2019s say that you have created a FileStream for a file on a network share and then you add FileStream\u2019s Length property to the debugger\u2019s watch window. Now, every time you hit a breakpoint, the debugger will call Length\u2019s get accessor method, which internally makes a network request to the server to get the current length of the file!</p><p>Similarly, if your property\u2019s get accessor method has a side effect, then this side effect will execute every time you hit a breakpoint. For example, let\u2019s say that your property\u2019s get accessor method increments a counter every time it is called; this counter will now be incremented every time you hit a breakpoint, too. Because of these potential problems, Visual Studio allows you to turn off property evaluation for properties shown in watch windows. To turn property evaluation off in Visual Studio, select Tools, Options, Debugging, and General and in the list box in Figure 10-1, and clear the Enable Property Evaluation And Other Implicit Function Calls check box. Note that even with this item cleared, you can add the property to the watch window and manually force Visual Studio to evaluate it by clicking the force evaluation circle in the watch window\u2019s Value column.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile87.png\"/><figcaption>FIGURE 10-1 The Visual Studio General Debugger settings.</figcaption><h6>Object and Collection Initializers</h6><p>It is very common to construct an object and then set some of the object\u2019s public properties (or fields). To simplify this common programming pattern, the C# language supports a special object initialization syntax. The following is an example.</p><p>Employee e = new Employee() { Name = \"Jeff\", Age = 45 };</p><p>With this one statement, I am constructing an Employee object, calling its parameterless constructor, and then setting its public Name property to \"Jeff\" and its public Age property to 45. In fact, the preceding code is identical to the following, which you could verify by examining the Intermediate Language (IL) for both of these code fragments.</p><p>Employee _tempVar = new Employee(); _tempVar.Name = \"Jeff\"; _tempVar.Age = 45;</p><p>// Only assign to e if the assignments above don't throw an exception. // This prevents e from referring to a partially initialized object. Employee e = _tempVar;</p><p>The real benefit of the object initializer syntax is that it allows you to code in an expression context (as opposed to a statement context), permitting composability of functions, which in turn increases code readability. For example, I can now write the following.</p><p>String s = new Employee() { Name = \"Jeff\", Age = 45 }.ToString().ToUpper();</p><p>So now, in one statement, I have constructed an Employee object, called its constructor, initialized two public properties, and then, using the resulting expression, called ToString on it followed by calling ToUpper. For more about composability of functions, see the \u201cExtension Methods\u201d section in Chapter 8, \u201cMethods.\u201d</p><p>As a small side note, C# also lets you omit the parentheses before the open brace if you want to call a parameterless constructor. The following line produces the same IL as the preceding line.</p><p>String s = new Employee { Name = \u201cJeff\u201d, Age = 45 }.ToString().ToUpper();</p><p>If a property\u2019s type implements the IEnumerable or IEnumerable&lt;T&gt; interface, then the property is considered to be a collection, and initializing a collection is an additive operation as opposed to a replacement operation. For example, suppose I have the following class definition.</p><p>public sealed class Classroom { private List&lt;String&gt; m_students = new List&lt;String&gt;(); public List&lt;String&gt; Students { get { return m_students; } }</p><p>public Classroom() {} }</p><p>I can now have code that constructs a Classroom object and initializes the Students collection as follows.</p><p>public static void M() { Classroom classroom = new Classroom {</p><p>Students = { \"Jeff\", \"Kristin\", \"Aidan\", \"Grant\" } };</p><p>// Show the 4 students in the classroom foreach (var student in classroom.Students)</p><p>Console.WriteLine(student); }</p><p>When compiling this code, the compiler sees that the Students property is of type List&lt;String&gt;</p><p>and that this type implements the IEnumerable&lt;String&gt; interface. Now, the compiler assumes that the List&lt;String&gt; type offers a method called Add (because most collection classes actually offer an Add method that adds items to the collection). The compiler then generates code to call the collec-</p><p>tion\u2019s Add method. So, the preceding code is converted by the compiler into the following.</p><p>public static void M() { Classroom classroom = new Classroom(); classroom.Students.Add(\"Jeff\"); classroom.Students.Add(\"Kristin\"); classroom.Students.Add(\"Aidan\"); classroom.Students.Add(\"Grant\");</p><p>// Show the 4 students in the classroom foreach (var student in classroom.Students)</p><p>Console.WriteLine(student); }</p><p>If the property\u2019s type implements IEnumerable or IEnumerable&lt;T&gt; but the type doesn\u2019t offer an Add method, then the compiler does not let you use the collection initialize syntax to add items to the collection; instead, the compiler issues something like the following message: error CS0117: 'System.Collections.Generic.IEnumerable&lt;string&gt;' does not contain a definition for 'Add'.</p><p>Some collection\u2019s Add methods take multiple arguments\u2014for example, Dictionary\u2019s Add method.</p><p>public void Add(TKey key, TValue value);</p><p>You can pass multiple arguments to an Add method by using nested braces in a collection initializer, as follows.</p><p>var table = new Dictionary&lt;String, Int32&gt; {</p><p>{ \"Jeffrey\", 1 }, { \"Kristin\", 2 }, { \"Aidan\", 3 }, { \"Grant\", 4 } };</p><p>The preceding line is identical to the following.</p><p>var table = new Dictionary&lt;String, Int32&gt;(); table.Add(\"Jeffrey\", 1); table.Add(\"Kristin\", 2); table.Add(\"Aidan\", 3); table.Add(\"Grant\", 4);</p><h6>Anonymous Types</h6><p>C#\u2019s anonymous type feature allows you to automatically declare an immutable tuple type by using a very simple and succinct syntax. A tuple type is a type that contains a collection of properties that are usually related to each other in some way.1 In the top line of the following code, I am defining a class with two properties (Name of type String, and Year of type Int32), constructing an instance of this type, and setting its Name property to \"Jeff\" and its Year property to 1964.</p><p>// Define a type, construct an instance of it, &amp; initialize its properties var o1 = new { Name = \"Jeff\", Year = 1964 };</p><p>// Display the properties on the console: Console.WriteLine(\"Name={0}, Year={1}\", o1.Name, o1.Year);// Displays: Name=Jeff, Year=1964</p><p>This top line of code creates an anonymous type because I did not specify a type name after the new keyword, so the compiler will create a type name for me automatically and not tell me what it is (which is why it is called an anonymous type). The line of code uses the object initializer syntax discussed in the previous section to declare the properties and also to initialize these properties. Also, because I (the developer) do not know the name of the type at compile time, I do not know what type to declare the variable o1 as. However, this is not a problem, because I can use C#\u2019s implicitly typed local variable feature (var), as discussed in Chapter 9, \u201cParameters,\u201d to have the compiler infer the type from the expression on the right of the assignment operator (=).</p><p>Now, let\u2019s focus on what the compiler is actually doing. When you write a line of code like this:</p><p>var o = new { property1 = expression1, ..., propertyN = expressionN };</p><p>the compiler infers the type of each expression, creates private fields of these inferred types, creates public read-only properties for each of the fields, and creates a constructor that accepts all these expressions. The constructor\u2019s code initializes the private read-only fields from the expression results passed in to it. In addition, the compiler overrides Object\u2019s Equals, GetHashCode, and ToString methods and generates code inside all these methods. In effect, the class that the compiler generates looks like the following.</p><p>[CompilerGenerated] internal sealed class &lt;&gt;f__AnonymousType0&lt;...&gt;: Object {</p><p>private readonly t1 f1; public t1 p1 { get { return f1; } }</p><p>...</p><p>1 The term originated as an abstraction of the sequence: single, double, triple, quadruple, quintuple, n-tuple.</p><p>private readonly tn fn; public tn pn { get { return fn; } }</p><p>public &lt;&gt;f__AnonymousType0&lt;...&gt;(t1 a1, ..., tn an) {</p><p>f1 = a1; ...; fn = an; // Set all fields }</p><p>public override Boolean Equals(Object value) {</p><p>// Return false if any fields don't match; else true }</p><p>public override Int32 GetHashCode() {</p><p>// Returns a hash code generated from each fields' hash code }</p><p>}</p><p>public override String ToString() {</p><p>// Return comma\u00adseparated set of property name = value pairs }</p><p>The compiler generates Equals and GetHashCode methods so that instances of the anonymous type can be placed in a hash table collection. The properties are readonly as opposed to read/write to help prevent the object\u2019s hashcode from changing. Changing the hashcode for an object used as a key in a hashtable can prevent the object from being found. The compiler generates the ToString method to help with debugging. In the Visual Studio debugger, you can place the mouse cursor over a variable that refers to an instance of an anonymous type, and Visual Studio will invoke the ToString method and show the resulting string in a datatip window. By the way, Visual Studio\u2019s IntelliSense will suggest the property names as you write code in the editor\u2014a very nice feature.</p><p>The compiler supports two additional syntaxes for declaring a property inside an anonymous type where it can infer the property names and types from variables.</p><p>String Name = \"Grant\"; DateTime dt = DateTime.Now;</p><p>// Anonymous type with two properties</p><ul style=\"list-style-type: none;\">\n<li><p>// 1. String Name property set to Grant</p></li>\n<li><p>// 2. Int32 Year property set to the year inside the dt var o2 = new { Name, dt.Year };</p></li>\n</ul><p>In this example, the compiler determines that the first property should be called Name. Because Name is the name of a local variable, the compiler sets the type of the property to be the same type as the local variable: String. For the second property, the compiler uses the name of the field/property: Year. Year is an Int32 property of the DateTime class, and therefore the Year property in the anonymous type will also be an Int32. Now, when the compiler constructs an instance of this anonymous type, it will set the instance\u2019s Name property to the same value that is in the Name local variable so the Name property will refer to the same \"Grant\" string. The compiler will set the instance\u2019s Year property to the same value that is returned from dt\u2019s Year property.</p><p>The compiler is very intelligent about defining anonymous types. If the compiler sees that you are defining multiple anonymous types in your source code that have the identical structure, the compiler will create just one definition for the anonymous type and create multiple instances of that type. By \u201csame structure,\u201d I mean that the anonymous types have the same type and name for each property and that these properties are specified in the same order. In the preceding code examples, the type of variable o1 and the type of variable o2 will be the same type because the two lines of code are defining an anonymous type with a Name/String property and a Year/Int32 property, and Name comes before Year.</p><p>Because the two variables are of the same type, we get to do some cool things, such as checking whether the two objects contain equal values and assigning a reference to one object into the other\u2019s variable, as follows.</p><p>// One type allows equality and assignment operations. Console.WriteLine(\"Objects are equal: \" + o1.Equals(o2)); o1 = o2; // Assignment</p><p>Also, because of this type identity, we can create an implicitly typed array (discussed in the \u201cInitializing Array Elements\u201d section in Chapter 16, \u201cArrays\u201d) of anonymous types.</p><p>// This works because all of the objects are of the same anonymous type var people = new[] {</p><p>o1, // From earlier in this section new { Name = \"Kristin\", Year = 1970 }, new { Name = \"Aidan\", Year = 2003 }, new { Name = \"Grant\", Year = 2008 }</p><p>};</p><p>// This shows how to walk through the array of anonymous types (var is required) foreach (var person in people)</p><p>Console.WriteLine(\"Person={0}, Year={1}\", person.Name, person.Year);</p><p>Anonymous types are most commonly used with the Language Integrated Query (LINQ) technology, where you perform a query that results in a collection of objects that are all of the same anonymous type. Then, you process the objects in the resulting collection. All this takes place in the same method. Here is an example that returns all the files in my document directory that have been modified within the past seven days.</p><p>String myDocuments = Environment.GetFolderPath(Environment.SpecialFolder.MyDocuments); var query =</p><p>from pathname in Directory.GetFiles(myDocuments) let LastWriteTime = File.GetLastWriteTime(pathname) where LastWriteTime &gt; (DateTime.Now \u00ad TimeSpan.FromDays(7)) orderby LastWriteTime select new { Path = pathname, LastWriteTime };// Set of anonymous type objects</p><p>foreach (var file in query) Console.WriteLine(\"LastWriteTime={0}, Path={1}\", file.LastWriteTime, file.Path);</p><p>Instances of anonymous types are not supposed to leak outside of a method. A method cannot be prototyped as accepting a parameter of an anonymous type because there is no way to specify the anonymous type. Similarly, a method cannot indicate that it returns a reference to an anonymous type. Although it is possible to treat an instance of an anonymous type as an Object (because all anonymous types are derived from Object), there is no way to cast a variable of type Object back into an anonymous type because you don\u2019t know the name of the anonymous type at compile time. If you want to pass a tuple around, then you should consider using the System.Tuple type discussed in the next section.</p><h6>The System.Tuple Type</h6><p>In the System namespace, Microsoft has defined several generic Tuple types (all derived from Object) that differ by arity (the number of generic parameters). Here is what the simplest and most complex ones essentially look like.</p><p>// This is the simplest: [Serializable] public class Tuple&lt;T1&gt; {</p><p>private T1 m_Item1; public Tuple(T1 item1) { m_Item1 = item1; } public T1 Item1 { get { return m_Item1; } }</p><p>}</p><p>// This is the most complex: [Serializable] public class Tuple&lt;T1, T2, T3, T4, T5, T6, T7, TRest&gt; {</p><p>private T1 m_Item1; private T2 m_Item2; private T3 m_Item3; private T4 m_Item4; private T5 m_Item5; private T6 m_Item6; private T7 m_Item7; private TRest m_Rest;</p><p>public Tuple(T1 item1, T2 item2, T3 item3, T4 item4, T5 item5, T6 item6, T7 item7, TRest rest) { m_Item1 = item1; m_Item2 = item2; m_Item3 = item3; m_Item4 = item4; m_Item5 = item5; m_Item6 = item6; m_Item7 = item7; m_Rest = rest;</p><p>}</p><p>}</p><ul style=\"list-style-type: none;\">\n<li><p>public T1 Item1 { get { return m_Item1; } }</p></li>\n<li><p>public T2 Item2 { get { return m_Item2; } }</p></li>\n<li><p>public T3 Item3 { get { return m_Item3; } }</p></li>\n<li><p>public T4 Item4 { get { return m_Item4; } }</p></li>\n<li><p>public T5 Item5 { get { return m_Item5; } }</p></li>\n<li><p>public T6 Item6 { get { return m_Item6; } }</p></li>\n<li><p>public T7 Item7 { get { return m_Item7; } } public TRest Rest { get { return m_Rest; } }</p></li>\n</ul><p>Like anonymous types, after a Tuple is created, it is immutable (all properties are read-only). I don\u2019t show it here, but the Tuple classes also offer CompareTo, Equals, GetHashCode, and ToString methods, as well as a Size property. In addition, all the Tuple types implement the IStructuralEquatable, IStructuralComparable, and IComparable interfaces so that you can compare two Tuple objects with each other to see how their fields compare with each other. Refer to the SDK documentation to learn more about these methods and interfaces.</p><p>Here is an example of a method that uses a Tuple type to return two pieces of information to a caller.</p><p>// Returns minimum in Item1 &amp; maximum in Item2 private static Tuple&lt;Int32, Int32&gt; MinMax(Int32 a, Int32 b) {</p><p>return new Tuple&lt;Int32, Int32&gt;(Math.Min(a, b), Math.Max(a, b)); }</p><p>// This shows how to call the method and how to use the returned Tuple private static void TupleTypes() {</p><p>var minmax = MinMax(6, 2); Console.WriteLine(\"Min={0}, Max={1}\", minmax.Item1, minmax.Item2); // Min=2, Max=6</p><p>}</p><p>Of course, it is very important that the producer and consumer of the Tuple have a clear understanding of what is being returned in the Item# properties. With anonymous types, the properties are given actual names based on the source code that defines the anonymous type. With Tuple types, the properties are assigned their Item# names by Microsoft and you cannot change this at all. Unfortunately, these names have no real meaning or significance, so it is up to the producer and consumer to assign meanings to them. This also reduces code readability and maintainability so you should add comments to your code explaining what the producer/consumer understanding is.</p><p>The compiler can only infer generic types when calling a generic method, not when you are calling a constructor. For this reason, the System namespace also includes a non-generic, static Tuple class containing a bunch of static Create methods that can infer generic types from arguments. This class acts as a factory for creating Tuple objects, and it exists simply to simplify your code. Here is a rewrite of the MinMax method shown earlier using the static Tuple class.</p><p>// Returns minimum in Item1 &amp; maximum in Item2 private static Tuple&lt;Int32, Int32&gt; MinMax(Int32 a, Int32 b) {</p><p>return Tuple.Create(Math.Min(a, b), Math.Max(a, b)); // Simpler syntax }</p><p>If you want to create a Tuple with more than eight elements in it, then you would pass another Tuple for the Rest parameter as follows.</p><p>var t = Tuple.Create(0, 1, 2, 3, 4, 5, 6, Tuple.Create(7, 8)); Console.WriteLine(\"{0}, {1}, {2}, {3}, {4}, {5}, {6}, {7}, {8}\",</p><p>t.Item1, t.Item2, t.Item3, t.Item4, t.Item5, t.Item6, t.Item7, t.Rest.Item1.Item1, t.Rest.Item1.Item2);</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile88.png\"/><p>Note In addition to anonymous types and the Tuple types, you might want to take a look at the System.Dynamic.ExpandoObject class (defined in the System.Core.dll assembly). When you use this class with C#\u2019s dynamic type (discussed in Chapter 5, \u201cPrimitive, Reference, and Value Types\u201d), you have another way of grouping a set of properties (key/ value pairs) together. The result is not compile-time type-safe, but the syntax looks nice (although you get no IntelliSense support), and you can pass ExpandoObject objects between C# and dynamic languages like Python. Here\u2019s some sample code that uses an ExpandoObject.</p><p>dynamic e = new System.Dynamic.ExpandoObject();</p><ul style=\"list-style-type: none;\">\n<li><p>e.x = 6; // Add an Int32 'x' property whose value is 6</p></li>\n<li><p>e.y = \"Jeff\"; // Add a String 'y' property whose value is \"Jeff\"</p></li>\n<li><p>e.z = null; // Add an Object 'z' property whose value is null</p></li>\n</ul><p>// See all the properties and their values: foreach (var v in (IDictionary&lt;String, Object&gt;)e)</p><p>Console.WriteLine(\"Key={0}, V={1}\", v.Key, v.Value);</p><p>// Remove the 'x' property and its value var d = (IDictionary&lt;String, Object&gt;)e; d.Remove(\"x\");</p><h6>Parameterful Properties</h6><p>In the previous section, the get accessor methods for the properties accepted no parameters. For this reason, I called these properties parameterless properties. These properties are easy to understand because they have the feel of accessing a field. In addition to these field-like properties, programming languages also support what I call parameterful properties, whose get accessor methods accept one or more parameters and whose set accessor methods accept two or more parameters. Different programming languages expose parameterful properties in different ways. Also, languages use different terms to refer to parameterful properties: C# calls them indexers and Visual Basic calls them default properties. In this section, I\u2019ll focus on how C# exposes its indexers by using parameterful properties.</p><p>In C#, parameterful properties (indexers) are exposed using an array-like syntax. In other words, you can think of an indexer as a way for the C# developer to overload the [] operator. Here\u2019s an example of a BitArray class that allows array-like syntax to index into the set of bits maintained by an instance of the class.</p><p>using System;</p><p>public sealed class BitArray { // Private array of bytes that hold the bits private Byte[] m_byteArray; private Int32 m_numBits;</p><p>// Constructor that allocates the byte array and sets all bits to 0 public BitArray(Int32 numBits) {</p><p>// Validate arguments first. if (numBits &lt;= 0)</p><p>throw new ArgumentOutOfRangeException(\"numBits must be &gt; 0\");</p><p>// Save the number of bits. m_numBits = numBits;</p><p>}</p><p>// Allocate the bytes for the bit array. m_byteArray = new Byte[(numBits + 7) / 8];</p><p>// This is the indexer (parameterful property). public Boolean this[Int32 bitPos] {</p><p>// This is the indexer's get accessor method. get {</p><p>// Validate arguments first if ((bitPos &lt; 0) || (bitPos &gt;= m_numBits))</p><p>throw new ArgumentOutOfRangeException(\"bitPos\");</p><p>}</p><p>// Return the state of the indexed bit. return (m_byteArray[bitPos / 8] &amp; (1 &lt;&lt; (bitPos % 8))) != 0;</p><p>}</p><p>// This is the indexer's set accessor method. set {</p><p>if ((bitPos &lt; 0) || (bitPos &gt;= m_numBits)) throw new ArgumentOutOfRangeException(\"bitPos\", bitPos.ToString());</p><p>if (value) { // Turn the indexed bit on. m_byteArray[bitPos / 8] = (Byte)</p><p>(m_byteArray[bitPos / 8] | (1 &lt;&lt; (bitPos % 8)));</p><p>} else { // Turn the indexed bit off. m_byteArray[bitPos / 8] = (Byte)</p><p>(m_byteArray[bitPos / 8] &amp; ~(1 &lt;&lt; (bitPos % 8))); }</p><p>} }</p><p>Using the BitArray class\u2019s indexer is incredibly simple.</p><p>// Allocate a BitArray that can hold 14 bits. BitArray ba = new BitArray(14);</p><p>// Turn all the even\u00adnumbered bits on by calling the set accessor. for (Int32 x = 0; x &lt; 14; x++) {</p><p>ba[x] = (x % 2 == 0); }</p><p>// Show the state of all the bits by calling the get accessor. for (Int32 x = 0; x &lt; 14; x++) {</p><p>Console.WriteLine(\"Bit \" + x + \" is \" + (ba[x] ? \"On\" : \"Off\")); }</p><p>In the BitArray example, the indexer takes one Int32 parameter, bitPos. All indexers must have at least one parameter, but they can have more. These parameters (as well as the return type) can be of any data type (except void). An example of an indexer that has more than one parameter can be found in the System.Drawing.Imaging.ColorMatrix class, which ships in the System.Drawing.dll assembly.</p><p>It\u2019s quite common to create an indexer to look up values in an associative array. In fact, the System.Collections.Generic.Dictionary type offers an indexer that takes a key and returns the value associated with the key. Unlike parameterless properties, a type can offer multiple, overloaded indexers as long as their signatures differ.</p><p>Like a parameterless property\u2019s set accessor method, an indexer\u2019s set accessor method also contains a hidden parameter, called value in C#. This parameter indicates the new value desired for the \u201cindexed element.\u201d</p><p>The CLR doesn\u2019t differentiate parameterless properties and parameterful properties; to the CLR, each is simply a pair of methods and a piece of metadata defined within a type. As mentioned earlier, different programming languages require different syntax to create and use parameterful properties. The fact that C# requires this[...] as the syntax for expressing an indexer was purely a choice made by the C# team. What this choice means is that C# allows indexers to be defined only on instances of objects. C# doesn\u2019t offer syntax allowing a developer to define a static indexer property, although the CLR does support static parameterful properties.</p><p>Because the CLR treats parameterful properties just as it does parameterless properties, the compiler will emit either two or three of the following items into the resulting managed assembly:</p><p>\u25a0\u25a0 A method representing the parameterful property\u2019s get accessor method. This is emitted only</p><p>if you define a get accessor method for the property.</p><p>\u25a0\u25a0 A method representing the parameterful property\u2019s set accessor method. This is emitted only</p><p>if you define a set accessor method for the property.</p><p>\u25a0\u25a0 A property definition in the managed assembly\u2019s metadata, which is always emitted. There\u2019s no special parameterful property metadata definition table because, to the CLR, parameterful properties are just properties.</p><p>For the BitArray class shown earlier, the compiler compiles the indexer as though the original source code were written as follows.</p><p>public sealed class BitArray {</p><p>// This is the indexer's get accessor method. public Boolean get_Item(Int32 bitPos) { /* ... */ }</p><p>}</p><p>// This is the indexer's set accessor method. public void set_Item(Int32 bitPos, Boolean value) { /* ... */ }</p><p>The compiler automatically generates names for these methods by prepending get_ and set_ to the indexer name. Because the C# syntax for an indexer doesn\u2019t allow the developer to specify an indexer name, the C# compiler team had to choose a default name to use for the accessor methods; they chose Item. Therefore, the method names emitted by the compiler are get_Item and set_Item.</p><p>When examining the .NET Framework Reference documentation, you can tell if a type offers an indexer by looking for a property named Item. For example, the System.Collections.Generic.List type offers a public instance property named Item; this property is List\u2019s indexer.</p><p>When you program in C#, you never see the name of Item, so you don\u2019t normally care that the compiler has chosen this name for you. However, if you\u2019re designing an indexer for a type that code written in other programming languages will be accessing, you might want to change the default name, Item, given to your indexer\u2019s get and set accessor methods. C# allows you to rename these methods by applying the System.Runtime.CompilerServices.IndexerNameAttribute custom attribute to the indexer. The following code demonstrates how to do this.</p><p>using System; using System.Runtime.CompilerServices;</p><p>public sealed class BitArray {</p><p>}</p><p>[IndexerName(\"Bit\")] public Boolean this[Int32 bitPos] {</p><p>// At least one accessor method is defined here }</p><p>Now the compiler will emit methods called get_Bit and set_Bit instead of get_Item and set_ Item. When compiling, the C# compiler sees the IndexerName attribute, and this tells the compiler how to name the methods and the property metadata; the attribute itself is not emitted into the assembly\u2019s metadata.2</p><p>Here\u2019s some Visual Basic code that demonstrates how to access this C# indexer.</p><p>' Construct an instance of the BitArray type. Dim ba as New BitArray(10)</p><p>' Visual Basic uses () instead of [] to specify array elements. Console.WriteLine(ba(2)) ' Displays True or False</p><p>' Visual Basic also allows you to access the indexer by its name. Console.WriteLine(ba.Bit(2)) ' Displays same as previous line</p><p>In C#, a single type can define multiple indexers as long as the indexers all take different param-</p><p>eter sets. In other programming languages, the IndexerName attribute allows you to define multiple indexers with the same signature because each can have a different name. The reason C# won\u2019t allow you to do this is because its syntax doesn\u2019t refer to the indexer by name; the compiler wouldn\u2019t know which indexer you were referring to. Attempting to compile the following C# source code causes the</p><p>2 For this reason, the IndexerNameAttribute class is not part of the ECMA standardization of the CLI and the C# language.</p><p>compiler to generate the following message: error C0111: Type 'SomeType' already defines a member called 'this' with the same parameter types.</p><p>using System; using System.Runtime.CompilerServices;</p><p>public sealed class SomeType {</p><p>// Define a get_Item accessor method. public Int32 this[Boolean b] {</p><p>get { return 0; } }</p><p>}</p><p>// Define a get_Jeff accessor method. [IndexerName(\"Jeff\")] public String this[Boolean b] {</p><p>get { return null; } }</p><p>You can clearly see that C# thinks of indexers as a way to overload the [] operator, and this operator can\u2019t be used to disambiguate parameterful properties with different method names and identical parameter sets.</p><p>By the way, the System.String type is an example of a type that changed the name of its indexer. The name of String\u2019s indexer is Chars instead of Item. This read-only property allows you to get an individual character within a string. For programming languages that don\u2019t use [] operator syntax to access this property, Chars was decided to be a more meaningful name.</p><h6>Selecting the Primary Parameterful Property</h6><p>C#\u2019s limitations with respect to indexers brings up the following two questions:</p><p>\u25a0\u25a0 What if a type is defined in a programming language that does allow the developer to</p><p>define several parameterful properties?</p><p>\u25a0\u25a0 How can this type be consumed from C#?</p><p>The answer to both questions is that a type must select one of the parameterful property names to be the default property by applying an instance of System.Reflection.DefaultMemberAttribute to the class itself. For the record, DefaultMemberAttribute can be applied to a class, a structure, or an interface. In C#, when you compile a type that defines a parameterful property, the compiler automatically applies an instance of DefaultMember attribute to the defining type and takes it into account when you use the IndexerName attribute. This attribute\u2019s constructor specifies the name that is to be used for the type\u2019s default parameterful property.</p><p>So, in C#, if you define a type that has a parameterful property and you don\u2019t specify the IndexerName attribute, the defining type will have a DefaultMember attribute indicating Item. If you apply the IndexerName attribute to a parameterful property, the defining type</p><p>will have a DefaultMember attribute indicating the string name specified in the IndexerName attribute. Remember, C# won\u2019t compile the code if it contains parameterful properties with different names.</p><p>For a language that supports several parameterful properties, one of the property method names must be selected and identified by the type\u2019s DefaultMember attribute. This is the only parameterful property that C# will be able to access.</p><p>When the C# compiler sees code that is trying to get or set an indexer, the compiler actually emits a call to one of these methods. Some programming languages might not support parameterful properties. To access a parameterful property from one of these languages, you must call the desired accessor method explicitly. To the CLR, there\u2019s no difference between parameterless properties and parameterful properties, so you use the same System.Reflection.PropertyInfo class to find the association between a parameterful property and its accessor methods.</p><h6>The Performance of Calling Property Accessor Methods</h6><p>For simple get and set accessor methods, the just-in-time (JIT) compiler inlines the code so that there\u2019s no run-time performance hit as a result of using properties rather than fields. Inlining is when the code for a method (or accessor method, in this case) is compiled directly in the method that is making the call. This removes the overhead associated with making a call at run time at the expense of making the compiled method\u2019s code bigger. Because property accessor methods typically contain very little code, inlining them can make the native code smaller and can make it execute faster.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile89.png\"/><p>Note The JIT compiler does not inline property methods when debugging code because inlined code is harder to debug. This means that the performance of accessing a property can be fast in a release build and slow in a debug build. Field access is fast in both debug and release builds.</p><h6>Property Accessor Accessibility</h6><p>Occasionally, when designing a type, it is desired to have one accessibility for a get accessor method and a different accessibility for a set accessor method. The most common scenario is to have a public get accessor and a protected set accessor.</p><p>public class SomeType { private String m_name; public String Name {</p><p>get { return m_name; } protected set {m_name = value; }</p><p>} }</p><p>As you can see from the code above, the Name property is itself declared as a public property, and this means that the get accessor method will be public and therefore callable by all code. However, notice that the set accessor is declared as protected and will be callable only from code defined within SomeType or from code in a class that is derived from SomeType.</p><p>When defining a property with accessor methods that have different accessibilities, C# syntax requires that the property itself must be declared with the least-restrictive accessibility and that more restrictive accessibility be applied to just one of the accessor methods. In the example above, the property is public, and the set accessor is protected (more restrictive than public).</p><h6>Generic Property Accessor Methods</h6><p>Because properties are really just methods, and because C# and the CLR allow methods to be generic, sometimes people want to define properties that introduce their own generic type parameters (as opposed to using the enclosing type\u2019s generic type parameter). However, C# does not allow this. The main reason why properties cannot introduce their own generic type parameters is because they don\u2019t make sense conceptually. A property is supposed to represent a characteristic of an object that can be queried or set. Introducing a generic type parameter would mean that the behavior of the querying/setting could be changed, but conceptually, a property is not supposed to have behavior. If you want your object to expose some behavior\u2014generic or not\u2014define a method, not a property.</p><h6>C H A P T E R 1 1</h6><h2>Events</h2><h6>In this chapter:</h6><p>Designing a Type That Exposes an Event . . . . . . . . . . . . . . . . . . . 250 How the Compiler Implements an Event . . . . . . . . . . . . . . . . . . . 256 Designing a Type That Listens for an Event . . . . . . . . . . . . . . . . . 258 Explicitly Implementing an Event. . . . . . . . . . . . . . . . . . . . . . . . . . 260</p><p>In this chapter, I\u2019ll talk about the last kind of member a type can define: events. A type that defines an event member allows the type (or instances of the type) to notify other objects that something special has happened. For example, the Button class offers an event called Click. When a Button object is clicked, one or more objects in an application may want to receive notification about this event in order to perform some action. Events are type members that allow this interaction. Specifically, defining an event member means that a type is offering the following capabilities:</p><p>\u25a0\u25a0 A method can register its interest in the event.</p><p>\u25a0\u25a0 A method can unregister its interest in the event.</p><p>\u25a0\u25a0 Registered methods will be notified when the event occurs. Types can offer this functionality when defining an event because they maintain a list of the registered methods. When the event occurs, the type notifies all of the registered methods in the collection.</p><p>The common language runtime\u2019s (CLR\u2019s) event model is based on delegates. A delegate is a typesafe way to invoke a callback method. Callback methods are the means by which objects receive the notifications they subscribed to. In this chapter, I\u2019ll be using delegates, but I won\u2019t fully explain all their details until Chapter 17, \u201cDelegates.\u201d</p><p>To help you fully understand the way events work within the CLR, I\u2019ll start with a scenario in which events are useful. Suppose you want to design an email application. When an email message arrives, the user might like the message to be forwarded to a fax machine or a pager. In architecting this application, let\u2019s say that you\u2019ll first design a type called MailManager that receives the incoming email messages. MailManager will expose an event called NewMail. Other types (such as Fax and Pager) may register interest in this event. When MailManager receives a new email message, it will raise the event, causing the message to be distributed to each of the registered objects. Each object can process the message in any way it desires.</p><p>249</p><p>When the application initializes, let\u2019s instantiate just one MailManager instance\u2014the application can then instantiate any number of Fax and Pager types. Figure 11-1 shows how the application initializes and what happens when a new email message arrives.</p><p>Fax #1 Pager #1</p><p>4</p><p>1</p><p>2</p><p>MailManager</p><p>3</p><table border=\"1\">\n<tr>\n<td><ul style=\"list-style-type: none;\">\n<li><p>1. A method in the Fax object registers interest with the MailManager\u2019s event.</p></li>\n<li><p>2. A method in the Pager object registers interest with the MailManager\u2019s event.</p></li>\n<li><p>3. A new mail message arrives at MailManager.</p></li>\n<li><p>4. The MailManager object fires the notification off to all the registered methods, which process the mail message as desired.</p></li>\n</ul>\n</td>\n</tr>\n</table><p>FIGURE 11-1 Architecting an application to use events.</p><p>Here\u2019s how the application illustrated in Figure 11-1 works: the application initializes by constructing an instance of MailManager. MailManager offers a NewMail event. When the Fax and Pager objects are constructed, they register an instance method with MailManager\u2019s NewMail event so that MailManager knows to notify the Fax and Pager objects when new email messages arrive. Now, when MailManager receives a new email message (sometime in the future), it will raise the NewMail event, giving all of the registered methods an opportunity to process the new message in any way they want.</p><h6>Designing a Type That Exposes an Event</h6><p>There are many steps a developer must take in order to define a type that exposes one or more event members. In this section, I\u2019ll walk through each of the necessary steps. The MailManager sample application (which can be downloaded from the Books section in Resources at http://wintellect.com/ Books) shows all of the source code for the MailManager type, the Fax type, and the Pager type. You\u2019ll notice that the Pager type is practically identical to the Fax type.</p><h6>Step #1: Define a type that will hold any additional information that should be sent to receivers of the event notification</h6><p>When an event is raised, the object raising the event may want to pass some additional information to the objects receiving the event notification. This additional information needs to be encapsulated into its own class, which typically contains a bunch of private fields along with some read-only public properties to expose these fields. By convention, classes that hold event information to be passed to the event handler should be derived from System.EventArgs, and the name of the class should be suffixed with EventArgs. In this example, the NewMailEventArgs class has fields identifying who sent the message (m_from), who is receiving the message (m_to), and the subject of the message (m_subject).</p><p>// Step #1: Define a type that will hold any additional information that // should be sent to receivers of the event notification internal class NewMailEventArgs : EventArgs {</p><p>private readonly String m_from, m_to, m_subject;</p><p>public NewMailEventArgs(String from, String to, String subject) {</p><p>m_from = from; m_to = to; m_subject = subject; }</p><p>}</p><p>public String From { get { return m_from; } } public String To { get { return m_to; } } public String Subject { get { return m_subject; } }</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile90.png\"/><p>Note The EventArgs class is defined in the Microsoft .NET Framework Class Library (FCL) and is implemented like the following.</p><p>[ComVisible(true), Serializable] public class EventArgs {</p><p>public static readonly EventArgs Empty = new EventArgs(); public EventArgs() { }</p><p>}</p><p>As you can see, this type is nothing to write home about. It simply serves as a base type from which other types can derive. Many events don\u2019t have any additional information to pass on. For example, when a Button notifies its registered receivers that it has been clicked, just invoking the callback method is enough information. When you\u2019re defining an event that doesn\u2019t have any additional data to pass on, just use EventArgs.Empty rather than constructing a new EventArgs object.</p><h6>Step #2: Define the event member</h6><p>An event member is defined using the C# keyword event. Each event member is given accessibility (which is almost always public so that other code can access the event member), a type of delegate indicating the prototype of the method(s) that will be called, and a name (which can be any valid identifier). Here is what the event member in our MailManager class looks like.</p><p>internal class MailManager {</p><p>// Step #2: Define the event member public event EventHandler&lt;NewMailEventArgs&gt; NewMail;</p><p>... }</p><p>NewMail is the name of this event. The type of the event member is EventHandler&lt;NewMailEventArgs&gt;, which means that all receivers of the event notification must supply a callback method whose prototype matches that of the EventHandler&lt;NewMailEventArgs&gt; delegate type. Because the generic System.EventHandler delegate is defined as follows.</p><p>public delegate void EventHandler&lt;TEventArgs&gt;(Object sender, TEventArgs e);</p><p>the method prototypes must look like the following.</p><p>void MethodName(Object sender, NewMailEventArgs e);</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile91.png\"/><p>Note A lot of people wonder why the event pattern requires the sender parameter to al-</p><p>ways be of type Object. After all, because the MailManager will be the only type raising an event with a NewMailEventArgs object, it makes more sense for the callback method to be prototyped like the following.</p><p>void MethodName(MailManager sender, NewMailEventArgs e);</p><p>The pattern requires the sender parameter to be of type Object mostly because of inheritance. What if MailManager were used as a base class for SmtpMailManager? In this case, the callback method should have the sender parameter prototyped as SmtpMailManager instead of MailManager, but this can\u2019t happen because SmtpMailManager just inherited the NewMail event. So the code that was expecting SmtpMailManager to raise the event must still have to cast the sender argument to SmtpMailManager. In other words, the cast is still required, so the sender parameter might as well be typed as Object.</p><p>The next reason for typing the sender parameter as Object is just flexibility. It allows the delegate to be used by multiple types that offer an event that passes a NewMailEventArgs object. For example, a PopMailManager class could use the delegate even if this class were not derived from MailManager.</p><p>The event pattern also requires that the delegate definition and the callback method name the EventArgs-derived parameter e. The only reason for this is to add additional consistency to the pattern, making it easier for developers to learn and implement the pattern. Tools that spit out source code (such as Microsoft Visual Studio) also know to call the parameter e.</p><p>Finally, the event pattern requires all event handlers to have a return type of void. This is necessary because raising an event might call several callback methods, and there is no way to get the return values from all of them. Having a return type of void doesn\u2019t allow the callbacks to return a value. Unfortunately, there are some event handlers in the FCL, such as ResolveEventHandler, that did not follow Microsoft\u2019s own prescribed pattern because it returns an object of type Assembly.</p><h6>Step #3: Define a method responsible for raising the event to notify registered objects that the event has occurred</h6><p>By convention, the class should define a protected, virtual method that is called by code internally within the class and its derived classes when the event is to be raised. This method takes one parameter, a NewMailEventArgs object, which includes the information passed to the objects receiving the notification. The default implementation of this method simply checks if any objects have registered interest in the event and, if so, the event will be raised, thereby notifying the registered methods that the event has occurred. Here is what the method in our MailManager class looks like.</p><p>internal class MailManager {</p><p>... // Step #3: Define a method responsible for raising the event // to notify registered objects that the event has occurred // If this class is sealed, make this method private and nonvirtual protected virtual void OnNewMail(NewMailEventArgs e) {</p><p>// Copy a reference to the delegate field now into a temporary field for thread safety EventHandler&lt;NewMailEventArgs&gt; temp = Volatile.Read(ref NewMail);</p><p>// If any methods registered interest with our event, notify them if (temp != null) temp(this, e);</p><p>}</p><p>... }</p><h6>Raising an Event in a Thread-Safe Way</h6><p>When the .NET Framework first shipped, the recommended way for developers to raise an event was by using code similar to the following.</p><ul style=\"list-style-type: none;\">\n<li><p>// Version 1 protected virtual void OnNewMail(NewMailEventArgs e) {</p><p>if (NewMail != null) NewMail(this, e); }</p>\n<p>The problem with the OnNewMail method is that the thread could see that NewMail is not</p>\n<p>null, and then, just before invoking NewMail, another thread could remove a delegate from the chain making NewMail null, resulting in a NullReferenceException being thrown. To fix this race condition, many developers write the OnNewMail method as follows.</p>\n</li>\n<li><p>// Version 2 protected virtual void OnNewMail(NewMailEventArgs e) {</p><p>EventHandler&lt;NewMailEventArgs&gt; temp = NewMail; if (temp != null) temp(this, e);</p>\n<p>}</p>\n<p>The thinking here is that a reference to NewMail is copied into a temporary variable, temp, which refers to the chain of delegates at the moment the assignment is performed. Now, this method compares temp and null and invokes temp, so it doesn\u2019t matter if another thread changes NewMail after the assignment to temp. Remember that delegates are immutable and this is why this technique works in theory. However, what a lot of developers don\u2019t realize is that this code could be optimized by the compiler to remove the local temp variable entirely. If this happens, this version of the code is identical to the first version, so a NullReferenceException is still possible.</p>\n<p>To really fix this code, you should rewrite OnNewMail like the following.</p>\n</li>\n<li><p>// Version 3 protected virtual void OnNewMail(NewMailEventArgs e) {</p></li>\n</ul><p>EventHandler&lt;NewMailEventArgs&gt; temp = Volatile.Read(ref NewMail); if (temp != null) temp(this, e);</p><p>}</p><p>The call to Volatile.Read forces NewMail to be read at the point of the call and the reference really has to be copied to the temp variable now. Then, temp will be invoked only if it is not null. See Chapter 29, \u201cPrimitive Thread Synchronization Constructs,\u201d for more information about the Volatile.Read method.</p><p>Although the last version of this code is the best, technically correct version, you can actually use the second version because the just-in-time (JIT) compiler is aware of this pattern and it knows not to optimize away the local temp variable. Specifically, all of Microsoft\u2019s JIT compilers respect the invariant of not introducing new reads to heap memory and therefore, caching a reference in a local variable ensures that the heap reference is accessed only once. This is not documented and, in theory, it could change, which is why you should use the last version. But in reality, Microsoft\u2019s JIT compiler would never embrace a change that would break this pattern because too many applications would break.1 In addition, events are mostly used in singlethreaded scenarios (Windows Presentation Foundation and Windows Store apps) and so thread safety is not an issue anyway.</p><p>It is very important to note that due to this thread race condition, it is also possible that a method will be invoked after it has been removed from the event\u2019s delegate chain.</p><p>As a convenience, you could define an extension method (as discussed in Chapter 8, \u201cMethods\u201d) that encapsulates this thread-safety logic. Define the extension method as follows.</p><p>public static class EventArgExtensions { public static void Raise&lt;TEventArgs&gt;(this TEventArgs e, Object sender, ref EventHandler&lt;TEventArgs&gt; eventDelegate) {</p><p>// Copy a reference to the delegate field now into a temporary field for thread safety EventHandler&lt;TEventArgs&gt; temp = Volatile.Read(ref eventDelegate);</p><p>} }</p><p>// If any methods registered interest with our event, notify them if (temp != null) temp(sender, e);</p><p>And now, we can rewrite the OnNewMail method as follows.</p><p>protected virtual void OnNewMail(NewMailEventArgs e) {</p><p>e.Raise(this, ref m_NewMail); }</p><p>A class that uses MailManager as a base type is free to override the OnNewMail method. This capability gives the derived class control over the raising of the event. The derived class can handle the new email message in any way it sees fit. Usually, a derived type calls the base type\u2019s OnNewMail method so that the registered method(s) receive the notification. However, the derived class might decide to disallow the event from being forwarded.</p><p>1 This was actually told to me by a member of the Microsoft JIT compiler team.</p><h6>Step #4: Define a method that translates the input into the desired event</h6><p>Your class must have some method that takes some input and translates it into the raising of the event. In my MailManager example, the SimulateNewMail method is called to indicate that a new email message has arrived into MailManager.</p><p>internal class MailManager {</p><p>// Step #4: Define a method that translates the // input into the desired event public void SimulateNewMail(String from, String to, String subject) {</p><p>// Construct an object to hold the information we want // to pass to the receivers of our notification NewMailEventArgs e = new NewMailEventArgs(from, to, subject);</p><p>} }</p><p>// Call our virtual method notifying our object that the event // occurred. If no type overrides this method, our object will // notify all the objects that registered interest in the event OnNewMail(e);</p><p>SimulateNewMail accepts information about the message and constructs a NewMailEventArgs object, passing the message information to its constructor. MailManager\u2019s own virtual OnNewMail method is then called to formally notify the MailManager object of the new email message. Usually, this causes the event to be raised, notifying all of the registered methods. (As mentioned before, a class using MailManager as a base class can override this behavior.)</p><h6>How the Compiler Implements an Event</h6><p>Now that you know how to define a class that offers an event member, let\u2019s take a closer look at what an event really is and how it works. In the MailManager class, we have a line of code that defines the event member itself.</p><p>public event EventHandler&lt;NewMailEventArgs&gt; NewMail;</p><p>When the C# compiler compiles the line above, it translates this single line of source code into the following three constructs.</p><ul style=\"list-style-type: none;\">\n<li><p>// 1. A PRIVATE delegate field that is initialized to null private EventHandler&lt;NewMailEventArgs&gt; NewMail = null;</p></li>\n<li><p>// 2. A PUBLIC add_Xxx method (where Xxx is the Event name) // Allows methods to register interest in the event. public void add_NewMail(EventHandler&lt;NewMailEventArgs&gt; value) {</p></li>\n</ul><p>// The loop and the call to CompareExchange is all just a fancy way // of adding a delegate to the event in a thread\u00adsafe way EventHandler&lt;NewMailEventArgs&gt;prevHandler;</p><p>}</p><p>EventHandler&lt;NewMailEventArgs&gt; newMail = this.NewMail; do {</p><p>prevHandler = newMail; EventHandler&lt;NewMailEventArgs&gt; newHandler =</p><p>(EventHandler&lt;NewMailEventArgs&gt;) Delegate.Combine(prevHandler, value); newMail = Interlocked.CompareExchange&lt;EventHandler&lt;NewMailEventArgs&gt;&gt;(</p><p>ref this.NewMail, newHandler, prevHandler); } while (newMail != prevHandler);</p><p>// 3. A PUBLIC remove_Xxx method (where Xxx is the Event name) // Allows methods to unregister interest in the event. public void remove_NewMail(EventHandler&lt;NewMailEventArgs&gt; value) {</p><p>// The loop and the call to CompareExchange is all just a fancy way // of removing a delegate from the event in a thread\u00adsafe way EventHandler&lt;NewMailEventArgs&gt; prevHandler; EventHandler&lt;NewMailEventArgs&gt; newMail = this.NewMail; do {</p><p>prevHandler = newMail; EventHandler&lt;NewMailEventArgs&gt; newHandler =</p><p>(EventHandler&lt;NewMailEventArgs&gt;) Delegate.Remove(prevHandler, value); newMail = Interlocked.CompareExchange&lt;EventHandler&lt;NewMailEventArgs&gt;&gt;(</p><p>ref this.NewMail, newHandler, prevHandler); } while (newMail != prevHandler);</p><p>}</p><p>The first construct is simply a field of the appropriate delegate type. This field is a reference to the head of a list of delegates that will be notified when this event occurs. This field is initialized to null, meaning that no listeners have registered interest in the event. When a method registers interest in the event, this field refers to an instance of the EventHandler&lt;NewMailEventArgs&gt; delegate, which may refer to additional EventHandler&lt;NewMailEventArgs&gt; delegates. When a listener registers interest in an event, the listener is simply adding an instance of the delegate type to the list. Obviously, unregistering means removing the delegate from the list.</p><p>You\u2019ll notice that the delegate field, NewMail in this example, is always private even though the original line of source code defines the event as public. The reason for making the delegate field private is to prevent code outside the defining class from manipulating it improperly. If the field were public, any code could alter the value in the field and potentially wipe out all of the delegates that have registered interest in the event.</p><p>The second construct the C# compiler generates is a method that allows other objects to register their interest in the event. The C# compiler automatically names this function by prepending add_ to the event\u2019s name (NewMail). The C# compiler automatically generates the code that is inside this method. The code always calls System.Delegate\u2019s static Combine method, which adds the instance of a delegate to the list of delegates and returns the new head of the list, which gets saved back in the field.</p><p>The third construct the C# compiler generates is a method that allows an object to unregister its interest in the event. Again, the C# compiler automatically names this function by prepending remove_ to the event\u2019s name (NewMail). The code inside this method always calls Delegate\u2019s static Remove method, which removes the instance of a delegate from the list of delegates and returns the new head of the list, which gets saved back in the field.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile92.png\"/><p>Warning If you attempt to remove a method that was never added, then Delegate\u2019s Remove method internally does nothing. That is, you get no exception or warning of any type; the event\u2019s collection of methods remains unchanged.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile93.png\"/><p>Note The add and remove methods use a well-known pattern to update a value in a thread-safe way. This pattern is discussed in the \u201cThe Interlocked Anything Pattern\u201d section of Chapter 29.</p><p>In this example, the add and remove methods are public. The reason they are public is that the original line of source code declared the event to be public. If the event had been declared protected, the add and remove methods generated by the compiler would also have been declared protected. So, when you define an event in a type, the accessibility of the event determines what code can register and unregister interest in the event, but only the type itself can ever access the delegate field directly. Event members can also be declared as static or virtual, in which case the add and remove methods generated by the compiler would be either static or virtual, respectively.</p><p>In addition to emitting the aforementioned three constructs, compilers also emit an event definition entry into the managed assembly\u2019s metadata. This entry contains some flags and the underlying delegate type, and refers to the add and remove accessor methods. This information exists simply to draw an association between the abstract concept of an \u201cevent\u201d and its accessor methods. Compilers and other tools can use this metadata, and this information can also be obtained by using the System.Reflection.EventInfo class. However, the CLR itself doesn\u2019t use this metadata information and requires only the accessor methods at run time.</p><h6>Designing a Type That Listens for an Event</h6><p>The hard work is definitely behind you at this point. In this section, I\u2019ll show you how to define a type that uses an event provided by another type. Let\u2019s start off by examining the code for the Fax type.</p><p>internal sealed class Fax { // Pass the MailManager object to the constructor public Fax(MailManager mm) {</p><p>}</p><p>// Construct an instance of the EventHandler&lt;NewMailEventArgs&gt; // delegate that refers to our FaxMsg callback method. // Register our callback with MailManager's NewMail event mm.NewMail += FaxMsg;</p><p>// This is the method the MailManager will call // when a new email message arrives private void FaxMsg(Object sender, NewMailEventArgs e) {</p><p>// 'sender' identifies the MailManager object in case // we want to communicate back to it.</p><p>// 'e' identifies the additional event information // the MailManager wants to give us.</p><p>// Normally, the code here would fax the email message. // This test implementation displays the info in the console Console.WriteLine(\"Faxing mail message:\"); Console.WriteLine(\" From={0}, To={1}, Subject={2}\",</p><p>e.From, e.To, e.Subject); }</p><p>// This method could be executed to have the Fax object unregister // itself with the NewMail event so that it no longer receives // notifications public void Unregister(MailManager mm) {</p><p>} }</p><p>// Unregister with MailManager's NewMail event mm.NewMail \u00ad= FaxMsg;</p><p>When the email application initializes, it would first construct a MailManager object and save the reference to this object in a variable. Then the application would construct a Fax object, passing the reference to the MailManager object as a parameter. In the Fax constructor, the Fax object registers its interest in MailManager\u2019s NewMail event using C#\u2019s += operator.</p><p>mm.NewMail += FaxMsg;</p><p>Because the C# compiler has built-in support for events, the compiler translates the use of the += operator into the following line of code to add the object\u2019s interest in the event.</p><p>mm.add_NewMail(new EventHandler&lt;NewMailEventArgs&gt;(this.FaxMsg));</p><p>As you can see, the C# compiler is generating code that will construct an EventHandler&lt;NewMailEventArgs&gt; delegate object that wraps the Fax class\u2019s FaxMsg method. Then, the C# compiler calls the MailManager\u2019s add_NewMail method, passing it the new delegate. Of course, you can verify all of this by compiling the code and looking at the IL with a tool such as ILDasm.exe.</p><p>Even if you\u2019re using a programming language that doesn\u2019t directly support events, you can still register a delegate with the event by calling the add accessor method explicitly. The effect is identical; the source code will just not look as pretty. It\u2019s the add method that registers the delegate with the event by adding it to the event\u2019s list of delegates.</p><p>When the MailManager object raises the event, the Fax object\u2019s FaxMsg method gets called. The method is passed a reference to the MailManager object as the first parameter, sender. Most of the time, this parameter is ignored, but it can be used if the Fax object wants to access members of the MailManager object in response to the event notification. The second parameter is a reference to a NewMailEventArgs object. This object contains any additional information the designer of MailManager and NewMailEventArgs thought would be useful to the event receivers.</p><p>From the NewMailEventArgs object, the FaxMsg method has easy access to the message\u2019s sender, the message\u2019s recipient, and the message\u2019s subject. In a real Fax object, this information would be faxed somewhere. In this example, the information is simply displayed in the console window.</p><p>When an object is no longer interested in receiving event notifications, it should unregister its interest. For example, the Fax object would unregister its interest in the NewMail event if the user no longer wanted his or her email forwarded to a fax. As long as an object has registered one of its methods with an event, the object can\u2019t be garbage collected. If your type implements IDisposable\u2019s Dispose method, the implementation should cause it to unregister interest in all events. (See Chapter 21, \u201cThe Managed Heap and Garbage Collection,\u201d for more information about IDisposable.)</p><p>Code that demonstrates how to unregister for an event is shown in Fax\u2019s Unregister method. This method is practically identical to the code shown in the Fax constructor. The only difference is that this code uses \u00ad= instead of +=. When the C# compiler sees code using the \u00ad= operator to unregister a delegate with an event, the compiler emits a call to the event\u2019s remove method.</p><p>mm.remove_NewMail(new EventHandler&lt;NewMailEventArgs&gt;(FaxMsg));</p><p>As with the += operator, even if you\u2019re using a programming language that doesn\u2019t directly support events, you can still unregister a delegate with the event by calling the remove accessor method explicitly. The remove method unregisters the delegate from the event by scanning the list for a delegate that wraps the same method as the one passed in. If a match is found, the existing delegate is removed from the event\u2019s list of delegates. If a match isn\u2019t found, no error occurs, and the list is unaltered.</p><p>By the way, C# requires your code to use the += and \u00ad= operators to add and remove delegates from the list. If you try to call the add or remove method explicitly, the C# compiler produces the CS0571 cannot explicitly call operator or accessor error message.</p><h6>Explicitly Implementing an Event</h6><p>The System.Windows.Forms.Control type defines about 70 events. If the Control type implemented the events by allowing the compiler to implicitly generate the add and remove accessor methods and delegate fields, every Control object would have 70 delegate fields in it just for the events! Because most programmers care about just a few events, an enormous amount of memory would be wasted for each object created from a Control-derived type. By the way, the ASP.NET System.Web.UI.Control and the Windows Presentation Foundation (WPF) System.Windows.UIElement type also offer many events that most programmers do not use.</p><p>In this section, I discuss how the C# compiler allows a class developer to explicitly implement an event, allowing the developer to control how the add and remove methods manipulate the callback delegates. I\u2019m going to demonstrate how explicitly implementing an event can be used to efficiently implement a class that offers many events. However, there are certainly other scenarios where you might want to explicitly implement a type\u2019s event.</p><p>To efficiently store event delegates, each object that exposes events will maintain a collection (usually a dictionary) with some sort of event identifier as the key and a delegate list as the value. When a new object is constructed, this collection is empty. When interest in an event is registered, the event\u2019s identifier is looked up in the collection. If the event identifier is there, the new delegate is combined with the list of delegates for this event. If the event identifier isn\u2019t in the collection, the event identifier is added with the delegate.</p><p>When the object needs to raise an event, the event identifier is looked up in the collection. If the collection doesn\u2019t have an entry for the event identifier, nothing has registered interest in the event and no delegates need to be called back. If the event identifier is in the collection, the delegate list associated with the event identifier is invoked. Implementing this design pattern is the responsibility of the developer who is designing the type that defines the events; the developer using the type has no idea how the events are implemented internally.</p><p>Here is an example of how you could accomplish this pattern. First, I implemented an EventSet class that represents a collection of events and each event\u2019s delegate list as follows.</p><p>using System; using System.Collections.Generic;</p><p>// This class exists to provide a bit more type safety and // code maintainability when using EventSet public sealed class EventKey { }</p><p>public sealed class EventSet {</p><p>// The private dictionary used to maintain EventKey \u00ad&gt; Delegate mappings private readonly Dictionary&lt;EventKey, Delegate&gt; m_events =</p><p>new Dictionary&lt;EventKey, Delegate&gt;();</p><p>// Adds an EventKey \u00ad&gt; Delegate mapping if it doesn't exist or // combines a delegate to an existing EventKey public void Add(EventKey eventKey, Delegate handler) {</p><p>Monitor.Enter(m_events); Delegate d; m_events.TryGetValue(eventKey, out d); m_events[eventKey] = Delegate.Combine(d, handler); Monitor.Exit(m_events);</p><p>}</p><p>// Removes a delegate from an EventKey (if it exists) and // removes the EventKey \u00ad&gt; Delegate mapping the last delegate is removed public void Remove(EventKey eventKey, Delegate handler) {</p><p>Monitor.Enter(m_events); // Call TryGetValue to ensure that an exception is not thrown if // attempting to remove a delegate from an EventKey not in the set Delegate d; if (m_events.TryGetValue(eventKey, out d)) {</p><p>d = Delegate.Remove(d, handler);</p><p>}</p><p>// If a delegate remains, set the new head else remove the EventKey if (d != null) m_events[eventKey] = d; else m_events.Remove(eventKey);</p><p>} Monitor.Exit(m_events);</p><p>// Raises the event for the indicated EventKey public void Raise(EventKey eventKey, Object sender, EventArgs e) {</p><p>// Don't throw an exception if the EventKey is not in the set Delegate d; Monitor.Enter(m_events); m_events.TryGetValue(eventKey, out d); Monitor.Exit(m_events);</p><p>if (d != null) { // Because the dictionary can contain several different delegate types, // it is impossible to construct a type\u00adsafe call to the delegate at // compile time. So, I call the System.Delegate type's DynamicInvoke // method, passing it the callback method's parameters as an array of // objects. Internally, DynamicInvoke will check the type safety of the // parameters with the callback method being called and call the method. // If there is a type mismatch, then DynamicInvoke will throw an exception. d.DynamicInvoke(new Object[] { sender, e }); }</p><p>}</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile94.png\"/><p>Note The FCL defines a type, System.Windows.EventHandlersStore, which does essen-</p><p>tially the same thing as my EventSet class. Various WPF types use the EventHandlersStore type internally to maintain their sparse set of events. You\u2019re certainly welcome to use the FCL\u2019s EventHandlersStore type if you\u2019d like. The big difference between the</p><p>EventHandlersStore type and my EventSet type is that EventHandlersStore doesn\u2019t offer any thread-safe way to access the events; you would have to implement your own thread-safe wrapper around the EventHandlersStore collection if you need to do this.</p><p>Now, I show a class that uses my EventSet class. This class has a field that refers to an EventSet object, and each of this class\u2019s events is explicitly implemented so that each event\u2019s add method stores the specified callback delegate in the EventSet object and each event\u2019s remove method eliminates the specified callback delegate (if found).</p><p>using System;</p><p>// Define the EventArgs\u00adderived type for this event. public class FooEventArgs : EventArgs { }</p><p>public class TypeWithLotsOfEvents {</p><p>// Define a private instance field that references a collection. // The collection manages a set of Event/Delegate pairs. // NOTE: The EventSet type is not part of the FCL, it is my own type. private readonly EventSet m_eventSet = new EventSet();</p><p>// The protected property allows derived types access to the collection. protected EventSet EventSet { get { return m_eventSet; } }</p><p>}</p><p>#region Code to support the Foo event (repeat this pattern for additional events) // Define the members necessary for the Foo event.</p><ul style=\"list-style-type: none;\">\n<li><p>// 2a. Construct a static, read\u00adonly object to identify this event. // Each object has its own hash code for looking up this // event's delegate linked list in the object's collection. protected static readonly EventKey s_fooEventKey = new EventKey();</p></li>\n<li><p>// 2b. Define the event's accessor methods that add/remove the // delegate from the collection. public event EventHandler&lt;FooEventArgs&gt; Foo {</p><p>add { m_eventSet.Add(s_fooEventKey, value); } remove { m_eventSet.Remove(s_fooEventKey, value); }</p>\n<p>}</p>\n</li>\n<li><p>// 2c. Define the protected, virtual On method for this event. protected virtual void OnFoo(FooEventArgs e) {</p><p>m_eventSet.Raise(s_fooEventKey, this, e); }</p>\n</li>\n<li><p>// 2d. Define the method that translates input to this event. public void SimulateFoo() { OnFoo(new FooEventArgs()); } #endregion</p></li>\n</ul><p>Code that uses the TypeWithLotsOfEvents type can\u2019t tell whether the events have been implemented implicitly by the compiler or explicitly by the developer. They just register the events by using normal syntax. Here is some code demonstrating this.</p><p>public sealed class Program { public static void Main() { TypeWithLotsOfEvents twle = new TypeWithLotsOfEvents();</p><p>// Add a callback here twle.Foo += HandleFooEvent;</p><p>}</p><p>// Prove that it worked twle.SimulateFoo();</p><p>}</p><p>private static void HandleFooEvent(object sender, FooEventArgs e) {</p><p>Console.WriteLine(\"Handling Foo Event here...\"); }</p><h6>C H A P T E R 1 2</h6><h2>Generics</h2><h6>In this chapter:</h6><p>Generics in the Framework Class Library . . . . . . . . . . . . . . . . . . . 270 Generics Infrastructure . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 271 Generic Interfaces. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 277 Generic Delegates. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 278 Delegate and Interface Contra-variant and Covariant Generic Type Arguments . . . . . . . . . . . . . . . . . . . . . . . . 279 Generic Methods . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 281 Generics and Other Members . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 284 Verifiability and Constraints. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 284</p><p>Developers who are familiar with object-oriented programming know the benefits it offers. One of the big benefits that make developers extremely productive is code reuse, which is the ability to derive a class that inherits all of the capabilities of a base class. The derived class can simply override virtual methods or add some new methods to customize the behavior of the base class to meet the developer\u2019s needs. Generics is another mechanism offered by the common language runtime (CLR) and programming languages that provides one more form of code reuse: algorithm reuse.</p><p>Basically, one developer defines an algorithm such as sorting, searching, swapping, comparing, or converting. However, the developer defining the algorithm doesn\u2019t specify what data type(s) the algorithm operates on; the algorithm can be generically applied to objects of different types. Another developer can then use this existing algorithm as long as he or she indicates the specific data type(s) the algorithm should operate on, for example, a sorting algorithm that operates on Int32s, Strings, etc., or a comparing algorithm that operates on DateTimes, Versions, etc.</p><p>Most algorithms are encapsulated in a type, and the CLR allows the creation of generic reference types as well as generic value types, but it does not allow the creation of generic enumerated types. In addition, the CLR allows the creation of generic interfaces and generic delegates. Occasionally, a single method can encapsulate a useful algorithm, and therefore, the CLR allows the creation of generic methods that are defined in a reference type, value type, or interface.</p><p>Let\u2019s look at a quick example. The Framework Class Library (FCL) defines a generic list algorithm that knows how to manage a set of objects; the data type of these objects is not specified by the generic algorithm. Someone wanting to use the generic list algorithm can specify the exact data type to use with it later.</p><p>265</p><p>The FCL class that encapsulates the generic list algorithm is called List&lt;T&gt; (pronounced List of Tee), and this class is defined in the System.Collections.Generic namespace. Here is what this class definition looks like (the code is severely abbreviated).</p><p>[Serializable] public class List&lt;T&gt; : IList&lt;T&gt;, ICollection&lt;T&gt;, IEnumerable&lt;T&gt;,</p><p>IList, ICollection, IEnumerable {</p><p>public List(); public void Add(T item); public Int32 BinarySearch(T item); public void Clear(); public Boolean Contains(T item); public Int32 IndexOf(T item); public Boolean Remove(T item); public void Sort(); public void Sort(IComparer&lt;T&gt; comparer); public void Sort(Comparison&lt;T&gt; comparison); public T[] ToArray();</p><p>}</p><p>public Int32 Count { get; } public T this[Int32 index] { get; set; }</p><p>The programmer who defined the generic List class indicates that it works with an unspecified data type by placing the &lt;T&gt; immediately after the class name. When defining a generic type or method, any variables it specifies for types (such as T) are called type parameters. T is a variable name that can be used in source code anywhere a data type can be used. For example, in the List class definition, you see T being used for method parameters (the Add method accepts a parameter of type T) and return types (the ToArray method returns a single-dimension array of type T). Another example is the indexer method (called this in C#). The indexer has a get accessor method that returns a value of type T and a set accessor method that accepts a parameter of type T. Because the T variable can be used anywhere that a data type can be specified, it is also possible to use T when defining local variables inside a method or when defining fields inside a type.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile95.png\"/><p>Note Microsoft\u2019s design guidelines state that generic parameter variables should either be called T or at least start with an uppercase T (as in TKey and TValue). The uppercase T stands for type, just as an uppercase I stands for interface (as in IComparable).</p><p>Now that the generic List&lt;T&gt; type has been defined, other developers can use this generic algorithm by specifying the exact data type they would like the algorithm to operate on. When using a generic type or method, the specified data types are referred to as type arguments. For example, a developer might want to work with the List algorithm by specifying a DateTime type argument.</p><p>Here is some code that shows this.</p><p>private static void SomeMethod() { // Construct a List that operates on DateTime objects List&lt;DateTime&gt; dtList = new List&lt;DateTime&gt;();</p><p>// Add a DateTime object to the list dtList.Add(DateTime.Now); // No boxing</p><p>// Add another DateTime object to the list dtList.Add(DateTime.MinValue); // No boxing</p><p>// Attempt to add a String object to the list dtList.Add(\"1/1/2004\"); // Compile\u00adtime error</p><p>}</p><p>// Extract a DateTime object out of the list DateTime dt = dtList[0]; // No cast required</p><p>Generics provide the following big benefits to developers as exhibited by the code just shown:</p><p>\u25a0\u25a0 Source code protection The developer using a generic algorithm doesn\u2019t need to have access to the algorithm\u2019s source code. With C++ templates, however, the algorithm\u2019s source code must be available to the developer who is using the algorithm.</p><p>\u25a0\u25a0 Type safety When a generic algorithm is used with a specific type, the compiler and the CLR understand this and ensure that only objects compatible with the specified data type are used with the algorithm. Attempting to use an object of an incompatible type will result in either a compiler error or a run-time exception being thrown. In the example, attempting to pass a String object to the Add method results in the compiler issuing an error.</p><p>\u25a0\u25a0 Cleaner code Because the compiler enforces type safety, fewer casts are required in your source code, meaning that your code is easier to write and maintain. In the last line of SomeMethod, a developer doesn\u2019t need to use a (DateTime) cast to put the result of the indexer (querying element at index 0) into the dt variable.</p><p>\u25a0\u25a0 Better performance Before generics, the way to define a generalized algorithm was to define all of its members to work with the Object data type. If you wanted to use the algorithm with value type instances, the CLR had to box the value type instance prior to calling the members of the algorithm. As discussed in Chapter 5, \u201cPrimitive, Reference, and Value Types,\u201d boxing causes memory allocations on the managed heap, which causes more frequent garbage collections, which, in turn, hurt an application\u2019s performance. Because a generic algorithm can now be created to work with a specific value type, the instances of the value type can be passed by value, and the CLR no longer has to do any boxing. In addition, because casts are not necessary (see the previous bullet), the CLR doesn\u2019t have to check the type safety of the attempted cast, and this results in faster code too.</p><p>To drive home the performance benefits of generics, I wrote a program that tests the performance of the generic List algorithm against the FCL\u2019s non-generic ArrayList algorithm. In fact, I tested the performance of these two algorithms by using both value type objects and reference type objects. Here is the program itself.</p><p>using System; using System.Collections; using System.Collections.Generic; using System.Diagnostics;</p><p>public static class Program {</p><p>public static void Main() { ValueTypePerfTest(); ReferenceTypePerfTest();</p><p>}</p><p>private static void ValueTypePerfTest() { const Int32 count = 100000000;</p><p>using (new OperationTimer(\"List&lt;Int32&gt;\")) { List&lt;Int32&gt; l = new List&lt;Int32&gt;(); for (Int32 n = 0; n &lt; count; n++) {</p><p>l.Add(n); // No boxing Int32 x = l[n]; // No unboxing</p><p>} l = null; // Make sure this gets garbage collected</p><p>}</p><p>using (new OperationTimer(\"ArrayList of Int32\")) { ArrayList a = new ArrayList(); for (Int32 n = 0; n &lt; count; n++) {</p><p>a.Add(n); // Boxing Int32 x = (Int32) a[n]; // Unboxing</p><p>} a = null; // Make sure this gets garbage collected</p><p>} }</p><p>private static void ReferenceTypePerfTest() { const Int32 count = 100000000;</p><p>using (new OperationTimer(\"List&lt;String&gt;\")) { List&lt;String&gt; l = new List&lt;String&gt;(); for (Int32 n = 0; n &lt; count; n++) {</p><p>l.Add(\"X\"); // Reference copy String x = l[n]; // Reference copy</p><p>} l = null; // Make sure this gets garbage collected</p><p>}</p><p>using (new OperationTimer(\"ArrayList of String\")) { ArrayList a = new ArrayList(); for (Int32 n = 0; n &lt; count; n++) {</p><p>a.Add(\"X\"); // Reference copy String x = (String) a[n]; // Cast check &amp; reference copy</p><p>}</p><p>} }</p><p>} a = null; // Make sure this gets garbage collected</p><p>// This class is useful for doing operation performance timing internal sealed class OperationTimer : IDisposable {</p><p>private Stopwatch m_stopwatch; private String m_text; private Int32 m_collectionCount;</p><p>public OperationTimer(String text) { PrepareForOperation();</p><p>m_text = text; m_collectionCount = GC.CollectionCount(0);</p><p>}</p><p>// This should be the last statement in this // method to keep timing as accurate as possible m_stopwatch = Stopwatch.StartNew();</p><p>public void Dispose() { Console.WriteLine(\"{0} (GCs={1,3}) {2}\", (m_stopwatch.Elapsed),</p><p>GC.CollectionCount(0) \u00ad m_collectionCount, m_text); }</p><p>private static void PrepareForOperation() { GC.Collect(); GC.WaitForPendingFinalizers(); GC.Collect();</p><p>} }</p><p>When I compile and run a release build (with optimizations turned on) of this program on my computer, I get the following output.</p><ul style=\"list-style-type: none;\">\n<li><p>00:00:01.6246959 (GCs= 6) List&lt;Int32&gt; 00:00:10.8555008 (GCs=390) ArrayList of Int32</p></li>\n<li><p>00:00:02.5427847 (GCs= 4) List&lt;String&gt; 00:00:02.7944831 (GCs= 7) ArrayList of String</p></li>\n</ul><p>The output here shows that using the generic List algorithm with the Int32 type is much faster than using the non-generic ArrayList algorithm with Int32. In fact, the difference is phenomenal: 1.6 seconds versus almost 11 seconds. That\u2019s ~7 times faster! In addition, using a value type (Int32) with ArrayList causes a lot of boxing operations to occur, which results in 390 garbage collections. Meanwhile, the List algorithm required 6 garbage collections.</p><p>The result of the test using reference types is not as momentous. Here we see that the times and number of garbage collections are about the same. So it doesn\u2019t appear that the generic List algorithm is of any benefit here. However, keep in mind that when using a generic algorithm, you also get cleaner code and compile-time type safety. So although the performance improvement is not huge, the other benefits you get when using a generic algorithm are usually an improvement.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile96.png\"/><p>Note You do need to realize that the CLR generates native code for each method the first time the method is called for a particular data type. This will increase an application\u2019s working set size, which will hurt performance. I will talk about this more in the \u201cGenerics Infrastructure\u201d section of this chapter.</p><h6>Generics in the Framework Class Library</h6><p>Certainly, the most obvious use of generics is with collection classes, and the FCL defines several generic collection classes available for your use. Most of these classes can be found in the System. Collections.Generic namespace and the System.Collections.ObjectModel namespace. There are also thread-safe generic collection classes available in the System.Collections.Concurrent namespace. Microsoft recommends that programmers use the generic collection classes and now discourages use of the non-generic collection classes for several reasons. First, the non-generic collection classes are not generic, and so you don\u2019t get the type safety, cleaner code, and better performance that you get when you use generic collection classes. Second, the generic classes have a better object model than the non-generic classes. For example, fewer methods are virtual, resulting in better performance, and new members have been added to the generic collections to provide new functionality.</p><p>The collection classes implement many interfaces, and the objects that you place into the collections can implement interfaces that the collection classes use for operations such as sorting and searching. The FCL ships with many generic interface definitions so that the benefits of generics can be realized when working with interfaces as well. The commonly used interfaces are contained in the System.Collections.Generic namespace.</p><p>The new generic interfaces are not a replacement for the old non-generic interfaces; in many sce-</p><p>narios, you will have to use both. The reason is backward compatibility. For example, if the List&lt;T&gt; class implemented only the IList&lt;T&gt; interface, no code could consider a List&lt;DateTime&gt; object an IList.</p><p>I should also point out that the System.Array class, the base class of all array types, offers many static generic methods, such as AsReadOnly, BinarySearch, ConvertAll, Exists, Find, FindAll, FindIndex, FindLast, FindLastIndex, ForEach, IndexOf, LastIndexOf, Resize, Sort, and TrueForAll. Here are examples showing what some of these methods look like.</p><p>public abstract class Array : ICloneable, IList, ICollection, IEnumerable, IStructuralComparable, IStructuralEquatable {</p><p>public static void Sort&lt;T&gt;(T[] array); public static void Sort&lt;T&gt;(T[] array, IComparer&lt;T&gt; comparer);</p><p>public static Int32 BinarySearch&lt;T&gt;(T[] array, T value); public static Int32 BinarySearch&lt;T&gt;(T[] array, T value,</p><p>IComparer&lt;T&gt; comparer);</p><p>... }</p><p>Here is code that demonstrates how to use some of these methods.</p><p>public static void Main() { // Create &amp; initialize a byte array Byte[] byteArray = new Byte[] { 5, 1, 4, 2, 3 };</p><p>// Call Byte[] sort algorithm Array.Sort&lt;Byte&gt;(byteArray);</p><p>}</p><p>// Call Byte[] binary search algorithm Int32 i = Array.BinarySearch&lt;Byte&gt;(byteArray, 1); Console.WriteLine(i); // Displays \"0\"</p><h6>Generics Infrastructure</h6><p>Generics were added to version 2.0 of the CLR, and it was a major task that required many people working for quite some time. Specifically, to make generics work, Microsoft had to do the following:</p><p>\u25a0\u25a0 Create new Intermediate Language (IL) instructions that are aware of type arguments.</p><p>\u25a0\u25a0 Modify the format of existing metadata tables so that type names and methods with generic</p><p>parameters could be expressed.</p><p>\u25a0\u25a0 Modify the various programming languages (C#, Microsoft Visual Basic .NET, etc.) to support the new syntax, allowing developers to define and reference generic types and methods.</p><p>\u25a0\u25a0 Modify the compilers to emit the new IL instructions and the modified metadata format.</p><p>\u25a0\u25a0 Modify the just-in-time (JIT) compiler to process the new type-argument\u2013aware IL instructions</p><p>that produce the correct native code.</p><p>\u25a0\u25a0 Create new reflection members so that developers can query types and members to determine if they have generic parameters. Also, new reflection emit members had to be defined so that developers could create generic type and method definitions at run time.</p><p>\u25a0\u25a0 Modify the debugger to show and manipulate generic types, members, fields, and local vari-</p><p>ables.</p><p>\u25a0\u25a0 Modify the Microsoft Visual Studio IntelliSense feature to show specific member prototypes</p><p>when using a generic type or a method with a specific data type.</p><p>Now let\u2019s spend some time discussing how the CLR handles generics internally. This information could impact how you architect and design a generic algorithm. It could also impact your decision to use an existing generic algorithm or not.</p><h6>Open and Closed Types</h6><p>In various chapters throughout this book, I have discussed how the CLR creates an internal data structure for each and every type in use by an application. These data structures are called type objects. Well, a type with generic type parameters is still considered a type, and the CLR will create an internal type object for each of these. This applies to reference types (classes), value types (structs), interface types, and delegate types. However, a type with generic type parameters is called an open type, and the CLR does not allow any instance of an open type to be constructed (similar to how the CLR prevents an instance of an interface type from being constructed).</p><p>When code references a generic type, it can specify a set of generic type arguments. If actual data types are passed in for all of the type arguments, the type is called a closed type, and the CLR does allow instances of a closed type to be constructed. However, it is possible for code referencing a generic type to leave some generic type arguments unspecified. This creates a new open type object in the CLR, and instances of this type cannot be created. The following code should make this clear.</p><p>using System; using System.Collections.Generic;</p><p>// A partially specified open type internal sealed class DictionaryStringKey&lt;TValue&gt; :</p><p>Dictionary&lt;String, TValue&gt; { }</p><p>public static class Program { public static void Main() { Object o = null;</p><p>// Dictionary&lt;,&gt; is an open type having 2 type parameters Type t = typeof(Dictionary&lt;,&gt;);</p><p>// Try to create an instance of this type (fails)</p><ul style=\"list-style-type: none;\">\n<li><p>o = CreateInstance(t); Console.WriteLine();</p><p>// DictionaryStringKey&lt;&gt; is an open type having 1 type parameter t = typeof(DictionaryStringKey&lt;&gt;);</p>\n<p>// Try to create an instance of this type (fails)</p>\n</li>\n<li><p>o = CreateInstance(t); Console.WriteLine();</p><p>// DictionaryStringKey&lt;Guid&gt; is a closed type t = typeof(DictionaryStringKey&lt;Guid&gt;);</p>\n<p>// Try to create an instance of this type (succeeds)</p>\n</li>\n<li><p>o = CreateInstance(t);</p></li>\n</ul><p>}</p><p>// Prove it actually worked Console.WriteLine(\"Object type=\" + o.GetType());</p><p>private static Object CreateInstance(Type t) {</p><p>} }</p><p>Object o = null; try {</p><p>o = Activator.CreateInstance(t); Console.Write(\"Created instance of {0}\", t.ToString());</p><p>} catch (ArgumentException e) {</p><p>Console.WriteLine(e.Message);</p><p>} return o;</p><p>When I compile the preceding code and run it, I get the following output.</p><p>Cannot create an instance of System.Collections.Generic. Dictionary`2[TKey,TValue] because Type.ContainsGenericParameters is true.</p><p>Cannot create an instance of DictionaryStringKey`1[TValue] because Type.ContainsGenericParameters is true.</p><p>Created instance of DictionaryStringKey`1[System.Guid] Object type=DictionaryStringKey`1[System.Guid]</p><p>As you can see, Activator\u2019s CreateInstance method throws an ArgumentException when you ask it to construct an instance of an open type. In fact, the exception\u2019s string message indicates that the type still contains some generic parameters.</p><p>In the output, you\u2019ll notice that the type names end with a backtick (`) followed by a number. The number indicates the type\u2019s arity, which indicates the number of type parameters required by the type. For example, the Dictionary class has an arity of 2 because it requires that types be specified for TKey and TValue. The DictionaryStringKey class has an arity of 1 because it requires just one type to be specified for TValue.</p><p>I should also point out that the CLR allocates a type\u2019s static fields inside the type object (as discussed in Chapter 4, \u201cType Fundamentals\u201d). So each closed type has its own static fields. In other words, if List&lt;T&gt; defined any static fields, these fields are not shared between a List&lt;DateTime&gt; and a List&lt;String&gt;; each closed type object has its own static fields. Also, if a generic type defines a static constructor (discussed in Chapter 8, \u201cMethods\u201d), this constructor will execute once per closed type. Sometimes people define a static constructor on a generic type to ensure that the type arguments will meet certain criteria. For example, if you wanted to define a generic type that can be used only with enumerated types, you could do the following.</p><p>internal sealed class GenericTypeThatRequiresAnEnum&lt;T&gt; { static GenericTypeThatRequiresAnEnum() { if (!typeof(T).IsEnum) {</p><p>throw new ArgumentException(\"T must be an enumerated type\"); }</p><p>} }</p><p>The CLR has a feature called constraints that offers a better way for you to define a generic type indicating what type arguments are valid for it. I\u2019ll discuss constraints later in this chapter. Unfortunately, constraints do not support the ability to limit a type argument to enumerated types only, which is why the previous example requires a static constructor to ensure that the type is an enumerated type.</p><h6>Generic Types and Inheritance</h6><p>A generic type is a type, and as such, it can be derived from any other type. When you use a generic type and specify type arguments, you are defining a new type object in the CLR, and the new type object is derived from whatever type the generic type was derived from. In other words, because List&lt;T&gt; is derived from Object, List&lt;String&gt; and List&lt;Guid&gt; are also derived from Object. Similarly, because DictionaryStringKey&lt;TValue&gt; is derived from Dictionary&lt;String, TValue&gt;, DictionaryStringKey&lt;Guid&gt; is also derived from Dictionary&lt;String, Guid&gt;. Understanding that specifying type arguments doesn\u2019t have anything to do with inheritance hierarchies will help you to recognize what kind of casting you can and can\u2019t do.</p><p>For example, if a linked-list node class is defined like this:</p><p>internal sealed class Node&lt;T&gt; { public T m_data; public Node&lt;T&gt; m_next;</p><p>public Node(T data) : this(data, null) { }</p><p>public Node(T data, Node&lt;T&gt; next) { m_data = data; m_next = next;</p><p>}</p><p>}</p><p>public override String ToString() { return m_data.ToString() +</p><p>((m_next != null) ? m_next.ToString() : String.Empty); }</p><p>then I can write some code to build up a linked list that would look something like the following.</p><p>private static void SameDataLinkedList() { Node&lt;Char&gt; head = new Node&lt;Char&gt;('C'); head = new Node&lt;Char&gt;('B', head); head = new Node&lt;Char&gt;('A', head); Console.WriteLine(head.ToString()); // Displays \"ABC\"</p><p>}</p><p>In the Node class just shown, the m_next field must refer to another node that has the same kind of data type in its m_data field. This means that the linked list must contain nodes in which all data items are of the same type (or derived type). For example, I can\u2019t use the Node class to create a linked list in which one element contains a Char, another element contains a DateTime, and another</p><p>element contains a String. Well, I could if I use Node&lt;Object&gt; everywhere, but then I would lose compile-time type safety, and value types would get boxed.</p><p>So a better way to go would be to define a non-generic Node base class and then define a generic TypedNode class (using the Node class as a base class). Now, I can have a linked list in which each node can be of a specific data type (not Object), get compile-time type safety, and avoid the boxing of value types. Here are the new class definitions.</p><p>internal class Node { protected Node m_next;</p><p>}</p><p>public Node(Node next) {</p><p>m_next = next; }</p><p>internal sealed class TypedNode&lt;T&gt; : Node { public T m_data;</p><p>public TypedNode(T data) : this(data, null) { }</p><p>public TypedNode(T data, Node next) : base(next) {</p><p>m_data = data; }</p><p>}</p><p>public override String ToString() { return m_data.ToString() +</p><p>((m_next != null) ? m_next.ToString() : String.Empty); }</p><p>I can now write code to create a linked list in which each node is a different data type. The code could look something like the following.</p><p>private static void DifferentDataLinkedList() { Node head = new TypedNode&lt;Char&gt;('.'); head = new TypedNode&lt;DateTime&gt;(DateTime.Now, head); head = new TypedNode&lt;String&gt;(\"Today is \", head); Console.WriteLine(head.ToString());</p><p>}</p><h6>Generic Type Identity</h6><p>Sometimes generic syntax confuses developers. After all, there can be a lot of less-than (&lt;) and greater-than (&gt;) signs sprinkled throughout your source code, and this hurts readability. To improve syntax, some developers define a new non-generic class type that is derived from a generic type and that specifies all of the type arguments. For example, to simplify code like this:</p><p>List&lt;DateTime&gt; dtl = new List&lt;DateTime&gt;();</p><p>some developers might first define a class like the following.</p><p>internal sealed class DateTimeList : List&lt;DateTime&gt; {</p><p>// No need to put any code in here! }</p><p>Now, the code that creates a list can be rewritten more simply (without less-than and greater-than signs) like the following.</p><p>DateTimeList dtl = new DateTimeList();</p><p>Although this seems like a convenience, especially if you use the new type for parameters, local variables, and fields, you should never define a new class explicitly for the purpose of making your source code easier to read. The reason is because you lose type identity and equivalence, as you can see in the following code.</p><p>Boolean sameType = (typeof(List&lt;DateTime&gt;) == typeof(DateTimeList));</p><p>When the preceding code runs, sameType will be initialized to false because you are comparing two different type objects. This also means that a method prototyped as accepting a DateTimeList will not be able to have a List&lt;DateTime&gt; passed to it. However, a method prototyped as accepting a List&lt;DateTime&gt; can have a DateTimeList passed to it because DateTimeList is derived from List&lt;DateTime&gt;. Programmers may become easily confused by all of this.</p><p>Fortunately, C# does offer a way to use simplified syntax to refer to a generic closed type while not affecting type equivalence at all; you can use the good-old using directive at the top of your source code file. Here is an example.</p><p>using DateTimeList = System.Collections.Generic.List&lt;System.DateTime&gt;;</p><p>Here, the using directive is really just defining a symbol called DateTimeList. As the code compiles, the compiler substitutes all occurrences of DateTimeList with System.Collections. Generic.List&lt;System.DateTime&gt;. This just allows developers to use a simplified syntax without affecting the actual meaning of the code, and therefore, type identity and equivalence are maintained. So now, when the following line executes, sameType will be initialized to true.</p><p>Boolean sameType = (typeof(List&lt;DateTime&gt;) == typeof(DateTimeList));</p><p>As another convenience, you can use C#\u2019s implicitly typed local variable feature, where the compiler infers the type of a method\u2019s local variable from the type of the expression you are assigning to it.</p><p>using System; using System.Collections.Generic;</p><p>... internal sealed class SomeType {</p><p>private static void SomeMethod () {</p><p>// Compiler infers that dtl is of type // System.Collections.Generic.List&lt;System.DateTime&gt; var dtl = new List&lt;DateTime&gt;();</p><p>... }</p><p>}</p><h6>Code Explosion</h6><p>When a method that uses generic type parameters is JIT-compiled, the CLR takes the method\u2019s IL, substitutes the specified type arguments, and then creates native code that is specific to that method operating on the specified data types. This is exactly what you want and is one of the main features of generics. However, there is a downside to this: the CLR keeps generating native code for every method/type combination. This is referred to as code explosion. This can end up increasing the application\u2019s working set substantially, thereby hurting performance.</p><p>Fortunately, the CLR has some optimizations built into it to reduce code explosion. First, if a method is called for a particular type argument, and later, the method is called again using the same type argument, the CLR will compile the code for this method/type combination just once. So if one assembly uses List&lt;DateTime&gt;, and a completely different assembly (loaded in the same AppDomain) also uses List&lt;DateTime&gt;, the CLR will compile the methods for List&lt;DateTime&gt; just once. This reduces code explosion substantially.</p><p>The CLR has another optimization: the CLR considers all reference type arguments to be identical, and so again, the code can be shared. For example, the code compiled by the CLR for List&lt;String&gt;\u2019s methods can be used for List&lt;Stream&gt;\u2019s methods, because String and Stream are both reference types. In fact, for any reference type, the same code will be used. The CLR can perform this optimization because all reference type arguments or variables are really just pointers (all 32 bits on a 32-bit Windows system and 64 bits on a 64-bit Windows system) to objects on the heap, and object pointers are all manipulated in the same way.</p><p>But if any type argument is a value type, the CLR must produce native code specifically for that value type. The reason is because value types can vary in size. And even if two value types are the same size (such as Int32 and UInt32, which are both 32 bits), the CLR still can\u2019t share the code because different native CPU instructions can be used to manipulate these values.</p><h6>Generic Interfaces</h6><p>Obviously, the ability to define generic reference and value types was the main feature of generics. However, it was critical for the CLR to also allow generic interfaces. Without generic interfaces, any time you tried to manipulate a value type by using a non-generic interface (such as IComparable), boxing and a loss of compile-time type safety would happen again. This would severely limit the usefulness of generic types. And so the CLR does support generic interfaces. A reference or value type</p><p>can implement a generic interface by specifying type arguments, or a type can implement a generic interface by leaving the type arguments unspecified. Let\u2019s look at some examples.</p><p>Here is the definition of a generic interface that ships as part of the FCL (in the System.Collections.Generic namespace).</p><p>public interface IEnumerator&lt;T&gt; : IDisposable, IEnumerator {</p><p>T Current { get; } }</p><p>Here is an example of a type that implements this generic interface and that specifies type arguments. Notice that a Triangle object can enumerate a set of Point objects. Also note that the Current property is of the Point data type.</p><p>internal sealed class Triangle : IEnumerator&lt;Point&gt; { private Point[] m_vertices;</p><p>// IEnumerator&lt;Point&gt;'s Current property is of type Point public Point Current { get { ... } }</p><p>... }</p><p>Now let\u2019s look at an example of a type that implements the same generic interface but with the type arguments left unspecified.</p><p>internal sealed class ArrayEnumerator&lt;T&gt; : IEnumerator&lt;T&gt; { private T[] m_array;</p><p>// IEnumerator&lt;T&gt;'s Current property is of type T public T Current { get { ... } }</p><p>... }</p><p>Notice that an ArrayEnumerator object can enumerate a set of T objects (where T is unspecified allowing code using the generic ArrayEnumerator type to specify a type for T later). Also note that the Current property is now of the unspecified data type T. Much more information about generic interfaces is presented in Chapter 13, \u201cInterfaces.\u201d</p><h6>Generic Delegates</h6><p>The CLR supports generic delegates to ensure that any type of object can be passed to a callback method in a type-safe way. Furthermore, generic delegates allow a value type instance to be passed to a callback method without any boxing. As discussed in Chapter 17, \u201cDelegates,\u201d a delegate is really just a class definition with four methods: a constructor, an Invoke method, a BeginInvoke method, and an EndInvoke method. When you define a delegate type that specifies type parameters, the compiler defines the delegate class\u2019s methods, and the type parameters are applied to any methods having parameters/return types of the specified type parameter.</p><p>For example, if you define a generic delegate like this:</p><p>public delegate TReturn CallMe&lt;TReturn, TKey, TValue&gt;(TKey key, TValue value);</p><p>the compiler turns that into a class that logically looks like the following.</p><p>public sealed class CallMe&lt;TReturn, TKey, TValue&gt; : MulticastDelegate { public CallMe(Object object, IntPtr method); public virtual TReturn Invoke(TKey key, TValue value); public virtual IAsyncResult BeginInvoke(TKey key, TValue value,</p><p>AsyncCallback callback, Object object);</p><p>public virtual TReturn EndInvoke(IAsyncResult result); }</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile97.png\"/><p>Note It is recommended that you use the generic Action and Func delegates that come predefined in the Framework Class Library (FCL) wherever possible. I describe these delegate types in the \u201cEnough with the Delegate Definitions Already (Generic Delegates)\u201d section of Chapter 17.</p><h6>Delegate and Interface Contra-variant and Covariant Generic Type Arguments</h6><p>Each of a delegate\u2019s generic type parameters can be marked as covariant or contra-variant. This feature allows you to cast a variable of a generic delegate type to the same delegate type where the generic parameter types differ. A generic type parameter can be any one of the following:</p><p>\u25a0\u25a0 Invariant Meaning that the generic type parameter cannot be changed. I have shown only</p><p>invariant generic type parameters so far in this chapter.</p><p>\u25a0\u25a0 Contra-variant Meaning that the generic type parameter can change from a class to a class derived from it. In C#, you indicate contra-variant generic type parameters with the in keyword. Contra-variant generic type parameters can appear only in input positions such as a method\u2019s argument.</p><p>\u25a0\u25a0 Covariant Meaning that the generic type argument can change from a class to one of its base classes. In C#, you indicate covariant generic type parameters with the out keyword. Covariant generic type parameters can appear only in output positions such as a method\u2019s return type.</p><p>For example, let\u2019s say that the following delegate type definition exists (which, by the way, it does).</p><p>public delegate TResult Func&lt;in T, out TResult&gt;(T arg);</p><p>Here, the generic type parameter T is marked with the in keyword, making it contra-variant; and the generic type parameter TResult is marked with the out keyword, making it covariant.</p><p>So now, if I have a variable declared as follows.</p><p>Func&lt;Object, ArgumentException&gt; fn1 = null;</p><p>I can cast it to another Func type, where the generic type parameters are different.</p><p>Func&lt;String, Exception&gt;fn2 = fn1; // No explicit cast is required here Exception e = fn2(\"\");</p><p>What this is saying is that fn1 refers to a function that accepts an Object and returns an ArgumentException. The fn2 variable wants to refer to a method that takes a String and returns an Exception. Because you can pass a String to a method that wants an Object (because String is derived from Object), and because you can take the result of a method that returns an ArgumentException and treat it as an Exception (because Exception is a base class of ArgumentException), the code above compiles and is known at compile time to preserve type safety.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile98.png\"/><p>Note Variance applies only if the compiler can verify that a reference conversion exists between types. In other words, variance is not possible for value types because boxing would be required. In my opinion, this restriction is what makes these variance features not that useful. For example, if I have the following method.</p><p>void ProcessCollection(IEnumerable&lt;Object&gt; collection) { ... }</p><p>I can\u2019t call it passing in a reference to a List&lt;DateTime&gt; object because a reference conversion doesn\u2019t exist between the DateTime value type and Object even though DateTime is derived from Object. You solve this problem by declaring ProcessCollection as follows.</p><p>void ProcessCollection&lt;T&gt;(IEnumerable&lt;T&gt; collection) { ... }</p><p>Plus, the big benefit of ProcessCollection(IEnumerable&lt;Object&gt; collection) is that there is only one version of the JITted code. However, with ProcessCollection&lt;T&gt;\u00ad</p><p>(IEnumerable&lt;T&gt;collection), there is also only one version of the JITted code shared by all Ts that are reference types. You do get other versions of JITted code for Ts that are value types, but now you can at least call the method passing it a collection of value types.</p><p>Also, variance is not allowed on a generic type parameter if an argument of that type is passed to a method by using the out or ref keyword. For example, the line of code below causes the compiler to generate the following error message: Invalid variance:</p><p>The type parameter 'T' must be invariantly valid on 'SomeDelegate&lt;T&gt;. Invoke(ref T)'. 'T' is contravariant.</p><p>delegate void SomeDelegate&lt;in T&gt;(ref T t);</p><p>When using delegates that take generic arguments and return types, it is recommended to always specify the in and out keywords for contra-variance and covariance whenever possible, because doing this has no ill effects and enables your delegate to be used in more scenarios.</p><p>Like delegates, an interface with generic type parameters can have its type parameters be contravariant or covariant. Here is an example of an interface with a covariant generic type parameter.</p><p>public interface IEnumerator&lt;out T&gt; : IEnumerator { Boolean MoveNext(); T Current { get; }</p><p>}</p><p>Because T is covariant, it is possible to have the following code compile and run successfully.</p><p>// This method accepts an IEnumerable of any reference type Int32 Count(IEnumerable&lt;Object&gt; collection) { ... }</p><p>... // The call below passes an IEnumerable&lt;String&gt; to Count Int32 c = Count(new[] { \"Grant\" });</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile99.png\"/><p>Important Sometimes developers ask why they must explicitly put in or out on generic type parameters. They think the compiler should be able to examine the delegate or interface declaration and automatically detect what generic type parameters can be contravariant and covariant. Although it is true that the compiler could detect this automatically, the C# team believes that you are declaring a contract and that you should be explicit about what you want to allow. For example, it would be bad if the compiler determined that a generic type parameter could be contra-variant and then, in the future, you added a member to an interface that had the type parameter used in an output position. The next time you compile, the compiler would determine that the type parameter should be invariant, but all code sites that reference the other members might now produce errors if they had used the fact that the type parameter had been contra-variant.</p><p>For this reason, the compiler team forces you to be explicit when declaring a generic type parameter. Then, if you attempt to use this type parameter in a context that doesn\u2019t match how you declared it, the compiler issues an error letting you know that you are attempting to break the contract. If you then decide to break the contract by adding in or out on generic type parameters, you should expect to have to modify some of the code sites that were using the old contract.</p><h6>Generic Methods</h6><p>When you define a generic class, struct, or interface, any methods defined in these types can refer to a type parameter specified by the type. A type parameter can be used as a method\u2019s parameter, a method\u2019s return type, or as a local variable defined inside the method. However, the CLR also supports the ability for a method to specify its very own type parameters. And these type parameters can also be used for parameters, return types, or local variables.</p><p>Here is a somewhat contrived example of a type that defines a type parameter and a method that has its very own type parameter.</p><p>internal sealed class GenericType&lt;T&gt; { private T m_value;</p><p>public GenericType(T value) { m_value = value; }</p><p>public TOutput Converter&lt;TOutput&gt;() { TOutput result = (TOutput) Convert.ChangeType(m_value, typeof(TOutput)); return result;</p><p>} }</p><p>In this example, you can see that the GenericType class defines its own type parameter (T), and the Converter method defines its own type parameter (TOutput). This allows a GenericType to be constructed to work with any type. The Converter method can convert the object referred to by the m_value field to various types depending on what type argument is passed to it when called. The ability to have type parameters and method parameters allows for phenomenal flexibility.</p><p>A reasonably good example of a generic method is the Swap method.</p><p>private static void Swap&lt;T&gt;(ref T o1, ref T o2) { T temp = o1; o1 = o2; o2 = temp;</p><p>}</p><p>Code can now call Swap like as follows.</p><p>private static void CallingSwap() { Int32 n1 = 1, n2 = 2; Console.WriteLine(\"n1={0}, n2={1}\", n1, n2); Swap&lt;Int32&gt;(ref n1, ref n2); Console.WriteLine(\"n1={0}, n2={1}\", n1, n2);</p><p>}</p><p>String s1 = \"Aidan\", s2 = \"Grant\"; Console.WriteLine(\"s1={0}, s2={1}\", s1, s2); Swap&lt;String&gt;(ref s1, ref s2); Console.WriteLine(\"s1={0}, s2={1}\", s1, s2);</p><p>Using generic types with methods that take out and ref parameters can be particularly interesting because the variable you pass as an out/ref argument must be the same type as the method\u2019s parameter to avoid a potential type safety exploit. This issue related to out/ref parameters is discussed toward the end of the \u201cPassing Parameters by Reference to a Method\u201d section in Chapter 9, \u201cParameters.\u201d In fact, the Interlocked class\u2019s Exchange and CompareExchange methods offer generic overloads for precisely this reason.1</p><p>1 The where clause will be explained in the \u201cVerifiability and Constraints\u201d section later in this chapter.</p><p>public static class Interlocked { public static T Exchange&lt;T&gt;(ref T location1, T value) where T: class; public static T CompareExchange&lt;T&gt;(</p><p>ref T location1, T value, T comparand) where T: class; }</p><h6>Generic Methods and Type Inference</h6><p>For many developers, the C# generic syntax can be confusing with all of its less-than and greaterthan signs. To help improve code creation, readability, and maintainability, the C# compiler offers type inference when calling a generic method. Type inference means that the compiler attempts to</p><p>determine (or infer) the type to use automatically when calling a generic method. Here is some code that demonstrates type inference.</p><p>private static void CallingSwapUsingInference() { Int32 n1 = 1, n2 = 2; Swap(ref n1, ref n2);// Calls Swap&lt;Int32&gt;</p><p>}</p><p>String s1 = \"Aidan\"; Object s2 = \"Grant\"; Swap(ref s1, ref s2);// Error, type can't be inferred</p><p>In this code, notice that the calls to Swap do not specify type arguments in less-than/greater-than signs. In the first call to Swap, the C# compiler was able to infer that n1 and n2 are Int32s, and therefore, it should call Swap by using an Int32 type argument.</p><p>When performing type inference, C# uses the variable\u2019s data type, not the actual type of the</p><p>object referred to by the variable. So in the second call to Swap, C# sees that s1 is a String and s2 is an Object (even though it happens to refer to a String). Because s1 and s2 are variables of different data types, the compiler can\u2019t accurately infer the type to use for Swap\u2019s type argument, and it issues the following message: error CS0411: The type arguments for method 'Program.</p><p>Swap&lt;T&gt;(ref T, ref T)' cannot be inferred from the usage. Try specifying the type arguments explicitly.</p><p>A type can define multiple methods with one of its methods taking a specific data type and another taking a generic type parameter, as in the following example.</p><p>private static void Display(String s) {</p><p>Console.WriteLine(s); }</p><p>private static void Display&lt;T&gt;(T o) {</p><p>Display(o.ToString()); // Calls Display(String) }</p><p>Here are some ways to call the Display method.</p><p>Display(\"Jeff\"); // Calls Display(String) Display(123); // Calls Display&lt;T&gt;(T) Display&lt;String&gt;(\"Aidan\"); // Calls Display&lt;T&gt;(T)</p><p>In the first call, the compiler could actually call either the Display method that takes a String or the generic Display method (replacing T with String). However, the C# compiler always prefers a more explicit match over a generic match, and therefore, it generates a call to the nongeneric Display method that takes a String. For the second call, the compiler can\u2019t call the non-generic Display method that takes a String, so it must call the generic Display method. By the way, it is fortunate that the compiler always prefers the more explicit match; if the compiler had preferred the generic method, because the generic Display method calls Display again (but with a String returned by ToString), there would have been infinite recursion.</p><p>The third call to Display specifies a generic type argument, String. This tells the compiler not to try to infer type arguments but instead to use the type arguments that I explicitly specified. In this case, the compiler also assumes that I must really want to call the generic Display method, so the generic Display will be called. Internally, the generic Display method will call ToString on the passed-in string, which results in a string that is then passed to the non-generic Display method.</p><h6>Generics and Other Members</h6><p>In C#, properties, indexers, events, operator methods, constructors, and finalizers cannot themselves have type parameters. However, they can be defined within a generic type, and the code in these members can use the type\u2019s type parameters.</p><p>C# doesn\u2019t allow these members to specify their own generic type parameters because Microsoft\u2019s C# team believes that developers would rarely have a need to use these members as generic. Furthermore, the cost of adding generic support to these members would be quite high in terms of designing adequate syntax into the language. For example, when you use a + operator in code, the compiler could call an operator overload method. There is no way to indicate any type arguments in your code along with the + operator.</p><h6>Verifiability and Constraints</h6><p>When compiling generic code, the C# compiler analyzes it and ensures that the code will work for any type that exists today or that may be defined in the future. Let\u2019s look at the following method.</p><p>private static Boolean MethodTakingAnyType&lt;T&gt;(T o) { T temp = o; Console.WriteLine(o.ToString()); Boolean b = temp.Equals(o); return b;</p><p>}</p><p>This method declares a temporary variable (temp) of type T, and then the method performs a couple of variable assignments and a few method calls. This method works for any type. If T is a reference type, it works. If T is a value or enumeration type, it works. If T is an interface or delegate type, it works. This method works for all types that exist today or that will be defined tomorrow because</p><p>every type supports assignment and calls to methods defined by Object (such as ToString and Equals).</p><p>Now look at the following method.</p><p>private static T Min&lt;T&gt;(T o1, T o2) { if (o1.CompareTo(o2) &lt; 0) return o1; return o2;</p><p>}</p><p>The Min method attempts to use the o1 variable to call the CompareTo method. But there are lots of types that do not offer a CompareTo method, and therefore, the C# compiler can\u2019t compile this code and guarantee that this method would work for all types. If you attempt to compile the above code, the compiler issues the following message: error CS1061: 'T' does not contain a definition for 'CompareTo' accepting a first argument of type 'T' could be found (are you missing a using directive or an assembly reference?)</p><p>So it would seem that when using generics, you can declare variables of a generic type, perform some variable assignments, call methods defined by Object, and that\u2019s about it! This makes generics practically useless. Fortunately, compilers and the CLR support a mechanism called constraints that you can take advantage of to make generics useful again.</p><p>A constraint is a way to limit the number of types that can be specified for a generic argument. Limiting the number of types allows you to do more with those types. Here is a new version of the Min method that specifies a constraint (in bold).</p><p>public static T Min&lt;T&gt;(T o1, T o2) where T : IComparable&lt;T&gt; { if (o1.CompareTo(o2) &lt; 0) return o1; return o2;</p><p>}</p><p>The C# where token tells the compiler that any type specified for T must implement the generic IComparable interface of the same type (T). Because of this constraint, the compiler now allows the method to call the CompareTo method because this method is defined by the IComparable&lt;T&gt; interface.</p><p>Now, when code references a generic type or method, the compiler is responsible for ensuring that a type argument that meets the constraints is specified. For example, the following code causes the compiler to issue the following message: error CS0311: The type 'object' cannot be used as type parameter 'T' in the generic type or method 'SomeType.Min&lt;T&gt;(T, T)'. There is no implicit reference conversion from 'object' to 'System. IComparable&lt;object&gt;'.</p><p>private static void CallMin() { Object o1 = \"Jeff\", o2 = \"Richter\"; Object oMin = Min&lt;Object&gt;(o1, o2); // Error CS0311</p><p>}</p><p>The compiler issues the error because System.Object doesn\u2019t implement the IComparable&lt;Object&gt; interface. In fact, System.Object doesn\u2019t implement any interfaces at all.</p><p>Now that you have a sense of what constraints are and how they work, we\u2019ll start to look a little deeper into them. Constraints can be applied to a generic type\u2019s type parameters as well as to a generic method\u2019s type parameters (as shown in the Min method). The CLR doesn\u2019t allow overloading based on type parameter names or constraints; you can overload types or methods based only on arity. The following examples show what I mean.</p><p>// It is OK to define the following types: internal sealed class AType {} internal sealed class AType&lt;T&gt; {} internal sealed class AType&lt;T1, T2&gt; {}</p><p>// Error: conflicts with AType&lt;T&gt; that has no constraints internal sealed class AType&lt;T&gt; where T : IComparable&lt;T&gt; {}</p><p>// Error: conflicts with AType&lt;T1, T2&gt; internal sealed class AType&lt;T3, T4&gt; {}</p><p>internal sealed class AnotherType { // It is OK to define the following methods: private static void M() {} private static void M&lt;T&gt;() {} private static void M&lt;T1, T2&gt;() {}</p><p>// Error: conflicts with M&lt;T&gt; that has no constraints private static void M&lt;T&gt;() where T : IComparable&lt;T&gt; {}</p><p>}</p><p>// Error: conflicts with M&lt;T1, T2&gt; private static void M&lt;T3, T4&gt;() {}</p><p>When overriding a virtual generic method, the overriding method must specify the same number of type parameters, and these type parameters will inherit the constraints specified on them by the base class\u2019s method. In fact, the overriding method is not allowed to specify any constraints on its type parameters at all. However, it can change the names of the type parameters. Similarly, when implementing an interface method, the method must specify the same number of type parameters as the interface method, and these type parameters will inherit the constraints specified on them by the interface\u2019s method. Here is an example that demonstrates this rule by using virtual methods.</p><p>internal class Base {</p><p>public virtual void M&lt;T1, T2&gt;() where T1 : struct where T2 : class {</p><p>} }</p><p>internal sealed class Derived : Base { public override void M&lt;T3, T4&gt;()</p><ul style=\"list-style-type: none;\">\n<li><p>where T3 : EventArgs // Error</p></li>\n<li><p>where T4 : class // Error { }</p></li>\n</ul><p>}</p><p>Attempting to compile the preceding code causes the compiler to issue the following message: error CS0460: Constraints for override and explicit interface implementation methods are inherited from the base method, so they cannot be specified directly. If we remove the two where lines from the Derived class\u2019s M&lt;T3, T4&gt; method, the code will compile just fine. Notice that you can change the names of the type parameters (as in the example: from T1 to T3 and T2 to T4); however, you cannot change (or even specify) constraints.</p><p>Now let\u2019s talk about the different kinds of constraints the compiler/CLR allows you to apply to a type parameter. A type parameter can be constrained by using a primary constraint, a secondary constraint, and/or a constructor constraint. I\u2019ll talk about these three kinds of constraints in the next three sections.</p><h6>Primary Constraints</h6><p>A type parameter can specify zero primary constraints or one primary constraint. A primary constraint can be a reference type that identifies a class that is not sealed. You cannot specify one of the following special reference types: System.Object, System.Array, System.Delegate, System.MulticastDelegate, System.ValueType, System.Enum, or System.Void.</p><p>When specifying a reference type constraint, you are promising the compiler that a specified type argument will either be of the same type or of a type derived from the constraint type. For example, see the following generic class.</p><p>internal sealed class PrimaryConstraintOfStream&lt;T&gt; where T : Stream { public void M(T stream) {</p><p>stream.Close();// OK }</p><p>}</p><p>In this class definition, the type parameter T has a primary constraint of Stream (defined in the System.IO namespace). This tells the compiler that code using PrimaryConstraintOfStream</p><p>must specify a type argument of Stream or a type derived from Stream (such as FileStream). If a type parameter doesn\u2019t specify a primary constraint, System.Object is assumed. However, the C# compiler issues an error message (error CS0702: Constraint cannot be special class</p><p>'object') if you explicitly specify System.Object in your source code.</p><p>There are two special primary constraints: class and struct. The class constraint promises the compiler that a specified type argument will be a reference type. Any class type, interface type, delegate type, or array type satisfies this constraint. For example, see the following generic class.</p><p>internal sealed class PrimaryConstraintOfClass&lt;T&gt; where T : class { public void M() {</p><p>T temp = null;// Allowed because T must be a reference type }</p><p>}</p><p>In this example, setting temp to null is legal because T is known to be a reference type, and all reference type variables can be set to null. If T were unconstrained, the preceding code would not compile because T could be a value type, and value type variables cannot be set to null.</p><p>The struct constraint promises the compiler that a specified type argument will be a value type. Any value type, including enumerations, satisfies this constraint. However, the compiler and the CLR treat any System.Nullable&lt;T&gt; value type as a special type, and nullable types do not satisfy this constraint. The reason is because the Nullable&lt;T&gt; type constrains its type parameter to struct, and the CLR wants to prohibit a recursive type such as Nullable&lt;Nullable&lt;T&gt;&gt;. Nullable types are discussed in Chapter 19, \u201cNullable Value Types.\u201d</p><p>Here is an example class that constrains its type parameter by using the struct constraint.</p><p>internal sealed class PrimaryConstraintOfStruct&lt;T&gt; where T : struct {</p><p>public static T Factory() { // Allowed because all value types implicitly // have a public, parameterless constructor return new T();</p><p>} }</p><p>In this example, newing up a T is legal because T is known to be a value type, and all value types implicitly have a public, parameterless constructor. If T were unconstrained, constrained to a reference type, or constrained to class, the above code would not compile because some reference types do not have public, parameterless constructors.</p><h6>Secondary Constraints</h6><p>A type parameter can specify zero or more secondary constraints where a secondary constraint represents an interface type. When specifying an interface type constraint, you are promising the compiler that a specified type argument will be a type that implements the interface. And because you can specify multiple interface constraints, the type argument must specify a type that implements all of the interface constraints (and all of the primary constraints too, if specified). Chapter 13 discusses interface constraints in detail.</p><p>There is another kind of secondary constraint called a type parameter constraint (sometimes referred to as a naked type constraint). This kind of constraint is used much less often than an interface constraint. It allows a generic type or method to indicate that there must be a relationship between specified type arguments. A type parameter can have zero or more type constraints applied to it. Here is a generic method that demonstrates the use of a type parameter constraint.</p><p>private static List&lt;TBase&gt; ConvertIList&lt;T, TBase&gt;(IList&lt;T&gt; list) where T : TBase { List&lt;TBase&gt; baseList = new List&lt;TBase&gt;(list.Count); for (Int32 index = 0; index &lt; list.Count; index++) {</p><p>baseList.Add(list[index]);</p><p>} return baseList;</p><p>}</p><p>The ConvertIList method specifies two type parameters in which the T parameter is constrained by the TBase type parameter. This means that whatever type argument is specified for T, the type argument must be compatible with whatever type argument is specified for TBase. Here is a method showing some legal and illegal calls to ConvertIList.</p><p>private static void CallingConvertIList() { // Construct and initialize a List&lt;String&gt; (which implements IList&lt;String&gt;) IList&lt;String&gt; ls = new List&lt;String&gt;(); ls.Add(\"A String\");</p><p>// Convert the IList&lt;String&gt; to an IList&lt;Object&gt; IList&lt;Object&gt; lo = ConvertIList&lt;String, Object&gt;(ls);</p><p>// Convert the IList&lt;String&gt; to an IList&lt;IComparable&gt; IList&lt;IComparable&gt; lc = ConvertIList&lt;String, IComparable&gt;(ls);</p><p>// Convert the IList&lt;String&gt; to an IList&lt;IComparable&lt;String&gt;&gt; IList&lt;IComparable&lt;String&gt;&gt; lcs =</p><p>ConvertIList&lt;String, IComparable&lt;String&gt;&gt;(ls);</p><p>// Convert the IList&lt;String&gt; to an IList&lt;String&gt; IList&lt;String&gt; ls2 = ConvertIList&lt;String, String&gt;(ls);</p><p>}</p><p>// Convert the IList&lt;String&gt; to an IList&lt;Exception&gt; IList&lt;Exception&gt; le = ConvertIList&lt;String, Exception&gt;(ls);// Error</p><p>In the first call to ConvertIList, the compiler ensures that String is compatible with Object. Because String is derived from Object, the first call adheres to the type parameter constraint. In the second call to ConvertIList, the compiler ensures that String is compatible with IComparable. Because String implements the IComparable interface, the second call adheres to the type parameter constraint. In the third call to ConvertIList, the compiler ensures that String is compatible with</p><p>IComparable&lt;String&gt;. Because String implements the IComparable&lt;String&gt; interface, the third call adheres to the type parameter constraint. In the fourth call to ConvertIList, the compiler knows that String is compatible with itself. In the fifth call to ConvertIList, the compiler ensures that</p><p>String is compatible with Exception. Because String is not compatible with Exception, the fifth call doesn\u2019t adhere to the type parameter constraint, and the compiler issues the following message:</p><p>error CS0311: The type 'string' cannot be used as type parameter 'T' in the generic type or method Program.ConvertIList&lt;T,TBase&gt;(System.Collections.Generic.IList&lt;T&gt;)'. There is no implicit reference conversion from 'string' to 'System.Exception'.</p><h6>Constructor Constraints</h6><p>A type parameter can specify zero constructor constraints or one constructor constraint. When specifying a constructor constraint, you are promising the compiler that a specified type argument will be a non-abstract type that implements a public, parameterless constructor. Note that the C# compiler considers it an error to specify a constructor constraint with the struct constraint because it is</p><p>redundant; all value types implicitly offer a public, parameterless constructor. Here is an example class that constrains its type parameter by using the constructor constraint.</p><p>internal sealed class ConstructorConstraint&lt;T&gt; where T : new() {</p><p>public static T Factory() { // Allowed because all value types implicitly // have a public, parameterless constructor and because // the constraint requires that any specified reference // type also have a public, parameterless constructor return new T();</p><p>} }</p><p>In this example, newing up a T is legal because T is known to be a type that has a public, parameterless constructor. This is certainly true of all value types, and the constructor constraint requires that it be true of any reference type specified as a type argument.</p><p>Sometimes developers would like to declare a type parameter by using a constructor constraint whereby the constructor takes various parameters itself. As of now, the CLR (and therefore the C# compiler) supports only parameterless constructors. Microsoft feels that this will be good enough for almost all scenarios, and I agree.</p><h6>Other Verifiability Issues</h6><p>In the remainder of this section, I\u2019d like to point out a few other code constructs that have unexpected behavior when used with generics due to verifiability issues and how constraints can be used to make the code verifiable again.</p><h6>Casting a Generic Type Variable</h6><p>Casting a generic type variable to another type is illegal unless you are casting to a type compatible with a constraint.</p><ul style=\"list-style-type: none;\">\n<li><p>private static void CastingAGenericTypeVariable1&lt;T&gt;(T obj) { Int32 x = (Int32) obj; // Error String s = (String) obj; // Error</p><p>}</p>\n<p>The compiler issues an error on both lines above because T could be any type, and there is no guarantee that the casts will succeed. You can modify this code to get it to compile by casting to Object first.</p>\n</li>\n<li><p>private static void CastingAGenericTypeVariable2&lt;T&gt;(T obj) { Int32 x = (Int32) (Object) obj; // No error String s = (String) (Object) obj; // No error</p></li>\n</ul><p>}</p><p>Although this code will now compile, it is still possible for the CLR to throw an InvalidCastException at run time.</p><p>If you are trying to cast to a reference type, you can also use the C# as operator. Here is code modified to use the as operator with String (because Int32 is a value type).</p><p>private static void CastingAGenericTypeVariable3&lt;T&gt;(T obj) {</p><p>String s = obj as String; // No error }</p><h6>Setting a Generic Type Variable to a Default Value</h6><p>Setting a generic type variable to null is illegal unless the generic type is constrained to a reference type.</p><p>private static void SettingAGenericTypeVariableToNull&lt;T&gt;() { T temp = null; // CS0403 \u2013 Cannot convert null to type parameter 'T' because it could</p><p>// be a non\u00adnullable value type. Consider using 'default(T)' instead }</p><p>Because T is unconstrained, it could be a value type, and setting a variable of a value type to null is not possible. If T were constrained to a reference type, setting temp to null would compile and run just fine.</p><p>Microsoft\u2019s C# team felt that it would be useful to give developers the ability to set a variable to a default value. So the C# compiler allows you to use the default keyword to accomplish this.</p><p>private static void SettingAGenericTypeVariableToDefaultValue&lt;T&gt;() {</p><p>T temp = default(T); // OK }</p><p>The use of the default keyword above tells the C# compiler and the CLR\u2019s JIT compiler to produce code to set temp to null if T is a reference type and to set temp to all-bits-zero if T is a value type.</p><p>Comparing a Generic Type Variable with null Comparing a generic type variable to null by using the == or != operator is legal regardless of whether the generic type is constrained.</p><p>private static void ComparingAGenericTypeVariableWithNull&lt;T&gt;(T obj) {</p><p>if (obj == null) { /* Never executes for a value type */ } }</p><p>Because T is unconstrained, it could be a reference type or a value type. If T is a value type, obj can never be null. Normally, you\u2019d expect the C# compiler to issue an error because of this. However, the C# compiler does not issue an error; instead, it compiles the code just fine. When this method is called using a type argument that is a value type, the JIT compiler sees that the if statement can never be true, and the JIT compiler will not emit the native code for the if test or the code in the braces. If I had used the != operator, the JIT compiler would not emit the code for the if test (because it is always true), and it will emit the code inside the if\u2019s braces.</p><p>By the way, if T had been constrained to a struct, the C# compiler would issue an error because you shouldn\u2019t be writing code that compares a value type variable with null because the result is always the same.</p><h6>Comparing Two Generic Type Variables with Each Other</h6><p>Comparing two variables of the same generic type is illegal if the generic type parameter is not known to be a reference type.</p><p>private static void ComparingTwoGenericTypeVariables&lt;T&gt;(T o1, T o2) {</p><p>if (o1 == o2) { } // Error }</p><p>In this example, T is unconstrained, and whereas it is legal to compare two reference type variables with one another, it is not legal to compare two value type variables with one another unless the value type overloads the == operator. If T were constrained to class, this code would compile, and the == operator would return true if the variables referred to the same object, checking for exact identity. Note that if T were constrained to a reference type that overloaded the operator == method, the compiler would emit calls to this method when it sees the == operator. Obviously, this whole discussion applies to uses of the != operator too.</p><p>When you write code to compare the primitive value types\u2014Byte, Int32, Single, Decimal, etc.\u2014the C# compiler knows how to emit the right code. However, for non-primitive value types, the C# compiler doesn\u2019t know how to emit the code to do comparisons. So if ComparingTwoGenericTypeVariables method\u2019s T were constrained to struct, the compiler would issue an error. And you\u2019re not allowed to constrain a type parameter to a specific value type because it is implicitly sealed, and therefore no types exist that are derived from the value type. Allowing this would make the generic method constrained to a specific type, and the C# compiler doesn\u2019t allow this because it is more efficient to just make a non-generic method.</p><h6>Using Generic Type Variables as Operands</h6><p>Finally, it should be noted that there are a lot of issues about using operators with generic type operands. In Chapter 5, I talked about C# and how it handles its primitive types: Byte, Int16, Int32, Int64, Decimal, and so on. In particular, I mentioned that C# knows how to interpret operators (such as +, \u00ad, *, and /) when applied to the primitive types. Well, these operators can\u2019t be applied to variables of a generic type because the compiler doesn\u2019t know the type at compile time. This means that you can\u2019t use any of these operators with variables of a generic type. So it is impossible to write a mathematical algorithm that works on an arbitrary numeric data type. Here is an example of a generic method that I\u2019d like to write.</p><p>private static T Sum&lt;T&gt;(T num) where T : struct { T sum = default(T) ; for (T n = default(T) ; n &lt; num ; n++)</p><p>sum += n; return sum;</p><p>}</p><p>I\u2019ve done everything possible to try to get this method to compile. I\u2019ve constrained T to struct, and I\u2019m using default(T) to initialize sum and n to 0. But when I compile this code, I get the following three errors.</p><p>\u25a0\u25a0 error CS0019: Operator '&lt;' cannot be applied to operands of type 'T'</p><p>and 'T'</p><p>\u25a0\u25a0 error CS0023: Operator '++' cannot be applied to operand of type 'T'</p><p>\u25a0\u25a0 error CS0019: Operator '+=' cannot be applied to operands of type 'T'</p><p>and 'T'</p><p>This is a severe limitation on the CLR\u2019s generic support, and many developers (especially in the scientific, financial, and mathematical world) are very disappointed by this limitation. Many people have tried to come up with techniques to work around this limitation by using reflection (see Chapter 23, \u201cAssembly Loading and Reflection\u201d), the dynamic primitive type (see Chapter 5), operator overloading, and so on. But all of these cause a severe performance penalty or hurt readability of the code substantially. Hopefully, this is an area that Microsoft will address in a future version of the CLR and the compilers.</p><h6>C H A P T E R 1 3</h6><h2>Interfaces</h2><h6>In this chapter:</h6><p>Class and Interface Inheritance. . . . . . . . . . . . . . . . . . . . . . . . . . . . 296 Defining an Interface. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 296 Inheriting an Interface. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 298 More About Calling Interface Methods . . . . . . . . . . . . . . . . . . . . 300 Implicit and Explicit Interface Method Implementations (What\u2019s Happening Behind the Scenes) . . . . . . . . . . . . . . . . . . . . 301 Generic Interfaces. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 303 Generics and Interface Constraints . . . . . . . . . . . . . . . . . . . . . . . . 305 Implementing Multiple Interfaces That Have the Same Method Name and Signature . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 307 Improving Compile-Time Type Safety with Explicit Interface Method Implementations. . . . . . . . . . . . . . . . . . . . . . . . 308 Be Careful with Explicit Interface Method Implementations. . 310 Design: Base Class or Interface? . . . . . . . . . . . . . . . . . . . . . . . . . . . 312</p><p>Many programmers are familiar with the concept of multiple inheritance: the ability to define a class that is derived from two or more base classes. For example, imagine a class named TransmitData, whose function is to transmit data, and another class named ReceiveData, whose function is to receive data. Now imagine that you want to create a class named SocketPort, whose function is to transmit and receive data. In order to accomplish this, you would want to derive SocketPort from both TransmitData and ReceiveData.</p><p>Some programming languages allow multiple inheritance, making it possible for the SocketPort class to be derived from the two base classes, TransmitData and ReceiveData. However, the common language runtime (CLR)\u2014and therefore all managed programming languages\u2014does not support multiple inheritance. Rather than not offer any kind of multiple inheritance at all, the CLR does offer scaled-down multiple inheritance via interfaces. This chapter will discuss how to define and use interfaces as well as provide some guidelines to help you determine when to use an interface rather than a base class.</p><p>295</p><h6>Class and Interface Inheritance</h6><p>In the Microsoft .NET Framework, there is a class called System.Object that defines four public instance methods: ToString, Equals, GetHashCode, and GetType. This class is the root or ultimate base class of all other classes\u2014all classes will inherit Object\u2019s four instance methods. This also means that code written to operate on an instance of the Object class can actually perform operations on an instance of any class.</p><p>Because someone at Microsoft has implemented Object\u2019s methods, any class derived from Object is actually inheriting the following:</p><p>\u25a0\u25a0 The method signatures This allows code to think that it is operating on an instance of the</p><p>Object class, when in fact, it could be operating on an instance of some other class.</p><p>\u25a0\u25a0 The implementation of these methods This allows the developer defining a class derived</p><p>from Object not to be required to manually implement Object\u2019s methods.</p><p>In the CLR, a class is always derived from one and only one class (that must ultimately be derived from Object). This base class provides a set of method signatures and implementations for these methods. And a cool thing about defining a new class is that it can become the base class for another class defined in the future by some other developer\u2014all of the method signatures and their implementations will be inherited by the new derived class.</p><p>The CLR also allows developers to define an interface, which is really just a way to give a name to a set of method signatures. These methods do not come with any implementation at all. A class inherits an interface by specifying the interface\u2019s name, and the class must explicitly provide implementations of the interface\u2019s methods before the CLR will consider the type definition to be valid. Of course, implementing interface methods can be tedious, which is why I referred to interface inheritance as a scaled-down mechanism to achieve multiple inheritance. The C# compiler and the CLR actually allow a class to inherit several interfaces, and of course, the class must provide implementations for all of the inherited interface methods.</p><p>One of the great features of class inheritance is that it allows instances of a derived type to be substituted in all contexts that expect instances of a base type. Similarly, interface inheritance allows instances of a type that implements the interface to be substituted in all contexts that expect instances of the named interface type. We will now look at how to define interfaces to make our discussion more concrete.</p><h6>Defining an Interface</h6><p>As mentioned in the previous section, an interface is a named set of method signatures. Note that interfaces can also define events, parameterless properties, and parameterful properties (indexers in C#) because all of these are just syntax shorthands that map to methods anyway, as shown in previous chapters. However, an interface cannot define any constructor methods. In addition, an interface is not allowed to define any instance fields.</p><p>Although the CLR does allow an interface to define static methods, static fields, constants, and static constructors, a Common Language Specification (CLS)\u2013compliant interface must not have any of these static members because some programming languages aren\u2019t able to define or access them. In fact, C# prevents an interface from defining any of these static members.</p><p>In C#, you use the interface keyword to define an interface, giving it a name and its set of instance method signatures. Here are the definitions of a few interfaces defined in the Framework Class Library (FCL).</p><p>public interface IDisposable {</p><p>void Dispose(); }</p><p>public interface IEnumerable {</p><p>IEnumerator GetEnumerator(); }</p><p>public interface IEnumerable&lt;out T&gt; : IEnumerable {</p><p>new IEnumerator&lt;T&gt; GetEnumerator(); }</p><p>public interface ICollection&lt;T&gt; : IEnumerable&lt;T&gt;, IEnumerable { void Add(T item); void Clear(); Boolean Contains(T item); void CopyTo(T[] array, Int32 arrayIndex); Boolean Remove(T item); Int32 Count { get; } // Read\u00adonly property Boolean IsReadOnly { get; } // Read\u00adonly property</p><p>}</p><p>To the CLR, an interface definition is just like a type definition. That is, the CLR will define an internal data structure for the interface type object, and reflection can be used to query features of the interface type. Like types, an interface can be defined at file scope or defined nested within another type. When defining the interface type, you can specify whatever visibility/accessibility (public, protected, internal, etc.) you want.</p><p>By convention, interface type names are prefixed with an uppercase I, making it easy to spot an interface type in source code. The CLR does support generic interfaces (as you can see from some of the previous examples) as well as generic methods in an interface. I will discuss some of the many features offered by generic interfaces later in this chapter and in Chapter 12, \u201cGenerics,\u201d in which I cover generics more broadly.</p><p>An interface definition can \u201cinherit\u201d other interfaces. However, I use the word inherit here rather loosely because interface inheritance doesn\u2019t work exactly like class inheritance. I prefer to think of interface inheritance as including the contract of other interfaces. For example, the ICollection&lt;T&gt; interface definition includes the contracts of the IEnumerable&lt;T&gt; and IEnumerable interfaces.</p><p>This means that:</p><p>\u25a0\u25a0 Any class that inherits the ICollection&lt;T&gt; interface must implement all of the methods</p><p>defined by the ICollection&lt;T&gt;, IEnumerable&lt;T&gt;, and IEnumerable interfaces.</p><p>\u25a0\u25a0 Any code that expects an object whose type implements the ICollection&lt;T&gt; interface can assume that the object\u2019s type also implements the methods of the IEnumerable&lt;T&gt; and IEnumerable interfaces.</p><h6>Inheriting an Interface</h6><p>In this section, I\u2019ll show how to define a type that implements an interface, and then I\u2019ll show how to create an instance of this type and use the object to call the interface\u2019s methods. C# actually makes this pretty simple, but what happens behind the scenes is a bit more complicated. I\u2019ll explain what is happening behind the scenes later in this chapter.</p><p>The System.IComparable&lt;T&gt; interface is defined (in MSCorLib.dll) as follows.</p><p>public interface IComparable&lt;in T&gt; {</p><p>Int32 CompareTo(T other); }</p><p>The following code shows how to define a type that implements this interface and also shows code that compares two Point objects.</p><p>using System;</p><p>// Point is derived from System.Object and implements IComparable&lt;T&gt; for Point. public sealed class Point : IComparable&lt;Point&gt; {</p><p>private Int32 m_x, m_y;</p><p>public Point(Int32 x, Int32 y) {</p><ul style=\"list-style-type: none;\">\n<li><p>m_x = x;</p></li>\n<li><p>m_y = y;</p></li>\n</ul><p>}</p><p>// This method implements IComparable&lt;T&gt;.CompareTo() for Point public Int32 CompareTo(Point other) {</p><p>return Math.Sign(Math.Sqrt(m_x * m_x + m_y * m_y)</p><p>\u00ad Math.Sqrt(other.m_x * other.m_x + other.m_y * other.m_y)); }</p><p>}</p><p>public override String ToString() {</p><p>return String.Format(\"({0}, {1})\", m_x, m_y); }</p><p>public static class Program { public static void Main() { Point[] points = new Point[] {</p><p>new Point(3, 3), new Point(1, 2),</p><p>};</p><p>} }</p><p>// Here is a call to Point's IComparable&lt;T&gt; CompareTo method if (points[0].CompareTo(points[1]) &gt; 0) {</p><p>Point tempPoint = points[0]; points[0] = points[1]; points[1] = tempPoint;</p><p>} Console.WriteLine(\"Points from closest to (0, 0) to farthest:\"); foreach (Point p in points) Console.WriteLine(p);</p><p>The C# compiler requires that a method that implements an interface be marked as public. The CLR requires that interface methods be marked as virtual. If you do not explicitly mark the method as virtual in your source code, the compiler marks the method as virtual and sealed; this prevents a derived class from overriding the interface method. If you explicitly mark the method as virtual, the compiler marks the method as virtual (and leaves it unsealed); this allows a derived class to override the interface method.</p><p>If an interface method is sealed, a derived class cannot override the method. However, a derived class can re-inherit the same interface and can provide its own implementation for the interface\u2019s methods. When calling an interface\u2019s method on an object, the implementation associated with the object\u2019s type is called. Here is an example that demonstrates this.</p><p>using System;</p><p>public static class Program {</p><p>public static void Main() { /************************* First Example *************************/ Base b = new Base();</p><p>// Calls Dispose by using b's type: \"Base's Dispose\" b.Dispose();</p><p>// Calls Dispose by using b's object's type: \"Base's Dispose\" ((IDisposable)b).Dispose();</p><p>/************************* Second Example ************************/ Derived d = new Derived();</p><p>// Calls Dispose by using d's type: \"Derived's Dispose\" d.Dispose();</p><p>// Calls Dispose by using d's object's type: \"Derived's Dispose\" ((IDisposable)d).Dispose();</p><p>/************************* Third Example *************************/ b = new Derived();</p><p>// Calls Dispose by using b's type: \"Base's Dispose\" b.Dispose();</p><p>} }</p><p>// Calls Dispose by using b's object's type: \"Derived's Dispose\" ((IDisposable)b).Dispose();</p><p>// This class is derived from Object and it implements IDisposable internal class Base : IDisposable {</p><p>// This method is implicitly sealed and cannot be overridden public void Dispose() {</p><p>Console.WriteLine(\"Base's Dispose\"); }</p><p>}</p><p>// This class is derived from Base and it re\u00adimplements IDisposable internal class Derived : Base, IDisposable {</p><p>// This method cannot override Base's Dispose. 'new' is used to indicate // that this method re\u00adimplements IDisposable's Dispose method new public void Dispose() {</p><p>Console.WriteLine(\"Derived's Dispose\");</p><p>} }</p><p>// NOTE: The next line shows how to call a base class's implementation (if desired) // base.Dispose();</p><h6>More About Calling Interface Methods</h6><p>The FCL\u2019s System.String type inherits System.Object\u2019s method signatures and their implementations. In addition, the String type also implements several interfaces: IComparable, ICloneable, IConvertible, IEnumerable, IComparable&lt;String&gt;, IEnumerable&lt;Char&gt;, and IEquatable&lt;String&gt;. This means that the String type isn\u2019t required to implement (or override) the methods its Object base type offers. However, the String type must implement the methods declared in all of the interfaces.</p><p>The CLR allows you to define field, parameter, or local variables that are of an interface type. Using a variable of an interface type allows you to call methods defined by that interface. In addition, the CLR will allow you to call methods defined by Object because all classes inherit Object\u2019s methods. The following code demonstrates this.</p><p>// The s variable refers to a String object. String s = \"Jeffrey\"; // Using s, I can call any method defined in // String, Object, IComparable, ICloneable, IConvertible, IEnumerable, etc.</p><p>// The cloneable variable refers to the same String object ICloneable cloneable = s;</p><p>// Using cloneable, I can call any method declared by the // ICloneable interface (or any method defined by Object) only.</p><p>// The comparable variable refers to the same String object IComparable comparable = s; // Using comparable, I can call any method declared by the // IComparable interface (or any method defined by Object) only.</p><p>// The enumerable variable refers to the same String object // At run time, you can cast a variable from one interface to another as // long as the object's type implements both interfaces. IEnumerable enumerable = (IEnumerable) comparable; // Using enumerable, I can call any method declared by the // IEnumerable interface (or any method defined by Object) only.</p><p>In this code, all of the variables refer to the same \u201cJeffrey\u201d String object that is in the managed heap, and therefore, any method that I call while using any of these variables applies to the one \u201cJeffrey\u201d String object. However, the type of the variable indicates the action that I can perform on the object. The s variable is of type String, and therefore, I can use s to call any members defined by the String type (such as the Length property). I can also use the variable s to call any methods inherited from Object (such as GetType).</p><p>The cloneable variable is of the ICloneable interface type, and therefore, using the cloneable variable, I can call the Clone method defined by this interface. In addition, I can call any method defined by Object (such as GetType) because the CLR knows that all types derive from Object. However, using the cloneable variable, I cannot call public methods defined by String itself or any methods defined by any other interface that String implements. Similarly, using the comparable variable, I can call CompareTo or any method defined by Object, but no other methods are callable using this variable.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile100.png\"/><p>Important Like a reference type, a value type can implement zero or more interfaces. However, when you cast an instance of a value type to an interface type, the value type instance must be boxed. This is because an interface variable is a reference that must point to an object on the heap so that the CLR can examine the object\u2019s type object pointer to determine the exact type of the object. Then, when calling an interface method with a boxed value type, the CLR will follow the object\u2019s type object pointer to find the type object\u2019s method table in order to call the proper method.</p><h6>Implicit and Explicit Interface Method Implementations (What\u2019s Happening Behind the Scenes)</h6><p>When a type is loaded into the CLR, a method table is created and initialized for the type (as discussed in Chapter 1, \u201cThe CLR\u2019s Execution Model\u201d). This method table contains one entry for every new method introduced by the type as well as entries for any virtual methods inherited by the type.</p><p>Inherited virtual methods include methods defined by the base types in the inheritance hierarchy as well as any methods defined by the interface types. So if you have a simple type defined like this:</p><p>internal sealed class SimpleType : IDisposable {</p><p>public void Dispose() { Console.WriteLine(\"Dispose\"); } }</p><p>the type\u2019s method table contains entries for the following:</p><p>\u25a0\u25a0 All the virtual instance methods defined by Object, the implicitly inherited base class.</p><p>\u25a0\u25a0 All the interface methods defined by IDisposable, the inherited interface. In this example, there is only one method, Dispose, because the IDisposable interface defines just one method.</p><p>\u25a0\u25a0 The new method, Dispose, introduced by SimpleType. To make things simple for the programmer, the C# compiler assumes that the Dispose method</p><p>introduced by SimpleType is the implementation for IDisposable\u2019s Dispose method. The C# compiler makes this assumption because the method is public, and the signatures of the interface method and the newly introduced method are identical. That is, the methods have the same parameter and return types. By the way, if the new Dispose method were marked as virtual, the C# compiler would still consider this method to be a match for the interface method.</p><p>When the C# compiler matches a new method to an interface method, it emits metadata indicating that both entries in SimpleType\u2019s method table should refer to the same implementation. To help make this clearer, here is some code that demonstrates how to call the class\u2019s public Dispose method as well as how to call the class\u2019s implementation of IDisposable\u2019s Dispose method.</p><p>public sealed class Program { public static void Main() { SimpleType st = new SimpleType();</p><p>// This calls the public Dispose method implementation st.Dispose();</p><p>} }</p><p>// This calls IDisposable's Dispose method implementation IDisposable d = st; d.Dispose();</p><p>In the first call to Dispose, the Dispose method defined by SimpleType is called. Then I define a variable, d, which is of the IDisposable interface type. I initialize the d variable to refer to the SimpleType object. Now when I call d.Dispose(), I am calling the IDisposable interface\u2019s Dispose method. Because C# requires the public Dispose method to also be the implementation for IDisposable\u2019s Dispose method, the same code will execute, and, in this example, you can\u2019t see any observable difference. The output is as follows.</p><p>Dispose Dispose</p><p>Now, let me rewrite the preceding SimpleType so that you can see an observable difference.</p><p>internal sealed class SimpleType : IDisposable { public void Dispose() { Console.WriteLine(\"public Dispose\"); } void IDisposable.Dispose() { Console.WriteLine(\"IDisposable Dispose\"); }</p><p>}</p><p>Without changing the Main method shown earlier, if we just recompile and rerun the program, the output will be the following.</p><p>public Dispose IDisposable Dispose</p><p>In C#, when you prefix the name of a method with the name of the interface that defines the method (IDisposable.Dispose as in this example), you are creating an explicit interface method implementation (EIMI). Note that when you define an explicit interface method in C#, you are not allowed to specify any accessibility (such as public or private). However, when the compiler generates the metadata for the method, its accessibility is set to private, preventing any code using an instance of the class from simply calling the interface method. The only way to call the interface method is through a variable of the interface\u2019s type.</p><p>Also note that an EIMI method cannot be marked as virtual and therefore cannot be overridden. This is because the EIMI method is not really part of the type\u2019s object model; it\u2019s a way of attaching an interface (set of behaviors or methods) onto a type without making the behaviors/methods obvious. If all of this seems a bit kludgy to you, you are understanding it correctly\u2014this is all a bit kludgy. Later in this chapter, I\u2019ll show some valid reasons for using EIMIs.</p><h6>Generic Interfaces</h6><p>C#\u2019s and the CLR\u2019s support of generic interfaces offers many great features for developers. In this section, I\u2019d like to discuss the benefits offered when using generic interfaces.</p><p>First, generic interfaces offer great compile-time type safety. Some interfaces (such as the nongeneric IComparable interface) define methods that have Object parameters or return types. When code calls these interface methods, a reference to an instance of any type can be passed. But this is usually not desired. The following code demonstrates.</p><p>private void SomeMethod1() { Int32 x = 1, y = 2; IComparable c = x;</p><p>// CompareTo expects an Object; passing y (an Int32) is OK c.CompareTo(y); // y is boxed here</p><p>}</p><p>// CompareTo expects an Object; passing \"2\" (a String) compiles // but an ArgumentException is thrown at runtime c.CompareTo(\"2\");</p><p>Obviously, it is preferable to have the interface method strongly typed, and this is why the FCL includes a generic IComparable&lt;in T&gt; interface.</p><p>Here is the new version of the code revised by using the generic interface.</p><p>private void SomeMethod2() { Int32 x = 1, y = 2; IComparable&lt;Int32&gt; c = x;</p><p>// CompareTo expects an Int32; passing y (an Int32) is OK c.CompareTo(y); // y is not boxed here</p><p>}</p><p>// CompareTo expects an Int32; passing \"2\" (a String) results // in a compiler error indicating that String cannot be cast to an Int32 c.CompareTo(\"2\"); // Error</p><p>The second benefit of generic interfaces is that much less boxing will occur when working with value types. Notice in SomeMethod1 that the non-generic IComparable interface\u2019s CompareTo method expects an Object; passing y (an Int32 value type) causes the value in y to be boxed. However, in SomeMethod2, the generic IComparable&lt;in T&gt; interface\u2019s CompareTo method expects an Int32; passing y causes it to be passed by value, and no boxing is necessary.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile101.png\"/><p>Note The FCL defines non-generic and generic versions of the IComparable, ICollection, IList, and IDictionary interfaces, as well as some others. If you are defining a type, and you want to implement any of these interfaces, you should typically implement the generic versions of these interfaces. The non-generic versions are in the FCL for backward compatibility to work with code written before the .NET Framework supported generics. The nongeneric versions also provide users a way of manipulating the data in a more general, less type-safe fashion.</p><p>Some of the generic interfaces inherit the non-generic versions, so your class will have to implement both the generic and non-generic versions of the interfaces. For example, the generic IEnumerable&lt;out T&gt; interface inherits the non-generic IEnumerable interface. So if your class implements IEnumerable&lt;out T&gt;, your class must also implement IEnumerable.</p><p>Sometimes when integrating with other code, you may have to implement a non-generic interface because a generic version of the interface simply doesn\u2019t exist. In this case, if any of the interface\u2019s methods take or return Object, you will lose compile-time type safety, and you will get boxing with value types. You can alleviate this situation to some extent by using a technique I describe in the \u201cImproving Compile-Time Type Safety with Explicit Interface Method Implementations\u201d section near the end of this chapter.</p><p>The third benefit of generic interfaces is that a class can implement the same interface multiple times as long as different type parameters are used.</p><p>The following code shows an example of how useful this could be.</p><p>using System;</p><p>// This class implements the generic IComparable&lt;T&gt; interface twice public sealed class Number: IComparable&lt;Int32&gt;, IComparable&lt;String&gt; {</p><p>private Int32 m_val = 5;</p><p>// This method implements IComparable&lt;Int32&gt;'s CompareTo public Int32 CompareTo(Int32 n) {</p><p>return m_val.CompareTo(n); }</p><p>}</p><p>// This method implements IComparable&lt;String&gt;'s CompareTo public Int32 CompareTo(String s) {</p><p>return m_val.CompareTo(Int32.Parse(s)); }</p><p>public static class Program {</p><p>public static void Main() { Number n = new Number();</p><p>// Here, I compare the value in n with an Int32 (5) IComparable&lt;Int32&gt; cInt32 = n; Int32 result = cInt32.CompareTo(5);</p><p>} }</p><p>// Here, I compare the value in n with a String (\"5\") IComparable&lt;String&gt; cString = n; result = cString.CompareTo(\"5\");</p><p>An interface\u2019s generic type parameters can also be marked as contra-variant and covariant, which allows even more flexibility for using generic interfaces. For more about contra-variance and covariance, see the \u201cDelegate and Interface Contra-variant and Covariant Generic Type Arguments\u201d section in Chapter 12.</p><h6>Generics and Interface Constraints</h6><p>In the previous section, I discussed the benefits of using generic interfaces. In this section, I\u2019ll discuss the benefits of constraining generic type parameters to interfaces.</p><p>The first benefit is that you can constrain a single generic type parameter to multiple interfaces. When you do this, the type of parameter you are passing in must implement all of the interface constraints.</p><p>Here is an example.</p><p>public static class SomeType {</p><p>private static void Test() { Int32 x = 5; Guid g = new Guid();</p><p>// This call to M compiles fine because // Int32 implements IComparable AND IConvertible M(x);</p><p>}</p><p>// This call to M causes a compiler error because // Guid implements IComparable but it does not implement IConvertible M(g);</p><p>}</p><p>// M's type parameter, T, is constrained to work only with types that // implement both the IComparable AND IConvertible interfaces private static Int32 M&lt;T&gt;(T t) where T : IComparable, IConvertible {</p><p>... }</p><p>This is actually quite cool! When you define a method\u2019s parameters, each parameter\u2019s type indicates that the argument passed must be of the parameter\u2019s type or be derived from it. If the parameter type is an interface, this indicates that the argument can be of any class type as long as the class implements the interface. Using multiple interface constraints actually lets the method indicate that the passed argument must implement multiple interfaces.</p><p>In fact, if we constrained T to a class and two interfaces, we are saying that the type of argument passed must be of the specified base class (or derived from it), and it must also implement the two interfaces. This flexibility allows the method to really dictate what callers can pass, and compiler errors will be generated if callers do not meet these constraints.</p><p>The second benefit of interface constraints is reduced boxing when passing instances of value types. In the previous code fragment, the M method was passed x (an instance of an Int32, which is a value type). No boxing will occur when x is passed to M. If code inside M does call t.CompareTo(...), still no boxing occurs to make the call (boxing may still happen for arguments passed to CompareTo).</p><p>On the other hand, if M had been declared like this:</p><p>private static Int32 M(IComparable t) {</p><p>... }</p><p>then in order to pass x to M, x would have to be boxed.</p><p>For interface constraints, the C# compiler emits certain Intermediate Language (IL) instructions that result in calling the interface method on the value type directly without boxing it. Aside from using interface constraints, there is no other way to get the C# compiler to emit these IL instructions, and therefore, calling an interface method on a value type always causes boxing.</p><h6>Implementing Multiple Interfaces That Have the Same Method Name and Signature</h6><p>Occasionally, you might find yourself defining a type that implements multiple interfaces that define methods with the same name and signature. For example, imagine that there are two interfaces defined as follows.</p><p>public interface IWindow {</p><p>Object GetMenu(); }</p><p>public interface IRestaurant {</p><p>Object GetMenu(); }</p><p>Let\u2019s say that you want to define a type that implements both of these interfaces. You\u2019d have to implement the type\u2019s members by using explicit interface method implementations as follows.</p><p>// This type is derived from System.Object and // implements the IWindow and IRestaurant interfaces. public sealed class MarioPizzeria : IWindow, IRestaurant {</p><p>// This is the implementation for IWindow's GetMenu method. Object IWindow.GetMenu() { ... }</p><p>// This is the implementation for IRestaurant's GetMenu method. Object IRestaurant.GetMenu() { ... }</p><p>}</p><p>// This (optional method) is a GetMenu method that has nothing // to do with an interface. public Object GetMenu() { ... }</p><p>Because this type must implement multiple and separate GetMenu methods, you need to tell the C# compiler which GetMenu method contains the implementation for a particular interface.</p><p>Code that uses a MarioPizzeria object must cast to the specific interface to call the desired method. The following code demonstrates.</p><p>MarioPizzeria mp = new MarioPizzeria();</p><p>// This line calls MarioPizzeria's public GetMenu method mp.GetMenu();</p><p>// These lines call MarioPizzeria's IWindow.GetMenu method IWindow window = mp; window.GetMenu();</p><p>// These lines call MarioPizzeria's IRestaurant.GetMenu method IRestaurant restaurant = mp; restaurant.GetMenu();</p><h6>Improving Compile-Time Type Safety with Explicit Interface Method Implementations</h6><p>Interfaces are great because they define a standard way for types to communicate with each other. Earlier, I talked about generic interfaces and how they improve compile-time type safety and reduce boxing. Unfortunately, there may be times when you need to implement a non-generic interface because a generic version doesn\u2019t exist. If any of the interface\u2019s method(s) accept parameters of type System.Object or return a value whose type is System.Object, you will lose compile-time type safety, and you will get boxing. In this section, I\u2019ll show you how you can use EIMI to improve this situation somewhat.</p><p>Look at the very common IComparable interface.</p><p>public interface IComparable {</p><p>Int32 CompareTo(Object other); }</p><p>This interface defines one method that accepts a parameter of type System.Object. If I define my own type that implements this interface, the type definition might look like the following.</p><p>internal struct SomeValueType : IComparable { private Int32 m_x; public SomeValueType(Int32 x) { m_x = x; } public Int32 CompareTo(Object other) {</p><p>return(m_x \u00ad ((SomeValueType) other).m_x); }</p><p>}</p><p>Using SomeValueType, I can now write the following code.</p><p>public static void Main() { SomeValueType v = new SomeValueType(0); Object o = new Object(); Int32 n = v.CompareTo(v); // Undesired boxing n = v.CompareTo(o); // InvalidCastException</p><p>}</p><p>There are two characteristics of this code that are not ideal.</p><p>\u25a0\u25a0 Undesired boxing When v is passed as an argument to the CompareTo method, it must be</p><p>boxed because CompareTo expects an Object.</p><p>\u25a0\u25a0 The lack of type safety This code compiles, but an InvalidCastException is thrown</p><p>inside the CompareTo method when it attempts to cast o to SomeValueType.</p><p>Both of these issues can be fixed by using EIMIs. Here\u2019s a modified version of SomeValueType that has an EIMI added to it.</p><p>internal struct SomeValueType : IComparable { private Int32 m_x; public SomeValueType(Int32 x) { m_x = x; }</p><p>public Int32 CompareTo(SomeValueType other) {</p><p>return(m_x \u00ad other.m_x); }</p><p>}</p><p>// NOTE: No public/private used on the next line Int32 IComparable.CompareTo(Object other) {</p><p>return CompareTo((SomeValueType) other); }</p><p>Notice several changes in this new version. First, it now has two CompareTo methods. The first CompareTo method no longer takes an Object as a parameter; it now takes a SomeValueType instead. Because this parameter has changed, the code that casts other to SomeValueType is no longer necessary and has been removed. Second, changing the first CompareTo method to make it type-safe means that SomeValueType no longer adheres to the contract placed on it by implementing the IComparable interface. So SomeValueType must implement a CompareTo method that satisfies the IComparable contract. This is the job of the second IComparable.CompareTo method, which is an EIMI.</p><p>Having made these two changes means that we now get compile-time type safety and no boxing.</p><p>public static void Main() { SomeValueType v = new SomeValueType(0); Object o = new Object(); Int32 n = v.CompareTo(v); // No boxing n = v.CompareTo(o); // compile\u00adtime error</p><p>}</p><p>If, however, we define a variable of the interface type, we will lose compile-time type safety and experience undesired boxing again.</p><p>public static void Main() { SomeValueType v = new SomeValueType(0); IComparable c = v; // Boxing!</p><p>}</p><p>Object o = new Object(); Int32 n = c.CompareTo(v); // Undesired boxing n = c.CompareTo(o); // InvalidCastException</p><p>In fact, as mentioned earlier in this chapter, when casting a value type instance to an interface type, the CLR must box the value type instance. Because of this fact, two boxings will occur in the previous Main method.</p><p>EIMIs are frequently used when implementing interfaces such as IConvertible, ICollection, IList, and IDictionary. They let you create type-safe versions of these interfaces\u2019 methods, and they enable you to reduce boxing operations for value types.</p><h6>Be Careful with Explicit Interface Method Implementations</h6><p>It is critically important for you to understand some ramifications that exist when using EIMIs. And because of these ramifications, you should try to avoid EIMIs as much as possible. Fortunately, generic interfaces help you avoid EIMIs quite a bit. But there may still be times when you will need to use them (such as implementing two interface methods with the same name and signature). Here are the big problems with EIMIs:</p><p>\u25a0\u25a0 There is no documentation explaining how a type specifically implements an EIMI method,</p><p>and there is no Microsoft Visual Studio IntelliSense support.</p><p>\u25a0\u25a0 Value type instances are boxed when cast to an interface.</p><p>\u25a0\u25a0 An EIMI cannot be called by a derived type. Let\u2019s take a closer look at these problems. When examining the methods for a type in the .NET Framework reference documentation, explicit</p><p>interface method implementations are listed, but no type-specific help exists; you can just read the general help about the interface methods. For example, the documentation for the Int32 type shows that it implements all of IConvertible interface\u2019s methods. This is good because developers know that these methods exist; however, this has been very confusing to developers because you can\u2019t call an IConvertible method on an Int32 directly. For example, the following method won\u2019t compile.</p><p>public static void Main() { Int32 x = 5; Single s = x.ToSingle(null); // Trying to call an IConvertible method</p><p>}</p><p>When compiling this method, the C# compiler produces the following message: messagepil17: 'int' does not contain a definition for 'ToSingle'. This error message confuses the developer because it\u2019s clearly stating that the Int32 type doesn\u2019t define a ToSingle method when, in fact, it does.</p><p>To call ToSingle on an Int32, you must first cast the Int32 to an IConvertible, as shown in the following method.</p><p>public static void Main() { Int32 x = 5; Single s = ((IConvertible) x).ToSingle(null);</p><p>}</p><p>Requiring this cast isn\u2019t obvious at all, and many developers won\u2019t figure this out on their own. But an even more troublesome problem exists: casting the Int32 value type to an IConvertible also boxes the value type, wasting memory and hurting performance. This is the second of the big problems I mentioned at the beginning of this section.</p><p>The third and perhaps the biggest problem with EIMIs is that they cannot be called by a derived class. Here is an example.</p><p>internal class Base : IComparable {</p><p>// Explicit Interface Method Implementation Int32 IComparable.CompareTo(Object o) {</p><p>Console.WriteLine(\"Base's CompareTo\"); return 0;</p><p>} }</p><p>internal sealed class Derived : Base, IComparable {</p><p>// A public method that is also the interface implementation public Int32 CompareTo(Object o) {</p><p>Console.WriteLine(\"Derived's CompareTo\");</p><p>} }</p><p>// This attempt to call the base class's EIMI causes a compiler error: // error CS0117: 'Base' does not contain a definition for 'CompareTo' base.CompareTo(o); return 0;</p><p>In Derived\u2019s CompareTo method, I try to call base.CompareTo, but this causes the C# compiler to issue an error. The problem is that the Base class doesn\u2019t offer a public or protected CompareTo method that can be called; it offers a CompareTo method that can be called only by using a variable that is of the IComparable type. I could modify Derived\u2019s CompareTo method so that it looks like the following.</p><p>// A public method that is also the interface implementation public Int32 CompareTo(Object o) {</p><p>Console.WriteLine(\"Derived's CompareTo\");</p><p>// This attempt to call the base class's EIMI causes infinite recursion IComparable c = this; c.CompareTo(o);</p><p>return 0; }</p><p>In this version, I am casting this to an IComparable variable, c. And then, I use c to call CompareTo. However, the Derived\u2019s public CompareTo method serves as the implementation for Derived\u2019s IComparableCompareTo method, and therefore, infinite recursion occurs. This could be fixed by declaring the Derived class without the IComparable interface, like the following.</p><p>internal sealed class Derived : Base /*, IComparable */ { ... }</p><p>Now the previous CompareTo method will call the CompareTo method in Base. But sometimes you cannot simply remove the interface from the type because you want the derived type to implement an interface method. The best way to fix this is for the base class to provide a virtual method in addition to the interface method that it has chosen to implement explicitly. Then the Derived class can override the virtual method. Here is the correct way to define the Base and Derived classes.</p><p>internal class Base : IComparable {</p><p>// Explicit Interface Method Implementation Int32 IComparable.CompareTo(Object o) {</p><p>Console.WriteLine(\"Base's IComparable CompareTo\"); return CompareTo(o); // This now calls the virtual method</p><p>}</p><p>// Virtual method for derived classes (this method could have any name) public virtual Int32 CompareTo(Object o) {</p><p>Console.WriteLine(\"Base's virtual CompareTo\"); return 0;</p><p>} }</p><p>internal sealed class Derived : Base, IComparable {</p><p>// A public method that is also the interface implementation public override Int32 CompareTo(Object o) {</p><p>Console.WriteLine(\"Derived's CompareTo\");</p><p>} }</p><p>// Now, we can call Base's virtual method return base.CompareTo(o);</p><p>Note that I have defined the virtual method above as a public method, but in some cases, you will prefer to make the method protected instead. It is fine to make this method protected instead of public, but that will necessitate other minor changes. This discussion clearly shows you that EIMIs should be used with great care. When many developers first learn about EIMIs, they think that they\u2019re cool and they start using them whenever possible. Don\u2019t do this! EIMIs are useful in some circumstances, but you should avoid them whenever possible because they make using a type much more difficult.</p><h6>Design: Base Class or Interface?</h6><p>I often hear the question, \u201cShould I design a base type or an interface?\u201d The answer isn\u2019t always clearcut. Here are some guidelines that might help you:</p><p>\u25a0\u25a0 IS-A vs. CAN-DO relationship A type can inherit only one implementation. If the derived type can\u2019t claim an IS-A relationship with the base type, don\u2019t use a base type; use an interface. Interfaces imply a CAN-DO relationship. If the CAN-DO functionality appears to belong with various object types, use an interface. For example, a type can convert instances of itself</p><p>to another type (IConvertible), a type can serialize an instance of itself (ISerializable), etc. Note that value types must be derived from System.ValueType, and therefore, they cannot be derived from an arbitrary base class. In this case, you must use a CAN-DO relationship and define an interface.</p><p>\u25a0\u25a0 Ease of use It\u2019s generally easier for you as a developer to define a new type derived from a</p><p>base type than to implement all of the methods of an interface. The base type can provide a lot of functionality, so the derived type probably needs only relatively small modifications to its behavior. If you supply an interface, the new type must implement all of the members.</p><p>\u25a0\u25a0 Consistent implementation No matter how well an interface contract is documented, it\u2019s very unlikely that everyone will implement the contract 100 percent correctly. In fact, COM suffers from this very problem, which is why some COM objects work correctly only with Microsoft Word or with Windows Internet Explorer. By providing a base type with a good default implementation, you start off using a type that works and is well tested; you can then modify parts that need modification.</p><p>\u25a0\u25a0 Versioning If you add a method to the base type, the derived type inherits the new method, you start off using a type that works, and the user\u2019s source code doesn\u2019t even have to be recompiled. Adding a new member to an interface forces the inheritor of the interface to change its source code and recompile.</p><p>In the FCL, the classes related to streaming data use an implementation inheritance design. The System.IO.Stream class is the abstract base class. It provides a bunch of methods, such as Read and Write. Other classes\u2014System.IO.FileStream, System.IO.MemoryStream, and System.Net. Sockets.NetworkStream\u2014are derived from Stream. Microsoft chose an IS-A relationship between each of these three classes and the Stream class because it made implementing the concrete classes easier. For example, the derived classes need to implement only synchronous I/O operations; they inherit the ability to perform asynchronous I/O operations from the Stream base class.</p><p>Admittedly, choosing to use inheritance for the stream classes isn\u2019t entirely clear-cut; the Stream base class actually provides very little implementation. However, if you consider the Windows Forms control classes, in which Button, CheckBox, ListBox, and all of the other controls are derived from System.Windows.Forms.Control, it\u2019s easy to imagine all of the code that Control implements,</p><p>which the various control classes simply inherit to function correctly.</p><p>By contrast, Microsoft designed the FCL collections to be interface based. The System.Collections.Generic namespace defines several collection-related interfaces: IEnumerable&lt;out T&gt;, ICollection&lt;T&gt;, IList&lt;T&gt;, and IDictionary&lt;TKey, TValue&gt;. Then Microsoft provided a number of classes, such as List&lt;T&gt;, Dictionary&lt;TKey, TValue&gt;, Queue&lt;T&gt;, Stack&lt;T&gt;, and so on, that implement combinations of these interfaces. Here the designers chose a CAN-DO relationship between the classes and the interfaces because the implementations of these various collection classes are radically different from one another. In other words, there isn\u2019t a lot of sharable code between a List&lt;T&gt;, a Dictionary&lt;TKey, TValue&gt;, and a Queue&lt;T&gt;.</p><p>The operations these collection classes offer are, nevertheless, pretty consistent. For example, they all maintain a set of elements that can be enumerated, and they all allow adding and removing of elements. If you have a reference to an object whose type implements the IList&lt;T&gt; interface, you can write code to insert elements, remove elements, and search for an element without having to know exactly what type of collection you\u2019re working with. This is a very powerful mechanism.</p><p>Finally, it should be pointed out that you can actually do both: define an interface and provide a base class that implements the interface. For example, the FCL defines the IComparer&lt;in T&gt; interface and any type can choose to implement this interface. In addition, the FCL provides an abstract base class, Comparer&lt;T&gt;, which implements this interface and provides a default implementation for the non-generic IComparer\u2019s Compare method. Having both an interface definition and a base class offers great flexibility because developers can now choose whichever they prefer.</p><h6>PART III</h6>"}, {"id": "lesson-2", "title": "Essential Types", "readTime": "5 min read", "subtitle": "", "content": "<ul style=\"list-style-type: none;\">\n<li><p>CHAPTER 14 Chars, Strings, and Working with Text . . . . . . . . . .317</p></li>\n<li><p>CHAPTER 15 Enumerated Types and Bit Flags . . . . . . . . . . . . . . .361</p></li>\n<li><p>CHAPTER 16 Arrays. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . .373</p></li>\n<li><p>CHAPTER 17 Delegates. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . .391</p></li>\n<li><p>CHAPTER 18 Custom Attributes . . . . . . . . . . . . . . . . . . . . . . . . . . .421</p></li>\n<li><p>CHAPTER 19 Nullable Value Types . . . . . . . . . . . . . . . . . . . . . . . . .441</p></li>\n</ul><h6>C H A P T E R 1 4</h6><h2>Chars, Strings, and Working with Text</h2><h6>In this chapter:</h6><p>Characters. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 317 The System.String Type . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 320 Constructing a String Efficiently. . . . . . . . . . . . . . . . . . . . . . . . . . . 336 Obtaining a String Representation of an Object: ToString 339 Parsing a String to Obtain an Object: Parse. . . . . . . . . . . . . . . .348 Encodings: Converting Between Characters and Bytes. . . . . . . 350 Secure Strings . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 357</p><p>In this chapter, I\u2019ll explain the mechanics of working with individual characters and strings in the Microsoft .NET Framework. I\u2019ll start by talking about the System.Char structure and the various ways that you can manipulate a character. Then I\u2019ll go over the more useful System.String class, which allows you to work with immutable strings. (Once created, strings can\u2019t be modified in any way.) After examining strings, I\u2019ll show you how to perform various operations efficiently to build a string dynamically via the System.Text.StringBuilder class. With the string basics out of the way, I\u2019ll then describe how to format objects into strings and how to efficiently persist or transmit strings by using various encodings. Finally, I\u2019ll discuss the System.Security.SecureString class, which can be used to protect sensitive string data such as passwords and credit card information.</p><h6>Characters</h6><p>In the .NET Framework, characters are always represented in 16-bit Unicode code values, easing the development of global applications. A character is represented with an instance of the System.Char structure (a value type). The System.Char type is pretty simple. It offers two public read-only constant fields: MinValue, defined as '\\0', and MaxValue, defined as '\\uffff'.</p><p>Given an instance of a Char, you can call the static GetUnicodeCategory method, which returns a value of the System.Globalization.UnicodeCategory enumerated type. This value indicates whether the character is a control character, a currency symbol, a lowercase letter, an uppercase letter, a punctuation character, a math symbol, or another character (as defined by the Unicode standard).</p><p>To ease developing, the Char type also offers several static methods, such as IsDigit, IsLetter, IsWhiteSpace, IsUpper, IsLower, IsPunctuation, IsLetterOrDigit, IsControl, IsNumber, IsSeparator, IsSurrogate, IsLowSurrogate, IsHighSurrogate, and IsSymbol. Most of these methods call GetUnicodeCategory internally and simply return true or false accordingly. Note that all of these methods take either a single character for a parameter or a String and the index of a character within the String as parameters.</p><p>In addition, you can convert a single character to its lowercase or uppercase equivalent in a culture-agnostic way by calling the static ToLowerInvariant or ToUpperInvariant method. Alternatively, the ToLower and ToUpper methods convert the character by using the culture information associated with the calling thread (which the methods obtain internally by querying the static CurrentCulture property of the System.Globalization.CultureInfo class). You can also specify a particular culture by passing an instance of the CultureInfo class to these methods. ToLower and ToUpper require culture information because letter casing is a culture-dependent operation. For example, Turkish considers the uppercase of U+0069 (LATIN LOWERCASE LETTER I) to be U+0130 (LATIN UPPERCASE LETTER I WITH DOT ABOVE), whereas other cultures consider the result to be U+0049 (LATIN CAPITAL LETTER I).</p><p>Besides these static methods, the Char type also offers a few instance methods of its own. The Equals method returns true if two Char instances represent the same 16-bit Unicode code point. The CompareTo methods (defined by the IComparable/IComparable&lt;Char&gt; interfaces) return a comparison of two Char instances; this comparison is not culture-sensitive. The ConvertFromUtf32 method produces a string consisting of one or two UTF-16 characters from a single UTF-32 character. The ConvertToUtf32 produces a UTF-32 character from a low/high surrogate pair or from a string. The ToString method returns a String consisting of a single character. The opposite of ToString is Parse/TryParse, which takes a single-character String and returns its UTF-16 code point.</p><p>The last method, GetNumericValue, returns the numeric equivalent of a character. I demonstrate this method in the following code.</p><p>using System;</p><p>public static class Program {</p><p>public static void Main() { Double d; // '\\u0033' is the \"digit 3\" d = Char.GetNumericValue('\\u0033'); // '3' would work too Console.WriteLine(d.ToString()); // Displays \"3\"</p><p>// '\\u00bc' is the \"vulgar fraction one quarter ('\u00bc')\" d = Char.GetNumericValue('\\u00bc'); Console.WriteLine(d.ToString()); // Displays \"0.25\"</p><p>} }</p><p>// 'A' is the \"Latin capital letter A\" d = Char.GetNumericValue('A'); Console.WriteLine(d.ToString()); // Displays \"\u00ad1\"</p><p>Finally, three techniques allow you to convert between various numeric types to Char instances and vice versa. The techniques are listed here in order of preference:</p><p>\u25a0\u25a0 Casting The easiest way to convert a Char to a numeric value such as an Int32 is simply by casting. Of the three techniques, this is the most efficient because the compiler emits Intermediate Language (IL) instructions to perform the conversion, and no methods have to be called. In addition, some languages (such as C#) allow you to indicate whether the conversion should be performed using checked or unchecked code (discussed in Chapter 5, \u201cPrimitive, Reference, and Value Types\u201d).</p><p>\u25a0\u25a0 Use the Convert type The System.Convert type offers several static methods that are capable of converting a Char to a numeric type and vice versa. All of these methods perform the conversion as a checked operation, causing an OverflowException to be thrown if the conversion results in the loss of data.</p><p>\u25a0\u25a0 Use the IConvertible interface The Char type and all of the numeric types in the .NET Framework Class Library (FCL) implement the IConvertible interface. This interface defines methods such as ToUInt16 and ToChar. This technique is the least efficient of the three because calling an interface method on a value type requires that the instance be boxedChar and all of the numeric types are value types. The methods of IConvertible throw a System.InvalidCastException if the type can\u2019t be converted (such as converting a Char to a Boolean) or if the conversion results in a loss of data. Note that many types (including the FCL\u2019s Char and numeric types) implement IConvertible\u2019s methods as explicit interface member implementations (described in Chapter 13, \u201cInterfaces\u201d). This means that you must explicitly cast the instance to an IConvertible before you can call any of the interface\u2019s methods. All of the methods of IConvertible except GetTypeCode accept a reference to an object that implements the IFormatProvider interface. This parameter is useful if for some reason the conversion needs to take culture information into account. For most conversions, you can pass null for this parameter because it would be ignored anyway.</p><p>The following code demonstrates how to use these three techniques.</p><p>using System;</p><p>public static class Program { public static void Main() {</p><p>Char c; Int32 n;</p><p>// Convert number &lt;\u00ad&gt; character using C# casting c = (Char) 65; Console.WriteLine(c); // Displays \"A\"</p><p>n = (Int32) c; Console.WriteLine(n); // Displays \"65\"</p><p>c = unchecked((Char) (65536 + 65)); Console.WriteLine(c); // Displays \"A\"</p><p>// Convert number &lt;\u00ad&gt; character using Convert c = Convert.ToChar(65); Console.WriteLine(c); // Displays \"A\"</p><p>n = Convert.ToInt32(c); Console.WriteLine(n); // Displays \"65\"</p><p>// This demonstrates Convert's range checking try {</p><p>c = Convert.ToChar(70000); // Too big for 16 bits Console.WriteLine(c); // Doesn't execute</p><p>} catch (OverflowException) {</p><p>Console.WriteLine(\"Can't convert 70000 to a Char.\"); }</p><p>// Convert number &lt;\u00ad&gt; character using IConvertible c = ((IConvertible) 65).ToChar(null); Console.WriteLine(c); // Displays \"A\"</p><p>} }</p><p>n = ((IConvertible) c).ToInt32(null); Console.WriteLine(n); // Displays \"65\"</p><h6>The System.String Type</h6><p>One of the most used types in any application is System.String. A String represents an immutable sequence of characters. The String type is derived immediately from Object, making it a reference type, and therefore, String objects (its array of characters) always live in the heap, never on a thread\u2019s stack. The String type also implements several interfaces (IComparable/ IComparable&lt;String&gt;, ICloneable, IConvertible, IEnumerable/IEnumerable&lt;Char&gt;, and IEquatable&lt;String&gt;).</p><h6>Constructing Strings</h6><p>Many programming languages (including C#) consider String to be a primitive type\u2014that is, the compiler lets you express literal strings directly in your source code. The compiler places these literal strings in the module\u2019s metadata, and they are then loaded and referenced at run time.</p><p>In C#, you can\u2019t use the new operator to construct a String object from a literal string.</p><p>using System;</p><p>public static class Program {</p><p>public static void Main() { String s = new String(\"Hi there.\"); // &lt;\u00ad\u00ad Error Console.WriteLine(s);</p><p>} }</p><p>Instead, you must use the following simplified syntax.</p><p>using System;</p><p>public static class Program {</p><p>public static void Main() { String s = \"Hi there.\"; Console.WriteLine(s);</p><p>} }</p><p>If you compile this code and examine its IL (using ILDasm.exe), you\u2019d see the following.</p><p>.method public hidebysig static void Main() cil managed {</p><p>.entrypoint // Code size 13 (0xd)</p><p>.maxstack 1</p><p>.locals init ([0] string s) IL_0000: ldstr \"Hi there.\"</p><ul style=\"list-style-type: none;\">\n<li><p>IL_0005: stloc.0</p></li>\n<li><p>IL_0006: ldloc.0</p></li>\n<li><p>IL_0007: call void [mscorlib]System.Console::WriteLine(string) IL_000c: ret</p></li>\n</ul><p>} // end of method Program::Main</p><p>The newobj IL instruction constructs a new instance of an object. However, no newobj instruction appears in the IL code example. Instead, you see the special ldstr (load string) IL instruction, which constructs a String object by using a literal string obtained from metadata. This shows you that the common language runtime (CLR) does, in fact, have a special way of constructing literal String objects.</p><p>If you are using unsafe code, you can construct a String object from a Char* or SByte*. To accomplish this, you would use C#\u2019s new operator and call one of the constructors provided by the String type that takes Char* or SByte* parameters. These constructors create a String object,</p><p>initializing the string from an array of Char instances or signed bytes. The other constructors don\u2019t have any pointer parameters and can be called using safe (verifiable) code written in any managed programming language.</p><p>C# offers some special syntax to help you enter literal strings into the source code. For special characters such as new lines, carriage returns, and backspaces, C# uses the escape mechanism familiar to C/C++ developers.</p><p>// String containing carriage\u00adreturn and newline characters String s = \"Hi\\r\\nthere.\";</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile102.png\"/><p>Important Although the preceding example hard-codes carriage-return and newline characters into the string, I don\u2019t recommend this practice. Instead, the System.Environment type defines a read-only NewLine property that returns a string consisting of these characters when your application is running on Windows. However, the NewLine property is platform sensitive, and it returns the appropriate string required to obtain a newline by the underlying platform. So, for example, if the Common Language Infrastructure (CLI) is ported to a UNIX system, the NewLine property would return a string consisting of just a single character \\n. Here\u2019s the proper way to define the previous string so that it works correctly on any platform.</p><p>String s = \"Hi\" + Environment.NewLine + \"there.\";</p><p>You can concatenate several strings to form a single string by using C#\u2019s + operator as follows.</p><p>// Three literal strings concatenated to form a single literal string String s = \"Hi\" + \" \" + \"there.\";</p><p>In this code, because all of the strings are literal strings, the C# compiler concatenates them at compile time and ends up placing just one string\u2014\"Hi there.\"\u2014in the module\u2019s metadata. Using the + operator on nonliteral strings causes the concatenation to be performed at run time. To concatenate several strings together at run time, avoid using the + operator because it creates multiple string objects on the garbage-collected heap. Instead, use the System.Text.StringBuilder type (which I\u2019ll explain later in this chapter).</p><p>Finally, C# also offers a special way to declare a string in which all characters between quotes are considered part of the string. These special declarations are called verbatim strings and are typically used when specifying the path of a file or directory or when working with regular expressions. Here is some code showing how to declare the same string with and without using the verbatim string character (@).</p><p>// Specifying the pathname of an application String file = \"C:\\\\Windows\\\\System32\\\\Notepad.exe\";</p><p>// Specifying the pathname of an application by using a verbatim string String file = @\"C:\\Windows\\System32\\Notepad.exe\";</p><p>You could use either one of the preceding code lines in a program because they produce identical strings in the assembly\u2019s metadata. However, the @ symbol before the string on the second line tells the compiler that the string is a verbatim string. In effect, this tells the compiler to treat backslash characters as backslash characters instead of escape characters, making the path much more readable in your source code.</p><p>Now that you\u2019ve seen how to construct a string, let\u2019s talk about some of the operations you can perform on String objects.</p><h6>Strings Are Immutable</h6><p>The most important thing to know about a String object is that it is immutable. That is, once created, a string can never get longer, get shorter, or have any of its characters changed. Having immutable strings offers several benefits. First, it allows you to perform operations on a string without actually changing the string.</p><p>if (s.ToUpperInvariant().Substring(10, 21).EndsWith(\"EXE\")) {</p><p>... }</p><p>Here, ToUpperInvariant returns a new string; it doesn\u2019t modify the characters of the string s. Substring operates on the string returned by ToUpperInvariant and also returns a new string, which is then examined by EndsWith. The two temporary strings created by ToUpperInvariant and Substring are not referenced for long by the application code, and the garbage collector will reclaim their memory at the next collection. If you perform a lot of string manipulations, you end up creating a lot of String objects on the heap, which causes more frequent garbage collections, thus hurting your application\u2019s performance.</p><p>Having immutable strings also means that there are no thread synchronization issues when manipulating or accessing a string. In addition, it\u2019s possible for the CLR to share multiple identical String contents through a single String object. This can reduce the number of strings in the system\u2014thereby conserving memory usage\u2014and it is what string interning (discussed later in the chapter) is all about.</p><p>For performance reasons, the String type is tightly integrated with the CLR. Specifically, the CLR knows the exact layout of the fields defined within the String type, and the CLR accesses these fields directly. This performance and direct access come at a small development cost: the String class is sealed, which means that you cannot use it as a base class for your own type. If you were able to define your own type, using String as a base type, you could add your own fields, which would break the CLR\u2019s assumptions. In addition, you could break some assumptions that the CLR team has made about String objects being immutable.</p><h6>Comparing Strings</h6><p>Comparing is probably the most common operation performed on strings. There are two reasons to compare two strings with each other. We compare two strings to determine equality or to sort them (usually for presentation to a user).</p><p>In determining string equality or when comparing strings for sorting, it is highly recommended that you call one of these methods (defined by the String class).</p><p>Boolean Equals(String value, StringComparison comparisonType) static Boolean Equals(String a, String b, StringComparison comparisonType)</p><p>static Int32 Compare(String strA, String strB, StringComparison comparisonType) static Int32 Compare(string strA, string strB, Boolean ignoreCase, CultureInfo culture) static Int32 Compare(String strA, String strB, CultureInfo culture, CompareOptions options)</p><p>static Int32 Compare(String strA, Int32 indexA, String strB, Int32 indexB, Int32 length, StringComparison comparisonType) static Int32 Compare(String strA, Int32 indexA, String strB, Int32 indexB, Int32 length, CultureInfo culture, CompareOptions options) static Int32 Compare(String strA, Int32 indexA, String strB, Int32 indexB, Int32 length, Boolean ignoreCase, CultureInfo culture)</p><p>Boolean StartsWith(String value, StringComparison comparisonType) Boolean StartsWith(String value,</p><p>Boolean ignoreCase, CultureInfo culture)</p><p>Boolean EndsWith(String value, StringComparison comparisonType) Boolean EndsWith(String value, Boolean ignoreCase, CultureInfo culture)</p><p>When sorting, you should always perform case-sensitive comparisons. The reason is that if two strings differing only by case are considered to be equal, they could be ordered differently each time you sort them; this would confuse the user.</p><p>The comparisonType argument (in most of the preceding methods) is one of the values defined by the StringComparison enumerated type, which is defined as follows.</p><p>public enum StringComparison { CurrentCulture = 0, CurrentCultureIgnoreCase = 1, InvariantCulture = 2, InvariantCultureIgnoreCase = 3, Ordinal = 4, OrdinalIgnoreCase = 5</p><p>}</p><p>The options argument (in two of the preceding methods) is one of the values defined by the CompareOptions enumerator type.</p><p>[Flags] public enum CompareOptions {</p><p>None = 0, IgnoreCase = 1, IgnoreNonSpace = 2, IgnoreSymbols = 4, IgnoreKanaType = 8, IgnoreWidth = 0x00000010, Ordinal = 0x40000000, OrdinalIgnoreCase = 0x10000000, StringSort = 0x20000000</p><p>}</p><p>Methods that accept a CompareOptions argument also force you to explicitly pass in a culture. When passing in the Ordinal or OrdinalIgnoreCase flag, these Compare methods ignore the specified culture.</p><p>Many programs use strings for internal programmatic purposes such as path names, file names, URLs, registry keys and values, environment variables, reflection, Extensible Markup Language (XML) tags, XML attributes, and so on. Often, these strings are not shown to a user and are used only within the program. When comparing programmatic strings, you should always use</p><p>StringComparison.Ordinal or StringComparison.OrdinalIgnoreCase. This is the fastest way to perform a comparison that is not to be affected in any linguistic way because culture information is not taken into account when performing the comparison.</p><p>On the other hand, when you want to compare strings in a linguistically correct manner (usually for display to an end user), you should use StringComparison.CurrentCulture or StringComparison.CurrentCultureIgnoreCase.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile103.png\"/><p>Important For the most part, StringComparison.InvariantCulture and StringComparison.InvariantCultureIgnoreCase should not be used. Although these values cause the comparison to be linguistically correct, using them to compare programmatic strings takes longer than performing an ordinal comparison. Furthermore, the invariant culture is culture agnostic, which makes it an incorrect choice when working with strings that you want to show to an end user.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile104.png\"/><p>Important If you want to change the case of a string's characters before performing an ordinal comparison, you should use String\u2019s ToUpperInvariant or ToLowerInvariant method. When normalizing strings, it is highly recommended that you use ToUpperInvariant instead of ToLowerInvariant because Microsoft has optimized the code for performing uppercase comparisons. In fact, the FCL internally normalizes strings to uppercase prior to performing case-insensitive comparisons. We use ToUpperInvariant and ToLowerInvariant methods because the String class does not offer ToUpperOrdinal and ToLowerOrdinal methods. We do not use the ToUpper and ToLower methods because these are culture sensitive.</p><p>Sometimes, when you compare strings in a linguistically correct manner, you want to specify a specific culture rather than use a culture that is associated with the calling thread. In this case, you can use the overloads of the StartsWith, EndsWith, and Compare methods shown earlier, all of which take Boolean and CultureInfo arguments.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile105.png\"/><p>Important The String type defines several overloads of the Equals, StartsWith, EndsWith, and Compare methods in addition to the versions shown earlier. Microsoft recommends that these other versions (not shown in this book) be avoided. Furthermore, String\u2019s other comparison methods\u2014CompareTo (required by the IComparable interface), CompareOrdinal, and the == and != operators\u2014should also be avoided. The reason for avoiding these methods and operators is because the caller does not explicitly indicate how the string comparison should be performed, and you cannot determine from the name of the method what the default comparison will be. For example, by default,</p><p>CompareTo performs a culture-sensitive comparison, whereas Equals performs an ordinal comparison. Your code will be easier to read and maintain if you always indicate explicitly how you want to perform your string comparisons.</p><p>Now, let\u2019s talk about how to perform linguistically correct comparisons. The .NET Framework uses the System.Globalization.CultureInfo type to represent a language/country pair (as described by the RFC 1766 standard). For example, \u201cen-US\u201d identifies English as written in the United States, \u201cenAU\u201d identifies English as written in Australia, and \u201cde-DE\u201d identifies German as written in Germany. In the CLR, every thread has two properties associated with it. Each of these properties refers to a CultureInfo object. The two properties are:</p><p>\u25a0\u25a0 CurrentUICulture This property is used to obtain resources that are shown to an end user. It is most useful for GUI or Web Forms applications because it indicates the language that should be used when displaying UI elements such as labels and buttons. By default, when you create a thread, this thread property is set to a CultureInfo object, which identifies the language of the Windows version the application is running on using the Win32 GetUserDefaultUILanguage function. If you\u2019re running a Multilingual User Interface (MUI) version of Windows, you can set this via the Regional And Language Options Control Panel Settings dialog box. On a non-MUI version of Windows, the language is determined by the localized version of the operating system installed (or the installed language pack) and the language is not changeable.</p><p>\u25a0\u25a0 CurrentCulture This property is used for everything that CurrentUICulture isn\u2019t used for, including number and date formatting, string casing, and string comparing. When formatting, both the language and country parts of the CultureInfo object are used. By default, when you create a thread, this thread property is set to a CultureInfo object, whose value is determined by calling the Win32 GetUserDefaultLCID method, whose value is set in the Regional And Language Control Panel applet.</p><p>For the two thread properties mentioned above, you can override the default value used by the system when a new thread gets created with AppDomain defaults by setting CultureInfo\u2019s static DefaultThreadCurrentCulture and DefaultThreadCurrentUICulture properties.</p><p>On many computers, a thread\u2019s CurrentUICulture and CurrentCulture properties will be set to the same CultureInfo object, which means that they both use the same language/country information. However, they can be set differently. For example: an application running in the United States could use Spanish for all of its menu items and other GUI elements while properly displaying all of the currency and date formatting for the United States. To do this, the thread\u2019s CurrentUICulture property should be set to a CultureInfo object initialized with a language of \u201ces\u201d (for Spanish), while the thread\u2019s CurrentCulture property should be set to a CultureInfo object initialized with a language/country pair of \u201cen-US.\u201d</p><p>Internally, a CultureInfo object has a field that refers to a System.Globalization.Compare\u00ad</p><p>Info object, which encapsulates the culture\u2019s character-sorting table information as defined by the Unicode standard. The following code demonstrates the difference between performing an ordinal comparison and a culturally aware string comparison.</p><p>using System; using System.Globalization;</p><p>public static class Program { public static void Main() {</p><ul style=\"list-style-type: none;\">\n<li><p>String s1 = \"Strasse\";</p></li>\n<li><p>String s2 = \"Stra\u00dfe\"; Boolean eq;</p></li>\n</ul><p>// CompareOrdinal returns nonzero. eq = String.Compare(s1, s2, StringComparison.Ordinal) == 0; Console.WriteLine(\"Ordinal comparison: '{0}' {2} '{1}'\", s1, s2,</p><p>eq ? \"==\" : \"!=\");</p><p>// Compare Strings appropriately for people // who speak German (de) in Germany (DE) CultureInfo ci = new CultureInfo(\"de\u00adDE\");</p><p>}</p><p>// Compare returns zero. eq = String.Compare(s1, s2, true, ci) == 0; Console.WriteLine(\"Cultural comparison: '{0}' {2} '{1}'\", s1, s2,</p><p>eq ? \"==\" : \"!=\"); }</p><p>Building and running this code produces the following output.</p><p>Ordinal comparison: 'Strasse' != 'Stra\u00dfe' Cultural comparison: 'Strasse' == 'Stra\u00dfe'</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile106.png\"/><p>Note When the Compare method is not performing an ordinal comparison, it performs character expansions. A character expansion is when a character is expanded to multiple characters regardless of culture. In the above case, the German Eszet character \u2018\u00df\u2019 is always expanded to \u2018ss.\u2019 Similarly, the \u2018\u00c6\u2019 ligature character is always expanded to \u2018AE.\u2019 So in the code example, the second call to Compare will always return 0 regardless of which culture I actually pass in to it.</p><p>In some rare circumstances, you may need to have even more control when comparing strings for equality or for sorting. This could be necessary when comparing strings consisting of Japanese characters. This additional control can be accessed via the CultureInfo object\u2019s CompareInfo property. As mentioned earlier, a CompareInfo object encapsulates a culture\u2019s character comparison tables, and there is just one CompareInfo object per culture.</p><p>When you call String\u2019s Compare method, if the caller specifies a culture, the specified culture is used, or if no culture is specified, the value in the calling thread\u2019s CurrentCulture property is used. Internally, the Compare method obtains the reference to the CompareInfo object for the appropriate culture and calls the Compare method of the CompareInfo object, passing along the appropriate options (such as case insensitivity). Naturally, you could call the Compare method of a specific CompareInfo object yourself if you need the additional control.</p><p>The Compare method of the CompareInfo type takes as a parameter a value from the CompareOptions enumerated type (as shown earlier). You can OR these bit flags together to gain significantly greater control when performing string comparisons. For a complete description of these symbols, consult the .NET Framework documentation.</p><p>The following code demonstrates how important culture is to sorting strings and shows various ways of performing string comparisons.</p><p>using System; using System.Text; using System.Windows.Forms; using System.Globalization; using System.Threading;</p><p>public sealed class Program {</p><p>public static void Main() { String output = String.Empty; String[] symbol = new String[] { \"&lt;\", \"=\", \"&gt;\" }; Int32 x; CultureInfo ci;</p><p>// The code below demonstrates how strings compare // differently for different cultures.</p><ul style=\"list-style-type: none;\">\n<li><p>String s1 = \"cot\u00e9\";</p></li>\n<li><p>String s2 = \"c\u00f4te\";</p></li>\n</ul><p>// Sorting strings for French in France. ci = new CultureInfo(\"fr\u00adFR\"); x = Math.Sign(ci.CompareInfo.Compare(s1, s2)); output += String.Format(\"{0} Compare: {1} {3} {2}\",</p><p>ci.Name, s1, s2, symbol[x + 1]); output += Environment.NewLine;</p><p>// Sorting strings for Japanese in Japan. ci = new CultureInfo(\"ja\u00adJP\"); x = Math.Sign(ci.CompareInfo.Compare(s1, s2)); output += String.Format(\"{0} Compare: {1} {3} {2}\",</p><p>ci.Name, s1, s2, symbol[x + 1]); output += Environment.NewLine;</p><p>// Sorting strings for the thread's culture ci = Thread.CurrentThread.CurrentCulture; x = Math.Sign(ci.CompareInfo.Compare(s1, s2)); output += String.Format(\"{0} Compare: {1} {3} {2}\",</p><p>ci.Name, s1, s2, symbol[x + 1]); output += Environment.NewLine + Environment.NewLine;</p><p>// The code below demonstrates how to use CompareInfo.Compare's // advanced options with 2 Japanese strings. One string represents // the word \"shinkansen\" (the name for the Japanese high\u00adspeed // train) in hiragana (one subtype of Japanese writing), and the // other represents the same word in katakana (another subtype of // Japanese writing).</p><ul style=\"list-style-type: none;\">\n<li><p>s1 = \"\"; // (\"\\u3057\\u3093\\u304B\\u3093\\u305b\\u3093\")</p></li>\n<li><p>s2 = \"\"; // (\"\\u30b7\\u30f3\\u30ab\\u30f3\\u30bb\\u30f3\")</p></li>\n</ul><p>// Here is the result of a default comparison ci = new CultureInfo(\"ja\u00adJP\"); x = Math.Sign(String.Compare(s1, s2, true, ci)); output += String.Format(\"Simple {0} Compare: {1} {3} {2}\",</p><p>ci.Name, s1, s2, symbol[x + 1]); output += Environment.NewLine;</p><p>// Here is the result of a comparison that ignores // kana type (a type of Japanese writing) CompareInfo compareInfo = CompareInfo.GetCompareInfo(\"ja\u00adJP\"); x = Math.Sign(compareInfo.Compare(s1, s2, CompareOptions.IgnoreKanaType)); output += String.Format(\"Advanced {0} Compare: {1} {3} {2}\",</p><p>ci.Name, s1, s2, symbol[x + 1]);</p><p>}</p><p>MessageBox.Show(output, \"Comparing Strings For Sorting\"); }</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile107.png\"/><p>Note This source code file can\u2019t be saved in ANSI or the Japanese characters will be lost. To save this file in Microsoft Visual Studio, go to the Save File As dialog box, click the down arrow that is part of the Save button and select Save With Encoding. I selected Unicode (UTF-8 With Signature) \u2013 Codepage 65001. Microsoft\u2019s C# compiler can successfully parse source code files by using this code page.</p><p>Building and running this code produces the output shown in Figure 14-1.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile108.png\"/><figcaption>FIGURE 14-1 String sorting results.</figcaption><p>In addition to Compare, the CompareInfo class offers the IndexOf, LastIndexOf, IsPrefix, and IsSuffix methods. Because all of these methods offer overloads that take a CompareOptions enumeration value as a parameter, they give you more control than the Compare, IndexOf, LastIndexOf, StartsWith, and EndsWith methods defined by the String class. Also, you should be aware that the FCL includes a System.StringComparer class that you can also use for performing string comparisons. This class is useful when you want to perform the same kind of comparison repeatedly for many different strings.</p><h6>String Interning</h6><p>As I said in the preceding section, checking strings for equality is a common operation for many applications\u2014this task can hurt performance significantly. When performing an ordinal equality check, the CLR quickly tests to see if both strings have the same number of characters. If they don\u2019t, the strings are definitely not equal; if they do, the strings might be equal, and the CLR must then compare each individual character to determine for sure. When performing a culturally aware comparison, the CLR must always compare all of the individual characters because strings of different lengths might be considered equal.</p><p>In addition, if you have several instances of the same string duplicated in memory, you\u2019re wasting memory because strings are immutable. You\u2019ll use memory much more efficiently if there is just one instance of the string in memory and all variables needing to refer to the string can just point to the single string object.</p><p>If your application frequently compares strings for equality by using case-sensitive, ordinal comparisons, or if you expect to have many string objects with the same value, you can enhance performance substantially if you take advantage of the string interning mechanism in the CLR. When the CLR initializes, it creates an internal hash table in which the keys are strings and the values are references to String objects in the managed heap. Initially, the table is empty (of course). The String class offers two methods that allow you to access this internal hash table.</p><p>public static String Intern(String str); public static String IsInterned(String str);</p><p>The first method, Intern, takes a String, obtains a hash code for it, and checks the internal hash table for a match. If an identical string already exists, a reference to the already existing String object is returned. If an identical string doesn\u2019t exist, a copy of the string is made, the copy is added to the internal hash table, and a reference to this copy is returned. If the application no longer holds a reference to the original String object, the garbage collector is able to free the memory of that string. Note that the garbage collector can\u2019t free the strings that the internal hash table refers to because the hash table holds the reference to those String objects. String objects referred to by the internal hash table can\u2019t be freed until the AppDomain is unloaded or the process terminates.</p><p>As does the Intern method, the IsInterned method takes a String and looks it up in the internal hash table. If a matching string is in the hash table, IsInterned returns a reference to the interned string object. If a matching string isn\u2019t in the hash table, however, IsInterned returns null; it doesn\u2019t add the string to the hash table.</p><p>By default, when an assembly is loaded, the CLR interns all of the literal strings described in the assembly\u2019s metadata. Microsoft learned that this hurts performance significantly due to the additional hash table lookups, so it is now possible to turn this \u201cfeature\u201d off. If an assembly is marked with a System.Runtime.CompilerServices.CompilationRelaxationsAttribute specifying the System.Runtime.CompilerServices.CompilationRelaxations.NoStringInterning flag value, the CLR may, according to the ECMA specification, choose not to intern all of the strings defined in that assembly\u2019s metadata. Note that, in an attempt to improve your application\u2019s performance, the C# compiler always specifies this attribute/flag whenever you compile an assembly.</p><p>Even if an assembly has this attribute/flag specified, the CLR may choose to intern the strings, but you should not count on this. In fact, you really should never write code that relies on strings being interned unless you have written code that explicitly calls the String\u2019s Intern method yourself.</p><p>The following code demonstrates string interning.</p><p>String s1 = \"Hello\"; String s2 = \"Hello\"; Console.WriteLine(Object.ReferenceEquals(s1, s2)); // Should be 'False'</p><p>s1 = String.Intern(s1); s2 = String.Intern(s2); Console.WriteLine(Object.ReferenceEquals(s1, s2)); // 'True'</p><p>In the first call to the ReferenceEquals method, s1 refers to a \"Hello\" string object in the heap, and s2 refers to a different \"Hello\" string object in the heap. Because the references are different, False should be displayed. However, if you run this on version 4.5 of the CLR, you\u2019ll see that True is displayed. The reason is because this version of the CLR chooses to ignore the attribute/flag emitted by the C# compiler, and the CLR interns the literal \"Hello\" string when the assembly is loaded into the AppDomain. This means that s1 and s2 refer to the single \"Hello\" string in the heap. However, as mentioned previously, you should never write code that relies on this behavior because a future version of the CLR might honor the attribute/flag and not intern the \"Hello\" string. In fact, version 4.5 of the CLR does honor the attribute/flag when this assembly\u2019s code has been compiled using the NGen.exe utility.</p><p>Before the second call to the ReferenceEquals method, the \"Hello\" string has been explicitly interned, and s1 now refers to an interned \"Hello\". Then by calling Intern again, s2 is set to refer to the same \"Hello\" string as s1. Now, when ReferenceEquals is called the second time, we are guaranteed to get a result of True regardless of whether the assembly was compiled with the attribute/flag.</p><p>So now, let\u2019s look at an example to see how you can use string interning to improve performance and reduce memory usage. The NumTimesWordAppearsEquals method below takes two arguments: a word and an array of strings in which each array element refers to a single word. This method then determines how many times the specified word appears in the wordlist and returns this count.</p><p>private static Int32 NumTimesWordAppearsEquals(String word, String[] wordlist) { Int32 count = 0; for (Int32 wordnum = 0; wordnum &lt; wordlist.Length; wordnum++) {</p><p>if (word.Equals(wordlist[wordnum], StringComparison.Ordinal)) count++;</p><p>} return count;</p><p>}</p><p>As you can see, this method calls String\u2019s Equals method, which internally compares the strings\u2019 individual characters and checks to ensure that all characters match. This comparison can be slow. In addition, the wordlist array might have multiple entries that refer to multiple String objects containing the same set of characters. This means that multiple identical strings might exist in the heap and are surviving ongoing garbage collections.</p><p>Now, let\u2019s look at a version of this method that was written to take advantage of string interning.</p><p>private static Int32 NumTimesWordAppearsIntern(String word, String[] wordlist) { // This method assumes that all entries in wordlist refer to interned strings. word = String.Intern(word); Int32 count = 0; for (Int32 wordnum = 0; wordnum &lt; wordlist.Length; wordnum++) {</p><p>if (Object.ReferenceEquals(word, wordlist[wordnum])) count++;</p><p>} return count;</p><p>}</p><p>This method interns the word and assumes that the wordlist contains references to interned strings. First, this version might be saving memory if a word appears in the wordlist multiple times because, in this version, wordlist would now contain multiple references to the same single String object in the heap. Second, this version will be faster because determining if the specified word is in the array is simply a matter of comparing pointers.</p><p>Although the NumTimesWordAppearsIntern method is faster than the NumTimesWordAppearsEquals method, the overall performance of the application might be slower when using the NumTimesWordAppearsIntern method because of the time it takes to intern all of the strings when they were added to the wordlist array (code not shown). The NumTimesWordAppearsIntern method will really show its performance and memory improvement if the application needs to call the method multiple times using the same wordlist. The point of this discussion is to make it clear that string interning is useful, but it should be used with care and caution. In fact, this is why the C# compiler indicates that it doesn\u2019t want string interning to be enabled.</p><h6>String Pooling</h6><p>When compiling source code, your compiler must process each literal string and emit the string into the managed module\u2019s metadata. If the same literal string appears several times in your source code, emitting all of these strings into the metadata will bloat the size of the resulting file.</p><p>To remove this bloat, many compilers (including the C# compiler) write the literal string into the module\u2019s metadata only once. All code that references the string will be modified to refer to the one string in the metadata. This ability of a compiler to merge multiple occurrences of a single string into a single instance can reduce the size of a module substantially. This process is nothing new\u2014C/C++ compilers have been doing it for years. (Microsoft\u2019s C/C++ compiler calls this string pooling.) Even so, string pooling is another way to improve the performance of strings and just one more piece of knowledge that you should have in your repertoire.</p><h6>Examining a String\u2019s Characters and Text Elements</h6><p>Although comparing strings is useful for sorting them or for detecting equality, sometimes you need just to examine the characters within a string. The String type offers several properties and methods to help you do this, including Length, Chars (an indexer in C#), GetEnumerator, ToCharArray, Contains, IndexOf, LastIndexOf, IndexOfAny, and LastIndexOfAny.</p><p>In reality, a System.Char represents a single 16-bit Unicode code value that doesn\u2019t necessarily</p><p>equate to an abstract Unicode character. For example, some abstract Unicode characters are a combination of two code values. When combined, the U+0625 (the Arabic letter Alef with Hamza below) and U+0650 (the Arabic Kasra) characters form a single abstract character or text element.</p><p>In addition, some Unicode text elements require more than a 16-bit value to represent them. These text elements are represented using two 16-bit code values. The first code value is called the high surrogate, and the second code value is called the low surrogate. High surrogates have a value between U+D800 and U+DBFF, and low surrogates have a value between U+DC00 and U+DFFF. The use of surrogates allows Unicode to express more than a million different characters.</p><p>Surrogates are rarely used in the United States and Europe but are more commonly used in East Asia. To properly work with text elements, you should use the System.Globalization.StringInfo type. The easiest way to use this type is to construct an instance of it, passing its constructor a string. Then you can see how many text elements are in the string by querying the StringInfo\u2019s LengthInTextElements property. You can then call StringInfo\u2019s SubstringByTextElements method to extract the text element or the number of consecutive text elements that you desire.</p><p>In addition, the StringInfo class offers a static GetTextElementEnumerator method, which acquires a System.Globalization.TextElementEnumerator object that allows you to enumerate through all of the abstract Unicode characters contained in the string. Finally, you could call StringInfo\u2019s static ParseCombiningCharacters method to obtain an array of Int32 values. The length</p><p>of the array indicates how many text elements are contained in the string. Each element of the array identifies an index into the string where the first code value for a new text element can be found.</p><p>The following code demonstrates the various ways of using the StringInfo class to manipulate a string\u2019s text elements.</p><p>using System; using System.Text; using System.Globalization; using System.Windows.Forms;</p><p>public sealed class Program {</p><p>public static void Main() { // The string below contains combining characters String s = \"a\\u0304\\u0308bc\\u0327\"; SubstringByTextElements(s); EnumTextElements(s); EnumTextElementIndexes(s);</p><p>}</p><p>private static void SubstringByTextElements(String s) { String output = String.Empty;</p><p>}</p><p>StringInfo si = new StringInfo(s); for (Int32 element = 0; element &lt; si.LengthInTextElements; element++) {</p><p>output += String.Format( \"Text element {0} is '{1}'{2}\", element, si.SubstringByTextElements(element, 1), Environment.NewLine);</p><p>} MessageBox.Show(output, \"Result of SubstringByTextElements\");</p><p>private static void EnumTextElements(String s) { String output = String.Empty;</p><p>}</p><p>TextElementEnumerator charEnum =</p><p>StringInfo.GetTextElementEnumerator(s); while (charEnum.MoveNext()) {</p><p>output += String.Format( \"Character at index {0} is '{1}'{2}\", charEnum.ElementIndex, charEnum.GetTextElement(), Environment.NewLine);</p><p>} MessageBox.Show(output, \"Result of GetTextElementEnumerator\");</p><p>private static void EnumTextElementIndexes(String s) { String output = String.Empty;</p><p>} }</p><p>Int32[] textElemIndex = StringInfo.ParseCombiningCharacters(s); for (Int32 i = 0; i &lt; textElemIndex.Length; i++) {</p><p>output += String.Format( \"Character {0} starts at index {1}{2}\", i, textElemIndex[i], Environment.NewLine);</p><p>} MessageBox.Show(output, \"Result of ParseCombiningCharacters\");</p><p>Building and running this code produces the message boxes shown in Figures 14-2, 14-3, and 14-4.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile109.png\"/><figcaption>FIGURE 14-2 Result of SubstringByTextElements.</figcaption><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile110.png\"/><ul style=\"list-style-type: none;\">\n<li><p>FIGURE 14-3 Result of GetTextElementEnumerator.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile111.png\"/>\n</li>\n<li><p>FIGURE 14-4 Result of ParseCombiningCharacters.</p></li>\n</ul><h6>Other String Operations</h6><p>The String type also offers methods that allow you to copy a string or parts of it. Table 14-1 summarizes these methods.</p><p>TABLE 14-1 Methods for Copying Strings</p><table border=\"1\">\n<tr>\n<td><p>Member</p>\n<p>Clone</p>\n</td>\n<td><p>Method Type Instance</p>\n</td>\n<td><h6>Description</h6>\n<p>Returns a reference to the same object (this). This is OK because String objects are immutable. This method implements String\u2019s ICloneable interface.</p>\n</td>\n</tr>\n<tr>\n<td><p>Copy</p>\n</td>\n<td><p>Static</p>\n</td>\n<td><p>Returns a new duplicate string of the specified string. This method is rarely used and exists to help applications that treat strings as tokens. Normally, strings with the same set of characters are interned to a single string. This method creates a new string object so that the references (pointers) are different even though the strings contain the same characters.</p>\n</td>\n</tr>\n<tr>\n<td><p>CopyTo</p>\n</td>\n<td><p>Instance</p>\n</td>\n<td><p>Copies a portion of the string\u2019s characters to an array of characters.</p>\n</td>\n</tr>\n<tr>\n<td><p>Substring</p>\n</td>\n<td><p>Instance</p>\n</td>\n<td><p>Returns a new string that represents a portion of the original string.</p>\n</td>\n</tr>\n<tr>\n<td><p>ToString</p>\n</td>\n<td><p>Instance</p>\n</td>\n<td><p>Returns a reference to the same object (this).</p>\n</td>\n</tr>\n</table><figcaption>In addition to these methods, String offers many static and instance methods that manipulate a string, such as Insert, Remove, PadLeft, Replace, Split, Join, ToLower, ToUpper, Trim, Concat, Format, and so on. Again, the important thing to remember about all of these methods is that they return new string objects; because strings are immutable, after they\u2019re created, they can\u2019t be modified (using safe code).</figcaption><h6>Constructing a String Efficiently</h6><p>Because the String type represents an immutable string, the FCL provides another type, System. Text.StringBuilder, which allows you to perform dynamic operations efficiently with strings and characters to create a String. Think of StringBuilder as a fancy constructor to create a String that can be used with the rest of the framework. In general, you should design methods that take String parameters, not StringBuilder parameters.</p><p>Logically, a StringBuilder object contains a field that refers to an array of Char structures. StringBuilder\u2019s members allow you to manipulate this character array, effectively shrinking the string or changing the characters in the string. If you grow the string past the allocated array of characters, the StringBuilder automatically allocates a new, larger array, copies the characters, and starts using the new array. The previous array is garbage collected.</p><p>When finished using the StringBuilder object to construct your string, \u201cconvert\u201d the StringBuilder\u2019s character array into a String simply by calling the StringBuilder\u2019s ToString method. This creates a new String object in the heap that contains the string that was in the StringBuilder at the time you called ToString. At this point, you can continue to manipulate the string inside the StringBuilder, and later you can call ToString again to convert it into another String object.</p><h6>Constructing a StringBuilder Object</h6><p>Unlike with the String class, the CLR has no special information about the StringBuilder class. In addition, most languages (including C#) don\u2019t consider the StringBuilder class to be a primitive type. You construct a StringBuilder object as you would any other non-primitive type.</p><p>StringBuilder sb = new StringBuilder();</p><p>The StringBuilder type offers many constructors. The job of each constructor is to allocate and initialize the state maintained by each StringBuilder object:</p><p>\u25a0\u25a0 Maximum capacity An Int32 value that specifies the maximum number of characters that can be placed in the string. The default is Int32.MaxValue (approximately 2 billion). It\u2019s unusual to change this value. However, you might specify a smaller maximum capacity to ensure that you never create a string over a certain length. Once constructed, a StringBuilder\u2019s maximum capacity value can\u2019t be changed.</p><p>\u25a0\u25a0 Capacity An Int32 value indicating the size of the character array being maintained by the StringBuilder. The default is 16. If you have some idea of how many characters you\u2019ll place in the StringBuilder, you should use this number to set the capacity when constructing the StringBuilder object.</p><p>When appending characters to the character array, the StringBuilder detects if the array is trying to grow beyond the array\u2019s capacity. If it is, the StringBuilder automatically doubles the capacity field, allocates a new array (the size of the new capacity), and copies the characters from the original array into the new array. The original array will be garbage collected</p><p>in the future. Dynamically growing the array hurts performance; avoid this by setting a good initial capacity.</p><p>\u25a0\u25a0 Character array An array of Char structures that maintains the set of characters in the \u201cstring.\u201d The number of characters is always less than or equal to the capacity and maximum capacity values. You can use the StringBuilder\u2019s Length property to obtain the number of characters used in the array. The Length is always less than or equal to the StringBuilder\u2019s capacity value. When constructing a StringBuilder, you can pass a String to initialize the character array. If you don\u2019t specify a string, the array initially contains no characters\u2014that is, the Length property returns 0.</p><h6>StringBuilder Members</h6><p>Unlike a String, a StringBuilder represents a mutable string. This means that most of StringBuilder\u2019s members change the contents in the array of characters and don\u2019t cause new objects to be allocated on the managed heap. A StringBuilder allocates a new object on only two occasions:</p><p>\u25a0\u25a0 You dynamically build a string whose length is longer than the capacity you\u2019ve set.</p><p>\u25a0\u25a0 You call StringBuilder\u2019s ToString method. Table 14-2 summarizes StringBuilder\u2019s members.</p><figcaption>TABLE 14-2 StringBuilder Members</figcaption><table border=\"1\">\n<tr>\n<td><p>Member</p>\n<p>MaxCapacity</p>\n</td>\n<td><p>Member Type Read-only property</p>\n</td>\n<td><h6>Description</h6>\n<p>Returns the largest number of characters that can be placed in the string.</p>\n</td>\n</tr>\n<tr>\n<td><p>Capacity</p>\n</td>\n<td><p>Read/write property</p>\n</td>\n<td><p>Gets or sets the size of the character array. Trying to set the capacity smaller than the string\u2019s length or bigger than MaxCapacity throws an ArgumentOutOfRangeException.</p>\n</td>\n</tr>\n<tr>\n<td><p>EnsureCapacity</p>\n</td>\n<td><p>Method</p>\n</td>\n<td><p>Guarantees that the character array is at least the size specified. If the value passed is larger than the StringBuilder\u2019s current capacity, the current capacity increases. If the current capacity is already larger than the value passed to this method, no change occurs.</p>\n</td>\n</tr>\n<tr>\n<td><p>Length</p>\n</td>\n<td><p>Read/write property</p>\n</td>\n<td><p>Gets or sets the number of characters in the \u201cstring.\u201d This will likely be smaller than the character array\u2019s current capacity. Setting this property to 0 resets the StringBuilder\u2019s contents to an empty string.</p>\n</td>\n</tr>\n<tr>\n<td><p>ToString</p>\n</td>\n<td><p>Method</p>\n</td>\n<td><p>The parameterless version of this method returns a String representing the StringBuilder\u2019s character array.</p>\n</td>\n</tr>\n<tr>\n<td><p>Chars</p>\n</td>\n<td><p>Read/write indexer property</p>\n</td>\n<td><p>Gets or sets the character at the specified index into the character array. In C#, this is an indexer (parameterful property) that you access using array syntax ([]).</p>\n</td>\n</tr>\n<tr>\n<td><p>Clear</p>\n</td>\n<td><p>Method</p>\n</td>\n<td><p>Clears the contents of the StringBuilder object, the same as setting its Length property to 0.</p>\n</td>\n</tr>\n</table><table border=\"1\">\n<tr>\n<td><p>Member</p>\n<p>Append</p>\n</td>\n<td><p>Member Type Method</p>\n</td>\n<td><h6>Description</h6>\n<p>Appends a single object to the end of the character array, growing the array if necessary. The object is converted to a string by using the general format and the culture associated with the calling thread.</p>\n</td>\n</tr>\n<tr>\n<td><p>Insert</p>\n</td>\n<td><p>Method</p>\n</td>\n<td><p>Inserts a single object into the character array, growing the array if necessary. The object is converted to a string by using the general format and the culture associated with the calling thread.</p>\n</td>\n</tr>\n<tr>\n<td><p>AppendFormat</p>\n</td>\n<td><p>Method</p>\n</td>\n<td><p>Appends the specified objects to the end of the character array, growing the array if necessary. The objects are converted to strings by using the formatting and culture information provided by the caller. AppendFormat is one of the most common methods used with StringBuilder objects.</p>\n</td>\n</tr>\n<tr>\n<td><p>AppendLine</p>\n</td>\n<td><p>Method</p>\n</td>\n<td><p>Appends a blank line or a string with a blank line to the end of the character array, increasing the capacity of the array if necessary.</p>\n</td>\n</tr>\n<tr>\n<td><p>Replace</p>\n</td>\n<td><p>Method</p>\n</td>\n<td><p>Replaces one character with another or one string with another from within the character array.</p>\n</td>\n</tr>\n<tr>\n<td><p>Remove</p>\n</td>\n<td><p>Method</p>\n</td>\n<td><p>Removes a range of characters from the character array.</p>\n</td>\n</tr>\n<tr>\n<td><p>Equals</p>\n</td>\n<td><p>Method</p>\n</td>\n<td><p>Returns true only if both StringBuilder objects have the same maximum capacity, capacity, and characters in the array.</p>\n</td>\n</tr>\n<tr>\n<td><p>CopyTo</p>\n</td>\n<td><p>Method</p>\n</td>\n<td><p>Copies a subset of the StringBuilder\u2019s characters to a Char array.</p>\n</td>\n</tr>\n</table><figcaption>One important thing to note about StringBuilder\u2019s methods is that most of them return a reference to the same StringBuilder object. This allows a convenient syntax to chain several operations together.</figcaption><p>StringBuilder sb = new StringBuilder(); String s = sb.AppendFormat(\"{0} {1}\", \"Jeffrey\", \"Richter\").</p><p>Replace(' ', '\u00ad').Remove(4, 3).ToString(); Console.WriteLine(s); // \"Jeff\u00adRichter\"</p><p>You\u2019ll notice that the String and StringBuilder classes don\u2019t have full method parity; that is, String has ToLower, ToUpper, EndsWith, PadLeft, PadRight, Trim, and so on. The StringBuilder class doesn\u2019t offer any of these methods. On the other hand, the StringBuilder class offers a richer Replace method that allows you to replace characters or strings in a portion of the string (not the whole string). It\u2019s unfortunate that there isn\u2019t complete parity between these two classes because now you must convert between String and StringBuilder to accomplish certain tasks. For example, to build up a string, convert all characters to uppercase, and then insert a string requires code like the following.</p><p>// Construct a StringBuilder to perform string manipulations. StringBuilder sb = new StringBuilder();</p><p>// Perform some string manipulations by using the StringBuilder. sb.AppendFormat(\"{0} {1}\", \"Jeffrey\", \"Richter\").Replace(\" \", \"\u00ad\");</p><p>// Convert the StringBuilder to a String in // order to uppercase all the characters. String s = sb.ToString().ToUpper();</p><p>// Clear the StringBuilder (allocates a new Char array). sb.Length = 0;</p><p>// Load the uppercase String into the StringBuilder, // and perform more manipulations. sb.Append(s).Insert(8, \"Marc\u00ad\");</p><p>// Convert the StringBuilder back to a String. s = sb.ToString();</p><p>// Display the String to the user. Console.WriteLine(s); // \"JEFFREY\u00adMarc\u00adRICHTER\"</p><p>It\u2019s inconvenient and inefficient to have to write this code just because StringBuilder doesn\u2019t offer all of the operations that String does. In the future, I hope that Microsoft will add more string operation methods to StringBuilder to make it a more complete class.</p><h6>Obtaining a String Representation of an Object: ToString</h6><p>You frequently need to obtain a string representation of an object. Usually, this is necessary when you want to display a numeric type (such as Byte, Int32, and Single) or a DateTime object to the user. Because the .NET Framework is an object-oriented platform, every type is responsible for providing code that converts an instance\u2019s value to a string equivalent. When designing how types should accomplish this, the designers of the FCL devised a pattern that would be used consistently throughout. In this section, I\u2019ll describe this pattern.</p><p>You can obtain a string representation for any object by calling the ToString method. A public, virtual, parameterless ToString method is defined by System.Object and is therefore callable using an instance of any type. Semantically, ToString returns a string representing the object\u2019s current value, and this string should be formatted for the calling thread\u2019s current culture; that is, the string representation of a number should use the proper decimal separator, digit-grouping symbol, and other elements associated with the culture assigned to the calling thread.</p><p>System.Object\u2019s implementation of ToString simply returns the full name of the object\u2019s type.</p><p>This value isn\u2019t particularly useful, but it is a reasonable default for the many types that can\u2019t offer a sensible string. For example, what should a string representation of a FileStream or a Hashtable object look like?</p><p>All types that want to offer a reasonable way to obtain a string representing the current value of</p><p>the object should override the ToString method. Many of the core types built into the FCL (Byte, Int32, UInt64, Double, and so on) override their ToString method and return a culturally aware</p><p>string. In the Visual Studio debugger, a datatip is displayed when the mouse is placed over a particular variable. The text shown in the datatip is obtained by calling the object\u2019s ToString method. So, when you define a class, you should always override the ToString method so that you get good debugging support.</p><h6>Specific Formats and Cultures</h6><p>The parameterless ToString method has two problems. First, the caller has no control over the formatting of the string. For example, an application might want to format a number into a currency string, decimal string, percent string, or hexadecimal string. Second, the caller can\u2019t easily choose to format a string by using a specific culture. This second problem is more troublesome for server-side application code than for client-side code. On rare occasions, an application needs to format a string by using a culture other than the culture associated with the calling thread. To have more control over string formatting, you need a version of the ToString method that allows you to specify precise formatting and culture information.</p><p>Types that offer the caller a choice in formatting and culture implement the System.IFormattable interface.</p><p>public interface IFormattable {</p><p>String ToString(String format, IFormatProvider formatProvider); }</p><p>In the FCL, all of the base types (Byte, SByte, Int16/UInt16, Int32/UInt32, Int64/UInt64, Single, Double, Decimal, and DateTime) implement this interface. In addition, some other types, such as Guid, implement it. Finally, every enumerated type definition will automatically implement the IFormattable interface so that a meaningful string symbol from an instance of the enumerated type can be obtained.</p><p>IFormattable\u2019s ToString method takes two parameters. The first, format, is a string that tells the method how the object should be formatted. ToString\u2019s second parameter, formatProvider, is an instance of a type that implements the System.IFormatProvider interface. This type supplies specific culture information to the ToString method. I\u2019ll discuss how shortly.</p><p>The type implementing the IFormattable interface\u2019s ToString method determines which format strings it\u2019s going to recognize. If you pass a format string that the type doesn\u2019t recognize, the type is supposed to throw a System.FormatException.</p><p>Many of the types Microsoft has defined in the FCL recognize several formats. For example, the DateTime type supports \u201cd\u201d for short date, \u201cD\u201d for long date, \u201cg\u201d for general, \u201cM\u201d for month/day, \u201cs\u201d for sortable, \u201cT\u201d for long time, \u201cu\u201d for universal time in ISO 8601 format, \u201cU\u201d for universal time in full date format, \u201cY\u201d for year/month, and others. All enumerated types support \u201cG\u201d for general, \u201cF\u201d for flags, \u201cD\u201d for decimal, and \u201cX\u201d for hexadecimal. I\u2019ll cover formatting enumerated types in more detail in Chapter 15, \u201cEnumerated Types and Bit Flags.\u201d</p><p>Also, all of the built-in numeric types support \u201cC\u201d for currency, \u201cD\u201d for decimal, \u201cE\u201d for exponential (scientific) notation, \u201cF\u201d for fixed-point, \u201cG\u201d for general, \u201cN\u201d for number, \u201cP\u201d for percent, \u201cR\u201d for round-trip, and \u201cX\u201d for hexadecimal. In fact, the numeric types also support picture format strings just in case the simple format strings don\u2019t offer you exactly what you\u2019re looking for. Picture format strings contain special characters that tell the type\u2019s ToString method exactly how many digits to</p><p>show, exactly where to place a decimal separator, exactly how many digits to place after the decimal separator, and so on. For complete information about format strings, see \u201cFormatting Types\u201d in the</p><p>.NET Framework SDK.</p><p>For most types, calling ToString and passing null for the format string is identical to calling ToString and passing \u201cG\u201d for the format string. In other words, objects format themselves using the \u201cGeneral format\u201d by default. When implementing a type, choose a format that you think will be the most commonly used format; this format is the \u201cGeneral format.\u201d By the way, the ToString method that takes no parameters assumes that the caller wants the \u201cGeneral format.\u201d</p><p>So now that format strings are out of the way, let\u2019s turn to culture information. By default, strings are formatted using the culture information associated with the calling thread. The parameterless ToString method certainly does this, and so does IFormattable\u2019s ToString if you pass null for the formatProvider parameter.</p><p>Culture-sensitive information applies when you\u2019re formatting numbers (including currency, integers, floating point, percentages, dates, and times). The Guid type has a ToString method that returns only a string representing its value. There\u2019s no need to consider a culture when generating the Guid\u2019s string because GUIDs are used for programmatic purposes only.</p><p>When formatting a number, the ToString method sees what you\u2019ve passed for the formatProvider parameter. If null is passed, ToString determines the culture associated with the calling thread by reading the System.Globalization.CultureInfo.CurrentCulture property. This property returns an instance of the System.Globalization.CultureInfo type.</p><p>Using this object, ToString reads its NumberFormat or DateTimeFormat property, depending on whether a number or date/time is being formatted. These properties return an instance of System.Globalization.NumberFormatInfo or System.Globalization.DateTimeFormatInfo, respectively. The NumberFormatInfo type defines a bunch of properties, such as CurrencyDecimalSeparator, CurrencySymbol, NegativeSign, NumberGroupSeparator, and PercentSymbol. Likewise, the DateTimeFormatInfo type defines an assortment of properties,</p><p>such as Calendar, DateSeparator, DayNames, LongDatePattern, ShortTimePattern, and TimeSeparator. ToString reads these properties when constructing and formatting a string.</p><p>When calling IFormattable\u2019s ToString method, instead of passing null, you can pass a reference to an object whose type implements the IFormatProvider interface.</p><p>public interface IFormatProvider {</p><p>Object GetFormat(Type formatType); }</p><p>Here\u2019s the basic idea behind the IFormatProvider interface: when a type implements this interface, it is saying that an instance of the type is able to provide culture-specific formatting information and that the culture information associated with the calling thread should be ignored.</p><p>The System.Globalization.CultureInfo type is one of the very few types defined in the FCL that implements the IFormatProvider interface. If you want to format a string for, say, Vietnam, you\u2019d construct a CultureInfo object and pass that object in as ToString\u2019s formatProvider parameter. The following code obtains a string representation of a Decimal numeric value formatted as currency appropriate for Vietnam.</p><p>Decimal price = 123.54M; String s = price.ToString(\"C\", new CultureInfo(\"vi\u00adVN\")); MessageBox.Show(s);</p><p>If you build and run this code, the message box shown in Figure 14-5 appears.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile112.png\"/><figcaption>FIGURE 14-5 Numeric value formatted correctly to represent Vietnamese currency.</figcaption><p>Internally, Decimal\u2019s ToString method sees that the formatProvider argument is not null and calls the object\u2019s GetFormat method as follows.</p><p>NumberFormatInfo nfi = (NumberFormatInfo) formatProvider.GetFormat(typeof(NumberFormatInfo));</p><p>This is how ToString requests the appropriate number-formatting information from the (CultureInfo) object. Number types (such as Decimal) request only number-formatting information. But other types (such as DateTime) could call GetFormat like the following.</p><p>DateTimeFormatInfo dtfi = (DateTimeFormatInfo) formatProvider.GetFormat(typeof(DateTimeFormatInfo));</p><p>Actually, because GetFormat\u2019s parameter can identify any type, the method is flexible enough to allow any type of format information to be requested. The types in the .NET Framework call GetFormat, requesting only number or date/time information; in the future, other kinds of formatting information could be requested.</p><p>By the way, if you want to obtain a string for an object that isn\u2019t formatted for any particular culture, you should call System.Globalization.CultureInfo\u2019s static InvariantCulture property and pass the object returned as ToString\u2019s formatProvider parameter.</p><p>Decimal price = 123.54M; String s = price.ToString(\"C\", CultureInfo.InvariantCulture); MessageBox.Show(s);</p><p>If you build and run this code, the message box shown in Figure 14-6 appears. Notice the first character in the resulting string: \u00a4. This is the international sign for currency (U+00A4).</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile113.png\"/><figcaption>FIGURE 14-6 Numeric value formatted to represent a culture-neutral currency.</figcaption><p>Normally, you wouldn\u2019t display a string formatted by using the invariant culture to a user. Typically, you\u2019d just save this string in a data file so that it could be parsed later.</p><p>In the FCL, just three types implement the IFormatProvider interface. The first is CultureInfo, which I\u2019ve already explained. The other two are NumberFormatInfo and DateTimeFormatInfo. When GetFormat is called on a NumberFormatInfo object, the method checks whether the type being requested is a NumberFormatInfo. If it is, this is returned; if it\u2019s not, null is returned. Similarly, calling GetFormat on a DateTimeFormatInfo object returns this if a DateTimeFormatInfo is requested and null if it\u2019s not. These two types implement this interface simply as a programming convenience. When trying to obtain a string representation of an object, the caller commonly specifies a format and uses the culture associated with the calling thread. For this reason, you often call ToString, passing a string for the format parameter and null for the formatProvider parameter. To make calling ToString easier for you, many types offer several overloads of the ToString method. For example, the Decimal type offers four different ToString methods.</p><p>// This version calls ToString(null, null). // Meaning: General numeric format, thread's culture information public override String ToString();</p><p>// This version is where the actual implementation of ToString goes. // This version implements IFormattable's ToString method. // Meaning: Caller\u00adspecified format and culture information public String ToString(String format, IFormatProvider formatProvider);</p><p>// This version simply calls ToString(format, null). // Meaning: Caller\u00adspecified format, thread's culture information public String ToString(String format);</p><p>// This version simply calls ToString(null, formatProvider). // This version implements IConvertible's ToString method. // Meaning: General format, caller\u00adspecified culture information public String ToString(IFormatProvider formatProvider);</p><h6>Formatting Multiple Objects into a Single String</h6><p>So far, I\u2019ve explained how an individual type formats its own objects. At times, however, you want to construct strings consisting of many formatted objects. For example, the following string has a date, a person\u2019s name, and an age.</p><p>String s = String.Format(\"On {0}, {1} is {2} years old.\",</p><p>new DateTime(2012, 4, 22, 14, 35, 5), \"Aidan\", 9); Console.WriteLine(s);</p><p>If you build and run this code where \u201cen-US\u201d is the thread\u2019s current culture, you\u2019ll see the following line of output.</p><p>On 4/22/2012 2:35:05 PM, Aidan is 9 years old.</p><p>String\u2019s static Format method takes a format string that identifies replaceable parameters by us-</p><p>ing numbers in braces. The format string used in this example tells the Format method to replace {0} with the first parameter after the format string (the date/time), replace {1} with the second parameter after the format string (\u201cAidan\u201d), and replace {2} with the third parameter after the format string (7).</p><p>Internally, the Format method calls each object\u2019s ToString method to obtain a string representation for the object. Then the returned strings are all appended and the complete, final string is returned. This is all fine and good, but it means that all of the objects are formatted by using their general format and the calling thread\u2019s culture information.</p><p>You can have more control when formatting an object if you specify format information within braces. For example, the following code is identical to the previous example except that I\u2019ve added formatting information to replaceable parameters 0 and 2.</p><p>String s = String.Format(\"On {0:D}, {1} is {2:E} years old.\",</p><p>new DateTime(2012, 4, 22, 14, 35, 5), \"Aidan\", 9); Console.WriteLine(s);</p><p>If you build and run this code where \u201cen-US\u201d is the thread\u2019s current culture, you\u2019ll see the following line of output.</p><p>On Sunday, April 22, 2012, Aidan is 9.000000E+000 years old.</p><p>When the Format method parses the format string, it sees that replaceable parameter 0 should have its IFormattable interface\u2019s ToString method called passing \"D\" and null for its two parameters. Likewise, Format calls replaceable parameter 2\u2019s IFormattable ToString method, passing \"E\" and null. If the type doesn\u2019t implement the IFormattable interface, Format calls its parameterless ToString method inherited from Object (and possibly overridden), and the default format is appended into the resulting string.</p><p>The String class offers several overloads of the static Format method. One version takes an object that implements the IFormatProvider interface so that you can format all of the replaceable parameters by using caller-specified culture information. Obviously, Format calls each object\u2019s IFormattable.ToString method, passing it whatever IFormatProvider object was passed to Format.</p><p>If you\u2019re using StringBuilder instead of String to construct a string, you can call StringBuilder\u2019s AppendFormat method. This method works exactly as String\u2019s Format method except that it formats a string and appends to the StringBuilder\u2019s character array. As does String\u2019s Format, AppendFormat takes a format string, and there\u2019s a version that takes an IFormatProvider.</p><p>System.Console offers Write and WriteLine methods that also take format strings and replaceable parameters. However, there are no overloads of Console\u2019s Write and WriteLine methods that allow you to pass an IFormatProvider. If you want to format a string for a specific culture, you have to call String\u2019s Format method, first passing the desired IFormatProvider object and then passing the resulting string to Console\u2019s Write or WriteLine method. This shouldn\u2019t be a big deal because, as I said earlier, it\u2019s rare for client-side code to format a string by using a culture other than the one associated with the calling thread.</p><h6>Providing Your Own Custom Formatter</h6><p>By now it should be clear that the formatting capabilities in the .NET Framework were designed to offer you a great deal of flexibility and control. However, we\u2019re not quite finished. It\u2019s possible for you to define a method that StringBuilder\u2019s AppendFormat method will call whenever any object is being formatted into a string. In other words, instead of calling ToString for each object, AppendFormat can call a function you define, allowing you to format any or all of the objects in any way you want. What I\u2019m about to describe also works with String\u2019s Format method.</p><p>Let me explain this mechanism by way of an example. Let\u2019s say that you\u2019re formatting HTML text that a user will view in an Internet browser. You want all Int32 values to appear in bold. To accomplish this, every time an Int32 value is formatted into a String, you want to surround the string with HTML bold tags: &lt;B&gt; and &lt;/B&gt;. The following code demonstrates how easy it is to do this.</p><p>using System; using System.Text; using System.Threading;</p><p>public static class Program {</p><p>public static void Main() { StringBuilder sb = new StringBuilder(); sb.AppendFormat(new BoldInt32s(), \"{0} {1} {2:M}\", \"Jeff\", 123, DateTime.Now); Console.WriteLine(sb);</p><p>} }</p><p>internal sealed class BoldInt32s : IFormatProvider, ICustomFormatter {</p><p>public Object GetFormat(Type formatType) { if (formatType == typeof(ICustomFormatter)) return this; return Thread.CurrentThread.CurrentCulture.GetFormat(formatType);</p><p>}</p><p>public String Format(String format, Object arg, IFormatProvider formatProvider) { String s;</p><p>IFormattable formattable = arg as IFormattable;</p><p>if (formattable == null) s = arg.ToString(); else s = formattable.ToString(format, formatProvider);</p><p>} }</p><p>if (arg.GetType() == typeof(Int32))</p><p>return \"&lt;B&gt;\" + s + \"&lt;/B&gt;\"; return s;</p><p>When you compile and run this code where \u201cen-US\u201d is the thread\u2019s current culture, it displays the following output (your date may be different, of course).</p><p>Jeff &lt;B&gt;123&lt;/B&gt; September 1</p><p>In Main, I\u2019m constructing an empty StringBuilder and then appending a formatted string into it. When I call AppendFormat, the first parameter is an instance of the BoldInt32s class. This class implements the IFormatProvider interface that I discussed earlier. In addition, this class implements the ICustomFormatter interface.</p><p>public interface ICustomFormatter { String Format(String format, Object arg,</p><p>IFormatProvider formatProvider); }</p><p>This interface\u2019s Format method is called whenever StringBuilder\u2019s AppendFormat needs to obtain a string for an object. You can do some pretty clever things inside this method that give you a great deal of control over string formatting. Let\u2019s look inside the AppendFormat method to see exactly how it works. The following pseudocode shows how AppendFormat works.</p><p>public StringBuilder AppendFormat(IFormatProvider formatProvider, String format, params Object[] args) {</p><p>// If an IFormatProvider was passed, find out // whether it offers an ICustomFormatter object. ICustomFormatter cf = null;</p><p>if (formatProvider != null) cf = (ICustomFormatter) formatProvider.GetFormat(typeof(ICustomFormatter));</p><p>// Keep appending literal characters (not shown in this pseudocode) // and replaceable parameters to the StringBuilder's character array. Boolean MoreReplaceableArgumentsToAppend = true; while (MoreReplaceableArgumentsToAppend) {</p><p>// argFormat refers to the replaceable format string obtained // from the format parameter String argFormat = /* ... */;</p><p>// argObj refers to the corresponding element // from the args array parameter Object argObj = /* ... */;</p><p>// argStr will refer to the formatted string to be appended // to the final, resulting string String argStr = null;</p><p>// If a custom formatter is available, let it format the argument. if (cf != null)</p><p>argStr = cf.Format(argFormat, argObj, formatProvider);</p><p>// If there is no custom formatter or if it didn't format // the argument, try something else. if (argStr == null) {</p><p>// Does the argument's type support rich formatting? IFormattable formattable = argObj as IFormattable; if (formattable != null) {</p><p>// Yes; pass the format string and provider to // the type's IFormattable ToString method. argStr = formattable.ToString(argFormat, formatProvider);</p><p>} else { // No; get the default format by using // the thread's culture information. if (argObj != null) argStr = argObj.ToString(); else argStr = String.Empty;</p><p>}</p><p>} // Append argStr's characters to the character array field member. /* ... */</p><p>}</p><p>// Check if any remaining parameters to format MoreReplaceableArgumentsToAppend = /* ... */;</p><p>} return this;</p><p>When Main calls AppendFormat, AppendFormat calls my format provider\u2019s GetFormat method, passing it the ICustomFormatter type. The GetFormat method defined in my BoldInt32s type sees that the ICustomFormatter is being requested and returns a reference to itself because it implements this interface. If my GetFormat method is called and is passed any other type, I call the GetFormat method of the CultureInfo object associated with the calling thread.</p><p>Whenever AppendFormat needs to format a replaceable parameter, it calls ICustomFormatter\u2019s Format method. In my example, AppendFormat calls the Format method defined by my BoldInt32s type. In my Format method, I check whether the object being formatted supports rich formatting via the IFormattable interface. If the object doesn\u2019t, I then call the simple, parameterless ToString method (inherited from Object) to format the object. If the object does support IFormattable, I then call the rich ToString method, passing it the format string and the format provider.</p><p>Now that I have the formatted string, I check whether the corresponding object is an Int32 type, and if it is, I wrap the formatted string in &lt;B&gt; and &lt;/B&gt; HTML tags and return the new string. If the object is not an Int32, I simply return the formatted string without any further processing.</p><h6>Parsing a String to Obtain an Object: Parse</h6><p>In the preceding section, I explained how to take an object and obtain a string representation of that object. In this section, I\u2019ll talk about the opposite: how to take a string and obtain an object representation of it. Obtaining an object from a string isn\u2019t a very common operation, but it does occasionally come in handy. Microsoft felt it necessary to formalize a mechanism by which strings can be parsed into objects.</p><p>Any type that can parse a string offers a public, static method called Parse. This method takes a String and returns an instance of the type; in a way, Parse acts as a factory. In the FCL, a Parse method exists on all of the numeric types as well as for DateTime, TimeSpan, and a few other types (such as the SQL data types).</p><p>Let\u2019s look at how to parse a string into a number type. Almost all of the numeric types (Byte, SByte, Int16/UInt16, Int32/UInt32, Int64/UInt64, Single, Double, Decimal, and BigInteger) offer at least one Parse method. Here I\u2019ll show you just the Parse method defined by the Int32 type. (The Parse methods for the other numeric types work similarly to Int32\u2019s Parse method.)</p><p>public static Int32 Parse(String s, NumberStyles style, IFormatProvider provider);</p><p>Just from looking at the prototype, you should be able to guess exactly how this method works. The String parameter, s, identifies a string representation of a number you want parsed into an Int32 object. The System.Globalization.NumberStyles parameter, style, is a set of bit flags that identify characters that Parse should expect to find in the string. And the IFormatProvider parameter, provider, identifies an object that the Parse method can use to obtain culture-specific information, as discussed earlier in this chapter.</p><p>For example, the following code causes Parse to throw a System.FormatException because the string being parsed contains a leading space.</p><p>Int32 x = Int32.Parse(\" 123\", NumberStyles.None, null);</p><p>To allow Parse to skip over the leading space, change the style parameter as follows.</p><p>Int32 x = Int32.Parse(\" 123\", NumberStyles.AllowLeadingWhite, null);</p><p>See the .NET Framework SDK documentation for a complete description of the bit symbols and common combinations that the NumberStyles enumerated type defines.</p><p>Here\u2019s a code fragment showing how to parse a hexadecimal number.</p><p>Int32 x = Int32.Parse(\"1A\", NumberStyles.HexNumber, null); Console.WriteLine(x); // Displays \"26\"</p><p>This Parse method accepts three parameters. For convenience, many types offer additional overloads of Parse so you don\u2019t have to pass as many arguments. For example, Int32 offers four overloads of the Parse method.</p><p>// Passes NumberStyles.Integer for style // and thread's culture's provider information. public static Int32 Parse(String s);</p><p>// Passes thread's culture's provider information. public static Int32 Parse(String s, NumberStyles style);</p><p>// Passes NumberStyles.Integer for the style parameter. public static Int32 Parse(String s, IFormatProvider provider);</p><p>// This is the method I've been talking about in this section. public static Int32 Parse(String s, NumberStyles style,</p><p>IFormatProvider provider);</p><p>The DateTime type also offers a Parse method.</p><p>public static DateTime Parse(String s, IFormatProvider provider, DateTimeStyles styles);</p><p>This method works just as the Parse method defined on the number types except that DateTime\u2019s Parse method takes a set of bit flags defined by the System.Globalization.DateTimeStyles enumerated type instead of the NumberStyles enumerated type. See the .NET Framework SDK documentation for a complete description of the bit symbols and common combinations the DateTimeStyles type defines.</p><p>For convenience, the DateTime type offers three overloads of the Parse method.</p><p>// Passes thread's culture's provider information // and DateTimeStyles.None for the style public static DateTime Parse(String s);</p><p>// Passes DateTimeStyles.None for the style public static DateTime Parse(String s, IFormatProvider provider);</p><p>// This is the method I've been talking about in this section. public static DateTime Parse(String s,</p><p>IFormatProvider provider, DateTimeStyles styles);</p><p>Parsing dates and times is complex. Many developers have found the Parse method of the DateTime type too forgiving in that it sometimes parses strings that don\u2019t contain dates or times. For this reason, the DateTime type also offers a ParseExact method that accepts a picture format string that indicates exactly how the date/time string should be formatted and how it should be parsed. For more information about picture format strings, see the DateTimeFormatInfo class in the .NET Framework SDK.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile114.png\"/><p>Note Some developers have reported the following back to Microsoft: when their application calls Parse frequently, and Parse throws exceptions repeatedly (due to invalid user input), performance of the application suffers. For these performance-sensitive uses of Parse, Microsoft added TryParse methods to all of the numeric data types, DateTime, DateTimeOffset, TimeSpan, and even IPAddress. This is what one of the two Int32\u2019s two TryParse method overloads looks like.</p><p>public static Boolean TryParse(String s, NumberStyles style, IFormatProvider provider, out Int32 result);</p><p>As you can see, this method returns true or false indicating whether the specified string can be parsed into an Int32. If the method returns true, the variable passed by reference to the result parameter will contain the parsed numeric value. The TryXxx pattern is discussed in Chapter 20, \u201cExceptions and State Management.\u201d</p><h6>Encodings: Converting Between Characters and Bytes</h6><p>In Win32, programmers all too frequently have to write code to convert Unicode characters and strings to Multi-Byte Character Set (MBCS) characters and strings. I\u2019ve certainly written my share of this code, and it\u2019s very tedious to write and error-prone to use. In the CLR, all characters are represented as 16-bit Unicode code values and all strings are composed of 16-bit Unicode code values. This makes working with characters and strings easy at run time.</p><p>At times, however, you want to save strings to a file or transmit them over a network. If the strings consist mostly of characters readable by English-speaking people, saving or transmitting a set of 16bit values isn\u2019t very efficient because half of the bytes written would contain zeros. Instead, it would be more efficient to encode the 16-bit values into a compressed array of bytes and then decode the array of bytes back into an array of 16-bit values.</p><p>Encodings also allow a managed application to interact with strings created by non-Unicode systems. For example, if you want to produce a file readable by an application running on a Japanese version of Windows 95, you have to save the Unicode text by using the Shift-JIS (code page 932) encoding. Likewise, y ou\u2019d use Shift-JIS encoding to read a text file produced on a Japanese Windows 95 system into the CLR.</p><p>Encoding is typically done when you want to send a string to a file or network stream by using the System.IO.BinaryWriter or System.IO.StreamWriter type. Decoding is typically done when you want to read a string from a file or network stream by using the System.IO.BinaryReader or System.IO.StreamReader type. If you don\u2019t explicitly select an encoding, all of these types default to using UTF-8. (UTF stands for Unicode Transformation Format.) However, at times, you might want to explicitly encode or decode a string. Even if you don't want to explicitly do this, this section will give you more insight into the reading and writing of strings from and to streams.</p><p>Fortunately, the FCL offers some types to make character encoding and decoding easy. The two most frequently used encodings are UTF-16 and UTF-8:</p><p>\u25a0\u25a0 UTF-16 encodes each 16-bit character as 2 bytes. It doesn\u2019t affect the characters at all, and no compression occurs\u2014its performance is excellent. UTF-16 encoding is also referred to as Unicode encoding. Also note that UTF-16 can be used to convert from little-endian to big-endian and vice versa.</p><p>\u25a0\u25a0 UTF-8 encodes some characters as 1 byte, some characters as 2 bytes, some characters as 3 bytes, and some characters as 4 bytes. Characters with a value below 0x0080 are compressed to 1 byte, which works very well for characters used in the United States. Characters between 0x0080 and 0x07FF are converted to 2 bytes, which works well for European and Middle Eastern languages. Characters of 0x0800 and above are converted to 3 bytes, which works well for East Asian languages. Finally, surrogate pairs are written out as 4 bytes. UTF-8 is an extremely popular encoding, but it\u2019s less efficient than UTF-16 if you encode many characters with values of 0x0800 or above.</p><p>Although the UTF-16 and UTF-8 encodings are by far the most common, the FCL also supports some encodings that are used less frequently:</p><p>\u25a0\u25a0 UTF-32 encodes all characters as 4 bytes. This encoding is useful when you want to write a simple algorithm to traverse characters and you don\u2019t want to have to deal with characters taking a variable number of bytes. For example, with UTF-32, you do not need to think about surrogates because every character is 4 bytes. Obviously, UTF-32 is not an efficient encoding in terms of memory usage and is therefore rarely used for saving or transmitting strings to a file or network. This encoding is typically used inside the program itself. Also note that UTF-32 can be used to convert from little-endian to big-endian and vice versa.</p><p>\u25a0\u25a0 UTF-7 encoding is typically used with older systems that work with characters that can be expressed using 7-bit values. You should avoid this encoding because it usually ends up expanding the data rather than compressing it. The Unicode Consortium has deprecated this encoding.</p><p>\u25a0\u25a0 ASCII encodes the 16-bit characters into ASCII characters; that is, any 16-bit character with a value of less than 0x0080 is converted to a single byte. Any character with a value greater than 0x007F can\u2019t be converted, so that character\u2019s value is lost. For strings consisting of characters in the ASCII range (0x00 to 0x7F), this encoding compresses the data in half and is very fast (because the high byte is just cut off). This encoding isn\u2019t appropriate if you have characters outside of the ASCII range because the character\u2019s values will be lost.</p><p>Finally, the FCL also allows you to encode 16-bit characters to an arbitrary code page. As with the ASCII encoding, encoding to a code page is dangerous because any character whose value can\u2019t be expressed in the specified code page is lost. You should always use UTF-16 or UTF-8 encoding unless you must work with some legacy files or applications that already use one of the other encodings.</p><p>When you need to encode or decode a set of characters, you should obtain an instance of a class derived from System.Text.Encoding. Encoding is an abstract base class that offers several static readonly properties, each of which returns an instance of an Encoding-derived class.</p><p>Here\u2019s an example that encodes and decodes characters by using UTF-8.</p><p>using System; using System.Text;</p><p>public static class Program {</p><p>public static void Main() { // This is the string we're going to encode. String s = \"Hi there.\";</p><p>// Obtain an Encoding\u00adderived object that knows how // to encode/decode using UTF8 Encoding encodingUTF8 = Encoding.UTF8;</p><p>// Encode a string into an array of bytes. Byte[] encodedBytes = encodingUTF8.GetBytes(s);</p><p>// Show the encoded byte values. Console.WriteLine(\"Encoded bytes: \" +</p><p>BitConverter.ToString(encodedBytes));</p><p>// Decode the byte array back to a string. String decodedString = encodingUTF8.GetString(encodedBytes);</p><p>} }</p><p>// Show the decoded string. Console.WriteLine(\"Decoded string: \" + decodedString);</p><p>This code yields the following output.</p><p>Encoded bytes: 48\u00ad69\u00ad20\u00ad74\u00ad68\u00ad65\u00ad72\u00ad65\u00ad2E Decoded string: Hi there.</p><p>In addition to the UTF8 static property, the Encoding class also offers the following static properties: Unicode, BigEndianUnicode, UTF32, UTF7, ASCII, and Default. The Default property returns an object that is able to encode/decode using the user\u2019s code page as specified by the Language For Non-Unicode Programs option of the Region/Administrative dialog box in Control Panel. (See the GetACP Win32 function for more information.) However, using the Default property is discouraged because your application\u2019s behavior would be machine-setting dependent, so if you change the system\u2019s default code page or if your application runs on another machine, your application will behave differently.</p><p>In addition to these properties, Encoding also offers a static GetEncoding method that allows you to specify a code page (by integer or by string) and returns an object that can encode/decode using the specified code page. You can call GetEncoding, passing \"Shift\u00adJIS\" or 932, for example.</p><p>When you first request an encoding object, the Encoding class\u2019s property or GetEncoding method constructs a single object for the requested encoding and returns this object. If an alreadyrequested encoding object is requested in the future, the encoding class simply returns the object it previously constructed; it doesn\u2019t construct a new object for each request. This efficiency reduces the number of objects in the system and reduces pressure in the garbage-collected heap.</p><p>Instead of calling one of Encoding\u2019s static properties or its GetEncoding method, you could also construct an instance of one of the following classes: System.Text.UnicodeEncoding,</p><p>System.Text.UTF8Encoding, System.Text.UTF32Encoding, System.Text.UTF7Encoding, or System.Text.ASCIIEncoding. However, keep in mind that constructing any of these classes creates new objects in the managed heap, which hurts performance.</p><p>Four of these classes, UnicodeEncoding, UTF8Encoding, UTF32Encoding, and UTF7Encoding, offer multiple constructors, providing you with more control over the encoding and preamble. (Preamble is sometimes referred to as a byte order mark or BOM.) The first three aforementioned classes also offer constructors that let you tell the class to throw exceptions when decoding an invalid byte sequence; you should use these constructors when you want your application to be secure and resistant to invalid incoming data.</p><p>You might want to explicitly construct instances of these encoding types when working with a BinaryWriter or a StreamWriter. The ASCIIEncoding class has only a single constructor and therefore doesn\u2019t offer any more control over the encoding. If you need an ASCIIEncoding object, always obtain it by querying Encoding\u2019s ASCII property; this returns a reference to a single ASCIIEncoding object. If you construct ASCIIEncoding objects yourself, you are creating more objects on the heap, which hurts your application\u2019s performance.</p><p>After you have an Encoding-derived object, you can convert a string or an array of characters to an array of bytes by calling the GetBytes method. (Several overloads of this method exist.) To convert an array of bytes to an array of characters or a string, call the GetChars method or the more useful GetString method. (Several overloads exist for both of these methods.) The preceding code demonstrated calls to the GetBytes and GetString methods.</p><p>All Encoding-derived types offer a GetByteCount method that obtains the number of bytes necessary to encode a set of characters without actually encoding. Although GetByteCount isn\u2019t especially useful, you can use this method to allocate an array of bytes. There\u2019s also a GetCharCount method that returns the number of characters that would be decoded without actually decoding them. These methods are useful if you\u2019re trying to save memory and reuse an array.</p><p>The GetByteCount/GetCharCount methods aren\u2019t that fast because they must analyze the array of characters/bytes in order to return an accurate result. If you prefer speed to an exact result, you can call the GetMaxByteCount or GetMaxCharCount method instead. Both methods take an integer specifying the number of bytes or number of characters and return a worst-case value.</p><p>Each Encoding-derived object offers a set of public read-only properties that you can query to obtain detailed information about the encoding. See the .NET Framework SDK documentation for a description of these properties.</p><p>To illustrate most of the properties and their meanings, I wrote the following program that displays the property values for several different encodings.</p><p>using System; using System.Text;</p><p>public static class Program { public static void Main() {</p><p>foreach (EncodingInfo ei in Encoding.GetEncodings()) { Encoding e = ei.GetEncoding(); Console.WriteLine(\"{1}{0}\" +</p><p>\"\\tCodePage={2}, WindowsCodePage={3}{0}\" + \"\\tWebName={4}, HeaderName={5}, BodyName={6}{0}\" + \"\\tIsBrowserDisplay={7}, IsBrowserSave={8}{0}\" + \"\\tIsMailNewsDisplay={9}, IsMailNewsSave={10}{0}\",</p><p>}</p><p>} }</p><p>Environment.NewLine, e.EncodingName, e.CodePage, e.WindowsCodePage, e.WebName, e.HeaderName, e.BodyName, e.IsBrowserDisplay, e.IsBrowserSave, e.IsMailNewsDisplay, e.IsMailNewsSave);</p><p>Running this program yields the following output (abridged to conserve paper).</p><p>IBM EBCDIC (US\u00adCanada) CodePage=37, WindowsCodePage=1252 WebName=IBM037, HeaderName=IBM037, BodyName=IBM037 IsBrowserDisplay=False, IsBrowserSave=False IsMailNewsDisplay=False, IsMailNewsSave=False</p><p>OEM United States CodePage=437, WindowsCodePage=1252 WebName=IBM437, HeaderName=IBM437, BodyName=IBM437 IsBrowserDisplay=False, IsBrowserSave=False IsMailNewsDisplay=False, IsMailNewsSave=False</p><p>IBM EBCDIC (International) CodePage=500, WindowsCodePage=1252 WebName=IBM500, HeaderName=IBM500, BodyName=IBM500 IsBrowserDisplay=False, IsBrowserSave=False IsMailNewsDisplay=False, IsMailNewsSave=False</p><p>Arabic (ASMO 708) CodePage=708, WindowsCodePage=1256 WebName=ASMO\u00ad708, HeaderName=ASMO\u00ad708, BodyName=ASMO\u00ad708 IsBrowserDisplay=True, IsBrowserSave=True IsMailNewsDisplay=False, IsMailNewsSave=False</p><p>Unicode</p><p>CodePage=1200, WindowsCodePage=1200 WebName=utf\u00ad16, HeaderName=utf\u00ad16, BodyName=utf\u00ad16 IsBrowserDisplay=False, IsBrowserSave=True IsMailNewsDisplay=False, IsMailNewsSave=False</p><p>Unicode (Big\u00adEndian) CodePage=1201, WindowsCodePage=1200 WebName=unicodeFFFE, HeaderName=unicodeFFFE, BodyName=unicodeFFFE IsBrowserDisplay=False, IsBrowserSave=False IsMailNewsDisplay=False, IsMailNewsSave=False</p><p>Western European (DOS) CodePage=850, WindowsCodePage=1252 WebName=ibm850, HeaderName=ibm850, BodyName=ibm850 IsBrowserDisplay=False, IsBrowserSave=False IsMailNewsDisplay=False, IsMailNewsSave=False</p><p>Unicode (UTF\u00ad8) CodePage=65001, WindowsCodePage=1200 WebName=utf\u00ad8, HeaderName=utf\u00ad8, BodyName=utf\u00ad8 IsBrowserDisplay=True, IsBrowserSave=True IsMailNewsDisplay=True, IsMailNewsSave=True</p><p>Table 14-3 covers the most commonly used methods offered by all Encoding-derived classes.</p><figcaption>TABLE 14-3 Methods of the Encoding-Derived Classes</figcaption><table border=\"1\">\n<tr>\n<td><p>Method</p>\n<p>GetPreamble</p>\n</td>\n<td><h6>Description</h6>\n<p>Returns an array of bytes indicating what should be written to a stream before writing any encoded bytes. Frequently, these bytes are referred to as BOM bytes. When you start reading from a stream, the BOM bytes automatically help detect the encoding that was used when the stream was written so that the correct decoder can be used. For some Encoding-derived classes, this method returns an array of 0 bytes\u2014that is, no preamble bytes. A UTF8Encoding object can be explicitly constructed so that this method returns a 3-byte array of 0xEF, 0xBB, 0xBF. A UnicodeEncoding object can be explicitly constructed so that this method returns a 2-byte array of 0xFE, 0xFF for bigendian encoding or a 2-byte array of 0xFF, 0xFE for little-endian encoding. The default is little-endian.</p>\n</td>\n</tr>\n<tr>\n<td><p>Convert</p>\n</td>\n<td><p>Converts an array of bytes specified in a source encoding to an array of bytes specified by a destination encoding. Internally, this static method calls the source encoding object\u2019s GetChars method and passes the result to the destination encoding object\u2019s GetBytes method. The resulting byte array is returned to the caller.</p>\n</td>\n</tr>\n<tr>\n<td><p>Equals</p>\n</td>\n<td><p>Returns true if two Encoding-derived objects represent the same code page and preamble setting.</p>\n</td>\n</tr>\n<tr>\n<td><p>GetHashCode</p>\n</td>\n<td><p>Returns the encoding object\u2019s code page.</p>\n</td>\n</tr>\n</table><h6>Encoding and Decoding Streams of Characters and Bytes</h6><p>Imagine that you\u2019re reading a UTF-16 encoded string via a System.Net.Sockets.NetworkStream object. The bytes will very likely stream in as chunks of data. In other words, you might first read 5 bytes from the stream, followed by 7 bytes. In UTF-16, each character consists of 2 bytes. So calling</p><p>Encoding\u2019s GetString method passing the first array of 5 bytes will return a string consisting of just two characters. If you later call GetString, passing in the next 7 bytes that come in from the stream, GetString will return a string consisting of three characters, and all of the code points will have the</p><p>wrong values!</p><p>This data corruption problem occurs because none of the Encoding-derived classes maintains any state in between calls to their methods. If you\u2019ll be encoding or decoding characters/bytes in chunks, you must do some additional work to maintain state between calls, preventing any loss of data.</p><p>To decode chunks of bytes, you should obtain a reference to an Encoding-derived object (as described in the previous section) and call its GetDecoder method. This method returns a reference to a newly constructed object whose type is derived from the System.Text.Decoder class. Like the Encoding class, the Decoder class is an abstract base class. If you look in the .NET Framework SDK documentation, you won\u2019t find any classes that represent concrete implementations of the Decoder class. However, the FCL does define a bunch of Decoder-derived classes. These classes are all internal to the FCL, but the GetDecoder method can construct instances of these classes and return them to your application code.</p><p>All Decoder-derived classes offer two important methods: GetChars and GetCharCount. Obviously, these methods are used for decoding an array of bytes and work similarly to Encoding\u2019s GetChars and GetCharCount methods, discussed earlier. When you call one of these methods, it decodes the byte array as much as possible. If the byte array doesn\u2019t contain enough bytes to complete a character, the leftover bytes are saved inside the decoder object. The next time you call one of these methods, the decoder object uses the leftover bytes plus the new byte array passed to it\u2014this ensures that the chunks of data are decoded properly. Decoder objects are very useful when reading bytes from a stream.</p><p>An Encoding-derived type can be used for stateless encoding and decoding. However, a Decoderderived type can be used only for decoding. If you want to encode strings in chunks, call GetEncoder instead of calling the Encoding object\u2019s GetDecoder method. GetEncoder returns a newly constructed object whose type is derived from the abstract base class System.Text.Encoder. Again, the .NET Framework SDK documentation doesn\u2019t contain any classes representing concrete implementations of the Encoder class. However, the FCL does define some Encoder-derived classes. As with the Decoderderived classes, these classes are all internal to the FCL, but the GetEncoder method can construct instances of these classes and return them to your application code.</p><p>All Encoder-derived classes offer two important methods: GetBytes and GetByteCount. On each call, the Encoder-derived object maintains any leftover state information so that you can encode data in chunks.</p><h6>Base-64 String Encoding and Decoding</h6><p>As of this writing, the UTF-16 and UTF-8 encodings are quite popular. It is also quite popular to encode a sequence of bytes to a base-64 string. The FCL does offer methods to do base-64 encoding and decoding, and you might expect that this would be accomplished via an Encoding-derived type. However, for some reason, base-64 encoding and decoding is done using some static methods offered by the System.Convert type.</p><p>To encode a base-64 string as an array of bytes, you call Convert\u2019s static FromBase64String or FromBase64CharArray method. Likewise, to decode an array of bytes as a base-64 string, you call</p><p>Convert\u2019s static ToBase64String or ToBase64CharArray method. The following code demonstrates how to use some of these methods.</p><p>using System;</p><p>public static class Program {</p><p>public static void Main() { // Get a set of 10 randomly generated bytes Byte[] bytes = new Byte[10]; new Random().NextBytes(bytes);</p><p>// Display the bytes Console.WriteLine(BitConverter.ToString(bytes));</p><p>// Decode the bytes into a base\u00ad64 string and show the string String s = Convert.ToBase64String(bytes); Console.WriteLine(s);</p><p>} }</p><p>// Encode the base\u00ad64 string back to bytes and show the bytes bytes = Convert.FromBase64String(s); Console.WriteLine(BitConverter.ToString(bytes));</p><p>Compiling this code and running the executable file produces the following output (your output might vary from mine because of the randomly generated bytes).</p><p>3B\u00adB9\u00ad27\u00ad40\u00ad59\u00ad35\u00ad86\u00ad54\u00ad5F\u00adF1 O7knQFk1hlRf8Q== 3B\u00adB9\u00ad27\u00ad40\u00ad59\u00ad35\u00ad86\u00ad54\u00ad5F\u00adF1</p><h6>Secure Strings</h6><p>Often, String objects are used to contain sensitive data such as a user\u2019s password or credit-card information. Unfortunately, String objects contain an array of characters in memory, and if some unsafe or unmanaged code is allowed to execute, the unsafe/unmanaged code could snoop around the process\u2019s address space, locate the string containing the sensitive information, and use this data in an unauthorized way. Even if the String object is used for just a short time and then garbage collected, the CLR might not immediately reuse the String object\u2019s memory (especially if the String object was in an older generation), leaving the String\u2019s characters in the process\u2019s memory, where the information could be compromised. In addition, because strings are immutable, as you manipulate them, the old copies linger in memory and you end up with different versions of the string scattered all over memory.</p><p>Some governmental departments have stringent security requirements that require very specific security guarantees. To meet these requirements, Microsoft added a more secure string class to the FCL: System.Security.SecureString. When you construct a SecureString object, it internally allocates a block of unmanaged memory that contains an array of characters. Unmanaged memory is used so that the garbage collector isn\u2019t aware of it.</p><p>These string\u2019s characters are encrypted, protecting the sensitive information from any malicious unsafe/unmanaged code. You can append, insert, remove, or set a character in the secure string by using any of these methods: AppendChar, InsertAt, RemoveAt, and SetAt. Whenever you call any of these methods, internally, the method decrypts the characters, performs the operation in place, and then re-encrypts the characters. This means that the characters are in an unencrypted state for a very short period of time. This also means that the performance of each operation is less than stellar, so you should perform as few of these operations as possible.</p><p>The SecureString class implements the IDisposable interface to provide an easy way to deterministically destroy the string\u2019s secured contents. When your application no longer needs the sensitive string information, you simply call SecureString\u2019s Dispose method or use a SecureString instance in a using construct. Internally, Dispose zeroes out the contents of the memory buffer to make sure that the sensitive information is not accessible to malicious code, and then the buffer is freed. Internally, a SecureString object has a field to a SafeBuffer-derived object, which maintains the actual string. Because the SafeBuffer class is ultimately derived from CriticalFinalizerObject, discussed in Chapter 21, \u201cThe Managed Heap and Garbage Collection,\u201d the</p><p>string\u2019s characters are guaranteed to be zeroed out and have its buffer freed when it is finalized. Unlike a String object, when a SecureString object is finalized, the encrypted string\u2019s characters will no longer be in memory.</p><p>Now that you know how to create and modify a SecureString object, let\u2019s talk about how to use one. Unfortunately, the most recent FCL has limited support for the SecureString class. In other words, there are only a few methods that accept a SecureString argument. In the .NET Framework 4, you can pass a SecureString as a password when</p><p>\u25a0\u25a0 Working with a cryptographic service provider (CSP). See the System.Security.Cryptog\u00ad</p><p>raphy.CspParameters class.</p><p>\u25a0\u25a0 Creating, importing, or exporting an X.509 certificate. See the System.Security.Cryptography.X509Certificates.X509Certificate and System.Security.Cryptography. X509Certificates.X509Certificate2 classes.</p><p>\u25a0\u25a0 Starting a new process under a specific user account. See the System.Diagnostics.</p><p>Process and System.Diagnostics.ProcessStartInfo classes.</p><p>\u25a0\u25a0 Constructing an event log session. See the System.Diagnostics.Eventing.Reader.</p><p>EventLogSession class.</p><p>\u25a0\u25a0 Using the System.Windows.Controls.PasswordBox control. See this class\u2019s SecurePass\u00ad</p><p>word property.</p><p>Finally, you can create your own methods that can accept a SecureString object parameter. Inside your method, you must have the SecureString object create an unmanaged memory buffer that contains the decrypted characters before your method uses the buffer. To keep the window of opportunity for malicious code to access the sensitive data as small as possible, your code should require access to the decrypted string for as short a period of time as possible. When finished using the string, your code should zero the buffer and free it as soon as possible. Also, never put the contents</p><p>of a SecureString into a String: if you do, the String lives unencrypted in the heap and will not have its characters zeroed out until the memory is reused after a garbage collection. The SecureString class does not override the ToString method specifically to avoid exposing the sensitive data (which converting it to a String would do).</p><p>Here is some sample code demonstrating how to initialize and use a SecureString (when compiling this, you\u2019ll need to specify the /unsafe switch to the C# compiler).</p><p>using System; using System.Security; using System.Runtime.InteropServices;</p><p>public static class Program { public static void Main() {</p><p>using (SecureString ss = new SecureString()) { Console.Write(\"Please enter password: \"); while (true) {</p><p>ConsoleKeyInfo cki = Console.ReadKey(true); if (cki.Key == ConsoleKey.Enter) break;</p><p>// Append password characters into the SecureString ss.AppendChar(cki.KeyChar); Console.Write(\"*\");</p><p>} Console.WriteLine();</p><p>}</p><p>// Password entered, display it for demonstration purposes DisplaySecureString(ss);</p><p>} // After 'using', the SecureString is Disposed; no sensitive data in memory</p><p>// This method is unsafe because it accesses unmanaged memory private unsafe static void DisplaySecureString(SecureString ss) {</p><p>Char* pc = null; try {</p><p>// Decrypt the SecureString into an unmanaged memory buffer pc = (Char*) Marshal.SecureStringToCoTaskMemUnicode(ss);</p><p>} }</p><p>// Access the unmanaged memory buffer that // contains the decrypted SecureString for (Int32 index = 0; pc[index] != 0; index++)</p><p>Console.Write(pc[index]);</p><p>} finally {</p><p>// Make sure we zero and free the unmanaged memory buffer that contains // the decrypted SecureString characters if (pc != null)</p><p>Marshal.ZeroFreeCoTaskMemUnicode((IntPtr) pc); }</p><p>The System.Runtime.InteropServices.Marshal class offers five methods that you can call to</p><p>decrypt a SecureString\u2019s characters into an unmanaged memory buffer. All of these methods are static, all accept a SecureString argument, and all return an IntPtr. Each of these methods has a corresponding method that you must call in order to zero the internal buffer and free it. Table 14-4 shows the System.Runtime.InteropServices.Marshal class\u2019s methods to decrypt a Secure\u00ad</p><p>String into a memory buffer and the corresponding method to zero and free the buffer.</p><figcaption>TABLE 14-4 Methods of the Marshal Class for Working with Secure Strings</figcaption><table border=\"1\">\n<tr>\n<td><p>Method to Decrypt SecureString to Buffer SecureStringToBSTR</p>\n</td>\n<td><p>Method to Zero and Free Buffer</p>\n<p>ZeroFreeBSTR</p>\n</td>\n</tr>\n<tr>\n<td><p>SecureStringToCoTaskMemAnsi</p>\n</td>\n<td><p>ZeroFreeCoTaskMemAnsi</p>\n</td>\n</tr>\n<tr>\n<td><p>SecureStringToCoTaskMemUnicode</p>\n</td>\n<td><p>ZeroFreeCoTaskMemUnicode</p>\n</td>\n</tr>\n<tr>\n<td><p>SecureStringToGlobalAllocAnsi</p>\n</td>\n<td><p>ZeroFreeGlobalAllocAnsi</p>\n</td>\n</tr>\n<tr>\n<td><p>SecureStringToGlobalAllocUnicode</p>\n</td>\n<td><p>ZeroFreeGlobalAllocUnicode</p>\n</td>\n</tr>\n</table><h6>C H A P T E R 1 5</h6><h2>Enumerated Types and Bit Flags</h2><h6>In this chapter:</h6><p>Enumerated Types . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 361 Bit Flags. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 367 Adding Methods to Enumerated Types. . . . . . . . . . . . . . . . . . . . . 371</p><p>In this chapter, I\u2019ll discuss enumerated types and bit flags. Because Windows and many programming languages have used these constructs for so many years, I\u2019m sure that many of you are already familiar with how to use enumerated types and bit flags. However, the common language runtime (CLR) and the Framework Class Library (FCL) work together to make enumerated types and bit flags real object-oriented types that offer cool features that I suspect most developers aren\u2019t familiar with. It\u2019s amazing to me how these features, which are the focus of this chapter, make developing application code so much easier.</p><h6>Enumerated Types</h6><p>An enumerated type is a type that defines a set of symbolic name and value pairs. For example, the Color type shown here defines a set of symbols, with each symbol identifying a single color.</p><p>internal enum Color {</p><p>White, // Assigned a value of 0 Red, // Assigned a value of 1 Green, // Assigned a value of 2 Blue, // Assigned a value of 3 Orange // Assigned a value of 4</p><p>}</p><p>Of course, programmers can always write a program using 0 to represent white, 1 to represent red, and so on. However, programmers shouldn\u2019t hard-code numbers into their code and should use an enumerated type instead, for at least two reasons:</p><p>\u25a0\u25a0 Enumerated types make the program much easier to write, read, and maintain. With enumerated types, the symbolic name is used throughout the code, and the programmer doesn\u2019t have to mentally map the meaning of each hard-coded value (for example, white is 0 or vice versa). Also, should a symbol\u2019s numeric value change, the code can simply be recompiled without requiring any changes to the source code. In addition, documentation tools and other utilities, such as a debugger, can show meaningful symbolic names to the programmer.</p><p>361</p><p>\u25a0\u25a0 Enumerated types are strongly typed. For example, the compiler will report an error if I attempt to pass Color.Orange as a value to a method requiring a Fruit enumerated type as a parameter.</p><p>In the Microsoft .NET Framework, enumerated types are more than just symbols that the compiler cares about. Enumerated types are treated as first-class citizens in the type system, which allows for very powerful operations that simply can\u2019t be done with enumerated types in other environments (such as in unmanaged C++, for example).</p><p>Every enumerated type is derived directly from System.Enum, which is derived from System. ValueType, which in turn is derived from System.Object. So enumerated types are value types (described in Chapter 5, \u201cPrimitive, Reference, and Value Types\u201d) and can be represented in unboxed and boxed forms. However, unlike other value types, an enumerated type can\u2019t define any methods, properties, or events. However, you can use C#\u2019s extension methods feature to simulate adding methods to an enumerated type. See the \u201cAdding Methods to Enumerated Types\u201d section at the end of this chapter for an example of this.</p><p>When an enumerated type is compiled, the C# compiler turns each symbol into a constant field of the type. For example, the compiler treats the Color enumeration shown earlier as if you had written code similar to the following.</p><p>internal struct Color : System.Enum { // Following are public constants defining Color's symbols and values public const Color White = (Color) 0; public const Color Red = (Color) 1; public const Color Green = (Color) 2; public const Color Blue = (Color) 3; public const Color Orange = (Color) 4;</p><p>}</p><p>// Following is a public instance field containing a Color variable's value // You cannot write code that references this instance field directly public Int32 value__;</p><p>The C# compiler won\u2019t actually compile this code because it forbids you from defining a type derived from the special System.Enum type. However, this pseudo-type definition shows you what\u2019s happening internally. Basically, an enumerated type is just a structure with a bunch of constant fields defined in it and one instance field. The constant fields are emitted to the assembly\u2019s metadata and can be accessed via reflection. This means that you can get all of the symbols and their values associated with an enumerated type at run time. It also means that you can convert a string symbol into its equivalent numeric value. These operations are made available to you by the System.Enum base type, which offers several static and instance methods that can be performed on an instance of an enumerated type, saving you the trouble of having to use reflection. I\u2019ll discuss some of these operations next.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile115.png\"/><p>Important Symbols defined by an enumerated type are constant values. So when a compiler sees code that references an enumerated type\u2019s symbol, the compiler substitutes the symbol\u2019s numeric value at compile time, and this code no longer references the enumerated type that defined the symbol. This means that the assembly that defines the enumerated type may not be required at run time; it was required only when compiling. If you have code that references the enumerated type\u2014rather than just having references to symbols defined by the type\u2014the assembly containing the enumerated type\u2019s definition will be required at run time. Some versioning issues arise because enumerated type symbols are constants instead of read-only values. I explained these issues in the \u201cConstants\u201d section of Chapter 7, \u201cConstants and Fields.\u201d</p><p>For example, the System.Enum type has a static method called GetUnderlyingType, and the System.Type type has an instance method called GetEnumUnderlyingType.</p><p>public static Type GetUnderlyingType(Type enumType); // Defined in System.Enum public Type GetEnumUnderlyingType(); // Defined in System.Type</p><p>These methods return the core type used to hold an enumerated type\u2019s value. Every enumerated type has an underlying type, which can be a byte, sbyte, short, ushort, int (the most common type and what C# chooses by default), uint, long, or ulong. Of course, these C# primitive types correspond to FCL types. However, to make the implementation of the compiler itself simpler, the C# compiler requires you to specify a primitive type name here; using an FCL type name (such as Int32) generates the following message: error CS1008: Type byte, sbyte, short, ushort, int, uint, long, or ulong expected. The following code shows how to declare an enumerated type with an underlying type of byte (System.Byte).</p><p>internal enum Color : byte { White, Red, Green, Blue, Orange</p><p>}</p><p>With the Color enumerated type defined in this way, the following code shows what GetUnderlyingType will return.</p><p>// The following line displays \"System.Byte\". Console.WriteLine(Enum.GetUnderlyingType(typeof(Color)));</p><p>The C# compiler treats enumerated types as primitive types. As such, you can use many of the familiar operators (==, !=, &lt;, &gt;, &lt;=, &gt;=, +, \u00ad, ^, &amp;, |, ~, ++, and \u00ad\u00ad) to manipulate enumerated type instances. All of these operators actually work on the value__ instance field inside each enumerated</p><p>type instance. Furthermore, the C# compiler allows you to explicitly cast instances of an enumerated type to a different enumerated type. You can also explicitly cast an enumerated type instance to a numeric type.</p><p>Given an instance of an enumerated type, it\u2019s possible to map that value to one of several string representations by calling the ToString method inherited from System.Enum.</p><p>Color c = Color.Blue; Console.WriteLine(c); // \"Blue\" (General format) Console.WriteLine(c.ToString()); // \"Blue\" (General format) Console.WriteLine(c.ToString(\"G\")); // \"Blue\" (General format) Console.WriteLine(c.ToString(\"D\")); // \"3\" (Decimal format) Console.WriteLine(c.ToString(\"X\")); // \"03\" (Hex format)</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile116.png\"/><p>Note When using hex formatting, ToString always outputs uppercase letters. In addition, the number of digits output depends on the enum\u2019s underlying type: 2 digits for byte/ sbyte, 4 digits for short/ushort, 8 digits for int/uint, and 16 digits for long/ulong. Leading zeros are output if necessary.</p><p>In addition to the ToString method, the System.Enum type offers a static Format method that you can call to format an enumerated type\u2019s value.</p><p>public static String Format(Type enumType, Object value, String format);</p><p>Generally, I prefer to call the ToString method because it requires less code and it\u2019s easier to call. But using Format has one advantage over ToString: Format lets you pass a numeric value for the value parameter; you don\u2019t have to have an instance of the enumerated type. For example, the following code will display \u201cBlue\u201d.</p><p>// The following line displays \"Blue\". Console.WriteLine(Enum.Format(typeof(Color), (Byte)3, \"G\"));</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile117.png\"/><p>Note It\u2019s possible to declare an enumerated type that has multiple symbols, all with the same numeric value. When converting a numeric value to a symbol by using general formatting, Enum\u2019s methods return one of the symbols. However, there\u2019s no guarantee of which symbol name is returned. Also, if no symbol is defined for the numeric value you\u2019re looking up, a string containing the numeric value is returned.</p><p>It\u2019s also possible to call System.Enum\u2019s static GetValues method or System.Type\u2019s instance GetEnumValues method to obtain an array that contains one element for each symbolic name in an enumerated type; each element contains the symbolic name\u2019s numeric value.</p><p>public static Array GetValues(Type enumType); // Defined in System.Enum public Array GetEnumValues(); // Defined in System.Type</p><p>Using this method along with the ToString method, you can display all of an enumerated type\u2019s symbolic and numeric values, like the following.</p><p>Color[] colors = (Color[]) Enum.GetValues(typeof(Color)); Console.WriteLine(\"Number of symbols defined: \" + colors.Length); Console.WriteLine(\"Value\\tSymbol\\n\u00ad\u00ad\u00ad\u00ad\u00ad\\t\u00ad\u00ad\u00ad\u00ad\u00ad\u00ad\"); foreach (Color c in colors) {</p><p>// Display each symbol in Decimal and General format. Console.WriteLine(\"{0,5:D}\\t{0:G}\", c);</p><p>}</p><p>The previous code produces the following output.</p><p>Number of symbols defined: 5 Value Symbol \u00ad\u00ad\u00ad\u00ad\u00ad \u00ad\u00ad\u00ad\u00ad\u00ad\u00ad</p><ul style=\"list-style-type: none;\">\n<li><p>0 White</p></li>\n<li><p>1 Red</p></li>\n<li><p>2 Green</p></li>\n<li><p>3 Blue</p></li>\n<li><p>4 Orange</p></li>\n</ul><p>Personally, I don\u2019t like the GetValues and GetEnumValues methods because they both return an Array, which I then have to cast to the appropriate array type. So, I always define my own method.</p><p>public static TEnum[] GetEnumValues&lt;TEnum&gt;() where TEnum : struct {</p><p>return (TEnum[])Enum.GetValues(typeof(TEnum)); }</p><p>With my generic GetEnumValues method, I can get better compile-time type-safety and simplify the first line of code in the previous example to the following.</p><p>Color[] colors = GetEnumValues&lt;Color&gt;();</p><p>This discussion shows some of the cool operations that can be performed on enumerated types. I suspect that the ToString method with the general format will be used quite frequently to show symbolic names in a program\u2019s user interface elements (list boxes, combo boxes, and the like), as long as the strings don\u2019t need to be localized (because enumerated types offer no support for localization). In addition to the GetValues method, the System.Enum type and the System.Type type also offer the following methods that return an enumerated type\u2019s symbols.</p><p>// Returns a String representation for the numeric value public static String GetName(Type enumType, Object value); // Defined in System.Enum public String GetEnumName(Object value); // Defined in System.Type</p><p>// Returns an array of Strings: one per symbol defined in the enum public static String[] GetNames(Type enumType); // Defined in System.Enum public String[] GetEnumNames(); // Defined in System.Type</p><p>I\u2019ve discussed a lot of methods that you can use to look up an enumerated type\u2019s symbol. But you also need a method that can look up a symbol\u2019s equivalent value, an operation that could be used to convert a symbol that a user enters into a text box, for example. Converting a symbol to an instance of an enumerated type is easily accomplished by using one of Enum\u2019s static Parse and TryParse methods.</p><p>public static Object Parse(Type enumType, String value); public static Object Parse(Type enumType, String value, Boolean ignoreCase); public static Boolean TryParse&lt;TEnum&gt;(String value, out TEnum result) where TEnum: struct; public static Boolean TryParse&lt;TEnum&gt;(String value, Boolean ignoreCase, out TEnum result)</p><p>where TEnum : struct;</p><p>Here\u2019s some code demonstrating how to use this method.</p><p>// Because Orange is defined as 4, 'c' is initialized to 4. Color c = (Color) Enum.Parse(typeof(Color), \"orange\", true);</p><p>// Because Brown isn't defined, an ArgumentException is thrown. c = (Color) Enum.Parse(typeof(Color), \"Brown\", false);</p><p>// Creates an instance of the Color enum with a value of 1 Enum.TryParse&lt;Color&gt;(\"1\", false, out c);</p><p>// Creates an instance of the Color enum with a value of 23 Enum.TryParse&lt;Color&gt;(\"23\", false, out c);</p><p>Finally, using the following Enum\u2019s static IsDefined method and Type\u2019s IsEnumDefined method:</p><p>public static Boolean IsDefined(Type enumType, Object value); // Defined in System.Enum public Boolean IsEnumDefined(Object value); // Defined in System.Type</p><p>you can determine whether a numeric value is legal for an enumerated type.</p><p>// Displays \"True\" because Color defines Red as 1 Console.WriteLine(Enum.IsDefined(typeof(Color), (Byte)1));</p><p>// Displays \"True\" because Color defines White as 0 Console.WriteLine(Enum.IsDefined(typeof(Color), \"White\"));</p><p>// Displays \"False\" because a case\u00adsensitive check is performed Console.WriteLine(Enum.IsDefined(typeof(Color), \"white\"));</p><p>// Displays \"False\" because Color doesn't have a symbol of value 10 Console.WriteLine(Enum.IsDefined(typeof(Color), (Byte)10));</p><p>The IsDefined method is frequently used for parameter validation. Here\u2019s an example.</p><p>public void SetColor(Color c) { if (!Enum.IsDefined(typeof(Color), c)) { throw(new ArgumentOutOfRangeException(\"c\", c, \"Invalid Color value.\"));</p><p>} // Set color to White, Red, Green, Blue, or Orange</p><p>... }</p><p>The parameter validation is useful because someone could call SetColor like the following.</p><p>SetColor((Color) 547);</p><p>Because no symbol has a corresponding value of 547, the SetColor method will throw an ArgumentOutOfRangeException exception, indicating which parameter is invalid and why.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile118.png\"/><p>Important The IsDefined method is very convenient, but you must use it with caution. First, IsDefined always does a case-sensitive search, and there is no way to get it to perform a case-insensitive search. Second, IsDefined is pretty slow because it uses reflection internally; if you wrote code to manually check each possible value, your application\u2019s performance would most certainly be better. Third, you should really use IsDefined only if the enum type itself is defined in the same assembly that is calling IsDefined. Here\u2019s why: Let\u2019s say the Color enum is defined in one assembly and the SetColor method is defined in another assembly. The SetColor method calls IsDefined, and if the color is White, Red, Green, Blue, or Orange, SetColor performs its work. However, if the Color enum changes in the future to include Purple, SetColor will now allow Purple, which it never expected before, and the method might execute with unpredictable results.</p><p>Finally, the System.Enum type offers a set of static ToObject methods that convert an instance of a Byte, SByte, Int16, UInt16, Int32, UInt32, Int64, or UInt64 to an instance of an enumerated type.</p><p>Enumerated types are always used in conjunction with some other type. Typically, they\u2019re used for the type\u2019s method parameters or return type, properties, and fields. A common question that arises is whether to define the enumerated type nested within the type that requires it or to define the enumerated type at the same level as the type that requires it. If you examine the FCL, you\u2019ll see that an enumerated type is usually defined at the same level as the class that requires it. The reason is simply to make the developer\u2019s life a little easier by reducing the amount of typing required. So you should define your enumerated type at the same level unless you\u2019re concerned about name conflicts.</p><h6>Bit Flags</h6><p>Programmers frequently work with sets of bit flags. When you call the System.IO.File type\u2019s GetAttributes method, it returns an instance of a FileAttributes type. A FileAttributes type is an instance of an Int32-based enumerated type, in which each bit reflects a single attribute of the file. The FileAttributes type is defined in the FCL as follows.</p><p>[Flags, Serializable] public enum FileAttributes {</p><p>ReadOnly = 0x00001, Hidden = 0x00002, System = 0x00004, Directory = 0x00010, Archive = 0x00020,</p><p>}</p><p>Device = 0x00040, Normal = 0x00080, Temporary = 0x00100, SparseFile = 0x00200, ReparsePoint = 0x00400, Compressed = 0x00800, Offline = 0x01000, NotContentIndexed = 0x02000, Encrypted = 0x04000, IntegrityStream = 0x08000, NoScrubData = 0x20000</p><p>To determine whether a file is hidden, you would execute code like the following.</p><p>String file = Assembly.GetEntryAssembly().Location; FileAttributes attributes = File.GetAttributes(file); Console.WriteLine(\"Is {0} hidden? {1}\", file, (attributes &amp; FileAttributes.Hidden) != 0);</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile119.png\"/><p>Note The Enum class defines a HasFlag method defined as follows.</p><p>public Boolean HasFlag(Enum flag);</p><p>Using this method, you could rewrite the call to Console.WriteLine like the following.</p><p>Console.WriteLine(\"Is {0} hidden? {1}\", file, attributes.HasFlag(FileAttributes.Hidden));</p><p>However, I recommend that you avoid the HasFlag method for this reason: Because it takes a parameter of type Enum, any value you pass to it must be boxed, requiring a memory allocation.</p><p>And here\u2019s code demonstrating how to change a file\u2019s attributes to read-only and hidden.</p><p>File.SetAttributes(file, FileAttributes.ReadOnly | FileAttributes.Hidden);</p><p>As the FileAttributes type shows, it\u2019s common to use enumerated types to express the set of bit flags that can be combined. However, although enumerated types and bit flags are similar, they don\u2019t have exactly the same semantics. For example, enumerated types represent single numeric values, and bit flags represent a set of bits, some of which are on, and some of which are off.</p><p>When defining an enumerated type that is to be used to identify bit flags, you should, of course, explicitly assign a numeric value to each symbol. Usually, each symbol will have an individual bit turned on. It is also common to see a symbol called None defined with a value of 0, and you can also define symbols that represent commonly used combinations (see the following ReadWrite symbol). It\u2019s also highly recommended that you apply the System.FlagsAttribute custom attribute type to the enumerated type, as shown here:</p><p>[Flags] // The C# compiler allows either \"Flags\" or \"FlagsAttribute\". internal enum Actions {</p><p>None = 0, Read = 0x0001,</p><p>}</p><p>Write = 0x0002, ReadWrite = Actions.Read | Actions.Write, Delete = 0x0004, Query = 0x0008, Sync = 0x0010</p><p>Because Actions is an enumerated type, you can use all of the methods described in the previous section when working with bit-flag enumerated types. However, it would be nice if some of those functions behaved a little differently. For example, let\u2019s say you had the following code.</p><p>Actions actions = Actions.Read | Actions.Delete; // 0x0005 Console.WriteLine(actions.ToString()); // \"Read, Delete\"</p><p>When ToString is called, it attempts to translate the numeric value into its symbolic equivalent. The numeric value is 0x0005, which has no symbolic equivalent. However, the ToString method detects the existence of the [Flags] attribute on the Actions type, and ToString now treats the numeric value not as a single value but as a set of bit flags. Because the 0x0001 and 0x0004 bits are set, ToString generates the following string: \"Read, Delete\". If you remove the [Flags] attribute from the Actions type, ToString would return \u201c5\u201d.</p><p>I discussed the ToString method in the previous section, and I showed that it offered three ways to format the output: \u201cG\u201d (general), \u201cD\u201d (decimal), and \u201cX\u201d (hex). When you\u2019re formatting an instance of an enumerated type by using the general format, the type is first checked to see if the [Flags] attribute is applied to it. If this attribute is not applied, a symbol matching the numeric value is looked up and returned. If the [Flags] attribute is applied, ToString works like this:</p><ul style=\"list-style-type: none;\">\n<li><p>1. The set of numeric values defined by the enumerated type is obtained, and the numbers are sorted in descending order.</p></li>\n<li><p>2. Each numeric value is bitwise-ANDed with the value in the enum instance, and if the result equals the numeric value, the string associated with the numeric value is appended to the output string, and the bits are considered accounted for and are turned off. This step is repeated until all numeric values have been checked or until the enum instance has all of its bits turned off.</p></li>\n<li><p>3. If, after all the numeric values have been checked, the enum instance is still not 0, the enum instance has some bits turned on that do not correspond to any defined symbols. In this case, ToString returns the original number in the enum instance as a string.</p></li>\n<li><p>4. If the enum instance\u2019s original value wasn\u2019t 0, the string with the comma-separated set of symbols is returned.</p></li>\n<li><p>5. If the enum instance\u2019s original value was 0 and if the enumerated type has a symbol defined with a corresponding value of 0, the symbol is returned.</p></li>\n<li><p>6. If we reach this step, \u201c0\u201d is returned.</p></li>\n</ul><p>If you prefer, you could define the Actions type without the [Flags] attribute and still get the correct string by using the \u201cF\u201d format.</p><p>// [Flags] // Commented out now internal enum Actions {</p><p>None = 0 Read = 0x0001, Write = 0x0002, ReadWrite = Actions.Read | Actions.Write, Delete = 0x0004, Query = 0x0008, Sync = 0x0010</p><p>}</p><p>Actions actions = Actions.Read | Actions.Delete; // 0x0005 Console.WriteLine(actions.ToString(\"F\")); // \"Read, Delete\"</p><p>If the numeric value has a bit that cannot be mapped to a symbol, the returned string will contain just a decimal number indicating the original numeric value; no symbols will appear in the string.</p><p>Note that the symbols you define in your enumerated type don\u2019t have to be pure powers of 2. For example, the Actions type could define a symbol called All with a value of 0x001F. If an instance of the Actions type has a value of 0x001F, formatting the instance will produce a string that contains \u201cAll\u201d. The other symbol strings won\u2019t appear.</p><p>So far, I\u2019ve discussed how to convert numeric values into a string of flags. It\u2019s also possible to convert a string of comma-delimited symbols into a numeric value by calling Enum\u2019s static Parse and TryParse method. Here\u2019s some code demonstrating how to use this method.</p><p>// Because Query is defined as 8, 'a' is initialized to 8. Actions a = (Actions) Enum.Parse(typeof(Actions), \"Query\", true); Console.WriteLine(a.ToString()); // \"Query\"</p><p>// Because Query and Read are defined, 'a' is initialized to 9. Enum.TryParse&lt;Actions&gt;(\"Query, Read\", false, out a); Console.WriteLine(a.ToString()); // \"Read, Query\"</p><p>// Creates an instance of the Actions enum with a value of 28 a = (Actions) Enum.Parse(typeof(Actions), \"28\", false); Console.WriteLine(a.ToString()); // \"Delete, Query, Sync\"</p><p>When Parse and TryParse are called, the following actions are performed internally:</p><ul style=\"list-style-type: none;\">\n<li><p>1. It removes all whitespace characters from the start and end of the string.</p></li>\n<li><p>2. If the first character of the string is a digit, plus sign (+), or minus sign (-), the string is assumed to be a number, and an enum instance is returned whose numeric value is equal to the string converted to its numeric equivalent.</p></li>\n<li><p>3. The passed string is split into a set of tokens (separated by commas), and all white space is trimmed away from each token.</p></li>\n</ul><ul style=\"list-style-type: none;\">\n<li><p>4. Each token string is looked up in the enum type\u2019s defined symbols. If the symbol is not found, Parse throws a System.ArgumentException and TryParse returns false. If the symbol is found, bitwise-OR its numeric value into a running result, and then look up the next token.</p></li>\n<li><p>5. If all tokens have been sought and found, return the running result.</p></li>\n</ul><p>You should never use the IsDefined method with bit flag\u2013enumerated types. It won\u2019t work for two reasons:</p><p>\u25a0\u25a0 If you pass a string to IsDefined, it doesn\u2019t split the string into separate tokens to look up; it will attempt to look up the string as through it were one big symbol with commas in it. Because you can\u2019t define an enum with a symbol that has commas in it, the symbol will never be found.</p><p>\u25a0\u25a0 If you pass a numeric value to IsDefined, it checks whether the enumerated type defines a single symbol whose numeric value matches the passed-in number. Because this is unlikely for bit flags, IsDefined will usually return false.</p><h6>Adding Methods to Enumerated Types</h6><p>Earlier in this chapter, I mentioned that you cannot define a method as part of an enumerated type. And, for many years, this has saddened me because there are many occasions when I would love to have been able to supply some methods to my enumerated type. Fortunately, I can use C#\u2019s extension method feature (discussed in Chapter 8, \u201cMethods\u201d) to simulate adding methods to an enumerated type.</p><p>If I want to add some methods to the FileAttributes enumerated type, I can define a static class with extension methods as follows.</p><p>internal static class FileAttributesExtensionMethods { public static Boolean IsSet(this FileAttributes flags, FileAttributes flagToTest) { if (flagToTest == 0)</p><p>throw new ArgumentOutOfRangeException(\"flagToTest\", \"Value must not be 0\"); return (flags &amp; flagToTest) == flagToTest;</p><p>}</p><p>public static Boolean IsClear(this FileAttributes flags, FileAttributes flagToTest) { if (flagToTest == 0)</p><p>throw new ArgumentOutOfRangeException(\"flagToTest\", \"Value must not be 0\"); return !IsSet(flags, flagToTest);</p><p>}</p><p>public static Boolean AnyFlagsSet(this FileAttributes flags, FileAttributes testFlags) {</p><p>return ((flags &amp; testFlags) != 0); }</p><p>public static FileAttributes Set(this FileAttributes flags, FileAttributes setFlags) {</p><p>return flags | setFlags; }</p><p>public static FileAttributes Clear(this FileAttributes flags, FileAttributes clearFlags) { return flags &amp; ~clearFlags;</p><p>}</p><p>}</p><p>public static void ForEach(this FileAttributes flags, Action&lt;FileAttributes&gt; processFlag) { if (processFlag == null) throw new ArgumentNullException(\"processFlag\"); for (UInt32 bit = 1; bit != 0; bit &lt;&lt;= 1) {</p><p>UInt32 temp = ((UInt32)flags) &amp; bit; if (temp != 0) processFlag((FileAttributes)temp);</p><p>} }</p><p>And here is some code that demonstrates calling some of these methods. As you can see, the code looks as if I\u2019m calling methods on the enumerated type.</p><p>FileAttributes fa = FileAttributes.System; fa = fa.Set(FileAttributes.ReadOnly); fa = fa.Clear(FileAttributes.System); fa.ForEach(f =&gt; Console.WriteLine(f));</p><h6>C H A P T E R 1 6</h6><h2>Arrays</h2><h6>In this chapter:</h6><p>Initializing Array Elements . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 376 Casting Arrays . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 378 All Arrays Are Implicitly Derived from System.Array . . . . . . . 380 All Arrays Implicitly Implement IEnumerable,</p><p>ICollection, and IList. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 381 Passing and Returning Arrays . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 382 Creating Non-Zero Lower Bound Arrays. . . . . . . . . . . . . . . . . . . . 383 Array Internals. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 384 Unsafe Array Access and Fixed-Size Array . . . . . . . . . . . . . . . . . . 388</p><p>Arrays are mechanisms that allow you to treat several items as a single collection. The Microsoft</p><p>.NET common language runtime (CLR) supports single-dimensional arrays, multi-dimensional arrays, and jagged arrays (that is, arrays of arrays). All array types are implicitly derived from the System.Array abstract class, which itself is derived from System.Object. This means that arrays are always reference types that are allocated on the managed heap and that your application\u2019s variable or field contains a reference to the array and not the elements of the array itself. The following code makes this clearer.</p><p>Int32[] myIntegers; // Declares a reference to an array myIntegers = new Int32[100]; // Creates an array of 100 Int32s</p><p>On the first line, myIntegers is a variable that\u2019s capable of pointing to a single-dimensional array of Int32s. Initially, myIntegers will be set to null because I haven\u2019t allocated an array. The second line of code allocates an array of 100 Int32 values; all of the Int32s are initialized to 0. Because arrays are reference types, the memory block required to hold the 100 unboxed Int32s is allocated on the managed heap. Actually, in addition to the array\u2019s elements, the memory block occupied by an array object also contains a type object pointer, a sync block index, and some additional overhead members as well. The address of this array\u2019s memory block is returned and saved in the variable myIntegers.</p><p>You can also create arrays of reference types.</p><p>Control[] myControls; // Declares a reference to an array myControls = new Control[50]; // Creates an array of 50 Control references</p><p>373</p><p>On the first line, myControls is a variable capable of pointing to a single-dimensional array of Control references. Initially, myControls will be set to null because I haven\u2019t allocated an array. The second line allocates an array of 50 Control references; all of these references are initialized to null. Because Control is a reference type, creating the array creates only a bunch of references; the actual objects aren\u2019t created at this time. The address of this memory block is returned and saved in the variable myControls.</p><p>Figure 16-1 shows how arrays of value types and arrays of reference types look in the managed heap.</p><p>99 98 97 96 \u2022 \u2022 \u2022 \u2022 3 2 1 0</p><p>Int32</p><p>Int32 Int32 Int32</p><p>Button ComboBox</p><p>Control Control Control Control \u2022 \u2022 Control Control Control Control Overhead</p><p>49 48 47 46 \u2022 \u2022 3 2 1 0</p><p>\u2022 \u2022 \u2022 \u2022</p><p>DataGrid</p><p>Int32 Int32 Int32 Int32</p><p>TextBox Button</p><p>Overhead</p><p>myIntegers myControls</p><p>FIGURE 16-1 Arrays of value and reference types in the managed heap.</p><p>In the figure, the Controls array shows the result after the following lines have executed.</p><ul style=\"list-style-type: none;\">\n<li><p>myControls[1] = new Button();</p></li>\n<li><p>myControls[2] = new TextBox();</p></li>\n<li><p>myControls[3] = myControls[2]; // Two elements refer to the same object. myControls[46] = new DataGrid();</p></li>\n</ul><ul style=\"list-style-type: none;\">\n<li><p>myControls[48] = new ComboBox();</p></li>\n<li><p>myControls[49] = new Button();</p></li>\n</ul><p>Common Language Specification (CLS) compliance requires all arrays to be zero-based. This allows a method written in C# to create an array and pass the array\u2019s reference to code written in another language, such as Microsoft Visual Basic .NET. In addition, because zero-based arrays are, by far, the most common arrays, Microsoft has spent a lot of time optimizing their performance. However, the CLR does support non-zero\u2013based arrays even though their use is discouraged. For those of you who don\u2019t care about a slight performance penalty or cross-language portability, I\u2019ll demonstrate how to create and use non-zero\u2013based arrays later in this chapter.</p><p>Notice in Figure 16-1 that each array has some additional overhead information associated with it. This information contains the rank of the array (number of dimensions), the lower bounds for each dimension of the array (almost always 0), and the length of each dimension. The overhead also contains</p><p>the array\u2019s element type. I\u2019ll mention the methods that allow you to query this overhead information later in this chapter.</p><p>So far, I\u2019ve shown examples demonstrating how to create single-dimensional arrays. When possible, you should stick with single-dimensional, zero-based arrays, sometimes referred to as SZ arrays, or vectors. Vectors give the best performance because you can use specific Intermediate Language (IL) instructions\u2014such as newarr, ldelem, ldelema, ldlen, and stelem\u2014to manipulate them. However, if you prefer to work with multi-dimensional arrays, you can. Here are some examples of multi-dimensional arrays.</p><p>// Create a two\u00addimensional array of Doubles. Double[,] myDoubles = new Double[10, 20];</p><p>// Create a three\u00addimensional array of String references. String[,,] myStrings = new String[5, 3, 10];</p><p>The CLR also supports jagged arrays, which are arrays of arrays. Zero-based, single-dimensional jagged arrays have the same performance as normal vectors. However, accessing the elements of a jagged array means that two or more array accesses must occur. Here are some examples of how to create an array of polygons with each polygon consisting of an array of Point instances.</p><p>// Create a single\u00addimensional array of Point arrays. Point[][] myPolygons = new Point[3][];</p><ul style=\"list-style-type: none;\">\n<li><p>// myPolygons[0] refers to an array of 10 Point instances.</p><ul style=\"list-style-type: none;\">\n<li><p>myPolygons[0] = new Point[10];</p><p>// myPolygons[1] refers to an array of 20 Point instances.</p>\n</li>\n<li><p>myPolygons[1] = new Point[20];</p><p>// myPolygons[2] refers to an array of 30 Point instances.</p>\n</li>\n<li><p>myPolygons[2] = new Point[30];</p></li>\n</ul>\n</li>\n</ul><p>// Display the Points in the first polygon. for (Int32 x = 0; x &lt; myPolygons[0].Length; x++)</p><p>Console.WriteLine(myPolygons[0][x]);</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile120.png\"/><p>Note The CLR verifies that an index into an array is valid. In other words, you can\u2019t create an array with 100 elements in it (numbered 0 through 99) and then try to access the element at index \u20135 or 100. Doing so will cause a System.IndexOutOfRangeException to be thrown. Allowing access to memory outside the range of an array would be a breach of type safety and a potential security hole, and the CLR doesn\u2019t allow verifiable code to do this. Usually, the performance degradation associated with index checking is insubstantial because the just-in-time (JIT) compiler normally checks array bounds once before a loop executes instead of at each loop iteration. However, if you\u2019re still concerned about the performance hit of the CLR\u2019s index checks, you can use unsafe code in C# to access the array. The \u201cArray Internals\u201d section later in this chapter demonstrates how to do this.</p><h6>Initializing Array Elements</h6><p>In the previous section, I showed how to create an array object and then I showed how to initialize the elements of the array. C# offers syntax that allows you to do these two operations in one statement. The following shows an example.</p><p>String[] names = new String[] { \"Aidan\", \"Grant\" };</p><p>The comma-separated set of tokens contained within the braces is called an array initializer. Each token can be an arbitrarily complex expression or, in the case of a multi-dimensional array, a nested array initializer. In the preceding example, I used just two simple String expressions.</p><p>If you are declaring a local variable in a method to refer to the initialized array, then you can use C#\u2019s implicitly typed local variable (var) feature to simplify the code a little.</p><p>// Using C#\u2019s implicitly typed local variable feature: var names = new String[] { \"Aidan\", \"Grant\" };</p><p>Here, the compiler is inferring that the names local variable should be of the String[] type because that is the type of the expression on the right of the assignment operator (=).</p><p>You can use C#\u2019s implicitly typed array feature to have the compiler infer the type of the array\u2019s elements. Notice the following line has no type specified between new and [].</p><p>// Using C#\u2019s implicitly typed local variable and implicitly typed array features: var names = new[] { \"Aidan\", \"Grant\", null };</p><p>In the preceding line, the compiler examines the types of the expressions being used inside the array to initialize the array\u2019s elements, and the compiler chooses the closest base class that all the elements have in common to determine the type of the array. In this example, the compiler sees two Strings and null. Because null is implicitly castable to any reference type (including String), the compiler infers that it should be creating and initializing an array of String references.</p><p>If you had this code:</p><p>// Using C#\u2019s implicitly typed local variable &amp; implicitly typed array features: (error) var names = new[] { \"Aidan\", \"Grant\", 123 };</p><p>the compiler would issue the message error CS0826: No best type found for implicitlytyped array. This is because the base type in common between the two Strings and the Int32 is Object, which would mean that the compiler would have to create an array of Object references and then box the 123 and have the last array element refer to a boxed Int32 with a value of 123. The C# compiler team thinks that boxing array elements is too heavy-handed for the compiler to do for you implicitly, and that is why the compiler issues the error.</p><p>As an added syntactical bonus when initializing an array, you can write the following.</p><p>String[] names = { \"Aidan\", \"Grant\" };</p><p>Notice that on the right of the assignment operator (=), only the array initializer expression is given with no new, no type, and no []s. This syntax is nice, but unfortunately, the C# compiler does not allow you to use implicitly typed local variables with this syntax.</p><p>// This is a local variable now (error) var names = { \"Aidan\", \"Grant\" };</p><p>If you try to compile the preceding line of code, the compiler issues two messages: error CS0820: Cannot initialize an implicitly\u00adtyped local variable with an array initializer and error CS0622: Can only use array initializer expressions to assign to array types. Try using a new expression instead. Although the compiler could make this work, the C# team thought that the compiler would be doing too much for you here. It would be inferring the type of the array, new\u2019ing the array, initializing the array, and inferring the type of the local variable, too.</p><p>The last thing I\u2019d like to show you is how to use implicitly typed arrays with anonymous types and implicitly typed local variables. Anonymous types and how type identity applies to them are discussed in Chapter 10, \u201cProperties.\u201d Examine the following code.</p><p>// Using C#\u2019s implicitly typed local, implicitly typed array, and anonymous type features: var kids = new[] {new { Name=\"Aidan\" }, new { Name=\"Grant\" }};</p><p>// Sample usage (with another implicitly typed local variable): foreach (var kid in kids)</p><p>Console.WriteLine(kid.Name);</p><p>In this example, I am using an array initializer that has two expressions for the array elements. Each expression represents an anonymous type (because no type name is specified after the new operator). Because the two anonymous types have the identical structure (one field called Name of type String), the compiler knows that these two objects are of the exact same type. Now, I use C#\u2019s implicitly typed array feature (no type specified between the new and the []s) so that the compiler will infer the type of the array itself, construct this array object, and initialize its references to the two instances of the one anonymous type.1 Finally, a reference to this array object is assigned to the kids local variable, the type of which is inferred by the compiler due to C#\u2019s implicitly typed local variable feature.</p><p>I show the foreach loop as an example of how to use this array that was just created and initialized with the two anonymous type objects. I have to use an implicitly typed local variable (kid) for the loop, too. When I run this code, I get the following output.</p><p>Aidan Grant</p><p>1 If you think these sentences are fun to read, you can only imagine how fun they were to write in the first place!</p><h6>Casting Arrays</h6><p>For arrays with reference type elements, the CLR allows you to implicitly cast the source array\u2019s element type to a target type. For the cast to succeed, both array types must have the same number of dimensions, and an implicit or explicit conversion from the source element type to the target element type must exist. The CLR doesn\u2019t allow the casting of arrays with value type elements to any other type. (However, by using the Array.Copy method, you can create a new array and populate its elements in order to obtain the desired effect.) The following code demonstrates how array casting works.</p><p>// Create a two\u00addimensional FileStream array. FileStream[,] fs2dim = new FileStream[5, 10];</p><p>// Implicit cast to a two\u00addimensional Object array Object[,] o2dim = fs2dim;</p><p>// Can't cast from two\u00addimensional array to one\u00addimensional array</p><ul style=\"list-style-type: none;\">\n<li><p>// Compiler error CS0030: Cannot convert type 'object[*,*]' to // 'System.IO.Stream[]' Stream[] s1dim = (Stream[]) o2dim;</p><p>// Explicit cast to two\u00addimensional Stream array Stream[,] s2dim = (Stream[,]) o2dim;</p>\n<p>// Explicit cast to two\u00addimensional String array</p>\n</li>\n<li><p>// Compiles but throws InvalidCastException at runtime String[,] st2dim = (String[,]) o2dim;</p></li>\n</ul><p>// Create a one\u00addimensional Int32 array (value types). Int32[] i1dim = new Int32[5];</p><p>// Can't cast from array of value types to anything else // Compiler error CS0030: Cannot convert type 'int[]' to 'object[]' Object[] o1dim = (Object[]) i1dim;</p><p>// Create a new array, then use Array.Copy to coerce each element in the // source array to the desired type in the destination array. // The following code creates an array of references to boxed Int32s. Object[] ob1dim = new Object[i1dim.Length]; Array.Copy(i1dim, ob1dim, i1dim.Length);</p><p>The Array.Copy method is not just a method that copies elements from one array to another. The Copy method handles overlapping regions of memory correctly, as does C\u2019s memmove function. C\u2019s memcpy function, on the other hand, doesn\u2019t handle overlapping regions correctly. The Copy method can also convert each array element as it is copied if conversion is required. The Copy method is capable of performing the following conversions:</p><p>\u25a0\u25a0 Boxing value type elements to reference type elements, such as copying an Int32[] to an</p><p>Object[].</p><p>\u25a0\u25a0 Unboxing reference type elements to value type elements, such as copying an Object[] to</p><p>an Int32[].</p><p>\u25a0\u25a0 Widening CLR primitive value types, such as copying elements from an Int32[] to a</p><p>Double[].</p><p>\u25a0\u25a0 Downcasting elements when copying between array types that can\u2019t be proven to be compatible based on the array\u2019s type, such as when casting from an Object[] to an IFormattable[]. If every object in the Object[] implements IFormattable, Copy will succeed.</p><p>Here\u2019s another example showing the usefulness of Copy.</p><p>// Define a value type that implements an interface. internal struct MyValueType : IComparable {</p><p>public Int32 CompareTo(Object obj) {</p><p>... }</p><p>}</p><p>public static class Program {</p><p>public static void Main() { // Create an array of 100 value types. MyValueType[] src = new MyValueType[100];</p><p>// Create an array of IComparable references. IComparable[] dest = new IComparable[src.Length];</p><p>} }</p><p>// Initialize an array of IComparable elements to refer to boxed // versions of elements in the source array. Array.Copy(src, dest, src.Length);</p><p>As you might imagine, the Framework Class Library (FCL) takes advantage of Array\u2019s Copy method quite frequently.</p><p>In some situations, it is useful to cast an array from one type to another. This kind of functionality is called array covariance. When you take advantage of array covariance, you should be aware of an associated performance penalty. Let\u2019s say you have the following code.</p><p>String[] sa = new String[100]; Object[] oa = sa; // oa refers to an array of String elements oa[5] = \"Jeff\"; // Perf hit: CLR checks oa's element type for String; OK oa[3] = 5; // Perf hit: CLR checks oa's element type for Int32; throws</p><p>// ArrayTypeMismatchException</p><p>In the preceding code, the oa variable is typed as an Object[]; however, it really refers to a String[]. The compiler will allow you to write code that attempts to put a 5 into an array element because 5 is an Int32, which is derived from Object. Of course, the CLR must ensure type safety, and when assigning to an array element, the CLR must ensure that the assignment is legal. So the CLR must check at run time whether the array contains Int32 elements. In this case, it doesn\u2019t, and the assignment cannot be allowed; the CLR will throw an ArrayTypeMismatchException.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile121.png\"/><p>Note If you just need to make a copy of some array elements to another array, System. Buffer\u2019s BlockCopy method executes faster than Array\u2019s Copy method. However, Buffer\u2019s BlockCopy supports only primitive types; it does not offer the same casting abilities as Array\u2019s Copy method. The Int32 parameters are expressed as byte offsets within the ar-</p><p>ray, not as element indexes. BlockCopy is really designed for copying data that is bitwisecompatible from one array type to another blittable array type, such as copying a Byte[] containing Unicode characters (in the proper byte order) to a Char[]. This method allows programmers to partially make up for the lack of the ability to treat an array as a block of memory of any type.</p><p>If you need to reliably copy a set of array elements from one array to another array, you should use System.Array\u2019s ConstrainedCopy method. This method guarantees that the copy operation will either complete or throw an exception without destroying any data within the destination array. This allows ConstrainedCopy to be used in a constrained execution region (CER). In order to offer this guarantee, ConstrainedCopy requires that the source array\u2019s element type be the same as or derived from the destination array\u2019s element type. In addition, it will not perform any boxing, unboxing, or downcasting.</p><h6>All Arrays Are Implicitly Derived from System.Array</h6><p>When you declare an array variable like this:</p><p>FileStream[] fsArray;</p><p>then the CLR automatically creates a FileStream[] type for the AppDomain. This type will be implicitly derived from the System.Array type, and therefore, all of the instance methods and properties defined on the System.Array type will be inherited by the FileStream[] type, allowing these methods and properties to be called using the fsArray variable. This makes working with arrays extremely convenient because there are many helpful instance methods and properties defined by System.Array, such as Clone, CopyTo, GetLength, GetLongLength, GetLowerBound, GetUpperBound, Length, Rank, and others.</p><p>The System.Array type also exposes a large number of extremely useful static methods that operate on arrays. These methods all take a reference to an array as a parameter. Some of the useful static methods are AsReadOnly, BinarySearch, Clear, ConstrainedCopy, ConvertAll, Copy, Exists, Find, FindAll, FindIndex, FindLast, FindLastIndex, ForEach, IndexOf, LastIndexOf, Resize, Reverse, Sort, and TrueForAll. There are many overloads for each of these methods. In fact, many of the methods provide generic overloads for compile-time type safety as well as good performance. I encourage you to examine the SDK documentation to get an understanding of how useful and powerful these methods are.</p><h6>All Arrays Implicitly Implement IEnumerable, ICollection, and IList</h6><p>There are many methods that operate on various collection objects because the methods are declared with parameters such as IEnumerable, ICollection, and IList. It is possible to pass arrays to these methods because System.Array also implements these three interfaces. System.Array implements these non-generic interfaces because they treat all elements as System.Object. However, it would be nice to have System.Array implement the generic equivalent of these interfaces, providing better compile-time type safety as well as better performance.</p><p>The CLR team didn\u2019t want System.Array to implement IEnumerable&lt;T&gt;, ICollection&lt;T&gt;, and IList&lt;T&gt;, though, because of issues related to multi-dimensional arrays and non-zero\u2013based arrays. Defining these interfaces on System.Array would have enabled these interfaces for all array types. Instead, the CLR performs a little trick: when a single-dimensional, zero\u2013lower bound array type is created, the CLR automatically makes the array type implement IEnumerable&lt;T&gt;, ICollection&lt;T&gt;, and IList&lt;T&gt; (where T is the array\u2019s element type) and also implements the three interfaces for all of the array type\u2019s base types as long as they are reference types. The following hierarchy diagram helps make this clear.</p><p>Object Array (non\u00adgeneric IEnumerable, ICollection, IList)</p><p>Object[] (IEnumerable, ICollection, IList of Object) String[] (IEnumerable, ICollection, IList of String) Stream[] (IEnumerable, ICollection, IList of Stream)</p><p>FileStream[] (IEnumerable, ICollection, IList of FileStream)</p><p>.</p><p>. (other arrays of reference types)</p><p>.</p><p>So, for example, if you have the following line of code:</p><p>FileStream[] fsArray;</p><p>then when the CLR creates the FileStream[] type, it will cause this type to automatically implement the IEnumerable&lt;FileStream&gt;, ICollection&lt;FileStream&gt;, and IList&lt;FileStream&gt; interfaces. Furthermore, the FileStream[] type will also implement the interfaces for the base types: IEnumerable&lt;Stream&gt;, IEnumerable&lt;Object&gt;, ICollection&lt;Stream&gt;, ICollection&lt;Object&gt;, IList&lt;Stream&gt;, and IList&lt;Object&gt;. Because all of these interfaces are automatically implemented by the CLR, the fsArray variable could be used wherever any of these interfaces exist. For example, the fsArray variable could be passed to methods that have any of the following prototypes.</p><p>void M1(IList&lt;FileStream&gt; fsList) { \u2026 } void M2(ICollection&lt;Stream&gt; sCollection) { \u2026 } void M3(IEnumerable&lt;Object&gt; oEnumerable) { \u2026 }</p><p>Note that if the array contains value type elements, the array type will not implement the interfaces for the element\u2019s base types. For example, if you have the following line of code:</p><p>DateTime[] dtArray; // An array of value types</p><p>then the DateTime[] type will implement IEnumerable&lt;DateTime&gt;, ICollection&lt;DateTime&gt;, and IList&lt;DateTime&gt; only; it will not implement versions of these interfaces that are generic over System.ValueType or System.Object. This means that the dtArray variable cannot be passed as an argument to the M3 method shown earlier. The reason for this is because arrays of value types are laid out in memory differently than arrays of reference types. Array memory layout was discussed earlier in this chapter.</p><h6>Passing and Returning Arrays</h6><p>When passing an array as an argument to a method, you are really passing a reference to that array. Therefore, the called method is able to modify the elements in the array. If you don\u2019t want to allow this, you must make a copy of the array and pass the copy into the method. Note that the Array.Copy method performs a shallow copy, and therefore, if the array\u2019s elements are reference types, the new array refers to the already existing objects.</p><p>Similarly, some methods return a reference to an array. If the method constructs and initializes the array, returning a reference to the array is fine. But if the method wants to return a reference to an internal array maintained by a field, you must decide if you want the method\u2019s caller to have direct access to this array and its elements. If you do, just return the array\u2019s reference. But most often, you won\u2019t want the method\u2019s caller to have such access, so the method should construct a new array and call Array.Copy, returning a reference to the new array. Again, be aware that Array.Copy makes a shallow copy of the original array.</p><p>If you define a method that returns a reference to an array, and if that array has no elements in it, your method can return either null or a reference to an array with zero elements in it. When you\u2019re implementing this kind of method, Microsoft strongly recommends that you implement the method by having it return a zero-length array because doing so simplifies the code that a developer calling the method must write. For example, this easy-to-understand code runs correctly even if there are no appointments to iterate over.</p><p>// This code is easier to write and understand. Appointment[] appointments = GetAppointmentsForToday(); for (Int32 a = 0; a &lt; appointments.Length; a++) {</p><p>... }</p><p>The following code also runs correctly if there are no appointments to iterate over. However, this code is slightly more difficult to write and understand.</p><p>// This code is harder to write and understand. Appointment[] appointments = GetAppointmentsForToday(); if (appointments != null) {</p><p>}</p><p>for (Int32 a = 0, a &lt; appointments.Length; a++) {</p><p>// Do something with appointments[a] }</p><p>If you design your methods to return arrays with zero elements instead of null, callers of your methods will have an easier time working with them. By the way, you should do the same for fields. If your type has a field that\u2019s a reference to an array, you should consider having the field refer to an array even if the array has no elements in it.</p><h6>Creating Non-Zero Lower Bound Arrays</h6><p>Earlier I mentioned that it\u2019s possible to create and work with arrays that have non-zero lower bounds. You can dynamically create your own arrays by calling Array\u2019s static CreateInstance method. Several overloads of this method exist, allowing you to specify the type of the elements in the array, the number of dimensions in the array, the lower bounds of each dimension, and the number of elements in each dimension. CreateInstance allocates memory for the array, saves the parameter information in the overhead portion of the array\u2019s memory block, and returns a reference to the array. If the array has two or more dimensions, you can cast the reference returned from CreateInstance to an ElementType[] variable (where ElementType is some type name), making it easier for you to access the elements in the array. If the array has just one dimension, in C#, you have to use Array\u2019s GetValue and SetValue methods to access the elements of the array.</p><p>Here\u2019s some code that demonstrates how to dynamically create a two-dimensional array of System.Decimal values. The first dimension represents calendar years from 2005 to 2009 inclusive, and the second dimension represents quarters from 1 to 4 inclusive. The code iterates over all the elements in the dynamic array. I could have hard-coded the array\u2019s bounds into the code, which would have given better performance, but I decided to use System.Array\u2019s GetLowerBound and GetUpperBound methods to demonstrate their use.</p><p>using System;</p><p>public static class DynamicArrays {</p><p>public static void Main() { // I want a two\u00addimensional array [2005..2009][1..4]. Int32[] lowerBounds = { 2005, 1 }; Int32[] lengths = { 5, 4 }; Decimal[,] quarterlyRevenue = (Decimal[,])</p><p>Array.CreateInstance(typeof(Decimal), lengths, lowerBounds);</p><p>Console.WriteLine(\"{0,4} {1,9} {2,9} {3,9} {4,9}\", \"Year\", \"Q1\", \"Q2\", \"Q3\", \"Q4\");</p><p>Int32 firstYear = quarterlyRevenue.GetLowerBound(0); Int32 lastYear = quarterlyRevenue.GetUpperBound(0); Int32 firstQuarter = quarterlyRevenue.GetLowerBound(1); Int32 lastQuarter = quarterlyRevenue.GetUpperBound(1);</p><p>}</p><p>for (Int32 year = firstYear; year &lt;= lastYear; year++) { Console.Write(year + \" \"); for (Int32 quarter = firstQuarter; quarter &lt;= lastQuarter; quarter++) {</p><p>Console.Write(\"{0,9:C} \", quarterlyRevenue[year, quarter]);</p><p>} Console.WriteLine();</p><p>} }</p><p>If you compile and run this code, you get the following output.</p><p>Year Q1 Q2 Q3 Q4</p><ul style=\"list-style-type: none;\">\n<li><p>2005 $0.00 $0.00 $0.00 $0.00</p></li>\n<li><p>2006 $0.00 $0.00 $0.00 $0.00</p></li>\n<li><p>2007 $0.00 $0.00 $0.00 $0.00</p></li>\n<li><p>2008 $0.00 $0.00 $0.00 $0.00</p></li>\n<li><p>2009 $0.00 $0.00 $0.00 $0.00</p></li>\n</ul><h6>Array Internals</h6><p>Internally, the CLR actually supports two different kinds of arrays:</p><p>\u25a0\u25a0 Single-dimensional arrays with a lower bound of 0. These arrays are sometimes called SZ (for</p><p>single-dimensional, zero-based) arrays or vectors.</p><p>\u25a0\u25a0 Single-dimensional and multi-dimensional arrays with an unknown lower bound. You can actually see the different kinds of arrays by executing the following code (the output is</p><p>shown in the code\u2019s comments).</p><p>using System;</p><p>public sealed class Program { public static void Main() { Array a;</p><p>// Create a 1\u00addim, 0\u00adbased array, with no elements in it a = new String[0]; Console.WriteLine(a.GetType()); // \"System.String[]\"</p><ul style=\"list-style-type: none;\">\n<li><p>// Create a 1\u00addim, 0\u00adbased array, with no elements in it a = Array.CreateInstance(typeof(String),</p><ul style=\"list-style-type: none;\">\n<li><p>new Int32[] { 0 }, new Int32[] { 0 });</p><p>Console.WriteLine(a.GetType()); // \"System.String[]\"</p>\n<p>// Create a 1\u00addim, 1\u00adbased array, with no elements in it a = Array.CreateInstance(typeof(String),</p>\n</li>\n<li><p>new Int32[] { 0 }, new Int32[] { 1 });</p></li>\n</ul>\n</li>\n</ul><p>Console.WriteLine(a.GetType()); // \"System.String[*]\" &lt;\u00ad\u00ad INTERESTING!</p><p>Console.WriteLine();</p><p>// Create a 2\u00addim, 0\u00adbased array, with no elements in it a = new String[0, 0]; Console.WriteLine(a.GetType()); // \"System.String[,]\"</p><ul style=\"list-style-type: none;\">\n<li><p>// Create a 2\u00addim, 0\u00adbased array, with no elements in it a = Array.CreateInstance(typeof(String),</p><ul style=\"list-style-type: none;\">\n<li><p>new Int32[] { 0, 0 }, new Int32[] { 0, 0 });</p><p>Console.WriteLine(a.GetType()); // \"System.String[,]\"</p>\n<p>// Create a 2\u00addim, 1\u00adbased array, with no elements in it a = Array.CreateInstance(typeof(String),</p>\n</li>\n<li><p>new Int32[] { 0, 0 }, new Int32[] { 1, 1 });</p></li>\n</ul>\n</li>\n</ul><p>}</p><p>Console.WriteLine(a.GetType()); // \"System.String[,]\" }</p><p>Next to each Console.WriteLine is a comment that indicates the output. For the singledimensional arrays, the zero-based arrays display a type name of System.String[], whereas the 1-based array displays a type name of System.String[*]. The * indicates that the CLR knows that this array is not zero-based. Note that C# does not allow you to declare a variable of type String[*], and therefore it is not possible to use C# syntax to access a single-dimensional, non-zero\u2013based array. Although you can call Array\u2019s GetValue and SetValue methods to access the elements of the array, this access will be slow due to the overhead of the method call.</p><p>For multi-dimensional arrays, the zero-based and 1-based arrays all display the same type name: System.String[,]. The CLR treats all multi-dimensional arrays as though they are not zero-based at run time. This would make you think that the type name should display as System.String[*,*]; however, the CLR doesn\u2019t use the *s for multi-dimensional arrays because they would always be present, and the asterisks would just confuse most developers.</p><p>Accessing the elements of a single-dimensional, zero-based array is slightly faster than accessing the elements of a non-zero\u2013based, single-dimensional array or a multi-dimensional array. There are several reasons for this. First, there are specific IL instructions\u2014such as newarr, ldelem, ldelema, ldlen, and stelem\u2014to manipulate single-dimensional, zero-based arrays, and these special IL instructions cause the JIT compiler to emit optimized code. For example, the JIT compiler will emit code that assumes that the array is zero-based, and this means that an offset doesn\u2019t have to be subtracted from the specified index when accessing an element. Second, in common situations, the JIT compiler is able to hoist the index range\u2013checking code out of the loop, causing it to execute just once. For example, look at the following commonly written code.</p><p>using System;</p><p>public static class Program {</p><p>public static void Main() { Int32[] a = new Int32[5]; for(Int32 index = 0; index &lt; a.Length; index++) {</p><p>// Do something with a[index] }</p><p>} }</p><p>The first thing to notice about this code is the call to the array\u2019s Length property in the for loop\u2019s test expression. Because Length is a property, querying the length actually represents a method call. However, the JIT compiler knows that Length is a property on the Array class, and the JIT compiler will actually generate code that calls the property just once and stores the result in a temporary variable that will be checked with each iteration of the loop. The result is that the JITted code is fast. In fact, some developers have underestimated the abilities of the JIT compiler and have tried to write \u201cclever code\u201d in an attempt to help the JIT compiler. However, any clever attempts that you come up with will almost certainly impact performance negatively and make your code harder to read, reducing its maintainability. You are better off leaving the call to the array\u2019s Length property in the preceding code instead of attempting to cache it in a local variable yourself.</p><p>The second thing to notice about the preceding code is that the JIT compiler knows that the for loop is accessing array elements 0 through Length \u00ad 1. So the JIT compiler produces code that, at run time, tests that all array accesses will be within the array\u2019s valid range. Specifically, the JIT compiler produces code to check if (0 &gt;= a.GetLowerBound(0)) &amp;&amp; ((Length \u2013 1) &lt;= a.GetUpperBound(0)). This check occurs just before the loop. If the check is good, the JIT compiler will not generate code inside the loop to verify that each array access is within the valid range. This allows array access within the loop to be very fast.</p><p>Unfortunately, as I alluded to earlier in this chapter, accessing elements of a non-zero\u2013based single-dimensional array or of a multi-dimensional array is much slower than a single-dimensional, zerobased array. For these array types, the JIT compiler doesn\u2019t hoist index checking outside of loops, so each array access validates the specified indexes. In addition, the JIT compiler adds code to subtract the array\u2019s lower bounds from the specified index, which also slows the code down, even if you\u2019re using a multi-dimensional array that happens to be zero-based. So if performance is a concern to you, you might want to consider using an array of arrays (a jagged array) instead of a rectangular array.</p><p>C# and the CLR also allow you to access an array by using unsafe (non-verifiable) code, which is, in effect, a technique that allows you to turn off the index bounds checking when accessing an array. Note that this unsafe array manipulation technique is usable with arrays whose elements are SByte, Byte, Int16, UInt16, Int32, UInt32, Int64, UInt64, Char, Single, Double, Decimal, Boolean, an enumerated type, or a value type structure whose fields are any of the aforementioned types.</p><p>This is a very powerful feature that should be used with extreme caution because it allows you to perform direct memory accesses. If these memory accesses are outside the bounds of the array, an exception will not be thrown; instead, you will be corrupting memory, violating type safety, and possibly opening a security hole! For this reason, the assembly containing the unsafe code must either be granted full trust or at least have the Security Permission with Skip Verification turned on.</p><p>The following C# code demonstrates three techniques (safe, jagged, and unsafe), for accessing a two-dimensional array.</p><p>using System; using System.Diagnostics;</p><p>public static class Program { private const Int32 c_numElements = 10000;</p><p>public static void Main() { // Declare a two\u00addimensional array Int32[,] a2Dim = new Int32[c_numElements, c_numElements];</p><p>// Declare a two\u00addimensional array as a jagged array (a vector of vectors) Int32[][] aJagged = new Int32[c_numElements][]; for (Int32 x = 0; x &lt; c_numElements; x++)</p><p>aJagged[x] = new Int32[c_numElements];</p><p>}</p><ul style=\"list-style-type: none;\">\n<li><p>// 1: Access all elements of the array using the usual, safe technique Safe2DimArrayAccess(a2Dim);</p></li>\n<li><p>// 2: Access all elements of the array using the jagged array technique SafeJaggedArrayAccess(aJagged);</p></li>\n<li><p>// 3: Access all elements of the array using the unsafe technique Unsafe2DimArrayAccess(a2Dim);</p></li>\n</ul><p>private static Int32 Safe2DimArrayAccess(Int32[,] a) { Int32 sum = 0; for (Int32 x = 0; x &lt; c_numElements; x++) {</p><p>for (Int32 y = 0; y &lt; c_numElements; y++) {</p><p>sum += a[x, y]; }</p><p>} return sum;</p><p>}</p><p>private static Int32 SafeJaggedArrayAccess(Int32[][] a) { Int32 sum = 0; for (Int32 x = 0; x &lt; c_numElements; x++) {</p><p>for (Int32 y = 0; y &lt; c_numElements; y++) {</p><p>sum += a[x][y]; }</p><p>} return sum;</p><p>}</p><p>private static unsafe Int32 Unsafe2DimArrayAccess(Int32[,] a) { Int32 sum = 0; fixed (Int32* pi = a) {</p><p>for (Int32 x = 0; x &lt; c_numElements; x++) { Int32 baseOfDim = x * c_numElements; for (Int32 y = 0; y &lt; c_numElements; y++) {</p><p>sum += pi[baseOfDim + y]; }</p><p>}</p><p>} return sum;</p><p>} }</p><p>The Unsafe2DimArrayAccess method is marked with the unsafe modifier, which is required</p><p>to use C#\u2019s fixed statement. To compile this code, you\u2019ll have to specify the /unsafe switch when invoking the C# compiler or select the Allow Unsafe Code check box on the Build tab of the Project Properties pane in Microsoft Visual Studio.</p><p>Obviously, the unsafe technique has a time and place when it can best be used by your own code, but beware that there are three serious downsides to using this technique:</p><p>\u25a0\u25a0 The code that manipulates the array elements is more complicated to read and write than that which manipulates the elements using the other techniques because you are using C#\u2019s fixed statement and performing memory-address calculations.</p><p>\u25a0\u25a0 If you make a mistake in the calculation, you are accessing memory that is not part of the array. This can result in an incorrect calculation, corruption of memory, a type-safety violation, and a potential security hole.</p><p>\u25a0\u25a0 Due to the potential problems, the CLR forbids unsafe code from running in reduced-security</p><p>environments (like Microsoft Silverlight).</p><h6>Unsafe Array Access and Fixed-Size Array</h6><p>Unsafe array access is very powerful because it allows you to access:</p><p>\u25a0\u25a0 Elements within a managed array object that resides on the heap (as the previous section</p><p>demonstrated).</p><p>\u25a0\u25a0 Elements within an array that resides on an unmanaged heap. The SecureString example in Chapter 14, \u201cChars, Strings, and Working with Text,\u201d demonstrated using unsafe array access on an array returned from calling the System.Runtime.InteropServices.Marshal class\u2019s SecureStringToCoTaskMemUnicode method.</p><p>\u25a0\u25a0 Elements within an array that resides on the thread\u2019s stack. In cases in which performance is extremely critical, you could avoid allocating a managed array</p><p>object on the heap and instead allocate the array on the thread\u2019s stack by using C#\u2019s stackalloc statement (which works a lot like C\u2019s alloca function). The stackalloc statement can be used to create a single-dimensional, zero-based array of value type elements only, and the value type must not contain any reference type fields. Really, you should think of this as allocating a block of memory that you can manipulate by using unsafe pointers, and therefore, you cannot pass the address of this memory buffer to the vast majority of FCL methods. Of course, the stack-allocated memory (array) will automatically be freed when the method returns; this is where we get the performance improvement. Using this feature also requires you to specify the /unsafe switch to the C# compiler.</p><p>The StackallocDemo method in the following code shows an example of how to use C#\u2019s stackalloc statement.</p><p>using System;</p><p>public static class Program {</p><p>public static void Main() { StackallocDemo(); InlineArrayDemo();</p><p>}</p><p>private static void StackallocDemo() {</p><p>unsafe { const Int32 width = 20; Char* pc = stackalloc Char[width]; // Allocates array on stack</p><p>String s = \"Jeffrey Richter\"; // 15 characters</p><p>for (Int32 index = 0; index &lt; width; index++) { pc[width \u00ad index \u00ad 1] =</p><p>(index &lt; s.Length) ? s[index] : '.'; }</p><p>} }</p><p>// The following line displays \".....rethciR yerffeJ\" Console.WriteLine(new String(pc, 0, width));</p><p>private static void InlineArrayDemo() {</p><p>unsafe { CharArray ca; // Allocates array on stack Int32 widthInBytes = sizeof(CharArray); Int32 width = widthInBytes / 2;</p><p>String s = \"Jeffrey Richter\"; // 15 characters</p><p>for (Int32 index = 0; index &lt; width; index++) { ca.Characters[width \u00ad index \u00ad 1] =</p><p>(index &lt; s.Length) ? s[index] : '.'; }</p><p>}</p><p>} }</p><p>// The following line displays \".....rethciR yerffeJ\" Console.WriteLine(new String(ca.Characters, 0, width));</p><p>internal unsafe struct CharArray { // This array is embedded inline inside the structure public fixed Char Characters[20];</p><p>}</p><p>Normally, because arrays are reference types, an array field defined in a structure is really just a pointer or reference to an array; the array itself lives outside of the structure\u2019s memory. However, it is possible to embed an array directly inside a structure as shown by the CharArray structure in the preceding code. To embed an array directly inside a structure, there are several requirements:</p><p>\u25a0\u25a0 The type must be a structure (value type); you cannot embed an array inside a class (reference</p><p>type).</p><p>\u25a0\u25a0 The field or its defining structure must be marked with the unsafe keyword.</p><p>\u25a0\u25a0 The array field must be marked with the fixed keyword.</p><p>\u25a0\u25a0 The array must be single-dimensional and zero-based.</p><p>\u25a0\u25a0 The array\u2019s element type must be one of the following types: Boolean, Char, SByte, Byte,</p><p>Int16, UInt16, Int32, UInt32, Int64, UInt64, Single, or Double.</p><p>Inline arrays are typically used for scenarios that involve interoperating with unmanaged code where the unmanaged data structure also has an inline array. However, inline arrays can be used in other scenarios as well. The InlineArrayDemo method in the code shown earlier offers an example of how to use an inline array. The InlineArrayDemo method performs the same function as the StackallocDemo method; it just does it in a different way.</p><h6>C H A P T E R 17</h6><h2>Delegates</h2><h6>In this chapter:</h6><p>A First Look at Delegates . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 391 Using Delegates to Call Back Static Methods . . . . . . . . . . . . . . . 394 Using Delegates to Call Back Instance Methods. . . . . . . . . . . . . 395 Demystifying Delegates . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 396 Using Delegates to Call Back Many Methods (Chaining) . . . . . 400 Enough with the Delegate Definitions Already (Generic Delegates) . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 407 C#\u2019s Syntactical Sugar for Delegates . . . . . . . . . . . . . . . . . . . . . . . 408 Delegates and Reflection . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 416</p><p>In this chapter, I talk about callback functions. Callback functions are an extremely useful programming mechanism that has been around for years. The Microsoft .NET Framework exposes a callback function mechanism by using delegates. Unlike callback mechanisms used in other platforms, such as unmanaged C++, delegates offer much more functionality. For example, delegates ensure that the callback method is type-safe, in keeping with one of the most important goals of the common language runtime (CLR). Delegates also integrate the ability to call multiple methods sequentially and support the calling of static methods as well as instance methods.</p><h6>A First Look at Delegates</h6><p>The C runtime\u2019s qsort function takes a pointer to a callback function to sort elements within an array. In Windows, callback functions are required for window procedures, hook procedures, asynchronous procedure calls, and more. In the .NET Framework, callback methods are used for a whole slew of things. For example, you can register callback methods to get a variety of notifications such as unhandled exceptions, window state changes, menu item selections, file system changes, form control events, and completed asynchronous operations.</p><p>In unmanaged C/C++, the address of a non-member function is just a memory address. This address doesn\u2019t carry any additional information such as the number of parameters the function expects, the types of these parameters, the function\u2019s return value type, and the function\u2019s calling convention. In short, unmanaged C/C++ callback functions are not type-safe (although they are a very lightweight mechanism).</p><p>391</p><p>In the .NET Framework, callback functions are just as useful and pervasive as in unmanaged Windows programming. However, the .NET Framework provides a type-safe mechanism called delegates. I\u2019ll start off the discussion of delegates by showing you how to use them. The following code demonstrates how to declare, create, and use delegates.</p><p>using System; using System.Windows.Forms; using System.IO;</p><p>// Declare a delegate type; instances refer to a method that // takes an Int32 parameter and returns void. internal delegate void Feedback(Int32 value);</p><p>public sealed class Program {</p><p>public static void Main() { StaticDelegateDemo(); InstanceDelegateDemo(); ChainDelegateDemo1(new Program()); ChainDelegateDemo2(new Program());</p><p>}</p><p>private static void StaticDelegateDemo() { Console.WriteLine(\"\u00ad\u00ad\u00ad\u00ad\u00ad Static Delegate Demo \u00ad\u00ad\u00ad\u00ad\u00ad\"); Counter(1, 3, null); Counter(1, 3, new Feedback(Program.FeedbackToConsole)); Counter(1, 3, new Feedback(FeedbackToMsgBox)); // \"Program.\" is optional Console.WriteLine();</p><p>}</p><p>private static void InstanceDelegateDemo() { Console.WriteLine(\"\u00ad\u00ad\u00ad\u00ad\u00ad Instance Delegate Demo \u00ad\u00ad\u00ad\u00ad\u00ad\"); Program p = new Program(); Counter(1, 3, new Feedback(p.FeedbackToFile));</p><p>Console.WriteLine(); }</p><p>private static void ChainDelegateDemo1(Program p) { Console.WriteLine(\"\u00ad\u00ad\u00ad\u00ad\u00ad Chain Delegate Demo 1 \u00ad\u00ad\u00ad\u00ad\u00ad\");</p><ul style=\"list-style-type: none;\">\n<li><p>Feedback fb1 = new Feedback(FeedbackToConsole);</p></li>\n<li><p>Feedback fb2 = new Feedback(FeedbackToMsgBox);</p></li>\n<li><p>Feedback fb3 = new Feedback(p.FeedbackToFile);</p></li>\n</ul><p>Feedback fbChain = null;</p><ul style=\"list-style-type: none;\">\n<li><p>fbChain = (Feedback) Delegate.Combine(fbChain, fb1);</p></li>\n<li><p>fbChain = (Feedback) Delegate.Combine(fbChain, fb2);</p></li>\n<li><p>fbChain = (Feedback) Delegate.Combine(fbChain, fb3); Counter(1, 2, fbChain);</p></li>\n</ul><p>}</p><p>Console.WriteLine(); fbChain = (Feedback)</p><p>Delegate.Remove(fbChain, new Feedback(FeedbackToMsgBox)); Counter(1, 2, fbChain);</p><p>private static void ChainDelegateDemo2(Program p) { Console.WriteLine(\"\u00ad\u00ad\u00ad\u00ad\u00ad Chain Delegate Demo 2 \u00ad\u00ad\u00ad\u00ad\u00ad\");</p><ul style=\"list-style-type: none;\">\n<li><p>Feedback fb1 = new Feedback(FeedbackToConsole);</p></li>\n<li><p>Feedback fb2 = new Feedback(FeedbackToMsgBox);</p></li>\n<li><p>Feedback fb3 = new Feedback(p.FeedbackToFile);</p></li>\n</ul><p>Feedback fbChain = null;</p><ul style=\"list-style-type: none;\">\n<li><p>fbChain += fb1;</p></li>\n<li><p>fbChain += fb2;</p></li>\n<li><p>fbChain += fb3; Counter(1, 2, fbChain);</p></li>\n</ul><p>}</p><p>Console.WriteLine(); fbChain \u00ad= new Feedback(FeedbackToMsgBox); Counter(1, 2, fbChain);</p><p>private static void Counter(Int32 from, Int32 to, Feedback fb) {</p><p>for (Int32 val = from; val &lt;= to; val++) { // If any callbacks are specified, call them if (fb != null)</p><p>fb(val); }</p><p>}</p><p>private static void FeedbackToConsole(Int32 value) {</p><p>Console.WriteLine(\"Item=\" + value); }</p><p>private static void FeedbackToMsgBox(Int32 value) {</p><p>MessageBox.Show(\"Item=\" + value); }</p><p>private void FeedbackToFile(Int32 value) { using (StreamWriter sw = new StreamWriter(\"Status\", true)) {</p><p>sw.WriteLine(\"Item=\" + value); }</p><p>} }</p><p>Now I\u2019ll describe what this code is doing. At the top, notice the declaration of the internal delegate, Feedback. A delegate indicates the signature of a callback method. In this example, a Feedback delegate identifies a method that takes one parameter (an Int32) and returns void. In a way, a delegate is very much like an unmanaged C/C++ typedef that represents the address of a function.</p><p>The Program class defines a private, static method named Counter. This method counts integers from the from argument to the to argument. The Counter method also takes an fb, which is a reference to a Feedback delegate object. Counter iterates through all of the integers, and for each integer, if the fb variable is not null, the callback method (specified by the fb variable) is called. This callback method is passed the value of the item being processed, the item number. The callback method can be designed and implemented to process each item in any manner deemed appropriate.</p><h6>Using Delegates to Call Back Static Methods</h6><p>Now that you understand how the Counter method is designed and how it works, let\u2019s see how to use delegates to call back static methods. The StaticDelegateDemo method that appears in the previous code sample is the focus of this section.</p><p>The StaticDelegateDemo method calls the Counter method, passing null in the third parameter, which corresponds to Counter\u2019s fb parameter. Because Counter\u2019s fb parameter receives null, each item is processed without calling any callback method.</p><p>Next, the StaticDelegateDemo method calls Counter a second time, passing a newly con-</p><p>structed Feedback delegate object in the third parameter of the method call. This delegate object is a wrapper around a method, allowing the method to be called back indirectly via the wrapper. In this example, the name of the static method, Program.FeedbackToConsole, is passed to the Feedback type\u2019s constructor, indicating that it is the method to be wrapped. The reference returned from the</p><p>new operator is passed to Counter as its third parameter. Now when Counter executes, it will call the Program type\u2019s static FeedbackToConsole method for each item in the series. FeedbackToConsole simply writes a string to the console indicating the item being processed.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile122.png\"/><p>Note The FeedbackToConsole method is defined as private inside the Program type, but the Counter method is able to call Program\u2019s private method. In this case, you might not expect a problem because both Counter and FeedbackToConsole are defined in the same type. However, this code would work just fine even if the Counter method was defined in another type. In short, it is not a security or accessibility violation for one type to have code that calls another type\u2019s private member via a delegate as long as the delegate object is created by code that has ample security/accessibility.</p><p>The third call to Counter in the StaticDelegateDemo method is almost identical to the second call. The only difference is that the Feedback delegate object wraps the static Program.FeedbackToMsgBox method. FeedbackToMsgBox builds a string indicating the item being processed. This string is then displayed in a message box.</p><p>Everything in this example is type-safe. For instance, when constructing a Feedback delegate object, the compiler ensures that the signatures of Program\u2019s FeedbackToConsole and FeedbackToMsgBox methods are compatible with the signature defined by the Feedback delegate. Specifically, both methods must take one argument (an Int32), and both methods must have the same return type (void). If FeedbackToConsole had been defined like this:</p><p>private static Boolean FeedbackToConsole(String value) {</p><p>... }</p><p>the C# compiler wouldn\u2019t compile the code and would issue the following error: error CS0123: No overload for 'FeedbackToConsole' matches delegate 'Feedback'.</p><p>Both C# and the CLR allow for covariance and contra-variance of reference types when binding a method to a delegate. Covariance means that a method can return a type that is derived from the delegate\u2019s return type. Contra-variance means that a method can take a parameter that is a base of the delegate\u2019s parameter type. For example, given a delegate defined like this:</p><p>delegate Object MyCallback(FileStream s);</p><p>it is possible to construct an instance of this delegate type bound to a method that is prototyped like this.</p><p>String SomeMethod(Stream s);</p><p>Here, SomeMethod\u2019s return type (String) is a type that is derived from the delegate\u2019s return type (Object); this covariance is allowed. SomeMethod\u2019s parameter type (Stream) is a type that is a base class of the delegate\u2019s parameter type (FileStream); this contra-variance is allowed.</p><p>Note that covariance and contra-variance are supported only for reference types, not for value types or for void. So, for example, I cannot bind the following method to the MyCallback delegate.</p><p>Int32 SomeOtherMethod(Stream s);</p><p>Even though SomeOtherMethod\u2019s return type (Int32) is derived from MyCallback\u2019s return type (Object), this form of covariance is not allowed because Int32 is a value type. Obviously, the reason why value types and void cannot be used for covariance and contra-variance is because the memory structure for these things varies, whereas the memory structure for reference types is always a pointer. Fortunately, the C# compiler will produce an error if you attempt to do something that is not supported.</p><h6>Using Delegates to Call Back Instance Methods</h6><p>I just explained how delegates can be used to call static methods, but they can also be used to call instance methods for a specific object. To understand how calling back an instance method works, look at the InstanceDelegateDemo method that appears in the code shown at the beginning of this chapter.</p><p>Notice that a Program object named p is constructed in the InstanceDelegateDemo method. This Program object doesn\u2019t have any instance fields or properties associated with it; I created it merely for demonstration purposes. When the new Feedback delegate object is constructed in the call to the Counter method, its constructor is passed p.FeedbackToFile. This causes the delegate to wrap a reference to the FeedbackToFile method, which is an instance method (not a static method). When Counter calls the callback method identified by its fb argument, the FeedbackToFile instance method is called, and the address of the recently constructed object p will be passed as the implicit this argument to the instance method.</p><p>The FeedbackToFile method works as the FeedbackToConsole and FeedbackToMsgBox methods, except that it opens a file and appends the string to the end of the file. (The Status file that the method creates can be found in the application\u2019s AppBase directory.)</p><p>Again, the purpose of this example is to demonstrate that delegates can wrap calls to instance methods as well as static methods. For instance methods, the delegate needs to know the instance of the object the method is going to operate on. Wrapping an instance method is useful because code inside the object can access the object\u2019s instance members. This means that the object can have some state that can be used while the callback method is doing its processing.</p><h6>Demystifying Delegates</h6><p>On the surface, delegates seem easy to use: you define them by using C#\u2019s delegate keyword, you construct instances of them by using the familiar new operator, and you invoke the callback by using the familiar method-call syntax (except instead of a method name, you use the variable that refers to the delegate object).</p><p>However, what\u2019s really going on is quite a bit more complex than what the earlier examples illustrate. The compilers and the CLR do a lot of behind-the-scenes processing to hide the complexity. In this section, I\u2019ll focus on how the compiler and the CLR work together to implement delegates. Having this knowledge will improve your understanding of delegates and will teach you how to use them efficiently and effectively. I\u2019ll also touch on some additional features delegates make available.</p><p>Let\u2019s start by reexamining this line of code.</p><p>internal delegate void Feedback(Int32 value);</p><p>When the compiler sees this line, it actually defines a complete class that looks something like this.</p><p>internal class Feedback : System.MulticastDelegate { // Constructor public Feedback(Object @object, IntPtr method);</p><p>// Method with same prototype as specified by the source code public virtual void Invoke(Int32 value);</p><p>// Methods allowing the callback to be called asynchronously public virtual IAsyncResult BeginInvoke(Int32 value,</p><p>AsyncCallback callback, Object @object);</p><p>public virtual void EndInvoke(IAsyncResult result); }</p><p>The class defined by the compiler has four methods: a constructor, Invoke, BeginInvoke, and EndInvoke. In this chapter, I\u2019ll concentrate on the constructor and Invoke methods. The BeginInvoke and EndInvoke methods are related to the .NET Framework's Asynchronous Programming Model which is now considered obsolete and has been replaced by tasks that I discuss in Chapter 27, \u201cCompute-Bound Asynchronous Operations.\u201d</p><p>In fact, you can verify that the compiler did indeed generate this class automatically by examining the resulting assembly with ILDasm.exe, as shown in Figure 17-1.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile123.png\"/><figcaption>FIGURE 17-1 ILDasm.exe showing the metadata produced by the compiler for the delegate.</figcaption><p>In this example, the compiler has defined a class called Feedback that is derived from the System.MulticastDelegate type defined in the Framework Class Library (FCL). (All delegate types are derived from MulticastDelegate.)</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile124.png\"/><p>Important The System.MulticastDelegate class is derived from System.Delegate, which is itself derived from System.Object. The reason why there are two delegate classes is historical and unfortunate; there should be just one delegate class in the FCL. Sadly, you need to be aware of both of these classes because even though all delegate types you create have MulticastDelegate as a base class, you\u2019ll occasionally manipulate your delegate types by using methods defined by the Delegate class instead of the MulticastDelegate class. For example, the Delegate class has static methods called Combine and Remove. (I explain what these methods do later.) The signatures for both of these methods indicate that they take Delegate parameters. Because your delegate type is derived from MulticastDelegate, which is derived from Delegate, instances of your delegate type can be passed to these methods.</p><p>The class has private visibility because the delegate is declared as internal in the source code. If the source code had indicated public visibility, the Feedback class the compiler generated would also be public. You should be aware that delegate types can be defined within a type (nested within another type) or at global scope. Basically, because delegates are classes, a delegate can be defined anywhere a class can be defined.</p><p>Because all delegate types are derived from MulticastDelegate, they inherit MulticastDelegate\u2019s fields, properties, and methods. Of all of these members, three non-public fields are probably most significant. Table 17-1 describes these significant fields.</p><figcaption>TABLE 17-1 MulticastDelegate\u2019s Significant Non-Public Fields</figcaption><table border=\"1\">\n<tr>\n<td rowspan=\"2\"><p>Field</p>\n<p>_target</p>\n<p>_methodPtr</p>\n</td>\n<td rowspan=\"2\"><p>Type</p>\n<p>System.Object</p>\n<p>System.IntPtr</p>\n</td>\n<td><p>Description When the delegate object wraps a static method, this field is null. When the delegate objects wraps an instance method, this field refers to the object that should be operated on when the callback method is called. In other words, this field indicates the value that should be passed for the instance method\u2019s implicit this parameter.</p>\n</td>\n</tr>\n<tr>\n<td><p>An internal integer the CLR uses to identify the method that is to be called back.</p>\n</td>\n</tr>\n<tr>\n<td><p>_invocationList</p>\n</td>\n<td><p>System.Object</p>\n</td>\n<td><p>This field is usually null. It can refer to an array of delegates when building a delegate chain (discussed later in this chapter).</p>\n</td>\n</tr>\n</table><p>Notice that all delegates have a constructor that takes two parameters: a reference to an object and an integer that refers to the callback method. However, if you examine the source code, you\u2019ll see that I\u2019m passing in values such as Program.FeedbackToConsole or p.FeedbackToFile. Everything you\u2019ve learned about programming tells you that this code shouldn\u2019t compile!</p><p>However, the C# compiler knows that a delegate is being constructed and parses the source code to determine which object and method are being referred to. A reference to the object is passed for the constructor\u2019s object parameter, and a special IntPtr value (obtained from a MethodDef or MemberRef metadata token) that identifies the method is passed for the method parameter. For static methods, null is passed for the object parameter. Inside the constructor, these two arguments are saved in the _target and _methodPtr private fields, respectively. In addition, the constructor sets the _invocationList field to null. I\u2019ll postpone discussing this _invocationList field until the next section, \u201cUsing Delegates to Call Back Many Methods (Chaining).\u201d</p><p>So each delegate object is really a wrapper around a method and an object to be operated on when the method is called. So if I have two lines of code that look like this:</p><p>Feedback fbStatic = new Feedback(Program.FeedbackToConsole); Feedback fbInstance = new Feedback(new Program().FeedbackToFile);</p><p>the fbStatic and fbInstance variables refer to two separate Feedback delegate objects that are initialized, as shown in Figure 17-2.</p><p>_target _methodPtr _invocationList</p><p>null FeedbackToConsole null</p><p>fbStatic</p><p>(Program Object)</p><p>_target _methodPtr _invocationList</p><p>fbInstance</p><p>FeedbackToFile null</p><p>FIGURE 17-2 A variable that refers to a delegate to a static method and a variable that refers to a delegate to an instance method.</p><p>Now that you know how delegate objects are constructed and what their internal structure looks like, let\u2019s talk about how the callback method is invoked. For convenience, I\u2019ve repeated the code for the Counter method here.</p><p>private static void Counter(Int32 from, Int32 to, Feedback fb) {</p><p>for (Int32 val = from; val &lt;= to; val++) { // If any callbacks are specified, call them if (fb != null)</p><p>fb(val); }</p><p>}</p><p>Look at the line of code just below the comment. The if statement first checks to see if fb is not null. If fb is not null, on the next line, you see the code that invokes the callback method. The null check is required because fb is really just a variable that can refer to a Feedback delegate object; it could also be null. It might seem as if I\u2019m calling a function named fb and passing it one parameter (val). However, there is no function called fb. Again, because it knows that fb is a variable that refers to a delegate object, the compiler generates code to call the delegate object\u2019s Invoke method. In other words, the compiler sees the following.</p><p>fb(val);</p><p>But the compiler generates code as though the source code said the following.</p><p>fb.Invoke(val);</p><p>You can verify that the compiler produces code to call the delegate type\u2019s Invoke method by using ILDasm.exe to examine the Intermediate Language (IL) code created for the Counter method. Here is the IL for the Counter method. The instruction at IL_0009 in the figure indicates the call to Feedback\u2019s Invoke method.</p><p>.method private hidebysig static void Counter(int32 from, int32 'to', class Feedback fb) cil managed</p><p>{</p><p>// Code size 23 (0x17)</p><p>.maxstack 2</p><p>.locals init (int32 val)</p><ul style=\"list-style-type: none;\">\n<li><p>IL_0000: ldarg.0</p></li>\n<li><p>IL_0001: stloc.0</p></li>\n<li><p>IL_0002: br.s IL_0012 IL_0004: ldarg.2 IL_0005: brfalse.s IL_000e</p></li>\n</ul><ul style=\"list-style-type: none;\">\n<li><p>IL_0007: ldarg.2</p></li>\n<li><p>IL_0008: ldloc.0</p></li>\n<li><p>IL_0009: callvirt instance void Feedback::Invoke(int32) IL_000e: ldloc.0 IL_000f: ldc.i4.1</p></li>\n<li><p>IL_0010: add</p></li>\n<li><p>IL_0011: stloc.0</p></li>\n<li><p>IL_0012: ldloc.0</p></li>\n<li><p>IL_0013: ldarg.1</p></li>\n<li><p>IL_0014: ble.s IL_0004 IL_0016: ret</p></li>\n</ul><p>} // end of method Program::Counter</p><p>In fact, you could modify the Counter method to call Invoke explicitly, as shown here.</p><p>private static void Counter(Int32 from, Int32 to, Feedback fb) {</p><p>for (Int32 val = from; val &lt;= to; val++) { // If any callbacks are specified, call them if (fb != null)</p><p>fb.Invoke(val); }</p><p>}</p><p>You\u2019ll recall that the compiler defined the Invoke method when it defined the Feedback class. When Invoke is called, it uses the private _target and _methodPtr fields to call the desired method on the specified object. Note that the signature of the Invoke method matches the signature of the delegate; because the Feedback delegate takes one Int32 parameter and returns void, the Invoke method (as produced by the compiler) takes one Int32 parameter and returns void.</p><h6>Using Delegates to Call Back Many Methods (Chaining)</h6><p>By themselves, delegates are incredibly useful. But add in their support for chaining, and delegates become even more useful. Chaining is a set or collection of delegate objects, and it provides the ability to invoke, or call, all of the methods represented by the delegates in the set. To understand this, see the ChainDelegateDemo1 method that appears in the code shown at the beginning of this chapter. In this method, after the Console.WriteLine statement, I construct three delegate objects and have variables\u2014fb1, fb2, and fb3\u2014refer to each object, as shown in Figure 17-3.</p><p>_target _methodPtr _invocationList</p><p>null FeedbackToConsole null</p><ul style=\"list-style-type: none;\">\n<li><p>fb1</p></li>\n<li><p>fb2</p></li>\n<li><p>fb3</p></li>\n</ul><p>_target _methodPtr _invocationList</p><p>null FeedbackToMsgBox null</p><p>(Program Object)</p><p>_target _methodPtr _invocationList</p><p>FeedbackToFile null</p><p>FIGURE 17-3 Initial state of the delegate objects referred to by the fb1, fb2, and fb3 variables.</p><p>The reference variable to a Feedback delegate object, fbChain, is intended to refer to a chain</p><p>or set of delegate objects that wrap methods that can be called back. Initializing fbChain to null indicates that there currently are no methods to be called back. The Delegate class\u2019s public, static Combine method is used to add a delegate to the chain.</p><p>fbChain = (Feedback) Delegate.Combine(fbChain, fb1);</p><p>When this line of code executes, the Combine method sees that we are trying to combine null and fb1. Internally, Combine will simply return the value in fb1, and the fbChain variable will be set to refer to the same delegate object referred to by the fb1 variable, as shown in Figure 17-4.</p><p>fbChain</p><p>_target _methodPtr _invocationList</p><p>null FeedbackToConsole null</p><ul style=\"list-style-type: none;\">\n<li><p>fb1</p></li>\n<li><p>fb2</p></li>\n<li><p>fb3</p></li>\n</ul><p>_target _methodPtr _invocationList</p><p>null FeedbackToMsgBox null</p><p>(Program Object)</p><p>_target _methodPtr _invocationList</p><p>FeedbackToFile null</p><p>FIGURE 17-4 State of the delegate objects after inserting the first delegate in the chain.</p><p>To add another delegate to the chain, the Combine method is called again.</p><ul style=\"list-style-type: none;\">\n<li><p>fbChain = (Feedback) Delegate.Combine(fbChain, fb2);</p><p>Internally, the Combine method sees that fbChain already refers to a delegate object, so Combine will construct a new delegate object. This new delegate object initializes its private _target and _methodPtr fields to values that are not important for this discussion. However, what is important is that the _invocationList field is initialized to refer to an array of delegate objects. The first element of this array (index 0) will be initialized to refer to the delegate that wraps the FeedbackToConsole method (this is the delegate that fbChain currently refers to). The second element of the array (index</p>\n<p>1) will be initialized to refer to the delegate that wraps the FeedbackToMsgBox method (this is the delegate that fb2 refers to). Finally, fbChain will be set to refer to the newly created delegate object, shown in Figure 17-5.</p>\n<p>To add the third delegate to the chain, the Combine method is called once again.</p>\n</li>\n<li><p>fbChain = (Feedback) Delegate.Combine(fbChain, fb3);</p></li>\n</ul><p>Again, Combine sees that fbChain already refers to a delegate object, and this causes a new delegate object to be constructed, as shown in Figure 17-6. As before, this new delegate object initializes the private _target and _methodPtr fields to values unimportant to this discussion, and the _invocationList field is initialized to refer to an array of delegate objects. The first and second elements of this array (indexes 0 and 1) will be initialized to refer to the same delegates the previous delegate object referred to in its array. The third element of the array (index 2) will be initialized to refer to the delegate that wraps the FeedbackToFile method (this is the delegate that fb3 refers to). Finally, fbChain will be set to refer to this newly created delegate object. Note that the previously created delegate and the array referred to by its _invocationList field are now candidates for garbage collection.</p><p>_target _methodPtr _invocationList</p><p>(not important) (not important)</p><p>[0] [1]</p><p>fbChain</p><p>_target _methodPtr _invocationList</p><p>null FeedbackToConsole null</p><ul style=\"list-style-type: none;\">\n<li><p>fb1</p></li>\n<li><p>fb2</p></li>\n<li><p>fb3</p></li>\n</ul><p>_target _methodPtr _invocationList</p><p>null FeedbackToMsgBox null</p><p>(Program Object)</p><p>_target _methodPtr _invocationList</p><p>FeedbackToFile null</p><ul style=\"list-style-type: none;\">\n<li><p>FIGURE 17-5 State of the delegate objects after inserting the second delegate in the chain.</p><p>fbChain</p>\n<p>_target _methodPtr _invocationList</p>\n<p>(not important) (not important)</p>\n<ul style=\"list-style-type: none;\">\n<li><p>[0]</p></li>\n<li><p>[1]</p></li>\n<li><p>[2]</p></li>\n</ul>\n<p>_target _methodPtr _invocationList</p>\n<p>(not important) (not important)</p>\n<ul style=\"list-style-type: none;\">\n<li><p>[0]</p></li>\n<li><p>[1]</p></li>\n</ul>\n<p>_target _methodPtr _invocationList</p>\n<p>null FeedbackToConsole null</p>\n<p>_target _methodPtr _invocationList</p>\n<p>null FeedbackToMsgBox null</p>\n<p>_target _methodPtr _invocationList</p>\n<p>FeedbackToFile null</p>\n<p>(Program Object)</p>\n<ul style=\"list-style-type: none;\">\n<li><p>fb1</p></li>\n<li><p>fb2</p></li>\n<li><p>fb3</p></li>\n</ul>\n</li>\n<li><p>FIGURE 17-6 Final state of the delegate objects when the chain is complete.</p></li>\n</ul><p>After all of the code has executed to set up the chain, the fbChain variable is then passed to the Counter method.</p><p>Counter(1, 2, fbChain);</p><p>Inside the Counter method is the code that implicitly calls the Invoke method on the Feedback delegate object as I detailed earlier. When Invoke is called on the delegate referred to by fbChain, the delegate sees that the private _invocationList field is not null, causing it to execute a loop that iterates through all of the elements in the array, calling the method wrapped by each delegate. In this example, FeedbackToConsole will get called first, followed by FeedbackToMsgBox, followed by FeedbackToFile.</p><p>Feedback\u2019s Invoke method is essentially implemented something like the following (in pseudocode).</p><p>public void Invoke(Int32 value) { Delegate[] delegateSet = _invocationList as Delegate[]; if (delegateSet != null) {</p><p>// This delegate's array indicates the delegates that should be called foreach (Feedback d in delegateSet)</p><p>d(value); // Call each delegate</p><p>} else { // This delegate identifies a single method to be called back // Call the callback method on the specified target object. _methodPtr.Invoke(_target, value); // The preceding line is an approximation of the actual code. // What really happens cannot be expressed in C#.</p><p>} }</p><p>Note that it is also possible to remove a delegate from a chain by calling Delegate\u2019s public, static Remove method. This is demonstrated toward the end of the ChainDelegateDemo1 method.</p><p>fbChain = (Feedback) Delegate.Remove(fbChain, new Feedback(FeedbackToMsgBox));</p><p>When Remove is called, it scans the delegate array (from the end toward index 0) maintained inside the delegate object referred to by the first parameter (fbChain, in my example). Remove is looking for a delegate entry whose _target and _methodPtr fields match those in the second argument (the new Feedback delegate, in my example). If a match is found and there is only one item left in the array, that array item is returned. If a match is found and there are multiple items left in the array, a new delegate object is constructed\u2014the _invocationList array created and initialized will refer to all items in the original array except for the item being removed, of course\u2014and a reference to this new delegate object is returned. If you are removing the only element in the chain, Remove returns null. Note that each call to Remove removes just one delegate from the chain; it does not remove all delegates that have matching _target and _methodPtr fields.</p><p>So far, I\u2019ve shown examples in which my delegate type, Feedback, is defined as having a void return value. However, I could have defined my Feedback delegate as follows.</p><p>public delegate Int32 Feedback(Int32 value);</p><p>If I had, its Invoke method would have internally looked like the following (again, in pseudocode).</p><p>public Int32 Invoke(Int32 value) { Int32 result; Delegate[] delegateSet = _invocationList as Delegate[]; if (delegateSet != null) {</p><p>// This delegate's array indicates the delegates that should be called foreach (Feedback d in delegateSet)</p><p>result = d(value); // Call each delegate</p><p>} else { // This delegate identifies a single method to be called back // Call the callback method on the specified target object. result = _methodPtr.Invoke(_target, value); // The preceding line is an approximation of the actual code. // What really happens cannot be expressed in C#.</p><p>} return result;</p><p>}</p><p>As each delegate in the array is called, its return value is saved in the result variable. When the loop is complete, the result variable will contain only the result of the last delegate called (previous return values are discarded); this value is returned to the code that called Invoke.</p><h6>C#\u2019s Support for Delegate Chains</h6><p>To make things easier for C# developers, the C# compiler automatically provides overloads of the</p><p>+= and \u00ad= operators for instances of delegate types. These operators call Delegate.Combine and Delegate.Remove, respectively. Using these operators simplifies the building of delegate chains. The ChainDelegateDemo1 and ChainDelegateDemo2 methods in the source code shown at the beginning of this chapter produce absolutely identical IL code. The only difference between the methods is that the ChainDelegateDemo2 method simplifies the source code by taking advantage of C#\u2019s += and -= operators.</p><p>If you require proof that the resulting IL code is identical for the two methods, you can build the code and look at its IL for both methods by using ILDasm.exe. This will confirm that the C# compiler did in fact replace all += and -= operators with calls to the Delegate type\u2019s public static Combine and Remove methods, respectively.</p><h6>Having More Control over Delegate Chain Invocation</h6><p>At this point, you understand how to build a chain of delegate objects and how to invoke all of the objects in that chain. All items in the chain are invoked because the delegate type\u2019s Invoke method includes code to iterate through all of the items in the array, invoking each item. This is obviously a very simple algorithm. And although this simple algorithm is good enough for a lot of scenarios, it has many limitations. For example, the return values of the callback methods are all discarded except for the last one. Using this simple algorithm, there\u2019s no way to get the return values for all of the callback methods called. But this isn\u2019t the only limitation. What happens if one of the invoked delegates throws an exception or blocks for a very long time? Because the algorithm invoked each delegate in</p><p>the chain serially, a \u201cproblem\u201d with one of the delegate objects stops all of the subsequent delegates in the chain from being called. Clearly, this algorithm isn\u2019t robust.</p><p>For those scenarios in which this algorithm is insufficient, the MulticastDelegate class offers an instance method, GetInvocationList, that you can use to call each delegate in a chain explicitly, using any algorithm that meets your needs.</p><p>public abstract class MulticastDelegate : Delegate { // Creates a delegate array where each element refers // to a delegate in the chain. public sealed override Delegate[] GetInvocationList();</p><p>}</p><p>The GetInvocationList method operates on a MulticastDelegate-derived object and returns an array of Delegate references where each reference points to one of the chain\u2019s delegate objects. Internally, GetInvocationList constructs an array and initializes it with each element referring to a delegate in the chain; a reference to the array is then returned. If the _invocationList field is null, the returned array contains one element that references the only delegate in the chain: the delegate instance itself.</p><p>You can easily write an algorithm that explicitly calls each object in the array. The following code demonstrates.</p><p>using System; using System.Reflection; using System.Text;</p><p>// Define a Light component. internal sealed class Light {</p><p>// This method returns the light's status. public String SwitchPosition() { return \"The light is off\";</p><p>} }</p><p>// Define a Fan component. internal sealed class Fan {</p><p>// This method returns the fan's status. public String Speed() {</p><p>throw new InvalidOperationException(\"The fan broke due to overheating\"); }</p><p>}</p><p>// Define a Speaker component. internal sealed class Speaker {</p><p>// This method returns the speaker's status. public String Volume() {</p><p>return \"The volume is loud\"; }</p><p>}</p><p>public sealed class Program {</p><p>// Definition of delegate that allows querying a component's status. private delegate String GetStatus();</p><p>public static void Main() { // Declare an empty delegate chain. GetStatus getStatus = null;</p><p>// Construct the three components, and add their status methods // to the delegate chain. getStatus += new GetStatus(new Light().SwitchPosition); getStatus += new GetStatus(new Fan().Speed); getStatus += new GetStatus(new Speaker().Volume);</p><p>}</p><p>// Show consolidated status report reflecting // the condition of the three components. Console.WriteLine(GetComponentStatusReport(getStatus));</p><p>// Method that queries several components and returns a status report private static String GetComponentStatusReport(GetStatus status) {</p><p>// If the chain is empty, there is nothing to do. if (status == null) return null;</p><p>// Use this to build the status report. StringBuilder report = new StringBuilder();</p><p>// Get an array where each element is a delegate from the chain. Delegate[] arrayOfDelegates = status.GetInvocationList();</p><p>// Iterate over each delegate in the array. foreach (GetStatus getStatus in arrayOfDelegates) {</p><p>try { // Get a component's status string, and append it to the report. report.AppendFormat(\"{0}{1}{1}\", getStatus(), Environment.NewLine);</p><p>} catch (InvalidOperationException e) {</p><p>// Generate an error entry in the report for this component. Object component = getStatus.Target; report.AppendFormat(</p><p>\"Failed to get status from {1}{2}{0} Error: {3}{0}{0}\", Environment.NewLine, ((component == null) ? \"\" : component.GetType() + \".\"), getStatus.GetMethodInfo().Name, e.Message);</p><p>} }</p><p>} }</p><p>// Return the consolidated report to the caller. return report.ToString();</p><p>When you build and run this code, the following output appears.</p><p>The light is off</p><p>Failed to get status from Fan.Speed Error: The fan broke due to overheating</p><p>The volume is loud</p><h6>Enough with the Delegate Definitions Already (Generic Delegates)</h6><p>Many years ago, when the .NET Framework was just starting to be developed, Microsoft introduced the notion of delegates. As programmers were adding classes to the FCL, they would define new delegate types any place they introduced a callback method. Over time, many, many delegates got defined. In fact, in MSCorLib.dll alone, close to 50 delegate types are now defined. Let\u2019s just look at a few of them.</p><p>public delegate void TryCode(Object userData); public delegate void WaitCallback(Object state); public delegate void TimerCallback(Object state); public delegate void ContextCallback(Object state); public delegate void SendOrPostCallback(Object state); public delegate void ParameterizedThreadStart(Object obj);</p><p>Do you notice anything similar about the few delegate definitions that I selected? They are really all the same: a variable of any of these delegate types must refer to a method that takes an Object and returns void. There is really no reason to have all of these delegate types defined; there really just needs to be one.</p><p>In fact, now that the .NET Framework supports generics, we really just need a few generic delegates (defined in the System namespace) that represent methods that take up to 16 arguments.</p><p>public delegate void Action(); // OK, this one is not generic public delegate void Action&lt;T&gt;(T obj); public delegate void Action&lt;T1, T2&gt;(T1 arg1, T2 arg2); public delegate void Action&lt;T1, T2, T3&gt;(T1 arg1, T2 arg2, T3 arg3);</p><p>... public delegate void Action&lt;T1, ..., T16&gt;(T1 arg1, ..., T16 arg16);</p><p>So the .NET Framework now ships with 17 Action delegates that range from having no arguments to having 16 arguments. If you ever need to call a method that has more than 16 arguments, you will be forced to define your own delegate type, but this is very unlikely.</p><p>In addition to the Action delegates, the .NET Framework ships with 17 Func delegates, which allow the callback method to return a value.</p><p>public delegate TResult Func&lt;TResult&gt;(); public delegate TResult Func&lt;T, TResult&gt;(T arg); public delegate TResult Func&lt;T1, T2, TResult&gt;(T1 arg1, T2 arg2); public delegate TResult Func&lt;T1, T2, T3, TResult&gt;(T1 arg1, T2 arg2, T3 arg3);</p><p>... public delegate TResult Func&lt;T1,..., T16, TResult&gt;(T1 arg1, ..., T16 arg16);</p><p>It is now recommended that these delegate types be used wherever possible instead of developers defining even more delegate types in their code. This reduces the number of types in the system and also simplifies coding. However, you might have to define your own delegate if you need to pass an argument by reference using the ref or out keyword.</p><p>delegate void Bar(ref Int32 z);</p><p>You may also have to do this if you want your delegate to take a variable number of arguments via C#\u2019s params keyword, if you want to specify any default values for any of your delegate\u2019s arguments, or if you need to constrain a delegate\u2019s generic type argument.</p><p>When using delegates that take generic arguments and return values, contra-variance and covariance come into play, and it is recommended that you always take advantage of these features because they have no ill effects and enable your delegates to be used in more scenarios. For more information about this, see the \u201cDelegate and Interface Contra-variant and Covariant Generic Type Arguments\u201d section in Chapter 12, \u201cGenerics.\u201d</p><h6>C#\u2019s Syntactical Sugar for Delegates</h6><p>Most programmers find working with delegates to be cumbersome because the syntax is so strange. For example, take this line of code.</p><p>button1.Click += new EventHandler(button1_Click);</p><p>where button1_Click is a method that looks something like this.</p><p>void button1_Click(Object sender, EventArgs e) { // Do something, the button was clicked...</p><p>}</p><p>The idea behind the first line of code is to register the address of the button1_Click method with a button control so that when the button is clicked, the method will be called. To most programmers, it feels quite unnatural to construct an EventHandler delegate object just to specify the</p><p>address of the button1_Click method. However, constructing the EventHandler delegate object is required for the CLR because this object provides a wrapper that ensures that the method can be called only in a type-safe fashion. The wrapper also allows the calling of instance methods and chaining. Unfortunately, most programmers don\u2019t want to think about these details. Programmers would prefer to write the preceding code as follows.</p><p>button1.Click += button1_Click;</p><p>Fortunately, Microsoft\u2019s C# compiler offers programmers some syntax shortcuts when working with delegates. I\u2019ll explain all of these shortcuts in this section. One last point before we begin: what I\u2019m about to describe really boils down to C# syntactical sugar; these new syntax shortcuts are really just giving programmers an easier way to produce the IL that must be generated so that the CLR and other programming languages can work with delegates. This also means that what I\u2019m about to describe is specific to C#; other compilers might not offer the additional delegate syntax shortcuts.</p><h6>Syntactical Shortcut #1: No Need to Construct a Delegate Object</h6><p>As demonstrated already, C# allows you to specify the name of a callback method without having to construct a delegate object wrapper. Here is another example.</p><p>internal sealed class AClass { public static void CallbackWithoutNewingADelegateObject() {</p><p>ThreadPool.QueueUserWorkItem(SomeAsyncTask, 5); }</p><p>}</p><p>private static void SomeAsyncTask(Object o) {</p><p>Console.WriteLine(o); }</p><p>Here, the ThreadPool class\u2019s static QueueUserWorkItem method expects a reference to a WaitCallback delegate object that contains a reference to the SomeAsyncTask method. Because the C# compiler is capable of inferring this on its own, it allows me to omit code that constructs the WaitCallback delegate object, making the code much more readable and understandable. Of course, when the code is compiled, the C# compiler does produce IL that does, in fact, new up the WaitCallback delegate object\u2014we just got a syntactical shortcut.</p><h6>Syntactical Shortcut #2: No Need to Define a Callback Method (Lambda Expressions)</h6><p>In the preceding code, the name of the callback method, SomeAsyncTask, is passed to the ThreadPool\u2019s QueueUserWorkItem method. C# allows you to write the code for the callback method inline so it doesn\u2019t have to be written inside its very own method. For example, the preceding code could be rewritten as follows.</p><p>internal sealed class AClass { public static void CallbackWithoutNewingADelegateObject() {</p><p>ThreadPool.QueueUserWorkItem( obj =&gt; Console.WriteLine(obj), 5); }</p><p>}</p><p>Notice that the first \u201cargument\u201d to the QueueUserWorkItem method is code (which I italicized)! More formally, the italicized code is called a C# lambda expression, and it is easy to detect due to the use of C#'s lambda expression operator: =&gt;. You may use a lambda expression in your code where the compiler would normally expect to see a delegate. And, when the compiler sees the use of this lambda expression, the compiler automatically defines a new private method in the class (AClass, in this example). This new method is called an anonymous function because the compiler creates the name of the method for you automatically, and normally, you wouldn\u2019t know its name. However, you could use a tool such as ILDasm.exe to examine the compiler-generated code. After I wrote the preceding code and compiled it, I was able to see, by using ILDasm.exe, that the C# compiler decided to name this method &lt;CallbackWithoutNewingADelegateObject&gt;b__0 and ensured that this method took a single Object argument and returned void.</p><p>The compiler chose to start the method name with a &lt; sign because in C#, an identifier cannot contain a &lt; sign; this ensures that you will not accidentally define a method that coincides with the name the compiler has chosen for you. Incidentally, while C# forbids identifiers to contain a &lt; sign, the CLR allows it, and that is why this works. Also, note that although you could access the method via reflection by passing the method name as a string, the C# language specification states that there is no guarantee of how the compiler generates the name. For example, each time you compile the code, the compiler could produce a different name for the method.</p><p>Using ILDasm.exe, you might also notice that the C# compiler applies the System.Runtime. CompilerServices.CompilerGeneratedAttribute attribute to this method to indicate to various tools and utilities that this method was produced by a compiler as opposed to a programmer. The code to the right of the =&gt; operator is then placed in this compiler-generated method.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile125.png\"/><p>Note When writing a lambda expression, there is no way to apply your own custom attribute to the compiler-generated method. Furthermore, you cannot apply any method modifiers (such as unsafe) to the method. But this is usually not a problem because anonymous methods generated by the compiler always end up being private, and the method is either static or nonstatic depending on whether the method accesses any instance members. So there is no need to apply modifiers such as public, protected, internal, virtual, sealed, override, or abstract to the method.</p><p>Finally, if you write the preceding code and compile it, it\u2019s as if the C# compiler rewrote your code to look like the following (comments inserted by me).</p><p>internal sealed class AClass { // This private field is created to cache the delegate object. // Pro: CallbackWithoutNewingADelegateObject will not create // a new object each time it is called. // Con: The cached object never gets garbage collected [CompilerGenerated] private static WaitCallback &lt;&gt;9__CachedAnonymousMethodDelegate1;</p><p>public static void CallbackWithoutNewingADelegateObject() {</p><p>if (&lt;&gt;9__CachedAnonymousMethodDelegate1 == null) { // First time called, create the delegate object and cache it. &lt;&gt;9__CachedAnonymousMethodDelegate1 =</p><p>new WaitCallback(&lt;CallbackWithoutNewingADelegateObject&gt;b__0);</p><p>} ThreadPool.QueueUserWorkItem(&lt;&gt;9__CachedAnonymousMethodDelegate1, 5);</p><p>}</p><p>}</p><p>[CompilerGenerated] private static void &lt;CallbackWithoutNewingADelegateObject&gt;b__0(Object obj) {</p><p>Console.WriteLine(obj); }</p><p>The lambda expression must match that of the WaitCallback delegate: it returns void and takes an Object parameter. However, I specified the name of the parameter by simply putting obj to the left of the =&gt; operator. On the right of the =&gt; operator, Console.WriteLine happens to return void. However, if I had placed an expression that did not return void, the compiler-generated code would just ignore the return value because the method that the compiler generates must have a void return type to satisfy the WaitCallback delegate.</p><p>It is also worth noting that the anonymous function is marked as private; this forbids any code not defined within the type from accessing the method (although reflection will reveal that the method does exist). Also, note that the anonymous method is marked as static; this is because the code doesn\u2019t access any instance members (which it can\u2019t because CallbackWithoutNewingADelegateObject is itself a static method. However, the code can reference any static fields or static methods defined within the class. Here is an example.</p><p>internal sealed class AClass { private static String sm_name; // A static field</p><p>public static void CallbackWithoutNewingADelegateObject() {</p><p>ThreadPool.QueueUserWorkItem( // The callback code can reference static members. obj =&gt;Console.WriteLine(sm_name + \": \" + obj), 5);</p><p>} }</p><p>If the CallbackWithoutNewingADelegateObject method had not been static, the anonymous method\u2019s code could contain references to instance members. If it doesn\u2019t contain references to instance members, the compiler will still produce a static anonymous method because this is more efficient than an instance method because the additional this parameter is not necessary. But, if the anonymous method\u2019s code does reference an instance member, the compiler will produce a nonstatic anonymous method.</p><p>internal sealed class AClass { private String m_name; // An instance field</p><p>// An instance method public void CallbackWithoutNewingADelegateObject() {</p><p>ThreadPool.QueueUserWorkItem( // The callback code can reference instance members. obj =&gt; Console.WriteLine(m_name + \": \" + obj), 5);</p><p>} }</p><p>On the left side of the =&gt; operator is where you specify the names of any arguments that are to be passed to the lambda expression. There are some rules you must follow here. See the following examples.</p><p>// If the delegate takes no arguments, use () Func&lt;String&gt; f = () =&gt; \"Jeff\";</p><p>// If the delegate takes 1+ arguments, you can explicitly specify the types Func&lt;Int32, String&gt; f2 = (Int32 n) =&gt; n.ToString(); Func&lt;Int32, Int32, String&gt; f3 = (Int32 n1, Int32 n2) =&gt; (n1 + n2).ToString();</p><p>// If the delegate takes 1+ arguments, the compiler can infer the types Func&lt;Int32, String&gt; f4 = (n) =&gt; n.ToString(); Func&lt;Int32, Int32, String&gt; f5 = (n1, n2) =&gt; (n1 + n2).ToString();</p><p>// If the delegate takes 1 argument, you can omit the ()s Func&lt;Int32, String&gt; f6 = n =&gt; n.ToString();</p><p>// If the delegate has ref/out arguments, you must explicitly specify ref/out and the type Bar b = (out Int32 n) =&gt; n = 5;</p><p>For the last example, assume that Bar is defined as follows.</p><p>delegate void Bar(out Int32 z);</p><p>On the right side of the =&gt; operator is where you specify the anonymous function body. It is very common for the body to consist of a simple or complex expression that ultimately returns a nonvoid value. In the preceding code, I was assigning lambda expressions that returned Strings to all the Func delegate variables. It is also quite common for the body to consist of a single statement. An example of this is when I called ThreadPool.QueueUserWorkItem, passing it a lambda expression that called Console.WriteLine (which returns void).</p><p>If you want the body to consist of two or more statements, then you must enclose it in curly braces. And if the delegate expects a return value, then you must have a return statement inside the body. Here is an example.</p><p>Func&lt;Int32, Int32, String&gt; f7 = (n1, n2) =&gt; { Int32 sum = n1 + n2; return sum.ToString(); };</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile126.png\"/><p>Important In case it\u2019s not obvious, let me explicitly point out that the main benefit of lambda expressions is that they remove a level of indirection from within your source code. Normally, you\u2019d have to write a separate method, give that method a name, and then pass the name of that method where a delegate is required. The name gives you a way to refer to a body of code, and if you need to refer to the same body of code from multiple locations in your source code, then writing a method and giving it a name is a great way to go. However, if you need to have a body of code that is referred to only once within your source code, then a lambda expression allows you to put that code directly inline without having to assign it a name, thus increasing programmer productivity.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile127.png\"/><p>Note When C# 2.0 came out, it introduced a feature called anonymous methods. Like lambda expressions (introduced in C# 3.0), anonymous methods describes a syntax for creating anonymous functions. It is now recommended (in section 7.14 of the C# Language Specification) that developers use the newer lambda expression syntax rather than the older anonymous method syntax because the lambda expression syntax is more terse, making code easier to write, read, and maintain. Of course, Microsoft's C# compiler continues to support parsing both syntaxes for creating anonymous functions so that developers are not forced to modify any code that was originally written for C# 2.0. In this book, I will explain and use only the lambda expression syntax.</p><h6>Syntactical Shortcut #3: No Need to Wrap Local Variables in a Class Manually to Pass Them to a Callback Method</h6><p>I\u2019ve already shown how the callback code can reference other members defined in the class. However, sometimes, you might like the callback code to reference local parameters or variables that exist in the defining method. Here\u2019s an interesting example.</p><p>internal sealed class AClass {</p><p>public static void UsingLocalVariablesInTheCallbackCode(Int32 numToDo) { // Some local variables Int32[] squares = new Int32[numToDo]; AutoResetEvent done = new AutoResetEvent(false);</p><p>// Do a bunch of tasks on other threads for (Int32 n = 0; n &lt; squares.Length; n++) {</p><p>ThreadPool.QueueUserWorkItem( obj =&gt; { Int32 num = (Int32) obj;</p><p>// This task would normally be more time consuming squares[num] = num * num;</p><p>}</p><p>// If last task, let main thread continue running if (Interlocked.Decrement(ref numToDo) == 0)</p><p>done.Set();</p><p>}, n);</p><p>// Wait for all the other threads to finish done.WaitOne();</p><p>}</p><p>// Show the results for (Int32 n = 0; n &lt; squares.Length; n++)</p><p>Console.WriteLine(\"Index {0}, Square={1}\", n, squares[n]); }</p><p>This example really shows off how easy C# makes implementing what used to be a pretty complex task. The preceding method defines one parameter, numToDo, and two local variables, squares and done. And the body of the lambda expression refers to these variables.</p><p>Now imagine that the code in the body of the lambda expression is placed in a separate method (as is required by the CLR). How would the values of the variables be passed to the separate method? The only way to do this is to define a new helper class that also defines a field for each value that you want passed to the callback code. In addition, the callback code would have to be defined as an instance method in this helper class. Then, the UsingLocalVariablesInTheCallbackCode method would have to construct an instance of the helper class, initialize the fields from the values in its local variables, and then construct the delegate object bound to the helper object/instance method.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile128.png\"/><p>Note When a lambda expression causes the compiler to generate a class with parameter/ local variables turned into fields, the lifetime of the objects that the variables refer to are lengthened. Usually, a parameter/local variable goes out of scope at the last usage of the variable within a method. However, turning the variable into a field causes the field to keep the object that it refers to alive for the whole lifetime of the object containing the field. This is not a big deal in most applications, but it is something that you should be aware of.</p><p>This is very tedious and error-prone work, and, of course, the C# compiler does all this for you automatically. When you write the preceding code, it\u2019s as if the C# compiler rewrites your code so that it looks something like the following (comments inserted by me).</p><p>internal sealed class AClass { public static void UsingLocalVariablesInTheCallbackCode(Int32 numToDo) {</p><p>// Some local variables WaitCallback callback1 = null;</p><p>// Construct an instance of the helper class &lt;&gt;c__DisplayClass2 class1 = new &lt;&gt;c__DisplayClass2();</p><p>// Initialize the helper class's fields class1.numToDo = numToDo; class1.squares = new Int32[class1.numToDo]; class1.done = new AutoResetEvent(false);</p><p>// Do a bunch of tasks on other threads for (Int32 n = 0; n &lt; class1.squares.Length; n++) {</p><p>if (callback1 == null) { // New up delegate object bound to the helper object and // its anonymous instance method callback1 = new WaitCallback(</p><p>class1.&lt;UsingLocalVariablesInTheCallbackCode&gt;b__0); }</p><p>ThreadPool.QueueUserWorkItem(callback1, n); }</p><p>// Wait for all the other threads to finish class1.done.WaitOne();</p><p>// Show the results for (Int32 n = 0; n &lt; class1.squares.Length; n++)</p><p>Console.WriteLine(\"Index {0}, Square={1}\", n, class1.squares[n]); }</p><p>// The helper class is given a strange name to avoid potential // conflicts and is private to forbid access from outside AClass [CompilerGenerated] private sealed class &lt;&gt;c__DisplayClass2 : Object {</p><p>// One public field per local variable used in the callback code public Int32[] squares; public Int32 numToDo; public AutoResetEvent done;</p><p>// public parameterless constructor public &lt;&gt;c__DisplayClass2 { }</p><p>} }</p><p>// Public instance method containing the callback code public void &lt;UsingLocalVariablesInTheCallbackCode&gt;b__0(Object obj) {</p><p>Int32 num = (Int32) obj; squares[num] = num * num; if (Interlocked.Decrement(ref numToDo) == 0)</p><p>done.Set(); }</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile129.png\"/><p>Important Without a doubt, it doesn\u2019t take much for programmers to start abusing C#\u2019s lambda expression feature. When I first started using lambda expressions, it definitely took me some time to get used to them. After all, the code that you write in a method is not actually inside that method, and this also can make debugging and single-stepping through the code a bit more challenging. In fact, I\u2019m amazed at how well the Microsoft Visual Studio debugger actually handles stepping through lambda expressions in my source code.</p><p>I\u2019ve set up a rule for myself: If I need my callback method to contain more than three lines of code, I will not use a lambda expression; instead, I\u2019ll write the method manually and assign it a name of my own creation. But, used judiciously, lambda expressions can greatly increase programmer productivity as well as the maintainability of your code. The following is some code in which using lambda expressions feels very natural. Without them, this code would be tedious to write, harder to read, and harder to maintain.</p><p>// Create and initialize a String array String[] names = { \"Jeff\", \"Kristin\", \"Aidan\", \"Grant\" };</p><p>// Get just the names that have a lowercase 'a' in them. Char charToFind = 'a'; names = Array.FindAll(names, name =&gt; name.IndexOf(charToFind) &gt;= 0);</p><p>// Convert each string's characters to uppercase names = Array.ConvertAll(names, name =&gt; name.ToUpper());</p><p>// Display the results Array.ForEach(names, Console.WriteLine);</p><h6>Delegates and Reflection</h6><p>So far in this chapter, the use of delegates has required the developer to know up front the prototype of the method that is to be called back. For example, if fb is a variable that references a Feedback delegate (see this chapter\u2019s first program listing), to invoke the delegate, the code would look like the following.</p><p>fb(item); // item is defined as Int32</p><p>As you can see, the developer must know when coding how many parameters the callback method requires and the types of those parameters. Fortunately, the developer almost always has this information, so writing code like the preceding code isn\u2019t a problem.</p><p>In some rare circumstances, however, the developer doesn\u2019t have this information at compile time. I showed an example of this in Chapter 11, \u201cEvents,\u201d when I discussed the EventSet type. In this example, a dictionary maintained a set of different delegate types. At run time, to raise an event, one of the delegates was looked up in the dictionary and invoked. At compile time, it wasn\u2019t possible to know exactly which delegate would be called and which parameters were necessary to pass to the delegate\u2019s callback method.</p><p>Fortunately, System.Reflection.MethodInfo offers a CreateDelegate method that allows you to create a delegate when you just don\u2019t have all the necessary information about the delegate at compile time. Here are the method overloads that MethodInfo defines.</p><p>public abstract class MethodInfo : MethodBase { // Construct a delegate wrapping a static method. public virtual Delegate CreateDelegate(Type delegateType);</p><p>}</p><p>// Construct a delegate wrapping an instance method; target refers to the \u2018this\u2019 argument. public virtual Delegate CreateDelegate(Type delegateType, Object target);</p><p>After you\u2019ve created the delegate, you can call it by using Delegate\u2019s DynamicInvoke method, which looks like the following.</p><p>public abstract class Delegate { // Invoke a delegate passing it parameters public Object DynamicInvoke(params Object[] args);</p><p>}</p><p>Using reflection APIs (discussed in Chapter 23, \u201cAssembly Loading and Reflection\u201d), you must first acquire a MethodInfo object referring to the method you want to create a delegate to. Then, you call the CreateDelegate method to have it construct a new object of a Delegate-derived type identified by the first parameter, delegateType. If the delegate wraps an instance method, you will also pass to CreateDelegate a target parameter indicating the object that should be passed as the this parameter to the instance method.</p><p>System.Delegate\u2019s DynamicInvoke method allows you to invoke a delegate object\u2019s callback method, passing a set of parameters that you determine at run time. When you call DynamicInvoke, it internally ensures that the parameters you pass are compatible with the parameters the callback method expects. If they\u2019re compatible, the callback method is called. If they\u2019re not, an ArgumentException is thrown. DynamicInvoke returns the object the callback method returned.</p><p>The following code shows how to use the CreateDelegate and DynamicInvoke methods.</p><p>using System; using System.Reflection; using System.IO;</p><p>// Here are some different delegate definitions internal delegate Object TwoInt32s(Int32 n1, Int32 n2); internal delegate Object OneString(String s1);</p><p>public static class DelegateReflection { public static void Main(String[] args) { if (args.Length &lt; 2) {</p><p>String usage = @\"Usage:\" + \"{0} delType methodName [Arg1] [Arg2]\" + \"{0} where delType must be TwoInt32s or OneString\" +</p><p>}</p><p>\"{0} if delType is TwoInt32s, methodName must be Add or Subtract\" + \"{0} if delType is OneString, methodName must be NumChars or Reverse\" + \"{0}\" + \"{0}Examples:\" + \"{0} TwoInt32s Add 123 321\" + \"{0} TwoInt32s Subtract 123 321\" + \"{0} OneString NumChars \\\"Hello there\\\"\" + \"{0} OneString Reverse \\\"Hello there\\\"\";</p><p>Console.WriteLine(usage, Environment.NewLine); return;</p><p>// Convert the delType argument to a delegate type Type delType = Type.GetType(args[0]); if (delType == null) {</p><p>Console.WriteLine(\"Invalid delType argument: \" + args[0]); return;</p><p>}</p><p>Delegate d; try {</p><p>// Convert the Arg1 argument to a method MethodInfo mi = typeof(DelegateReflection).GetTypeInfo().GetDeclaredMethod(args[1]);</p><p>// Create a delegate object that wraps the static method d = mi.CreateDelegate(delType);</p><p>} catch (ArgumentException) {</p><p>Console.WriteLine(\"Invalid methodName argument: \" + args[1]); return;</p><p>}</p><p>// Create an array that that will contain just the arguments // to pass to the method via the delegate object Object[] callbackArgs = new Object[args.Length \u00ad 2];</p><p>if (d.GetType() == typeof(TwoInt32s)) {</p><p>try { // Convert the String arguments to Int32 arguments for (Int32 a = 2; a &lt; args.Length; a++)</p><p>callbackArgs[a \u00ad 2] = Int32.Parse(args[a]);</p><p>} catch (FormatException) {</p><p>Console.WriteLine(\"Parameters must be integers.\"); return;</p><p>} }</p><p>if (d.GetType() == typeof(OneString)) { // Just copy the String argument Array.Copy(args, 2, callbackArgs, 0, callbackArgs.Length);</p><p>}</p><p>}</p><p>try { // Invoke the delegate and show the result Object result = d.DynamicInvoke(callbackArgs); Console.WriteLine(\"Result = \" + result);</p><p>} catch (TargetParameterCountException) {</p><p>Console.WriteLine(\"Incorrect number of parameters specified.\"); }</p><p>// This callback method takes 2 Int32 arguments private static Object Add(Int32 n1, Int32 n2) {</p><p>return n1 + n2; }</p><p>// This callback method takes 2 Int32 arguments private static Object Subtract(Int32 n1, Int32 n2) {</p><p>return n1 \u00ad n2; }</p><p>// This callback method takes 1 String argument private static Object NumChars(String s1) {</p><p>return s1.Length; }</p><p>}</p><p>// This callback method takes 1 String argument private static Object Reverse(String s1) {</p><p>return new String(s1.Reverse().ToArray()); }</p><h6>C H A P T E R 1 8</h6><h2>Custom Attributes</h2><h6>In this chapter:</h6><p>Using Custom Attributes. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 421 Defining Your Own Attribute Class. . . . . . . . . . . . . . . . . . . . . . . . . 425 Attribute Constructor and Field/Property Data Types . . . . . . . 428 Detecting the Use of a Custom Attribute. . . . . . . . . . . . . . . . . . . 430 Matching Two Attribute Instances Against Each Other. . . . . . . 434 Detecting the Use of a Custom Attribute Without Creating Attribute-Derived Objects. . . . . . . . . . . . . . . . . . . . . . . . 437 Conditional Attribute Classes . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 440</p><p>In this chapter, I\u2019ll discuss one of the most innovative features the Microsoft .NET Framework has to offer: custom attributes. Custom attributes allow you to declaratively annotate your code constructs, thereby enabling special features. Custom attributes allow information to be defined and applied to almost any metadata table entry. This extensible metadata information can be queried at run time to dynamically alter the way code executes. As you use the various .NET Framework technologies (Windows Forms, WPF,WCF, , and so on), you\u2019ll see that they all take advantage of custom attributes, allowing developers to express their intentions within code very easily. A solid understanding of custom attributes is necessary for any .NET Framework developer.</p><h6>Using Custom Attributes</h6><p>Attributes, such as public, private, static, and so on, can be applied to types and members. I think we\u2019d all agree on the usefulness of applying attributes, but wouldn\u2019t it be even more useful if we could define our own attributes? For example, what if I could define a type and somehow indicate that the type can be remoted via serialization? Or maybe I could apply an attribute to a method to indicate that certain security permissions must be granted before the method can execute.</p><p>Of course, creating and applying user-defined attributes to types and methods would be great and convenient, but it would require the compiler to be aware of these attributes so it would emit the attribute information into the resulting metadata. Because compiler vendors usually prefer not to release the source code for their compiler, Microsoft came up with another way to allow user-defined attributes. This mechanism, called custom attributes, is an incredibly powerful mechanism that\u2019s useful at both application design time and run time. Anyone can define and use custom attributes, and all compilers that target the common language runtime (CLR) must be designed to recognize custom attributes and emit them into the resulting metadata.</p><p>421</p><p>The first thing you should realize about custom attributes is that they\u2019re just a way to associate additional information with a target. The compiler emits this additional information into the managed module\u2019s metadata. Most attributes have no meaning for the compiler; the compiler simply detects the attributes in the source code and emits the corresponding metadata.</p><p>The .NET Framework Class Library (FCL) defines literally hundreds of custom attributes that can be applied to items in your own source code. Here are some examples:</p><p>\u25a0\u25a0 Applying the DllImport attribute to a method informs the CLR that the implementation of</p><p>the method is actually in unmanaged code contained in the specified DLL.</p><p>\u25a0\u25a0 Applying the Serializable attribute to a type informs the serialization formatters that an</p><p>instance\u2019s fields may be serialized and deserialized.</p><p>\u25a0\u25a0 Applying the AssemblyVersion attribute to an assembly sets the version number of the as-</p><p>sembly.</p><p>\u25a0\u25a0 Applying the Flags attribute to an enumerated type causes the enumerated type to act as a</p><p>set of bit flags.</p><p>Following is some C# code with many attributes applied to it. In C#, you apply a custom attribute to a target by placing the attribute in square brackets immediately before the target. It\u2019s not important to understand what this code does. I just want you to see what attributes look like.</p><p>using System; using System.Runtime.InteropServices;</p><p>[StructLayout(LayoutKind.Sequential, CharSet = CharSet.Auto)] internal sealed class OSVERSIONINFO {</p><p>public OSVERSIONINFO() {</p><p>OSVersionInfoSize = (UInt32) Marshal.SizeOf(this); }</p><p>public UInt32 OSVersionInfoSize = 0; public UInt32 MajorVersion = 0; public UInt32 MinorVersion = 0; public UInt32 BuildNumber = 0; public UInt32 PlatformId = 0;</p><p>}</p><p>[MarshalAs(UnmanagedType.ByValTStr, SizeConst = 128)] public String CSDVersion = null;</p><p>internal sealed class MyClass { [DllImport(\"Kernel32\", CharSet = CharSet.Auto, SetLastError = true)] public static extern Boolean GetVersionEx([In, Out] OSVERSIONINFO ver);</p><p>}</p><p>In this case, the StructLayout attribute is applied to the OSVERSIONINFO class, the MarshalAs attribute is applied to the CSDVersion field, the DllImport attribute is applied to the GetVersionEx method, and the In and Out attributes are applied to GetVersionEx\u2019s ver parameter. Every programming language defines the syntax a developer must use in order to apply a custom attribute to a target. Microsoft Visual Basic .NET, for example, requires angle brackets (&lt;, &gt;) instead of square brackets.</p><p>The CLR allows attributes to be applied to just about anything that can be represented in a file\u2019s metadata. Most commonly, attributes are applied to entries in the following definition tables: TypeDef (classes, structures, enumerations, interfaces, and delegates), MethodDef (including constructors), ParamDef, FieldDef, PropertyDef, EventDef, AssemblyDef, and ModuleDef. Specifically, C# allows you to apply an attribute only to source code that defines any of the following targets: assembly, module, type (class, struct, enum, interface, delegate), field, method (including constructors), method parameter, method return value, property, event, and generic type parameter.</p><p>When you\u2019re applying an attribute, C# allows you to specify a prefix specifically indicating the target the attribute applies to. The following code shows all of the possible prefixes. In many cases, if you leave out the prefix, the compiler can still determine the target an attribute applies to, as shown in the previous example. In some cases, the prefix must be specified to make your intentions clear to the compiler. The prefixes shown in italics in the following code are mandatory.</p><p>using System;</p><p>[assembly: SomeAttr] // Applied to assembly [module: SomeAttr] // Applied to module</p><p>[type: SomeAttr] // Applied to type internal sealed class SomeType&lt;[typevar: SomeAttr] T&gt; { // Applied to generic type variable</p><p>[field: SomeAttr] // Applied to field public Int32 SomeField = 0;</p><p>[return: SomeAttr] // Applied to return value [method: SomeAttr] // Applied to method public Int32 SomeMethod(</p><p>[param: SomeAttr] // Applied to parameter Int32 SomeParam) { return SomeParam; }</p><p>[property: SomeAttr] // Applied to property public String SomeProp {</p><p>[method: SomeAttr] // Applied to get accessor method get { return null; }</p><p>}</p><p>}</p><p>[event: SomeAttr] // Applied to event [field: SomeAttr] // Applied to compiler\u00adgenerated field [method: SomeAttr] // Applied to compiler\u00adgenerated add &amp; remove methods public event EventHandler SomeEvent;</p><p>Now that you know how to apply a custom attribute, let\u2019s find out what an attribute really is. A custom attribute is simply an instance of a type. For Common Language Specification (CLS) compliance, custom attribute classes must be derived, directly or indirectly, from the public abstract System.Attribute class. C# allows only CLS-compliant attributes. By examining the .NET Framework SDK documentation, you\u2019ll see that the following classes (from the earlier example) are defined: StructLayoutAttribute, MarshalAsAttribute, DllImportAttribute, InAttribute, and OutAttribute. All of these classes happen to be defined in the System.Runtime.InteropServices namespace, but attribute classes can be defined in any namespace. Upon further examination, you\u2019ll notice that all of these classes are derived from System.Attribute, as all CLS-compliant attribute classes must be.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile130.png\"/><p>Note When applying an attribute to a target in source code, the C# compiler allows you to omit the Attribute suffix to reduce programming typing and to improve the readability of the source code. My code examples in this chapter take advantage of this C# convenience. For example, my source code contains [DllImport(...)] instead of [DllImportAttribute(...)].</p><p>As I mentioned earlier, an attribute is an instance of a class. The class must have a public constructor so that instances of it can be created. So when you apply an attribute to a target, the syntax is similar to that for calling one of the class\u2019s instance constructors. In addition, a language might permit some special syntax to allow you to set any public fields or properties associated with the attribute class. Let\u2019s look at an example. Recall the application of the DllImport attribute as it was applied to the GetVersionEx method earlier.</p><p>[DllImport(\"Kernel32\", CharSet = CharSet.Auto, SetLastError = true)]</p><p>The syntax of this line should look pretty strange to you because you could never use syntax like this when calling a constructor. If you examine the DllImportAttribute class in the documentation, you\u2019ll see that its constructor requires a single String parameter. In this example, \"Kernel32\" is being passed for this parameter. A constructor\u2019s parameters are called positional parameters and are mandatory; the parameter must be specified when the attribute is applied.</p><p>What are the other two \u201cparameters\u201d? This special syntax allows you to set any public fields or properties of the DllImportAttribute object after the object is constructed. In this example, when the DllImportAttribute object is constructed and \"Kernel32\" is passed to the constructor, the object\u2019s public instance fields, CharSet and SetLastError, are set to CharSet.Auto and true, respectively. The \u201cparameters\u201d that set fields or properties are called named parameters and are optional because the parameters don\u2019t have to be specified when you\u2019re applying an instance of the attribute. A little later on, I\u2019ll explain what causes an instance of the DllImportAttribute class to actually be constructed.</p><p>Also note that it\u2019s possible to apply multiple attributes to a single target. For example, in this</p><p>chapter\u2019s first program listing, the GetVersionEx method\u2019s ver parameter has both the In and Out attributes applied to it. When applying multiple attributes to a single target, be aware that the order</p><p>of attributes has no significance. Also, in C#, each attribute can be enclosed in square brackets, or multiple attributes can be comma-separated within a single set of square brackets. If the attribute class\u2019s constructor takes no parameters, the parentheses are optional. Finally, as mentioned earlier, the</p><p>Attribute suffix is also optional. The following lines behave identically and demonstrate all of the possible ways of applying multiple attributes.</p><p>[Serializable][Flags] [Serializable, Flags] [FlagsAttribute, SerializableAttribute] [FlagsAttribute()][Serializable()]</p><h6>Defining Your Own Attribute Class</h6><p>You know that an attribute is an instance of a class derived from System.Attribute, and you also know how to apply an attribute. Let\u2019s now look at how to define your own custom attribute classes. Say you\u2019re the Microsoft employee responsible for adding the bit flag support to enumerated types. To accomplish this, the first thing you have to do is define a FlagsAttribute class.</p><p>namespace System {</p><p>public class FlagsAttribute : System.Attribute { public FlagsAttribute() { }</p><p>} }</p><p>Notice that the FlagsAttribute class inherits from Attribute; this is what makes the FlagsAttribute class a CLS-compliant custom attribute. In addition, the class\u2019s name has a suffix of Attribute; this follows the standard convention but is not mandatory. Finally, all non-abstract attributes must contain at least one public constructor. The simple FlagsAttribute constructor takes no parameters and does absolutely nothing.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile131.png\"/><p>Important You should think of an attribute as a logical state container. That is, while an attribute type is a class, the class should be simple. The class should offer just one public constructor that accepts the attribute\u2019s mandatory (or positional) state information, and the class can offer public fields/properties that accept the attribute\u2019s optional (or named) state information. The class should not offer any public methods, events, or other members.</p><p>In general, I always discourage the use of public fields, and I still discourage them for attributes. It is much better to use properties because this allows more flexibility if you ever decide to change how the attribute class is implemented.</p><p>So far, instances of the FlagsAttribute class can be applied to any target, but this attribute</p><p>should really be applied to enumerated types only. It doesn\u2019t make sense to apply the attribute to a property or a method. To tell the compiler where this attribute can legally be applied, you apply</p><p>an instance of the System.AttributeUsageAttribute class to the attribute class. Here\u2019s the new code.</p><p>namespace System { [AttributeUsage(AttributeTargets.Enum, Inherited = false)] public class FlagsAttribute : System.Attribute {</p><p>public FlagsAttribute() { }</p><p>} }</p><p>In this new version, I\u2019ve applied an instance of AttributeUsageAttribute to the attribute. After all, the attribute type is just a class, and a class can have attributes applied to it. The AttributeUsage attribute is a simple class that allows you to specify to a compiler where your custom attribute can legally be applied. All compilers have built-in support for this attribute and generate errors when a user-defined custom attribute is applied to an invalid target. In this example, the AttributeUsage attribute specifies that instances of the Flags attribute can be applied only to enumerated type targets.</p><p>Because all attributes are just types, you can easily understand the AttributeUsageAttribute class. Here\u2019s what the FCL source code for the class looks like.</p><p>[Serializable] [AttributeUsage(AttributeTargets.Class, Inherited=true)] public sealed class AttributeUsageAttribute : Attribute {</p><p>internal static AttributeUsageAttribute Default = new AttributeUsageAttribute(AttributeTargets.All);</p><p>internal Boolean m_allowMultiple = false; internal AttributeTargets m_attributeTarget = AttributeTargets.All; internal Boolean m_inherited = true;</p><p>// This is the one public constructor public AttributeUsageAttribute(AttributeTargets validOn) {</p><p>m_attributeTarget = validOn; }</p><p>internal AttributeUsageAttribute(AttributeTargets validOn, Boolean allowMultiple, Boolean inherited) { m_attributeTarget = validOn; m_allowMultiple = allowMultiple; m_inherited = inherited;</p><p>}</p><p>public Boolean AllowMultiple { get { return m_allowMultiple; } set { m_allowMultiple = value; }</p><p>}</p><p>public Boolean Inherited { get { return m_inherited; } set { m_inherited = value; }</p><p>}</p><p>}</p><p>public AttributeTargets ValidOn {</p><p>get { return m_attributeTarget; } }</p><p>As you can see, the AttributeUsageAttribute class has a public constructor that allows you to pass bit flags that indicate where your attribute can legally be applied. The System.AttributeTargets enumerated type is defined in the FCL as follows.</p><p>[Flags, Serializable] public enum AttributeTargets {</p><p>Assembly = 0x0001, Module = 0x0002, Class = 0x0004, Struct = 0x0008, Enum = 0x0010, Constructor = 0x0020, Method = 0x0040, Property = 0x0080, Field = 0x0100, Event = 0x0200, Interface = 0x0400, Parameter = 0x0800, Delegate = 0x1000, ReturnValue = 0x2000, GenericParameter = 0x4000, All = Assembly | Module | Class | Struct | Enum |</p><p>Constructor | Method | Property | Field | Event | Interface | Parameter | Delegate | ReturnValue | GenericParameter</p><p>}</p><p>The AttributeUsageAttribute class offers two additional public properties that can optionally be set when the attribute is applied to an attribute class: AllowMultiple and Inherited.</p><p>For most attributes, it makes no sense to apply them to a single target more than once. For example, nothing is gained by applying the Flags or Serializable attributes more than once to a single target. In fact, if you tried to compile the following code, the compiler would report the following message: error CS0579: Duplicate 'Flags' attribute.</p><p>[Flags][Flags] internal enum Color {</p><p>Red }</p><p>For a few attributes, however, it does make sense to apply the attribute multiple times to a single target. In the FCL, the ConditionalAttribute attribute class allows multiple instances of itself to be applied to a single target. If you don\u2019t explicitly set AllowMultiple to true, your attribute can be applied no more than once to a selected target.</p><p>AttributeUsageAttribute\u2019s other property, Inherited, indicates if the attribute should be applied to derived classes and overriding methods when applied on the base class. The following code demonstrates what it means for an attribute to be inherited.</p><p>[AttributeUsage(AttributeTargets.Class | AttributeTargets.Method, Inherited=true)] internal class TastyAttribute : Attribute { }</p><p>[Tasty][Serializable] internal class BaseType {</p><p>[Tasty] protected virtual void DoSomething() { } }</p><p>internal class DerivedType : BaseType {</p><p>protected override void DoSomething() { } }</p><p>In this code, DerivedType and its DoSomething method are both considered Tasty because the TastyAttribute class is marked as inherited. However, DerivedType is not serializable because the FCL\u2019s SerializableAttribute class is marked as a noninherited attribute.</p><p>Be aware that the .NET Framework considers targets only of classes, methods, properties, events, fields, method return values, and parameters to be inheritable. So when you\u2019re defining an attribute type, you should set Inherited to true only if your targets include any of these targets. Note that inherited attributes do not cause additional metadata to be emitted for the derived types into the managed module. I\u2019ll say more about this a little later in the \u201cDetecting the Use of a Custom Attribute\u201d section.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile132.png\"/><p>Note If you define your own attribute class and forget to apply an AttributeUsage attribute to your class, the compiler and the CLR will assume that your attribute can be applied to all targets, can be applied only once to a single target, and is inherited. These assumptions mimic the default field values in the AttributeUsageAttribute class.</p><h6>Attribute Constructor and Field/Property Data Types</h6><p>When defining your own custom attribute class, you can define its constructor to take parameters that must be specified by developers when they apply an instance of your attribute type. In addition, you can define nonstatic public fields and properties in your type that identify settings that a developer can optionally choose for an instance of your attribute class.</p><p>When defining an attribute class\u2019s instance constructor, fields, and properties, you must restrict yourself to a small subset of data types. Specifically, the legal set of data types is limited to the following: Boolean, Char, Byte, SByte, Int16, UInt16, Int32, UInt32, Int64, UInt64, Single, Double, String, Type, Object, or an enumerated type. In addition, you can use a single-dimensional,</p><p>zero-based array of any of these types. However, you should avoid using arrays because a custom attribute class whose constructor takes an array is not CLS-compliant.</p><p>When applying an attribute, you must pass a compile-time constant expression that matches the type defined by the attribute class. Wherever the attribute class defines a Type parameter, Type field, or Type property, you must use C#\u2019s typeof operator, as shown in the following code. Wherever the attribute class defines an Object parameter, Object field, or Object property, you can pass an Int32, String, or any other constant expression (including null). If the constant expression represents a value type, the value type will be boxed at run time when an instance of the attribute is constructed.</p><p>Here\u2019s an example of an attribute and its usage.</p><p>using System;</p><p>internal enum Color { Red }</p><p>[AttributeUsage(AttributeTargets.All)] internal sealed class SomeAttribute : Attribute {</p><p>public SomeAttribute(String name, Object o, Type[] types) { // 'name' refers to a String // 'o' refers to one of the legal types (boxing if necessary) // 'types' refers to a 1\u00addimension, 0\u00adbased array of Types</p><p>} }</p><p>[Some(\"Jeff\", Color.Red, new Type[] { typeof(Math), typeof(Console) })] internal sealed class SomeType { }</p><p>Logically, when a compiler detects a custom attribute applied to a target, the compiler constructs an instance of the attribute class by calling its constructor, passing it any specified parameters. Then the compiler initializes any public fields and properties by using the values specified via the enhanced constructor syntax. Now that the custom attribute object is initialized, the compiler serializes the attribute object\u2019s state out to the target\u2019s metadata table entry.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile133.png\"/><p>Important I\u2019ve found this to be the best way for developers to think of custom attributes: instances of classes that have been serialized to a byte stream that resides in metadata. Later, at run time, an instance of the class can be constructed by deserializing the bytes contained in the metadata. In reality, what actually happens is that the compiler emits the information necessary to create an instance of the attribute class into metadata. Each constructor parameter is written out with a 1-byte type ID followed by the value. After \u201cserializing\u201d the constructor\u2019s parameters, the compiler emits each of the specified field and property values by writing out the field/property name followed by a 1-byte type ID and then the value. For arrays, the count of elements is saved first, followed by each individual element.</p><h6>Detecting the Use of a Custom Attribute</h6><p>Defining an attribute class is useless by itself. Sure, you could define attribute classes all you want and apply instances of them all you want, but this would just cause additional metadata to be written out to the assembly\u2014the behavior of your application code wouldn\u2019t change.</p><p>In Chapter 15, \u201cEnumerated Types and Bit Flags,\u201d you saw that applying the Flags attribute to an enumerated type altered the behavior of System.Enum\u2019s ToString and Format methods. The reason that these methods behave differently is that they check at run time if the enumerated type that they\u2019re operating on has the Flags attribute metadata associated with it. Code can look for the presence of attributes by using a technology called reflection. I\u2019ll give some brief demonstrations of reflection here, but I\u2019ll discuss it fully in Chapter 23, \u201cAssembly Loading and Reflection.\u201d</p><p>If you were the Microsoft employee responsible for implementing Enum\u2019s Format method, you would implement it like the following.</p><p>public override String ToString() {</p><p>// Does the enumerated type have an instance of // the FlagsAttribute type applied to it? if (this.GetType().IsDefined(typeof(FlagsAttribute), false)) {</p><p>// Yes; execute code treating value as a bit flag enumerated type.</p><p>... } else { // No; execute code treating value as a normal enumerated type.</p><p>... }</p><p>... }</p><p>This code calls Type\u2019s IsDefined method, effectively asking the system to look up the metadata for the enumerated type and see whether an instance of the FlagsAttribute class is associated with it. If IsDefined returns true, an instance of FlagsAttribute is associated with the enumerated type, and the Format method knows to treat the value as though it contained a set of bit flags. If IsDefined returns false, Format treats the value as a normal enumerated type.</p><p>So if you define your own attribute classes, you must also implement some code that checks for the existence of an instance of your attribute class (on some target) and then execute some alternate code path. This is what makes custom attributes so useful!</p><p>The FCL offers many ways to check for the existence of an attribute. If you\u2019re checking for the existence of an attribute via a System.Type object, you can use the IsDefined method as shown earlier. However, sometimes you want to check for an attribute on a target other than a type, such as an assembly, a module, or a method. For this discussion, let\u2019s concentrate on the extension methods defined by the System.Reflection.CustomAttributeExtensions class. This class defines three static methods for retrieving the attributes associated with a target: IsDefined, GetCustomAttributes, and GetCustomAttribute. Each of these functions has several overloaded versions. For example, each method has a version that works on type members (classes, structs, enums,</p><p>interfaces, delegates, constructors, methods, properties, fields, events, and return types), parameters, and assemblies. There are also versions that allow you to tell the system to walk up the derivation hierarchy to include inherited attributes in the results. Table 18-1 briefly describes what each method does.</p><figcaption>TABLE 18-1 System.Reflection.CustomAttributeExtensions\u2019 Methods That Reflect over Metadata Looking for Instances of CLS-Compliant Custom Attributes</figcaption><table border=\"1\">\n<tr>\n<td rowspan=\"2\"><p>Method</p>\n<p>IsDefined</p>\n<p>GetCustomAttributes</p>\n</td>\n<td><h6>Description</h6>\n<p>Returns true if there is at least one instance of the specified Attributederived class associated with the target. This method is efficient because it doesn\u2019t construct (deserialize) any instances of the attribute class.</p>\n</td>\n</tr>\n<tr>\n<td><p>Returns a collection of the specified attribute objects that have been applied to the target. Each instance is constructed (deserialized) by using the parameters, fields, and properties specified during compilation. If the target has no instances of the specified attribute class, an empty collection is returned. This method is typically used with attributes that have AllowMultiple set to true or to list all applied attributes.</p>\n</td>\n</tr>\n<tr>\n<td><p>GetCustomAttribute</p>\n</td>\n<td><p>Returns an instance of the specified attribute class that was applied to the target. The instance is constructed (deserialized) by using the parameters, fields, and properties specified during compilation. If the target has no instances of the specified attribute class, null is returned. If the target has multiple instances of the specified attribute applied to it, a System.Reflection. AmbiguousMatchException exception is thrown. This method is typically used with attributes that have AllowMultiple set to false.</p>\n</td>\n</tr>\n</table><p>If you just want to see if an attribute has been applied to a target, you should call IsDefined because it\u2019s more efficient than the other two methods. However, you know that when an attribute is applied to a target, you can specify parameters to the attribute\u2019s constructor and optionally set fields and properties. Using IsDefined won\u2019t construct an attribute object, call its constructor, or set its fields and properties.</p><p>If you want to construct an attribute object, you must call either GetCustomAttributes or GetCustomAttribute. Every time one of these methods is called, it constructs new instances of the specified attribute type and sets each of the instance\u2019s fields and properties based on the values specified in the source code. These methods return references to fully constructed instances of the applied attribute classes.</p><p>When you call any of these methods, internally, they must scan the managed module\u2019s metadata, performing string comparisons to locate the specified custom attribute class. Obviously, these operations take time. If you\u2019re performance conscious, you should consider caching the result of calling these methods rather than calling them repeatedly asking for the same information.</p><p>The System.Reflection namespace defines several classes that allow you to examine the contents of a module\u2019s metadata: Assembly, Module, ParameterInfo, MemberInfo, Type, MethodInfo, ConstructorInfo, FieldInfo, EventInfo, PropertyInfo, and their respective *Builder classes. All of these classes also offer IsDefined and GetCustomAttributes methods.</p><p>The version of GetCustomAttributes defined by the reflection classes returns an array of Object instances (Object[]) instead of an array of Attribute instances (Attribute[]). This is because the reflection classes are able to return objects of non\u2013CLS-compliant attribute classes. You</p><p>shouldn\u2019t be concerned about this inconsistency because non\u2013CLS-compliant attributes are incredibly rare. In fact, in all of the time I\u2019ve been working with the .NET Framework, I\u2019ve never even seen one.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile134.png\"/><p>Note Be aware that only Attribute, Type, and MethodInfo classes implement reflection methods that honor the Boolean inherit parameter. All other reflection methods that look up attributes ignore the inherit parameter and do not check the inheritance hierarchy. If you need to check the presence of an inherited attribute for events, properties, fields, constructors, or parameters, you must call one of Attribute\u2019s methods.</p><p>There\u2019s one more thing you should be aware of: When you pass a class to IsDefined, GetCustomAttribute, or GetCustomAttributes, these methods search for the application of the attribute class you specify or any attribute class derived from the specified class. If your code is looking for a specific attribute class, you should perform an additional check on the returned value to ensure that what these methods returned is the exact class you\u2019re looking for. You might also want to consider defining your attribute class to be sealed to reduce potential confusion and eliminate this extra check.</p><p>Here\u2019s some sample code that lists all of the methods defined within a type and displays the attributes applied to each method. The code is for demonstration purposes; normally, you wouldn\u2019t apply these particular custom attributes to these targets as I\u2019ve done here.</p><p>using System; using System.Diagnostics; using System.Reflection;</p><p>[assembly: CLSCompliant(true)]</p><p>[Serializable] [DefaultMemberAttribute(\"Main\")] [DebuggerDisplayAttribute(\"Richter\", Name = \"Jeff\", Target = typeof(Program))] public sealed class Program {</p><p>[Conditional(\"Debug\")] [Conditional(\"Release\")] public void DoSomething() { }</p><p>public Program() { }</p><p>[CLSCompliant(true)] [STAThread] public static void Main() {</p><p>// Show the set of attributes applied to this type ShowAttributes(typeof(Program));</p><p>// Get the set of methods associated with the type var members =</p><p>from m in typeof(Program).GetTypeInfo().DeclaredMembers.OfType&lt;MethodBase&gt;() where m.IsPublic select m;</p><p>foreach (MemberInfo member in members) { // Show the set of attributes applied to this member ShowAttributes(member);</p><p>} }</p><p>private static void ShowAttributes(MemberInfo attributeTarget) { var attributes = attributeTarget.GetCustomAttributes&lt;Attribute&gt;();</p><p>Console.WriteLine(\"Attributes applied to {0}: {1}\", attributeTarget.Name, (attributes.Count() == 0 ? \"None\" : String.Empty));</p><p>foreach (Attribute attribute in attributes) { // Display the type of each applied attribute Console.WriteLine(\" {0}\", attribute.GetType().ToString());</p><p>if (attribute is DefaultMemberAttribute) Console.WriteLine(\" MemberName={0}\", ((DefaultMemberAttribute) attribute).MemberName);</p><p>if (attribute is ConditionalAttribute) Console.WriteLine(\" ConditionString={0}\", ((ConditionalAttribute) attribute).ConditionString);</p><p>if (attribute is CLSCompliantAttribute) Console.WriteLine(\" IsCompliant={0}\", ((CLSCompliantAttribute) attribute).IsCompliant);</p><p>} }</p><p>DebuggerDisplayAttribute dda = attribute as DebuggerDisplayAttribute; if (dda != null) {</p><p>Console.WriteLine(\" Value={0}, Name={1}, Target={2}\",</p><p>dda.Value, dda.Name, dda.Target); }</p><p>} Console.WriteLine();</p><p>Building and running this application yields the following output.</p><p>Attributes applied to Program: System.SerializableAttribute System.Diagnostics.DebuggerDisplayAttribute</p><p>Value=Richter, Name=Jeff, Target=Program System.Reflection.DefaultMemberAttribute</p><p>MemberName=Main</p><p>Attributes applied to DoSomething: System.Diagnostics.ConditionalAttribute ConditionString=Release System.Diagnostics.ConditionalAttribute ConditionString=Debug</p><p>Attributes applied to Main: System.CLSCompliantAttribute IsCompliant=True System.STAThreadAttribute</p><p>Attributes applied to .ctor: None</p><h6>Matching Two Attribute Instances Against Each Other</h6><p>Now that your code knows how to check whether an instance of an attribute is applied to a target, it might want to check the fields of the attribute to see what values they have. One way to do this is to write code that explicitly checks the values of the attribute class\u2019s fields. However, System.Attribute overrides Object\u2019s Equals method, and internally, this method compares the types of the two objects. If they are not identical, Equals returns false. If the types are identical, then Equals uses reflection to compare the values of the two attribute objects\u2019 fields (by calling Equals for each field). If all the fields match, then true is returned; otherwise, false is returned. You might override Equals in your own attribute class to remove the use of reflection, improving performance.</p><p>System.Attribute also exposes a virtual Match method that you can override to provide richer</p><p>semantics. The default implementation of Match simply calls Equals and returns its result. The following code demonstrates how to override Equals and Match (which returns true if one attribute represents a subset of the other) and then shows how Match is used.</p><p>using System;</p><p>[Flags] internal enum Accounts {</p><p>Savings = 0x0001, Checking = 0x0002, Brokerage = 0x0004</p><p>}</p><p>[AttributeUsage(AttributeTargets.Class)] internal sealed class AccountsAttribute : Attribute {</p><p>private Accounts m_accounts;</p><p>public AccountsAttribute(Accounts accounts) {</p><p>m_accounts = accounts; }</p><p>public override Boolean Match(Object obj) {</p><p>// If the base class implements Match and the base class // is not Attribute, then uncomment the following line. // if (!base.Match(obj)) return false;</p><p>// Since 'this' isn't null, if obj is null, // then the objects can't match // NOTE: This line may be deleted if you trust // that the base type implemented Match correctly. if (obj == null) return false;</p><p>// If the objects are of different types, they can't match // NOTE: This line may be deleted if you trust // that the base type implemented Match correctly. if (this.GetType() != obj.GetType()) return false;</p><p>// Cast obj to our type to access fields. NOTE: This cast // can't fail since we know objects are of the same type AccountsAttribute other = (AccountsAttribute) obj;</p><p>// Compare the fields as you see fit // This example checks if 'this' accounts is a subset // of others' accounts if ((other.m_accounts &amp; m_accounts) != m_accounts)</p><p>return false;</p><p>return true; // Objects match }</p><p>public override Boolean Equals(Object obj) { // If the base class implements Equals, and the base class // is not Object, then uncomment the following line. // if (!base.Equals(obj)) return false;</p><p>// Since 'this' isn't null, if obj is null, // then the objects can't be equal // NOTE: This line may be deleted if you trust // that the base type implemented Equals correctly. if (obj == null) return false;</p><p>// If the objects are of different types, they can't be equal // NOTE: This line may be deleted if you trust // that the base type implemented Equals correctly. if (this.GetType() != obj.GetType()) return false;</p><p>// Cast obj to our type to access fields. NOTE: This cast // can't fail since we know objects are of the same type AccountsAttribute other = (AccountsAttribute) obj;</p><p>// Compare the fields to see if they have the same value // This example checks if 'this' accounts is the same // as other's accounts if (other.m_accounts != m_accounts)</p><p>return false;</p><p>return true; // Objects are equal }</p><p>}</p><p>// Override GetHashCode since we override Equals public override Int32 GetHashCode() {</p><p>return (Int32) m_accounts; }</p><p>[Accounts(Accounts.Savings)] internal sealed class ChildAccount { }</p><p>[Accounts(Accounts.Savings | Accounts.Checking | Accounts.Brokerage)] internal sealed class AdultAccount { }</p><p>public sealed class Program { public static void Main() {</p><p>CanWriteCheck(new ChildAccount()); CanWriteCheck(new AdultAccount());</p><p>}</p><p>// This just demonstrates that the method works correctly on a // type that doesn't have the AccountsAttribute applied to it. CanWriteCheck(new Program());</p><p>private static void CanWriteCheck(Object obj) { // Construct an instance of the attribute type and initialize it // to what we are explicitly looking for. Attribute checking = new AccountsAttribute(Accounts.Checking);</p><p>// Construct the attribute instance that was applied to the type Attribute validAccounts =</p><p>obj.GetType().GetCustomAttribute&lt;AccountsAttribute&gt;(false);</p><p>} }</p><p>// If the attribute was applied to the type AND the // attribute specifies the \"Checking\" account, then the // type can write a check if ((validAccounts != null) &amp;&amp; checking.Match(validAccounts)) {</p><p>Console.WriteLine(\"{0} types can write checks.\", obj.GetType()); } else {</p><p>Console.WriteLine(\"{0} types can NOT write checks.\", obj.GetType()); }</p><p>Building and running this application yields the following output.</p><p>ChildAccount types can NOT write checks. AdultAccount types can write checks. Program types can NOT write checks.</p><h6>Detecting the Use of a Custom Attribute Without Creating Attribute-Derived Objects</h6><p>In this section, I discuss an alternate technique for detecting custom attributes applied to a metadata entry. In some security-conscious scenarios, this alternate technique ensures that no code in an Attribute-derived class will execute. After all, when you call Attribute\u2019s GetCustomAttribute(s) methods, internally, these methods call the attribute class\u2019s constructor and can also call property set accessor methods. In addition, the first access to a type causes the CLR to invoke the type\u2019s type constructor (if it exists). The constructor, set accessor, and type constructor methods could contain code that will execute whenever code is just looking for an attribute. This allows unknown code to run in the AppDomain, and this is a potential security vulnerability.</p><p>To discover attributes without allowing attribute class code to execute, you use the System. Reflection.CustomAttributeData class. This class defines one static method for retrieving the attributes associated with a target: GetCustomAttributes. This method has four overloads: one that takes an Assembly, one that takes a Module, one that takes a ParameterInfo, and one that takes a MemberInfo. This class is defined in the System.Reflection namespace, which is discussed in Chapter 23. Typically, you\u2019ll use the CustomAttributeData class to analyze attributes in metadata for an assembly that is loaded via Assembly\u2019s static ReflectionOnlyLoad method (also discussed in Chapter 23). Briefly, ReflectionOnlyLoad loads an assembly in such a way that prevents the CLR from executing any code in it; this includes type constructors.</p><p>CustomAttributeData\u2019s GetCustomAttributes method acts as a factory. That is, when you call it, it returns a collection of CustomAttributeData objects in an object of type IList&lt;CustomAttributeData&gt;. The collection contains one element per custom attribute applied to the specified target. For each CustomAttributeData object, you can query some read-only properties to determine how the attribute object would be constructed and initialized. Specifically, the Constructor property indicates which constructor method would be called, the ConstructorArguments property returns the arguments that would be passed to this constructor as an instance of IList&lt;CustomAttributeTypedArgument&gt;, and the NamedArguments property returns the fields/properties that would be set as an instance of IList&lt;CustomAttributeNamedArgument&gt;. Notice that I say \u201cwould be\u201d in the previous sentences because the constructor and set accessor methods will not actually be called\u2014we get the added security by preventing any attribute class methods from executing.</p><p>Here\u2019s a modified version of a previous code sample that uses the CustomAttributeData class to securely obtain the attributes applied to various targets.</p><p>using System; using System.Diagnostics; using System.Reflection; using System.Collections.Generic;</p><p>[assembly: CLSCompliant(true)]</p><p>[Serializable]</p><p>[DefaultMemberAttribute(\"Main\")] [DebuggerDisplayAttribute(\"Richter\", Name=\"Jeff\", Target=typeof(Program))] public sealed class Program {</p><p>[Conditional(\"Debug\")] [Conditional(\"Release\")] public void DoSomething() { }</p><p>public Program() { }</p><p>[CLSCompliant(true)] [STAThread] public static void Main() {</p><p>// Show the set of attributes applied to this type ShowAttributes(typeof(Program));</p><p>// Get the set of methods associated with the type MemberInfo[] members = typeof(Program).FindMembers( MemberTypes.Constructor | MemberTypes.Method, BindingFlags.DeclaredOnly | BindingFlags.Instance | BindingFlags.Public | BindingFlags.Static, Type.FilterName, \"*\");</p><p>foreach (MemberInfo member in members) { // Show the set of attributes applied to this member ShowAttributes(member);</p><p>} }</p><p>private static void ShowAttributes(MemberInfo attributeTarget) { IList&lt;CustomAttributeData&gt; attributes = CustomAttributeData.GetCustomAttributes(attributeTarget);</p><p>Console.WriteLine(\"Attributes applied to {0}: {1}\", attributeTarget.Name, (attributes.Count == 0 ? \"None\" : String.Empty));</p><p>foreach (CustomAttributeData attribute in attributes) { // Display the type of each applied attribute Type t = attribute.Constructor.DeclaringType; Console.WriteLine(\" {0}\", t.ToString()); Console.WriteLine(\" Constructor called={0}\", attribute.Constructor);</p><p>IList&lt;CustomAttributeTypedArgument&gt; posArgs = attribute.ConstructorArguments; Console.WriteLine(\" Positional arguments passed to constructor:\" +</p><p>((posArgs.Count == 0) ? \" None\" : String.Empty)); foreach (CustomAttributeTypedArgument pa in posArgs) {</p><p>Console.WriteLine(\" Type={0}, Value={1}\", pa.ArgumentType, pa.Value); }</p><p>IList&lt;CustomAttributeNamedArgument&gt; namedArgs = attribute.NamedArguments; Console.WriteLine(\" Named arguments set after construction:\" +</p><p>((namedArgs.Count == 0) ? \" None\" : String.Empty)); foreach(CustomAttributeNamedArgument na in namedArgs) { Console.WriteLine(\" Name={0}, Type={1}, Value={2}\",</p><p>na.MemberInfo.Name, na.TypedValue.ArgumentType, na.TypedValue.Value); }</p><p>} }</p><p>Console.WriteLine();</p><p>} Console.WriteLine();</p><p>Building and running this application yields the following output.</p><p>Attributes applied to Program:</p><p>System.SerializableAttribute Constructor called=Void .ctor() Positional arguments passed to constructor: None Named arguments set after construction: None</p><p>System.Diagnostics.DebuggerDisplayAttribute Constructor called=Void .ctor(System.String) Positional arguments passed to constructor:</p><p>Type=System.String, Value=Richter</p><p>Named arguments set after construction: Name=Name, Type=System.String, Value=Jeff Name=Target, Type=System.Type, Value=Program</p><p>System.Reflection.DefaultMemberAttribute Constructor called=Void .ctor(System.String) Positional arguments passed to constructor:</p><p>Type=System.String, Value=Main Named arguments set after construction: None</p><p>Attributes applied to DoSomething:</p><p>System.Diagnostics.ConditionalAttribute Constructor called=Void .ctor(System.String) Positional arguments passed to constructor:</p><p>Type=System.String, Value=Release Named arguments set after construction: None</p><p>System.Diagnostics.ConditionalAttribute Constructor called=Void .ctor(System.String) Positional arguments passed to constructor:</p><p>Type=System.String, Value=Debug Named arguments set after construction: None</p><p>Attributes applied to Main:</p><p>System.CLSCompliantAttribute Constructor called=Void .ctor(Boolean) Positional arguments passed to constructor:</p><p>Type=System.Boolean, Value=True Named arguments set after construction: None</p><p>System.STAThreadAttribute Constructor called=Void .ctor() Positional arguments passed to constructor: None Named arguments set after construction: None</p><p>Attributes applied to .ctor: None</p><h6>Conditional Attribute Classes</h6><p>Over time, the ease of defining, applying, and reflecting over attributes has caused developers to use them more and more. Using attributes is also a very easy way to annotate your code while simultaneously implementing rich features. Lately, developers have been using attributes to assist them with design time and debugging. For example, the Microsoft Visual Studio code analysis tool (FxCopCmd.exe) offers a System.Diagnostics.CodeAnalysis.SuppressMessageAttribute that you can apply to types and members in order to suppress the reporting of a specific static analysis tool rule violation. This attribute is only looked for by the code analysis utility; the attribute is never looked for when the program is running normally. When not using code analysis, having SuppressMessage attributes sitting in the metadata just bloats the metadata, which makes your file bigger, increases your process\u2019s working set, and hurts your application\u2019s performance. It would be great if there were an easy way to have the compiler emit the SuppressMessage attributes only when you intend to use the code analysis tool. Fortunately, there is a way to do this by using conditional attribute classes.</p><p>An attribute class that has the System.Diagnostics.ConditionalAttribute applied to it is called a conditional attribute class. Here is an example.</p><p>//#define TEST #define VERIFY</p><p>using System; using System.Diagnostics;</p><p>[Conditional(\"TEST\")][Conditional(\"VERIFY\")] public sealed class CondAttribute : Attribute { }</p><p>[Cond] public sealed class Program {</p><p>public static void Main() { Console.WriteLine(\"CondAttribute is {0}applied to Program type.\", Attribute.IsDefined(typeof(Program),</p><p>typeof(CondAttribute)) ? \"\" : \"not \"); }</p><p>}</p><p>When a compiler sees an instance of the CondAttribute being applied to a target, the compiler will emit the attribute information into the metadata only if the TEST or VERIFY symbol is defined when the code containing the target is compiled. However, the attribute class definition metadata and implementation is still present in the assembly.</p><h6>C H A P T E R 1 9</h6><h2>Nullable Value Types</h2><h6>In this chapter:</h6><p>C#\u2019s Support for Nullable Value Types . . . . . . . . . . . . . . . . . . . . . 443 C#\u2019s Null-Coalescing Operator . . . . . . . . . . . . . . . . . . . . . . . . . . . . 446 The CLR Has Special Support for Nullable Value Types. . . . . . . 447</p><p>As you know, a variable of a value type can never be null; it always contains the value type\u2019s value itself. In fact, this is why they call these types value types. Unfortunately, there are some scenarios in which this is a problem. For example, when designing a database, it\u2019s possible to define a column\u2019s data type to be a 32-bit integer that would map to the Int32 data type of the Framework Class Library (FCL). But a column in a database can indicate that the value is nullable. That is, it is OK to have no value in the row\u2019s column. Working with database data by using the Microsoft .NET Framework can be quite difficult because in the common language runtime (CLR), there is no way to represent an Int32 value as null.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile135.png\"/><p>Note Microsoft ADO.NET\u2019s table adapters do support nullable types. But unfortunately, the types in the System.Data.SqlTypes namespace are not replaced by nullable types, partially because there isn\u2019t a one-to-one correspondence between types. For example, the SqlDecimal type has a maximum of 38 digits, whereas the regular Decimal type can reach only 29. In addition, the SqlString type supports its own locale and compare options, which are not supported by the normal String type.</p><p>Here is another example. In Java, the java.util.Date class is a reference type, and therefore, a variable of this type can be set to null. However, in the CLR, a System.DateTime is a value type, and a DateTime variable can never be null. If an application written in Java wants to communicate a date/time to a web service running the CLR, there is a problem if the Java application sends null because the CLR has no way to represent this and operate on it.</p><p>To improve this situation, Microsoft added the concept of nullable value types to the CLR. To understand how they work, we first need to look at the System.Nullable&lt;T&gt; structure, which is defined in the FCL.</p><p>441</p><p>Here is the logical representation of how the System.Nullable&lt;T&gt; type is defined.</p><p>[Serializable, StructLayout(LayoutKind.Sequential)] public struct Nullable&lt;T&gt; where T : struct {</p><p>// These 2 fields represent the state private Boolean hasValue = false; // Assume null internal T value = default(T); // Assume all bits zero</p><p>public Nullable(T value) { this.value = value; this.hasValue = true;</p><p>}</p><p>public Boolean HasValue { get { return hasValue; } }</p><p>public T Value { get { if (!hasValue) { throw new InvalidOperationException( \"Nullable object must have a value.\");</p><p>} return value;</p><p>} }</p><p>public T GetValueOrDefault() { return value; }</p><p>public T GetValueOrDefault(T defaultValue) { if (!HasValue) return defaultValue; return value;</p><p>}</p><p>public override Boolean Equals(Object other) { if (!HasValue) return (other == null); if (other == null) return false; return value.Equals(other);</p><p>}</p><p>public override int GetHashCode() { if (!HasValue) return 0; return value.GetHashCode();</p><p>}</p><p>public override string ToString() { if (!HasValue) return \"\"; return value.ToString();</p><p>}</p><p>public static implicit operator Nullable&lt;T&gt;(T value) {</p><p>return new Nullable&lt;T&gt;(value); }</p><p>}</p><p>public static explicit operator T(Nullable&lt;T&gt; value) {</p><p>return value.Value; }</p><p>As you can see, this class encapsulates the notion of a value type that can also be null. Because Nullable&lt;T&gt; is itself a value type, instances of it are still fairly lightweight. That is, instances can still be on the stack, and an instance is the same size as the original value type plus the size of a Boolean field. Notice that Nullable\u2019s type parameter, T, is constrained to struct. This was done because reference type variables can already be null.</p><p>So now, if you want to use a nullable Int32 in your code, you can write something like this.</p><ul style=\"list-style-type: none;\">\n<li><p>Nullable&lt;Int32&gt; x = 5;</p></li>\n<li><p>Nullable&lt;Int32&gt; y = null; Console.WriteLine(\"x: HasValue={0}, Value={1}\", x.HasValue, x.Value); Console.WriteLine(\"y: HasValue={0}, Value={1}\", y.HasValue, y.GetValueOrDefault());</p></li>\n</ul><p>When I compile and run this code, I get the following output.</p><p>x: HasValue=True, Value=5 y: HasValue=False, Value=0</p><h6>C#\u2019s Support for Nullable Value Types</h6><p>Notice in the code that C# allows you to use fairly simple syntax to initialize the two Nullable&lt;Int32&gt; variables, x and y. In fact, the C# team wants to integrate nullable value types into the C# language, making them first-class citizens. To that end, C# offers a cleaner syntax for working with nullable value types. C# allows the code to declare and initialize the x and y variables to be written using question mark notation.</p><ul style=\"list-style-type: none;\">\n<li><p>Int32? x = 5;</p></li>\n<li><p>Int32? y = null;</p></li>\n</ul><p>In C#, Int32? is a synonym notation for Nullable&lt;Int32&gt;. But C# takes this further. C# allows you to perform conversions and casts on nullable instances. And C# also supports applying operators to nullable instances. The following code shows examples of these.</p><p>private static void ConversionsAndCasting() { // Implicit conversion from non\u00adnullable Int32 to Nullable&lt;Int32&gt;</p><ul style=\"list-style-type: none;\">\n<li><p>Int32? a = 5;</p><p>// Implicit conversion from 'null' to Nullable&lt;Int32&gt;</p>\n</li>\n<li><p>Int32? b = null; // Same as \"Int32? b = new Int32?();\" which sets HasValue to false</p></li>\n</ul><p>// Explicit conversion from Nullable&lt;Int32&gt; to non\u00adnullable Int32 Int32 c = (Int32) a;</p><p>}</p><p>// Casting between nullable primitive types Double? d = 5; // Int32\u00ad&gt;Double? (d is 5.0 as a double) Double? e = b; // Int32?\u00ad&gt;Double? (e is null)</p><p>C# also allows you to apply operators to nullable instances. The following code shows examples of this.</p><p>private static void Operators() {</p><ul style=\"list-style-type: none;\">\n<li><p>Int32? a = 5;</p></li>\n<li><p>Int32? b = null;</p></li>\n</ul><p>// Unary operators (+ ++ \u00ad \u00ad\u00ad ! ~) a++; // a = 6 b = \u00adb; // b = null</p><p>// Binary operators (+ \u00ad * / % &amp; | ^ &lt;&lt; &gt;&gt;) a = a + 3; // a = 9 b = b * 3; // b = null;</p><p>// Equality operators (== !=) if (a == null) { /* no */ } else { /* yes */ } if (b == null) { /* yes */ } else { /* no */ } if (a != b) { /* yes */ } else { /* no */ }</p><p>}</p><p>// Comparison operators (&lt;&gt; &lt;= &gt;=) if (a &lt; b) { /* no */ } else { /* yes */ }</p><p>Here is how C# interprets the operators:</p><p>\u25a0\u25a0 Unary operators (+, ++, -, --, ! , ~) If the operand is null, the result is null.</p><p>\u25a0\u25a0 Binary operators (+, -, *, /, %, &amp;, |, ^, &lt;&lt;, &gt;&gt;) If either operand is null, the result is null. However, an exception is made when the &amp; and | operators are operating on Boolean? operands, so that the behavior of these two operators gives the same behavior as demonstrated by SQL\u2019s three-valued logic. For these two operators, if neither operand is null, the operator performs as expected, and if both operands are null, the result is null. The special behavior comes into play when just one of the operands is null. The following table lists the results produced by these two operators for all combinations of true, false, and null.</p><table border=\"1\">\n<tr>\n<td><p>Operand1 \u2192Operand2 \u2193</p>\n<p>True</p>\n</td>\n<td><h6>true</h6>\n<p>&amp; = true | = true</p>\n</td>\n<td><h6>false</h6>\n<p>&amp; = false | = true</p>\n</td>\n<td><h6>null</h6>\n<p>&amp; = null | = true</p>\n</td>\n</tr>\n<tr>\n<td><p>False</p>\n</td>\n<td><p>&amp; = false | = true</p>\n</td>\n<td><p>&amp; = false | = false</p>\n</td>\n<td><p>&amp; = false | = null</p>\n</td>\n</tr>\n<tr>\n<td><p>Null</p>\n</td>\n<td><p>&amp; = null | = true</p>\n</td>\n<td><p>&amp; = false | = null</p>\n</td>\n<td><p>&amp; = null | = null</p>\n</td>\n</tr>\n</table><p>\u25a0\u25a0 Equality operators (==, !=) If both operands are null, they are equal. If one operand is null, they are not equal. If neither operand is null, compare the values to determine if they are equal.</p><p>\u25a0\u25a0 Relational operators (&lt;, &gt;, &lt;=, &gt;=) If either operand is null, the result is false. If neither</p><p>operand is null, compare the values.</p><p>You should be aware that manipulating nullable instances does generate a lot of code. For example, see the following method.</p><p>private static Int32? NullableCodeSize(Int32? a, Int32? b) {</p><p>return a + b; }</p><p>When I compile this method, there is quite a bit of resulting Intermediate Language (IL) code, which also makes performing operations on nullable types slower than performing the same operation on non-nullable types. Here is the C# equivalent of the compiler-produced IL code.</p><p>private static Nullable&lt;Int32&gt; NullableCodeSize(Nullable&lt;Int32&gt; a, Nullable&lt;Int32&gt; b) {</p><p>}</p><p>Nullable&lt;Int32&gt; nullable1 = a; Nullable&lt;Int32&gt; nullable2 = b; if (!(nullable1.HasValue &amp; nullable2.HasValue)) {</p><p>return new Nullable&lt;Int32&gt;();</p><p>} return new Nullable&lt;Int32&gt;(nullable1.GetValueOrDefault() + nullable2.GetValueOrDefault());</p><p>Finally, let me point out that you can define your own value types that overload the various operators previously mentioned. I discuss how to do this in the \u201cOperator Overload Methods\u201d section in Chapter 8, \u201cMethods.\u201d If you then use a nullable instance of your own value type, the compiler does the right thing and invokes your overloaded operator. For example, suppose that you have a Point value type that defines overloads for the == and != operators as follows.</p><p>using System;</p><p>internal struct Point { private Int32 m_x, m_y; public Point(Int32 x, Int32 y) { m_x = x; m_y = y; }</p><p>public static Boolean operator==(Point p1, Point p2) { return (p1.m_x == p2.m_x) &amp;&amp; (p1.m_y == p2.m_y);</p><p>}</p><p>}</p><p>public static Boolean operator!=(Point p1, Point p2) {</p><p>return !(p1 == p2); }</p><p>At this point, you can use nullable instances of the Point type and the compiler will invoke your overloaded operators.</p><p>internal static class Program { public static void Main() {</p><p>Point? p1 = new Point(1, 1); Point? p2 = new Point(2, 2);</p><p>} }</p><p>Console.WriteLine(\"Are points equal? \" + (p1 == p2).ToString()); Console.WriteLine(\"Are points not equal? \" + (p1 != p2).ToString());</p><p>When I build and run the preceding code, I get the following output.</p><p>Are points equal? False Are points not equal? True</p><h6>C#\u2019s Null-Coalescing Operator</h6><p>C# has an operator called the null-coalescing operator (??), which takes two operands. If the operand on the left is not null, the operand\u2019s value is returned. If the operand on the left is null, the value of the right operand is returned. The null-coalescing operator offers a very convenient way to set a variable\u2019s default value.</p><p>A cool feature of the null-coalescing operator is that it can be used with reference types as well as nullable value types. Here is some code that demonstrates the use of the null-coalescing operator.</p><p>private static void NullCoalescingOperator() { Int32? b = null;</p><p>// The following line is equivalent to: // x = (b.HasValue) ? b.Value : 123 Int32 x = b ?? 123; Console.WriteLine(x); // \"123\"</p><p>}</p><p>// The following line is equivalent to: // String temp = GetFilename(); // filename = (temp != null) ? temp : \"Untitled\"; String filename = GetFilename() ?? \"Untitled\";</p><p>Some people argue that the null-coalescing operator is simply syntactic sugar for the ?: operator, and that the C# compiler team should not have added this operator to the language. However, the null-coalescing operator offers two significant syntactic improvements. The first is that the ?? operator works better with expressions.</p><p>Func&lt;String&gt; f = () =&gt; SomeMethod() ?? \"Untitled\";</p><p>This code is much easier to read and understand than the following line, which requires variable assignments and multiple statements.</p><p>Func&lt;String&gt; f = () =&gt; { var temp = SomeMethod(); return temp != null ? temp : \"Untitled\";};</p><p>The second improvement is that ?? works better in composition scenarios. For example, the following single line:</p><p>String s = SomeMethod1() ?? SomeMethod2() ?? \"Untitled\";</p><p>is far easier to read and understand than this chunk of code.</p><p>String s; var sm1 = SomeMethod1(); if (sm1 != null) s = sm1; else {</p><p>var sm2 = SomeMethod2(); if (sm2 != null) s = sm2; else s = \"Untitled\";</p><p>}</p><h6>The CLR Has Special Support for Nullable Value Types</h6><p>The CLR has built-in support for nullable value types. This special support is provided for boxing, unboxing, calling GetType, calling interface methods, and it is given to nullable types to make them fit more seamlessly into the CLR. This also makes them behave more naturally and as most developers would expect. Let\u2019s take a closer look at the CLR\u2019s special support for nullable types.</p><h6>Boxing Nullable Value Types</h6><p>Imagine a Nullable&lt;Int32&gt; variable that is logically set to null. If this variable is passed to a method prototyped as expecting an Object, the variable must be boxed, and a reference to the boxed Nullable&lt;Int32&gt; is passed to the method. This is not ideal because the method is now being passed a non-null value even though the Nullable&lt;Int32&gt; variable logically contained the value of null. To fix this, the CLR executes some special code when boxing a nullable variable to keep up the illusion that nullable types are first-class citizens in the environment.</p><p>Specifically, when the CLR is boxing a Nullable&lt;T&gt; instance, it checks to see if it is null, and if so, the CLR doesn\u2019t actually box anything, and null is returned. If the nullable instance is not null, the CLR takes the value out of the nullable instance and boxes it. In other words, a Nullable&lt;Int32&gt; with a value of 5 is boxed into a boxed-Int32 with a value of 5. Here is some code that demonstrates this behavior.</p><p>// Boxing Nullable&lt;T&gt; is null or boxed T Int32? n = null; Object o = n; // o is null Console.WriteLine(\"o is null={0}\", o == null); // \"True\"</p><p>n = 5; o = n; // o refers to a boxed Int32 Console.WriteLine(\"o's type={0}\", o.GetType()); // \"System.Int32\"</p><h6>Unboxing Nullable Value Types</h6><p>The CLR allows a boxed value type T to be unboxed into a T or a Nullable&lt;T&gt;. If the reference to the boxed value type is null, and you are unboxing it to a Nullable&lt;T&gt;, the CLR sets Nullable&lt;T&gt;\u2019s value to null. Here is some code to demonstrate this behavior.</p><p>// Create a boxed Int32 Object o = 5;</p><p>// Unbox it into a Nullable&lt;Int32&gt; and into an Int32 Int32? a = (Int32?) o; // a = 5 Int32 b = (Int32) o; // b = 5</p><p>// Create a reference initialized to null o = null;</p><p>// \"Unbox\" it into a Nullable&lt;Int32&gt; and into an Int32 a = (Int32?) o; // a = null b = (Int32) o; // NullReferenceException</p><h6>Calling GetType via a Nullable Value Type</h6><p>When calling GetType on a Nullable&lt;T&gt; object, the CLR actually lies and returns the type T instead of the type Nullable&lt;T&gt;. Here is some code that demonstrates this behavior.</p><p>Int32? x = 5;</p><p>// The following line displays \"System.Int32\"; not \"System.Nullable&lt;Int32&gt;\" Console.WriteLine(x.GetType());</p><h6>Calling Interface Methods via a Nullable Value Type</h6><p>In the following code, I\u2019m casting n, a Nullable&lt;Int32&gt;, to IComparable&lt;Int32&gt;, an interface type. However, the Nullable&lt;T&gt; type does not implement the IComparable&lt;Int32&gt; interface as Int32 does. The C# compiler allows this code to compile anyway, and the CLR\u2019s verifier considers this code verifiable to allow you a more convenient syntax.</p><p>Int32? n = 5; Int32 result = ((IComparable) n).CompareTo(5); // Compiles &amp; runs OK Console.WriteLine(result); // 0</p><p>If the CLR didn\u2019t provide this special support, it would be more cumbersome for you to write code to call an interface method on a nullable value type. You\u2019d have to cast the unboxed value type first before casting to the interface to make the call.</p><p>Int32 result = ((IComparable) (Int32) n).CompareTo(5); // Cumbersome</p><h6>PART IV</h6>"}, {"id": "lesson-3", "title": "Core Facilities", "readTime": "5 min read", "subtitle": "", "content": "<p>CHAPTER 20 Exceptions and State Management . . . . . . . . . . . .451 CHAPTER 21 The Managed Heap and Garbage Collection . . . .505 CHAPTER 22 CLR Hosting and AppDomains . . . . . . . . . . . . . . . .553 CHAPTER 23 Assembly Loading and Reflection. . . . . . . . . . . . . .583 CHAPTER 24 Runtime Serialization. . . . . . . . . . . . . . . . . . . . . . . . .611 CHAPTER 25 Interoperating with WinRT Components. . . . . . . .643</p><h6>C H A P T E R 2 0</h6><h2>Exceptions and State Management</h2><h6>In this chapter:</h6><p>Defining \u201cException\u201d . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 452 Exception-Handling Mechanics . . . . . . . . . . . . . . . . . . . . . . . . . . . 453 The System.Exception Class . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 460 FCL-Defined Exception Classes . . . . . . . . . . . . . . . . . . . . . . . . . . . . 463 Throwing an Exception . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 466 Defining Your Own Exception Class. . . . . . . . . . . . . . . . . . . . . . . . 467 Trading Reliability for Productivity . . . . . . . . . . . . . . . . . . . . . . . . 469 Guidelines and Best Practices . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 478 Unhandled Exceptions. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 485 Debugging Exceptions . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 490 Exception-Handling Performance Considerations . . . . . . . . . . . 492 Constrained Execution Regions (CERs) . . . . . . . . . . . . . . . . . . . . . 494 Code Contracts . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 498</p><p>This chapter is all about error handling. But it\u2019s not just about that. There are several parts to error handling. First, we\u2019ll define what an error actually is. Then, we\u2019ll talk about how to discover when your code is experiencing an error and about how to recover from this error. At this point, state becomes an issue because errors tend to come at inopportune times. It is likely that your code will be in the middle of mutating some state when it experiences the error, and your code likely will have to restore some state back to what it was prior to attempting to mutate it. Of course, we\u2019ll also talk about how your code can notify its callers that it has detected an error.</p><p>In my opinion, exception handling is the weakest area of the common language runtime (CLR) and therefore causes many problems for developers writing managed code. Over the years, Microsoft has made some significant improvements to help developers deal with errors, but I believe that there is much more that must be done before we can really have a good, reliable system. I will talk a lot about the various enhancements that have been made when dealing with unhandled exceptions, constrained execution regions, code contracts, runtime wrapped exceptions, uncatchable exceptions, and so on.</p><h6>Defining \u201cException\u201d</h6><p>When designing a type, you first imagine the various situations in which the type will be used. The type name is usually a noun, such as FileStream or StringBuilder. Then you define the properties, methods, events, and so on for the type. The way you define these members (property data types, method parameters, return values, and so forth) becomes the programmatic interface for your type. These members indicate actions that can be performed by the type itself or on an instance of the type. These action members are usually verbs such as Read, Write, Flush, Append, Insert, Remove, etc. When an action member cannot complete its task, the member should throw an exception.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile136.png\"/><p>Important An exception is when a member fails to complete the task it is supposed to perform as indicated by its name.</p><p>Look at the following class definition.</p><p>internal sealed class Account {</p><p>public static void Transfer(Account from, Account to, Decimal amount) { from \u00ad= amount; to += amount;</p><p>} }</p><p>The Transfer method accepts two Account objects and a Decimal value that identifies an amount of money to transfer between accounts. Obviously, the goal of the Transfer method is to subtract money from one account and add money to another. The Transfer method could fail for many reasons: the from or to argument might be null; the from or to argument might not refer to an open account; the from account might have insufficient funds; the to account might have so much money in it that adding more would cause it to overflow; or the amount argument might be 0, negative, or have more than two digits after the decimal place.</p><p>When the Transfer method is called, its code must check for all of these possibilities, and if any of them are detected, it cannot transfer the money and should notify the caller that it failed by throwing an exception. In fact, notice that the Transfer method\u2019s return type is void. This is because the Transfer method has no meaningful value to return; if it returns at all, it was successful. If it fails, it throws a meaningful exception.</p><p>Object-oriented programming allows developers to be very productive because you get to write code like this.</p><p>Boolean f = \"Jeff\".Substring(1, 1).ToUpper().EndsWith(\"E\"); // true</p><p>Here I\u2019m composing my intent by chaining several operations together.1 This code was easy for me to write and is easy for others to read and maintain because the intent is obvious: take a string, grab</p><p>1 In fact, C#\u2019s extension method feature exists in the language to allow you to chain more methods together that would not have been chainable otherwise.</p><p>a portion of it, uppercase that portion, and see if it ends with an \u201cE.\u201d This is great, but there is a big assumption being made here: no operation fails. But, of course, errors are always possible, so we need a way to handle those errors. In fact, there are many object-oriented constructs\u2014constructors, getting/setting a property, adding/removing an event, calling an operator overload, calling a conversion operator\u2014that have no way to return error codes, but these constructs must still be able to report an error. The mechanism provided by the Microsoft .NET Framework and all programming languages that support it is called exception handling.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile137.png\"/><p>Important Many developers incorrectly believe that an exception is related to how frequently something happens. For example, a developer designing a file Read method is likely to say the following: \u201cWhen reading from a file, you will eventually reach the end of its data. Because reaching the end will always happen, I\u2019ll design my Read method so that it reports the end by returning a special value; I won\u2019t have it throw an exception.\u201d The problem with this statement is that it is being made by the developer designing the Read method, not by the developer calling the Read method.</p><p>When designing the Read method, it is impossible for the developer to know all of the possible situations in which the method gets called. Therefore, the developer can\u2019t possibly know how often the caller of the Read method will attempt to read past the end of the file. In fact, because most files contain structured data, attempting to read past the end of a file is something that rarely happens.</p><h6>Exception-Handling Mechanics</h6><p>In this section, I\u2019ll introduce the mechanics and C# constructs needed in order to use exception handling, but it\u2019s not my intention to explain them in great detail. The purpose of this chapter is to offer useful guidelines for when and how to use exception handling in your code. If you want more information about the mechanics and language constructs for using exception handling, see the .NET Framework documentation and the C# language specification. Also, the .NET Framework exceptionhandling mechanism is built using the Structured Exception Handling (SEH) mechanism offered by Windows. SEH has been discussed in many resources, including the book, Windows via C/C++, Fifth Edition, by myself and Christophe Nasarre (Microsoft Press, 2007), which contains three chapters devoted to SEH.</p><p>The following C# code shows a standard usage of the exception-handling mechanism. This code gives you an idea of what exception-handling blocks look like and what their purpose is. In the subsections after the code, I\u2019ll formally describe the try, catch, and finally blocks and their purpose and provide some notes about their use.</p><p>private void SomeMethod() {</p><p>try {</p><p>// Put code requiring graceful recovery and/or cleanup operations here... }</p><p>catch (InvalidOperationException) { // Put code that recovers from an InvalidOperationException here...</p><p>} catch (IOException) {</p><p>// Put code that recovers from an IOException here...</p><p>} catch {</p><p>// Put code that recovers from any kind of exception other than those preceding this...</p><p>}</p><p>// When catching any exception, you usually re\u00adthrow the exception. // I explain re\u00adthrowing later in this chapter. throw;</p><p>} finally {</p><p>// Put code that cleans up any operations started within the try block here... // The code in here ALWAYS executes, regardless of whether an exception is thrown.</p><p>} // Code below the finally block executes if no exception is thrown within the try block // or if a catch block catches the exception and doesn't throw or re\u00adthrow an exception.</p><p>This code demonstrates one possible way to use exception-handling blocks. Don\u2019t let the code scare you\u2014most methods have simply a try block matched with a single finally block or a try block matched with a single catch block. It\u2019s unusual to have as many catch blocks as in this example. I put them there for illustration purposes.</p><h6>The try Block</h6><p>A try block contains code that requires common cleanup operations, exception-recovery operations, or both. The cleanup code should be placed in a single finally block. A try block can also contain code that might potentially throw an exception. The exception-recovery code should be placed in one or more catch blocks. You create one catch block for each kind of exception that your application can safely recover from. A try block must be associated with at least one catch or finally block; it makes no sense to have a try block that stands by itself, and C# will prevent you from doing this.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile138.png\"/><p>Important Sometimes developers ask how much code they should put inside a single try block. The answer to this depends on state management. If, inside a try block, you execute multiple operations that could all throw the same exception type and the way that you\u2019d recover this exception type is different depending on the operation, then you should put each operation in its own try block so that you can recover your state correctly.</p><h6>The catch Block</h6><p>A catch block contains code to execute in response to an exception. A try block can have zero or more catch blocks associated with it. If the code in a try block doesn\u2019t cause an exception to be thrown, the CLR will never execute the code contained within any of its catch blocks. The thread will simply skip over all of the catch blocks and execute the code in the finally block (if one exists). After the code in the finally block executes, execution continues with the statement following the finally block.</p><p>The parenthetical expression appearing after the catch keyword is called the catch type. In C#, you must specify a catch type of System.Exception or a type derived from System.Exception. For example, the previous code contains catch blocks for handling an InvalidOperationException (or any exception derived from it) and an IOException (or any exception derived from it). The last catch block (which doesn\u2019t specify a catch type) handles any exception except for the exception type specified by earlier catch blocks; this is equivalent to having a catch block that specifies a catch type of System.Exception except that you cannot access the exception information via code inside the catch block\u2019s braces.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile139.png\"/><p>Note When debugging through a catch block by using Microsoft Visual Studio, you can see the currently thrown exception object by adding the special $exception variable name to a watch window.</p><p>The CLR searches from top to bottom for a matching catch type, and therefore you should place the more specific exception types at the top. The most-derived exception types should appear first, followed by their base types (if any), down to System.Exception (or an exception block that doesn\u2019t specify a catch type). In fact, the C# compiler generates an error if more specific catch blocks appear closer to the bottom because the catch block would be unreachable.</p><p>If an exception is thrown by code executing within the try block (or any method called from within the try block), the CLR starts searching for catch blocks whose catch type is the same type as or a base type of the thrown exception. If none of the catch types matches the exception, the CLR continues searching up the call stack looking for a catch type that matches the exception. If after reaching the top of the call stack, no catch block is found with a matching catch type, an unhandled exception occurs. I\u2019ll talk more about unhandled exceptions later in this chapter.</p><p>After the CLR locates a catch block with a matching catch type, it executes the code in all inner finally blocks, starting from within the try block whose code threw the exception and stopping with the catch block that matched the exception. Note that any finally block associated with the catch block that matched the exception is not executed yet. The code in this finally block won\u2019t execute until after the code in the handling catch block has executed.</p><p>After all the code in the inner finally blocks has executed, the code in the handling catch block executes. This code typically performs some operations to deal with the exception. At the end of the catch block, you have three choices:</p><p>\u25a0\u25a0 Re-throw the same exception, notifying code higher up in the call stack of the exception.</p><p>\u25a0\u25a0 Throw a different exception, giving richer exception information to code higher up in the call</p><p>stack.</p><p>\u25a0\u25a0 Let the thread fall out of the bottom of the catch block. Later in this chapter, I\u2019ll offer some guidelines for when you should use each of these techniques. If</p><p>you choose either of the first two techniques, you\u2019re throwing an exception, and the CLR behaves just as it did before: it walks up the call stack looking for a catch block whose type matches the type of the exception thrown.</p><p>If you pick the last technique, when the thread falls out of the bottom of the catch block, it immediately starts executing code contained in the finally block (if one exists). After all of the code in the finally block executes, the thread drops out of the finally block and starts executing the statements immediately following the finally block. If no finally block exists, the thread continues execution at the statement following the last catch block.</p><p>In C#, you can specify a variable name after a catch type. When an exception is caught, this variable refers to the System.Exception-derived object that was thrown. The catch block\u2019s code can reference this variable to access information specific to the exception (such as the stack trace leading up to the exception). Although it\u2019s possible to modify this object, you shouldn\u2019t; consider the object to be read-only. I\u2019ll explain the Exception type and what you can do with it later in this chapter.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile140.png\"/><p>Note Your code can register with AppDomain\u2019s FirstChanceException event to receive notifications as soon as an exception occurs within an AppDomain. This notification occurs before the CLR searches for any catch blocks. For more information about this event, see Chapter 22, \u201cCLR Hosting and AppDomains.\u201d</p><h6>The finally Block</h6><p>A finally block contains code that\u2019s guaranteed to execute.2 Typically, the code in a finally block performs the cleanup operations required by actions taken in the try block. For example, if you open a file in a try block, you\u2019d put the code to close the file in a finally block.</p><p>private void ReadData(String pathname) {</p><p>FileStream fs = null; try {</p><p>fs = new FileStream(pathname, FileMode.Open);</p><p>2 Aborting a thread or unloading an AppDomain causes the CLR to throw a ThreadAbortException, which allows the</p><p>finally block to execute. If a thread is simply killed via the Win32 TerminateThread function, or if the process is killed via the Win32 TerminateProcess function or System.Environment\u2019s FailFast method, then the finally block will not execute. Of course Windows cleans up all resources that a process was using when a process terminates.</p><p>// Process the data in the file...</p><p>} catch (IOException) {</p><p>// Put code that recovers from an IOException here...</p><p>} finally {</p><p>// Make sure that the file gets closed. if (fs != null) fs.Close();</p><p>} }</p><p>If the code in the try block executes without throwing an exception, the file is guaranteed to be closed. If the code in the try block does throw an exception, the code in the finally block still executes, and the file is guaranteed to be closed, regardless of whether the exception is caught. It\u2019s improper to put the statement to close the file after the finally block; the statement wouldn\u2019t execute if an exception were thrown and not caught, which would result in the file being left open (until the next garbage collection).</p><p>A try block doesn\u2019t require a finally block associated with it; sometimes the code in a try block just doesn\u2019t require any cleanup code. However, if you do have a finally block, it must appear after any and all catch blocks. A try block can have no more than one finally block associated with it.</p><p>When a thread reaches the end of the code contained in a finally block, the thread simply starts executing the statements immediately following the finally block. Remember that the code in the finally block is cleanup code. This code should execute only what is necessary to clean up opera-</p><p>tions initiated in the try block. The code inside catch and finally blocks should be very short and should have a high likelihood of succeeding without itself throwing an exception. Usually the code in these blocks is just one or two lines of code.</p><p>It is always possible that exception-recovery code or cleanup code could fail and throw an exception. Although possible, it is unlikely and if it does happen it usually means that there is something very wrong somewhere. Most likely some state has gotten corrupted somewhere. If an exception is inadvertently thrown within a catch or finally block, the world will not come to an end\u2014the CLR\u2019s exception mechanism will execute as though the exception were thrown after the finally block. However, the CLR does not keep track of the first exception that was thrown in the corresponding try block (if any), and you will lose any and all information (such as the stack trace) available about the first exception. Probably (and hopefully), this new exception will not be handled by your code and the exception will turn into an unhandled exception. The CLR will then terminate your process, which is good because all the corrupted state will now be destroyed. This is much better than having your application continue to run with unpredictable results and possible security holes.</p><p>Personally, I think the C# team should have chosen different language keywords for the exceptionhandling mechanism. What programmers want to do is try to execute some piece of code. And then, if something fails, either recover from the failure and move on or compensate to undo some state change and continue to report the failure up to a caller. Programmers also want to have guaranteed cleanup no matter what happens.</p><p>The code on the left is what you have to write to make the C# compiler happy, but the code on the right is the way I prefer to think about it.</p><p>void Method() { void Method() { try { try {</p><p>... ... } } catch (XxxException) { handle (XxxException) {</p><p>... ... } } catch (YyyException) { handle (YyyException) {</p><p>... ... } } catch { compensate {</p><p>...; throw; ... } } finally { cleanup { ... ...</p><p>} } } }</p><h6>CLS and Non-CLS Exceptions</h6><p>All programming languages for the CLR must support the throwing of Exception-derived objects because the Common Language Specification (CLS) mandates this. However, the CLR actually allows an instance of any type to be thrown, and some programming languages will allow code to throw non\u2013CLS-compliant exception objects such as a String, Int32, or DateTime. The C# compiler allows code to throw only Exception-derived objects, whereas code written in some other languages allow code to throw Exception-derived objects in addition to objects that are not derived from Exception.</p><p>Many programmers are not aware that the CLR allows any object to be thrown to report an exception. Most developers believe that only Exception-derived objects can be thrown. Prior to CLR 2.0, when programmers wrote catch blocks to catch exceptions, they were catching CLS-compliant exceptions only. If a C# method called a method written in another language, and that method threw a non\u2013CLS-compliant exception, the C# code would not catch this exception at all, leading to some security vulnerabilities.</p><p>In CLR 2.0, Microsoft introduced a new RuntimeWrappedException class (defined in the</p><p>System.Runtime.CompilerServices namespace). This class is derived from Exception, so it is a CLS-compliant exception type. The RuntimeWrappedException class contains a private field of type Object (which can be accessed by using RuntimeWrappedException\u2019s WrappedException read-only property). In CLR 2.0, when a non\u2013CLS-compliant exception is thrown,</p><p>the CLR automatically constructs an instance of the RuntimeWrappedException class and initializes its private field to refer to the object that was actually thrown. In effect, the CLR now turns all non\u2013CLS-compliant exceptions into CLS-compliant exceptions. Any code that now catches an Exception type will catch non\u2013CLS-compliant exceptions, which fixes the potential security vulnerability problem.</p><p>Although the C# compiler allows developers to throw Exception-derived objects only, prior to C# 2.0, the C# compiler did allow developers to catch non\u2013CLS-compliant exceptions by using code like this.</p><p>private void SomeMethod() { try { // Put code requiring graceful recovery and/or cleanup operations here...</p><p>} catch (Exception e) {</p><p>// Before C# 2.0, this block catches CLS\u00adcompliant exceptions only // Now, this block catches CLS\u00adcompliant &amp; non\u2013CLS\u00adcompliant exceptions throw; // Re\u00adthrows whatever got caught</p><p>} catch {</p><p>// In all versions of C#, this block catches CLS\u00adcompliant // &amp; non\u2013CLS\u00adcompliant exceptions throw; // Re\u00adthrows whatever got caught</p><p>} }</p><p>Now, some developers were aware that the CLR supports both CLS-compliant and non\u2013 CLS-compliant exceptions, and these developers might have written the preceding two catch blocks in order to catch both kinds of exceptions. If the preceding code is recompiled for CLR 2.0 or later, the second catch block will never execute, and the C# compiler will indicate this by issuing a warning: CS1058: A previous catch clause already catches all exceptions. All non\u00adexceptions thrown will be wrapped in a System.Runtime. CompilerServices.RuntimeWrappedException.</p><p>There are two ways for developers to migrate code from a version of the .NET Framework prior to version 2.0. First, you can merge the code from the two catch blocks into a single catch block and delete one of the catch blocks. This is the recommended approach. Alternatively, you can tell the CLR that the code in your assembly wants to play by the old rules. That is, tell the CLR that your catch (Exception) blocks should not catch an instance of the new RuntimeWrappedException class. And instead, the CLR should unwrap the non\u2013 CLS-compliant object and call your code only if you have a catch block that doesn\u2019t specify any type at all. You tell the CLR that you want the old behavior by applying an instance of the RuntimeCompatibilityAttribute to your assembly like this.</p><p>using System.Runtime.CompilerServices; [assembly:RuntimeCompatibility(WrapNonExceptionThrows = false)]</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile141.png\"/><p>Note This attribute has an assembly-wide impact. There is no way to mix</p><p>wrapped and unwrapped exception styles in the same assembly. Be careful when adding new code (that expects the CLR to wrap exceptions) to an assembly containing old code (in which the CLR didn\u2019t wrap exceptions).</p><h6>The System.Exception Class</h6><p>The CLR allows an instance of any type to be thrown for an exception\u2014from an Int32 to a String and beyond. However, Microsoft decided against forcing all programming languages to throw and catch exceptions of any type, so they defined the System.Exception type and decreed that all CLScompliant programming languages must be able to throw and catch exceptions whose type is derived from this type. Exception types that are derived from System.Exception are said to be CLS-compliant. C# and many other language compilers allow your code to throw only CLS-compliant exceptions.</p><p>The System.Exception type is a very simple type that contains the properties described in Table 20-1. Usually, you will not write any code to query or access these properties in any way. Instead, when your application terminates due to an unhandled exception, you will look at these properties in the debugger or in a report that gets generated and written out to the Windows Application event log or crash dump.</p><figcaption>TABLE 20-1 Public Properties of the System.Exception Type</figcaption><table border=\"1\">\n<tr>\n<td><p>Property</p>\n<p>Message</p>\n</td>\n<td><p>Access Read-only</p>\n</td>\n<td><p>Type</p>\n<p>String</p>\n</td>\n<td><h6>Description</h6>\n<p>Contains helpful text indicating why the exception was thrown. The message is typically written to a log when a thrown exception is unhandled. Because end users do not see this message, the message should be as technical as possible so that developers viewing the log can use the information in the message to fix the code when producing a new version.</p>\n</td>\n</tr>\n<tr>\n<td><p>Data</p>\n</td>\n<td><p>Read-only</p>\n</td>\n<td><p>IDictionary</p>\n</td>\n<td><p>A reference to a collection of key-value pairs. Usually, the code throwing the exception adds entries to this collection prior to throwing it; code that catches the exception can query the entries and use the information in its exception-recovery processing.</p>\n</td>\n</tr>\n<tr>\n<td><p>Source</p>\n</td>\n<td><p>Read/write</p>\n</td>\n<td><p>String</p>\n</td>\n<td><p>Contains the name of the assembly that generated the exception.</p>\n</td>\n</tr>\n<tr>\n<td><p>StackTrace</p>\n</td>\n<td><p>Read-only</p>\n</td>\n<td><p>String</p>\n</td>\n<td><p>Contains the names and signatures of methods called that led up to the exception being thrown. This property is invaluable for debugging.</p>\n</td>\n</tr>\n<tr>\n<td><p>TargetSite</p>\n</td>\n<td><p>Read-only</p>\n</td>\n<td><p>MethodBase</p>\n</td>\n<td><p>Contains the method that threw the exception.</p>\n</td>\n</tr>\n<tr>\n<td><p>HelpLink</p>\n</td>\n<td><p>Read-only</p>\n</td>\n<td><p>String</p>\n</td>\n<td><p>Contains a URL (such as file://C:\\MyApp\\Help.htm #MyExceptionHelp) to documentation that can help a user understand the exception. Keep in mind that sound programming and security practices prevent users from ever being able to see raw unhandled exceptions, so unless you are trying to convey information to other programmers, this property is seldom used.</p>\n</td>\n</tr>\n</table><table border=\"1\">\n<tr>\n<td><p>Property</p>\n<p>InnerException</p>\n</td>\n<td><p>Access Read-only</p>\n</td>\n<td><p>Type</p>\n<p>Exception</p>\n</td>\n<td><h6>Description</h6>\n<p>Indicates the previous exception if the current exception were raised while handling an exception. This read-only property is usually null. The Exception type also offers a public GetBaseException method that traverses the linked list of inner exceptions and returns the originally thrown exception.</p>\n</td>\n</tr>\n<tr>\n<td><p>HResult</p>\n</td>\n<td><p>Read/write</p>\n</td>\n<td><p>Int32</p>\n</td>\n<td><p>A 32-bit value that is used when crossing managed and native code boundaries. For example, when COM APIs return failure HRESULT values, the CLR throws an Exception-derived object and maintains the HRESULT value in this property.</p>\n</td>\n</tr>\n</table><figcaption>I\u2019d like to say a few words about System.Exception\u2019s read-only StackTrace property. A catch block can read this property to obtain the stack trace indicating what methods were called that led up to the exception. This information can be extremely valuable when you\u2019re trying to detect the cause of an exception so that you can correct your code. When you access this property, you\u2019re actually calling into code in the CLR; the property doesn\u2019t simply return a string. When you construct a new object of an Exception-derived type, the StackTrace property is initialized to null. If you were to read the property, you wouldn\u2019t get back a stack trace; you would get back null.</figcaption><p>When an exception is thrown, the CLR internally records where the throw instruction occurred. When a catch block accepts the exception, the CLR records where the exception was caught. If, inside a catch block, you now access the thrown exception object\u2019s StackTrace property, the code that implements the property calls into the CLR, which builds a string identifying all of the methods between the place where the exception was thrown and the filter that caught the exception.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile142.png\"/><p>Important When you throw an exception, the CLR resets the starting point for the exception; that is, the CLR remembers only the location where the most recent exception object was thrown.</p><p>The following code throws the same exception object that it caught and causes the CLR to reset its starting point for the exception.</p><p>private void SomeMethod() { try { ... } catch (Exception e) {</p><p>... throw e; // CLR thinks this is where exception originated.</p><p>// FxCop reports this as an error }</p><p>}</p><p>In contrast, if you re-throw an exception object by using the throw keyword by itself, the CLR doesn\u2019t reset the stack\u2019s starting point. The following code re-throws the same exception object that it caught, causing the CLR to not reset its starting point for the exception.</p><p>private void SomeMethod() { try { ... } catch (Exception e) {</p><p>... throw; // This has no effect on where the CLR thinks the exception</p><p>// originated. FxCop does NOT report this as an error }</p><p>}</p><p>In fact, the only difference between these two code fragments is what the CLR thinks is the original location where the exception was thrown. Unfortunately, when you throw or re-throw an exception, Windows does reset the stack\u2019s starting point. So if the exception becomes unhandled, the stack location that gets reported to Windows Error Reporting is the location of the last throw or re-throw, even though the CLR knows the stack location where the original exception was thrown. This is unfortunate because it makes debugging applications that have failed in the field much more difficult. Some developers have found this so intolerable that they have chosen a different way to implement their code to ensure that the stack trace truly reflects the location where an exception was originally thrown.</p><p>private void SomeMethod() { Boolean trySucceeds = false; try {</p><p>... trySucceeds = true;</p><p>} finally {</p><p>if (!trySucceeds) { /* catch code goes in here */ } }</p><p>}</p><p>The string returned from the StackTrace property doesn\u2019t include any of the methods in the call stack that are above the point where the catch block accepted the exception object. If you want the complete stack trace from the start of the thread up to the exception handler, you can use the System.Diagnostics.StackTrace type. This type defines some properties and methods that allow a developer to programmatically manipulate a stack trace and the frames that make up the stack trace.</p><p>You can construct a StackTrace object by using several different constructors. Some constructors build the frames from the start of the thread to the point where the StackTrace object is constructed. Other constructors initialize the frames of the StackTrace object by using an Exceptionderived object passed as an argument.</p><p>If the CLR can find debug symbols (located in the .pdb files) for your assemblies, the string returned by System.Exception\u2019s StackTrace property or System.Diagnostics.StackTrace\u2019s ToString method will include source code file paths and line numbers. This information is incredibly useful for debugging.</p><p>Whenever you obtain a stack trace, you might find that some methods in the actual call stack don\u2019t appear in the stack trace string. There are two reasons for this. First, the stack is really a record of where the thread should return to, not where the thread has come from. Second, the just-in-time (JIT) compiler can inline methods to avoid the overhead of calling and returning from a separate method. Many compilers (including the C# compiler) offer a /debug command-line switch. When this switch is used, these compilers embed information into the resulting assembly to tell the JIT compiler not to inline any of the assembly\u2019s methods, making stack traces more complete and meaningful to the developer debugging the code.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile143.png\"/><p>Note The JIT compiler examines the System.Diagnostics.DebuggableAttribute custom attribute applied to the assembly. The C# compiler applies this attribute automatically. If this attribute has the DisableOptimizations flag specified, the JIT compiler won\u2019t inline the assembly\u2019s methods. Using the C# compiler\u2019s /debug switch sets this flag. By applying the System.Runtime.CompilerServices.MethodImplAttribute custom attribute to a method, you can forbid the JIT compiler from inlining the method for both debug and release builds. The following method definition shows how to forbid the method from being inlined.</p><p>using System; using System.Runtime.CompilerServices;</p><p>internal sealed class SomeType {</p><p>}</p><p>[MethodImpl(MethodImplOptions.NoInlining)] public void SomeMethod() {</p><p>... }</p><h6>FCL-Defined Exception Classes</h6><p>The Framework Class Library (FCL) defines many exception types (all ultimately derived from System. Exception). The following hierarchy shows the exception types defined in the MSCorLib.dll assembly; other assemblies define even more exception types. (The application used to obtain this hierarchy is shown in Chapter 23, \u201cAssembly Loading and Reflection.\u201d)</p><p>System.Exception System.AggregateException System.ApplicationException</p><p>System.Reflection.InvalidFilterCriteriaException System.Reflection.TargetException System.Reflection.TargetInvocationException System.Reflection.TargetParameterCountException System.Threading.WaitHandleCannotBeOpenedException</p><p>System.Diagnostics.Tracing.EventSourceException System.InvalidTimeZoneException System.IO.IsolatedStorage.IsolatedStorageException System.Threading.LockRecursionException System.Runtime.CompilerServices.RuntimeWrappedException System.SystemException</p><p>System.Threading.AbandonedMutexException System.AccessViolationException System.Reflection.AmbiguousMatchException System.AppDomainUnloadedException System.ArgumentException</p><p>System.ArgumentNullException System.ArgumentOutOfRangeException System.Globalization.CultureNotFoundException System.Text.DecoderFallbackException System.DuplicateWaitObjectException System.Text.EncoderFallbackException</p><p>System.ArithmeticException System.DivideByZeroException System.NotFiniteNumberException System.OverflowException</p><p>System.ArrayTypeMismatchException System.BadImageFormatException System.CannotUnloadAppDomainException System.ContextMarshalException System.Security.Cryptography.CryptographicException</p><p>System.Security.Cryptography.CryptographicUnexpectedOperationException System.DataMisalignedException System.ExecutionEngineException System.Runtime.InteropServices.ExternalException</p><p>System.Runtime.InteropServices.COMException System.Runtime.InteropServices.SEHException</p><p>System.FormatException</p><p>System.Reflection.CustomAttributeFormatException System.Security.HostProtectionException System.Security.Principal.IdentityNotMappedException System.IndexOutOfRangeException System.InsufficientExecutionStackException System.InvalidCastException System.Runtime.InteropServices.InvalidComObjectException System.Runtime.InteropServices.InvalidOleVariantTypeException System.InvalidOperationException</p><p>System.ObjectDisposedException System.InvalidProgramException System.IO.IOException</p><p>System.IO.DirectoryNotFoundException System.IO.DriveNotFoundException System.IO.EndOfStreamException System.IO.FileLoadException System.IO.FileNotFoundException System.IO.PathTooLongException</p><p>System.Collections.Generic.KeyNotFoundException System.Runtime.InteropServices.MarshalDirectiveException System.MemberAccessException</p><p>System.FieldAccessException System.MethodAccessException System.MissingMemberException</p><p>System.MissingFieldException System.MissingMethodException</p><p>System.Resources.MissingManifestResourceException System.Resources.MissingSatelliteAssemblyException System.MulticastNotSupportedException System.NotImplementedException System.NotSupportedException</p><p>System.PlatformNotSupportedException System.NullReferenceException System.OperationCanceledException</p><p>System.Threading.Tasks.TaskCanceledException System.OutOfMemoryException</p><p>System.InsufficientMemoryException System.Security.Policy.PolicyException System.RankException System.Reflection.ReflectionTypeLoadException System.Runtime.Remoting.RemotingException</p><p>System.Runtime.Remoting.RemotingTimeoutException System.Runtime.InteropServices.SafeArrayRankMismatchException System.Runtime.InteropServices.SafeArrayTypeMismatchException System.Security.SecurityException System.Threading.SemaphoreFullException System.Runtime.Serialization.SerializationException System.Runtime.Remoting.ServerException System.StackOverflowException System.Threading.SynchronizationLockException System.Threading.ThreadAbortException System.Threading.ThreadInterruptedException System.Threading.ThreadStartException System.Threading.ThreadStateException System.TimeoutException System.TypeInitializationException System.TypeLoadException</p><p>System.DllNotFoundException System.EntryPointNotFoundException System.TypeAccessException</p><p>System.TypeUnloadedException System.UnauthorizedAccessException</p><p>System.Security.AccessControl.PrivilegeNotHeldException System.Security.VerificationException System.Security.XmlSyntaxException</p><p>System.Threading.Tasks.TaskSchedulerException System.TimeZoneNotFoundException</p><p>The original idea was that System.Exception would be the base type for all exceptions and that two other types, System.SystemException and System.ApplicationException, would be the only two types immediately derived from Exception. Furthermore, exceptions thrown by the CLR would be derived from SystemException, and all application-thrown exceptions would be derived from ApplicationException. This way, developers could write a catch block that catches all CLRthrown exceptions or all application-thrown exceptions.</p><p>However, as you can see, this rule was not followed very well; some exception types are immediately derived from Exception (IsolatedStorageException), some CLR-thrown exceptions are derived from ApplicationException (TargetInvocationException), and some application-thrown</p><p>exceptions are derived from SystemException (FormatException). So it is all a big mess, and the result is that the SystemException and ApplicationException types have no special meaning at all. At this point, Microsoft would like to remove them from the exception class hierarchy, but they can\u2019t because it would break any code that already references these two types.</p><h6>Throwing an Exception</h6><p>When implementing your own methods, you should throw an exception when the method cannot complete its task as indicated by its name. When you want to throw an exception, there are two issues that you really need to think about and consider.</p><p>The first issue is about which Exception-derived type you are going to throw. You really want to select a type that is meaningful here. Consider the code that is higher up the call stack and how that code might want to determine that a method failed in order to execute some graceful recovery code. You can use a type that is already defined in the FCL, but there may not be one in the FCL that matches your exact semantics. So you\u2019ll probably need to define your own type, ultimately derived from System.Exception.</p><p>If you want to define an exception type hierarchy, it is highly recommended that the hierarchy be shallow and wide in order to create as few base classes as possible. The reason is that base classes act as a way of treating lots of errors as one error, and this is usually dangerous. Along these lines, you should never throw a System.Exception object, and you should use extreme caution if you throw any other base class exception type.3</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile144.png\"/><p>Important There are versioning ramifications here, too. If you define a new exception type derived from an existing exception type, then all code that catches the existing base type will now catch your new type as well. In some scenarios, this may be desired and in some scenarios, it may not be desired. The problem is that it really depends on how code that catches the base class responds to the exception type and types derived from it. Code that never anticipated the new exception may now behave unpredictably and open security holes. The person defining the new exception type can\u2019t know about all the places where the base exception is caught and how it is handled. And so, in practice, it is impossible to make a good intelligent decision here.</p><p>The second issue is about deciding what string message you are going to pass to the exception type\u2019s constructor. When you throw an exception, you should include a string message with detailed information indicating why the method couldn\u2019t complete its task. If the exception is caught and handled, this string message is not seen. However, if the exception becomes an unhandled exception, this message is usually logged. An unhandled exception indicates a true bug in the application, and a developer must get involved to fix the bug. An end user will not have the source code or the ability</p><p>3 In fact, the System.Exception class should have been marked as abstract, which would forbid code that tried to throw it from even compiling.</p><p>to fix the code and recompile it. In fact, this string message should not be shown to an end user. So these string messages can be very technically detailed and as geeky as is necessary to help developers fix their code.</p><p>Furthermore, because all developers have to speak English (at least to some degree, because</p><p>programming languages and the FCL classes and methods are in English), there is usually no need to localize exception string messages. However, you may want to localize the strings if you are building a class library that will be used by developers who speak different languages. Microsoft localizes the exception messages thrown by the FCL, because developers all over the world will be using this class library.</p><h6>Defining Your Own Exception Class</h6><p>Unfortunately, designing your own exception is tedious and error prone. The main reason for this is because all Exception-derived types should be serializable so that they can cross an AppDomain boundary or be written to a log or database. There are many issues related to serialization and they are discussed in Chapter 24, \u201cRuntime Serialization.\u201d So, in an effort to simplify things, I made my own generic Exception&lt;TExceptionArgs&gt; class, which is defined as follows.</p><p>[Serializable] public sealed class Exception&lt;TExceptionArgs&gt; : Exception, ISerializable</p><p>where TExceptionArgs : ExceptionArgs {</p><p>private const String c_args = \"Args\"; // For (de)serialization private readonly TExceptionArgs m_args;</p><p>public TExceptionArgs Args { get { return m_args; } }</p><p>public Exception(String message = null, Exception innerException = null) : this(null, message, innerException) { }</p><p>public Exception(TExceptionArgs args, String message = null, Exception innerException = null): base(message, innerException) { m_args = args; }</p><p>// This constructor is for deserialization; since the class is sealed, the constructor is // private. If this class were not sealed, this constructor should be protected [SecurityPermission(SecurityAction.LinkDemand,</p><p>Flags=SecurityPermissionFlag.SerializationFormatter)]</p><p>private Exception(SerializationInfo info, StreamingContext context) : base(info, context) { m_args = (TExceptionArgs)info.GetValue(c_args, typeof(TExceptionArgs));</p><p>}</p><p>// This method is for serialization; it\u2019s public because of the ISerializable interface [SecurityPermission(SecurityAction.LinkDemand,</p><p>Flags=SecurityPermissionFlag.SerializationFormatter)]</p><p>public override void GetObjectData(SerializationInfo info, StreamingContext context) { info.AddValue(c_args, m_args); base.GetObjectData(info, context);</p><p>}</p><p>public override String Message {</p><p>get { String baseMsg = base.Message; return (m_args == null) ? baseMsg : baseMsg + \" (\" + m_args.Message + \")\";</p><p>} }</p><p>}</p><p>public override Boolean Equals(Object obj) { Exception&lt;TExceptionArgs&gt; other = obj as Exception&lt;TExceptionArgs&gt;; if (other == null) return false; return Object.Equals(m_args, other.m_args) &amp;&amp; base.Equals(obj);</p><p>} public override int GetHashCode() { return base.GetHashCode(); }</p><p>And the ExceptionArgs base class that TExceptionArgs is constrained to is very simple and looks like this.</p><p>[Serializable] public abstract class ExceptionArgs {</p><p>public virtual String Message { get { return String.Empty; } } }</p><p>Now, with these two classes defined, I can trivially define more exception classes when I need to. To define an exception type indicating the disk is full, I simply do the following.</p><p>[Serializable] public sealed class DiskFullExceptionArgs : ExceptionArgs {</p><p>private readonly String m_diskpath; // private field set at construction time</p><p>public DiskFullExceptionArgs(String diskpath) { m_diskpath = diskpath; }</p><p>// Public read\u00adonly property that returns the field public String DiskPath { get { return m_diskpath; } }</p><p>// Override the Message property to include our field (if set) public override String Message {</p><p>get {</p><p>return (m_diskpath == null) ? base.Message : \"DiskPath=\" + m_diskpath; }</p><p>} }</p><p>And, if I have no additional data that I want to put inside the class, it gets as simple as the following.</p><p>[Serializable] public sealed class DiskFullExceptionArgs : ExceptionArgs { }</p><p>And now I can write code like this, which throws and catches one of these.</p><p>public static void TestException() { try { throw new Exception&lt;DiskFullExceptionArgs&gt;( new DiskFullExceptionArgs(@\"C:\\\"), \"The disk is full\");</p><p>} catch (Exception&lt;DiskFullExceptionArgs&gt; e) {</p><p>Console.WriteLine(e.Message); }</p><p>}</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile145.png\"/><figcaption>Note There are two issues to note about my Exception&lt;TExceptionArgs&gt; class. The first issue is that any exception type you define with it is always derived from System. Exception. In most scenarios, this is not a problem at all and, in fact, having a shallow and wide exception type hierarchy is preferred. The second issue is that Visual Studio\u2019s unhandled exception dialog box doesn\u2019t display Exception&lt;T&gt; type\u2019s generic type parameter, as you can see in the following graphic.</figcaption><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile146.png\"/><h6>Trading Reliability for Productivity</h6><p>I started writing software in 1975. I did a fair amount of BASIC programming, and as I got more interested in hardware, I switched to assembly language. Over time, I switched to the C programming language because it allowed me access to hardware with a much higher level of abstraction, making my programming easier. My background is in writing operating systems\u2019 code and platform/library code, so I always work hard to make my code as small and as fast as possible, because applications can only be as good as the operating system and libraries they consume.</p><p>In addition to creating small and fast code, I always focused on error recovery. When allocating memory (by using C++\u2019s new operator or by calling malloc, HeapAlloc, VirtualAlloc, etc.), I would always check the return value to ensure that the memory I requested was actually given to me. And, if the memory request failed, I always had an alternate code path ensuring that the rest of the program\u2019s state was unaffected and would let any of my callers know that I failed so that the calling code can take corrective measures too.</p><p>For some reason that I can\u2019t quite explain, this attention to detail is not done when writing code for the .NET Framework. Getting an out-of-memory situation is always possible and yet I almost never see any code containing a catch block to recover from an OutOfMemoryException. In fact, I\u2019ve even had some developers tell me that the CLR doesn\u2019t let a program catch an OutOfMemoryException. For the record, this is absolutely not true; you can catch this exception. In fact, there are many errors that are possible when executing managed code and I hardly ever see developers write code that attempts to recover from these potential failures. In this section, I\u2019d like to point out some of the potential failures and why it has become culturally acceptable to ignore them. I\u2019d also like to point out some of the significant problems that can occur when ignoring these failures and suggest some ways to help mitigate these problems.</p><p>Object-oriented programming allows developers to be very productive. A big part of this is composability which makes it easy to write, read and maintain code. Take this line of code, for example.</p><p>Boolean f = \"Jeff\".Substring(1, 1).ToUpper().EndsWith(\"E\");</p><p>There is a big assumption being made with the preceding code: no errors occur. But, of course, errors are always possible, and so we need a way to handle those errors. This is what the exception handling constructs and mechanisms are all about and why we need them as opposed to having methods that return true/false or an HRESULT to indicate success/failure the way that Win32 and COM functions do.</p><p>In addition to code composability, we are productive due to all kinds of great features provided by our compilers. For example, the compiler implicitly:</p><p>\u25a0\u25a0 Inserts optional parameters when calling a method.</p><p>\u25a0\u25a0 Boxes value type instances.</p><p>\u25a0\u25a0 Constructs/initializes parameter arrays.</p><p>\u25a0\u25a0 Binds to members of dynamic variables and expressions.</p><p>\u25a0\u25a0 Binds to extension methods.</p><p>\u25a0\u25a0 Binds/invokes overloaded operators.</p><p>\u25a0\u25a0 Constructs delegate objects.</p><p>\u25a0\u25a0 Infers types when calling generic methods, declaring a local variable, and using a lambda</p><p>expression.</p><p>\u25a0\u25a0 Defines/constructs closure classes for lambda expressions and iterators.</p><p>\u25a0\u25a0 Defines/constructs/initializes anonymous types and instances of them.</p><p>\u25a0\u25a0 Rewrites code to support Language Integrated Queries (LINQs; query expressions and</p><p>expression trees).</p><p>And, the CLR itself does all kinds of great things for developers to make our lives even easier. For example, the CLR implicitly:</p><p>\u25a0\u25a0 Invokes virtual methods and interface methods.</p><p>\u25a0\u25a0 Loads assemblies and JIT-compiles methods that can potentially throw FileLoadException, BadImageFormatException, InvalidProgramException, FieldAccessException, MethodAccessException, MissingFieldException, MissingMethodException, and VerificationException.</p><p>\u25a0\u25a0 Transitions across AppDomain boundaries when accessing an object of a MarshalBy\u00ad</p><p>RefObject-derived type which can potentially throw AppDomainUnloadedException.</p><p>\u25a0\u25a0 Serializes and deserializes objects when crossing an AppDomain boundary.</p><p>\u25a0\u25a0 Causes thread(s) to throw a ThreadAbortException when Thread.Abort or</p><p>AppDomain.Unload is called.</p><p>\u25a0\u25a0 Invokes Finalize methods after a garbage collection before objects have their memory</p><p>reclaimed.</p><p>\u25a0\u25a0 Creates type objects in the loader heap when using generic types.</p><p>\u25a0\u25a0 Invokes a type\u2019s static constructor potential throwing of TypeInitializationException. \u25a0\u25a0 Throws various exceptions, including OutOfMemoryException, DivideByZeroException,</p><p>NullReferenceException, RuntimeWrappedException, TargetInvocationException, OverflowException, NotFiniteNumberException, ArrayTypeMismatchException, DataMisalignedException, IndexOutOfRangeException, InvalidCastException, RankException, SecurityException, and more.</p><p>And, of course, the .NET Framework ships with a massive class library that contains tens of thousands of types, each type encapsulating common, reusable functionality. There are types for building web form applications, web services, rich GUI applications, working with security, manipulation of images, speech recognition, and the list goes on and on. Any of this code could throw an exception, indicating failure. And future versions could introduce new exception types derived from existing exception types and now your catch blocks catch exception types that never existed before.</p><p>All of this stuff\u2014object-oriented programming, compiler features, CLR features, and the enormous class library\u2014is what makes the .NET Framework such a compelling software development platform.4 My point is that all of this stuff introduces points of failure into your code, which you have little control over. As long as everything is working great, all is well: we write code easily, the code is easy to read and maintain. But, when something goes wrong, it is nearly impossible to fully understand what went wrong and why.</p><p>4 I should also add that Microsoft Visual Studio\u2019s editor, IntelliSense support, code snippet support, templates, extensibility system, debugging system, and various other tools also contribute to making the platform compelling for developers. However, I leave this out of the main discussion because it has no impact on the behavior of the code at run time.</p><p>Here is an example that should really help get my point across.</p><p>private static Object OneStatement(Stream stream, Char charToFind) { return (charToFind + \": \" + stream.GetType() + String.Empty + (stream.Position + 512M))</p><p>.Where(c=&gt;c == charToFind).ToArray(); }</p><p>This slightly contrived method contains just one C# statement in it, but this statement does an awful lot of work. In fact, here is the Intermediate Language (IL) the C# compiler produced for this method. (I\u2019ve put some lines in boldface italics that are potential points of failure due to implicit operations that are occurring.)</p><p>.method private hidebysig static object OneStatement( class [mscorlib]System.IO.Stream stream, char charToFind) cil managed {</p><p>.maxstack 4</p><p>.locals init (</p><ul style=\"list-style-type: none;\">\n<li><p>[0] class Program/&lt;&gt;c__DisplayClass1 V_0,</p></li>\n<li><p>[1] object[] V_1)</p></li>\n</ul><h6>IL_0000: newobj instance void Program/&lt;&gt;c__DisplayClass1::.ctor()</h6><ul style=\"list-style-type: none;\">\n<li><p>IL_0005: stloc.0</p></li>\n<li><p>IL_0006: ldloc.0</p></li>\n<li><p>IL_0007: ldarg.1</p></li>\n<li><p>IL_0008: stfld char Program/&lt;&gt;c__DisplayClass1::charToFind IL_000d: ldc.i4.5 IL_000e: newarr [mscorlib]System.Object</p></li>\n</ul><ul style=\"list-style-type: none;\">\n<li><p>IL_0013: stloc.1</p></li>\n<li><p>IL_0014: ldloc.1</p></li>\n<li><p>IL_0015: ldc.i4.0</p></li>\n<li><p>IL_0016: ldloc.0</p></li>\n<li><p>IL_0017: ldfld char Program/&lt;&gt;c__DisplayClass1::charToFind</p></li>\n</ul><h6>IL_001c: box [mscorlib]System.Char</h6><ul style=\"list-style-type: none;\">\n<li><p>IL_0021: stelem.ref</p></li>\n<li><p>IL_0022: ldloc.1</p></li>\n<li><p>IL_0023: ldc.i4.1</p></li>\n<li><p>IL_0024: ldstr \": \" IL_0029: stelem.ref</p></li>\n</ul><ul style=\"list-style-type: none;\">\n<li><p>IL_002a: ldloc.1</p></li>\n<li><p>IL_002b: ldc.i4.2</p></li>\n<li><p>IL_002c: ldarg.0</p></li>\n<li><p>IL_002d: callvirt instance class [mscorlib]System.Type [mscorlib]System.Object::GetType()</p></li>\n</ul><ul style=\"list-style-type: none;\">\n<li><p>IL_0032: stelem.ref</p></li>\n<li><p>IL_0033: ldloc.1</p></li>\n<li><p>IL_0034: ldc.i4.3</p></li>\n<li><p>IL_0035: ldsfld string [mscorlib]System.String::Empty</p></li>\n</ul><ul style=\"list-style-type: none;\">\n<li><p>IL_003a: stelem.ref</p></li>\n<li><p>IL_003b: ldloc.1</p></li>\n<li><p>IL_003c: ldc.i4.4</p></li>\n<li><p>IL_003d: ldarg.0</p></li>\n<li><p>IL_003e: callvirt instance int64 [mscorlib]System.IO.Stream::get_Position() IL_0043: call valuetype [mscorlib]System.Decimal</p></li>\n</ul><h6>[mscorlib]System.Decimal::op_Implicit(int64) IL_0048: ldc.i4 0x200 IL_004d: newobj instance void [mscorlib]System.Decimal::.ctor(int32)</h6><p>}</p><p>IL_0052: call valuetype [mscorlib]System.Decimal [mscorlib]System.Decimal::op_Addition</p><p>(valuetype [mscorlib]System.Decimal, valuetype [mscorlib]System.Decimal) IL_0057: box [mscorlib]System.Decimal IL_005c: stelem.ref IL_005d: ldloc.1 IL_005e: call string [mscorlib]System.String::Concat(object[]) IL_0063: ldloc.0 IL_0064: ldftn instance bool Program/&lt;&gt;c__DisplayClass1::&lt;OneStatement&gt;b__0(char) IL_006a: newobj instance</p><p>void [mscorlib]System.Func`2&lt;char, bool&gt;::.ctor(object, native int)</p><p>IL_006f: call class [mscorlib]System.Collections.Generic.IEnumerable`1&lt;!!0&gt; [System.Core]System.Linq.Enumerable::Where&lt;char&gt;( class [mscorlib]System.Collections.Generic.IEnumerable`1&lt;!!0&gt;, class [mscorlib]System.Func`2&lt;!!0, bool&gt;)</p><p>IL_0074: call !!0[] [System.Core]System.Linq.Enumerable::ToArray&lt;char&gt;</p><p>(class [mscorlib]System.Collections.Generic.IEnumerable`1&lt;!!0&gt;) IL_0079: ret</p><p>As you can see, an OutOfMemoryException is possible when constructing the &lt;&gt;c__DisplayClass1 class (a compiler-generated type), the Object[] array, the Func delegate, and boxing the char and Decimal. Memory is also allocated internally when Concat, Where, and ToArray are called. Constructing the Decimal instance could cause its type constructor to be invoked, resulting in a TypeInitializationException.5 And then, there are the implicit calls to Decimal\u2019s op_Implicit operator and its op_Addition operator methods, which could do anything, including throwing an OverflowException.</p><p>Querying Stream\u2019s Position property is interesting. First, it is a virtual property and so my OneStatement method has no idea what code will actually execute which could throw any exception at all. Second, Stream is derived from MarshalByRefObject, so the stream argument could actually refer to a proxy object which itself refers to an object in another AppDomain. The other AppDomain could be unloaded, so an AppDomainUnloadedException could also be thrown here.</p><p>Of course, all the methods that are being called are methods that I personally have no control over</p><p>because they are produced by Microsoft. And it\u2019s entirely possible that Microsoft might change how these methods are implemented in the future, so they could throw new exception types that I could not possibly know about on the day I wrote the OneStatement method. How can I possibly write my</p><p>OneStatement method to be completely robust against all possible failures? By the way, the opposite is also a problem: a catch block could catch an exception type derived from the specified exception type and now I\u2019m executing recovery code for a different kind of failure.</p><p>So now that you have a sense of all the possible failures, you can probably see why it has become culturally acceptable to not write truly robust and reliable code: it is simply impractical. Moreover, one could argue that it is actually impossible. The fact that errors do not occur frequently is another reason why it has become culturally acceptable. Because errors (like OutOfMemoryException) occur very infrequently, the community has decided to trade truly reliable code for programmer productivity.</p><p>5 By the way, System.Char, System.String, System.Type, and System.IO.Stream all define class constructors, which could all potentially cause a TypeInitializationException to be thrown at some point in this application.</p><p>One of the nice things about exceptions is that an unhandled one causes your application to terminate. This is nice because during testing, you will discover problems quickly and the information you get with an unhandled exception (error message and stack trace) are usually enough to allow you to fix your code. Of course, a lot of companies don\u2019t want their application to just terminate after it has been tested and deployed, so a lot of developers insert code to catch System.Exception, the base class of all exception types. However, the problem with catching System.Exception and allowing the application to continue running is that state may be corrupted.</p><p>Earlier in this chapter, I showed an Account class that defines a Transfer method whose job is to transfer money from one account to another account. What if, when this Transfer method is called, it successfully subtracts money from the from account and then throws an exception before it adds money to the to account? If calling code catches System.Exception and continues running, then the state of the application is corrupted: both the from and to accounts have less money in them then they should. Because we are talking about money here, this state corruption wouldn\u2019t just be considered a simple bug, it would definitely be considered a security bug. If the application continues running, it will attempt to perform more transfers to and from various accounts and now state corruption is running rampant within the application.</p><p>One could say that the Transfer method itself should catch System.Exception and restore money back into the from account. And this might actually work out OK if the Transfer method is simple enough. But if the Transfer method produces an audit record of the withdrawn money or if other threads are manipulating the same account at the same time, then attempting to undo the operation could fail as well, producing yet another thrown exception. And now, state corruption is getting worse, not better.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile147.png\"/><p>Note One could argue that knowing where something went wrong is more useful than knowing what error occurred. For example, it might be more useful to know that transferring money out of an account failed instead of knowing that Transfer failed due to a</p><p>SecurityException or OutOfMemoryException, etc. In fact, the Win32 error model works this way: methods return true/false to indicate success/failure so you know which method failed. Then, if your program cares about why it failed, it calls the Win32 GetLastError method. System.Exception does have a Source property that tells you the method that failed. But this property is a String that you\u2019d have to parse, and if two methods internally call the same method, you can\u2019t tell from the Source property alone which method your code called that failed. Instead, you\u2019d have to parse the String returned from Exception\u2019s StackTrace property to get this information. Because this is so difficult, I\u2019ve never seen anyone actually write code to do it.</p><p>There are several things you can do to help mitigate state corruption:</p><p>\u25a0\u25a0 The CLR doesn\u2019t allow a thread to be aborted when executing code inside a catch or finally</p><p>block. So, we could make the Transfer method more robust simply by doing the following.</p><p>public static void Transfer(Account from, Account to, Decimal amount) { try { /* do nothing in here */ } finally {</p><p>from \u00ad= amount; // Now, a thread abort (due to Thread.Abort/AppDomain.Unload) can\u2019t happen here to += amount;</p><p>} }</p><p>However, it is absolutely not recommended that you write all your code in finally blocks! You should only use this technique for modifying extremely sensitive state.</p><p>\u25a0\u25a0 You can use the System.Diagnostics.Contracts.Contract class to apply code contracts to your methods. Code contracts give you a way to validate arguments and other variables before you attempt to modify state by using these arguments/variables. If the arguments/ variables meet the contract, then the chance of corrupted state is minimized (not completely eliminated). If a contract fails, then an exception is thrown before any state has been modified. I will talk about code contracts later in this chapter.</p><p>\u25a0\u25a0 You can use constrained execution regions (CERs), which give you a way to take some CLR uncertainty out of the picture. For example, before entering a try block, you can have the CLR load any assemblies needed by code in any associated catch and finally blocks. In addition, the CLR will compile all the code in the catch and finally blocks including all the methods called from within those blocks. This will eliminate a bunch of potential exceptions (including FileLoadException, BadImageFormatException, InvalidProgramException, FieldAccessException, MethodAccessException, MissingFieldException,</p><p>and MissingMethodException) from occurring when trying to execute error recovery code (in catch blocks) or cleanup code (in the finally block). It will also reduce the potential for OutOfMemoryException and some other exceptions as well. I talk about CERs later in this</p><p>chapter.</p><p>\u25a0\u25a0 Depending on where the state lives, you can use transactions which ensure that all state is modified or no state is modified. If the data is in a database, for example, transactions work well. Windows also now supports transacted registry and file operations (on an NTFS volume only), so you might be able to use this; however, the .NET Framework doesn\u2019t expose this functionality directly today. You will have to P/Invoke to native code to leverage it. See the System.Transactions.TransactionScope class for more details about this.</p><p>\u25a0\u25a0 You can design your methods to be more explicit. For example, the Monitor class is typically</p><p>used for taking/releasing a thread synchronization lock as follows.</p><p>public static class SomeType { private static Object s_myLockObject = new Object();</p><p>}</p><p>public static void SomeMethod () { Monitor.Enter(s_myLockObject); // If this throws, did the lock get taken or</p><p>// not? If it did, then it won't get released! try {</p><p>// Do thread\u00adsafe operation here...</p><p>} finally {</p><p>Monitor.Exit(s_myLockObject); }</p><p>} // ...</p><p>Due to the problem just shown, the overload of the preceding Monitor\u2019s Enter method used is now discouraged, and it is recommended that you rewrite the preceding code as follows.</p><p>public static class SomeType { private static Object s_myLockObject = new Object();</p><p>public static void SomeMethod () { Boolean lockTaken = false; // Assume the lock was not taken try {</p><p>// This works whether an exception is thrown or not! Monitor.Enter(s_myLockObject, ref lockTaken);</p><p>}</p><p>// Do thread\u00adsafe operation here...</p><p>} finally {</p><p>// If the lock was taken, release it if (lockTaken) Monitor.Exit(s_myLockObject);</p><p>}</p><p>} // ...</p><p>Although the explicitness in this code is an improvement, in the case of thread synchronization locks, the recommendation now is to not use them with exception handling at all. See Chapter 30, \u201cHybrid Thread Synchronization Constructs,\u201d for more details about this.</p><p>If, in your code, you have determined that state has already been corrupted beyond repair, then you should destroy any corrupted state so that it can cause no additional harm. Then, restart your application so your state initializes itself to a good condition and hopefully, the state corruption will not happen again. Because managed state cannot leak outside of an AppDomain, you can destroy</p><p>any corrupted state that lives within an AppDomain by unloading the entire AppDomain by calling AppDomain\u2019s Unload method (see Chapter 22 for details).</p><p>And, if you feel that your state is so bad that the whole process should be terminated, then you can call Environment\u2019s static FailFast method.</p><p>public static void FailFast(String message); public static void FailFast(String message, Exception exception);</p><p>This method terminates the process without running any active try/finally blocks or Finalize methods. This is good because executing more code while state is corrupted could easily make matters worse. However, FailFast will allow any CriticalFinalizerObject-derived objects, discussed in Chapter 21, \u201cThe Managed Heap and Garbage Collection,\u201d a chance to clean up. This is usually OK because they tend to just close native resources, and Windows state is probably fine even if the CLR\u2019s state or your application\u2019s state is corrupted. The FailFast method writes the message string and optional exception (usually the exception captured in a catch block) to the Windows Application event log, produces a Windows error report, creates a memory dump of your application, and then terminates the current process.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile148.png\"/><p>Important Most of the FCL code does not ensure that state remains good in the case of an unexpected exception. If your code catches an exception that passes through FCL code and then continues to use FCL objects, there is a chance that these objects will behave unpredictably. It\u2019s a shame that more FCL objects don\u2019t maintain their state better in the face of unexpected exceptions or call FailFast if their state cannot be restored.</p><p>The point of this discussion is to make you aware of the potential problems related to using the CLR\u2019s exception-handling mechanism. Most applications cannot tolerate running with a corrupted state because it leads to incorrect data and possible security holes. If you are writing an application that cannot tolerate terminating (like an operating system or database engine), then managed code is not a good technology to use. And although Microsoft Exchange Server is largely written in managed code, it uses a native database to store email messages. The native database is called the Extensible Storage Engine; it ships with Windows, and can usually be found at C:\\Windows\\System32\\EseNT.dll. Your applications can also use this engine if you\u2019d like; search for \u201cExtensible Storage Engine\u201d on the Microsoft MSDN website.</p><p>Managed code is a good choice for applications that can tolerate an application terminating when state corruption has possibly occurred. There are many applications that fall into this category. Also, it takes significantly more resources and skills to write a robust native class library or application; for many applications, managed code is the better choice because it greatly enhances programmer productivity.</p><h6>Guidelines and Best Practices</h6><p>Understanding the exception mechanism is certainly important. It is equally important to understand how to use exceptions wisely. All too often, I see library developers catching all kinds of exceptions, preventing the application developer from knowing that a problem occurred. In this section, I offer some guidelines for developers to be aware of when using exceptions.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile149.png\"/><p>Important If you\u2019re a class library developer developing types that will be used by other developers, take these guidelines very seriously. You have a huge responsibility: you\u2019re trying to design the type in your class library so that it makes sense for a wide variety of applications. Remember that you don\u2019t have intimate knowledge of the code that you\u2019re calling back (via delegates, virtual methods, or interface methods). And you don\u2019t know which code is calling you. It\u2019s not feasible to anticipate every situation in which your type will be used, so don\u2019t make any policy decisions. Your code must not decide what conditions constitute an error; let the caller make that decision.</p><p>In addition, watch state very closely and try not to corrupt it. Verify arguments passed to your method by using code contracts (discussed later in this chapter). Try not to modify state at all. If you do modify state, be ready for a failure and then try to restore state. If you follow the guidelines in this chapter, application developers will not have a difficult time using the types in your class library.</p><p>If you\u2019re an application developer, define whatever policy you think is appropriate. Following the design guidelines in this chapter will help you discover problems in your code before it is released, allowing you to fix them and make your application more robust. However, feel free to diverge from these guidelines after careful consideration. You get to set the policy. For example, application code can get more aggressive about catching exceptions than class library code.</p><h6>Use finally Blocks Liberally</h6><p>I think finally blocks are awesome! They allow you to specify a block of code that\u2019s guaranteed to execute no matter what kind of exception the thread throws. You should use finally blocks to clean up from any operation that successfully started before returning to your caller or allowing code following the finally block to execute. You also frequently use finally blocks to explicitly dispose of any objects to avoid resource leaking. Here\u2019s an example that has all cleanup code (closing the file) in a finally block.</p><p>using System; using System.IO;</p><p>public sealed class SomeType {</p><p>private void SomeMethod() { FileStream fs = new FileStream(@\"C:\\Data.bin \", FileMode.Open); try {</p><p>}</p><p>// Display 100 divided by the first byte in the file. Console.WriteLine(100 / fs.ReadByte());</p><p>} finally {</p><p>// Put cleanup code in a finally block to ensure that the file gets closed regardless // of whether or not an exception occurs (for example, the first byte was 0). if (fs != null) fs.Dispose();</p><p>} }</p><p>Ensuring that cleanup code always executes is so important that many programming languages offer constructs that make writing cleanup code easier. For example, the C# language automatically emits try/finally blocks whenever you use the lock, using, and foreach statements. The C# compiler also emits try/finally blocks whenever you override a class\u2019s destructor (the Finalize method). When using these constructs, the compiler puts the code you\u2019ve written inside the try block and automatically puts the cleanup code inside the finally block. Specifically:</p><p>\u25a0\u25a0 When you use the lock statement, the lock is released inside a finally block.</p><p>\u25a0\u25a0 When you use the using statement, the object has its Dispose method called inside a</p><p>finally block.</p><p>\u25a0\u25a0 When you use the foreach statement, the IEnumerator object has its Dispose method</p><p>called inside a finally block.</p><p>\u25a0\u25a0 When you define a destructor method, the base class\u2019s Finalize method is called inside a</p><p>finally block.</p><p>For example, the following C# code takes advantage of the using statement. This code is shorter than the code shown in the previous example, but the code that the compiler generates is identical to the code generated in the previous example.</p><p>using System; using System.IO;</p><p>internal sealed class SomeType { private void SomeMethod() {</p><p>using (FileStream fs = new FileStream(@\"C:\\Data.bin\", FileMode.Open)) { // Display 100 divided by the first byte in the file. Console.WriteLine(100 / fs.ReadByte());</p><p>} }</p><p>}</p><p>For more about the using statement, see Chapter 21; and for more about the lock statement, see Chapter 30.</p><h6>Don\u2019t Catch Everything</h6><p>A ubiquitous mistake made by developers who have not been properly trained on the proper use of exceptions is to use catch blocks too often and improperly. When you catch an exception, you\u2019re stating that you expected this exception, you understand why it occurred, and you know how to deal with it. In other words, you\u2019re defining a policy for the application. This all goes back to the \u201cTrading Reliability for Productivity\u201c section earlier in this chapter.</p><p>All too often, I see code like this.</p><p>try { // try to execute code that the programmer knows might fail...</p><p>} catch (Exception) {</p><p>... }</p><p>This code indicates that it was expecting any and all exceptions and knows how to recover from any and all situations. How can this possibly be? A type that\u2019s part of a class library should never, ever, under any circumstance catch and swallow all exceptions because there is no way for the type to know exactly how the application intends to respond to an exception. In addition, the type will frequently call out to application code via a delegate, virtual method, or interface method. If the application code throws an exception, another part of the application is probably expecting to catch this exception. The exception should be allowed to filter its way up the call stack and let the application code handle the exception as it sees fit.</p><p>If the exception is unhandled, the CLR terminates the process. I\u2019ll discuss unhandled exceptions later in this chapter. Most unhandled exceptions will be discovered during testing of your code. To fix these unhandled exceptions, you will either modify the code to look for a specific exception, or you will rewrite the code to eliminate the conditions that cause the exception to be thrown. The final version of the code that will be running in a production environment should see very few unhandled exceptions and will be extremely robust.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile150.png\"/><p>Note In some cases, a method that can\u2019t complete its task will detect that some object\u2019s state has been corrupted and cannot be restored. Allowing the application to continue running might result in unpredictable behavior or security vulnerabilities. When this situation is detected, that method should not throw an exception; instead, it should force the process to terminate immediately by calling System.Environment\u2019s FailFast method.</p><p>By the way, it is OK to catch System.Exception and execute some code inside the catch block\u2019s braces as long as you re-throw the exception at the bottom of that code. Catching System.Exception and swallowing the exception (not re-throwing it) should never be done because it hides failures that allow the application to run with unpredictable results and potential security vulnerabilities. Visual Studio\u2019s code analysis tool (FxCopCmd.exe) will flag any code that contains a catch (Exception) block unless there is a throw statement included in the block\u2019s code. The \u201cBacking Out of a Partially</p><p>Completed Operation When an Unrecoverable Exception Occurs\u2014Maintaining State\u201d section, coming shortly in this chapter, will discuss this pattern.</p><p>Finally, it is OK to catch an exception occurring in one thread and re-throw the exception in another thread. The Asynchronous Programming Model (discussed in Chapter 28, \u201cI/O-Bound Asynchronous Operations\u201d) supports this. For example, if a thread pool thread executes code that throws an exception, the CLR catches and swallows the exception and allows the thread to return to the thread pool. Later, a thread should call an EndXxx method to determine the result of the asynchronous operation. The EndXxx method will throw the same exception object that was thrown by the thread pool thread that did the actual work. In this scenario, the exception is being swallowed by the first thread; however, the exception is being re-thrown by the thread that called the EndXxx method, so it is not being hidden from the application.</p><h6>Recovering Gracefully from an Exception</h6><p>Sometimes you call a method knowing in advance some of the exceptions that the method might throw. Because you expect these exceptions, you might want to have some code that allows your application to recover gracefully from the situation and continue running. Here\u2019s an example in pseudocode.</p><p>public String CalculateSpreadsheetCell(Int32 row, Int32 column) { String result; try {</p><p>result = /* Code to calculate value of a spreadsheet's cell */</p><p>} catch (DivideByZeroException) {</p><p>result = \"Can't show value: Divide by zero\";</p><p>} catch (OverflowException) {</p><p>result = \"Can't show value: Too big\";</p><p>} return result;</p><p>}</p><p>This pseudocode calculates the contents of a cell in a spreadsheet and returns a string representing the value to the caller so that the caller can display the string in the application\u2019s window. However, a cell\u2019s contents might be the result of dividing one cell by another cell. If the cell containing the denominator contains 0, the CLR will throw a DivideByZeroException object. In this case, the method catches this specific exception and returns a special string that will be displayed to the user. Similarly, a cell\u2019s contents might be the result of multiplying one cell by another. If the multiplied value doesn\u2019t fit in the number of bits allowed, the CLR will throw an OverflowException object, and again, a special string will be displayed to the user.</p><p>When you catch specific exceptions, fully understand the circumstances that cause the exception</p><p>to be thrown, and know what exception types are derived from the exception type you\u2019re catching. Don\u2019t catch and handle System.Exception (without re-throwing) because it\u2019s not feasible for you to know all of the possible exceptions that could be thrown within your try block (especially if you consider the OutOfMemoryException or the StackOverflowException, to name two).</p><h6>Backing Out of a Partially Completed Operation When an Unrecoverable Exception Occurs\u2014Maintaining State</h6><p>Usually, methods call several other methods to perform a single abstract operation. Some of the individual methods might complete successfully, and some might not. For example, let\u2019s say that you\u2019re serializing a set of objects to a disk file. After serializing 10 objects, an exception is thrown. (Perhaps the disk is full or the next object to be serialized isn\u2019t marked with the Serializable custom attribute.) At this point, the exception should filter up to the caller, but what about the state of the disk file? The file is now corrupted because it contains a partially serialized object graph. It would be great if the application could back out of the partially completed operation so that the file would be in the state it was in before any objects were serialized into it. The following code demonstrates the correct way to implement this.</p><p>public void SerializeObjectGraph(FileStream fs, IFormatter formatter, Object rootObj) {</p><p>// Save the current position of the file. Int64 beforeSerialization = fs.Position;</p><p>try { // Attempt to serialize the object graph to the file. formatter.Serialize(fs, rootObj);</p><p>} catch { // Catch any and all exceptions.</p><p>// If ANYTHING goes wrong, reset the file back to a good state. fs.Position = beforeSerialization;</p><p>// Truncate the file. fs.SetLength(fs.Position);</p><p>// NOTE: The preceding code isn't in a finally block because // the stream should be reset only when serialization fails.</p><p>} }</p><p>// Let the caller(s) know what happened by re\u00adthrowing the SAME exception. throw;</p><p>To properly back out of the partially completed operation, write code that catches all exceptions. Yes, catch all exceptions here because you don\u2019t care what kind of error occurred; you need to put your data structures back into a consistent state. After you\u2019ve caught and handled the exception, don\u2019t swallow it\u2014let the caller know that the exception occurred. You do this by re-throwing the same exception. In fact, C# and many other languages make this easy. Just use C#\u2019s throw keyword without specifying anything after throw, as shown in the previous code.</p><p>Notice that the catch block in the previous example doesn\u2019t specify any exception type because I want to catch any and all exceptions. In addition, the code in the catch block doesn\u2019t need to know exactly what kind of exception was thrown, just that something went wrong. Fortunately, C# lets me do this easily just by not specifying any exception type and by making the throw statement re-throw whatever object is caught.</p><h6>Hiding an Implementation Detail to Maintain a \u201cContract\u201d</h6><p>In some situations, you might find it useful to catch one exception and re-throw a different exception. The only reason to do this is to maintain the meaning of a method\u2019s contract. Also, the new exception type that you throw should be a specific exception (an exception that\u2019s not used as the base type of any other exception type). Imagine a PhoneBook type that defines a method that looks up a phone number from a name, as shown in the following pseudocode.</p><p>internal sealed class PhoneBook { private String m_pathname; // path name of file containing the address book</p><p>// Other methods go here.</p><p>public String GetPhoneNumber(String name) { String phone; FileStream fs = null; try {</p><p>fs = new FileStream(m_pathname, FileMode.Open); // Code to read from fs until name is found goes here phone = /* the phone # found */</p><p>} catch (FileNotFoundException e) {</p><p>// Throw a different exception containing the name, and // set the originating exception as the inner exception. throw new NameNotFoundException(name, e);</p><p>} catch (IOException e) {</p><p>// Throw a different exception containing the name, and // set the originating exception as the inner exception. throw new NameNotFoundException(name, e);</p><p>} finally {</p><p>if (fs != null) fs.Close();</p><p>} return phone;</p><p>} }</p><p>The phone book data is obtained from a file (versus a network connection or database). However, the user of the PhoneBook type doesn\u2019t know this because this is an implementation detail that could change in the future. So if the file isn\u2019t found or can\u2019t be read for any reason, the caller would see a FileNotFoundException or IOException, which wouldn\u2019t be anticipated. In other words, the file\u2019s existence and ability to be read is not part of the method\u2019s implied contract: there is no way the caller could have guessed this. So the GetPhoneNumber method catches these two exception types and throws a new NameNotFoundException.</p><p>When using this technique, you should catch specific exceptions that you fully understand the circumstances that cause the exception to be thrown. And, you should also know what exception types are derived from the exception type you\u2019re catching.</p><p>Throwing an exception still lets the caller know that the method cannot complete its task, and the NameNotFoundException type gives the caller an abstracted view as to why. Setting the inner exception to FileNotFoundException or IOException is important so that the real cause of the exception isn\u2019t lost. Besides, knowing what caused the exception could be useful to the developer of the PhoneBook type and possibly to a developer using the PhoneBook type.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile151.png\"/><p>Important When you use this technique, you are lying to callers about two things. First, you are lying about what actually went wrong. In my example, a file was not found but I\u2019m reporting that a name was not found. Second, you are lying about where the failure occurred. If the FileNotFoundException were allowed to propagate up the call stack, its StackTrace property would reflect that the error occurred inside FileStream\u2019s constructor. But when I swallow this exception and throw a new NameNotFoundException, the stack trace will indicate that the error occurred inside the catch block, several lines away from where the real exception was thrown. This can make debugging very difficult, so this technique should be used with great care.</p><p>Now let\u2019s say that the PhoneBook type was implemented a little differently. Assume that the type offers a public PhoneBookPathname property that allows the user to set or get the path name of the file in which to look up a phone number. Because the user is aware of the fact that the phone book data comes from a file, I would modify the GetPhoneNumber method so that it doesn\u2019t catch any exceptions; instead, I let whatever exception is thrown propagate out of the method. Note that I\u2019m not changing any parameters of the GetPhoneNumber method, but I am changing how it\u2019s abstracted to users of the PhoneBook type. Users now expect a path to be part of the PhoneBook\u2019s contract.</p><p>Sometimes developers catch one exception and throw a new exception in order to add additional data or context to an exception. However, if this is all you want to do, you should just catch the exception type you want, add data to the exception object\u2019s Data property collection, and then re-throw the same exception object.</p><p>private static void SomeMethod(String filename) { try { // Do whatevere here...</p><p>} catch (IOException e) {</p><p>// Add the filename to the IOException object e.Data.Add(\"Filename\", filename);</p><p>}</p><p>throw; // re\u00adthrow the same exception object that now has additional data in it }</p><p>Here is a good use of this technique: when a type constructor throws an exception that is not caught within the type constructor method, the CLR internally catches that exception and throws a new TypeInitializationException instead. This is useful because the CLR emits code within your methods to implicitly call type constructors.6 If the type constructor threw a DivideByZeroException,</p><p>6 For more information about this, see the \u201cType Constructors\u201d section in Chapter 8, \u201cMethods.\u201d</p><p>your code might try to catch it and recover from it but you didn\u2019t even know you were invoking the type constructor. So the CLR converts the DivideByZeroException into a TypeInitializationException so that you know clearly that the exception occurred due to a type constructor failing; the problem wasn\u2019t with your code.</p><p>On the other hand, here is a bad use of this technique: when you invoke a method via reflection, the CLR internally catches any exception thrown by the method and converts it to a TargetInvocationException. This is incredibly annoying because you must now catch the TargetInvocationException object and look at its InnerException property to discern the real reason for the failure. In fact, when using reflection, it is common to see code that looks like this.</p><p>private static void Reflection(Object o) {</p><p>try { // Invoke a DoSomething method on this object var mi = o.GetType().GetMethod(\"DoSomething\"); mi.Invoke(o, null); // The DoSomething method might throw an exception</p><p>} catch (System.Reflection.TargetInvocationException e) {</p><p>// The CLR converts reflection\u00adproduced exceptions to TargetInvocationException throw e.InnerException; // Re\u00adthrow what was originally thrown</p><p>} }</p><p>I have good news though: if you use C#\u2019s dynamic primitive type (discussed in Chapter 5, \u201cPrimitive, Reference, and Value Types\u201d) to invoke a member, the compiler-generated code does not catch any and all exceptions and throw a TargetInvocationException object; the originally thrown exception object simply walks up the stack. For many developers, this is a good reason to prefer using C#\u2019s dynamic primitive type rather than reflection.</p><h6>Unhandled Exceptions</h6><p>When an exception is thrown, the CLR climbs up the call stack looking for catch blocks that match the type of the exception object being thrown. If no catch block matches the thrown exception type, an unhandled exception occurs. When the CLR detects that any thread in the process has had an unhandled exception, the CLR terminates the process. An unhandled exception identifies a situation that the application didn\u2019t anticipate and is considered to be a true bug in the application. At this point, the bug should be reported back to the company that publishes the application. Hopefully, the publisher will fix the bug and distribute a new version of the application.</p><p>Class library developers should not even think about unhandled exceptions. Only application developers need to concern themselves with unhandled exceptions, and the application should have a policy in place for dealing with unhandled exceptions. Microsoft actually recommends that application developers just accept the CLR\u2019s default policy. That is, when an application gets an unhandled exception, Windows writes an entry to the system\u2019s event log. You can see this entry by opening the Event Viewer application and then looking under the Windows Logs \u2794 Application node in the tree, as shown in Figure 20-1.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile152.png\"/><h6>FIGURE 20-1 Windows Event log showing an application that terminated due to an unhandled exception.</h6><figcaption>However, you can get more interesting details about the problem by using the Windows Reliability Monitor applet. To start Reliability Monitor, open the Windows Control Panel and search for \u201creliability history\u201d. From here, you can see the applications that have terminated due to an unhandled exception in the bottom pane, as shown in Figure 20-2.</figcaption><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile153.png\"/><h6>FIGURE 20-2 Reliability Monitor showing an application that terminated due to an unhandled exception.</h6><p>To see more details about the terminated application, double-click a terminated application in Reliability Monitor. The details will look something like Figure 20-3 and the meaning of the problem signatures are described in Table 20-2. All unhandled exceptions produced by managed applications are placed in the CLR20r3 bucket.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile154.png\"/><figcaption>FIGURE 20-3 Reliability Monitor showing more details about the failed application.</figcaption><p>TABLE 20-2 Problem Signatures</p><table border=\"1\">\n<tr>\n<td><p>Problem Signature 01</p>\n</td>\n<td><p>Description* EXE file\u2019s name (32-character limit)</p>\n</td>\n</tr>\n<tr>\n<td><p>02</p>\n</td>\n<td><p>EXE file\u2019s assembly version number</p>\n</td>\n</tr>\n<tr>\n<td><p>03</p>\n</td>\n<td><p>EXE file\u2019s timestamp</p>\n</td>\n</tr>\n<tr>\n<td><p>04</p>\n</td>\n<td><p>EXE file\u2019s full assembly name (64-character limit)</p>\n</td>\n</tr>\n<tr>\n<td><p>05</p>\n</td>\n<td><p>Faulting assembly\u2019s version</p>\n</td>\n</tr>\n<tr>\n<td><p>06</p>\n</td>\n<td><p>Faulting assembly\u2019s timestamp</p>\n</td>\n</tr>\n<tr>\n<td><p>07</p>\n</td>\n<td><p>Faulting assembly\u2019s type and method. This value is a MethodDef metadata token (after stripping off the 0x06 high byte) identifying the method that threw the exception. From this value, you can use ILDasm.exe to determine the offending type and method.</p>\n</td>\n</tr>\n<tr>\n<td><p>08</p>\n</td>\n<td><p>Faulting method\u2019s IL instruction. This value is an offset within the faulting method of the IL instruction that threw the exception. From this value, you can use ILDasm.exe to determine the offending instruction.</p>\n</td>\n</tr>\n<tr>\n<td><p>09</p>\n</td>\n<td><p>Exception type thrown (32-character limit)</p>\n</td>\n</tr>\n</table><figcaption>* If a string is beyond the allowed limit, then some intelligent truncations are performed, like removing \u201cException\u201d from the exception type or \u201c.dll\u201d from a file name. If the resulting string is still too long, then the CLR will create a value by hashing or base-64\u2013encoding the string.</figcaption><p>After recording information about the failing application, Windows displays the message box allowing the end user to send information about the failing application to Microsoft servers.7 This is called Windows Error Reporting, and more information about it can be found at the Windows Quality website (http://WinQual.Microsoft.com).</p><p>Companies can optionally sign up with Microsoft to view this information about their own applications and components. Signing up is free, but it does require that your assemblies be signed with a VeriSign ID (also called a Software Publisher\u2019s Digital ID for Authenticode).</p><p>Naturally, you could also develop your own system for getting unhandled exception information back to you so that you can fix bugs in your code. When your application initializes, you can inform the CLR that you have a method that you want to be called whenever any thread in your application experiences an unhandled exception.</p><p>Unfortunately, every application model Microsoft produces has its own way of tapping into unhandled exceptions. The members that you want to look up in the FCL documentation are.</p><p>\u25a0\u25a0 For many applications, look at System.AppDomain\u2019s UnhandledException event. Windows</p><p>Store applications and Microsoft Silverlight applications cannot access this event.</p><p>\u25a0\u25a0 For a Windows Store App, look at Windows.UI.Xaml.Application\u2019s UnhandledException</p><p>event.</p><p>\u25a0\u25a0 For a Windows Forms application, look at System.Windows.Forms.NativeWindow\u2019s OnThreadException virtual method, System.Windows.Forms.Application\u2019s OnThreadException virtual method, and System.Windows.Forms.Application\u2019s ThreadException event.</p><p>\u25a0\u25a0 For a Windows Presentation Foundation (WPF) application, look at System.Windows. Application\u2019s DispatcherUnhandledException event and System.Windows. Threading.Dispatcher\u2019s UnhandledException and UnhandledExceptionFilter events.</p><p>\u25a0\u25a0 For Silverlight, look at System.Windows.Application\u2019s UnhandledException event.</p><p>\u25a0\u25a0 For an ASP.NET Web Form application, look at System.Web.UI.TemplateControl\u2019s Error event. TemplateControl is the base class of the System.Web.UI.Page and System.Web.UI.UserControl classes. Furthermore, you should also look at System. Web.HttpApplication\u2019s Error event.</p><p>\u25a0\u25a0 For a Windows Communication Foundation application, look at System.ServiceModel.</p><p>Dispatcher.ChannelDispatcher\u2019s ErrorHandlers property. Before I leave this section, I\u2019d like to say a few words about unhandled exceptions that could occur</p><p>in a distributed application such as a website or web service. In an ideal world, a server application that experiences an unhandled exception should log it, send some kind of notification back to the</p><p>7 You can actually disable this message box by using P/Invoke to call Win32\u2019s SetErrorMode function, passing in SEM_NOGPFAULTERRORBOX.</p><p>client indicating that the requested operation could not complete, and then the server should terminate. Unfortunately, we don\u2019t live in an ideal world, and therefore, it may not be possible to send a failure notification back to the client. For some stateful servers (such as Microsoft SQL Server), it may not be practical to terminate the server and start a brand new instance.</p><p>For a server application, information about the unhandled exception should not be returned to the client because there is little a client could do about it, especially if the client is implemented by a different company. Furthermore, the server should divulge as little information about itself as possible to its clients to reduce that potential of the server being hacked.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile155.png\"/><p>Note The CLR considers some exceptions thrown by native code as corrupted state exceptions (CSEs) because they are usually the result of a bug in the CLR itself or in some native</p><p>code for which the managed developer has no control over. By default, the CLR will not let managed code catch these exceptions and finally blocks will not execute. Here is the list of native Win32 exceptions that are considered CSEs.</p><p>EXCEPTION_ACCESS_VIOLATION EXCEPTION_STACK_OVERFLOW EXCEPTION_ILLEGAL_INSTRUCTION EXCEPTION_IN_PAGE_ERROR EXCEPTION_INVALID_DISPOSITION EXCEPTION_NONCONTINUABLE_EXCEPTION EXCEPTION_PRIV_INSTRUCTION STATUS_UNWIND_CONSOLIDATE.</p><p>Individual managed methods can override the default and catch these exceptions by applying the System.Runtime.ExceptionServices.HandleProcessCorruptedStateExceptionsAttribute to the method. In addition, the method must have the System. Security.SecurityCriticalAttribute applied to it. You can also override the default for an entire process by setting the legacyCorruptedStateExceptionPolicy element in the application\u2019s Extensible Markup Language (XML) configuration file to true. The CLR converts most of these to a System.Runtime.InteropServices.SEHException object except for EXCEPTION_ACCESS_VIOLATION, which is converted to a System.AccessViolationException object, and EXCEPTION_STACK_OVERFLOW, which is converted to a System.StackOverflowException object.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile156.png\"/><p>Note Just before invoking a method, you could check for ample stack space by calling the RuntimeHelper class\u2019s EnsureSufficientExecutionStack method. This method checks whether the calling thread has enough stack space available to execute the average method (which is not well defined). If there is insufficient stack space, the method throws an InsufficientExecutionStackException which you can catch. The EnsureSufficientExecutionStack method takes no arguments and returns void. This method is typically used by recursive methods.</p><h6>Debugging Exceptions</h6><figcaption>The Visual Studio debugger offers special support for exceptions. With a solution open, choose Exceptions from the Debug menu, and you\u2019ll see the dialog box shown in Figure 20-4.</figcaption><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile157.png\"/><ul style=\"list-style-type: none;\">\n<li><p>FIGURE 20-4 The Exceptions dialog box, showing the different kinds of exceptions.</p><p>This dialog box shows the different kinds of exceptions that Visual Studio is aware of. For Common Language Runtime Exceptions, expanding the corresponding branch in the dialog box, as in Figure 20-5, shows the set of namespaces that the Visual Studio debugger is aware of.</p>\n<img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile158.png\"/>\n</li>\n<li><p>FIGURE 20-5 The Exceptions dialog box, showing CLR exceptions by namespace.</p></li>\n</ul><p>If you expand a namespace, you\u2019ll see all of the System.Exception-derived types defined within that namespace. For example, Figure 20-6 shows what you\u2019ll see if you open the System namespace.</p><p>For any exception type, if its Thrown check box is selected, the debugger will break as soon as that exception is thrown. At this point, the CLR has not tried to find any matching catch blocks. This is useful if you want to debug your code that catches and handles an exception. It is also useful when you suspect that a component or library may be swallowing or re-throwing exceptions, and you are uncertain where exactly to set a break point to catch it in the act.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile159.png\"/><ul style=\"list-style-type: none;\">\n<li><p>FIGURE 20-6 The Exceptions dialog box, showing CLR exceptions defined in the System namespace.</p><p>If an exception type\u2019s Thrown check box is not selected, the debugger will also break where the exception was thrown, but only if the exception type was not handled. Developers usually leave the Thrown check box cleared because a handled exception indicates that the application anticipated the situation and dealt with it; the application continues running normally.</p>\n<p>If you define your own exception types, you can add them to this dialog box by clicking Add. This causes the dialog box in Figure 20-7 to appear.</p>\n<img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile160.png\"/>\n</li>\n<li><p>FIGURE 20-7 Making Visual Studio aware of your own exception type: the New Exception dialog box.</p></li>\n</ul><p>In this dialog box, you first select the type of exception to be Common Language Runtime Excep-</p><p>tions, and then you can enter the fully qualified name of your own exception type. Note that the type you enter doesn\u2019t have to be a type derived from System.Exception; non\u2013CLS-compliant types are fully supported. If you have two or more types with the same name but in different assemblies, there is no way to distinguish the types from one another. Fortunately, this situation rarely happens.</p><p>If your assembly defines several exception types, you must add them one at a time. In the future, I\u2019d like to see this dialog box allow me to browse for an assembly and automatically import all Exception-derived types into Visual Studio\u2019s debugger. Each type could then be identified by assembly as well, which would fix the problem of having two types with the same name in different assemblies.</p><h6>Exception-Handling Performance Considerations</h6><p>The developer community actively debates the performance of exception handling. Some people claim that exception handling performance is so bad that they refuse to even use exception handling. However, I contend that in an object-oriented platform, exception handling is not an option; it is mandatory. And besides, if you didn\u2019t use it, what would you use instead? Would you have your methods return true/false to indicate success/failure or perhaps some error code enum type? Well, if you did this, then you have the worst of both worlds: the CLR and the class library code will throw exceptions and your code will return error codes. You\u2019d have to now deal with both of these in your code.</p><p>It\u2019s difficult to compare performance between exception handling and the more conventional means of reporting exceptions (such as HRESULTs, special return codes, and so forth). If you write code to check the return value of every method call and filter the return value up to your own callers, your application\u2019s performance will be seriously affected. But performance aside, the amount of additional coding you must do and the potential for mistakes is incredibly high when you write code to check the return value of every method. Exception handling is a much better alternative.</p><p>However, exception handling has a price: unmanaged C++ compilers must generate code to track which objects have been constructed successfully. The compiler must also generate code that, when an exception is caught, calls the destructor of each successfully constructed object. It\u2019s great that the compiler takes on this burden, but it generates a lot of bookkeeping code in your application, adversely affecting code size and execution time.</p><p>On the other hand, managed compilers have it much easier because managed objects are allocated in the managed heap, which is monitored by the garbage collector. If an object is successfully constructed and an exception is thrown, the garbage collector will eventually free the object\u2019s memory. Compilers don\u2019t need to emit any bookkeeping code to track which objects are constructed successfully and don\u2019t need to ensure that a destructor has been called. Compared to unmanaged C++, this means that less code is generated by the compiler, and less code has to execute at run time, resulting in better performance for your application.</p><p>Over the years, I\u2019ve used exception handling in different programming languages, different operating systems, and different CPU architectures. In each case, exception handling is implemented differently with each implementation having its pros and cons with respect to performance. Some implementations compile exception handling constructs directly into a method, whereas other implementations store information related to exception handling in a data table associated with the method\u2014this table is accessed only if an exception is thrown. Some compilers can\u2019t inline methods that contain exception handlers, and some compilers won\u2019t enregister variables if the method contains exception handlers.</p><p>The point is that you can\u2019t determine how much additional overhead is added to an application when using exception handling. In the managed world, it\u2019s even more difficult to tell because your assembly\u2019s code can run on any platform that supports the .NET Framework. So the code produced by the JIT compiler to manage exception handling when your assembly is running on an x86 machine will be very different from the code produced by the JIT compiler when your code is running on an x64</p><p>or ARM processor. Also, JIT compilers associated with other CLR implementations (such as Microsoft\u2019s</p><p>.NET Compact Framework or the open-source Mono project) are likely to produce different code.</p><p>Actually, I\u2019ve been able to test some of my own code with a few different JIT compilers that Microsoft has internally, and the difference in performance that I\u2019ve observed has been quite dramatic and surprising. The point is that you must test your code on the various platforms that you expect your users to run on, and make changes accordingly. Again, I wouldn\u2019t worry about the performance of using exception handling; the benefits typically far outweigh any negative performance impact.</p><p>If you\u2019re interested in seeing how exception handling impacts the performance of your code, you can use the Performance Monitor tool that comes with Windows. The screen in Figure 20-8 shows the exception-related counters that are installed along with the .NET Framework.</p><p>Occasionally, you come across a method that you call frequently that has a high failure rate. In this situation, the performance hit of having exceptions thrown can be intolerable. For example, Microsoft heard back from several customers who were calling Int32\u2019s Parse method, frequently passing in data entered from an end user that could not be parsed. Because Parse was called frequently, the performance hit of throwing and catching the exceptions was taking a large toll on the application\u2019s overall performance.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile161.png\"/><figcaption>FIGURE 20-8 Performance Monitor showing the .NET CLR Exceptions counters.</figcaption><p>To address customers\u2019 concerns and to satisfy all the guidelines described in this chapter, Microsoft added a new method to the Int32 class. This new method is called TryParse, and it has two overloads that look like the following.</p><p>public static Boolean TryParse(String s, out Int32 result); public static Boolean TryParse(String s, NumberStyles styles,</p><p>IFormatProvider, provider, out Int32 result);</p><p>You\u2019ll notice that these methods return a Boolean that indicates whether the String passed in contains characters that can be parsed into an Int32. These methods also return an output parameter named result. If the methods return true, result will contain the result of parsing the string into a 32-bit integer. If the methods return false, result will contain 0, but you really shouldn\u2019t execute any code that looks at it anyway.</p><p>One thing I want to make absolutely clear: A TryXxx method\u2019s Boolean return value returns false to indicate one and only one type of failure. The method should still throw exceptions for any other type of failure. For example, Int32\u2019s TryParse throws an ArgumentException if the style\u2019s argument is not valid, and it is certainly still possible to have an OutOfMemoryException thrown when calling TryParse.</p><p>I also want to make it clear that object-oriented programming allows programmers to be productive. One way that it does this is by not exposing error codes in a type\u2019s members. In other words, constructors, methods, properties, etc. are all defined with the idea that calling them won\u2019t fail. And, if defined correctly, for most uses of a member, it will not fail, and there will be no performance hit because an exception will not be thrown.</p><p>When defining types and their members, you should define the members so that it is unlikely that they will fail for the common scenarios in which you expect your types to be used. If you later hear from users that they are dissatisfied with the performance due to exceptions being thrown, then and only then should you consider adding TryXxx methods. In other words, you should produce the best object model first and then, if users push back, add some TryXxx methods to your type so that the users who experience performance trouble can benefit. Users who are not experiencing performance trouble should continue to use the non-TryXxx versions of the methods because this is the better object model.</p><h6>Constrained Execution Regions (CERs)</h6><p>Many applications don\u2019t need to be robust and recover from any and all kinds of failures. This is true of many client applications like Notepad.exe and Calc.exe. And, of course, many of us have seen Microsoft Office applications like WinWord.exe, Excel.exe, and Outlook.exe terminate due to unhandled exceptions. Also, many server-side applications, like web servers, are stateless and are automatically restarted if they fail due to an unhandled exception. Of course some servers, like SQL Server, are all about state management and having data lost due to an unhandled exception is potentially much more disastrous.</p><p>In the CLR, we have AppDomains (discussed in Chapter 22), which contain state. When an AppDomain is unloaded, all its state is unloaded. And so, if a thread in an AppDomain experiences an unhandled exception, it is OK to unload the AppDomain (which destroys all its state) without terminating the whole process.8</p><p>8 This is definitely true if the thread lives its whole life inside a single AppDomain (like in the ASP.NET and managed SQL Server stored procedure scenarios). But you might have to terminate the whole process if a thread crosses AppDomain boundaries during its lifetime.</p><p>By definition, a CER is a block of code that must be resilient to failure. Because AppDomains can be unloaded, destroying their state, CERs are typically used to manipulate any state that is shared by multiple AppDomains or processes. CERs are useful when trying to maintain state in the face of exceptions that get thrown unexpectedly. Sometimes we refer to these kinds of exceptions as asynchronous exceptions. For example, when calling a method, the CLR has to load an assembly, create a type object in the AppDomain\u2019s loader heap, call the type\u2019s static constructor, JIT IL into native code, and so on. Any of these operations could fail, and the CLR reports the failure by throwing an exception.</p><p>If any of these operations fail within a catch or finally block, then your error recovery or cleanup code won\u2019t execute in its entirety. Here is an example of code that exhibits the potential problem.</p><ul style=\"list-style-type: none;\">\n<li><p>private static void Demo1() { try {</p><p>Console.WriteLine(\"In try\");</p>\n<p>} finally {</p>\n<p>// Type1\u2019s static constructor is implicitly called in here Type1.M();</p>\n<p>} }</p>\n<p>private sealed class Type1 {</p>\n<p>static Type1() { // if this throws an exception, M won\u2019t get called Console.WriteLine(\"Type1's static ctor called\");</p>\n<p>}</p>\n<p>public static void M() { } }</p>\n<p>When I run the preceding code, I get the following output.</p>\n<p>In try Type1's static ctor called</p>\n<p>What we want is to not even start executing the code in the preceding try block unless we know that the code in the associated catch and finally blocks is guaranteed (or as close as we can get to guaranteed) to execute. We can accomplish this by modifying the code as follows.</p>\n</li>\n<li><p>private static void Demo2() { // Force the code in the finally to be eagerly prepared RuntimeHelpers.PrepareConstrainedRegions(); // System.Runtime.CompilerServices namespace try {</p></li>\n</ul><p>Console.WriteLine(\"In try\");</p><p>} finally {</p><p>// Type2\u2019s static constructor is implicitly called in here Type2.M();</p><p>} }</p><p>public class Type2 { static Type2() {</p><p>Console.WriteLine(\"Type2's static ctor called\"); }</p><p>}</p><p>// Use this attribute defined in the System.Runtime.ConstrainedExecution namespace [ReliabilityContract(Consistency.WillNotCorruptState, Cer.Success)] public static void M() { }</p><p>Now, when I run this version of the code, I get the following output.</p><p>Type2's static ctor called In try</p><p>The PrepareConstrainedRegions method is a very special method. When the JIT compiler sees this method being called immediately before a try block, it will eagerly compile the code in the try\u2019s catch and finally blocks. The JIT compiler will load any assemblies, create any type objects, invoke any static constructors, and JIT any methods. If any of these operations result in an exception, then the exception occurs before the thread enters the try block.</p><p>When the JIT compiler eagerly prepares methods, it also walks the entire call graph eagerly preparing called methods. However, the JIT compiler only prepares methods that have the ReliabilityContractAttribute applied to them with either Consistency.WillNotCorruptState or Consistency.MayCorruptInstance because the CLR can\u2019t make any guarantees about methods that might corrupt AppDomain or process state. Inside a catch or finally block that you are protecting with a call to PrepareConstrainedRegions, you want to make sure that you only call methods with the ReliabillityContractAttribute set as I\u2019ve just described.</p><p>The ReliabilityContractAttribute looks like this.</p><p>public sealed class ReliabilityContractAttribute : Attribute { public ReliabilityContractAttribute(Consistency consistencyGuarantee, Cer cer); public Cer Cer { get; } public Consistency ConsistencyGuarantee { get; }</p><p>}</p><p>This attribute lets a developer document the reliability contract of a particular method to the method\u2019s potential callers. Both the Cer and Consistency types are enumerated types defined as follows.9</p><p>enum Consistency {</p><p>MayCorruptProcess, MayCorruptAppDomain, MayCorruptInstance, WillNotCorruptState }</p><p>enum Cer { None, MayFail, Success }</p><p>If the method you are writing promises not to corrupt any state, use Consistency.WillNotCorruptState. Otherwise, document what your method does by using one of the other three possible values that match whatever state your method might corrupt. If the method that you are writing</p><p>9 You can also apply this attribute to an interface, a constructor, a structure, a class, or an assembly to affect the members inside it.</p><p>promises not to fail, use Cer.Success. Otherwise, use Cer.MayFail. Any method that does not have the ReliabiiltyContractAttribute applied to it is equivalent to being marked like this.</p><p>[ReliabilityContract(Consistency.MayCorruptProcess, Cer.None)]</p><p>The Cer.None value indicates that the method makes no CER guarantees. In other words, it wasn\u2019t written with CERs in mind; therefore, it may fail and it may or may not report that it failed. Remember that most of these settings are giving a method a way to document what it offers to potential callers so that they know what to expect. The CLR and JIT compiler do not use this information.</p><p>When you want to write a reliable method, make it small and constrain what it does. Make sure that it doesn\u2019t allocate any objects (no boxing, for example), don\u2019t call any virtual methods or interface methods, use any delegates, or use reflection because the JIT compiler can\u2019t tell what method will actually be called. However, you can manually prepare these methods by calling one of these methods defined by the RuntimeHelpers\u2019s class.</p><p>public static void PrepareMethod(RuntimeMethodHandle method) public static void PrepareMethod(RuntimeMethodHandle method,</p><p>RuntimeTypeHandle[] instantiation) public static void PrepareDelegate(Delegate d); public static void PrepareContractedDelegate(Delegate d);</p><p>Note that the compiler and the CLR do nothing to verify that you\u2019ve written your method to actually live up to the guarantees you document via the ReliabiltyContractAttribute. If you do something wrong, then state corruption is possible.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile162.png\"/><p>Note Even if all the methods are eagerly prepared, a method call could still result in a StackOverflowException. When the CLR is not being hosted, a StackOverflowException causes the process to terminate immediately by the CLR internally calling Environment. FailFast. When hosted, the PreparedConstrainedRegions method checks the stack to see if there is approximately 48 KB of stack space remaining. If there is limited stack space, the StackOverflowException occurs before entering the try block.</p><p>You should also look at RuntimeHelper\u2019s ExecuteCodeWithGuaranteedCleanup method, which is another way to execute code with guaranteed cleanup.</p><p>public static void ExecuteCodeWithGuaranteedCleanup(TryCode code, CleanupCode backoutCode, Object userData);</p><p>When calling this method, you pass the body of the try and finally block as callback methods whose prototypes match these two delegates respectively.</p><p>public delegate void TryCode(Object userData); public delegate void CleanupCode(Object userData, Boolean exceptionThrown);</p><p>And finally, another way to get guaranteed code execution is to use the CriticalFinalizerObject class, which is explained in great detail in Chapter 21.</p><h6>Code Contracts</h6><p>Code contracts provide a way for you to declaratively document design decisions that you\u2019ve made about your code within the code itself. The contracts take the form of the following:</p><p>\u25a0\u25a0 Preconditions Typically used to validate arguments</p><p>\u25a0\u25a0 Postconditions Used to validate state when a method terminates either due to a normal</p><p>return or due to throwing an exception</p><p>\u25a0\u25a0 Object Invariants Used to ensure an object\u2019s fields remain in a good state through an ob-</p><p>ject\u2019s entire lifetime</p><p>Code contracts facilitate code usage, understanding, evolution, testing, documentation, and early error detection.10 You can think of preconditions, postconditions, and object invariants as parts of a method\u2019s signature. As such, you can loosen a contract with a new version of your code, but you cannot make a contract stricter with a new version without breaking backward compatibility.</p><p>At the heart of the code contracts is the static System.Diagnostics.Contracts.Contract class.</p><p>public static class Contract { // Precondition methods: [Conditional(\"CONTRACTS_FULL\")] public static void Requires(Boolean condition); public static void EndContractBlock();</p><p>// Preconditions: Always public static void Requires&lt;TException&gt;(Boolean condition) where TException : Exception;</p><p>// Postcondition methods: [Conditional(\"CONTRACTS_FULL\")] public static void Ensures(Boolean condition); public static void EnsuresOnThrow&lt;TException&gt;(Boolean condition)</p><p>where TException : Exception;</p><p>// Special Postcondition methods: Always public static T Result&lt;T&gt;(); public static T OldValue&lt;T&gt;(T value); public static T ValueAtReturn&lt;T&gt;(out T value);</p><p>// Object Invariant methods: [Conditional(\"CONTRACTS_FULL\")] public static void Invariant(Boolean condition);</p><p>// Quantifier methods: Always public static Boolean Exists&lt;T&gt;(IEnumerable&lt;T&gt; collection, Predicate&lt;T&gt; predicate); public static Boolean Exists(Int32 fromInclusive, Int32 toExclusive,</p><p>Predicate&lt;Int32&gt; predicate); public static Boolean ForAll&lt;T&gt;(IEnumerable&lt;T&gt; collection, Predicate&lt;T&gt; predicate); public static Boolean ForAll(Int32 fromInclusive, Int32 toExclusive,</p><p>Predicate&lt;Int32&gt; predicate); // Helper methods: [Conditional(\"CONTRACTS_FULL\")] or [Conditional(\"DEBUG\")] public static void Assert(Boolean condition);</p><p>10 To help with automated testing, see the Pex tool created by Microsoft Research: http://research.microsoft.com /en-us/projects/pex/.</p><p>public static void Assume(Boolean condition);</p><p>}</p><p>// Infrastructure event: usually your code will not use this event public static event EventHandler&lt;ContractFailedEventArgs&gt; ContractFailed;</p><p>As previously indicated, many of these static methods have the [Conditional(\"CONTRACTS_ FULL\")] attribute applied to them. Some of the helper methods also have the [Conditional(\"DEBUG\")] attribute applied to them. This means that the compiler will ignore any code you write that calls these methods unless the appropriate symbol is defined when compiling your code. Any methods marked with \u201cAlways\u201d mean that the compiler always emits code to call the method. Also, the Requires, Requires&lt;TException&gt;, Ensures, EnsuresOnThrow, Invariant, Assert, and Assume methods have an additional overload (not shown) that takes a String message argument so you can explicitly specify a string message that should appear when the contract is violated.</p><p>By default, contracts merely serve as documentation because you would not define the CONTRACTS_FULL symbol when you build your project. In order to get some additional value out of using contracts, you must download additional tools and a Visual Studio property pane from http://msdn.microsoft.com/en-us/devlabs/dd491992.aspx. The reason why all the code contract tools are not included with Visual Studio is because this technology is being improved rapidly. The Microsoft DevLabs website can offer new versions and improvements more quickly than Visual Studio itself. After downloading and installing the additional tools, you will see your projects have a new property pane available to them, as shown in Figure 20-9.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile163.png\"/><figcaption>FIGURE 20-9 The Code Contracts pane for a Visual Studio project.</figcaption><p>To turn on code contract features, select the Perform Runtime Contract Checking check box and select Full from the combo box next to it. This defines the CONTRACTS_FULL symbol when you build your project and invokes the appropriate tools (described shortly) after building your project. Now, at run time, when a contract is violated, Contract\u2019s ContractFailed event is raised. Usually, developers do not register any methods with this event, but if you do, then any methods you register will receive a ContractFailedEventArgs object that looks like this.</p><p>public sealed class ContractFailedEventArgs : EventArgs { public ContractFailedEventArgs(ContractFailureKind failureKind, String message, String condition, Exception originalException);</p><p>public ContractFailureKind FailureKind { get; } public String Message { get; } public String Condition { get; } public Exception OriginalException { get; }</p><p>public Boolean Handled { get; } // true if any handler called SetHandled public void SetHandled(); // Call to ignore the violation; sets Handled to true</p><p>}</p><p>public Boolean Unwind { get; } // true if any handler called SetUnwind or threw public void SetUnwind(); // Call to force ContractException; set Unwind to true</p><p>Multiple event handler methods can be registered with this event. Each method can process the contract violation any way it chooses. For example, a handler can log the violation, ignore the violation (by calling SetHandled), or terminate the process. If any method calls SetHandled, then the violation will be considered handled and, after all the handler methods return, the application code is allowed to continue running unless any handler calls SetUnwind. If a handler calls SetUnwind, then, after all the handler methods have completed running, a System.Diagnostics.Contracts. ContractException is thrown. Note that this type is internal to MSCorLib.dll and therefore you cannot write a catch block to catch it explicitly. Also note that if any handler method throws an unhandled exception, then the remaining handler methods are invoked and then a ContractException is thrown.</p><p>If there are no event handlers or if none of them call SetHandled, SetUnwind, or throw an unhandled exception, then default processing of the contract violation happens next. If the CLR is being hosted, then the host is notified that a contract failed. If the CLR is running an application on a non-interactive window station (which would be the case for a Windows service application), then Environment.FailFast is called to instantly terminate the process. If you compile with the Assert On Contract Failure check box selected, then an assert dialog box will appear allowing you to connect a debugger to your application. If this option is not selected, then a ContractException is thrown.</p><p>Let\u2019s look at a sample class that is using code contracts.</p><p>public sealed class Item { /* ... */ }</p><p>public sealed class ShoppingCart { private List&lt;Item&gt; m_cart = new List&lt;Item&gt;(); private Decimal m_totalCost = 0;</p><p>public ShoppingCart() { }</p><p>public void AddItem(Item item) {</p><p>AddItemHelper(m_cart, item, ref m_totalCost); }</p><p>private static void AddItemHelper(List&lt;Item&gt; m_cart, Item newItem, ref Decimal totalCost) {</p><p>// Preconditions: Contract.Requires(newItem != null); Contract.Requires(Contract.ForAll(m_cart, s =&gt; s != newItem));</p><p>// Postconditions: Contract.Ensures(Contract.Exists(m_cart, s =&gt; s == newItem)); Contract.Ensures(totalCost &gt;= Contract.OldValue(totalCost)); Contract.EnsuresOnThrow&lt;IOException&gt;(totalCost == Contract.OldValue(totalCost));</p><p>}</p><p>// Do some stuff (which could throw an IOException)... m_cart.Add(newItem); totalCost += 1.00M;</p><p>}</p><p>// Object invariant [ContractInvariantMethod] private void ObjectInvariant() {</p><p>Contract.Invariant(m_totalCost &gt;= 0); }</p><p>The AddItemHelper method defines a bunch of code contracts. The preconditions indicate that</p><p>newItem must not be null and that the item being added to the cart is not already in the cart. The postconditions indicate that the new item must be in the cart and that the total cost must be at least as much as it was before the item was added to the cart. The postconditions also indicate that if Add\u00ad</p><p>ItemHelper were to throw an IOException for some reason, then totalCost is unchanged from what it was when the method started to execute. The ObjectInvariant method is just a private method that, when called, makes sure that the object\u2019s m_totalCost field never contains a negative value.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile164.png\"/><p>Important All members referenced in a precondition, postcondition, or invariant test must be side-effect free. This is required because testing conditions should not change the state of the object itself. In addition, all members referenced in a precondition test must be at least as accessible as the method defining the precondition. This is required because callers of the method should be able to verify that they have met all the preconditions prior to invoking the method. On the other hand, members referenced in a postcondition or invariant test can have any accessibility as long as the code can compile. The reason why accessibility isn\u2019t important here is because postcondition and invariant tests do not affect the callers\u2019 ability to invoke the method correctly.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile165.png\"/><p>Important In regard to inheritance, a derived type cannot override and change the preconditions of a virtual member defined in a base type. Similarly, a type implementing an interface member cannot change the preconditions defined by that interface member. If a member does not have an explicit contract defined for it, then the member has an implicit contract that logically looks like this.</p><p>Contract.Requires(true);</p><p>And because a contract cannot be made stricter with new versions (without breaking compatibility), you should carefully consider preconditions when introducing a new virtual, abstract, or interface member. For postconditions and object invariants, contracts can be added and removed at will as the conditions expressed in the virtual/abstract/interface member and the conditions expressed in the overriding member are just logically AND-ed together.</p><p>So now you see how to declare contracts. Let\u2019s now talk about how they function at run time. You get to declare all your precondition and postcondition contracts at the top of your methods where they are easy to find. Of course, the precondition contracts will validate their tests when the method is invoked. However, we don\u2019t want the postcondition contracts to validate their tests until the method returns. In order to get the desired behavior, the assembly produced by the C# compiler must be processed by the Code Contract Rewriter tool (CCRewrite.exe, found in C:\\Program Files (x86)\\Microsoft\\Contracts\\Bin), which produces a modified version of the assembly. After you select the Perform Runtime Contract Checking check box for your project, Visual Studio will invoke this tool for you automatically whenever you build the project. This tool analyzes the IL in all your methods and it rewrites the IL so that any postcondition contracts are executed at the end of each method. If your method has multiple return points inside it, then the CCRewrite.exe tool modifies the method\u2019s IL code so that all return points execute the postcondition code prior to the method returning.</p><p>The CCRewrite.exe tool looks in the type for any method marked with the [ContractInvariantMethod] attribute. The method can have any name but, by convention, people usually name the method ObjectInvariant and mark the method as private (as I\u2019ve just done). The method must accept no arguments and have a void return type. When the CCRewrite.exe tool sees a method marked with this attribute, it inserts IL code at the end of every public instance method to call the ObjectInvariant method. This way, the object\u2019s state is checked as each method returns to ensure that no method has violated the contract. Note that the CCRewrite.exe tool does not modify a Finalize method or an IDisposable\u2019s Dispose method to call the ObjectInvariant method because it is OK for an object\u2019s state to be altered if it is considered to be destroyed or disposed. Also note that a single type can define multiple methods with the [ContractInvariantMethod] attribute; this is useful when working with partial types. The CCRewrite.exe tool will modify the IL to call all of these methods (in an undefined order) at the end of each public method.</p><p>The Assert and Assume methods are unlike the other methods. First, you should not consider</p><p>them to be part of the method\u2019s signature, and you do not have to put them at the beginning of a method. At run time, these two methods perform identically: they just verify that the condition</p><p>passed to them is true and throw an exception if it is not. However, there is another tool, the Code Contract Checker (CCCheck.exe), which analyzes the IL produced by the C# compiler in an attempt to statically verify that no code in the method violates a contract. This tool will attempt to prove that any condition passed to Assert is true, but it will just assume that any condition passed to Assume is true and the tool will add the expression to its body of facts known to be true. Usually, you will use Assert and then change an Assert to an Assume if the CCCheck.exe tool can\u2019t statically prove that the expression is true.</p><p>Let\u2019s walk through an example. Assume that I have the following type definition.</p><p>internal sealed class SomeType { private static String s_name = \"Jeffrey\";</p><p>}</p><p>public static void ShowFirstLetter() {</p><p>Console.WriteLine(s_name[0]); // warning: requires unproven: index &lt; this.Length }</p><p>When I build this code with the Perform Static Contract Checking function turned on, the CCCheck.exe tool produces the warning shown as a comment in the preceding code. This warning is notifying me that querying the first letter of s_name may fail and throw an exception because it is unproven that s_name always refers to a string consisting of at least one character.</p><p>Therefore, what we\u2019d like to do is add an assertion to the ShowFirstLetter method.</p><p>public static void ShowFirstLetter() { Contract.Assert(s_name.Length &gt;= 1); // warning: assert unproven Console.WriteLine(s_name[0]);</p><p>}</p><p>Unfortunately, when the CCCheck.exe tool analyzes this code, it is still unable to validate that s_name always refers to a string containing at least one letter, so the tool produces a similar warning. Sometimes the tool is unable to validate assertions due to limitations in the tool; future versions of the tool will be able to perform a more complete analysis.</p><p>To override shortcomings in the tool or to claim that something is true that the tool would never be able to prove, we can change Assert to Assume. If we know for a fact that no other code will modify s_name, then we can change ShowFirstLetter to this.</p><p>public static void ShowFirstLetter() { Contract.Assume(s_name.Length &gt;= 1); // No warning at all now! Console.WriteLine(s_name[0]);</p><p>}</p><p>With this version of the code, the CCCheck.exe tool just takes our word for it and concludes that s_name always refers to a string containing at least one letter. This version of the ShowFirstLetter method passes the code contract static checker without any warnings at all.</p><p>Now, let\u2019s talk about the Code Contract Reference Assembly Generator tool (CCRefGen.exe). Running the CCRewrite.exe tool to enable contract checking helps you find bugs more quickly, but all the code emitted during contract checking makes your assembly bigger and hurts its run-time performance. To improve this situation, you use the CCRefGen.exe tool to create a separate contract reference assembly. Visual Studio invokes this tool for you automatically if you set the Contract Reference Assembly combo box to Build. Contract assemblies are usually named AssemName.Contracts.dll (for example, MSCorLib.Contracts.dll), and these assemblies contain only metadata and the IL that describes the contracts\u2014nothing else. You can identify a contract reference assembly because it will have the System.Diagnostics.Contracts.ContractReferenceAssemblyAttribute applied to the assembly\u2019s assembly definition metadata table. The CCRewrite.exe tool and the CCCheck.exe tool can use contract reference assemblies as input when these tools are performing their instrumentation and analysis.</p><p>The last tool, the Code Contract Document Generator tool (CCDocGen.exe), adds contract information to the XML documentation files already produced by the C# compiler when you use the compiler\u2019s /doc:file switch. This XML file, enhanced by the CCDocGen.exe tool, can be processed by Microsoft\u2019s Sandcastle tool to produce MSDN-style documentation that will now include contract information.</p><h6>C H A P T E R 2 1</h6><h2>The Managed Heap and Garbage Collection</h2><h6>In this chapter:</h6><p>Managed Heap Basics . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 505 Generations: Improving Performance . . . . . . . . . . . . . . . . . . . . . . 513 Working with Types Requiring Special Cleanup . . . . . . . . . . . . . 525 Monitoring and Controlling the Lifetime of Objects Manually . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 545</p><p>In this chapter, I\u2019ll discuss how managed applications construct new objects, how the managed heap controls the lifetime of these objects, and how the memory for these objects gets reclaimed. In short, I\u2019ll explain how the garbage collector in the common language runtime (CLR) works, and I\u2019ll explain various performance issues related to it. I\u2019ll also discuss how to design applications so that they use memory most efficiently.</p><h6>Managed Heap Basics</h6><p>Every program uses resources of one sort or another, be they files, memory buffers, screen space, network connections, database resources, and so on. In fact, in an object-oriented environment, every type identifies some resource available for a program\u2019s use. To use any of these resources requires memory to be allocated to represent the type. The following steps are required to access a resource:</p><ul style=\"list-style-type: none;\">\n<li><p>1. Allocate memory for the type that represents the resource (usually accomplished by using C#\u2019s new operator).</p></li>\n<li><p>2. Initialize the memory to set the initial state of the resource and to make the resource usable. The type\u2019s instance constructor is responsible for setting this initial state.</p></li>\n<li><p>3. Use the resource by accessing the type\u2019s members (repeating as necessary).</p></li>\n<li><p>4. Tear down the state of a resource to clean up.</p></li>\n<li><p>5. Free the memory. The garbage collector is solely responsible for this step.</p></li>\n</ul><p>505</p><p>This seemingly simple paradigm has been one of the major sources of problems for programmers that must manually manage their memory; for example, native C++ developers. Programmers responsible for managing their own memory routinely forget to free memory, which causes a memory leak. In addition, these programmers frequently use memory after having released it, causing their program to experience memory corruption resulting in bugs and security holes. Furthermore, these two bugs are worse than most others because you can\u2019t predict the consequences or the timing of them. For other bugs, when you see your application misbehaving, you just fix the line of code that is not working.</p><p>As long as you are writing verifiably type-safe code (avoiding C#\u2019s unsafe keyword), then it is impossible for your application to experience memory corruption. It is still possible for your application to leak memory but it is not the default behavior. Memory leaks typically occur because your application is storing objects in a collection and never removes objects when they are no longer needed.</p><p>To simplify things even more, most types that developers use quite regularly do not require Step 4 (tear down the state of the resource to clean up). And so, the managed heap, in addition to abolishing the bugs I mentioned, also provides developers with a simple programming model: allocate and initialize a resource and use it as desired. For most types, there is no need to clean up the resource and the garbage collector will free the memory.</p><p>When consuming instances of types that require special cleanup, the programming model remains as simple as I\u2019ve just described. However, sometimes, you want to clean up a resource as soon as possible, rather than waiting for a GC to kick in. In these classes, you can call one additional method (called Dispose) in order to clean up the resource on your schedule. On the other hand, implementing a type that requires special cleanup is quite involved. I describe the details of all this in the \u201cWorking with Types Requiring Special Cleanup\u201d section later in this chapter. Typically, types that require special cleanup are those that wrap native resources like files, sockets, or database connections.</p><h6>Allocating Resources from the Managed Heap</h6><p>The CLR requires that all objects be allocated from the managed heap. When a process is initialized, the CLR allocates a region of address space for the managed heap. The CLR also maintains a pointer, which I\u2019ll call NextObjPtr. This pointer indicates where the next object is to be allocated within the heap. Initially, NextObjPtr is set to the base address of the address space region.</p><p>As region fills with non-garbage objects, the CLR allocates more regions and continues to do this</p><p>until the whole process\u2019s address space is full. So, your application\u2019s memory is limited by the process\u2019s virtual address space. In a 32-bit process, you can allocate close to 1.5 gigabytes (GB) and in a 64-bit process, you can allocate close to 8 terabytes.</p><p>C#\u2019s new operator causes the CLR to perform the following steps:</p><ul style=\"list-style-type: none;\">\n<li><p>1. Calculate the number of bytes required for the type\u2019s fields (and all the fields it inherits from its base types).</p></li>\n<li><p>2. Add the bytes required for an object\u2019s overhead. Each object has two overhead fields: a type object pointer and a sync block index. For a 32-bit application, each of these fields requires</p></li>\n</ul><p>32 bits, adding 8 bytes to each object. For a 64-bit application, each field is 64 bits, adding 16 bytes to each object.</p><p>3. The CLR then checks that the bytes required to allocate the object are available in the region. If there is enough free space in the managed heap, the object will fit, starting at the address pointed to by NextObjPtr, and these bytes are zeroed out. The type\u2019s constructor is called (passing NextObjPtr for the this parameter), and the new operator returns a reference to the object. Just before the reference is returned, NextObjPtr is advanced past the object and now points to the address where the next object will be placed in the heap.</p><figcaption>Figure 21-1 shows a managed heap consisting of three objects: A, B, and C. If another object were to be allocated, it would be placed where NextObjPtr points to (immediately after object C).</figcaption><table border=\"1\">\n<tr>\n<td><p>A</p>\n</td>\n<td><p>B</p>\n</td>\n<td><p>C</p>\n</td>\n<td></td>\n</tr>\n</table><p>NextObjPtr</p><p>FIGURE 21-1 Newly initialized managed heap with three objects constructed in it.</p><p>For the managed heap, allocating an object simply means adding a value to a pointer\u2014this is blazingly fast. In many applications, objects allocated around the same time tend to have strong relationships to each other and are frequently accessed around the same time. For example, it\u2019s very common to allocate a FileStream object immediately before a BinaryWriter object is created. Then the application would use the BinaryWriter object, which internally uses the FileStream object. Because the managed heap allocates these objects next to each other in memory, you get excellent performance when accessing these objects due to locality of reference. Specifically, this means that your process\u2019s working set is small, which means your application runs fast with less memory. It\u2019s also likely that the objects your code is accessing can all reside in the CPU\u2019s cache. The result is that your application will access these objects with phenomenal speed because the CPU will be able to perform most of its manipulations without having cache misses that would force slower access to RAM.</p><p>So far, it sounds like the managed heap provides excellent performance characteristics. However, what I have just described is assuming that memory is infinite and that the CLR can always allocate new objects at the end. However, memory is not infinite and so the CLR employs a technique known as garbage collection (GC) to \u201cdelete\u201d objects in the heap that your application no longer requires access to.</p><h6>The Garbage Collection Algorithm</h6><p>When an application calls the new operator to create an object, there might not be enough address space left in the region to allocate the object. If insufficient space exists, then the CLR performs a GC.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile166.png\"/><p>Important What I\u2019ve just said is an oversimplification. In reality, a GC occurs when generation 0 is full. I\u2019ll explain generations later in this chapter. Until then, it\u2019s easiest for you to think that a garbage collection occurs when the heap is full.</p><p>For managing the lifetime of objects, some systems use a reference counting algorithm. In fact, Microsoft\u2019s own Component Object Model (COM) uses reference counting. With a reference counting system, each object on the heap maintains an internal field indicating how many \u201cparts\u201d of the program are currently using that object. As each \u201cpart\u201d gets to a place in the code where it no longer requires access to an object, it decrements that object\u2019s count field. When the count field reaches 0, the object deletes itself from memory. The big problem with many reference counting systems is that they do not handle circular references well. For example, in a GUI application, a window will hold a reference to a child UI element. And the child UI element will hold a reference to its parent window. These references prevent the two objects\u2019 counters from reaching 0, so both objects will never be deleted even if the application itself no longer has a need for the window.</p><p>Due to this problem with reference counting garbage collector algorithms, the CLR uses a referencing tracking algorithm instead. The reference tracking algorithm cares only about reference type variables, because only these variables can refer to an object on the heap; value type variables contain the value type instance directly. Reference type variables can be used in many contexts: static and instance fields within a class or a method\u2019s arguments or local variables. We refer to all reference type variables as roots.</p><p>When the CLR starts a GC, the CLR first suspends all threads in the process. This prevents threads from accessing objects and changing their state while the CLR examines them. Then, the CLR performs what is called the marking phase of the GC. First, it walks through all the objects in the heap setting a bit (contained in the sync block index field) to 0. This indicates that all objects should be deleted. Then, the CLR looks at all active roots to see which objects they refer to. This is what makes the CLR\u2019s GC a reference tracking GC. If a root contains null, the CLR ignores the root and moves on to examine the next root.</p><p>Any root referring to an object on the heap causes the CLR to mark that object. Marking an object</p><p>means that the CLR sets the bit in the object\u2019s sync block index to 1. When an object is marked, the CLR examines the roots inside that object and marks the objects they refer to. If the CLR is about to mark an already-marked object, then it does not examine the object\u2019s fields again. This prevents an infinite loop from occurring in the case where you have a circular reference.</p><p>Figure 21-2 shows a heap containing several objects. In this example, the application roots refer directly to objects A, C, D, and F. All of these objects are marked. When marking object D, the garbage collector notices that this object contains a field that refers to object H, causing object H to be marked as well. The marking phase continues until all the application roots have been examined.</p><p>Once complete, the heap contains some marked and some unmarked objects. The marked objects must survive the collection because there is at least one root that refers to the object; we say that the object is reachable because application code can reach (or access) the object by way of the variable that still refers to it. Unmarked objects are unreachable because there is no root existing in the application that would allow for the object to ever be accessed again.</p><p>Roots: Fields &amp; variables</p><figcaption>Managed heap</figcaption><table border=\"1\">\n<tr>\n<td><p>A B C D\u2022 E F</p>\n</td>\n<td><p>G</p>\n</td>\n<td><p>H I J</p>\n</td>\n</tr>\n</table><p>NextObjPtr FIGURE 21-2 Managed heap before a collection.</p><p>Now that the CLR knows which objects must survive and which objects can be deleted, it begins the GC\u2019s compacting phase. During the compacting phase, the CLR shifts the memory consumed by the marked objects down in the heap, compacting all the surviving objects together so that they are contiguous in memory. This serves many benefits. First, all the surviving objects will be next to each other in memory; this restores locality of reference reducing your application\u2019s working set size, thereby improving the performance of accessing these objects in the future. Second, the free space is all contiguous as well, so this region of address space can be freed, allowing other things to use it. Finally, compaction means that there are no address space fragmentation issues with the managed heap as is known to happen with native heaps.1</p><p>When compacting memory, the CLR is moving objects around in memory. This is a problem because any root that referred to a surviving object now refers to where that object was in memory; not where the object has been relocated to. When the application\u2019s threads eventually get resumed, they would access the old memory locations and corrupt memory. Clearly, this can\u2019t be allowed and so, as part of the compacting phase, the CLR subtracts from each root the number of bytes that the object it referred to was shifted down in memory. This ensures that every root refers to the same object it did before; it\u2019s just that the object is at a different location in memory.</p><p>After the heap memory is compacted, the managed heap\u2019s NextObjPtr pointer is set to point to a location just after the last surviving object. This is where the next allocated object will be placed in memory. Figure 21-3 shows the managed heap after the compaction phase. After the compaction phase is complete, the CLR resumes all the application\u2019s threads and they continue to access the objects as if the GC never happened at all.</p><p>1 Objects in the large object heap (discussed later in this chapter) do not get compacted, and therefore address space fragmentation is possible with the large object heap.</p><p>Roots: Fields &amp; variables</p><figcaption>Managed heap</figcaption><table border=\"1\">\n<tr>\n<td><p>A</p>\n</td>\n<td><p>C D\u2022 F H</p>\n</td>\n</tr>\n</table><p>NextObjPtr</p><p>FIGURE 21-3 Managed heap after a collection.</p><p>If the CLR is unable to reclaim any memory after a GC and if there is no address space left in the processes to allocate a new GC segment, then there is just no more memory available for this process. In this case, the new operator that attempted to allocate more memory ends up throwing an OutOfMemoryException. Your application can catch this and recover from it but most applications do not attempt to do so; instead, the exception becomes an unhandled exception, Windows terminates the process, and then Windows reclaims all the memory that the process was using.</p><p>As a programmer, notice how the two bugs described at the beginning of this chapter no longer exist. First, it\u2019s not possible to leak objects because any object not accessible from your application\u2019s roots will be collected at some point. Second, it\u2019s not possible to corrupt memory by accessing an object that was freed because references can only refer to living objects, because this is what keeps the objects alive anyway.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile167.png\"/><p>Important A static field keeps whatever object it refers to forever or until the AppDomain that the types are loaded into is unloaded. A common way to leak memory is to have a static field refer to a collection object and then to keep adding items to the collection object. The static field keeps the collection object alive and the collection object keeps all its items alive. For this reason, it is best to avoid static fields whenever possible.</p><h6>Garbage Collections and Debugging</h6><p>As soon as a root goes out of scope, the object it refers to is unreachable and subject to having its memory reclaimed by a GC; objects aren\u2019t guaranteed to live throughout a method\u2019s lifetime. This can have an interesting impact on your application. For example, examine the following code.</p><p>using System; using System.Threading;</p><p>public static class Program {</p><p>public static void Main() { // Create a Timer object that knows to call our TimerCallback // method once every 2000 milliseconds. Timer t = new Timer(TimerCallback, null, 0, 2000);</p><p>}</p><p>// Wait for the user to hit &lt;Enter&gt;. Console.ReadLine();</p><p>private static void TimerCallback(Object o) { // Display the date/time when this method got called. Console.WriteLine(\"In TimerCallback: \" + DateTime.Now);</p><p>} }</p><p>// Force a garbage collection to occur for this demo. GC.Collect();</p><p>Compile this code from the command prompt without using any special compiler switches. When you run the resulting executable file, you\u2019ll see that the TimerCallback method is called just once!</p><p>From examining the preceding code, you\u2019d think that the TimerCallback method would get called once every 2,000 milliseconds. After all, a Timer object is created, and the variable t refers to this object. As long as the timer object exists, the timer should keep firing. But you\u2019ll notice in the TimerCallback method that I force a garbage collection to occur by calling GC.Collect().</p><p>When the collection starts, it first assumes that all objects in the heap are unreachable (garbage); this includes the Timer object. Then, the collector examines the application\u2019s roots and sees that</p><p>Main doesn\u2019t use the t variable after the initial assignment to it. Therefore, the application has no variable referring to the Timer object, and the garbage collection reclaims the memory for it; this stops the timer and explains why the TimerCallback method is called just once.</p><p>Let\u2019s say that you\u2019re using a debugger to step through Main, and a garbage collection just happens to occur just after t is assigned the address of the new Timer object. Then, let\u2019s say that you try to view the object that t refers to by using the debugger\u2019s Quick Watch window. What do you think will happen? The debugger can\u2019t show you the object because it was just garbage collected. This behavior would be considered very unexpected and undesirable by most developers, so Microsoft has come up with a solution.</p><p>When you compile your assembly by using the C# compiler\u2019s /debug switch, the compiler applies a System.Diagnostics.DebuggableAttribute with its DebuggingModes\u2019 DisableOptimizations flag set into the resulting assembly. At run time, when compiling a method, the JIT compiler sees this flag set, and artificially extends the lifetime of all roots to the end of the method. For my example, the JIT compiler tricks itself into believing that the t variable in Main must live until the end of the method. So, if a garbage collection were to occur, the garbage collector now thinks that t is still a root and that the Timer object that t refers to will continue to be reachable. The Timer object will survive the collection, and the TimerCallback method will get called repeatedly until Console. ReadLine returns and Main exits.</p><p>To see this, just recompile the program from a command prompt, but this time, specify the C# compiler\u2019s /debug switch. When you run the resulting executable file, you\u2019ll now see that the TimerCallback method is called repeatedly! Note, the C# compiler\u2019s /optimize+ compiler switch turns optimizations back on, so this compiler switch should not be specified when performing this experiment.</p><p>The JIT compiler does this to help you with JIT debugging. You may now start your application normally (without a debugger), and if the method is called, the JIT compiler will artificially extend the lifetime of the variables to the end of the method. Later, if you decide to attach a debugger to the process, you can put a breakpoint in a previously compiled method and examine the root variables.</p><p>So now you know how to build a program that works in a debug build but doesn\u2019t work correctly when you make a release build! Because no developer wants a program that works only when debugging it, there should be something we can do to the program so that it works all of the time regardless of the type of build.</p><p>You could try modifying the Main method to the following.</p><p>public static void Main() { // Create a Timer object that knows to call our TimerCallback // method once every 2000 milliseconds. Timer t = new Timer(TimerCallback, null, 0, 2000);</p><p>// Wait for the user to hit &lt;Enter&gt;. Console.ReadLine();</p><p>}</p><p>// Refer to t after ReadLine (this gets optimized away) t = null;</p><p>However, if you compile this (without the /debug+ switch) and run the resulting executable file, you\u2019ll see that the TimerCallback method is still called just once. The problem here is that the JIT compiler is an optimizing compiler, and setting a local variable or parameter variable to null is the same as not referencing the variable at all. In other words, the JIT compiler optimizes the t = null; line out of the code completely, and therefore, the program still does not work as we desire. The correct way to modify the Main method is as follows.</p><p>public static void Main() { // Create a Timer object that knows to call our TimerCallback // method once every 2000 milliseconds. Timer t = new Timer(TimerCallback, null, 0, 2000);</p><p>// Wait for the user to hit &lt;Enter&gt;. Console.ReadLine();</p><p>}</p><p>// Refer to t after ReadLine (t will survive GCs until Dispose returns) t.Dispose();</p><p>Now, if you compile this code (without the /debug+ switch) and run the resulting executable file, you\u2019ll see that the TimerCallback method is called multiple times, and the program is fixed. What\u2019s happening here is that the object t is required to stay alive so that the Dispose instance method can be called on it. (The value in t needs to be passed as the this argument to Dispose.) It\u2019s ironic: by explicitly indicating where you want the timer to be disposed, it must remain alive up to that point.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile168.png\"/><p>Note Please don\u2019t read this whole discussion and then worry about your own objects being garbage collected prematurely. I use the Timer class in this discussion because it has special behavior that no other class exhibits. The \u201cproblem/feature\u201d of Timer is that the existence of a Timer object in the heap causes something else to happen: A thread pool thread invokes a method periodically. No other type exhibits this behavior. For example, the existence of a String object in memory doesn\u2019t cause anything else to happen; the string just sits there. So, I use Timer to show how roots work and how object-lifetime works as related to the debugger, but the discussion is not really about how to keep objects alive. All non-Timer objects will live as needed by the application automatically.</p><h6>Generations: Improving Performance</h6><p>The CLR\u2019s GC is a generational garbage collector (also known as an ephemeral garbage collector, although I don\u2019t use the latter term in this book). A generational GC makes the following assumptions about your code:</p><p>\u25a0\u25a0 The newer an object is, the shorter its lifetime will be.</p><p>\u25a0\u25a0 The older an object is, the longer its lifetime will be.</p><p>\u25a0\u25a0 Collecting a portion of the heap is faster than collecting the whole heap. Numerous studies have demonstrated the validity of these assumptions for a very large set of ex-</p><p>isting applications, and these assumptions have influenced how the garbage collector is implemented. In this section, I\u2019ll describe how generations work.</p><p>When initialized, the managed heap contains no objects. Objects added to the heap are said to be in generation 0. Stated simply, objects in generation 0 are newly constructed objects that the garbage collector has never examined. Figure 21-4 shows a newly started application with five objects allocated (A through E). After a while, objects C and E become unreachable.</p><table border=\"1\">\n<tr>\n<td><p>A</p>\n</td>\n<td><p>B</p>\n</td>\n<td><p>C</p>\n</td>\n<td><p>D</p>\n</td>\n<td><p>E</p>\n</td>\n<td></td>\n</tr>\n</table><figcaption>Generation 0</figcaption><p>FIGURE 21-4 A newly initialized heap containing some objects, all in generation 0. No collections have occurred yet.</p><p>When the CLR initializes, it selects a budget size (in kilobytes) for generation 0. So if allocating a new object causes generation 0 to surpass its budget, a garbage collection must start. Let\u2019s say that objects A through E fill all of generation 0. When object F is allocated, a garbage collection must start.</p><p>The garbage collector will determine that objects C and E are garbage and will compact object D, causing it to be adjacent to object B. The objects that survive the garbage collection (objects A, B, and D) are said to be in generation 1. Objects in generation 1 have been examined by the garbage collector once. The heap now looks like Figure 21-5.</p><table border=\"1\">\n<tr>\n<td><p>A</p>\n</td>\n<td><p>B</p>\n</td>\n<td><p>D</p>\n</td>\n<td></td>\n</tr>\n</table><p>Generation 0</p><figcaption>Generation 1</figcaption><ul style=\"list-style-type: none;\">\n<li><p>FIGURE 21-5 After one collection, generation 0 survivors are promoted to generation 1; generation 0 is empty.</p><p>After a garbage collection, generation 0 contains no objects. As always, new objects will be allocated in generation 0. Figure 21-6 shows the application running and allocating objects F through K. In addition, while the application was running, objects B, H, and J became unreachable and should have their memory reclaimed at some point.</p>\n<table border=\"1\">\n<tr>\n<td><p>A</p>\n</td>\n<td><p>B</p>\n</td>\n<td><p>D</p>\n</td>\n<td><p>F</p>\n</td>\n<td><p>G</p>\n</td>\n<td><p>H</p>\n</td>\n<td><p>I</p>\n</td>\n<td></td>\n<td><p>J</p>\n</td>\n<td><p>K</p>\n</td>\n<td></td>\n</tr>\n</table>\n<p>Generation 1</p>\n<p>Generation 0</p>\n</li>\n<li><p>FIGURE 21-6 New objects are allocated in generation 0; generation 1 has some garbage.</p></li>\n</ul><p>Now let\u2019s say that attempting to allocate object L would put generation 0 over its budget. Because</p><ul style=\"list-style-type: none;\">\n<li><p>generation 0 has reached its budget, a garbage collection must start. When starting a garbage collection, the garbage collector must decide which generations to examine. Earlier, I said that when the CLR initializes, it selects a budget for generation 0. Well, it also selects a budget for generation 1.</p></li>\n</ul><p>When starting a garbage collection, the garbage collector also sees how much memory is occupied by generation 1. In this case, generation 1 occupies much less than its budget, so the garbage collector examines only the objects in generation 0. Look again at the assumptions that the generational garbage collector makes. The first assumption is that newly created objects have a short lifetime. So generation 0 is likely to have a lot of garbage in it, and collecting generation 0 will therefore reclaim a lot of memory. The garbage collector will just ignore the objects in generation 1, which will speed up the garbage collection process.</p><p>Obviously, ignoring the objects in generation 1 improves the performance of the garbage collector. However, the garbage collector improves performance more because it doesn\u2019t traverse every object in the managed heap. If a root or an object refers to an object in an old generation, the garbage collector can ignore any of the older objects\u2019 inner references, decreasing the amount of time required to build the graph of reachable objects. Of course, it\u2019s possible that an old object\u2019s field refers to a new object. To ensure that the updated fields of these old objects are examined, the garbage collector uses a mechanism internal to the JIT compiler that sets a bit when an object\u2019s reference field changes. This support lets the garbage collector know which old objects (if any) have been written to</p><p>because the last collection. Only old objects that have had fields change need to be examined to see whether they refer to any new object in generation 0.2</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile169.png\"/><p>Note Microsoft\u2019s performance tests show that it takes less than 1 millisecond to perform a garbage collection of generation 0. Microsoft\u2019s goal is to have garbage collections take no more time than an ordinary page fault.</p><p>A generational garbage collector also assumes that objects that have lived a long time will continue to live. So it\u2019s likely that the objects in generation 1 will continue to be reachable from the application. Therefore, if the garbage collector were to examine the objects in generation 1, it probably wouldn\u2019t find a lot of garbage. As a result, it wouldn\u2019t be able to reclaim much memory. So it is likely that collecting generation 1 is a waste of time. If any garbage happens to be in generation 1, it just stays there. The heap now looks like Figure 21-7.</p><table border=\"1\">\n<tr>\n<td><p>A</p>\n</td>\n<td><p>B</p>\n</td>\n<td><p>D</p>\n</td>\n<td><p>F</p>\n</td>\n<td><p>G</p>\n</td>\n<td><p>I</p>\n</td>\n<td><p>K</p>\n</td>\n<td></td>\n</tr>\n</table><figcaption>Generation 1 Generation 0</figcaption><p>FIGURE 21-7 After two collections, generation 0 survivors are promoted to generation 1 (growing the size of generation 1); generation 0 is empty.</p><p>As you can see, all of the generation 0 objects that survived the collection are now part of generation 1. Because the garbage collector didn\u2019t examine generation 1, object B didn\u2019t have its memory reclaimed even though it was unreachable at the time of the last garbage collection. Again, after a collection, generation 0 contains no objects and is where new objects will be placed. In fact, let\u2019s say that the application continues running and allocates objects L through O. And while running, the application stops using objects G, L, and M, making them all unreachable. The heap now looks like Figure 21-8.</p><table border=\"1\">\n<tr>\n<td><p>A</p>\n</td>\n<td><p>B</p>\n</td>\n<td><p>D</p>\n</td>\n<td><p>F</p>\n</td>\n<td><p>G</p>\n</td>\n<td><p>I</p>\n</td>\n<td><p>K</p>\n</td>\n<td><p>L</p>\n</td>\n<td><p>M</p>\n</td>\n<td><p>N</p>\n</td>\n<td><p>O</p>\n</td>\n<td></td>\n</tr>\n</table><figcaption>Generation 1 Generation 0</figcaption><p>FIGURE 21-8 New objects are allocated in generation 0; generation 1 has more garbage.</p><p>Let\u2019s say that allocating object P causes generation 0 to exceed its budget, causing a garbage collection to occur. Because the memory occupied by all of the objects in generation 1 is less than its</p><p>2 For the curious, here are some more details about this. When the JIT compiler produces native code that modifies a reference field inside an object, the native code includes a call to a write barrier method. This write barrier method checks whether the object whose field is being modified is in generation 1 or 2 and if it is, the write barrier code sets a bit in what is called the card table. The card table has 1 bit for every 128-byte range of data in the heap. When the next GC starts, it scans the card table to know which objects in generations 1 and 2 have had their fields changed because the last GC. If any of these modified objects refer to an object in generation 0, then the generation 0 objects survive the collection. After the GC, the card table is reset to all zeroes. The write barrier code causes a slight performance hit when writing to a reference field in an object (as opposed to a local variable or static field) and that performance hit is slightly worse if that object is in generation 1 or 2.</p><p>budget, the garbage collector again decides to collect only generation 0, ignoring the unreachable objects in generation 1 (objects B and G). After the collection, the heap looks like Figure 21-9.</p><table border=\"1\">\n<tr>\n<td><p>A</p>\n</td>\n<td><p>B</p>\n</td>\n<td><p>D</p>\n</td>\n<td><p>F</p>\n</td>\n<td><p>G</p>\n</td>\n<td><p>I</p>\n</td>\n<td><p>K</p>\n</td>\n<td><p>N</p>\n</td>\n<td><p>O</p>\n</td>\n<td></td>\n</tr>\n</table><figcaption>Generation 1 Generation 0</figcaption><ul style=\"list-style-type: none;\">\n<li><p>FIGURE 21-9 After three collections, generation 0 survivors are promoted to generation 1 (growing the size of</p><p>generation 1 again); generation 0 is empty.</p>\n<p>In Figure 21-9, you see that generation 1 keeps growing slowly. In fact, let\u2019s say that generation 1 has now grown to the point in which all of the objects in it occupy its full budget. At this point, the application continues running (because a garbage collection just finished) and starts allocating objects P through S, which fill generation 0 up to its budget. The heap now looks like Figure 21-10.</p>\n<table border=\"1\">\n<tr>\n<td><p>A</p>\n</td>\n<td><p>B</p>\n</td>\n<td><p>D</p>\n</td>\n<td><p>F</p>\n</td>\n<td><p>G</p>\n</td>\n<td><p>I</p>\n</td>\n<td><p>K</p>\n</td>\n<td><p>N</p>\n</td>\n<td><p>O</p>\n</td>\n<td><p>P</p>\n</td>\n<td><p>Q</p>\n</td>\n<td><p>R</p>\n</td>\n<td><p>S</p>\n</td>\n<td></td>\n</tr>\n</table>\n<p>Generation 1 Generation 0</p>\n</li>\n<li><p>FIGURE 21-10 New objects are allocated in generation 0; generation 1 has more garbage.</p><p>When the application attempts to allocate object T, generation 0 is full, and a garbage collection must start. This time, however, the garbage collector sees that the objects in generation 1 are occupying so much memory that generation 1\u2019s budget has been reached. Over the several generation 0 collections, it\u2019s likely that a number of objects in generation 1 have become unreachable (as in our example). So this time, the garbage collector decides to examine all of the objects in generation 1 and generation</p>\n<p>0. After both generations have been garbage collected, the heap now looks like Figure 21-11.</p>\n<table border=\"1\">\n<tr>\n<td><p>D</p>\n</td>\n<td><p>F</p>\n</td>\n<td><p>I</p>\n</td>\n<td><p>N</p>\n</td>\n<td><p>O</p>\n</td>\n<td><p>Q</p>\n</td>\n<td><p>S</p>\n</td>\n<td></td>\n</tr>\n</table>\n<p>Generation 2 Gener- Generation 0 ation 1</p>\n</li>\n<li><p>FIGURE 21-11 After four collections: generation 1 survivors are promoted to generation 2, generation 0 survivors are promoted to generation 1, and generation 0 is empty.</p></li>\n</ul><p>As before, any objects that were in generation 0 that survived the garbage collection are now in generation 1; any objects that were in generation 1 that survived the collection are now in generation</p><ul style=\"list-style-type: none;\">\n<li><p>2. As always, generation 0 is empty immediately after a garbage collection and is where new objects will be allocated. Objects in generation 2 are objects that the garbage collector has examined two or more times. There might have been several collections, but the objects in generation 1 are examined only when generation 1 reaches its budget, which usually requires several garbage collections of generation 0.</p></li>\n</ul><p>The managed heap supports only three generations: generation 0, generation 1, and generation 2; there is no generation 3.3 When the CLR initializes, it selects budgets for all three generations. However, the CLR\u2019s garbage collector is a self-tuning collector. This means that the garbage collector learns about your application\u2019s behavior whenever it performs a garbage collection. For example, if your application constructs a lot of objects and uses them for a very short period of time, it\u2019s possible that garbage collecting generation 0 will reclaim a lot of memory. In fact, it\u2019s possible that the memory for all objects in generation 0 can be reclaimed.</p><p>If the garbage collector sees that there are very few surviving objects after collecting generation 0, it might decide to reduce the budget of generation 0. This reduction in the allotted space will mean that garbage collections occur more frequently but will require less work for the garbage collector, so your process\u2019s working set will be small. In fact, if all objects in generation 0 are garbage, a garbage collection doesn\u2019t have to compact any memory; it can simply set NextObjPtr back to the beginning of generation 0, and then the garbage collection is performed. Wow, this is a fast way to reclaim memory!</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile170.png\"/><p>Note The garbage collector works extremely well for applications with threads that sit idle at the top of their stack most of the time. Then, when the thread has something to do, it wakes up, creates a bunch of short-lived objects, returns, and then goes back to sleep. Many applications follow this architecture. For example, GUI applications tend to have the GUI thread sitting in a message loop most of its life. Occasionally, the user generates some input (like a touch, mouse, or keyboard event), the thread wakes up, processes the input and returns back to the message pump. Most objects created to process the input are probably garbage now.</p><p>Similarly, server applications tend to have thread pool threads sitting in the pool waiting for client requests to come in. When a client request comes in, new objects are created to perform work on behalf of the client request. When the result is sent back to the client, the thread returns to the thread pool and all the objects it created are garbage now.</p><p>On the other hand, if the garbage collector collects generation 0 and sees that there are a lot of surviving objects, not a lot of memory was reclaimed in the garbage collection. In this case, the garbage collector will grow generation 0\u2019s budget. Now, fewer collections will occur, but when they do, a lot more memory should be reclaimed. By the way, if insufficient memory has been reclaimed after a collection, the garbage collector will perform a full collection before throwing an OutOfMemoryException.</p><p>Throughout this discussion, I\u2019ve been talking about how the garbage collector dynamically modifies generation 0\u2019s budget after every collection. But the garbage collector also modifies the budgets of generation 1 and generation 2 by using similar heuristics. When these generations are garbage collected, the garbage collector again sees how much memory is reclaimed and how many objects survived. Based on the garbage collector\u2019s findings, it might grow or shrink the thresholds of these</p><p>3 The System.GC class\u2019s static MaxGeneration method returns 2.</p><p>generations as well to improve the overall performance of the application. The end result is that the garbage collector fine-tunes itself automatically based on the memory load required by your application\u2014this is very cool!</p><p>The following GCNotification class raises an event whenever a generation 0 or generation 2 collection occurs. With these events, you could have the computer beep whenever a collection occurs or you calculate how much time passes between collections, how much memory is allocated between collections, and more. With this class, you could easily instrument your application to get a better understanding of how your application uses memory.</p><p>public static class GCNotification { private static Action&lt;Int32&gt; s_gcDone = null; // The event's field</p><p>public static event Action&lt;Int32&gt; GCDone {</p><p>add { // If there were no registered delegates before, start reporting notifications now if (s_gcDone == null) { new GenObject(0); new GenObject(2); } s_gcDone += value;</p><p>} remove { s_gcDone \u00ad= value; }</p><p>}</p><p>private sealed class GenObject { private Int32 m_generation; public GenObject(Int32 generation) { m_generation = generation; } ~GenObject() { // This is the Finalize method</p><p>// If this object is in the generation we want (or higher), // notify the delegates that a GC just completed if (GC.GetGeneration(this) &gt;= m_generation) {</p><p>Action&lt;Int32&gt; temp = Volatile.Read(ref s_gcDone); if (temp != null) temp(m_generation);</p><p>}</p><p>} }</p><p>// Keep reporting notifications if there is at least one delegate registered, // the AppDomain isn't unloading, and the process isn\u2019t shutting down if ((s_gcDone != null)</p><p>&amp;&amp; !AppDomain.CurrentDomain.IsFinalizingForUnload() &amp;&amp; !Environment.HasShutdownStarted) {</p><p>// For Gen 0, create a new object; for Gen 2, resurrect the object // &amp; let the GC call Finalize again the next time Gen 2 is GC'd</p><p>if (m_generation == 0) new GenObject(0); else GC.ReRegisterForFinalize(this);</p><p>} else { /* Let the objects go away */ } }</p><h6>Garbage Collection Triggers</h6><p>As you know, the CLR triggers a GC when it detects that generation 0 has filled its budget. This is the most common trigger of a GC; however, there are additional GC triggers as listed here:</p><p>\u25a0\u25a0 Code explicitly calls System.GC\u2019s static Collect method Code can explicitly request that the CLR perform a collection. Although Microsoft strongly discourages such requests, at times it might make sense for an application to force a collection. I discuss this more in the \u201cForcing Garbage Collections\u201d section later in this chapter.</p><p>\u25a0\u25a0 Windows is reporting low memory conditions The CLR internally uses the Win32 CreateMemoryResourceNotification and QueryMemoryResourceNotification functions to monitor system memory overall. If Windows reports low memory, the CLR will force a garbage collection in an effort to free up dead objects to reduce the size of a process\u2019s working set.</p><p>\u25a0\u25a0 The CLR is unloading an AppDomain When an AppDomain unloads, the CLR considers nothing in the AppDomain to be a root, and a garbage collection consisting of all generations is performed. I\u2019ll discuss AppDomains in Chapter 22, \u201cCLR Hosting and AppDomains.\u201d</p><p>\u25a0\u25a0 The CLR is shutting down The CLR shuts down when a process terminates normally (as opposed to an external shutdown via Task Manager, for example). During this shutdown, the CLR considers nothing in the process to be a root; it allows objects a chance to clean up but the CLR does not attempt to compact or free memory because the whole process is terminating, and Windows will reclaim all of the processes\u2019 memory.</p><h6>Large Objects</h6><p>There is one more performance improvement you might want to be aware of. The CLR considers each single object to be either a small object or a large object. So far, in this chapter, I\u2019ve been focusing on small objects. Today, a large object is 85,000 bytes or more in size.4 The CLR treats large objects slightly differently than how it treats small objects:</p><p>\u25a0\u25a0 Large objects are not allocated within the same address space as small objects; they are al-</p><p>located elsewhere within the process\u2019 address space.</p><p>\u25a0\u25a0 Today, the GC doesn\u2019t compact large objects because of the time it would require to move them in memory. For this reason, address space fragmentation can occur between large objects within the process leading to an OutOfMemoryException being thrown. In a future version of the CLR, large objects may participate in compaction.</p><p>4 In the future, the CLR could change the number of bytes required to consider an object to be a large object. Do not count 85,000 being a constant.</p><p>\u25a0\u25a0 Large objects are immediately considered to be part of generation 2; they are never in generation 0 or 1. So, you should create large objects only for resources that you need to keep alive for a long time. Allocating short-lived large objects will cause generation 2 to be collected more frequently, hurting performance. Usually large objects are large strings (like XML or JSON) or byte arrays that you use for I/O operations, such as reading bytes from a file or network into a buffer so you can process it.</p><p>For the most part, large objects are transparent to you; you can simply ignore that they exist and that they get special treatment until you run into some unexplained situation in your program (like why you\u2019re getting address space fragmentation).</p><h6>Garbage Collection Modes</h6><p>When the CLR starts, it selects a GC mode, and this mode cannot change during the lifetime of the process. There are two basic GC modes:</p><p>\u25a0\u25a0 Workstation This mode fine-tunes the garbage collector for client-side applications. It is optimized to provide for low-latency GCs in order to minimize the time an application\u2019s threads are suspended so as not to frustrate the end user. In this mode, the GC assumes that other applications are running on the machine and does not hog CPU resources.</p><p>\u25a0\u25a0 Server This mode fine-tunes the garbage collector for server-side applications. It is optimized for throughput and resource utilization. In this mode, the GC assumes no other applications (client or server) are running on the machine, and it assumes that all the CPUs on the machine are available to assist with completing the GC. This GC mode causes the managed heap to be split into several sections, one per CPU. When a garbage collection is initiated, the garbage collector dedicates one special thread per CPU; each thread collects its own section in parallel with the other threads. Parallel collections work well for server applications in which the worker threads tend to exhibit uniform behavior. This feature requires the application to be running on a computer with multiple CPUs so that the threads can truly be working simultaneously to attain a performance improvement.</p><p>By default, applications run with the Workstation GC mode. A server application (such as ASP.NET or Microsoft SQL Server) that hosts the CLR can request the CLR to load the Server GC. However, if the server application is running on a uniprocessor machine, then the CLR will always use Workstation GC mode. A stand-alone application can tell the CLR to use the Server GC mode by creating a configuration file (as discussed in Chapter 2, \u201cBuilding, Packaging, Deploying, and Administering Applications and Types,\u201d and Chapter 3, \u201cShared Assemblies and Strongly Named Assemblies\u201d) that contains a gcServer element for the application. Here\u2019s an example of a configuration file.</p><p>&lt;configuration&gt; &lt;runtime&gt;</p><p>&lt;gcServer enabled=\"true\"/&gt; &lt;/runtime&gt;</p><p>&lt;/configuration&gt;</p><p>When an application is running, it can ask the CLR if it is running in the Server GC mode by querying the GCSettings class\u2019s IsServerGC read-only Boolean property.</p><p>using System; using System.Runtime; // GCSettings is in this namespace</p><p>public static class Program { public static void Main() {</p><p>Console.WriteLine(\"Application is running with server GC=\" + GCSettings.IsServerGC); }</p><p>}</p><p>In addition to the two modes, the GC can run in two sub-modes: concurrent (the default) or nonconcurrent. In concurrent mode, the GC has an additional background thread that marks objects concurrently while the application runs. When a thread allocates an object that pushes generation 0 over its budget, the GC first suspends all threads and then determines which generations to collect. If the garbage collector needs to collect generation 0 or 1, it proceeds as normal. However, if generation 2 needs collecting, the size of generation 0 will be increased beyond its budget to allocate the new object, and then the application\u2019s threads are resumed.</p><p>While the application\u2019s threads are running, the garbage collector has a normal priority background thread that finds unreachable objects. Once found, the garbage collector suspends all threads again and decides whether to compact memory. If the garbage collector decides to compact memory, memory is compacted, root references are fixed up, and the application\u2019s threads are resumed. This garbage collection takes less time than usual because the set of unreachable objects has already been built. However, the garbage collector might decide not to compact memory; in fact, the garbage collector favors this approach. If you have a lot of free memory, the garbage collector won\u2019t compact the heap; this improves performance but grows your application\u2019s working set. When using the concurrent garbage collector, you\u2019ll typically find that your application is consuming more memory than it would with the non-concurrent garbage collector.</p><p>You can tell the CLR not to use the concurrent collector by creating a configuration file for the application that contains a gcConcurrent element. Here\u2019s an example of a configuration file.</p><p>&lt;configuration&gt; &lt;runtime&gt;</p><p>&lt;gcConcurrent enabled=\"false\"/&gt;</p><p>&lt;/runtime&gt; &lt;/configuration&gt;</p><p>The GC mode is configured for a process and it cannot change while the process runs. However, your application can have some control over the garbage collection by using the GCSettings class\u2019s GCLatencyMode property. This read/write property can be set to any of the values in the GCLatencyMode enumerated type, as shown in Table 21-1.</p><p>The LowLatency mode requires some additional explanation. Typically, you would set this mode, perform a short-term, time-sensitive operation, and then set the mode back to either Batch or Interactive. While the mode is set to LowLatency, the GC will really avoid doing any generation 2</p><p>collections because these could take a long time. Of course, if you call GC.Collect(), then generation 2 still gets collected. Also, the GC will perform a generation 2 collection if Windows tells the CLR that system memory is low (see the \u201cGarbage Collection Triggers\u201d section earlier in this chapter).</p><figcaption>TABLE 21-1 Symbols Defined by the GCLatencyMode Enumerated Type</figcaption><table border=\"1\">\n<tr>\n<td><p>Symbol Name Batch (default for the Server GC mode)</p>\n</td>\n<td><p>Description Turns off the concurrent GC.</p>\n</td>\n</tr>\n<tr>\n<td><p>Interactive (default for the Workstation GC mode)</p>\n</td>\n<td><p>Turns on the concurrent GC.</p>\n</td>\n</tr>\n<tr>\n<td><p>LowLatency</p>\n</td>\n<td><p>Use this latency mode during short-term, time-sensitive operations (like drawing animations) where a generation 2 collection might be disruptive.</p>\n</td>\n</tr>\n<tr>\n<td><p>SustainedLowLatency</p>\n</td>\n<td><p>Use this latency mode to avoid long GC pauses for the bulk of your application\u2019s execution. This setting prevents all blocking generation 2 collections from occurring as long as memory is available. In fact, users of these applications would prefer to install more RAM in the machine in order to avoid GC pauses. A stock market application that must respond immediately to price changes is an example of this kind of application.</p>\n</td>\n</tr>\n</table><p>Under LowLatency mode, it is more likely that your application could get an OutOfMemoryException thrown. Therefore, stay in this mode for as short a time as possible, avoid allocating many objects, avoid allocating large objects, and set the mode back to Batch or Interactive by using a constrained execution region (CER), as discussed in Chapter 20, \u201cExceptions and State Management.\u201d Also, remember that the latency mode is a process-wide setting and threads may be running concurrently. These other threads could even change this setting while another thread is using it, so you may want to update some kind of counter (manipulated via Interlocked methods) when you have multiple threads manipulating this setting. Here is some code showing how to use the LowLatency mode.</p><p>private static void LowLatencyDemo() { GCLatencyMode oldMode = GCSettings.LatencyMode; System.Runtime.CompilerServices.RuntimeHelpers.PrepareConstrainedRegions(); try {</p><p>GCSettings.LatencyMode = GCLatencyMode.LowLatency; // Run your code here...</p><p>} finally {</p><p>GCSettings.LatencyMode = oldMode; }</p><p>}</p><h6>Forcing Garbage Collections</h6><p>The System.GC type allows your application some direct control over the garbage collector. For starters, you can query the maximum generation supported by the managed heap by reading the GC.MaxGeneration property; this property always returns 2.</p><p>You can also force the garbage collector to perform a collection by calling GC class\u2019s Collect method, optionally passing in a generation to collect up to, a GCCollectionMode, and a Boolean</p><p>indicating whether you want to perform a blocking (non-current) or background (concurrent) collection. Here is the signature of the most complex overload of the Collect method.</p><p>void Collect(Int32 generation, GCCollectionMode mode, Boolean blocking);</p><p>The GCCollectionMode type is an enum whose values are described in Table 21-2.</p><figcaption>TABLE 21-2 Symbols Defined by the GCCollectionMode Enumerated Type</figcaption><table border=\"1\">\n<tr>\n<td><p>Symbol Name</p>\n<p>Default</p>\n</td>\n<td><h6>Description</h6>\n<p>The same as calling GC.Collect with no flag. Today, this is the same as passing Forced, but this may change in a future version of the CLR.</p>\n</td>\n</tr>\n<tr>\n<td><p>Forced</p>\n</td>\n<td><p>Forces a collection to occur immediately for all generations up to and including the specified generation.</p>\n</td>\n</tr>\n<tr>\n<td><p>Optimized</p>\n</td>\n<td><p>The garbage collector will only perform a collection if the collection would be productive either by freeing a lot of memory or by reducing fragmentation. If the garbage collection would not be productive, then the call has no effect</p>\n</td>\n</tr>\n</table><p>Under most circumstances, you should avoid calling any of the Collect methods; it\u2019s best just to let the garbage collector run on its own accord and fine-tune its generation budgets based on actual application behavior. However, if you\u2019re writing a console user interface (CUI) or GUI application, your application code owns the process and the CLR in that process. For these application types, you might want to suggest a garbage collection to occur at certain times using a GCCollectionMode of Optimized. Normally, modes of Default and Forced are used for debugging, testing, and looking for memory leaks.</p><p>For example, you might consider calling the Collect method if some non-recurring event has just occurred that has likely caused a lot of old objects to die. The reason that calling Collect in such a circumstance may not be so bad is that the GC\u2019s predictions of the future based on the past are not likely to be accurate for non-recurring events. For example, it might make sense for your application to force a full GC of all generations after your application initializes or after the user saves a data file. Because calling Collect causes the generation budgets to adjust, do not call Collect to try to improve your application\u2019s response time; call it to reduce your process\u2019s working set.</p><p>For some applications (especially server applications that tend to keep a lot of objects in memory), the time required for the GC to do a full collection that includes generation 2 can be excessive. In fact, if the collection takes a very long time to complete, then client requests might time out. To help these kinds of applications, the GC class offers a RegisterForFullGCNotification method. Using this method and some additional helper methods (WaitForFullGCApproach, WaitForFullGCComplete, and CancelFullGCNotification), an application can now be notified when the garbage collector is getting close to performing a full collection. The application can then call GC.Collect to force a collection at a more opportune time, or the application could communicate with another server to better load balance the client requests. For more information, examine these methods and the \u201cGarbage Collection Notifications\u201d topic in the Microsoft .NET Framework SDK documentation. Note that you should always call the WaitForFullGCApproach and WaitForFullGCComplete methods in pairs because the CLR handles them as pairs internally.</p><h6>Monitoring Your Application\u2019s Memory Usage</h6><p>Within a process, there are a few methods that you can call to monitor the garbage collector. Specifically, the GC class offers the following static methods, which you can call to see how many collections have occurred of a specific generation or how much memory is currently being used by objects in the managed heap.</p><p>Int32 CollectionCount(Int32 generation); Int64 GetTotalMemory(Boolean forceFullCollection);</p><p>To profile a particular code block, I have frequently written code to call these methods before and after the code block and then calculate the difference. This gives me a very good indication of how my code block has affected my process\u2019s working set and indicates how many garbage collections occurred while executing the code block. If the numbers are high, I know to spend more time tuning the algorithms in my code block.</p><p>You can also see how much memory is being used by individual AppDomains as opposed to the whole process. For more information about this, see the \u201cAppDomain Monitoring\u201d section in Chapter 22.</p><figcaption>When you install the .NET Framework, it installs a set of performance counters that offer a lot of real-time statistics about the CLR\u2019s operations. These statistics are visible via the PerfMon.exe tool or the System Monitor ActiveX control that ships with Windows. The easiest way to access the System Monitor control is to run PerfMon.exe and click the + toolbar button, which causes the Add Counters dialog box shown in Figure 21-12 to appear.</figcaption><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile171.png\"/><ul style=\"list-style-type: none;\">\n<li><p>FIGURE 21-12 PerfMon.exe showing the .NET CLR Memory counters.</p></li>\n</ul><p>To monitor the CLR\u2019s garbage collector, select the .NET CLR Memory performance object. Then select a specific application from the instance list box. Finally, select the set of counters that you\u2019re interested in monitoring, click Add, and then click OK. At this point, the System Monitor will graph the</p><p>selected real-time statistics. For an explanation of a particular counter, select the desired counter and then select the Show Description check box.</p><p>Another great tool for analyzing the memory and performance of your application is PerfView. This tool can collect Event Tracing for Windows (ETW) logs and process them. The best way to acquire this tool is for you to search the web for PerfView. Finally, you should look into using the SOS Debugging Extension (SOS.dll), which can often offer great assistance when debugging memory problems and other CLR problems. For memory-related actions, the SOS Debugging Extension allows you to see how much memory is allocated within the process to the managed heap, displays all objects registered for finalization in the finalization queue, displays the entries in the GCHandle table per AppDomain or for the entire process, shows the roots that are keeping an object alive in the heap, and more.</p><h6>Working with Types Requiring Special Cleanup</h6><p>At this point, you should have a basic understanding of garbage collection and the managed heap, including how the garbage collector reclaims an object\u2019s memory. Fortunately for us, most types need only memory to operate. However, some types require more than just memory to be useful; some types require the use of a native resource in addition to memory.</p><p>The System.IO.FileStream type, for example, needs to open a file (a native resource) and store the file\u2019s handle. Then the type\u2019s Read and Write methods use this handle to manipulate the file. Similarly, the System.Threading.Mutex type opens a Windows mutex kernel object (a native resource) and stores its handle, using it when the Mutex\u2019s methods are called.</p><p>If a type wrapping a native resource gets GC\u2019d, the GC will reclaim the memory used by the object in the managed heap; but the native resource, which the GC doesn\u2019t know anything about, will be leaked. This is clearly not desirable, so the CLR offers a mechanism called finalization. Finalization allows an object to execute some code after the object has been determined to be garbage but before the object\u2019s memory is reclaimed from the managed heap. All types that wrap a native resourcesuch as a file, network connection, socket, or mutex\u2014support finalization. When the CLR determines that one of these objects is no longer reachable, the object gets to finalize itself, releasing the native resource it wraps, and then, later, the GC will reclaim the object from the managed heap.</p><p>System.Object, the base class of everything, defines a protected and virtual method called Finalize. When the garbage collector determines that an object is garbage, it calls the object\u2019s Finalize method (if it is overridden). Microsoft\u2019s C# team felt that Finalize methods were a special kind of method requiring special syntax in the programming language (similar to how C# requires special syntax to define a constructor). So, in C#, you must define a Finalize method by placing a tilde symbol (~) in front of the class name, as shown in the following code sample.</p><p>internal sealed class SomeType { // This is the Finalize method ~SomeType() {</p><p>// The code here is inside the Finalize method }</p><p>}</p><p>If you were to compile this code and examine the resulting assembly with ILDasm.exe, you\u2019d see that the C# compiler did, in fact, emit a protected override method named Finalize into the module\u2019s metadata. If you examined the Finalize method\u2019s IL code, you\u2019d also see that the code inside the method\u2019s body is emitted into a try block, and that a call to base.Finalize is emitted into a finally block.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile172.png\"/><p>Important If you\u2019re familiar with C++, you\u2019ll notice that the special syntax C# requires for defining a Finalize method looks just like the syntax you\u2019d use to define a C++ destructor. In fact, the C# Programming Language Specification calls this method a destructor. However, a Finalize method doesn\u2019t work like a C++ destructor at all, and this has caused a great deal of confusion for developers migrating from one language to another.</p><p>The problem is that developers mistakenly believe that using the C# destructor syntax means that the type\u2019s objects will be deterministically destructed when they go out of lexical scope, just as they would be in C++. However, the CLR doesn\u2019t support deterministic destruction, preventing C# from providing this mechanism.</p><p>Finalize methods are called at the completion of a garbage collection on objects that the GC has determined to be garbage. This means that the memory for these objects cannot be reclaimed right away because the Finalize method might execute code that accesses a field. Because a finalizable object must survive the collection, it gets promoted to another generation, forcing the object to live much longer than it should. This is not ideal in terms of memory consumption and is why you should avoid finalization when possible. To make matters worse, when finalizable objects get promoted, any object referred to by its fields also get promoted because they must continue to live too. So, try to avoid defining finalizable objects with reference type fields.</p><p>Furthermore, be aware of the fact that you have no control over when the Finalize method will execute. Finalize methods run when a garbage collection occurs, which may happen when your application requests more memory. Also, the CLR doesn\u2019t make any guarantees as to the order in which Finalize methods are called. So, you should avoid writing a Finalize method that accesses other objects whose type defines a Finalize method; those other objects could have been finalized already. However, it is perfectly OK to access value type instances or reference type objects that do not define a Finalize method. You also need to be careful when calling static methods because these methods can internally access objects that have been finalized, causing the behavior of the static method to become unpredictable.</p><p>The CLR uses a special, high-priority dedicated thread to call Finalize methods to avoid some deadlock scenarios that could occur otherwise.5 If a Finalize method blocks (for example, enters an infinite loop or waits for an object that is never signaled), this special thread can\u2019t call any more</p><p>Finalize methods. This is a very bad situation because the application will never be able to reclaim the memory occupied by the finalizable objects\u2014the application will leak memory as long as it runs.</p><p>5 A future version of the CLR might use multiple finalizer threads to improve performance.</p><p>If a Finalize method throws an unhandled exception, then the process terminates; there is no way to catch this exception.</p><p>So, as you can see, there are a lot of caveats related to Finalize methods and they must be used with caution. Specifically, they are designed for releasing native resources. To simplify working with them, it is highly recommended that developers avoid overriding Object\u2019s Finalize method; instead, use helper classes that Microsoft now provides in the Framework Class Library (FCL). The helper classes override Finalize and add some special CLR magic I\u2019ll talk about as we go on. You will then derive your own classes from the helper classes and inherit the CLR magic.</p><p>If you are creating a managed type that wraps a native resource, you should first derive a class from a special base class called System.Runtime.InteropServices.SafeHandle, which looks like the following (I\u2019ve added comments in the methods to indicate what they do).</p><p>public abstract class SafeHandle : CriticalFinalizerObject, IDisposable { // This is the handle to the native resource protected IntPtr handle;</p><p>protected SafeHandle(IntPtr invalidHandleValue, Boolean ownsHandle) { this.handle = invalidHandleValue; // If ownsHandle is true, then the native resource is closed when // this SafeHandle\u00adderived object is collected</p><p>}</p><p>protected void SetHandle(IntPtr handle) {</p><p>this.handle = handle; }</p><p>// You can explicitly release the resource by calling Dispose // This is the IDisposable interface\u2019s Dispose method public void Dispose() { Dispose(true); }</p><p>// The default Dispose implementation (shown here) is exactly what you want. // Overriding this method is strongly discouraged. protected virtual void Dispose(Boolean disposing) {</p><p>// The default implementation ignores the disposing argument. // If resource already released, return // If ownsHandle is false, return // Set flag indicating that this resource has been released // Call virtual ReleaseHandle method // Call GC.SuppressFinalize(this) to prevent Finalize from being called // If ReleaseHandle returned true, return // If we get here, fire ReleaseHandleFailed Managed Debugging Assistant (MDA)</p><p>}</p><p>// The default Finalize implementation (shown here) is exactly what you want. // Overriding this method is very strongly discouraged. ~SafeHandle() { Dispose(false); }</p><p>// A derived class overrides this method to implement the code that releases the resource protected abstract Boolean ReleaseHandle();</p><p>public void SetHandleAsInvalid() {</p><p>}</p><p>// Set flag indicating that this resource has been released // Call GC.SuppressFinalize(this) to prevent Finalize from being called</p><p>public Boolean IsClosed { get {</p><p>// Returns flag indicating whether resource was released }</p><p>}</p><p>public abstract Boolean IsInvalid { // A derived class overrides this property. // The implementation should return true if the handle's value doesn't // represent a resource (this usually means that the handle is 0 or \u00ad1)</p><p>get; }</p><p>}</p><p>// These three methods have to do with security and reference counting; // I'll talk about them at the end of this section public void DangerousAddRef(ref Boolean success) {...} public IntPtr DangerousGetHandle() {...} public void DangerousRelease() {...}</p><p>The first thing to notice about the SafeHandle class is that it is derived from CriticalFinalizerObject, which is defined in the System.Runtime.ConstrainedExecution namespace. The CLR treats this class and classes derived from it in a very special manner. In particular, the CLR endows this class with three cool features:</p><p>\u25a0\u25a0 The first time an object of any CriticalFinalizerObject-derived type is constructed,</p><p>the CLR immediately JIT-compiles all of the Finalize methods in the inheritance hierarchy. Compiling these methods upon object construction guarantees that the native resource will be released when the object is determined to be garbage. Without this eager compiling of the</p><p>Finalize method, it would be possible to allocate the native resource and use it, but not to get rid of it. Under low memory conditions, the CLR might not be able to find enough memory to compile the Finalize method, which would prevent it from executing, causing the native resource to leak. Or the resource might not be freed if the Finalize method contained code that referred to a type in another assembly, and the CLR failed to locate this other assembly.</p><p>\u25a0\u25a0 The CLR calls the Finalize method of CriticalFinalizerObject-derived types after calling the Finalize methods of non\u2013CriticalFinalizerObject-derived types. This ensures that managed resource classes that have a Finalize method can access Critical\u00ad</p><p>FinalizerObject-derived objects within their Finalize methods successfully. For example, the FileStream class\u2019s Finalize method can flush data from a memory buffer to an underlying disk with confidence that the disk file has not been closed yet.</p><p>\u25a0\u25a0 The CLR calls the Finalize method of CriticalFinalizerObject-derived types if an AppDomain is rudely aborted by a host application (such as SQL Server or ASP.NET). This also is part of ensuring that the native resource is released even in a case in which a host application no longer trusts the managed code running inside of it.</p><p>The second thing to notice about SafeHandle is that the class is abstract; it is expected that another class will be derived from SafeHandle, and this class will provide a constructor that invokes the protected constructor, the abstract method ReleaseHandle, and the abstract IsInvalid property get accessor method.</p><p>Most native resources are manipulated with handles (32-bit values on a 32-bit system and 64-bit values on a 64-bit system). So the SafeHandle class defines a protected IntPtr field called handle. In Windows, most handles are invalid if they have a value of 0 or \u00ad1. The Microsoft.Win32.SafeHandles namespace contains another helper class called SafeHandleZeroOrMinusOneIsInvalid, which looks like this.</p><p>public abstract class SafeHandleZeroOrMinusOneIsInvalid : SafeHandle { protected SafeHandleZeroOrMinusOneIsInvalid(Boolean ownsHandle)</p><p>: base(IntPtr.Zero, ownsHandle) { }</p><p>}</p><p>public override Boolean IsInvalid {</p><p>get { if (base.handle == IntPtr.Zero) return true; if (base.handle == (IntPtr) (\u00ad1)) return true; return false;</p><p>} }</p><p>Again, you\u2019ll notice that the SafeHandleZeroOrMinusOneIsInvalid class is abstract, and therefore, another class must be derived from this one to override the protected constructor and the abstract method ReleaseHandle. The .NET Framework provides just a few public classes derived from SafeHandleZeroOrMinusOneIsInvalid, including SafeFileHandle, SafeRegistryHandle, SafeWaitHandle, and SafeMemoryMappedViewHandle. Here is what the SafeFileHandle class looks like.</p><p>public sealed class SafeFileHandle : SafeHandleZeroOrMinusOneIsInvalid {</p><p>public SafeFileHandle(IntPtr preexistingHandle, Boolean ownsHandle) : base(ownsHandle) { base.SetHandle(preexistingHandle);</p><p>}</p><p>protected override Boolean ReleaseHandle() { // Tell Windows that we want the native resource closed. return Win32Native.CloseHandle(base.handle);</p><p>} }</p><p>The SafeWaitHandle class is implemented similarly to the SafeFileHandle class just shown. The only reason why there are different classes with similar implementations is to achieve type safety; the compiler won\u2019t let you use a file handle as an argument to a method that expects a wait handle, and vice versa. The SafeRegistryHandle class\u2019s ReleaseHandle method calls the Win32 RegCloseKey function.</p><p>It would be nice if the .NET Framework included additional classes that wrap various native resources. For example, one could imagine classes such as SafeProcessHandle, SafeThreadHandle, SafeTokenHandle, SafeLibraryHandle (its ReleaseHandle method would call the Win32 FreeLibrary function), SafeLocalAllocHandle (its ReleaseHandle method would call the Win32 LocalFree function), and so on.</p><p>All of the classes just listed (and more) actually do ship with the Framework Class Library (FCL). However, these classes are not publicly exposed; they are all internal to the assemblies that define them. Microsoft didn\u2019t expose these classes publicly because they didn\u2019t want to document them and do full testing of them. However, if you need any of these classes for your own work, I\u2019d recommend that you use a tool such as ILDasm.exe or some IL decompiler tool to extract the code for these classes and integrate that code into your own project\u2019s source code. All of these classes are trivial to implement, and writing them yourself from scratch would also be quite easy.</p><p>The SafeHandle-derived classes are extremely useful because they ensure that the native resource is freed when a GC occurs. In addition to what we\u2019ve already discussed, SafeHandle offers two more capabilities. First, the CLR gives SafeHandle-derived types special treatment when used in scenarios in which you are interoperating with native code. For example, let\u2019s examine the following code.</p><p>using System; using System.Runtime.InteropServices; using Microsoft.Win32.SafeHandles;</p><p>internal static class SomeType { [DllImport(\"Kernel32\", CharSet=CharSet.Unicode, EntryPoint=\"CreateEvent\")] // This prototype is not robust private static extern IntPtr CreateEventBad(</p><p>IntPtr pSecurityAttributes, Boolean manualReset, Boolean initialState, String name);</p><p>// This prototype is robust [DllImport(\"Kernel32\", CharSet=CharSet.Unicode, EntryPoint=\"CreateEvent\")] private static extern SafeWaitHandle CreateEventGood(</p><p>IntPtr pSecurityAttributes, Boolean manualReset, Boolean initialState, String name);</p><p>public static void SomeMethod() { IntPtr handle = CreateEventBad(IntPtr.Zero, false, false, null); SafeWaitHandle swh = CreateEventGood(IntPtr.Zero, false, false, null);</p><p>} }</p><p>You\u2019ll notice that the CreateEventBad method is prototyped as returning an IntPtr, which will return the handle back to managed code; however, interoperating with native code this way is not robust. You see, after CreateEventBad is called (which creates the native event resource), it is possible that a ThreadAbortException could be thrown prior to the handle being assigned to the handle variable. In the rare cases when this would happen, the managed code would leak the native resource. The only way to get the event closed is to terminate the whole process.</p><p>The SafeHandle class fixes this potential resource leak. Notice that the CreateEventGood method is prototyped as returning a SafeWaitHandle (instead of an IntPtr). When CreateEventGood is called, the CLR calls the Win32 CreateEvent function. As the CreateEvent function returns to managed code, the CLR knows that SafeWaitHandle is derived from SafeHandle, causing the CLR to automatically construct an instance of the SafeWaitHandle class on the managed heap, passing in the handle value returned from CreateEvent. The constructing of the SafeWaitHandle object and the assignment of the handle happen in native code now, which cannot be interrupted by a ThreadAbortException. Now, it is impossible for managed code to leak this native resource. Eventually, the SafeWaitHandle object will be garbage collected and its Finalize method will be called, ensuring that the resource is released.</p><p>One last feature of SafeHandle-derived classes is that they prevent someone from trying to exploit a potential security hole. The problem is that one thread could be trying to use a native resource while another thread tries to free the resource. This could manifest itself as a handle-recycling exploit. The SafeHandle class prevents this security vulnerability by using reference counting. Internally, the SafeHandle class defines a private field that maintains a count. When a SafeHandle-derived object</p><p>is set to a valid handle, the count is set to 1. Whenever a SafeHandle-derived object is passed as an argument to a native method, the CLR knows to automatically increment the counter. Likewise, when the native method returns to managed code, the CLR knows to decrement the counter. For example, you would prototype the Win32 SetEvent function as follows.</p><p>[DllImport(\"Kernel32\", ExactSpelling=true)] private static extern Boolean SetEvent(SafeWaitHandle swh);</p><p>Now when you call this method passing in a reference to a SafeWaitHandle object, the CLR will increment the counter just before the call and decrement the counter just after the call. Of course, the manipulation of the counter is performed in a thread-safe fashion. How does this improve security? Well, if another thread tries to release the native resource wrapped by the SafeHandle object, the CLR knows that it cannot actually release it because the resource is being used by a native function. When the native function returns, the counter is decremented to 0, and the resource will be released.</p><p>If you are writing or calling code to manipulate a handle as an IntPtr, you can access it out of a SafeHandle object, but you should manipulate the reference counting explicitly. You accomplish this via SafeHandle\u2019s DangerousAddRef and DangerousRelease methods. You gain access to the raw handle via the DangerousGetHandle method.</p><p>I would be remiss if I didn\u2019t mention that the System.Runtime.InteropServices namespace also defines a CriticalHandle class. This class works exactly as the SafeHandle class in all ways except that it does not offer the reference-counting feature. The CriticalHandle class and the classes derived from it sacrifice security for better performance when you use it (because counters don\u2019t get manipulated). As does SafeHandle, the CriticalHandle class has two types derived from it:</p><p>CriticalHandleMinusOneIsInvalid and CriticalHandleZeroOrMinusOneIsInvalid. Because Microsoft favors a more secure system over a faster system, the class library includes no types derived from either of these two classes. For your own work, I would recommend that you use Critical\u00ad</p><p>Handle-derived types only if performance is an issue. If you can justify reducing security, you can switch to a CriticalHandle-derived type.</p><h6>Using a Type That Wraps a Native Resource</h6><p>Now that you know how to define a SafeHandle-derived class that wraps a native resource, let\u2019s take a look at how a developer uses it. Let\u2019s start by talking about the common System.IO.FileStream class. The FileStream class offers the ability to open a file, read bytes from the file, write bytes to the file, and close the file. When a FileStream object is constructed, the Win32 CreateFile function is called, the returned handle is saved in a SafeFileHandle object, and a reference to this object is maintained via a private field in the FileStream object. The FileStream class also offers several additional properties (such as Length, Position, CanRead) and methods (such as Read, Write, Flush).</p><p>Let\u2019s say that you want to write some code that creates a temporary file, writes some bytes to the file, and then deletes the file. You might start writing the code like this.</p><p>using System; using System.IO;</p><p>public static class Program { public static void Main() {</p><p>// Create the bytes to write to the temporary file. Byte[] bytesToWrite = new Byte[] { 1, 2, 3, 4, 5 };</p><p>// Create the temporary file. FileStream fs = new FileStream(\"Temp.dat\", FileMode.Create);</p><p>// Write the bytes to the temporary file. fs.Write(bytesToWrite, 0, bytesToWrite.Length);</p><p>} }</p><p>// Delete the temporary file. File.Delete(\"Temp.dat\"); // Throws an IOException</p><p>Unfortunately, if you build and run this code, it might work, but most likely it won\u2019t. The problem is that the call to File\u2019s static Delete method requests that Windows delete a file while it is still open. So Delete throws a System.IO.IOException exception with the following string message: The process cannot access the file \"Temp.dat\" because it is being used by another process.</p><p>Be aware that in some cases, the file might actually be deleted! If another thread somehow caused a garbage collection to start after the call to Write and before the call to Delete, the FileStream\u2019s SafeFileHandle field would have its Finalize method called, which would close the file and allow Delete to work. The likelihood of this situation is extremely rare, however, and therefore the previous</p><p>code will fail more than 99 percent of the time.</p><p>Classes that allow the consumer to control the lifetime of native resources it wraps implement the IDisposable interface, which looks like this.</p><p>public interface IDisposable {</p><p>void Dispose(); }</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile173.png\"/><p>Important If a class defines a field in which the field\u2019s type implements the dispose pattern, the class itself should also implement the dispose pattern. The Dispose method should dispose of the object referred to by the field. This allows someone using the class to call Dispose on it, which in turn releases the resources used by the object itself.</p><p>Fortunately, the FileStream class implements the IDisposable interface and its implementation</p><p>internally calls Dispose on the FileStream object\u2019s private SafeFileHandle field. Now, we can modify our code to explicitly close the file when we want to as opposed to waiting for some GC to happen in the future. Here\u2019s the corrected source code.</p><p>using System; using System.IO;</p><p>public static class Program { public static void Main() {</p><p>// Create the bytes to write to the temporary file. Byte[] bytesToWrite = new Byte[] { 1, 2, 3, 4, 5 };</p><p>// Create the temporary file. FileStream fs = new FileStream(\"Temp.dat\", FileMode.Create);</p><p>// Write the bytes to the temporary file. fs.Write(bytesToWrite, 0, bytesToWrite.Length);</p><p>// Explicitly close the file when finished writing to it. fs.Dispose();</p><p>} }</p><p>// Delete the temporary file. File.Delete(\"Temp.dat\"); // This always works now.</p><p>Now, when File\u2019s Delete method is called, Windows sees that the file isn\u2019t open and successfully deletes it.</p><p>Keep in mind that calling Dispose is not required to guarantee native resource cleanup. Native resource cleanup will always happen eventually; calling Dispose lets you control when that cleanup happens. Also, calling Dispose does not delete the managed object from the managed heap. The only way to reclaim memory in the managed heap is for a garbage collection to kick in. This means you can still call methods on the managed object even after you dispose of any native resources it may have been using.</p><p>The following code calls the Write method after the file is closed, attempting to write more bytes to the file. Obviously, the bytes can\u2019t be written, and when the code executes, the second call to the Write method throws a System.ObjectDisposedException exception with the following string message: Cannot access a closed file.</p><p>using System; using System.IO;</p><p>public static class Program { public static void Main() {</p><p>// Create the bytes to write to the temporary file. Byte[] bytesToWrite = new Byte[] { 1, 2, 3, 4, 5 };</p><p>// Create the temporary file. FileStream fs = new FileStream(\"Temp.dat\", FileMode.Create);</p><p>// Write the bytes to the temporary file. fs.Write(bytesToWrite, 0, bytesToWrite.Length);</p><p>// Explicitly close the file when finished writing to it. fs.Dispose();</p><p>// Try to write to the file after closing it. fs.Write(bytesToWrite, 0, bytesToWrite.Length); // Throws ObjectDisposedException</p><p>} }</p><p>// Delete the temporary file. File.Delete(\"Temp.dat\");</p><p>Note that no memory corruption occurs here because the memory for the FileStream object still exists in the managed heap; it\u2019s just that the object can\u2019t successfully execute its methods after it is explicitly disposed.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile174.png\"/><p>Important When defining your own type that implements the IDisposable interface, be sure to write code in all of your methods and properties to throw a System.ObjectDisposedException if the object has been explicitly cleaned up. A Dispose method should never throw an exception; if it\u2019s called multiple times, it should just return.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile175.png\"/><p>Important In general, I strongly discourage explicitly calling Dispose in your code. The reason is that the CLR\u2019s garbage collector is well written, and you should let it do its job. The garbage collector knows when an object is no longer accessible from application code, and only then will it collect the object.6 When application code calls Dispose, it is effectively saying that it knows when the application no longer has a need for the object. For many applications, it is impossible to know for sure when an object is no longer required.</p><p>For example, if you have code that constructs a new object, and you then pass a reference to this object to another method, the other method could save a reference to the object in some internal field variable (a root). There is no way for the calling method to know that this has happened. Sure, the calling method can call Dispose, but later, some other code might try to access the object, causing an ObjectDisposedException to be thrown. I recommend that you call Dispose only at places in your code where you know you must clean up the resource (as in the case of attempting to delete an open file).</p><p>Along the same lines, it is possible to have multiple threads call Dispose on a single object simultaneously. However, the design guidelines state that Dispose does not have to be thread-safe. The reason is because code should be calling Dispose only if the code knows for a fact that no other thread is using the object.</p><p>The previous code examples show how to explicitly call a type\u2019s Dispose method. If you decide to call Dispose explicitly, I highly recommend that you place the call in an exception-handling finally block. This way, the cleanup code is guaranteed to execute. So it would be better to write the previous code example as follows.</p><p>using System; using System.IO;</p><p>public static class Program { public static void Main() {</p><p>// Create the bytes to write to the temporary file. Byte[] bytesToWrite = new Byte[] { 1, 2, 3, 4, 5 };</p><p>// Create the temporary file. FileStream fs = new FileStream(\"Temp.dat\", FileMode.Create); try {</p><p>// Write the bytes to the temporary file. fs.Write(bytesToWrite, 0, bytesToWrite.Length);</p><p>} finally {</p><p>// Explicitly close the file when finished writing to it.</p><p>6 There are many nice features about a garbage collected system: no memory leaks, no memory corruption, no address space fragmentation, and a reduced working set. And now, a new one: synchronization. That\u2019s right, you can use the GC as a thread synchronization mechanism. Question: How can you know when all threads are done using an object? Answer: the GC finalizes the object. There is nothing wrong with taking advantage of all the GC features as you architect your software.</p><p>if (fs != null) fs.Dispose(); }</p><p>} }</p><p>// Delete the temporary file. File.Delete(\"Temp.dat\");</p><p>Adding the exception-handling code is the right thing to do, and you must have the diligence to do it. Fortunately, the C# language provides a using statement, which offers a simplified syntax that produces code identical to the code just shown. Here\u2019s how the preceding code would be rewritten using C#\u2019s using statement.</p><p>using System; using System.IO;</p><p>public static class Program { public static void Main() {</p><p>// Create the bytes to write to the temporary file. Byte[] bytesToWrite = new Byte[] { 1, 2, 3, 4, 5 };</p><p>// Create the temporary file. using (FileStream fs = new FileStream(\"Temp.dat\", FileMode.Create)) {</p><p>// Write the bytes to the temporary file. fs.Write(bytesToWrite, 0, bytesToWrite.Length);</p><p>}</p><p>} }</p><p>// Delete the temporary file. File.Delete(\"Temp.dat\");</p><p>In the using statement, you initialize an object and save its reference in a variable. Then you ac-</p><p>cess the variable via code contained inside using\u2019s braces. When you compile this code, the compiler automatically emits the try and finally blocks. Inside the finally block, the compiler emits code to cast the object to an IDisposable and calls the Dispose method. Obviously, the compiler allows the using statement to be used only with types that implement the IDisposable interface.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile176.png\"/><p>Note C#\u2019s using statement supports the capability to initialize multiple variables as long as the variables are all of the same type. It also supports the capability to use just an already initialized variable. For more information about this topic, refer to the \u201cUsing Statements\u201d topic in the C# Programmer\u2019s Reference.</p><h6>An Interesting Dependency Issue</h6><p>The System.IO.FileStream type allows the user to open a file for reading and writing. To improve performance, the type\u2019s implementation makes use of a memory buffer. Only when the buffer fills does the type flush the contents of the buffer to the file. A FileStream supports the writing of bytes only. If you want to write characters and strings, you can use a System.IO.StreamWriter, as is demonstrated in the following code.</p><p>FileStream fs = new FileStream(\"DataFile.dat\", FileMode.Create); StreamWriter sw = new StreamWriter(fs); sw.Write(\"Hi there\");</p><p>// The following call to Dispose is what you should do. sw.Dispose(); // NOTE: StreamWriter.Dispose closes the FileStream; // the FileStream doesn't have to be explicitly closed.</p><p>Notice that the StreamWriter\u2019s constructor takes a reference to a Stream object as a parameter, allowing a reference to a FileStream object to be passed as an argument. Internally, the StreamWriter object saves the Stream\u2019s reference. When you write to a StreamWriter object, it internally buffers the data in its own memory buffer. When the buffer is full, the StreamWriter object writes the data to the Stream.</p><p>When you\u2019re finished writing data via the StreamWriter object, you should call Dispose. (Because the StreamWriter type implements the IDisposable interface, you can also use it with C#\u2019s using statement.) This causes the StreamWriter object to flush its data to the Stream object and close the Stream object.7</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile177.png\"/><p>Note You don\u2019t have to explicitly call Dispose on the FileStream object because the</p><p>StreamWriter calls it for you. However, if you do call Dispose explicitly, the FileStream will see that the object has already been cleaned up\u2014the method does nothing and just returns.</p><p>What do you think would happen if there were no code to explicitly call Dispose? Well, at some point, the garbage collector would correctly detect that the objects were garbage and finalize them. But the garbage collector doesn\u2019t guarantee the order in which objects are finalized. So if the FileStream object were finalized first, it would close the file. Then when the StreamWriter object was finalized, it would attempt to write data to the closed file, throwing an exception. If, on the other hand, the StreamWriter object were finalized first, the data would be safely written to the file.</p><p>7 You can override this behavior by calling StreamWriter\u2019s constructor that accepts a Boolean leaveOpen parameter.</p><p>How was Microsoft to solve this problem? Making the garbage collector finalize objects in a specific order would have been impossible because objects could contain references to each other, and there would be no way for the garbage collector to correctly guess the order in which to finalize these objects. Here is Microsoft\u2019s solution: the StreamWriter type does not support finalization, and therefore it never flushes data in its buffer to the underlying FileStream object. This means that if you forget to explicitly call Dispose on the StreamWriter object, data is guaranteed to be lost. Microsoft expects developers to see this consistent loss of data and fix the code by inserting an explicit call to Dispose.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile178.png\"/><p>Note The .NET Framework offers a feature called Managed Debugging Assistants (MDAs). When an MDA is enabled, the .NET Framework looks for certain common programmer errors and fires a corresponding MDA. In the debugger, it looks like an exception has been thrown. There is an MDA available to detect when a StreamWriter object is garbage collected without previously having been explicitly disposed. To enable this MDA in Microsoft Visual Studio, open your project and select the Debug.Exceptions menu item. In the Exceptions dialog box, expand the Managed Debugging Assistants node and scroll to the bottom. There you will see the StreamWriterBufferredDataLost MDA. Select the Thrown check box to have the Visual Studio debugger stop whenever a StreamWriter object\u2019s data is lost.</p><h6>Other GC Features for Use with Native Resources</h6><p>Sometimes, a native resource consumes a lot of memory, but the managed object wrapping that resource occupies very little memory. The quintessential example of this is the bitmap. A bitmap can occupy several megabytes of native memory, but the managed object is tiny because it contains only an HBITMAP (a 4-byte or 8-byte value). From the CLR\u2019s perspective, a process could allocate hundreds of bitmaps (using little managed memory) before performing a collection. But if the process is manipulating many bitmaps, the process\u2019s memory consumption will grow at a phenomenal rate. To fix this situation, the GC class offers the following two static methods.</p><p>public static void AddMemoryPressure(Int64 bytesAllocated); public static void RemoveMemoryPressure(Int64 bytesAllocated);</p><p>A class that wraps a potentially large native resource should use these methods to give the garbage collector a hint as to how much memory is really being consumed. Internally, the garbage collector monitors this pressure, and when it gets high, a garbage collection is forced.</p><p>There are some native resources that are fixed in number. For example, Windows formerly had a restriction that it could create only five device contexts. There had also been a restriction on the number of files that an application could open. Again, from the CLR\u2019s perspective, a process could allocate hundreds of objects (that use little memory) before performing a collection. But if the number of these native resources is limited, attempting to use more than are available will typically result in exceptions being thrown.</p><p>To fix this situation, the System.Runtime.InteropServices namespace offers the HandleCollector class.</p><p>public sealed class HandleCollector { public HandleCollector(String name, Int32 initialThreshold); public HandleCollector(String name, Int32 initialThreshold, Int32 maximumThreshold); public void Add(); public void Remove();</p><p>}</p><p>public Int32 Count { get; } public Int32 InitialThreshold { get; } public Int32 MaximumThreshold { get; } public String Name { get; }</p><p>A class that wraps a native resource that has a limited quantity available should use an instance of this class to give the garbage collector a hint as to how many instances of the resource are really being consumed. Internally, this class object monitors the count, and when it gets high, a garbage collection is forced.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile179.png\"/><p>Note Internally, the GC.AddMemoryPressure and HandleCollector.Add methods call GC.Collect, forcing a garbage collection to start prior to generation 0 reaching its budget. Normally, forcing a garbage collection to start is strongly discouraged, because it usually has an adverse effect on your application\u2019s performance. However, classes that call these methods are doing so in an effort to keep limited native resources available for the application. If the native resources run out, the application will fail. For most applications, it is better to work with reduced performance than to not be working at all.</p><p>Here is some code that demonstrates the use and effect of the memory pressure methods and the HandleCollector class.</p><p>using System; using System.Runtime.InteropServices;</p><p>public static class Program {</p><p>public static void Main() { MemoryPressureDemo(0); // 0 causes infrequent GCs MemoryPressureDemo(10 * 1024 * 1024); // 10MB causes frequent GCs</p><p>HandleCollectorDemo(); }</p><p>private static void MemoryPressureDemo(Int32 size) { Console.WriteLine(); Console.WriteLine(\"MemoryPressureDemo, size={0}\", size); // Create a bunch of objects specifying their logical size for (Int32 count = 0; count &lt; 15; count++) {</p><p>new BigNativeResource(size); }</p><p>}</p><p>// For demo purposes, force everything to be cleaned\u00adup GC.Collect();</p><p>private sealed class BigNativeResource { private readonly Int32 m_size;</p><p>public BigNativeResource(Int32 size) { m_size = size; // Make the GC think the object is physically bigger if (m_size &gt; 0) GC.AddMemoryPressure(m_size); Console.WriteLine(\"BigNativeResource create.\");</p><p>}</p><p>~BigNativeResource() { // Make the GC think the object released more memory if (m_size &gt; 0) GC.RemoveMemoryPressure(m_size); Console.WriteLine(\"BigNativeResource destroy.\");</p><p>} }</p><p>private static void HandleCollectorDemo() { Console.WriteLine(); Console.WriteLine(\"HandleCollectorDemo\"); for (Int32 count = 0; count &lt; 10; count++) new LimitedResource();</p><p>}</p><p>// For demo purposes, force everything to be cleaned\u00adup GC.Collect();</p><p>private sealed class LimitedResource { // Create a HandleCollector telling it that collections should // occur when two or more of these objects exist in the heap private static readonly HandleCollector s_hc = new HandleCollector(\"LimitedResource\", 2);</p><p>}</p><p>public LimitedResource() { // Tell the HandleCollector a LimitedResource has been added to the heap s_hc.Add(); Console.WriteLine(\"LimitedResource create. Count={0}\", s_hc.Count);</p><p>} ~LimitedResource() {</p><p>// Tell the HandleCollector a LimitedResource has been removed from the heap s_hc.Remove(); Console.WriteLine(\"LimitedResource destroy. Count={0}\", s_hc.Count);</p><p>} }</p><p>If you compile and run the preceding code, your output will be similar to the following output.</p><p>MemoryPressureDemo, size=0 BigNativeResource create. BigNativeResource create. BigNativeResource create. BigNativeResource create. BigNativeResource create. BigNativeResource create. BigNativeResource create. BigNativeResource create. BigNativeResource create. BigNativeResource create. BigNativeResource create. BigNativeResource create. BigNativeResource create. BigNativeResource create. BigNativeResource create. BigNativeResource destroy. BigNativeResource destroy. BigNativeResource destroy. BigNativeResource destroy. BigNativeResource destroy. BigNativeResource destroy. BigNativeResource destroy. BigNativeResource destroy. BigNativeResource destroy. BigNativeResource destroy. BigNativeResource destroy. BigNativeResource destroy. BigNativeResource destroy. BigNativeResource destroy. BigNativeResource destroy.</p><p>MemoryPressureDemo, size=10485760 BigNativeResource create. BigNativeResource create. BigNativeResource create. BigNativeResource create. BigNativeResource create. BigNativeResource create. BigNativeResource create. BigNativeResource create. BigNativeResource create. BigNativeResource create. BigNativeResource create. BigNativeResource create. BigNativeResource destroy.</p><p>BigNativeResource destroy. BigNativeResource destroy. BigNativeResource destroy. BigNativeResource destroy. BigNativeResource destroy. BigNativeResource destroy. BigNativeResource create. BigNativeResource create. BigNativeResource create. BigNativeResource destroy. BigNativeResource destroy. BigNativeResource destroy. BigNativeResource destroy. BigNativeResource destroy. BigNativeResource destroy. BigNativeResource destroy. BigNativeResource destroy.</p><p>HandleCollectorDemo LimitedResource create. Count=1 LimitedResource create. Count=2 LimitedResource create. Count=3 LimitedResource destroy. Count=3 LimitedResource destroy. Count=2</p><ul style=\"list-style-type: none;\">\n<li><p>LimitedResource destroy. Count=1</p><ul style=\"list-style-type: none;\">\n<li><p>LimitedResource create. Count=1</p></li>\n<li><p>LimitedResource create. Count=2</p></li>\n<li><p>LimitedResource create. Count=3</p></li>\n</ul>\n</li>\n<li><p>LimitedResource destroy. Count=2 LimitedResource create. Count=3</p></li>\n<li><p>LimitedResource destroy. Count=3 LimitedResource destroy. Count=2 LimitedResource destroy. Count=1 LimitedResource create. Count=1 LimitedResource create. Count=2 LimitedResource create. Count=3 LimitedResource destroy. Count=2 LimitedResource destroy. Count=1 LimitedResource destroy. Count=0</p></li>\n</ul><h6>Finalization Internals</h6><p>On the surface, finalization seems pretty straightforward: you create an object and its Finalize method is called when it is collected. But after you dig in, finalization is more complicated than this.</p><p>When an application creates a new object, the new operator allocates the memory from the heap. If the object\u2019s type defines a Finalize method, a pointer to the object is placed on the finalization list just before the type\u2019s instance constructor is called. The finalization list is an internal data structure controlled by the garbage collector. Each entry in the list points to an object that should have its Finalize method called before the object\u2019s memory can be reclaimed.</p><p>Figure 21-13 shows a heap containing several objects. Some of these objects are reachable from application roots, and some are not. When objects C, E, F, I, and J were created, the system detected</p><p>that these objects\u2019 types defined a Finalize method and so added references to these objects to the finalization list.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile180.png\"/><p>Roots: Fields &amp; variables</p><p>Managed heap</p><table border=\"1\">\n<tr>\n<td><p>A</p>\n</td>\n<td><p>B C D E F</p>\n</td>\n<td><p>G</p>\n</td>\n<td><p>H I J</p>\n</td>\n</tr>\n</table><p>C</p><p>E F I J</p><p>F</p><p>Finalization list</p><p>Freachable queue</p><ul style=\"list-style-type: none;\">\n<li><p>FIGURE 21-13 The managed heap showing pointers in its finalization list.</p><p>Note Even though System.Object defines a Finalize method, the CLR knows to ignore it; that is, when constructing an instance of a type, if the type\u2019s Finalize method is the one inherited from System.Object, the object isn\u2019t considered finalizable. One of the derived types must override Object\u2019s Finalize method.</p>\n<p>When a garbage collection occurs, objects B, E, G, H, I, and J are determined to be garbage. The garbage collector scans the finalization list looking for references to these objects. When a reference is found, the reference is removed from the finalization list and appended to the freachable queue. The freachable queue (pronounced \u201cF-reachable\u201d) is another of the garbage collector\u2019s internal data structures. Each reference in the freachable queue identifies an object that is ready to have its Finalize method called. After the collection, the managed heap looks like Figure 21-14.</p>\n<p>Finalization list</p>\n<p>Managed heap</p>\n<table border=\"1\">\n<tr>\n<td><p>A</p>\n</td>\n<td><p>C D E F I J</p>\n</td>\n</tr>\n</table>\n<p>C F E I J</p>\n<p>Freachable queue</p>\n<p>Roots: Fields &amp; variables</p>\n</li>\n<li><p>FIGURE 21-14 The managed heap showing pointers that moved from the finalization list to the freachable queue.</p></li>\n</ul><p>In this figure, you see that the memory occupied by objects B, G, and H has been reclaimed because these objects didn\u2019t have a Finalize method. However, the memory occupied by objects E, I, and J couldn\u2019t be reclaimed because their Finalize methods haven\u2019t been called yet.</p><p>A special high-priority CLR thread is dedicated to calling Finalize methods. A dedicated thread is used to avoid potential thread synchronization situations that could arise if one of the application\u2019s normal-priority threads were used instead. When the freachable queue is empty (the usual case), this thread sleeps. But when entries appear, this thread wakes, removes each entry from the queue, and then calls each object\u2019s Finalize method. Because of the way this thread works, you shouldn\u2019t execute any code in a Finalize method that makes any assumptions about the thread that\u2019s executing the code. For example, avoid accessing thread-local storage in the Finalize method.</p><p>In the future, the CLR may use multiple finalizer threads. So you should avoid writing any code that assumes that Finalize methods will be called serially. With just one finalizer thread, there could be performance and scalability issues in the scenario in which you have multiple CPUs allocating finalizable objects but only one thread executing Finalize methods\u2014the one thread might not be able to keep up with the allocations.</p><p>The interaction between the finalization list and the freachable queue is fascinating. First, I\u2019ll tell you how the freachable queue got its name. Well, the \u201cf\u201d is obvious and stands for finalization; every entry in the freachable queue is a reference to an object in the managed heap that should have its Finalize method called. But the reachable part of the name means that the objects are reachable. To put it another way, the freachable queue is considered a root, just as static fields are roots. So a reference in the freachable queue keeps the object it refers to reachable and is not garbage.</p><p>In short, when an object isn\u2019t reachable, the garbage collector considers the object to be garbage. Then when the garbage collector moves an object\u2019s reference from the finalization list to the freachable queue, the object is no longer considered garbage and its memory can\u2019t be reclaimed. When an object is garbage and then not garbage, we say that the object has been resurrected.</p><p>As freachable objects are marked, objects referred to by their reference type fields are also marked recursively; all these objects must get resurrected in order to survive the collection. At this point, the garbage collector has finished identifying garbage. Some of the objects identified as garbage have been resurrected. The garbage collector compacts the reclaimable memory, which promotes the resurrected object to an older generation (not ideal). And now, the special finalization thread empties the freachable queue, executing each object\u2019s Finalize method.</p><p>The next time the garbage collector is invoked on the older generation, it will see that the finalized objects are truly garbage because the application\u2019s roots don\u2019t point to it and the freachable queue no longer points to it either. The memory for the object is simply reclaimed. The important point to get from all of this is that two garbage collections are required to reclaim memory used by objects that require finalization. In reality, more than two collections will be necessary because the objects get promoted to another generation. Figure 21-15 shows what the managed heap looks like after the second garbage collection.</p><p>Roots: Fields &amp; variables</p><figcaption>Managed heap</figcaption><table border=\"1\">\n<tr>\n<td><p>A</p>\n</td>\n<td><p>C D F</p>\n</td>\n</tr>\n</table><p>C F</p><p>Finalization list</p><p>Freachable queue</p><p>FIGURE 21-15 Status of the managed heap after second garbage collection.</p><h6>Monitoring and Controlling the Lifetime of Objects Manually</h6><p>The CLR provides each AppDomain with a GC handle table. This table allows an application to monitor the lifetime of an object or manually control the lifetime of an object. When an AppDomain is created, the table is empty. Each entry on the table consists of a reference to an object on the managed heap and a flag indicating how you want to monitor or control the object. An application adds and removes entries from the table via the System.Runtime.InteropServices.GCHandle type, as follows.</p><p>// This type is defined in the System.Runtime.InteropServices namespace public struct GCHandle {</p><p>// Static methods that create an entry in the table public static GCHandle Alloc(object value); public static GCHandle Alloc(object value, GCHandleType type);</p><p>// Static methods that convert a GCHandle to an IntPtr public static explicit operator IntPtr(GCHandle value); public static IntPtr ToIntPtr(GCHandle value);</p><p>// Static methods that convert an IntPtr to a GCHandle public static explicit operator GCHandle(IntPtr value); public static GCHandle FromIntPtr(IntPtr value);</p><p>// Static methods that compare two GCHandles public static Boolean operator ==(GCHandle a, GCHandle b); public static Boolean operator !=(GCHandle a, GCHandle b);</p><p>// Instance method to free the entry in the table (index is set to 0) public void Free();</p><p>// Instance property to get/set the entry's object reference public object Target { get; set; }</p><p>// Instance property that returns true if index is not 0 public Boolean IsAllocated { get; }</p><p>// For a pinned entry, this returns the address of the object public IntPtr AddrOfPinnedObject();</p><p>}</p><p>public override Int32 GetHashCode(); public override Boolean Equals(object o);</p><p>Basically, to control or monitor an object\u2019s lifetime, you call GCHandle\u2019s static Alloc method, passing a reference to the object that you want to monitor/control, and a GCHandleType, which is a flag indicating how you want to monitor/control the object. The GCHandleType type is an enumerated type defined as follows.</p><p>public enum GCHandleType {</p><p>Weak = 0, // Used for monitoring an object\u2019s existence WeakTrackResurrection = 1, // Used for monitoring an object\u2019s existence Normal = 2, // Used for controlling an object\u2019s lifetime Pinned = 3 // Used for controlling an object\u2019s lifetime</p><p>}</p><p>Now, here\u2019s what each flag means:</p><p>\u25a0\u25a0 Weak This flag allows you to monitor the lifetime of an object. Specifically, you can detect when the garbage collector has determined this object to be unreachable from application code. Note that the object\u2019s Finalize method may or may not have executed yet and therefore, the object may still be in memory.</p><p>\u25a0\u25a0 WeakTrackResurrection This flag allows you to monitor the lifetime of an object. Specifically, you can detect when the garbage collector has determined that this object is unreachable from application code. Note that the object\u2019s Finalize method (if it exists) has definitely executed, and the object\u2019s memory has been reclaimed.</p><p>\u25a0\u25a0 Normal This flag allows you to control the lifetime of an object. Specifically, you are telling the garbage collector that this object must remain in memory even though there may be no roots in the application that refer to this object. When a garbage collection runs, the memory for this object can be compacted (moved). The Alloc method that doesn\u2019t take a GCHandleType flag assumes that GCHandleType.Normal is specified.</p><p>\u25a0\u25a0 Pinned This flag allows you to control the lifetime of an object. Specifically, you are telling the garbage collector that this object must remain in memory even though there might be no roots in the application that refer to this object. When a garbage collection runs, the memory for this object cannot be compacted. This is typically useful when you want to hand the address of the memory out to native code. The native code can write to this memory in the managed heap knowing that a GC will not move the object.</p><p>When you call GCHandle\u2019s static Alloc method, it scans the AppDomain\u2019s GC handle table, looking for an available entry where the reference of the object you passed to Alloc is stored, and a flag is set to whatever you passed for the GCHandleType argument. Then, Alloc returns a GCHandle</p><p>instance back to you. A GCHandle is a lightweight value type that contains a single instance field, an IntPtr, which refers to the index of the entry in the table. When you want to free this entry in the GC handle table, you take the GCHandle instance and call the Free method (which also invalidates the GCHandle instance by setting its IntPtr field to zero).</p><p>Here\u2019s how the garbage collector uses the GC handle table. When a garbage collection occurs:</p><ul style=\"list-style-type: none;\">\n<li><p>1. The garbage collector marks all of the reachable objects (as described at the beginning of this chapter). Then, the garbage collector scans the GC handle table; all Normal or Pinned objects are considered roots, and these objects are marked as well (including any objects that these objects refer to via their fields).</p></li>\n<li><p>2. The garbage collector scans the GC handle table looking for all of the Weak entries. If a Weak entry refers to an object that isn\u2019t marked, the reference identifies an unreachable object (garbage), and the entry has its reference value changed to null.</p></li>\n<li><p>3. The garbage collector scans the finalization list. If a reference in the list refers to an unmarked object, the reference identifies an unreachable object, and the reference is moved from the finalization list to the freachable queue. At this point, the object is marked because the object is now considered reachable.</p></li>\n<li><p>4. The garbage collector scans the GC handle table looking for all of the WeakTrackResurrection entries. If a WeakTrackResurrection entry refers to an object that isn\u2019t marked (which now is an object referenced by an entry in the freachable queue), the reference identifies an unreachable object (garbage), and the entry has its reference value changed to null.</p></li>\n<li><p>5. The garbage collector compacts the memory, squeezing out the holes left by the unreachable objects. Pinned objects are not compacted (moved); the garbage collector will move other objects around them.</p></li>\n</ul><p>Now that you have an understanding of the mechanism, let\u2019s take a look at when you\u2019d use them. The easiest flags to understand are the Normal and Pinned flags, so let\u2019s start with these two. Both of these flags are typically used when interoperating with native code.</p><p>The Normal flag is used when you need to hand a pointer to a managed object to native code because, at some point in the future, the native code is going to call back into managed code, passing it the pointer. You can\u2019t actually pass a pointer to a managed object out to native code, because if a garbage collection occurs, the object could move in memory, invalidating the pointer. So to work around this, you would call GCHandle\u2019s Alloc method, passing in a reference to the object and the Normal flag. Then you\u2019d cast the returned GCHandle instance to an IntPtr and pass the IntPtr into the native code. When the native code calls back into managed code, the managed code would cast the passed IntPtr back to a GCHandle and then query the Target property to get the reference (or current address) of the managed object. When the native code no longer needs the reference, you\u2019d call GCHandle\u2019s Free method, which allows a future garbage collection to free the object (assuming no other root exists to this object).</p><p>Notice that in this scenario, the native code is not actually using the managed object itself; the native code wants a way just to reference the object. In some scenarios, the native code needs to actually</p><p>use the managed object. In these scenarios, the managed object must be pinned. Pinning prevents the garbage collector from moving/compacting the object. A common example is when you want to pass a managed String object to a Win32 function. In this case, the String object must be pinned because you can\u2019t pass the reference of a managed object to native code and then have the garbage collector move the object in memory. If the String object were moved, the native code would either be reading or writing to memory that no longer contained the String object\u2019s characters\u2014this will surely cause the application to run unpredictably.</p><p>When you use the CLR\u2019s P/Invoke mechanism to call a method, the CLR pins the arguments for you automatically and unpins them when the native method returns. So, in most cases, you never have to use the GCHandle type to explicitly pin any managed objects yourself. You do have to use the GCHandle type explicitly when you need to pass the pointer to a managed object to native code; then the native function returns, but native code might still need to use the object later. The most common example of this is when performing asynchronous I/O operations.</p><p>Let\u2019s say that you allocate a byte array that should be filled as data comes in from a socket. Then, you would call GCHandle\u2019s Alloc method, passing in a reference to the array object and the Pinned flag. Then, using the returned GCHandle instance, you call the AddrOfPinnedObject method. This returns an IntPtr that is the actual address of the pinned object in the managed heap; you\u2019d then pass this address into the native function, which will return back to managed code immediately. While the data is coming from the socket, this byte array buffer should not move in memory; preventing this buffer from moving is accomplished by using the Pinned flag. When the asynchronous I/O operation has completed, you\u2019d call GCHandle\u2019s Free method, which will allow a future garbage collection to move the buffer. Your managed code should still have a reference to the buffer so that you can access the data, and this reference will prevent a garbage collection from freeing the buffer from memory completely.</p><p>It is also worth mentioning that C# offers a fixed statement that effectively pins an object over a block of code. Here is some code that demonstrates its use.</p><p>unsafe public static void Go() { // Allocate a bunch of objects that immediately become garbage for (Int32 x = 0; x &lt; 10000; x++) new Object();</p><p>IntPtr originalMemoryAddress; Byte[] bytes = new Byte[1000]; // Allocate this array after the garbage objects</p><p>// Get the address in memory of the Byte[] fixed (Byte* pbytes = bytes) { originalMemoryAddress = (IntPtr) pbytes; }</p><p>// Force a collection; the garbage objects will go away &amp; the Byte[] might be compacted GC.Collect();</p><p>}</p><p>// Get the address in memory of the Byte[] now &amp; compare it to the first address fixed (Byte* pbytes = bytes) {</p><p>Console.WriteLine(\"The Byte[] did{0} move during the GC\",</p><p>(originalMemoryAddress == (IntPtr) pbytes) ? \" not\" : null); }</p><p>Using C#\u2019s fixed statement is more efficient that allocating a pinned GC handle. What happens is that the C# compiler emits a special \u201cpinned\u201d flag on the pbytes local variable. During a garbage collection, the GC examines the contents of this root, and if the root is not null, it knows not to move the object referred to by the variable during the compaction phase. The C# compiler emits IL to initialize the pbytes local variable to the address of the object at the start of a fixed block, and the compiler emits an IL instruction to set the pbytes local variable back to null at the end of the fixed block so that the variable doesn\u2019t refer to any object, allowing the object to move when the next garbage collection occurs.</p><p>Now, let\u2019s talk about the next two flags, Weak and WeakTrackResurrection. These two flags can be used in scenarios when interoperating with native code, but they can also be used in scenarios that use only managed code. The Weak flag lets you know when an object has been determined to be garbage but the object\u2019s memory is not guaranteed to be reclaimed yet. The WeakTrackResurrection flag lets you know when an object\u2019s memory has been reclaimed. Of the two flags, the Weak flag is much more commonly used than the WeakTrackResurrection flag. In fact, I\u2019ve never seen anyone use the WeakTrackResurrection flag in a real application.</p><p>Let\u2019s say that Object\u00adA periodically calls a method on Object\u00adB. However, the fact that Object\u00adA has a reference to Object\u00adB forbids Object\u00adB from being garbage collected, and in some rare scenarios, this may not be desired; instead, we might want Object\u00adA to call Object\u00adB\u2019s method if Object\u00adB is still alive in the managed heap. To accomplish this scenario, Object\u00adA would call GCHandle\u2019s Alloc method, passing in the reference to Object\u00adB and the Weak flag. Object\u00adA would now just save the returned GCHandle instance instead of the reference to Object\u00adB.</p><p>At this point, Object\u00adB can be garbage collected if no other roots are keeping it alive. When Object\u00adA wants to call Object\u00adB\u2019s method, it would query GCHandle\u2019s read-only Target property. If this property returns a non-null value, then Object\u00adB is still alive. Object\u00adA\u2019s code would then cast the returned reference to Object\u00adB\u2019s type and call the method. If the Target property returns null, then Object\u00adB has been collected (but not necessarily finalized) and Object\u00adA would not attempt to call the method. At this point, Object\u00adA\u2019s code would probably also call GCHandle\u2019s Free method to relinquish the GCHandle instance.</p><p>Because working with the GCHandle type can be a bit cumbersome and because it requires elevated security to keep or pin an object in memory, the System namespace includes a WeakReference&lt;T&gt; class to help you.</p><p>public sealed class WeakReference&lt;T&gt; : ISerializable where T : class { public WeakReference(T target); public WeakReference(T target, Boolean trackResurrection); public void SetTarget(T target); public Boolean TryGetTarget(out T target);</p><p>}</p><p>This class is really just an object-oriented wrapper around a GCHandle instance: logically, its constructor calls GCHandle\u2019s Alloc, its TryGetTarget method queries GCHandle\u2019s Target property, its SetTarget method sets GCHandle\u2019s Target property, and its Finalize method (not shown in the preceding code, because it\u2019s protected) calls GCHandle\u2019s Free method. In addition, no special permissions are required for code to use the WeakReference&lt;T&gt; class because the class supports only weak</p><p>references; it doesn\u2019t support the behavior provided by GCHandle instances allocated with a GCHandleType of Normal or Pinned. The downside of the WeakReference&lt;T&gt; class is that an instance of it must be allocated on the heap. So the WeakReference&lt;T&gt; class is a heavier-weight object than a</p><p>GCHandle instance.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile181.png\"/><p>Important When developers start learning about weak references, they immediately start thinking that they are useful in caching scenarios. For example, they think it would be cool to construct a bunch of objects that contain a lot of data and then to create weak references to these objects. When the program needs the data, the program checks the weak reference to see if the object that contains the data is still around, and if it is, the program just uses it; the program experiences high performance. However, if a garbage collection occurred, the objects that contained the data would be destroyed, and when the program has to re-create the data, the program experiences lower performance.</p><p>The problem with this technique is the following: garbage collections do not only occur when memory is full or close to full. Instead, garbage collections occur whenever generation 0 is full. So objects are being tossed out of memory much more frequently than desired, and your application\u2019s performance suffers greatly.</p><p>Weak references can be used quite effectively in caching scenarios, but building a good cache algorithm that finds the right balance between memory consumption and speed is very complex. Basically, you want your cache to keep strong references to all of your objects and then, when you see that memory is getting tight, you start turning strong references into weak references. Currently, the CLR offers no mechanism to notify an application that memory is getting tight. But some people have had much success by periodically calling the Win32 GlobalMemoryStatusEx function and checking the returned</p><p>MEMORYSTATUSEX structure\u2019s dwMemoryLoad member. If this member reports a value above 80, memory is getting tight, and you can start converting strong references to weak references based on whether you want a least-recently used algorithm, a most-frequently used algorithm, a time-base algorithm, or whatever.</p><p>Developers frequently want to associate a piece of data with another entity. For example, you can associate data with a thread or with an AppDomain. It is also possible to associate data with an individual object by using the System.Runtime.CompilerServices.ConditionalWeakTable &lt;TKey,TValue&gt; class, which looks like this.</p><p>public sealed class ConditionalWeakTable&lt;TKey, TValue&gt; where TKey : class where TValue : class { public ConditionalWeakTable(); public void Add(TKey key, TValue value); public TValue GetValue(TKey key, CreateValueCallback&lt;TKey, TValue&gt; createValueCallback); public Boolean TryGetValue(TKey key, out TValue value); public TValue GetOrCreateValue(TKey key); public Boolean Remove(TKey key);</p><p>public delegate TValue CreateValueCallback(TKey key); // Nested delegate definition }</p><p>If you want to associate some arbitrary data with one or more objects, you would first create an instance of this class. Then, call the Add method, passing in a reference to some object for the key parameter and the data you want to associate with the object in the value parameter. If you attempt to add a reference to the same object more than once, the Add method throws an ArgumentException; to change the value associated with an object, you must remove the key and then add it back in with the new value. Note that this class is thread-safe so multiple threads can use it concurrently, although this means that the performance of the class is not stellar; you should test the performance of this class to see how well it works for your scenario.</p><p>Of course, a table object internally stores a WeakReference to the object passed in as the key; this ensures that the table doesn\u2019t forcibly keep the object alive. But what makes the ConditionalWeakTable class so special is that it guarantees that the value remains in memory as long as the</p><p>object identified by the key is in memory. So this is more than a normal WeakReference because if it were, the value could be garbage collected even though the key object continued to live. The ConditionalWeakTable class could be used to implement the dependency property mechanism</p><p>used by XAML. It can also be used internally by dynamic languages to dynamically associate data with objects.</p><p>Here is some code that demonstrates the use of the ConditionalWeakTable class. It allows you to call the GCWatch extension method on any object passing in some String tag. Then it notifies you via the console window whenever that particular object gets garbage collected.</p><p>internal static class ConditionalWeakTableDemo {</p><p>public static void Main() { Object o = new Object().GCWatch(\"My Object created at \" + DateTime.Now); GC.Collect(); // We will not see the GC notification here GC.KeepAlive(o); // Make sure the object o refers to lives up to here o = null; // The object that o refers to can die now</p><p>} }</p><p>GC.Collect(); // We'll see the GC notification sometime after this line Console.ReadLine();</p><p>internal static class GCWatcher { // NOTE: Be careful with Strings due to interning and MarshalByRefObject proxy objects private readonly static ConditionalWeakTable&lt;Object, NotifyWhenGCd&lt;String&gt;&gt; s_cwt =</p><p>new ConditionalWeakTable&lt;Object, NotifyWhenGCd&lt;String&gt;&gt;();</p><p>private sealed class NotifyWhenGCd&lt;T&gt; { private readonly T m_value;</p><p>}</p><p>internal NotifyWhenGCd(T value) { m_value = value; } public override string ToString() { return m_value.ToString(); } ~NotifyWhenGCd() { Console.WriteLine(\"GC'd: \" + m_value); }</p><p>public static T GCWatch&lt;T&gt;(this T @object, String tag) where T : class { s_cwt.Add(@object, new NotifyWhenGCd&lt;String&gt;(tag)); return @object;</p><p>} }</p><h6>C H A P T E R 2 2</h6><h2>CLR Hosting and AppDomains</h2><h6>In this chapter:</h6><p>CLR Hosting . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 554 AppDomains . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 556 AppDomain Unloading . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 570 AppDomain Monitoring . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 571 AppDomain First-Chance Exception Notifications. . . . . . . . . . . 573 How Hosts Use AppDomains. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 574 Advanced Host Control. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 576</p><p>In this chapter, I\u2019ll discuss two main topics that really show off the incredible value provided by the Microsoft .NET Framework: hosting and AppDomains. Hosting allows any application to use the features of the common language runtime (CLR). In particular, this allows existing applications to be at least partially written using managed code. Furthermore, hosting allows applications the ability to offer customization and extensibility via programming.</p><p>Allowing extensibility means that third-party code will be running inside your process. In Windows, loading a third party\u2019s DLLs into a process has been fraught with peril. The DLL could easily have code in it that could compromise the application\u2019s data structures and code. The DLL could also try to use the security context of the application to gain access to resources that it should not have access to. The CLR\u2019s AppDomain feature solves all of these problems. AppDomains allow third-party untrusted code to run in an existing process, and the CLR guarantees that the data structures, code, and security context will not be exploited or compromised.</p><p>Programmers typically use hosting and AppDomains along with assembly loading and reflection. Using these four technologies together makes the CLR an incredibly rich and powerful platform. In this chapter, I\u2019ll focus on hosting and AppDomains. In the next chapter, I\u2019ll focus on assembly loading and reflection. When you learn and understand all of these technologies, you\u2019ll see how your investment in the .NET Framework today will certainly pay off down the line.</p><p>553</p><h6>CLR Hosting</h6><p>The .NET Framework runs on top of Windows. This means that the .NET Framework must be built using technologies that Windows can interface with. For starters, all managed module and assembly files must use the Windows portable executable (PE) file format and be either a Windows executable (EXE) file or a DLL.</p><p>When developing the CLR, Microsoft implemented it as a COM server contained inside a DLL; that is, Microsoft defined a standard COM interface for the CLR and assigned GUIDs to this interface and the COM server. When you install the .NET Framework, the COM server representing the CLR is registered in the Windows registry just as any other COM server would. If you want more information about this topic, refer to the MetaHost.h C++ header file that ships with the .NET Framework SDK. This header file defines the GUIDs and the unmanaged ICLRMetaHost interface definition.</p><p>Any Windows application can host the CLR. However, you shouldn\u2019t create an instance of the CLR COM server by calling CoCreateInstance; instead, your unmanaged host should call the CLRCreateInstance function declared in MetaHost.h. The CLRCreateInstance function is implemented in the MSCorEE.dll file, which is usually found in the C:\\Windows\\System32 directory. This DLL is affectionately referred to as the shim, and its job is to determine which version of the CLR to create; the shim DLL doesn\u2019t contain the CLR COM server itself.</p><p>A single machine may have multiple versions of the CLR installed, but there will be only one version of the MSCorEE.dll file (the shim).1 The version of MSCorEE.dll installed on the machine is the version that shipped with the latest version of the CLR installed on the machine. Therefore, this version of MSCorEE.dll knows how to find any previous versions of the CLR that may be installed.</p><p>The actual CLR code is contained in a file whose name has changed with different versions of the CLR. For versions 1.0, 1.1, and 2.0, the CLR code is in a file called MSCorWks.dll, and for version 4, the CLR code is in a file called Clr.dll. Because you can have multiple versions of the CLR installed on a single machine, these files are installed into different directories as follows.2</p><p>\u25a0\u25a0 Version 1.0 is in C:\\Windows\\Microsoft.NET\\Framework\\v1.0.3705 \u25a0\u25a0 Version 1.1 is in C:\\Windows\\Microsoft.NET\\Framework\\v1.0.4322 \u25a0\u25a0 Version 2.0 is in C:\\Windows\\Microsoft.NET\\Framework\\v2.0.50727</p><p>\u25a0\u25a0 Version 4 is in C:\\Windows\\Microsoft.NET\\Framework\\v4.0.21006 The CLRCreateInstance function can return an ICLRMetaHost interface. A host application can</p><p>call this interface\u2019s GetRuntime function, specifying the version of the CLR that the host would like to create. The shim then loads the desired version of the CLR into the host\u2019s process.</p><ul style=\"list-style-type: none;\">\n<li><p>1 If you are using a 64-bit version of Windows, there are actually two versions of the MSCorEE.dll file installed. One version is the 32-bit x86 version, which will be located in the C:\\Windows\\SysWOW64 directory. The other version is the 64-bit x64 or IA64 version (depending on your computer\u2019s CPU architecture), which will be located in the C:\\Windows\\ System32 directory.</p></li>\n<li><p>2 Note that versions 3.0 and 3.5 of the .NET Framework were shipped with version 2.0 of the CLR; I do not show the directories for .NET Framework versions 3.0 and 3.5 because the CLR DLL loads from the v2.0.50727 directory.</p></li>\n</ul><p>By default, when a managed executable starts, the shim examines the executable file and extracts the information indicating the version of the CLR that the application was built and tested with. However, an application can override this default behavior by placing requiredRuntime and supportedRuntime entries in its XML configuration file (described in Chapter 2, \u201cBuilding, Packaging, Deploying, and Administering Applications and Types,\u201d and Chapter 3, \u201cShared Assemblies and Strongly Named Assemblies\u201d).</p><p>The GetRuntime function returns a pointer to the unmanaged ICLRRuntimeInfo interface from which the ICLRRuntimeHost interface is obtained via the GetInterface method. The hosting application can call methods defined by this interface to:</p><p>\u25a0\u25a0 Set Host managers. Tell the CLR that the host wants to be involved in making decisions related to memory allocations, thread scheduling/synchronization, assembly loading, and more. The host can also state that it wants notifications of garbage collection starts and stops and when certain operations time out.</p><p>\u25a0\u25a0 Get CLR managers. Tell the CLR to prevent the use of some classes/members. In addition, the</p><p>host can tell which code can and can\u2019t be debugged and which methods in the host should be called when a special event\u2014such as an AppDomain unload, CLR stop, or stack overflow exception\u2014occurs.</p><p>\u25a0\u25a0 Initialize and start the CLR.</p><p>\u25a0\u25a0 Load an assembly and execute code in it.</p><p>\u25a0\u25a0 Stop the CLR, thus preventing any more managed code from running in the Windows process. There are many reasons why hosting the CLR is useful. Hosting allows any application to offer CLR features and a programmability story and to be at least partially written in managed code. Any application that hosts the runtime offers many benefits to developers who are trying to extend the application. Here are some of the benefits:</p><p>\u25a0\u25a0 Programming can be done in any programming language.</p><p>\u25a0\u25a0 Code is just-in-time (JIT)\u2013compiled for speed (versus being interpreted).</p><p>\u25a0\u25a0 Code uses garbage collection to avoid memory leaks and corruption.</p><p>\u25a0\u25a0 Code runs in a secure sandbox.</p><p>\u25a0\u25a0 The host doesn\u2019t need to worry about providing a rich development environment. The host makes use of existing technologies: languages, compilers, editors, debuggers, profilers, and more.</p><p>If you are interested in using the CLR for hosting scenarios, I highly recommend that you get Steven Pratschner\u2019s excellent book, Customizing the Microsoft .NET Framework Common Language Runtime (Microsoft Press 2005), even though it focuses on pre-4 versions of the CLR.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile182.png\"/><p>Note Of course, a Windows process does not need to load the CLR at all. It needs to be loaded only if you want to execute managed code in a process. Prior to .NET Framework 4, the CLR allowed only one instance of itself to reside within a Windows process. That is, a process could contain no CLR, v1.0 of the CLR, v1.1 of the CLR, or v2.0 of the CLR. Allowing only one CLR version per process is a huge limitation. For example, Microsoft Outlook couldn\u2019t load two add-ins that were built and tested against different versions of the .NET Framework.</p><p>However, with .NET Framework 4, Microsoft now supports the ability to load v2.0 and v4.0 in a single Windows process, allowing components written for .NET Framework versions 2.0 and 4 to run side by side without experiencing any compatibility problems. This is a fantastic new feature, because it allows .NET Framework components to be used reliably in more scenarios than ever before. You can use the ClrVer.exe tool to see which CLR version(s) are loaded into any given process.</p><p>After a CLR is loaded into a Windows process, it can never be unloaded; calling the AddRef and Release methods on the ICLRRuntimeHost interface has no effect. The only way for the CLR to be unloaded from a process is for the process to terminate, causing Windows to clean up all resources used by the process.</p><h6>AppDomains</h6><p>When the CLR COM server initializes, it creates an AppDomain. An AppDomain is a logical container for a set of assemblies. The first AppDomain created when the CLR is initialized is called the default AppDomain; this AppDomain is destroyed only when the Windows process terminates.</p><p>In addition to the default AppDomain, a host using either unmanaged COM interface methods or managed type methods can instruct the CLR to create additional AppDomains. The whole purpose of an AppDomain is to provide isolation. Here are the specific features offered by an AppDomain:</p><p>\u25a0\u25a0 Objects created by code in one AppDomain cannot be accessed directly by code in another AppDomain When code in an AppDomain creates an object, that object is \u201cowned\u201d by that AppDomain. In other words, the object is not allowed to live beyond the lifetime of the AppDomain whose code constructed it. Code in other AppDomains can access another AppDomain\u2019s object only by using marshal-by-reference or marshal-by-value semantics. This enforces a clean separation and boundary because code in one AppDomain can\u2019t have a direct reference to an object created by code in a different AppDomain. This isolation allows AppDomains to be easily unloaded from a process without affecting code running in other AppDomains.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile183.png\"/><p>\u25a0\u25a0 AppDomains can be unloaded The CLR doesn\u2019t support the ability to unload a single assembly from an AppDomain. However, you can tell the CLR to unload an AppDomain, which will cause all of the assemblies currently contained in it to be unloaded as well.</p><p>\u25a0\u25a0 AppDomains can be individually secured When created, an AppDomain can have a permission set applied to it that determines the maximum rights granted to assemblies running in the AppDomain. This allows a host to load some code and be ensured that the code cannot corrupt or read important data structures used by the host itself.</p><p>\u25a0\u25a0 AppDomains can be individually configured When created, an AppDomain can have a bunch of configuration settings associated with it. These settings mostly affect how the CLR loads assemblies into the AppDomain. There are configuration settings related to search paths, version binding redirects, shadow copying, and loader optimizations.</p><p>Important A great feature of Windows is that it runs each application in its own process address space. This ensures that code in one application cannot access code or data in use by another application. Process isolation prevents security holes, data corruption, and other unpredictable behaviors from occurring, making Windows and the applications running on it robust. Unfortunately, creating processes in Windows is very expensive. The Win32 CreateProcess function is very slow, and Windows requires a lot of memory to virtualize a process\u2019s address space. However, if an application consists entirely of managed code that is verifiably safe and doesn\u2019t call out into unmanaged code, there are no problems related to running multiple managed applications in a single Windows process. And AppDomains provide the isolation required to secure, configure, and terminate each of these applications.</p><p>Figure 22-1 shows a single Windows process that has one CLR COM server running in it. This CLR is currently managing two AppDomains (although there is no hard-coded limit to the number of AppDomains that could be running in a single Windows process). Each AppDomain has its own loader heap, each of which maintains a record of which types have been accessed because the AppDomain was created. These type objects were discussed in Chapter 4, \u201cType Fundamentals;\u201d each type object in the loader heap has a method table, and each entry in the method table points to JIT-compiled native code if the method has been executed at least once.</p><p>In addition, each AppDomain has some assemblies loaded into it. AppDomain #1 (the default AppDomain) has three assemblies: MyApp.exe, TypeLib.dll, and System.dll. AppDomain #2 has two assemblies loaded into it: Wintellect.dll and System.dll.</p><p>You\u2019ll notice that the System.dll assembly has been loaded into both AppDomains. If both AppDomains are using a single type from System.dll, both AppDomains will have a type object for the same type allocated in each loader heap; the memory for the type object is not shared by all of the AppDomains. Furthermore, as code in an AppDomain calls methods defined by a type, the method\u2019s Intermediate Language (IL) code is JIT-compiled, and the resulting native code is associated with each AppDomain; the code for the method is not shared by all AppDomains that call it.</p><p>Not sharing the memory for the type objects or native code is wasteful. However, the whole purpose of AppDomains is to provide isolation; the CLR needs to be able to unload an AppDomain and free up all of its resources without adversely affecting any other AppDomain. Replicating the CLR data structures ensures that this is possible. It also ensures that a type used by multiple AppDomains has a set of static fields for each AppDomain.</p><h6>Windows Process</h6><table border=\"1\">\n<tr>\n<td><h6>AppDomain #1 (Default)</h6>\n<p>MyApp.exe</p>\n<p>TypeLib.dll</p>\n<p>System.dll</p>\n<p>M1() (x86)</p>\n<p>M2() (x86) ...</p>\n<p>M1() (x86)</p>\n<p>M2() (x86) ...</p>\n<p>Type1</p>\n<p>...</p>\n<p>M1() (x86)</p>\n<p>M2()</p>\n<p>Type2 (x86) ...</p>\n<p>Loader Heap</p>\n</td>\n</tr>\n</table><table border=\"1\">\n<tr>\n<td><h6>AppDomain #2</h6>\n<p>Wintellect.dll</p>\n<p>System.dll</p>\n<p>M1() (x86)</p>\n<p>M2() (x86) ...</p>\n<p>M1() (x86)</p>\n<p>M2() (x86) ...</p>\n<p>Type1</p>\n<p>...</p>\n<p>M1() (x86)</p>\n<p>M2()</p>\n<p>Type2 (x86) ...</p>\n<p>Loader Heap</p>\n</td>\n</tr>\n</table><p>Domain-Neutral Assemblies</p><p>MSCorLib.dll</p><p>Loader Heap</p><p>M1() (x86)</p><p>M2() (x86) ...</p><p>Type1</p><p>M1() (x86)</p><p>M2()</p><p>Type2 (x86) ...</p><p>...</p><p>M1() (x86)</p><p>M2() (x86) ...</p><table border=\"1\">\n<tr>\n<td><p>Execution Engine (MSCorEE.dll (shim) Clr.dll (actual CLR)</p>\n</td>\n</tr>\n</table><p>FIGURE 22-1 A single Windows process hosting the CLR and two AppDomains.</p><p>Some assemblies are expected to be used by several AppDomains. MSCorLib.dll is the best example. This assembly contains System.Object, System.Int32, and all of the other types that are so integral to the .NET Framework. This assembly is automatically loaded when the CLR initializes, and all AppDomains share the types in this assembly. To reduce resource usage, MSCorLib.dll is loaded in an AppDomain-neutral fashion; that is, the CLR maintains a special loader heap for assemblies that are loaded in a domain-neutral fashion. All type objects in this loader heap and all native code for methods of these types are shared by all AppDomains in the process. Unfortunately, the benefit gained by sharing these resources does come with a price: assemblies that are loaded domain-neutral can never be unloaded. The only way to reclaim the resources used by them is to terminate the Windows process to cause Windows to reclaim the resources.</p><h6>Accessing Objects Across AppDomain Boundaries</h6><p>Code in one AppDomain can communicate with types and objects contained in another AppDomain. However, access to these types and objects is allowed only through well-defined mechanisms. The following Ch22-1-AppDomains sample application demonstrates how to create a new AppDomain, load an assembly into it, and construct an instance of a type defined in that assembly. The code shows the different behaviors when constructing a type that is marshaled by reference, a type that is marshaled by value, and a type that can\u2019t be marshaled at all. The code also shows how these differently marshaled objects behave when the AppDomain that created them is unloaded. The Ch221-AppDomains sample application has very little code in it, but I have added a lot of comments. After the code listing, I\u2019ll walk through the code, explaining what the CLR is doing.</p><p>private static void Marshalling() { // Get a reference to the AppDomain that the calling thread is executing in AppDomain adCallingThreadDomain = Thread.GetDomain();</p><p>// Every AppDomain is assigned a friendly string name (helpful for debugging) // Get this AppDomain's friendly string name and display it String callingDomainName = adCallingThreadDomain.FriendlyName; Console.WriteLine(\"Default AppDomain's friendly name={0}\", callingDomainName);</p><p>// Get and display the assembly in our AppDomain that contains the 'Main' method String exeAssembly = Assembly.GetEntryAssembly().FullName; Console.WriteLine(\"Main assembly={0}\", exeAssembly);</p><p>// Define a local variable that can refer to an AppDomain AppDomain ad2 = null;</p><p>// *** DEMO 1: Cross\u00adAppDomain Communication Using Marshal\u00adby\u00adReference *** Console.WriteLine(\"{0}Demo #1\", Environment.NewLine);</p><p>// Create new AppDomain (security and configuration match current AppDomain) ad2 = AppDomain.CreateDomain(\"AD #2\", null, null); MarshalByRefType mbrt = null;</p><p>// Load our assembly into the new AppDomain, construct an object, marshal // it back to our AD (we really get a reference to a proxy) mbrt = (MarshalByRefType)</p><p>ad2.CreateInstanceAndUnwrap(exeAssembly, \"MarshalByRefType\");</p><p>Console.WriteLine(\"Type={0}\", mbrt.GetType()); // The CLR lies about the type</p><p>// Prove that we got a reference to a proxy object Console.WriteLine(\"Is proxy={0}\", RemotingServices.IsTransparentProxy(mbrt));</p><p>// This looks like we're calling a method on MarshalByRefType but we're not. // We're calling a method on the proxy type. The proxy transitions the thread // to the AppDomain owning the object and calls this method on the real object. mbrt.SomeMethod();</p><p>// Unload the new AppDomain AppDomain.Unload(ad2); // mbrt refers to a valid proxy object; the proxy object refers to an invalid AppDomain</p><p>try { // We're calling a method on the proxy type. The AD is invalid, exception is thrown mbrt.SomeMethod(); Console.WriteLine(\"Successful call.\");</p><p>} catch (AppDomainUnloadedException) {</p><p>Console.WriteLine(\"Failed call.\"); }</p><p>// *** DEMO 2: Cross\u00adAppDomain Communication Using Marshal\u00adby\u00adValue ***</p><ul style=\"list-style-type: none;\">\n<li><p>Console.WriteLine(\"{0}Demo #2\", Environment.NewLine);</p><p>// Create new AppDomain (security and configuration match current AppDomain) ad2 = AppDomain.CreateDomain(\"AD #2\", null, null);</p>\n<p>// Load our assembly into the new AppDomain, construct an object, marshal // it back to our AD (we really get a reference to a proxy) mbrt = (MarshalByRefType)</p>\n<p>ad2.CreateInstanceAndUnwrap(exeAssembly, \"MarshalByRefType\");</p>\n<p>// The object's method returns a COPY of the returned object; // the object is marshaled by value (not by reference). MarshalByValType mbvt = mbrt.MethodWithReturn();</p>\n<p>// Prove that we did NOT get a reference to a proxy object Console.WriteLine(\"Is proxy={0}\", RemotingServices.IsTransparentProxy(mbvt));</p>\n<p>// This looks like we're calling a method on MarshalByValType and we are. Console.WriteLine(\"Returned object created \" + mbvt.ToString());</p>\n<p>// Unload the new AppDomain AppDomain.Unload(ad2); // mbvt refers to valid object; unloading the AppDomain has no impact.</p>\n<p>try { // We're calling a method on an object; no exception is thrown Console.WriteLine(\"Returned object created \" + mbvt.ToString()); Console.WriteLine(\"Successful call.\");</p>\n<p>} catch (AppDomainUnloadedException) {</p>\n<p>Console.WriteLine(\"Failed call.\"); }</p>\n<p>// DEMO 3: Cross\u00adAppDomain Communication Using non\u00admarshalable type ***</p>\n</li>\n<li><p>Console.WriteLine(\"{0}Demo #3\", Environment.NewLine);</p></li>\n</ul><p>// Create new AppDomain (security and configuration match current AppDomain) ad2 = AppDomain.CreateDomain(\"AD #2\", null, null);</p><p>// Load our assembly into the new AppDomain, construct an object, marshal // it back to our AD (we really get a reference to a proxy) mbrt = (MarshalByRefType)</p><p>ad2.CreateInstanceAndUnwrap(exeAssembly, \"MarshalByRefType\");</p><p>}</p><p>// The object's method returns a non\u00admarshalable object; exception NonMarshalableType nmt = mbrt.MethodArgAndReturn(callingDomainName); // We won't get here...</p><p>// Instances can be marshaled\u00adby\u00adreference across AppDomain boundaries public sealed class MarshalByRefType : MarshalByRefObject {</p><p>public MarshalByRefType() { Console.WriteLine(\"{0} ctor running in {1}\",</p><p>this.GetType().ToString(), Thread.GetDomain().FriendlyName); }</p><p>public void SomeMethod() {</p><p>Console.WriteLine(\"Executing in \" + Thread.GetDomain().FriendlyName); }</p><p>public MarshalByValType MethodWithReturn() { Console.WriteLine(\"Executing in \" + Thread.GetDomain().FriendlyName); MarshalByValType t = new MarshalByValType(); return t;</p><p>}</p><p>public NonMarshalableType MethodArgAndReturn(String callingDomainName) { // NOTE: callingDomainName is [Serializable] Console.WriteLine(\"Calling from '{0}' to '{1}'.\",</p><p>callingDomainName, Thread.GetDomain().FriendlyName); NonMarshalableType t = new NonMarshalableType(); return t;</p><p>} }</p><p>// Instances can be marshaled\u00adby\u00advalue across AppDomain boundaries [Serializable] public sealed class MarshalByValType : Object {</p><p>private DateTime m_creationTime = DateTime.Now; // NOTE: DateTime is [Serializable]</p><p>public MarshalByValType() {</p><p>Console.WriteLine(\"{0} ctor running in {1}, Created on {2:D}\", this.GetType().ToString(), Thread.GetDomain().FriendlyName, m_creationTime);</p><p>}</p><p>}</p><p>public override String ToString() {</p><p>return m_creationTime.ToLongDateString(); }</p><p>// Instances cannot be marshaled across AppDomain boundaries // [Serializable] public sealed class NonMarshalableType : Object {</p><p>public NonMarshalableType() {</p><p>Console.WriteLine(\"Executing in \" + Thread.GetDomain().FriendlyName); }</p><p>}</p><p>If you build and run the Ch22-1-AppDomains application, you get the following output.</p><p>Default AppDomain's friendly name= Ch22\u00ad1\u00adAppDomains.exe Main assembly=Ch22\u00ad1\u00adAppDomains, Version=0.0.0.0, Culture=neutral, PublicKeyToken=null</p><ul style=\"list-style-type: none;\">\n<li><p>Demo #1 MarshalByRefType ctor running in AD #2 Type=MarshalByRefType Is proxy=True Executing in AD #2 Failed call.</p></li>\n<li><p>Demo #2 MarshalByRefType ctor running in AD #2 Executing in AD #2 MarshalByValType ctor running in AD #2, Created on Friday, August 07, 2009 Is proxy=False Returned object created Saturday, June 23, 2012 Returned object created Saturday, June 23, 2012 Successful call.</p></li>\n<li><p>Demo #3 MarshalByRefType ctor running in AD #2 Calling from 'Ch22\u00ad1\u00adAppDomains.exe' to 'AD #2'. Executing in AD #2</p></li>\n</ul><p>Unhandled Exception: System.Runtime.Serialization.SerializationException: Type 'NonMarshalableType' in assembly 'Ch22\u00ad1\u00adAppDomains, Version=0.0.0.0, Culture=neutral, PublicKeyToken=null' is not marked as serializable. at MarshalByRefType.MethodArgAndReturn(String callingDomainName) at Program.Marshalling() at Program.Main()</p><p>Now, I will discuss what this code and the CLR are doing. Inside the Marshalling method, I first get a reference to an AppDomain object that identifies the</p><p>AppDomain the calling thread is currently executing in. In Windows, a thread is always created in the context of one process, and the thread lives its entire lifetime in that process. However, a one-to-one correspondence doesn\u2019t exist between threads and AppDomains. AppDomains are a CLR feature; Windows knows nothing about AppDomains. Because multiple AppDomains can be in a single Windows process, a thread can execute code in one AppDomain and then execute code in another AppDomain. From the CLR\u2019s perspective, a thread is executing code in one AppDomain at a time. A thread can ask the CLR what AppDomain it is currently executing in by calling System.Threading. Thread\u2019s static GetDomain method. The thread could also query System.AppDomain\u2019s static, readonly CurrentDomain property to get the same information.</p><p>When an AppDomain is created, it can be assigned a friendly name. A friendly name is just a String that you can use to identify an AppDomain. This is typically useful in debugging scenarios. Because the CLR creates the default AppDomain before any of our code can run, the CLR uses the executable file\u2019s file name as the default AppDomain\u2019s friendly name. My Marshalling method queries the default AppDomain\u2019s friendly name by using System.AppDomain\u2019s read-only FriendlyName property.</p><p>Next, my Marshalling method queries the strong-name identity of the assembly (loaded into the default AppDomain) that defines the entry point method Main that calls Marshalling. This assembly defines several types: Program, MarshalByRefType, MarshalBy ValType, and NonMarshalableType. At this point, we\u2019re ready to look at the three demos that are all pretty similar to each other.</p><h6>Demo #1: Cross-AppDomain Communication Using Marshal-by-Reference</h6><p>In Demo #1, System.AppDomain\u2019s static CreateDomain method is called, instructing the CLR to create a new AppDomain in the same Windows process. The AppDomain type actually offers several overloads of the CreateDomain method; I encourage you to study them and select the version that is most appropriate when you are writing code to create a new AppDomain. The version of CreateDomain that I call accepts three arguments:</p><h6>\u25a0\u25a0 A String identifying the friendly name I want assigned to the new AppDomain I\u2019m</h6><p>passing in \u201cAD #2\u201d here.</p><h6>\u25a0\u25a0 A System.Security.Policy.Evidence identifying the evidence that the CLR should</h6><p>use to calculate the AppDomain\u2019s permission set I\u2019m passing null here so that the new AppDomain will inherit the same permission set as the AppDomain creating it. Usually, if you want to create a security boundary around code in an AppDomain, you\u2019d construct a System.</p><p>Security.PermissionSet object, add the desired permission objects to it (instances of</p><p>types that implement the IPermission interface), and then pass the resulting PermissionSet object reference to the overloaded version of the CreateDomain method that accepts a PermissionSet.</p><p>\u25a0\u25a0 A System.AppDomainSetup identifying the configuration settings the CLR should use for the new AppDomain Again, I\u2019m passing null here so that the new AppDomain will inherit the same configuration settings as the AppDomain creating it. If you want the AppDomain to have a special configuration, construct an AppDomainSetup object, set its various properties to whatever you want, such as the name of the configuration file, and then pass the resulting AppDomainSetup object reference to the CreateDomain method.</p><p>Internally, the CreateDomain method creates a new AppDomain in the process. This AppDomain will be assigned the specified friendly name, security, and configuration settings. The new AppDomain will have its very own loader heap, which will be empty because there are currently no assemblies loading into the new AppDomain. When you create an AppDomain, the CLR does not create any threads in this AppDomain; no code runs in the AppDomain unless you explicitly have a thread call code in the AppDomain.</p><p>Now to create an instance of an object in the new AppDomain, we must first load an assembly into the new AppDomain and then construct an instance of a type defined in this assembly. This is precisely what the call to AppDomain\u2019s public, instance CreateInstanceAndUnwrap method does. When calling CreateInstanceAndUnwrap, I pass two arguments: a String identifying the assembly I want loaded into the new AppDomain (referenced by the ad2 variable) and another String identifying the name of the type that I want to construct an instance of. Internally, CreateInstanceAndUnwrap causes the calling thread to transition from the current AppDomain into the new AppDomain. Now, the thread (which is inside the call to CreateInstanceAndUnwrap) loads the specified assembly into the new AppDomain and then scans the assembly\u2019s type definition metadata table, looking for the specified type (\u201cMarshalByRefType\u201d). After the type is found, the thread calls the MarshalByRefType\u2019s parameterless constructor. Now the thread transitions back to the default AppDomain so that CreateInstanceAndUnwrap can return a reference to the new MarshalByRefType object.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile184.png\"/><p>Note There are overloaded versions of CreateInstanceAndUnwrap that allow you to call a type\u2019s constructor passing in arguments.</p><p>Although this sounds all fine and good, there is a problem: the CLR cannot allow a variable (root) living in one AppDomain to reference an object created in another AppDomain. If CreateInstanceAndUnwrap simply returned the reference to the object, isolation would be broken, and isolation is the whole purpose of AppDomains! So, just before CreateInstanceAndUnwrap returns the object reference, it performs some additional logic.</p><p>You\u2019ll notice that the MarshalByRefType type is derived from a very special base class: System. MarshalByRefObject. When CreateInstanceAndUnwrap sees that it is marshalling an object whose type is derived from MarshalByRefObject, the CLR will marshal the object by reference across the AppDomain boundaries. Here is what it means to marshal an object by reference from one AppDomain (the source AppDomain where the object is really created) to another AppDomain (the destination AppDomain from where CreateInstanceAndUnwrap is called).</p><p>When a source AppDomain wants to send or return the reference of an object to a destination AppDomain, the CLR defines a proxy type in the destination AppDomain\u2019s loader heap. This proxy type is defined using the original type\u2019s metadata, and therefore, it looks exactly like the original type; it has all of the same instance members (properties, events, and methods). The instance fields are not part of the type, but I\u2019ll talk more about this in a moment. This new type does have some instance fields defined inside of it, but these fields are not identical to that of the original data type. Instead, these fields indicate which AppDomain \u201cowns\u201d the real object and how to find the real object in the owning AppDomain. (Internally, the proxy object uses a GCHandle instance that refers to the real object. The GCHandle type is discussed in Chapter 21, \u201cThe Managed Heap and Garbage Collection.\u201d)</p><p>After this type is defined in the destination AppDomain, CreateInstanceAndUnwrap creates an instance of this proxy type, initializes its fields to identify the source AppDomain and the real object, and returns a reference to this proxy object to the destination AppDomain. In my Ch22-1-AppDomains application, the mbrt variable will be set to refer to this proxy. Notice that the object returned from</p><p>CreateInstanceAndUnwrap is actually not an instance of the MarshalByRefType type. The CLR will usually not allow you to cast an object of one type to an incompatible type. However, in this situation, the CLR does allow the cast, because this new type has the same instance members as defined on the original type. In fact, if you use the proxy object to call GetType, it actually lies to you and says that it is a MarshalByRefType object.</p><p>However, it is possible to prove that the object returned from CreateInstanceAndUnwrap is actually a reference to a proxy object. To do this, my Ch22-1-AppDomains application calls System. Runtime.Remoting.RemotingService\u2019s public, static IsTransparentProxy method passing in the reference returned from CreateInstanceAndUnwrap. As you can see from the output, IsTransparentProxy returns true, indicating that the object is a proxy.</p><p>Now, my Ch22-1-AppDomains application uses the proxy to call the SomeMethod method. Because the mbrt variable refers to a proxy object, the proxy\u2019s implementation of this method is called. The proxy\u2019s implementation uses the information fields inside the proxy object to transition the calling thread from the default AppDomain to the new AppDomain. Any actions now performed by this thread run under the new AppDomain\u2019s security and configuration settings. Next, the thread uses the proxy object\u2019s GCHandle field to find the real object in the new AppDomain, and then it uses the real object to call the real SomeMethod method.</p><p>There are two ways to prove that the calling thread has transitioned from the default AppDomain to the new AppDomain. First, inside the SomeMethod method, I call Thread.GetDomain().FriendlyName. This will return \u201cAD #2\u201d (as evidenced by the output) because the thread is now running in the new AppDomain created by using AppDomain.CreateDomain with \u201cAD #2\u201d as the friendly name parameter. Second, if you step through the code in a debugger and display the Call Stack window, the [AppDomain Transition] line marks where a thread has transitioned across an AppDomain boundary. See the Call Stack window near the bottom of Figure 22-2.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile185.png\"/><figcaption>FIGURE 22-2 The Debugger\u2019s Call Stack window showing an AppDomain transition.</figcaption><p>When the real SomeMethod method returns, it returns to the proxy\u2019s SomeMethod method, which transitions the thread back to the default AppDomain, and then the thread continues executing code in the default AppDomain.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile186.png\"/><p>Note When a thread in one AppDomain calls a method in another AppDomain, the thread transitions between the two AppDomains. This means that method calls across AppDomain boundaries are executed synchronously. However, at any given time, a thread is considered to be in just one AppDomain, and it executes code using that AppDomain\u2019s security and configuration settings. If you want to execute code in multiple AppDomains concurrently, you should create additional threads and have them execute whatever code you want in whatever AppDomains you want.</p><p>The next thing that my Ch22-1-AppDomains application does is call AppDomain\u2019s public, static Unload method to force the CLR to unload the specified AppDomain, including all of the assemblies loaded into it. A garbage collection is forced to free up any objects that were created by code in the unloading AppDomain. At this point, the default AppDomain\u2019s mbrt variable still refers to a valid proxy object; however, the proxy object no longer refers to a valid AppDomain (because it has been unloaded).</p><p>When the default AppDomain attempts to use the proxy object to call the SomeMethod method,</p><p>the proxy\u2019s implementation of this method is called. The proxy\u2019s implementation determines that the AppDomain that contained the real object has been unloaded, and the proxy\u2019s SomeMethod method throws an AppDomainUnloadedException to let the caller know that the operation cannot complete.</p><p>Wow! The CLR team at Microsoft had to do a lot of work to ensure AppDomain isolation, but it is important work because these features are used heavily and are being used more and more by developers every day. Obviously, accessing objects across AppDomain boundaries by using marshalby-reference semantics has some performance costs associated with it, so you typically want to keep the use of this feature to a minimum.</p><p>I promised you that I\u2019d talk a little more about instance fields. A type derived from MarshalByRefObject can define instance fields. However, these instance fields are not defined as being part of the proxy type and are not contained inside a proxy object. When you write code that reads from or writes to an instance field of a type derived from MarshalByRefObject, the JIT compiler emits code that uses the proxy object (to find the real AppDomain/object) by calling System.Object\u2019s FieldGetter or FieldSetter methods, respectively. These methods are private and undocumented; they are basically methods that use reflection to get and set the value in a field. So although you can access fields of a type derived from MarshalByRefObject, the performance is particularly bad because the CLR really ends up calling methods to perform the field access. In fact, the performance is bad even if the object that you are accessing is in your own AppDomain.3</p><p>3 If the CLR required that all fields be private (which is recommended for good data encapsulation), then the FieldGetter and FieldSetter methods wouldn\u2019t have to exist and accessing fields from methods could always have been direct, avoiding any performance penalty.</p><h6>The Performance of Accessing Instance Fields</h6><p>To demonstrate the significance of this performance hit, I wrote the following code.</p><p>private sealed class NonMBRO : Object { public Int32 x; } private sealed class MBRO : MarshalByRefObject { public Int32 x; }</p><p>private static void FieldAccessTiming(){ const Int32 count = 100000000; NonMBRO nonMbro = new NonMBRO(); MBRO mbro = new MBRO();</p><p>Stopwatch sw = Stopwatch.StartNew(); for (Int32 c = 0; c &lt; count; c++) nonMbro.x++; Console.WriteLine(\"{0}\", sw.Elapsed); // 00:00:00.4073560</p><p>}</p><p>sw = Stopwatch.StartNew(); for (Int32 c = 0; c &lt; count; c++) mbro.x++; Console.WriteLine(\"{0}\", sw.Elapsed); // 00:00:02.5388665</p><p>When I ran this code, it took ~.4 seconds to access the instance field of a NonMBRO class that is derived from Object, and it took 2.54 seconds to access the instance field of an MBRO class that is derived from MarshalByRefObject. So, accessing an instance field of a class derived from MarshalByRefObject takes more than ~6 times longer!</p><p>From a usability standpoint, a type derived from MarshalByRefObject should really avoid defining any static members. The reason is that static members are always accessed in the context of the calling AppDomain. No AppDomain transition can occur because a proxy object contains the information identifying which AppDomain to transition to, but there is no proxy object when calling a static member. Having a type\u2019s static members execute in one AppDomain while instance members execute in another AppDomain would make a very awkward programming model.</p><p>Because there are no roots in the second AppDomain, the original object referred to by the proxy could be garbage collected. Of course, this is not ideal. On the other hand, if the original object is held in memory indefinitely, then the proxy could go away and the original object would still live; this is also not ideal. The CLR solves this problem by using a lease manager. When a proxy for an object is created, the CLR keeps the object alive for five minutes. If no calls have been made through the proxy after five minutes, then the object is deactivated and will have its memory freed at the next garbage collection. After each call into the object, the lease manager renews the object\u2019s lease so that it is guaranteed to remain in memory for another two minutes before being deactivated. If an application attempts to call into an object through a proxy after the object\u2019s lease has expired, the CLR throws a System.Runtime.Remoting.RemotingException.</p><p>It is possible to override the default lease times of five minutes and two minutes by overriding MarshalByRefObject\u2019s virtual InitializeLifetimeServices method. For more information, see the section titled \u201cLifetime Leases\u201d in the .NET Framework SDK documentation.</p><h6>Demo #2: Cross-AppDomain Communication Using Marshal-by-Value</h6><p>Demo #2 is very similar to Demo #1. Again, another AppDomain is created exactly as Demo #1 did it. Then, CreateInstanceAndUnwrap is called to load the same assembly into the new AppDomain and create an instance of a MarshalByRefType object in this new AppDomain. Next, the CLR creates a proxy to the object and the mbrt variable (in the default AppDomain) is initialized referring to the proxy. Now, using the proxy, I call MethodWithReturn. This method, which takes no arguments, will execute in the new AppDomain to create an instance of the MarshalByValType type before returning a reference to the object to the default AppDomain.</p><p>MarshalByValType is not derived from System.MarshalByRefObject, and therefore, the CLR cannot define a proxy type to create an instance from; the object can\u2019t be marshaled by reference across the AppDomain boundary.</p><p>However, because MarshalByValType is marked with the [Serializable] custom attribute, MethodWithReturn is allowed to marshal the object by value. The next paragraph describes what</p><p>it means to marshal an object by value from one AppDomain (the source AppDomain) to another AppDomain (the destination AppDomain). For more information about the CLR's serialization and deserialization mechanisms, see Chapter 24, \"Runtime Serialization.\u201d</p><p>When a source AppDomain wants to send or return a reference to an object to a destination AppDomain, the CLR serializes the object\u2019s instance fields into a byte array. This byte array is copied from the source AppDomain to the destination AppDomain. Then, the CLR deserializes the byte array in the destination AppDomain. This forces the CLR to load the assembly that defines the type being deserialized into the destination AppDomain if it is not already loaded. Then, the CLR creates an instance of the type and uses the values in the byte array to initialize the object\u2019s fields so that they have values identical to those they had in the original object. In other words, the CLR makes an exact duplicate of the source object in the destination\u2019s AppDomain MethodWithReturn, and then returns a reference to this copy; the object has been marshaled by value across the AppDomain\u2019s boundary.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile187.png\"/><p>Important When loading the assembly, the CLR uses the destination AppDomain\u2019s poli-</p><p>cies and configuration settings (for example, the AppDomain can have a different AppBase directory or different version binding redirections). These policy differences might prevent the CLR from locating the assembly. If the assembly cannot be loaded, an exception will be thrown, and the destination will not receive a reference to the object.</p><p>At this point, the object in the source AppDomain and the object in the destination AppDomain live separate lifetimes, and their states can change independently of each other. If there are no roots in the source AppDomain keeping the original object alive (as in my Ch22-1-AppDomains application), its memory will be reclaimed at the next garbage collection.</p><p>To prove that the object returned from MethodWithReturn is not a reference to a proxy object, my Ch22-1-AppDomains application calls System.Runtime.Remoting.RemotingService\u2019s public, static IsTransparentProxy method passing in the reference returned from MethodWithReturn. As you can see from the output, IsTransparentProxy returns false, indicating that the object is a real object, not a proxy.</p><p>Now, my program uses the real object to call the ToString method. Because the mbvt variable refers to a real object, the real implementation of this method is called, and no AppDomain transition occurs. This can be evidenced by examining the debugger\u2019s Call Stack window, which will not show an [Appdomain Transition] line.</p><p>To further prove that no proxy is involved, my Ch22-1-AppDomains application unloads the new AppDomain and then attempts to call the ToString method again. Unlike in Demo #1, the call succeeds this time because unloading the new AppDomain had no impact on objects \u201cowned\u201d by the default AppDomain, and this includes the object that was marshaled by value.</p><h6>Demo #3: Cross-AppDomain Communication Using Non-Marshalable Types</h6><p>Demo #3 starts out very similar to Demos #1 and #2. Just as in Demos #1 and #2, an AppDomain is created. Then, CreateInstanceAndUnwrap is called to load the same assembly into the new AppDomain, create a MarshalByRefType object in this new AppDomain, and have the mbrt variable refer to a proxy to this object.</p><p>Then, using this proxy, I call MethodArgAndReturn, which accepts an argument. Again, the CLR must maintain AppDomain isolation, so it cannot simply pass the reference to the argument into the new AppDomain. If the type of the object is derived from MarshalByRefObject, the CLR will make a proxy for it and marshal it by reference. If the object\u2019s type is marked as [Serializable], the CLR will serialize the object (and its children) to a byte array, marshal the byte array into the new AppDomain, and then deserialize the byte array into an object graph, passing the root of the object graph into the MethodArgAndReturn method.</p><p>In this particular demo, I am passing a System.String object across AppDomain boundaries. The System.String type is not derived from MarshalByRefObject, so the CLR cannot create a proxy. Fortunately, System.String is marked as [Serializable], and therefore the CLR can marshal it by value, which allows the code to work. Note that for String objects, the CLR performs a special optimization. When marshaling a String object across an AppDomain boundary, the CLR just passes the reference to the String object across the boundary; it does not make a copy of the String object. The CLR can offer this optimization because String objects are immutable; therefore, it is impossible for code in one AppDomain to corrupt a String object\u2019s characters.4 For more about String immutability, see Chapter 14, \u201cChars, Strings, and Working with Text.\u201d</p><p>4 By the way, this is why the System.String class is sealed. If the class were not sealed, then you could define your own class derived from String and add your own fields. If you did this, there is no way that the CLR could ensure that your \u201cstring\u201d class was immutable.</p><p>Inside MethodArgAndReturn, I display the string passed into it to show that the string came across the AppDomain boundary, and then I create an instance of the NonMarshalableType type and return a reference to this object to the default AppDomain. Because NonMarshalableType is not derived from System.MarshalByRefObject and is also not marked with the [Serializable] custom attribute, MethodArgAndReturn is not allowed to marshal the object by reference or by value\u2014the object cannot be marshaled across an AppDomain boundary at all! To report this, MethodArgAndReturn throws a SerializationException in the default AppDomain. Because my program doesn\u2019t catch this exception, the program just dies.</p><h6>AppDomain Unloading</h6><p>One of the great features of AppDomains is that you can unload them. Unloading an AppDomain causes the CLR to unload all of the assemblies in the AppDomain, and the CLR frees the AppDomain\u2019s loader heap as well. To unload an AppDomain, you call AppDomain\u2019s Unload static method (as the Ch22-1-AppDomains application does). This call causes the CLR to perform a lot of actions to gracefully unload the specified AppDomain:</p><ul style=\"list-style-type: none;\">\n<li><p>1. The CLR suspends all threads in the process that have ever executed managed code.</p></li>\n<li><p>2. The CLR examines all of the threads\u2019 stacks to see which threads are currently executing code in the AppDomain being unloaded, or which threads might return at some point to code in the AppDomain that is being unloaded. The CLR forces any threads that have the unloading AppDomain on their stack to throw a ThreadAbortException (resuming the thread\u2019s execution). This causes the threads to unwind, executing any finally blocks on their way out so that cleanup code executes. If no code catches the ThreadAbortException, it will eventually become an unhandled exception that the CLR swallows; the thread dies, but the process is allowed to continue running. This is unusual, because for all other unhandled exceptions, the CLR kills the process.</p></li>\n</ul><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile188.png\"/><p>Important The CLR will not immediately abort a thread that is currently executing code in a finally block, catch block, a class constructor, a critical execution region, or in unmanaged code. If the CLR allowed this, cleanup code, error recovery code, type initialization code, critical code, or arbitrary code that the CLR knows nothing about would not complete, resulting in the application behaving unpredictably and with potential security holes. An aborting thread is allowed to finish executing these code blocks and then, at the end of the code block, the CLR forces the thread to throw a ThreadAbortException.</p><ul style=\"list-style-type: none;\">\n<li><p>3. After all threads discovered in step 2 have left the AppDomain, the CLR then walks the heap and sets a flag in each proxy object that referred to an object created by the unloaded AppDomain. These proxy objects now know that the real object they referred to is gone. If any code now calls a method on an invalid proxy object, the method will throw an AppDomainUnloadedException.</p></li>\n<li><p>4. The CLR forces a garbage collection to occur, reclaiming the memory used by any objects that were created by the now unloaded AppDomain. The Finalize methods for these objects are called, giving the objects a chance to clean themselves up properly.</p></li>\n<li><p>5. The CLR resumes all of the remaining threads. The thread that called AppDomain.Unload will now continue running; calls to AppDomain.Unload occur synchronously.</p></li>\n</ul><p>My Ch22-1-AppDomains application uses just one thread to do all of the work. Whenever my code calls AppDomain.Unload, there are no threads in the unloading AppDomain, and therefore, the CLR doesn\u2019t have to throw any ThreadAbortException exceptions. I\u2019ll talk more about ThreadAbortException later in this chapter.</p><p>By the way, when a thread calls AppDomain.Unload, the CLR waits 10 seconds for the threads in the unloading AppDomain to leave it. If after 10 seconds, the thread that called AppDomain.Unload doesn\u2019t return, it will throw a CannotUnloadAppDomainException, and the AppDomain may or may not be unloaded in the future.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile189.png\"/><p>Note If a thread calling AppDomain.Unload is in the AppDomain being unloaded, the CLR creates another thread that attempts to unload the AppDomain. The first thread will forcibly throw the ThreadAbortException and unwind. The new thread will wait for the AppDomain to unload, and then the new thread terminates. If the AppDomain fails to unload, the new thread will process a CannotUnloadAppDomainException, but because you did not write the code that this new thread executes, you can\u2019t catch this exception.</p><h6>AppDomain Monitoring</h6><p>A host application can monitor the resources that an AppDomain consumes. Some hosts will use this information to decide when to forcibly unload an AppDomain should its memory or CPU consumption rise above what the host considers reasonable. Monitoring can also be used to compare the resource consumption of different algorithms to determine which uses fewer resources. Because AppDomain monitoring incurs additional overhead, hosts must explicitly turn the monitoring on by setting AppDomain\u2019s static MonitoringEnabled property to true. This turns on monitoring for all AppDomains. After monitoring is turned on, it cannot be turned off; attempting to set the MonitoringEnabled property to false causes an ArgumentException to be thrown.</p><p>After monitoring is turned on, your code can query the following four read-only properties offered by the AppDomain class:</p><p>\u25a0\u25a0 MonitoringSurvivedProcessMemorySize This static Int64 property returns the number of bytes that are currently in use by all AppDomains controlled by the current CLR instance. The number is accurate as of the last garbage collection.</p><p>\u25a0\u25a0 MonitoringTotalAllocatedMemorySize This instance Int64 property returns the number of bytes that have been allocated by a specific AppDomain. The number is accurate as of the last garbage collection.</p><p>\u25a0\u25a0 MonitoringSurvivedMemorySize This instance Int64 property returns the number of bytes that are currently in use by a specific AppDomain. The number is accurate as of the last garbage collection.</p><p>\u25a0\u25a0 MonitoringTotalProcessorTime This instance TimeSpan property returns the amount of</p><p>CPU usage incurred by a specific AppDomain.</p><p>The following class shows how to use three of these properties to see what has changed within an AppDomain between two points in time.</p><p>private sealed class AppDomainMonitorDelta : IDisposable { private AppDomain m_appDomain; private TimeSpan m_thisADCpu; private Int64 m_thisADMemoryInUse; private Int64 m_thisADMemoryAllocated;</p><p>static AppDomainMonitorDelta() { // Make sure that AppDomain monitoring is turned on AppDomain.MonitoringIsEnabled = true;</p><p>}</p><p>public AppDomainMonitorDelta(AppDomain ad) { m_appDomain = ad ?? AppDomain.CurrentDomain; m_thisADCpu = m_appDomain.MonitoringTotalProcessorTime; m_thisADMemoryInUse = m_appDomain.MonitoringSurvivedMemorySize; m_thisADMemoryAllocated = m_appDomain.MonitoringTotalAllocatedMemorySize;</p><p>}</p><p>public void Dispose() { GC.Collect(); Console.WriteLine(\u201cFriendlyName={0}, CPU={1}ms\u201d, m_appDomain.FriendlyName,</p><p>(m_appDomain.MonitoringTotalProcessorTime \u00ad m_thisADCpu).TotalMilliseconds);</p><p>Console.WriteLine(\u201c Allocated {0:N0} bytes of which {1:N0} survived GCs\u201d, m_appDomain.MonitoringTotalAllocatedMemorySize \u00ad m_thisADMemoryAllocated, m_appDomain.MonitoringSurvivedMemorySize \u00ad m_thisADMemoryInUse);</p><p>} }</p><p>The following code shows how to use the AppDomainMonitorDelta class.</p><p>private static void AppDomainResourceMonitoring() {</p><p>using (new AppDomainMonitorDelta(null)) { // Allocate about 10 million bytes that will survive collections var list = new List&lt;Object&gt;(); for (Int32 x = 0; x &lt; 1000; x++) list.Add(new Byte[10000]);</p><p>// Allocate about 20 million bytes that will NOT survive collections for (Int32 x = 0; x &lt; 2000; x++) new Byte[10000].GetType();</p><p>} }</p><p>// Spin the CPU for about 5 seconds Int64 stop = Environment.TickCount + 5000; while (Environment.TickCount &lt; stop) ;</p><p>When I execute this code, I get the following output.</p><p>FriendlyName=03\u00adCh22\u00ad1\u00adAppDomains.exe, CPU=5031.25ms Allocated 30,159,496 bytes of which 10,085,080 survived GCs</p><h6>AppDomain First-Chance Exception Notifications</h6><p>Each AppDomain can have associated with it a series of callback methods that get invoked when the CLR begins looking for catch blocks within an AppDomain. These methods can perform logging, or a host can use this mechanism to monitor exceptions being thrown within an AppDomain. The callbacks cannot handle the exception or swallow it in any way; they are just receiving a notification that the exception has occurred. To register a callback method, just add a delegate to AppDomain\u2019s instance FirstChanceException event.</p><p>Here is how the CLR processes an exception: when the exception is first thrown, the CLR invokes any FirstChanceException callback methods registered with the AppDomain that are throwing the exception. Then, the CLR looks for any catch blocks on the stack that are within the same AppDomain. If a catch block handles the exception, then processing of the exception is complete and execution continues as normal. If the AppDomain has no catch block to handle the exception, then the CLR walks up the stack to the calling AppDomain and throws the same exception object again (after serializing and deserializing it). At this point, it is as if a brand new exception is being thrown, and the CLR invokes any FirstChanceException callback methods registered with the now current AppDomain. This continues until the top of the thread\u2019s stack is reached. At that point, if the exception is not handled by any code, the CLR terminates the whole process.</p><h6>How Hosts Use AppDomains</h6><p>So far, I\u2019ve talked about hosts and how they load the CLR. I\u2019ve also talked about how the hosts tell the CLR to create and unload AppDomains. To make the discussion more concrete, I\u2019ll describe some common hosting and AppDomain scenarios. In particular, I\u2019ll explain how different application types host the CLR and how they manage AppDomains.</p><h6>Executable Applications</h6><p>Console UI applications, NT Service applications, Windows Forms applications, and Windows Presentation Foundation (WPF) applications are all examples of self-hosted applications that have managed EXE files. When Windows initializes a process by using a managed EXE file, Windows loads the shim, and the shim examines the CLR header information contained in the application\u2019s assembly (the EXE file). The header information indicates the version of the CLR that was used to build and test the application. The shim uses this information to determine which version of the CLR to load into the process. After the CLR loads and initializes, it again examines the assembly\u2019s CLR header to determine which method is the application\u2019s entry point (Main). The CLR invokes this method, and the application is now up and running.</p><p>As the code runs, it accesses other types. When referencing a type contained in another assembly, the CLR locates the necessary assembly and loads it into the same AppDomain. Any additionally referenced assemblies also load into the same AppDomain. When the application\u2019s Main method returns, the Windows process terminates (destroying the default AppDomain and all other AppDomains).</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile190.png\"/><p>Note By the way, you can call System.Environment\u2019s static Exit method if you want to shut down the Windows process including all of its AppDomains. Exit is the most graceful way of terminating a process because it first calls the Finalize methods of all of the objects on the managed heap and then releases all of the unmanaged COM objects held by the CLR. Finally, Exit calls the Win32 ExitProcess function.</p><p>It\u2019s possible for the application to tell the CLR to create additional AppDomains in the process\u2019s address space. In fact, this is what my Ch22-1-AppDomains application did.</p><h6>Microsoft Silverlight Rich Internet Applications</h6><p>Microsoft\u2019s Silverlight runtime technology uses a special CLR that is different from the normal desktop version of the .NET Framework. After the Silverlight runtime is installed, navigating to a website that uses Silverlight causes the Silverlight CLR (CoreClr.dll) to load in your browser (which may or may not be Windows Internet Explorer\u2014you may not even be using a Windows machine). Each Silverlight control on the page runs in its own AppDomain. When the user closes a tab or navigates to another website, any Silverlight controls no longer in use have their AppDomains unloaded. The Silverlight code running in the AppDomain runs in a limited-security sandbox so that it cannot harm the user or the machine in any way.</p><h6>Microsoft ASP.NET and XML Web Services Applications</h6><p>ASP.NET is implemented as an ISAPI DLL (implemented in ASPNet_ISAPI.dll). The first time a client requests a URL handled by the ASP.NET ISAPI DLL, ASP.NET loads the CLR. When a client makes a request of a web application, ASP.NET determines if this is the first time a request has been made. If it is, ASP.NET tells the CLR to create a new AppDomain for this web application; each web application is identified by its virtual root directory. ASP.NET then tells the CLR to load the assembly that contains the type exposed by the web application into this new AppDomain, creates an instance of this type, and starts calling methods in it to satisfy the client\u2019s web request. If the code references more types, the CLR will load the required assemblies into the web application\u2019s AppDomain.</p><p>When future clients make requests of an already running web application, ASP.NET doesn\u2019t create a new AppDomain; instead, it uses the existing AppDomain, creates a new instance of the web application\u2019s type, and starts calling methods. The methods will already be JIT-compiled into native code, so the performance of processing all subsequent client requests is excellent.</p><p>If a client makes a request of a different web application, ASP.NET tells the CLR to create a new AppDomain. This new AppDomain is typically created inside the same worker process as the other AppDomains. This means that many web applications run in a single Windows process, which improves the efficiency of the system overall. Again, the assemblies required by each web application are loaded into an AppDomain created for the sole purpose of isolating that web application\u2019s code and objects from other web applications.</p><p>A fantastic feature of ASP.NET is that the code for a website can be changed on the fly without shutting down the web server. When a website\u2019s file is changed on the hard disk, ASP.NET detects this, unloads the AppDomain that contains the old version of the files (when the last currently running request finishes), and then creates a new AppDomain, loading into it the new versions of the files. To make this happen, ASP.NET uses an AppDomain feature called shadow copying.</p><h6>Microsoft SQL Server</h6><p>Microsoft SQL Server is an unmanaged application because most of its code is still written in unmanaged C++. SQL Server allows developers to create stored procedures by using managed code. The first time a request comes in to the database to run a stored procedure written in managed code, SQL Server loads the CLR. Stored procedures run in their own secured AppDomain, prohibiting the stored procedures from adversely affecting the database server.</p><p>This functionality is absolutely incredible! It means that developers will be able to write stored procedures in the programming language of their choice. The stored procedure can use strongly typed data objects in its code. The code will also be JIT-compiled into native code when executed instead of being interpreted. And developers can take advantage of any types defined in the Framework Class Library (FCL) or in any other assembly. The result is that our job becomes much easier and our applications perform much better. What more could a developer ask for?!</p><h6>Your Own Imagination</h6><p>Productivity applications such as word processors and spreadsheets also allow users to write macros in any programming language they choose. These macros will have access to all of the assemblies and types that work with the CLR. They will be compiled, so they will execute fast, and, most important, these macros will run in a secure AppDomain so that users don\u2019t get hit with any unwanted surprises. Your own applications can use this ability, too, in any way you want.</p><h6>Advanced Host Control</h6><p>In this section, I\u2019ll mention some more advanced topics related to hosting the CLR. My intent is to give you a taste of what is possible, and this will help you to understand more of what the CLR is capable of. I encourage you to seek out other texts if you find this information particularly interesting.</p><h6>Managing the CLR by Using Managed Code</h6><p>The System.AppDomainManager class allows a host to override CLR default behavior by using managed code instead of using unmanaged code. Of course, using managed code makes implementing a host easier. All you need to do is define your class and derive it from the System.AppDomainManager class, overriding any virtual methods where you want to take over control. Your class should then be built into its very own assembly and installed into the global assembly cache (GAC) because the assembly needs to be granted full-trust, and all assemblies in the GAC are always granted full-trust.</p><p>Then, you need to tell the CLR to use your AppDomainManager-derived class. In code, the best way to do this is to create an AppDomainSetup object initializing its AppDomainManagerAssembly and AppDomainManagerType properties, both of which are of type String. Set the AppDomainManagerAssembly property to the string identifying the strong-name identity of the assembly that defines your AppDomainManager-derived class, and then set the AppDomainManagerType property to the full name of your AppDomainManager-derived class. Alternatively, AppDomainManager can be set in your application\u2019s XML configuration file by using the appDomainManagerAssembly and appDomainManagerType elements. In addition, a native host could query for the ICLRControl interface and call this interface\u2019s SetAppDomainManagerType function, passing in the identity of the GAC-installed assembly and the name of the AppDomainManager-derived class.5</p><p>Now, let\u2019s talk about what an AppDomainManager-derived class can do. The purpose of the AppDomainManager-derived class is to allow a host to maintain control even when an add-in tries to create AppDomains of its own. When code in the process tries to create a new AppDomain, the AppDomainManager-derived object in that AppDomain can modify security and configuration settings.</p><p>5 It is also possible to configure an AppDomainManager by using environment variables and registry settings, but these mechanisms are more cumbersome than the methods mentioned in the text and should be avoided except for some testing scenarios.</p><p>It can also decide to fail an AppDomain creation, or it can decide to return a reference to an existing AppDomain instead. When a new AppDomain is created, the CLR creates a new AppDomainManagerderived object in the AppDomain. This object can also modify configuration settings, how execution context is flowed between threads, and permissions granted to an assembly.</p><h6>Writing a Robust Host Application</h6><p>A host can tell the CLR what actions to take when a failure occurs in managed code. Here are some examples (listed from least severe to most severe):</p><p>\u25a0\u25a0 The CLR can abort a thread if the thread is taking too long to execute and return a response.</p><p>(I\u2019ll discuss this more in the next section.)</p><p>\u25a0\u25a0 The CLR can unload an AppDomain. This aborts all of the threads that are in the AppDomain</p><p>and causes the problematic code to be unloaded.</p><p>\u25a0\u25a0 The CLR can be disabled. This stops any more managed code from executing in the process,</p><p>but unmanaged code is still allowed to run.</p><p>\u25a0\u25a0 The CLR can exit the Windows process. This aborts all of the threads and unloads all of the</p><p>AppDomains first so that cleanup operations occur, and then the process terminates.</p><p>The CLR can abort a thread or AppDomain gracefully or rudely. A graceful abort means that cleanup code executes. In other words, code in finally blocks runs, and objects have their Finalize methods executed. A rude abort means that cleanup code does not execute. In other words, code in finally blocks may not run, and objects may not have their Finalize methods executed. A graceful abort cannot abort a thread that is in a catch or finally block. However, a rude abort will abort a thread that is in a catch or finally block. Unfortunately, a thread that is in unmanaged code or in a constrained execution region (CER) cannot be aborted at all.</p><p>A host can set what is called an escalation policy, which tells the CLR how to deal with managed code failures. For example, SQL Server tells the CLR what to do should an unhandled exception be thrown while the CLR is executing managed code. When a thread experiences an unhandled exception, the CLR first attempts to upgrade the exception to a graceful thread abort. If the thread does not abort in a specified time period, the CLR attempts to upgrade the graceful thread abort to a rude thread abort.</p><p>What I just described is what usually happens. However, if the thread experiencing the unhandled exception is in a critical region, the policy is different. A thread that is in a critical region is a thread that has entered a thread synchronization lock that must be released by the same thread, for example, a thread that has called Monitor.Enter, Mutex\u2019s WaitOne, or one of ReaderWriterLock\u2019s AcquireReaderLock or AcquireWriterLock methods.6 Successfully waiting for an AutoResetEvent, ManualResetEvent, or Semaphore doesn\u2019t cause the thread to be in a critical region because another</p><p>6 All of these locks internally call Thread\u2019s BeginCriticalRegion and EndCriticalRegion methods to indicate when they enter and leave critical regions. Your code can call these methods too if you need to. Normally, this would be necessary only if you are interoperating with unmanaged code.</p><p>thread can signal these synchronization objects. When a thread is in a critical region, the CLR believes that the thread is accessing data that is shared by multiple threads in the same AppDomain. After all, this is probably why the thread took the lock. If the thread is accessing shared data, just terminating the thread isn\u2019t good enough, because other threads may then try to access the shared data that is now corrupt, causing the AppDomain to run unpredictably or with possible security vulnerabilities.</p><p>So, when a thread in a critical region experiences an unhandled exception, the CLR first attempts</p><p>to upgrade the exception to a graceful AppDomain unload in an effort to get rid of all of the threads and data objects that are currently in use. If the AppDomain doesn\u2019t unload in a specified amount of time, the CLR upgrades the graceful AppDomain unload to a rude AppDomain unload.</p><h6>How a Host Gets Its Thread Back</h6><p>Normally, a host application wants to stay in control of its threads. Let\u2019s take a database server as an example. When a request comes into the database server, a thread picks up the request and then dispatches the request to another thread that is to perform the actual work. This other thread may need to execute code that wasn\u2019t created and tested by the team that produced the database server. For example, imagine a request coming into the database server to execute a stored procedure written in managed code by the company running the server. It\u2019s great that the database server can run the stored procedure code in its own AppDomain, which is locked down with security. This prevents the stored procedure from accessing any objects outside of its own AppDomain, and it also prevents the code from accessing resources that it is not allowed to access, such as disk files or the clipboard.</p><p>But what if the code in the stored procedure enters an infinite loop? In this case, the database server has dispatched one of its threads into the stored procedure code, and this thread is never coming back. This puts the server in a precarious position; the future behavior of the server is unknown. For example, the performance might be terrible now because a thread is in an infinite loop. Should the server create more threads? Doing so uses more resources (such as stack space), and these threads could also enter an infinite loop themselves.</p><p>To solve these problems, the host can take advantage of thread aborting. Figure 22-3 shows the typical architecture of a host application trying to solve the runaway thread problem. Here\u2019s how it works (the numbers correspond to the circled numbers in the figure):</p><ul style=\"list-style-type: none;\">\n<li><p>1. A client sends a request to the server.</p></li>\n<li><p>2. A server thread picks up this request and dispatches it to a thread pool thread to perform the actual work.</p></li>\n<li><p>3. A thread pool thread picks up the client request and executes trusted code written by the company that built and tested the host application.</p></li>\n</ul><ul style=\"list-style-type: none;\">\n<li><p>4. This trusted code then enters a try block, and from within the try block, calls across an AppDomain boundary (via a type derived from MarshalByRefObject). This AppDomain contains the untrusted code (perhaps a stored procedure) that was not built and tested by the company that produced the host application. At this point, the server has given control of its thread to some untrusted code; the server is feeling nervous right now.</p></li>\n<li><p>5. When the host originally received the client\u2019s request, it recorded the time. If the untrusted code doesn\u2019t respond to the client in some administrator-set amount of time, the host calls Thread\u2019s Abort method, asking the CLR to stop the thread pool thread, forcing it to throw a ThreadAbortException.</p></li>\n<li><p>6. At this point, the thread pool thread starts unwinding, calling finally blocks so that cleanup code executes. Eventually, the thread pool thread crosses back over the AppDomain boundary. Because the host\u2019s stub code called the untrusted code from inside a try block, the host\u2019s stub code has a catch block that catches the ThreadAbortException.</p></li>\n<li><p>7. In response to catching the ThreadAbortException, the host calls Thread\u2019s ResetAbort method. I\u2019ll explain the purpose of this call shortly.</p></li>\n<li><p>8. Now that the host\u2019s code has caught the ThreadAbortException, the host can return some sort of failure back to the client and allow the thread pool thread to return to the pool so that it can be used for a future client request.</p></li>\n</ul><p>1</p><p>Host\u2019s supervisor code</p><p>5</p><p>Thread.Abort()</p><p>2</p><p>AppDomain boundary</p><p>3</p><p>4</p><p>Host\u2019s stub (trusted)</p><p>Add-in code (untrusted)</p><p>Thread pool</p><p>7 6</p><p>Thread.ResetAbort()</p><p>8 FIGURE 22-3 How a host application gets its thread back.</p><p>Let me now clear up a few loose ends about this architecture. First, Thread\u2019s Abort method is asynchronous. When Abort is called, it sets the target thread\u2019s AbortRequested flag and returns immediately. When the runtime detects that a thread is to be aborted, the runtime tries to get the thread to a safe place. A thread is in a safe place when the runtime feels that it can stop what the thread is doing without causing disastrous effects. A thread is in a safe place if it is performing a managed blocking operation such as sleeping or waiting. A thread can be corralled to a safe place by using hijacking (described in Chapter 21). A thread is not in a safe place if it is executing a type\u2019s class constructor, code in a catch or finally block, code in a CER, or unmanaged code.</p><p>After the thread reaches a safe place, the runtime will detect that the AbortRequested flag is set for the thread. This causes the thread to throw a ThreadAbortException. If this exception is not caught, the exception will be unhandled, all pending finally blocks will execute, and the thread will kill itself gracefully. Unlike all other exceptions, an unhandled ThreadAbortException does not cause the application to terminate. The runtime silently eats this exception and the thread dies, but the application and all of its remaining threads continue to run just fine.</p><p>In my example, the host catches the ThreadAbortException, allowing the host to regain control of the thread and return it to the pool. But there is a problem: What is to stop the untrusted code from catching the ThreadAbortException itself to keep control of the thread? The answer is that the CLR treats the ThreadAbortException in a very special manner. Even when code catches the ThreadAbortException, the CLR doesn\u2019t allow the exception to be swallowed. In other words, at the end of the catch block, the CLR automatically rethrows the ThreadAbortException exception.</p><p>This CLR feature raises another question: If the CLR rethrows the ThreadAbortException at the end of a catch block, how can the host catch it to regain control of the thread? Inside the host\u2019s catch block, there is a call to Thread\u2019s ResetAbort method. Calling this method tells the CLR to stop rethrowing the ThreadAbortException at the end of each catch block.</p><p>This raises yet another question: What\u2019s to stop the untrusted code from catching the ThreadAbortException and calling Thread\u2019s ResetAbort method itself to keep control of the thread? The answer is that Thread\u2019s ResetAbort method requires the caller to have the SecurityPermission with the ControlThread flag set to true. When the host creates the AppDomain for the untrusted code, the host will not grant this permission, and now, the untrusted code cannot keep control of the host\u2019s thread.</p><p>I should point out that there is still a potential hole in this story: while the thread is unwinding from its ThreadAbortException, the untrusted code can execute catch and finally blocks. Inside these blocks, the untrusted code could enter an infinite loop, preventing the host from regaining control of its thread. A host application fixes this problem by setting an escalation policy (discussed earlier). If an aborting thread doesn\u2019t finish in a reasonable amount of time, the CLR can upgrade the thread abort to a rude thread abort, a rude AppDomain unload, disabling of the CLR, or killing of the process. I should also note that the untrusted code could catch the ThreadAbortException and, inside the catch block, throw some other kind of exception. If this other exception is caught, at the end of the catch block, the CLR automatically rethrows the ThreadAbortException.</p><p>It should be noted, though, that most untrusted code is not actually intended to be malicious; it is just written in such a way so as to be taking too long by the host\u2019s standards. Usually, catch and finally blocks contain very little code, and this code usually executes quickly without any infinite loops or long-running tasks. And so it is very unlikely that the escalation policy will have to go into effect for the host to regain control of its thread.</p><p>By the way, the Thread class actually offers two Abort methods: one takes no parameters, and the other takes an Object parameter allowing you to pass anything. When code catches the ThreadAbortException, it can query its read-only ExceptionState property. This property returns the object that was passed to Abort. This allows the thread calling Abort to specify some additional information that can be examined by code catching the ThreadAbortException. The host can use this to let its own handling code know why it is aborting threads.</p><h6>C H A P T E R 2 3</h6><h2>Assembly Loading and Reflection</h2><h6>In this chapter:</h6><p>Assembly Loading . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 584 Using Reflection to Build a Dynamically Extensible Application. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 588 Reflection Performance. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 589 Designing an Application That Supports Add-Ins . . . . . . . . . . . 596 Using Reflection to Discover a Type\u2019s Members . . . . . . . . . . . . . 599</p><p>This chapter is all about discovering information about types, creating instances of them, and accessing their members when you didn\u2019t know anything about them at compile time. The information in this chapter is typically used to create a dynamically extensible application. This is the kind of application for which one company builds a host application and other companies create add-ins to extend the host application. The host can\u2019t be built or tested against the add-ins because the add-ins are created by different companies and are likely to be created after the host application has already shipped. This is why the host needs to discover the add-ins at run time.</p><p>A dynamically extensible application could take advantage of common language runtime (CLR) hosting and AppDomains as discussed in Chapter 22, \u201cCLR Hosting and AppDomains.\u201d The host could run the add-in code in an AppDomain with its own security and configuration settings. The host could also unload the add-in code by unloading the AppDomain. At the end of this chapter, I\u2019ll talk a little about how to put all of this stuff together\u2014CLR hosting, AppDomains, assembly loading, type discovery, type instance construction, and reflection\u2014in order to build a robust, secure, and dynamically extensible application.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile191.png\"/><p>Important For version 4.5 of the .NET Framework, Microsoft has introduced a new reflection API. The old API had many shortcomings. For example, it did not support LINQ well, it had policies embedded in it that were not correct for some languages, it would sometimes force the loading of assemblies unnecessarily, and it was an overly complex API that offered solutions for problems rarely encountered. The new API fixes all of these issues. However, as of .NET 4.5, the new reflection API is not as complete as the old API. With the new API and some extension methods (in the System.Reflection. RuntimeReflectionExtensions class), you can accomplish all you need. Expect additional methods to be added to the new API in future versions of the .NET Framework.</p><p>583</p><p>Of course, for desktop applications, the old API still exists so that it doesn\u2019t break existing code when re-compiling it. However, the new API is the recommended API going forward, and that is why I explain the new API exclusively in this chapter. For Windows Store Apps (where backward compatibility is not an issue), using the new API is mandatory.</p><h6>Assembly Loading</h6><p>As you know, when the just-in-time (JIT) compiler compiles the Intermediate Language (IL) for a method, it sees what types are referenced in the IL code. Then at run time, the JIT compiler uses the assembly\u2019s TypeRef and AssemblyRef metadata tables to determine what assembly defines the type being referenced. The AssemblyRef metadata table entry contains all of the parts that make up the strong name of the assembly. The JIT compiler grabs all of these parts\u2014name (without extension or path), version, culture, and public key token\u2014concatenates them into a string, and then attempts to load an assembly matching this identity into the AppDomain (assuming that it\u2019s not already loaded). If the assembly being loaded is weakly named, the identity is just the name of the assembly (no version, culture, or public key token information).</p><p>Internally, the CLR attempts to load this assembly by using the System.Reflection.Assembly class\u2019s static Load method. This method is publicly documented, and you can call it to explicitly load an assembly into your AppDomain. This method is the CLR equivalent of Win32\u2019s LoadLibrary function. There are actually several overloaded versions of Assembly\u2019s Load method. Here are the prototypes of the more commonly used overloads.</p><p>public class Assembly { public static Assembly Load(AssemblyName assemblyRef); public static Assembly Load(String assemblyString); // Less commonly used overloads of Load are not shown</p><p>}</p><p>Internally, Load causes the CLR to apply a version-binding redirection policy to the assembly and looks for the assembly in the global assembly cache (GAC), followed by the application\u2019s base directory, private path subdirectories, and codebase locations. If you call Load by passing a weakly named assembly, Load doesn\u2019t apply a version-binding redirection policy to the assembly, and the CLR won\u2019t look in the GAC for the assembly. If Load finds the specified assembly, it returns a reference to an Assembly object that represents the loaded assembly. If Load fails to find the specified assembly, it throws a System.IO.FileNotFoundException.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile192.png\"/><p>Note In some extremely rare situations, you may want to load an assembly that was built for a specific CPU architecture. In this case, when specifying an assembly\u2019s identity, you can also include a process architecture part. For example, if my GAC happened to have an ILneutral and an x86-specific version of an assembly, the CLR would favor the CPU-specific version of the assembly (as discussed in Chapter 3, \u201cShared Assemblies and Strongly Named Assemblies\u201d). However, I can force the CLR to load the IL-neutral version by passing the following string to Assembly\u2019s Load method.</p><p>\"SomeAssembly, Version=2.0.0.0, Culture=neutral, PublicKeyToken=01234567890abcde, ProcessorArchitecture=MSIL\"</p><p>Today, the CLR supports five possible values for ProcessorArchitecture: MSIL (Microsoft IL), x86, IA64, AMD64, and Arm.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile193.png\"/><p>Important Some developers notice that System.AppDomain offers a Load method. Unlike Assembly\u2019s static Load method, AppDomain\u2019s Load method is an instance method that allows you to load an assembly into the specified AppDomain. This method was designed to be called by unmanaged code, and it allows a host to inject an assembly into a specific AppDomain. Managed code developers generally shouldn\u2019t call this method because when AppDomain\u2019s Load method is called, you pass it a string that identifies an assembly. The method then applies policy and searches the normal places looking for the assembly. Recall that an AppDomain has settings associated with it that tell the CLR how to look for assemblies. To load this assembly, the CLR will use the settings associated with the specified AppDomain, not the calling AppDomain.</p><p>However, AppDomain\u2019s Load method returns a reference to an assembly. Because the System.Assembly class isn\u2019t derived from System.MarshalByRefObject, the assembly object must be marshaled by value back to the calling AppDomain. But the CLR will now use the calling AppDomain\u2019s settings to locate the assembly and load it. If the assembly can\u2019t be found by using the calling AppDomain\u2019s policy and search locations, a FileNotFoundException is thrown. This behavior is usually undesirable and is the reason that you should avoid AppDomain\u2019s Load method.</p><p>In most dynamically extensible applications, Assembly\u2019s Load method is the preferred way of loading an assembly into an AppDomain. However, it does require that you have all of the pieces that make up an assembly\u2019s identity. Frequently, developers write tools or utilities (such as ILDasm.exe, PEVerify.exe, CorFlags.exe, GACUtil.exe, SGen.exe, SN.exe, XSD.exe) that perform some kind of processing on an assembly. All of these tools take a command-line argument that refers to the path name of an assembly file (including file extension).</p><p>To load an assembly specifying a path name, you call Assembly\u2019s LoadFrom method.</p><p>public class Assembly { public static Assembly LoadFrom(String path); // Less commonly used overloads of LoadFrom are not shown</p><p>}</p><p>Internally, LoadFrom first calls System.Reflection.AssemblyName\u2019s static GetAssemblyName</p><p>method, which opens the specified file, finds the AssemblyDef metadata table\u2019s entry, and extracts the assembly identity information and returns it in a System.Reflection.AssemblyName object (the file is also closed). Then, LoadFrom internally calls Assembly\u2019s Load method, passing it the</p><p>AssemblyName object. At this point, the CLR applies a version-binding redirection policy and searches the various locations looking for a matching assembly. If Load finds the assembly, it will load it, and an Assembly object that represents the loaded assembly will be returned; LoadFrom returns this value. If Load fails to find an assembly, LoadFrom loads the assembly at the path name specified in LoadFrom\u2019s argument. Of course, if an assembly with the same identity is already loaded, LoadFrom simply returns an Assembly object that represents the already loaded assembly.</p><p>By the way, the LoadFrom method allows you to pass a URL as the argument. Here is an example.</p><p>Assembly a = Assembly.LoadFrom(@\"http://Wintellect.com/SomeAssembly.dll\");</p><p>When you pass an Internet location, the CLR downloads the file, installs it into the user\u2019s download cache, and loads the file from there. Note that you must be online or an exception will be thrown. However, if the file has been downloaded previously, and if Windows Internet Explorer has been set to work offline (see Internet Explorer\u2019s Work Offline menu item in its File menu), the previously downloaded file will be used, and no exception will be thrown. You can also call UnsafeLoadFrom, which can load a web-downloaded assembly, bypassing some security checks.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile194.png\"/><p>Important It is possible to have different assemblies on a single machine all with the same identity. Because LoadFrom calls Load internally, it is possible that the CLR will not load the specified file and instead will load a different file, giving you unexpected behavior. It is highly recommended that each build of your assembly change the version number; this ensures that each version has its own identity, and because of this, LoadFrom will now work as expected.</p><p>Microsoft Visual Studio\u2019s UI designers and other tools typically use Assembly\u2019s LoadFile method. This method can load an assembly from any path and can be used to load an assembly with the same identity multiple times into a single AppDomain. This can happen as changes to an application\u2019s UI are made in the designer/tool and the user rebuilds the assembly. When loading an assembly via LoadFile, the CLR will not resolve any dependencies automatically; your code must register with AppDomain\u2019s AssemblyResolve event and have your event callback method explicitly load any dependent assemblies.</p><p>If you are building a tool that simply analyzes an assembly\u2019s metadata via reflection (as discussed later in this chapter), and you want to ensure that none of the code contained inside the assembly</p><p>executes, the best way for you to load an assembly is to use Assembly\u2019s ReflectionOnlyLoadFrom method, or in some rarer cases, Assembly\u2019s ReflectionOnlyLoad method. Here are the prototypes of both methods.</p><p>public class Assembly { public static Assembly ReflectionOnlyLoadFrom(String assemblyFile); public static Assembly ReflectionOnlyLoad(String assemblyString); // Less commonly used overload of ReflectionOnlyLoad is not shown</p><p>}</p><p>The ReflectionOnlyLoadFrom method will load the file specified by the path; the strongname identity of the file is not obtained, and the file is not searched for in the GAC or elsewhere. The ReflectionOnlyLoad method will search for the specified assembly looking in the GAC, application base directory, private paths, and codebases. However, unlike the Load method, the ReflectionOnlyLoad method does not apply versioning policies, so you will get the exact version that you specify. If you want to apply versioning policy yourself to an assembly identity, you can pass the string into AppDomain\u2019s ApplyPolicy method.</p><p>When an assembly is loaded with ReflectionOnlyLoadFrom or ReflectionOnlyLoad, the CLR forbids any code in the assembly from executing; any attempt to execute code in an assembly loaded with either of these methods causes the CLR to throw an InvalidOperationException. These methods allow a tool to load an assembly that was delay-signed, would normally require security permissions that prevent it from loading, or was created for a different CPU architecture.</p><p>Frequently, when using reflection to analyze an assembly loaded with one of these two methods, the code will have to register a callback method with AppDomain\u2019s ReflectionOnlyAssemblyResolve event to manually load any referenced assemblies (calling AppDomain\u2019s ApplyPolicy method, if desired); the CLR doesn\u2019t do it automatically for you. When the callback method is invoked, it must call Assembly\u2019s ReflectionOnlyLoadFrom or ReflectionOnlyLoad method to explicitly load a referenced assembly and return a reference to this assembly.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile195.png\"/><p>Note People often ask about assembly unloading. Unfortunately, the CLR doesn\u2019t support the ability to unload individual assemblies. If the CLR allowed it, your application would crash if a thread returned back from a method to code in the unloaded assembly. The CLR is all about robustness, security, and allowing an application to crash in this way would be counterproductive to its goals. If you want to unload an assembly, you must unload the entire AppDomain that contains it. This was discussed in great detail in Chapter 22.</p><p>It would seem that assemblies loaded with either the ReflectionOnlyLoadFrom or the ReflectionOnlyLoad method could be unloaded. After all, code in these assemblies is not allowed to execute. However, the CLR also doesn\u2019t allow assemblies loaded via either of these two methods to be unloaded. The reason is that after an assembly is loaded this way, you can still use reflection to create objects that refer to the metadata defined inside these assemblies. Unloading the assembly would require the objects to be invalidated somehow. Keeping track of this would be too expensive in terms of implementation and execution speed.</p><p>Many applications consist of an EXE file that depends on many DLL files. When deploying this application, all the files must be deployed. However, there is a technique that you can use to deploy just a single EXE file. First, identify all the DLL files that your EXE file depends on that do not ship as part of the Microsoft .NET Framework itself. Then add these DLLs to your Visual Studio project. For each DLL file you add, display its properties and change its Build Action to Embedded Resource. This causes the C# compiler to embed the DLL file(s) into your EXE file, and you can deploy this one EXE file.</p><p>At run time, the CLR won\u2019t be able to find the dependent DLL assemblies, which is a problem. To fix this, when your application initializes, register a callback method with the AppDomain\u2019s ResolveAssembly event. The callback method\u2019s code should look something like the following.</p><p>private static Assembly ResolveEventHandler(Object sender, ResolveEventArgs args) { String dllName = new AssemblyName(args.Name).Name + \".dll\";</p><p>var assem = Assembly.GetExecutingAssembly(); String resourceName = assem.GetManifestResourceNames().FirstOrDefault(rn =&gt;</p><p>rn.EndsWith(dllName)); if (resourceName == null) return null; // Not found, maybe another handler will find it using (var stream = assem.GetManifestResourceStream(resourceName)) {</p><p>Byte[] assemblyData = new Byte[stream.Length]; stream.Read(assemblyData, 0, assemblyData.Length); return Assembly.Load(assemblyData);</p><p>} }</p><p>Now, the first time a thread calls a method that references a type in a dependent DLL file, the AssemblyResolve event will be raised and the preceding callback code will find the embedded DLL resource desired and load it by calling an overload of Assembly\u2019s Load method that takes a Byte[] as an argument. Although I love the technique of embedding dependent DLLs inside another assembly, you should be aware that this does increase the memory used by your application at run time.</p><h6>Using Reflection to Build a Dynamically Extensible Application</h6><p>As you know, metadata is stored in a bunch of tables. When you build an assembly or a module, the compiler that you\u2019re using creates a type definition table, a field definition table, a method definition table, and so on. The System.Reflection namespace contains several types that allow you to write code that reflects over (or parses) these metadata tables. In effect, the types in this namespace offer an object model over the metadata contained in an assembly or a module.</p><p>Using these object model types, you can easily enumerate all of the types in a type definition metadata table. Then for each type, you can obtain its base type, the interfaces it implements, and the flags that are associated with the type. Additional types in the System.Reflection namespace allow you to query the type\u2019s fields, methods, properties, and events by parsing the corresponding metadata tables. You can also discover any custom attributes (covered in Chapter 18, \u201cCustom Attributes\u201d) that have been applied to any of the metadata entities. There are even classes that let you determine referenced assemblies and methods that return the IL byte stream for a method. With all of this information, you could easily build a tool very similar to Microsoft\u2019s ILDasm.exe.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile196.png\"/><p>Note You should be aware that some of the reflection types and some of the members defined by these types are designed specifically for use by developers who are producing compilers for the CLR. Application developers don\u2019t typically use these types and members. The Framework Class Library (FCL) documentation doesn\u2019t explicitly point out which of these types and members are for compiler developers rather than application developers, but if you realize that not all reflection types and their members are for everyone, the documentation can be less confusing.</p><p>In reality, very few applications will have the need to use the reflection types. Reflection is typically used by class libraries that need to understand a type\u2019s definition in order to provide some rich functionality. For example, the FCL\u2019s serialization mechanism (discussed in Chapter 24, \u201cRuntime Serialization\u201d) uses reflection to determine what fields a type defines. The serialization formatter can then obtain the values of these fields and write them into a byte stream that is used for sending across the Internet, saving to a file, or copying to the clipboard. Similarly, Visual Studio\u2019s designers use reflection to determine which properties should be shown to developers when laying out controls on their Web Forms or Windows Forms at design time.</p><p>Reflection is also used when an application needs to load a specific type from a specific assembly at run time to accomplish some task. For example, an application might ask the user to provide the name of an assembly and a type. The application could then explicitly load the assembly, construct an instance of the type, and call methods defined in the type. This usage is conceptually similar to calling Win32\u2019s LoadLibrary and GetProcAddress functions. Binding to types and calling methods in this way is frequently referred to as late binding. (Early binding is when the types and methods used by an application are determined at compile time.)</p><h6>Reflection Performance</h6><p>Reflection is an extremely powerful mechanism because it allows you to discover and use types and members at run time that you did not know about at compile time. This power does come with two main drawbacks:</p><p>\u25a0\u25a0 Reflection prevents type safety at compile time. Because reflection uses strings heavily, you lose type safety at compile time. For example, if you call Type.GetType(\"int\"); to ask reflection to find a type called \u201cint\u201d, the code compiles but returns null at run time because the CLR knows the \u201cint\u201d type as \u201cSystem.Int32\u201d.</p><p>\u25a0\u25a0 Reflection is slow. When using reflection, the names of types and their members are not known at compile time; you discover them at run time by using a string name to identify each type and member. This means that reflection is constantly performing string searches as the types in the System.Reflection namespace scan through an assembly\u2019s metadata. Often, the string searches are case-insensitive comparisons, which can slow this down even more.</p><p>Invoking a member by using reflection will also hurt performance. When using reflection to invoke a method, you must first package the arguments into an array; internally, reflection must unpack these on to the thread\u2019s stack. Also, the CLR must check that the arguments are of the correct data type before invoking a method. Finally, the CLR ensures that the caller has the proper security permission to access the member being invoked.</p><p>For all of these reasons, it\u2019s best to avoid using reflection to access a field or invoke a method/ property. If you\u2019re writing an application that will dynamically discover and construct type instances, you should take one of the following approaches:</p><p>\u25a0\u25a0 Have the types derive from a base type that is known at compile time. At run time, construct an instance of the derived type, place the reference in a variable that is of the base type (by way of a cast), and call virtual methods defined by the base type.</p><p>\u25a0\u25a0 Have the type implement an interface that is known at compile time. At run time, construct an instance of the type, place the reference in a variable that is of the interface type (by way of a cast), and call the methods defined by the interface.</p><p>I tend to prefer using the interface technique over the base type technique because the base type technique doesn\u2019t allow the developer to choose the base type that works best in a particular situation. Although the base type technique works better in versioning scenarios, because you could always add a member to the base type and the derived types just inherit it; you can\u2019t add a member to an interface without forcing all types that implement the interface to modify their code and recompile it.</p><p>When you use either of these two techniques, I strongly suggest that the interface or base type be defined in its own assembly. This will reduce versioning issues. For more information about how to do this, see the section titled \u201cDesigning an Application That Supports Add-Ins\u201d later in this chapter.</p><h6>Discovering Types Defined in an Assembly</h6><p>Reflection is frequently used to determine what types an assembly defines. The FCL offers many APIs to get this information. By far, the most commonly used API is Assembly\u2019s ExportedTypes. property. Here is an example of code that loads an assembly and shows the names of all of the publicly exported types defined in it.</p><p>using System; using System.Reflection;</p><p>public static class Program { public static void Main() { String dataAssembly = \"System.Data, version=4.0.0.0, \" +</p><p>\"culture=neutral, PublicKeyToken=b77a5c561934e089\"; LoadAssemAndShowPublicTypes(dataAssembly);</p><p>}</p><p>private static void LoadAssemAndShowPublicTypes(String assemId) { // Explicitly load an assembly in to this AppDomain Assembly a = Assembly.Load(assemId);</p><p>}</p><p>// Execute this loop once for each Type // publicly\u00adexported from the loaded assembly foreach (Type t in a.ExportedTypes) {</p><p>// Display the full name of the type Console.WriteLine(t.FullName);</p><p>} }</p><h6>What Exactly Is a Type Object?</h6><p>Notice that the previous code iterates over a sequence of System.Type objects. The System.Type type is your starting point for doing type and object manipulations. A System.Type object represents a type reference (as opposed to a type definition).</p><p>Recall that System.Object defines a public, nonvirtual instance method named GetType. When you call this method, the CLR determines the specified object\u2019s type and returns a reference to its Type object. Because there is only one Type object per type in an AppDomain, you can use equality and inequality operators to see whether two objects are of the same type.</p><p>private static Boolean AreObjectsTheSameType(Object o1, Object o2) {</p><p>return o1.GetType() == o2.GetType(); }</p><p>In addition to calling Object\u2019s GetType method, the FCL offers several more ways to obtain a Type object:</p><p>\u25a0\u25a0 The System.Type type offers several overloaded versions of the static GetType method. All versions of this method take a String. The string must specify the full name of the type (including its namespace). Note that the primitive type names supported by the compiler (such as C#\u2019s int, string, bool, and so on) aren\u2019t allowed because these names mean nothing to the CLR. If the string is simply the name of a type, the method checks the calling assembly to see whether it defines a type of the specified name. If it does, a reference to the appropriate Type object is returned.</p><p>If the calling assembly doesn\u2019t define the specified type, the types defined by MSCorLib.dll are checked. If a type with a matching name still can\u2019t be found, null is returned or a System. TypeLoadException is thrown, depending on which overload of the GetType method you called and what parameters you passed to it. The FCL documentation fully explains this method. You can pass an assembly-qualified type string, such as \u201cSystem.Int32, mscorlib, Version=4.0.0.0, Culture=neutral, PublicKeyToken=b77a5c561934e089\u201d, to GetType. In this case, GetType will look for the type in the specified assembly (loading the assembly if necessary).</p><p>\u25a0\u25a0 The System.Type type offers a static ReflectionOnlyGetType method. This method behaves similarly to the GetType method mentioned in the previous bullet, except that the type is loaded so that it can be reflected over but cannot be executed.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile197.png\"/><p>\u25a0\u25a0 The System.TypeInfo type offers the following instance members: DeclaredNestedTypes</p><p>and GetDeclaredNestedType.</p><p>\u25a0\u25a0 The System.Reflection.Assembly type offers the following instance members: GetType,</p><p>DefinedTypes, and ExportedTypes.</p><p>Note Microsoft has defined a Backus-Naur Form grammar for type names and assemblyqualified type names that is used for constructing strings that will be passed to reflection methods. Knowledge of the grammar can come in quite handy when you are using reflection, specifically if you are working with nested types, generic types, generic methods, reference parameters, or arrays. For the complete grammar, see the FCL documentation or do a web search for \u201cBackus-Naur Form Grammar for Type Names.\u201d You can also look at Type\u2019s and TypeInfo\u2019s MakeArrayType, MakeByRefType, MakeGenericType, and MakePointerType methods.</p><p>Many programming languages also offer an operator that allows you to obtain a Type object from a type name that is known at compile time. When possible, you should use this operator to obtain a reference to a Type instead of using any of the methods in the preceding list, because the operator generally produces faster code. In C#, the operator is called typeof, and you use this operator typically to compare late-bound type information with early-bound (known at compile time) type information. The following code demonstrates an example of its use.</p><p>private static void SomeMethod(Object o) { // GetType returns the type of the object at runtime (late\u00adbound) // typeof returns the type of the specified class (early\u00adbound) if (o.GetType() == typeof(FileInfo)) { ... } if (o.GetType() == typeof(DirectoryInfo)) { ... }</p><p>}</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile198.png\"/><p>Note The first if statement in the code checks whether the variable o refers to an object of the FileInfo type; it does not check whether o refers to an object that is derived from the FileInfo type. In other words, the preceding code tests for an exact match, not a compatible match, which is what you would get if you use a cast or C#\u2019s is or as operators.</p><p>As mentioned earlier, a Type object represents a type reference that is a lightweight object. If you want to learn more about the type itself, then you must acquire a TypeInfo object, which represents a type definition. You can convert a Type object to a TypeInfo object by calling System.Reflection.IntrospectionExtensions\u2019 GetTypeInfo extension method.</p><p>Type typeReference = ...; // For example: o.GetType() or typeof(Object) TypeInfo typeDefinition = typeReference.GetTypeInfo();</p><p>And, although less useful, you can convert a TypeInfo object to a Type object by calling TypeInfo\u2019s AsType method.</p><p>TypeInfo typeDefinition = ...; Type typeReference = typeDefinition.AsType();</p><p>Obtaining a TypeInfo object forces the CLR to resolve the type by ensuring that the assembly that defines the type is loaded. This can be an expensive operation that can be avoided if all you need are type references (Type objects). However, after you have a TypeInfo object, you can query many of the type\u2019s properties to learn more about it. Most of the properties, such as IsPublic, IsSealed, IsAbstract, IsClass, IsValueType, and so on, indicate flags associated with the type. Other properties, such as Assembly, AssemblyQualifiedName, FullName, Module, and so on, return the name of the type\u2019s defining assembly or module and the full name of the type. You can also query the BaseType property to obtain a reference to the type\u2019s base type, and a slew of members will give you even more information about the type. The FCL documentation describes all of the methods and properties that TypeInfo exposes.</p><h6>Building a Hierarchy of Exception-Derived Types</h6><p>The following code uses many of the concepts discussed already in this chapter to load a bunch of assemblies into the AppDomain and display all of the classes that are ultimately derived from System. Exception. By the way, this is the program I wrote to build the exception hierarchy displayed in the \u201cFCL-Defined Exception Classes\u201d section in Chapter 20, \u201cExceptions and State Management.\u201d</p><p>public static void Go() { // Explicitly load the assemblies that we want to reflect over LoadAssemblies();</p><p>// Filter &amp; sort all the types var allTypes =</p><p>(from a in AppDomain.CurrentDomain.GetAssemblies() from t in a.ExportedTypes where typeof(Exception).GetTypeInfo().IsAssignableFrom(t.GetTypeInfo()) orderby t.Name select t).ToArray();</p><p>// Build the inheritance hierarchy tree and show it Console.WriteLine(WalkInheritanceHierarchy(new StringBuilder(), 0, typeof(Exception),</p><p>allTypes)); }</p><p>private static StringBuilder WalkInheritanceHierarchy( StringBuilder sb, Int32 indent, Type baseType, IEnumerable&lt;Type&gt; allTypes) { String spaces = new String(' ', indent * 3); sb.AppendLine(spaces + baseType.FullName); foreach (var t in allTypes) {</p><p>if (t.GetTypeInfo().BaseType != baseType) continue; WalkInheritanceHierarchy(sb, indent + 1, t, allTypes);</p><p>}</p><p>} return sb;</p><p>private static void LoadAssemblies() {</p><p>String[] assemblies = { \"System, PublicKeyToken={0}\", \"System.Core, PublicKeyToken={0}\", \"System.Data, PublicKeyToken={0}\", \"System.Design, PublicKeyToken={1}\", \"System.DirectoryServices, PublicKeyToken={1}\", \"System.Drawing, PublicKeyToken={1}\", \"System.Drawing.Design, PublicKeyToken={1}\", \"System.Management, PublicKeyToken={1}\", \"System.Messaging, PublicKeyToken={1}\", \"System.Runtime.Remoting, PublicKeyToken={0}\", \"System.Security, PublicKeyToken={1}\", \"System.ServiceProcess, PublicKeyToken={1}\", \"System.Web, PublicKeyToken={1}\", \"System.Web.RegularExpressions, PublicKeyToken={1}\", \"System.Web.Services, PublicKeyToken={1}\", \"System.Xml, PublicKeyToken={0}\",</p><p>};</p><p>String EcmaPublicKeyToken = \"b77a5c561934e089\"; String MSPublicKeyToken = \"b03f5f7f11d50a3a\";</p><p>// Get the version of the assembly containing System.Object // We'll assume the same version for all the other assemblies Version version = typeof(System.Object).Assembly.GetName().Version;</p><p>// Explicitly load the assemblies that we want to reflect over foreach (String a in assemblies) {</p><p>String AssemblyIdentity = String.Format(a, EcmaPublicKeyToken, MSPublicKeyToken) +</p><p>\", Culture=neutral, Version=\" + version; Assembly.Load(AssemblyIdentity);</p><p>} }</p><h6>Constructing an Instance of a Type</h6><p>After you have a reference to a Type-derived object, you might want to construct an instance of this type. The FCL offers several mechanisms to accomplish this:</p><p>\u25a0\u25a0 System.Activator\u2019s CreateInstance methods The Activator class offers several overloads of its static CreateInstance method. When you call this method, you can pass either a reference to a Type object or a String that identifies the type of object you want to create. The versions that take a type are simpler. You get to pass a set of arguments for the type\u2019s constructor, and the method returns a reference to the new object.</p><p>The versions of this method in which you specify the desired type by using a string are a bit more complex. First, you must also specify a string identifying the assembly that defines the</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile199.png\"/><p>type. Second, these methods allow you to construct a remote object if you have remoting options configured properly. Third, these versions don\u2019t return a reference to the new object. Instead, they return a System.Runtime.Remoting.ObjectHandle (which is derived from System.MarshalByRefObject).</p><p>An ObjectHandle is a type that allows an object created in one AppDomain to be passed around to other AppDomains without forcing the object to materialize. When you\u2019re ready to materialize the object, you call ObjectHandle\u2019s Unwrap method. This method loads the assembly that defines the type being materialized in the AppDomain where Unwrap is called. If the object is being marshaled by reference, the proxy type and object are created. If the object is being marshaled by value, the copy is deserialized.</p><p>\u25a0\u25a0 System.Activator\u2019s CreateInstanceFrom methods The Activator class also offers a set of static CreateInstanceFrom methods. These methods behave just as the CreateInstance method, except that you must always specify the type and its assembly via string</p><p>parameters. The assembly is loaded into the calling AppDomain by using Assembly\u2019s LoadFrom method (instead of Load). Because none of these methods takes a Type parameter, all of the CreateInstanceFrom methods return a reference to an ObjectHandle, which must be unwrapped.</p><p>\u25a0\u25a0 System.AppDomain\u2019s methods The AppDomain type offers four instance methods (each with several overloads) that construct an instance of a type: CreateInstance, CreateInstanceAndUnwrap, CreateInstanceFrom, and CreateInstanceFromAndUnwrap. These methods work just as Activator\u2019s methods except that these methods are instance methods, allowing you to specify which AppDomain the object should be constructed in. The methods that end with Unwrap exist for convenience so that you don\u2019t have to make an additional method call.</p><p>\u25a0\u25a0 System.Reflection.ConstructorInfo\u2019s Invoke instance method Using a reference to a TypeInfo object, you can bind to a particular constructor and obtain a reference to the constructor\u2019s ConstructorInfo object. Then you can use the reference to the ConstructorInfo object to call its Invoke method. The type is always created in the calling AppDomain, and a reference to the new object is returned. I\u2019ll also discuss this method in more detail later in this chapter.</p><p>Note The CLR doesn\u2019t require that value types define any constructors. However, this is a problem because all of the mechanisms in the preceding list construct an object by calling its constructor. However, Activator\u2019s CreateInstance methods will allow you to create an instance of a value type without calling a constructor. If you want to create an instance of a value type without calling a constructor, you must call the version of the CreateInstance method that takes a single Type parameter or the version that takes Type and Boolean parameters.</p><p>The mechanisms just listed allow you to create an object for all types except for arrays (System. Array-derived types) and delegates (System.MulticastDelegate-derived types). To create an array, you should call Array\u2019s static CreateInstance method (several overloaded versions exist). The first parameter to all versions of CreateInstance is a reference to the Type of elements you want in the array. CreateInstance\u2019s other parameters allow you to specify various combinations of dimensions and bounds. To create a delegate, you should call MethodInfo\u2019s CreateDelegate method. The first parameter to all versions of CreateDelegate is a reference to the Type of delegate you want to create. CreateDelegate\u2019s other parameter allows you to specify which object should be passed as the this parameter when calling an instance method.</p><p>To construct an instance of a generic type, first get a reference to the open type, and then call Type\u2019s MakeGenericType method, passing in an array of types that you want to use as the type arguments. Then, take the returned Type object and pass it into one of the various methods previously listed. Here is an example.</p><p>using System; using System.Reflection;</p><p>internal sealed class Dictionary&lt;TKey, TValue&gt; { }</p><p>public static class Program {</p><p>public static void Main() { // Get a reference to the generic type's type object Type openType = typeof(Dictionary&lt;,&gt;);</p><p>// Close the generic type by using TKey=String, TValue=Int32 Type closedType = openType.MakeGenericType(typeof(String), typeof(Int32));</p><p>// Construct an instance of the closed type Object o = Activator.CreateInstance(closedType);</p><p>} }</p><p>// Prove it worked Console.WriteLine(o.GetType());</p><p>If you compile the preceding code and run it, you get the following output.</p><p>Dictionary`2[System.String,System.Int32]</p><h6>Designing an Application That Supports Add-Ins</h6><p>When you\u2019re building extensible applications, interfaces should be the centerpiece. You could use a base class instead of an interface, but in general, an interface is preferred because it allows add-in developers to choose their own base class. Suppose, for example, that you\u2019re writing an application and you want others to be able to create types that your application can load and use seamlessly.</p><p>Here\u2019s the way to design this application:</p><p>\u25a0\u25a0 Create a Host SDK assembly that defines an interface whose methods are used as the communication mechanism between the host application and the add-in components. When defining the parameters and return types for the interface methods, try to use other interfaces or types defined in MSCorLib.dll. If you want to pass and return your own data types, define them in this Host SDK assembly, too. After you settle on your interface definitions, give this assembly a strong name (discussed in Chapter 3), and then package and deploy it to your partners and users. Once published, you should really avoid making any kind of breaking changes to the types in this assembly. For example, do not change the interface in any way. However, if you define any data types, it is OK to add new members. If you make any modifications to the assembly, you\u2019ll probably want to deploy it with a publisher policy file (also discussed in Chapter 3).</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile200.png\"/><p>Note You can use types defined in MSCorLib.dll because the CLR always loads the version of MSCorLib.dll that matches the version of the CLR itself. Also, only a single version of MSCorLib.dll is ever loaded into a CLR instance. In other words, different versions of MSCorLib.dll never load side by side (as described in Chapter 3). As a result, you won\u2019t have any type version mismatches, and your application will require less memory.</p><p>\u25a0\u25a0 The add-in developers will, of course, define their own types in their own Add-In assembly.</p><p>Their Add-In assembly will reference the types in your Host SDK assembly. The add-in developers are able to put out a new version of their assembly as often as they\u2019d like, and the host application will be able to consume the add-in types without any problem whatsoever.</p><p>\u25a0\u25a0 Create a separate Host Application assembly containing your application\u2019s types. This assembly will obviously reference the Host SDK assembly and use the types defined in it. Feel free to modify the code in the Host Application assembly to your heart\u2019s desire. Because the add-in developers don\u2019t reference the Host Application assembly, you can put out a new version of it every hour if you want to and not affect any of the add-in developers.</p><p>This section contains some very important information. When using types across assemblies, you need to be concerned with assembly-versioning issues. Take your time to architect this cleanly by isolating the types that you use for communication across assembly boundaries into their own assembly. Avoid mutating or changing these type definitions. However, if you really need to modify the type definitions, make sure that you change the assembly\u2019s version number and create a publisher policy file for the new version.</p><p>I\u2019ll now walk through a very simple scenario that puts all of this together. First, here is the code for the HostSDK.dll assembly.</p><p>using System;</p><p>namespace Wintellect.HostSDK { public interface IAddIn { String DoSomething(Int32 x);</p><p>} }</p><p>Second, here is the code for an AddInTypes.dll assembly defining two public types that implement the HostSDK\u2019s interface. To build this assembly, the HostSDK.dll assembly must be referenced.</p><p>using System; using Wintellect.HostSDK;</p><ul style=\"list-style-type: none;\">\n<li><p>public sealed class AddIn_A : IAddIn { public AddIn_A() { } public String DoSomething(Int32 x) {</p><p>return \"AddIn_A: \" + x.ToString(); }</p>\n<p>}</p>\n</li>\n<li><p>public sealed class AddIn_B : IAddIn { public AddIn_B() { } public String DoSomething(Int32 x) {</p></li>\n</ul><p>return \"AddIn_B: \" + (x * 2).ToString(); }</p><p>}</p><p>Third, here is the code for a simple Host.exe assembly (a console application). To build this assembly, the HostSDK.dll assembly must be referenced. To discover usable add-in types, this host code assumes that the types are defined in assemblies ending with a .dll file extension and that these assemblies are deployed into the same directory as the host\u2019s EXE file. Microsoft\u2019s Managed Extensibility Framework (MEF) is built on top of the various mechanisms that I show here, and it also offers add-in registration and discovery mechanisms. I urge you to check MEF out if you are building a dynamically extensible application, because it can simplify some of the material in this chapter.</p><p>using System; using System.IO; using System.Reflection; using System.Collections.Generic; using Wintellect.HostSDK;</p><p>public static class Program {</p><p>public static void Main() { // Find the directory that contains the Host exe String AddInDir = Path.GetDirectoryName(Assembly.GetEntryAssembly().Location);</p><p>// Assume AddIn assemblies are in same directory as host's EXE file var AddInAssemblies = Directory.EnumerateFiles(AddInDir, \"*.dll\");</p><p>// Create a collection of Add\u00adIn Types usable by the host var AddInTypes =</p><p>from file in AddInAssemblies let assembly = Assembly.Load(file) from t in assembly.ExportedTypes // Publicly\u00adexported types // Type is usable if it is a class that implements IAddIn where t.IsClass &amp;&amp; typeof(IAddIn).GetTypeInfo().IsAssignableFrom(t.GetTypeInfo())</p><p>select t; // Initialization complete: the host has discovered the usable Add\u00adIns</p><p>}</p><p>// Here's how the host can construct Add\u00adIn objects and use them foreach (Type t in AddInTypes) {</p><p>IAddIn ai = (IAddIn) Activator.CreateInstance(t); Console.WriteLine(ai.DoSomething(5));</p><p>} }</p><p>The simple host/add-in scenario just shown doesn\u2019t use AppDomains. However, in a real-life scenario, you will likely create each add-in in its own AppDomain with its own security and configuration settings. And of course, each AppDomain could be unloaded if you wanted to remove an add-in from memory. To communicate across the AppDomain boundary, you\u2019d either tell the add-in developers to derive their add-in types from MarshalByRefObject or, more likely, have the host application define its own internal type that is derived from MarshalByRefObject. As each AppDomain is created, the host would create an instance of its own MarshalByRefObject-derived type in the new AppDomain. The host\u2019s code (in the default AppDomain) would communicate with its own type (in the other AppDomains) to have it load add-in assemblies and create and use instances of the add-in types.</p><h6>Using Reflection to Discover a Type\u2019s Members</h6><p>So far, this chapter has focused on the parts of reflection\u2014assembly loading, type discovery, and object construction\u2014necessary to build a dynamically extensible application. In order to have good performance and compile-time type safety, you want to avoid using reflection as much as possible. In the dynamically extensible application scenario, after an object is constructed, the host code typically casts the object to an interface type or a base class that is known at compile time; this allows the object\u2019s members to be accessed in a high-performance and compile-time type-safe way.</p><p>In the remainder of this chapter, I\u2019m going to focus on some other aspects of reflection that you can use to discover and then invoke a type\u2019s members. The ability to discover and invoke a type\u2019s members is typically used to create developer tools and utilities that analyze an assembly by looking for certain programming patterns or uses of certain members. Examples of tools/utilities that do this are ILDasm.exe, FxCopCmd.exe, and Visual Studio\u2019s Windows Forms, Windows Presentation Foundation, and Web Forms designers. In addition, some class libraries use the ability to discover and invoke a type\u2019s members in order to offer rich functionality as a convenience to developers. Examples of class libraries that do so are serialization/deserialization and simple data binding.</p><h6>Discovering a Type\u2019s Members</h6><p>Fields, constructors, methods, properties, events, and nested types can all be defined as members within a type. The FCL contains a type called System.Reflection.MemberInfo. This class is an abstract base class that encapsulates a bunch of properties common to all type members. Derived from MemberInfo are a bunch of classes; each class encapsulates some more properties related to a specific type member. Figure 23-1 shows the hierarchy of these types.</p><table border=\"1\">\n<tr>\n<td colspan=\"2\"><p>System.Object</p>\n</td>\n</tr>\n<tr>\n<td></td>\n<td></td>\n</tr>\n</table><p>System.Reflection.MemberInfo</p><p>A nested type is a member</p><p>System.TypeInfo</p><p>System.Reflection.FieldInfo</p><p>System.Reflection.MethodBase</p><p>System.Reflection.ContructorInfo</p><p>System.Reflection.MethodInfo</p><p>System.Reflection.PropertyInfo</p><p>System.Reflection.EventInfo</p><p>FIGURE 23-1 Hierarchy of the reflection types that encapsulate information about a type\u2019s member.</p><p>The following program demonstrates how to query a type\u2019s members and display some information about them. This code processes all of the public types defined in all assemblies loaded in the calling AppDomain. For each type, the DeclaredMembers property is called and returns a collection of MemberInfo-derived objects; each object refers to a single member defined within the type. Then, for each member, its kind (field, constructor, method, property, etc.) and its string value (obtained by calling ToString) is shown.</p><p>using System; using System.Reflection;</p><p>public static class Program {</p><p>public static void Main() { // Loop through all assemblies loaded in this AppDomain Assembly[] assemblies = AppDomain.CurrentDomain.GetAssemblies(); foreach (Assembly a in assemblies) {</p><p>Show(0, \"Assembly: {0}\", a);</p><p>// Find Types in the assembly foreach (Type t in a.ExportedTypes) {</p><p>Show(1, \"Type: {0}\", t);</p><p>// Discover the type's members foreach (MemberInfo mi in t.GetTypeInfo().DeclaredMembers) {</p><p>String typeName = String.Empty; if (mi is Type) typeName = \"(Nested) Type\"; if (mi is FieldInfo) typeName = \"FieldInfo\"; if (mi is MethodInfo) typeName = \"MethodInfo\"; if (mi is ConstructorInfo) typeName = \"ConstructoInfo\";</p><p>}</p><p>if (mi is PropertyInfo) typeName = \"PropertyInfo\"; if (mi is EventInfo) typeName = \"EventInfo\"; Show(2, \"{0}: {1}\", typeName, mi);</p><p>} } }</p><p>} private static void Show(Int32 indent, String format, params Object[] args) {</p><p>Console.WriteLine(new String(' ', 3 * indent) + format, args); }</p><p>When you compile and run this code, a ton of output is produced. Here is a small sampling of what it looks like.</p><p>Assembly: mscorlib, Version=4.0.0.0, Culture=neutral, PublicKeyToken=b77a5c561934e089</p><p>Type: System.Object MethodInfo: System.String ToString() MethodInfo: Boolean Equals(System.Object) MethodInfo: Boolean Equals(System.Object, System.Object) MethodInfo: Boolean ReferenceEquals(System.Object, System.Object) MethodInfo: Int32 GetHashCode() MethodInfo: System.Type GetType() MethodInfo: Void Finalize() MethodInfo: System.Object MemberwiseClone() MethodInfo: Void FieldSetter(System.String, System.String, System.Object) MethodInfo: Void FieldGetter(System.String, System.String, System.Object ByRef) MethodInfo: System.Reflection.FieldInfo GetFieldInfo(System.String, System.String) ConstructoInfo: Void .ctor()</p><p>Type: System.Collections.Generic.IComparer`1[T] MethodInfo: Int32 Compare(T, T)</p><p>Type: System.Collections.IEnumerator MethodInfo: Boolean MoveNext() MethodInfo: System.Object get_Current() MethodInfo: Void Reset() PropertyInfo: System.Object Current</p><p>Type: System.IDisposable MethodInfo: Void Dispose()</p><p>Type: System.Collections.Generic.IEnumerator`1[T] MethodInfo: T get_Current() PropertyInfo: T Current</p><p>Type: System.ArraySegment`1[T] MethodInfo: T[] get_Array() MethodInfo: Int32 get_Offset() MethodInfo: Int32 get_Count() MethodInfo: Int32 GetHashCode() MethodInfo: Boolean Equals(System.Object) MethodInfo: Boolean Equals(System.ArraySegment`1[T]) MethodInfo: Boolean op_Equality(System.ArraySegment`1[T], System.ArraySegment`1[T]) MethodInfo: Boolean op_Inequality(System.ArraySegment`1[T], System.ArraySegment`1[T]) ConstructoInfo: Void .ctor(T[]) ConstructoInfo: Void .ctor(T[], Int32, Int32) PropertyInfo: T[] Array PropertyInfo: Int32 Offset PropertyInfo: Int32 Count FieldInfo: T[] _array FieldInfo: Int32 _offset</p><p>Because MemberInfo is the root of the member hierarchy, it makes sense for us to discuss it a bit more. Table 23-1 shows several read-only properties and methods offered by the MemberInfo class. These properties and methods are common to all members of a type. Don\u2019t forget that System. TypeInfo is derived from MemberInfo, and therefore, TypeInfo also offers all of the properties shown in Table 23-1.</p><figcaption>TABLE 23-1 Properties and Methods Common to All MemberInfo-Derived Types</figcaption><table border=\"1\">\n<tr>\n<td><p>Member Name</p>\n<p>Name</p>\n</td>\n<td><p>Member Type String property</p>\n</td>\n<td><p>Description Returns the name of the member.</p>\n</td>\n</tr>\n<tr>\n<td><p>DeclaringType</p>\n</td>\n<td><p>Type property</p>\n</td>\n<td><p>Returns the Type that declares the member.</p>\n</td>\n</tr>\n<tr>\n<td><p>Module</p>\n</td>\n<td><p>Module property</p>\n</td>\n<td><p>Returns the Module that declares the member.</p>\n</td>\n</tr>\n<tr>\n<td><p>CustomAttributes</p>\n</td>\n<td><p>Property returning</p>\n<p>IEnumerable&lt;CustomAttributeData&gt;</p>\n</td>\n<td><p>Returns a collection in which each element identifies an instance of a custom attribute applied to this member. Custom attributes can be applied to any member. Even though Assembly does not derive from MemberInfo, it provides the same property that can be used with assemblies.</p>\n</td>\n</tr>\n</table><p>Each element of the collection returned by querying DeclaredMembers is a reference to one of the concrete types in the hierarchy. Although TypeInfo\u2019s DeclaredMembers property returns all of the type\u2019s members, TypeInfo also offers methods that return specific member types for a specified string name. For example, TypeInfo offers GetDeclaredNestedType, GetDeclaredField, GetDeclaredMethod, GetDeclaredProperty, and GetDeclaredEvent. These methods all return a reference to a TypeInfo object, FieldInfo object, MethodInfo object, PropertyInfo object, or EventInfo object, respectively. There is also a GetDeclaredMethods method that returns a collection of MethodInfo objects describing the methods matching the specified string name.</p><p>Figure 23-2 summarizes the types used by an application to walk reflection\u2019s object model. From an AppDomain, you can discover the assemblies loaded into it. From an assembly, you can discover the modules that make it up. From an assembly or a module, you can discover the types that it defines. From a type, you can discover its nested types, fields, constructors, methods, properties, and events. Namespaces are not part of this hierarchy because they are simply syntactical gatherings of types. If you want to list all of the namespaces defined in an assembly, you need to enumerate all of the types in this assembly and take a look at their Namespace property.</p><p>From a type, it is also possible to discover the interfaces it implements. And from a constructor, method, property accessor method, or event add/remove method, you can call the GetParameters method to obtain an array of ParameterInfo objects, which tells you the types of the member\u2019s parameters. You can also query the read-only ReturnParameter property to get a ParameterInfo object for detailed information about a member\u2019s return type. For a generic type or method, you can call the GetGenericArguments method to get the set of type parameters. Finally, for any of these items, you can query the CustomAttributes property to obtain the set of custom attributes applied to them.</p><table border=\"1\">\n<tr>\n<td><p>FieldInfo #1</p>\n</td>\n</tr>\n</table><table border=\"1\">\n<tr>\n<td colspan=\"2\"><p>AppDomain</p>\n</td>\n</tr>\n<tr>\n<td></td>\n<td></td>\n</tr>\n</table><p>FieldInfo #2</p><p>Assembly #1</p><p>Module #1</p><p>TypeInfo #1</p><p>ConstructorInfo #1</p><table border=\"1\">\n<tr>\n<td><p>ConstructorInfo #2</p>\n</td>\n</tr>\n</table><p>Assembly #2</p><p>Module #2</p><p>TypeInfo #2</p><p>MethodInfo #1</p><p>MethodInfo #2</p><p>PropertyInfo #1</p><p>PropertyInfo #2</p><p>EventInfo #1</p><table border=\"1\">\n<tr>\n<td><p>EventInfo #2</p>\n</td>\n</tr>\n</table><p>FIGURE 23-2 Types an application uses to walk reflection\u2019s object model.</p><h6>Invoking a Type\u2019s Members</h6><p>Now that you know how to discover the members defined by a type, you may want to invoke one of these members. What invoke means depends on the kind of member being invoked. Table 23-2 shows which method to call for each kind of member to invoke that member.</p><p>TABLE 23-2 How to Invoke a Member</p><table border=\"1\">\n<tr>\n<td><p>Type of Member</p>\n<p>FieldInfo</p>\n</td>\n<td><h6>Method to Invoke Member</h6>\n<p>Call GetValue to get a field\u2019s value. Call SetValue to set a field\u2019s value.</p>\n</td>\n</tr>\n<tr>\n<td><p>ConstructorInfo</p>\n</td>\n<td><p>Call Invoke to construct an instance of the type and call a constructor.</p>\n</td>\n</tr>\n<tr>\n<td><p>MethodInfo</p>\n</td>\n<td><p>Call Invoke to call a method of the type.</p>\n</td>\n</tr>\n<tr>\n<td><p>PropertyInfo</p>\n</td>\n<td><p>Call GetValue to call a property\u2019s get accessor method. Call SetValue to call a property\u2019s set accessor method.</p>\n</td>\n</tr>\n<tr>\n<td><p>EventInfo</p>\n</td>\n<td><p>Call AddEventHandler to call an event\u2019s add accessor method. Call RemoveEventHandler to call an event\u2019s remove accessor method.</p>\n</td>\n</tr>\n</table><figcaption>The PropertyInfo type represents metadata information about a property (as discussed in Chapter 10, \u201cProperties\u201d); that is, PropertyInfo offers CanRead, CanWrite, and PropertyType read-only properties. These properties indicate whether a property is readable or writeable and what data type the property is. PropertyInfo also has read-only GetMethod and SetMethod properties, which return MethodInfo objects representing the methods that get and set a property\u2019s value.</figcaption><p>PropertyInfo\u2019s GetValue and SetValue methods exist for convenience; internally, they invoke the appropriate MethodInfo object. To support parameterful properties (C# indexers), the GetValue and SetValue methods offer an index parameter of Object[] type.</p><p>The EventInfo type represents metadata information about an event (as discussed in Chapter 11, \u201cEvents\u201d). The EventInfo type offers a read-only EventHandlerType property that returns the Type of the event\u2019s underlying delegate. The EventInfo type also has read-only AddMethod and RemoveMethod properties, which return the MethodInfo objects corresponding to the methods that add or remove a delegate to/from the event. To add or remove a delegate, you can invoke these MethodInfo objects, or you can call EventInfo\u2019s more convenient AddEventHandler and RemoveEventHandler methods.</p><p>The following sample application demonstrates the various ways to use reflection to access a type\u2019s members. The SomeType class represents a type that has various members: a private field (m_someField), a public constructor (SomeType) that takes an Int32 argument passed by reference, a public method (ToString), a public property (SomeProp), and a public event (SomeEvent). Having defined the SomeType type, I offer three different methods that use reflection to access SomeType\u2019s members. Each method uses reflection in a different way to accomplish the same thing.</p><p>\u25a0\u25a0 The BindToMemberThenInvokeTheMember method demonstrates how to bind to a member</p><p>and invoke it later.</p><p>\u25a0\u25a0 The BindToMemberCreateDelegateToMemberThenInvokeTheMember method demonstrates how to bind to an object or member, and then it creates a delegate that refers to that object or member. Calling through the delegate is very fast, and this technique yields faster performance if you intend to invoke the same member on the same object multiple times.</p><p>\u25a0\u25a0 The UseDynamicToBindAndInvokeTheMember method demonstrates how to use C# dynamic primitive type (discussed at the end of Chapter 5, \u201cPrimitive, Reference, and Value Types\u201d) to simplify the syntax for accessing members. In addition, this technique can give reasonably good performance if you intend to invoke the same member on different objects that are all of the same type because the binding will happen once per type and be cached so that it can be invoked multiple times quickly. You can also use this technique to invoke a member on objects of different types.</p><p>using System; using System.Reflection; using Microsoft.CSharp.RuntimeBinder; using System.Linq;</p><p>// This class is used to demonstrate reflection // It has a field, constructor, method, property, and an event internal sealed class SomeType { private Int32 m_someField; public SomeType(ref Int32 x) { x *= 2; } public override String ToString() { return m_someField.ToString(); } public Int32 SomeProp {</p><p>}</p><p>get { return m_someField; } set {</p><p>if (value &lt; 1)</p><p>throw new ArgumentOutOfRangeException(\"value\"); m_someField = value;</p><p>} public event EventHandler SomeEvent; private void NoCompilerWarnings() { SomeEvent.ToString();}</p><p>public static class Program {</p><p>public static void Main() { Type t = typeof(SomeType); BindToMemberThenInvokeTheMember(t); Console.WriteLine();</p><p>BindToMemberCreateDelegateToMemberThenInvokeTheMember(t); Console.WriteLine();</p><p>}</p><p>UseDynamicToBindAndInvokeTheMember(t); Console.WriteLine();</p><p>private static void BindToMemberThenInvokeTheMember(Type t) { Console.WriteLine(\"BindToMemberThenInvokeTheMember\");</p><p>// Construct an instance Type ctorArgument = Type.GetType(\"System.Int32&amp;\"); // or typeof(Int32).MakeByRefType(); ConstructorInfo ctor = t.GetTypeInfo().DeclaredConstructors.First(</p><p>c =&gt; c.GetParameters()[0].ParameterType == ctorArgument); Object[] args = new Object[] { 12 }; // Constructor arguments Console.WriteLine(\"x before constructor called: \" + args[0]); Object obj = ctor.Invoke(args); Console.WriteLine(\"Type: \" + obj.GetType()); Console.WriteLine(\"x after constructor returns: \" + args[0]);</p><p>// Read and write to a field FieldInfo fi = obj.GetType().GetTypeInfo().GetDeclaredField(\"m_someField\"); fi.SetValue(obj, 33); Console.WriteLine(\"someField: \" + fi.GetValue(obj));</p><p>// Call a method MethodInfo mi = obj.GetType().GetTypeInfo().GetDeclaredMethod(\"ToString\"); String s = (String)mi.Invoke(obj, null); Console.WriteLine(\"ToString: \" + s);</p><p>// Read and write a property PropertyInfo pi = obj.GetType().GetTypeInfo().GetDeclaredProperty(\"SomeProp\"); try {</p><p>pi.SetValue(obj, 0, null);</p><p>} catch (TargetInvocationException e) {</p><p>if (e.InnerException.GetType() != typeof(ArgumentOutOfRangeException)) throw;</p><p>Console.WriteLine(\"Property set catch.\");</p><p>} pi.SetValue(obj, 2, null); Console.WriteLine(\"SomeProp: \" + pi.GetValue(obj, null));</p><p>}</p><p>// Add and remove a delegate from the event EventInfo ei = obj.GetType().GetTypeInfo().GetDeclaredEvent(\"SomeEvent\"); EventHandler eh = new EventHandler(EventCallback); // See ei.EventHandlerType ei.AddEventHandler(obj, eh); ei.RemoveEventHandler(obj, eh);</p><p>// Callback method added to the event private static void EventCallback(Object sender, EventArgs e) { }</p><p>private static void BindToMemberCreateDelegateToMemberThenInvokeTheMember(Type t) { Console.WriteLine(\"BindToMemberCreateDelegateToMemberThenInvokeTheMember\");</p><p>// Construct an instance (You can't create a delegate to a constructor) Object[] args = new Object[] { 12 }; // Constructor arguments Console.WriteLine(\"x before constructor called: \" + args[0]); Object obj = Activator.CreateInstance(t, args); Console.WriteLine(\"Type: \" + obj.GetType().ToString()); Console.WriteLine(\"x after constructor returns: \" + args[0]);</p><p>// NOTE: You can't create a delegate to a field</p><p>// Call a method MethodInfo mi = obj.GetType().GetTypeInfo().GetDeclaredMethod(\"ToString\"); var toString = mi.CreateDelegate&lt;Func&lt;String&gt;&gt;(obj); String s = toString(); Console.WriteLine(\"ToString: \" + s);</p><p>// Read and write a property PropertyInfo pi = obj.GetType().GetTypeInfo().GetDeclaredProperty(\"SomeProp\"); var setSomeProp = pi.SetMethod.CreateDelegate&lt;Action&lt;Int32&gt;&gt;(obj); try {</p><p>setSomeProp(0);</p><p>} catch (ArgumentOutOfRangeException) {</p><p>Console.WriteLine(\"Property set catch.\");</p><p>} setSomeProp(2); var getSomeProp = pi.GetMethod.CreateDelegate&lt;Func&lt;Int32&gt;&gt;(obj); Console.WriteLine(\"SomeProp: \" + getSomeProp());</p><p>}</p><p>// Add and remove a delegate from the event EventInfo ei = obj.GetType().GetTypeInfo().GetDeclaredEvent(\"SomeEvent\"); var addSomeEvent = ei.AddMethod.CreateDelegate&lt;Action&lt;EventHandler&gt;&gt;(obj); addSomeEvent(EventCallback); var removeSomeEvent = ei.RemoveMethod.CreateDelegate&lt;Action&lt;EventHandler&gt;&gt;(obj); removeSomeEvent(EventCallback);</p><p>private static void UseDynamicToBindAndInvokeTheMember(Type t) { Console.WriteLine(\"UseDynamicToBindAndInvokeTheMember\");</p><p>// Construct an instance (You can't use dynamic to call a constructor) Object[] args = new Object[] { 12 }; // Constructor arguments Console.WriteLine(\"x before constructor called: \" + args[0]); dynamic obj = Activator.CreateInstance(t, args); Console.WriteLine(\"Type: \" + obj.GetType().ToString()); Console.WriteLine(\"x after constructor returns: \" + args[0]);</p><p>// Read and write to a field try {</p><p>obj.m_someField = 5; Int32 v = (Int32)obj.m_someField; Console.WriteLine(\"someField: \" + v);</p><p>} catch (RuntimeBinderException e) {</p><p>// We get here because the field is private Console.WriteLine(\"Failed to access field: \" + e.Message);</p><p>}</p><p>// Call a method String s = (String)obj.ToString(); Console.WriteLine(\"ToString: \" + s);</p><p>// Read and write a property try {</p><p>obj.SomeProp = 0;</p><p>} catch (ArgumentOutOfRangeException) {</p><p>Console.WriteLine(\"Property set catch.\");</p><p>} obj.SomeProp = 2; Int32 val = (Int32)obj.SomeProp; Console.WriteLine(\"SomeProp: \" + val);</p><p>} }</p><p>// Add and remove a delegate from the event obj.SomeEvent += new EventHandler(EventCallback); obj.SomeEvent \u00ad= new EventHandler(EventCallback);</p><p>internal static class ReflectionExtensions { // Helper extension method to simplify syntax to create a delegate public static TDelegate CreateDelegate&lt;TDelegate&gt;(this MethodInfo mi, Object target = null) {</p><p>return (TDelegate)(Object)mi.CreateDelegate(typeof(TDelegate), target); }</p><p>}</p><p>If you build and run this code, you\u2019ll see the following output.</p><p>BindToMemberThenInvokeTheMember x before constructor called: 12 Type: SomeType x after constructor returns: 24</p><p>someField: 33 ToString: 33 Property set catch. SomeProp: 2</p><p>BindToMemberCreateDelegateToMemberThenInvokeTheMember x before constructor called: 12 Type: SomeType x after constructor returns: 24 ToString: 0 Property set catch. SomeProp: 2</p><p>UseDynamicToBindAndInvokeTheMember x before constructor called: 12 Type: SomeType x after constructor returns: 24 Failed to access field: 'SomeType.m_someField' is inaccessible due to its protection level ToString: 0 Property set catch. SomeProp: 2</p><p>Notice that SomeType\u2019s constructor takes an Int32 by reference as its only parameter. The previous code shows how to call this constructor and how to examine the modified Int32 value after the constructor returns. Near the top of the BindToMemberThenInvokeTheMember method, I show how to accomplish this by calling Type\u2019s GetType method passing in a string of \"System.Int32&amp;\". The ampersand (&amp;) in the string allows me to identify a parameter passed by reference. This ampersand is part of the Backus-Naur Form grammar for type names, which you can look up in the FCL documentation. The code also shows how to accomplish the same thing using Type\u2019s MakeByRefType method.</p><h6>Using Binding Handles to Reduce Your Process\u2019s Memory Consumption</h6><p>Many applications bind to a bunch of types (Type objects) or type members (MemberInfo-derived objects) and save these objects in a collection of some sort. Then later, the application searches the collection for a particular object and then invokes this object. This is a fine way of doing things except for one small issue: Type and MemberInfo-derived objects require a lot of memory. So if an application holds on to too many of these objects and invokes them occasionally, the application\u2019s memory consumption increases dramatically, having an adverse effect on the application\u2019s performance.</p><p>Internally, the CLR has a more compact way of representing this information. The CLR creates these objects for our applications only to make things easier for developers. The CLR doesn\u2019t need these big objects itself in order to run. Developers who are saving/caching a lot of Type and MemberInfoderived objects can reduce their working set by using run-time handles instead of objects. The FCL defines three runtime handle types (all defined in the System namespace): RuntimeTypeHandle, RuntimeFieldHandle, and RuntimeMethodHandle. All of these types are value types that contain just one field, an IntPtr; this makes instances of these types cheap (memory-wise). The IntPtr field is a handle that refers to a type, field, or method in an AppDomain\u2019s loader heap. So what you need now is an easy and efficient way to convert a heavyweight Type/MemberInfo object to a lightweight</p><p>run-time handle instance and vice versa. Fortunately, this is easy using the following conversion methods and properties:</p><p>\u25a0\u25a0 To convert a Type object to a RuntimeTypeHandle, call Type\u2019s static GetTypeHandle</p><p>method passing in the reference to the Type object.</p><p>\u25a0\u25a0 To convert a RuntimeTypeHandle to a Type object, call Type\u2019s static GetTypeFromHandle</p><p>method passing in the RuntimeTypeHandle.</p><p>\u25a0\u25a0 To convert a FieldInfo object to a RuntimeFieldHandle, query FieldInfo\u2019s instance</p><p>read-only FieldHandle property.</p><p>\u25a0\u25a0 To convert a RuntimeFieldHandle to a FieldInfo object, call FieldInfo\u2019s static Get\u00ad</p><p>FieldFromHandle method.</p><p>\u25a0\u25a0 To convert a MethodInfo object to a RuntimeMethodHandle, query MethodInfo\u2019s instance</p><p>read-only MethodHandle property.</p><p>\u25a0\u25a0 To convert a RuntimeMethodHandle to a MethodInfo object, call MethodInfo\u2019s static Get\u00ad</p><p>MethodFromHandle method.</p><p>The following program sample acquires a lot of MethodInfo objects, converts them to RuntimeMethodHandle instances, and shows the working set difference.</p><p>using System; using System.Reflection; using System.Collections.Generic;</p><p>public sealed class Program { private const BindingFlags c_bf = BindingFlags.FlattenHierarchy | BindingFlags.Instance | BindingFlags.Static | BindingFlags.Public | BindingFlags.NonPublic;</p><p>public static void Main() { // Show size of heap before doing any reflection stuff Show(\"Before doing anything\");</p><p>// Build cache of MethodInfo objects for all methods in MSCorlib.dll List&lt;MethodBase&gt; methodInfos = new List&lt;MethodBase&gt;(); foreach (Type t in typeof(Object).Assembly.GetExportedTypes()) {</p><p>// Skip over any generic types if (t.IsGenericTypeDefinition) continue;</p><p>}</p><p>MethodBase[] mb = t.GetMethods(c_bf); methodInfos.AddRange(mb);</p><p>// Show number of methods and size of heap after binding to all methods Console.WriteLine(\"# of methods={0:N0}\", methodInfos.Count); Show(\"After building cache of MethodInfo objects\");</p><p>// Build cache of RuntimeMethodHandles for all MethodInfo objects List&lt;RuntimeMethodHandle&gt; methodHandles =</p><p>methodInfos.ConvertAll&lt;RuntimeMethodHandle&gt;(mb =&gt; mb.MethodHandle);</p><p>Show(\"Holding MethodInfo and RuntimeMethodHandle cache\"); GC.KeepAlive(methodInfos); // Prevent cache from being GC'd early</p><p>methodInfos = null; // Allow cache to be GC'd now Show(\"After freeing MethodInfo objects\");</p><p>methodInfos = methodHandles.ConvertAll&lt;MethodBase&gt;(</p><p>rmh=&gt; MethodBase.GetMethodFromHandle(rmh)); Show(\"Size of heap after re\u00adcreating MethodInfo objects\"); GC.KeepAlive(methodHandles); // Prevent cache from being GC'd early GC.KeepAlive(methodInfos); // Prevent cache from being GC'd early</p><p>} }</p><p>methodHandles = null; // Allow cache to be GC'd now methodInfos = null; // Allow cache to be GC'd now Show(\"After freeing MethodInfos and RuntimeMethodHandles\");</p><p>When I compiled and executed this program, I got the following output.</p><p>Heap size= 85,000 \u00ad Before doing anything # of methods=48,467 Heap size= 7,065,632 \u00ad After building cache of MethodInfo objects Heap size= 7,453,496 \u00ad Holding MethodInfo and RuntimeMethodHandle cache Heap size= 6,732,704 \u00ad After freeing MethodInfo objects Heap size= 7,372,704 \u00ad Size of heap after re\u00adcreating MethodInfo objects Heap size= 192,232 \u00ad After freeing MethodInfos and RuntimeMethodHandles</p><h6>C H A P T E R 2 4</h6><h2>Runtime Serialization</h2><h6>In this chapter:</h6><p>Serialization/Deserialization Quick Start . . . . . . . . . . . . . . . . . . . 613 Making a Type Serializable. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 617 Controlling Serialization and Deserialization. . . . . . . . . . . . . . . . 619 How Formatters Serialize Type Instances . . . . . . . . . . . . . . . . . . . 623 Controlling the Serialized/Deserialized Data. . . . . . . . . . . . . . . . 624 Streaming Contexts . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 631 Serializing a Type As a Different Type and Deserializing an Object As a Different Object. . . . . . . . . . . . . . . 633 Serialization Surrogates . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 636 Overriding the Assembly and/or Type When Deserializing an Object. . . . . . . . . . . . . . . . . . . . . . . . . . . . . 640</p><p>Serialization is the process of converting an object or a graph of connected objects into a stream of bytes. Deserialization is the process of converting a stream of bytes back into its graph of connected objects. The ability to convert objects to and from a byte stream is an incredibly useful mechanism. Here are some examples:</p><p>\u25a0\u25a0 An application\u2019s state (object graph) can easily be saved in a disk file or database and then restored the next time the application is run. ASP.NET saves and restores session state by way of serialization and deserialization.</p><p>\u25a0\u25a0 A set of objects can easily be copied to the system\u2019s clipboard and then pasted into the same or another application. In fact, Windows Forms and Windows Presentation Foundation (WPF) use this.</p><p>\u25a0\u25a0 A set of objects can be cloned and set aside as a \u201cbackup\u201d while a user manipulates the \u201cmain\u201d</p><p>set of objects.</p><p>\u25a0\u25a0 A set of objects can easily be sent over the network to a process running on another machine. The Microsoft .NET Framework\u2019s remoting architecture serializes and deserializes objects that are marshaled by value. It is also used to send objects across AppDomain boundaries, as discussed in Chapter 22, \u201cCLR Hosting and AppDomains.\u201d</p><p>611</p><p>In addition to the preceding examples, after you have serialized objects in a byte stream in memory, it is quite easy to process the data in more useful ways, such as encrypting and compressing the data.</p><p>With serialization being so useful, it is no wonder that many programmers have spent countless hours developing code to perform these types of actions. Historically, this code is difficult to write and is extremely tedious and error-prone. Some of the difficult issues that developers need to grapple with are communication protocols, client/server data type mismatches (such as little-endian/ big-endian issues), error handling, objects that refer to other objects, in and out parameters, arrays of structures, and the list goes on.</p><p>Well, you\u2019ll be happy to know that the .NET Framework has fantastic support for serialization and deserialization built right into it. This means that all of the difficult issues just mentioned are now handled completely and transparently by the .NET Framework. As a developer, you can work with your objects before serialization and after deserialization and have the .NET Framework handle the stuff in the middle.</p><p>In this chapter, I explain how the .NET Framework exposes its serialization and deserialization services. For almost all data types, the default behavior of these services will be sufficient, meaning that it is almost no work for you to make your own types serializable. However, there is a small minority of types where the serialization service\u2019s default behavior will not be sufficient. Fortunately, the serialization services are very extensible, and I will also explain how to tap into these extensibility mechanisms, allowing you to do some pretty powerful things when serializing or deserializing objects. For example, I\u2019ll demonstrate how to serialize Version 1 of an object out to a disk file and then deserialize it a year later into an object of Version 2.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile201.png\"/><p>Note This chapter focuses on the runtime serialization technology in the common language runtime (CLR), which has a deep understanding of CLR data types and can serialize all the public, protected, internal, and even private fields of an object to a compressed binary stream for high performance. See the System.Runtime.Serialization. NetDataContractSerializer class if you want to serialize CLR data types to an XML stream. The .NET Framework also offers other serialization technologies that are designed more for interoperating between CLR data types and non-CLR data types. These other serialization technologies use the System.Xml.Serialization.XmlSerializer class and the System.Runtime.Serialization.DataContractSerializer class.</p><h6>Serialization/Deserialization Quick Start</h6><p>Let\u2019s start off by looking at some code.</p><p>using System; using System.Collections.Generic; using System.IO; using System.Runtime.Serialization.Formatters.Binary;</p><p>internal static class QuickStart {</p><p>public static void Main() { // Create a graph of objects to serialize them to the stream var objectGraph = new List&lt;String&gt; { \"Jeff\", \"Kristin\", \"Aidan\", \"Grant\" }; Stream stream = SerializeToMemory(objectGraph);</p><p>// Reset everything for this demo stream.Position = 0; objectGraph = null;</p><p>}</p><p>// Deserialize the objects and prove it worked objectGraph = (List&lt;String&gt;) DeserializeFromMemory(stream); foreach (var s in objectGraph) Console.WriteLine(s);</p><p>private static MemoryStream SerializeToMemory(Object objectGraph) { // Construct a stream that is to hold the serialized objects MemoryStream stream = new MemoryStream();</p><p>// Construct a serialization formatter that does all the hard work BinaryFormatter formatter = new BinaryFormatter();</p><p>// Tell the formatter to serialize the objects into the stream formatter.Serialize(stream, objectGraph);</p><p>}</p><p>// Return the stream of serialized objects back to the caller return stream;</p><p>private static Object DeserializeFromMemory(Stream stream) { // Construct a serialization formatter that does all the hard work BinaryFormatter formatter = new BinaryFormatter();</p><p>} }</p><p>// Tell the formatter to deserialize the objects from the stream return formatter.Deserialize(stream);</p><p>Wow, look how simple this is! The SerializeToMemory method constructs a System.IO. MemoryStream object. This object identifies where the serialized block of bytes is to be placed. Then the method constructs a BinaryFormatter object (which can be found in the System. Runtime.Serialization.Formatters.Binary namespace). A formatter is a type (implementing the System.Runtime.Serialization.IFormatter interface) that knows how to serialize and deserialize an object graph. The Framework Class Library (FCL) ships with two formatters: the BinaryFormatter (used in this code example) and a SoapFormatter (which can be found in the System. Runtime.Serialization.Formatters.Soap namespace and is implemented in the System. Runtime.Serialization.Formatters.Soap.dll assembly).</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile202.png\"/><p>Note As of version 3.5 of the .NET Framework, the SoapFormatter class is obsolete and should not be used in production code. However, it can still be useful for debugging serialization code because it produces XML text that you can read. To use XML serialization and deserialization in production code, see the XmlSerializer and DataContractSerializer classes.</p><p>To serialize a graph of objects, just call the formatter\u2019s Serialize method and pass it two things: a reference to a stream object and a reference to the object graph that you want to serialize. The stream object identifies where the serialized bytes should be placed and can be an object of any type derived from the System.IO.Stream abstract base class. This means that you can serialize an object graph to a MemoryStream, a FileStream, a NetworkStream, and so on.</p><p>The second parameter to Serialize is a reference to an object. This object could be anything: an Int32, a String, a DateTime, an Exception, a List&lt;String&gt;, a Dictionary&lt;Int32, DatTime&gt;, and so on. The object referred to by the objectGraph parameter may refer to other objects.</p><p>For example, objectGraph may refer to a collection that refers to a set of objects. These objects may also refer to other objects. When the formatter\u2019s Serialize method is called, all objects in the graph are serialized to the stream.</p><p>Formatters know how to serialize the complete object graph by referring to the metadata that describes each object\u2019s type. The Serialize method uses reflection to see what instance fields are in each object\u2019s type as it is serialized. If any of these fields refer to other objects, then the formatter\u2019s Serialize method knows to serialize these objects, too.</p><p>Formatters have very intelligent algorithms. They know to serialize each object in the graph no more than once out to the stream. That is, if two objects in the graph refer to each other, then the formatter detects this, serializes each object just once, and avoids entering into an infinite loop.</p><p>In my SerializeToMemory method, when the formatter\u2019s Serialize method returns, the</p><p>MemoryStream is simply returned to the caller. The application uses the contents of this flat byte array any way it wants. For example, it could save it in a file, copy it to the clipboard, send it over a wire, or whatever.</p><p>The DeserializeFromStream method deserializes a stream back into an object graph. This method is even simpler than serializing an object graph. In this code, a BinaryFormatter is constructed and then its Deserialize method is called. This method takes the stream as a parameter and returns a reference to the root object within the deserialized object graph.</p><p>Internally, the formatter\u2019s Deserialize method examines the contents of the stream, constructs instances of all the objects that are in the stream, and initializes the fields in all these objects so that they have the same values they had when the object graph was serialized. Typically, you will cast the object reference returned from the Deserialize method into the type that your application is expecting.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile203.png\"/><p>Note Here\u2019s a fun, useful method that uses serialization to make a deep copy, or clone, of an object.</p><p>private static Object DeepClone(Object original) { // Construct a temporary memory stream using (MemoryStream stream = new MemoryStream()) {</p><p>// Construct a serialization formatter that does all the hard work BinaryFormatter formatter = new BinaryFormatter();</p><p>// This line is explained in this chapter's \"Streaming Contexts\" section formatter.Context = new StreamingContext(StreamingContextStates.Clone);</p><p>// Serialize the object graph into the memory stream formatter.Serialize(stream, original);</p><p>// Seek back to the start of the memory stream before deserializing stream.Position = 0;</p><p>} }</p><p>// Deserialize the graph into a new set of objects and // return the root of the graph (deep copy) to the caller return formatter.Deserialize(stream);</p><p>At this point, I\u2019d like to add a few notes to our discussion. First, it is up to you to ensure that your code uses the same formatter for both serialization and deserialization. For example, don\u2019t write code that serializes an object graph by using the SoapFormatter and then deserializes the graph by using the BinaryFormatter. If Deserialize can\u2019t decipher the contents of the stream, then a System.</p><p>Runtime.Serialization.SerializationException exception will be thrown.</p><p>The second thing I\u2019d like to point out is that it is possible and also quite useful to serialize multiple object graphs out to a single stream. For example, let\u2019s say that we have the following two class definitions.</p><p>[Serializable] internal sealed class Customer { /* ... */ } [Serializable] internal sealed class Order { /* ... */ }</p><p>And then, in the main class of our application, we define the following static fields.</p><p>private static List&lt;Customer&gt; s_customers = new List&lt;Customer&gt;(); private static List&lt;Order&gt; s_pendingOrders = new List&lt;Order&gt;(); private static List&lt;Order&gt; s_processedOrders = new List&lt;Order&gt;();</p><p>We can now serialize our application\u2019s state to a single stream with a method that looks like this.</p><p>private static void SaveApplicationState(Stream stream) { // Construct a serialization formatter that does all the hard work BinaryFormatter formatter = new BinaryFormatter();</p><p>}</p><p>// Serialize our application's entire state formatter.Serialize(stream, s_customers); formatter.Serialize(stream, s_pendingOrders); formatter.Serialize(stream, s_processedOrders);</p><p>To reconstruct our application\u2019s state, we would deserialize the state with a method that looks like this.</p><p>private static void RestoreApplicationState(Stream stream) { // Construct a serialization formatter that does all the hard work BinaryFormatter formatter = new BinaryFormatter();</p><p>}</p><p>// Deserialize our application's entire state (same order as serialized) s_customers = (List&lt;Customer&gt;) formatter.Deserialize(stream); s_pendingOrders = (List&lt;Order&gt;) formatter.Deserialize(stream); s_processedOrders = (List&lt;Order&gt;) formatter.Deserialize(stream);</p><p>The third and last thing I\u2019d like to point out has to do with assemblies. When serializing an object, the full name of the type and the name of the type\u2019s defining assembly are written to the stream. By default, BinaryFormatter outputs the assembly\u2019s full identity, which includes the assembly\u2019s file name (without extension), version number, culture, and public key information. When deserializing an object, the formatter first grabs the assembly identity and ensures that the assembly is loaded into the executing AppDomain by calling System.Reflection.Assembly\u2019s Load method (discussed in Chapter 23, \u201cAssembly Loading and Reflection\u201d).</p><p>After an assembly has been loaded, the formatter looks in the assembly for a type matching that of the object being deserialized. If the assembly doesn\u2019t contain a matching type, an exception is thrown and no more objects can be deserialized. If a matching type is found, an instance of the type is created and its fields are initialized from the values contained in the stream. If the type\u2019s fields don\u2019t exactly match the names of the fields as read from the stream, then a SerializationException exception is thrown and no more objects can be deserialized. Later in this chapter, I\u2019ll discuss some sophisticated mechanisms that allow you to override some of this behavior.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile204.png\"/><p>Important Some extensible applications use Assembly.LoadFrom to load an assembly and then construct objects from types defined in the loaded assembly. These objects can be serialized to a stream without any trouble. However, when deserializing this stream, the formatter attempts to load the assembly by calling Assembly\u2019s Load method instead of calling the LoadFrom method. In most cases, the CLR will not be able to locate the assembly file, causing a SerializationException exception to be thrown. This catches many developers by surprise\u2014because the objects serialized correctly, the developers expect that the objects will deserialize correctly as well.</p><p>If your application serializes objects whose types are defined in an assembly that your application loads using Assembly.LoadFrom, then I recommend that you implement a method whose signature matches the System.ResolveEventHandler delegate and register this method with System.AppDomain\u2019s AssemblyResolve event just before calling a formatter\u2019s Deserialize method. (Unregister this method with the event after Deserialize returns.) Now, whenever the formatter fails to load an assembly, the CLR calls your ResolveEventHandler method. This method is passed the identity of the assembly that failed to load. The method can extract the assembly file name from the assembly\u2019s identity and use this name to construct the path where the application knows the assembly file can be found. Then, the method can call Assembly.LoadFrom to load the assembly and return the resulting Assembly reference back from the ResolveEventHandler method.</p><p>This section covered the basics of how to serialize and deserialize object graphs. In the remaining sections, we\u2019ll look at what you must do in order to define your own serializable types, and we\u2019ll also look at various mechanisms that allow you to have greater control over serialization and deserialization.</p><h6>Making a Type Serializable</h6><p>When a type is designed, the developer must make the conscious decision as to whether or not to allow instances of the type to be serializable. By default, types are not serializable. For example, the following code does not perform as expected.</p><p>internal struct Point { public Int32 x, y; }</p><p>private static void OptInSerialization() { Point pt = new Point { x = 1, y = 2 }; using (var stream = new MemoryStream()) {</p><p>new BinaryFormatter().Serialize(stream, pt); // throws SerializationException }</p><p>}</p><p>If you were to build and run this code in your program, you\u2019d see that the formatter\u2019s Serialize method throws a System.Runtime.Serialization.SerializationException exception. The problem is that the developer of the Point type has not explicitly indicated that Point objects may be serialized. To solve this problem, the developer must apply the System.SerializableAttribute custom attribute to this type as follows. (Note that this attribute is defined in the System namespace, not the System.Runtime.Serialization namespace.)</p><p>[Serializable] internal struct Point { public Int32 x, y; }</p><p>Now, if we rebuild the application and run it, it does perform as expected and the Point objects will be serialized to the stream. When serializing an object graph, the formatter checks that every object\u2019s type is serializable. If any object in the graph is not serializable, the formatter\u2019s Serialize method throws the SerializationException exception.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile205.png\"/><p>Note When serializing a graph of objects, some of the object\u2019s types may be serializable and some of the objects may not be serializable. For performance reasons, formatters do not verify that all of the objects in the graph are serializable before serializing the graph. So, when serializing an object graph, it is entirely possible that some objects may be serialized to the stream before the SerializationException is thrown. If this happens, the stream contains corrupt data. If you think you may be serializing an object graph where some objects may not be serializable, your application code should be able to recover gracefully from this situation. One option is to serialize the objects into a MemoryStream first. Then, if all objects are successfully serialized, you can copy the bytes in the MemoryStream to whatever stream (for example, file, network) you really want the bytes written to.</p><p>The SerializableAttribute custom attribute may be applied to reference types (class), value types (struct), enumerated types (enum), and delegate types (delegate) only. (Note that enumerated and delegate types are always serializable, so there is no need to explicitly apply the SerializableAttribute attribute to these types.) In addition, the SerializableAttribute attribute is not inherited by derived types. So, given the following two type definitions, a Person object can be serialized, but an Employee object cannot.</p><p>[Serializable] internal class Person { ... }</p><p>internal class Employee : Person { ... }</p><p>To fix this, you would just apply the SerializableAttribute attribute to the Employee type as well.</p><p>[Serializable] internal class Person { ... }</p><p>[Serializable] internal class Employee : Person { ... }</p><p>Note that this problem was easy to fix. However, the reverse\u2014defining a type derived from a base</p><p>type that doesn\u2019t have the SerializableAttribute attribute applied to it\u2014is not easy to fix. But, this is by design; if the base type doesn\u2019t allow instances of its type to be serialized, its fields cannot be serialized, because a base object is effectively part of the derived object. This is why System.</p><p>Object has the SerializableAttribute attribute applied to it.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile206.png\"/><p>Note In general, it is recommended that most types you define be serializable. After all, this grants a lot of flexibility to users of your types. However, you must be aware that serialization reads all of an object\u2019s fields regardless of whether the fields are declared as public,</p><p>protected, internal, or private. You might not want to make a type serializable if it contains sensitive or secure data (like passwords) or if the data would have no meaning or value if transferred.</p><p>If you find yourself using a type that was not designed for serialization, and you do not have the source code of the type to add serialization support, all is not lost. In the \u201cOverriding the Assembly and/or Type When Deserializing an Object\u201d section later in this chapter, I will explain how you can make any non-serializable type serializable.</p><h6>Controlling Serialization and Deserialization</h6><p>When you apply the SerializableAttribute custom attribute to a type, all instance fields (public, private, protected, and so on) are serialized.1 However, a type may define some instance fields that should not be serialized. In general, there are two reasons why you would not want some of a type\u2019s instance fields to be serialized:</p><p>\u25a0\u25a0 The field contains information that would not be valid when deserialized. For example, an object that contains a handle to a Windows kernel object (such as a file, process, thread, mutex, event, semaphore, and so on) would have no meaning when deserialized into another process or machine because Windows\u2019 kernel handles are process-relative values.</p><p>1 Do not use C#\u2019s automatically implemented property feature to define properties inside types marked with the [Serializable] attribute, because the compiler generates the names of the fields and the generated names can be different each time that you recompile your code, preventing instances of your type from being deserializable.</p><p>\u25a0\u25a0 The field contains information that is easily calculated. In this case, you select which fields do not need to be serialized, thus improving your application\u2019s performance by reducing the amount of data transferred.</p><p>The following code uses the System.NonSerializedAttribute custom attribute to indicate which fields of the type should not be serialized. (Note that this attribute is also defined in the System namespace, not the System.Runtime.Serialization namespace.)</p><p>[Serializable] internal class Circle {</p><p>private Double m_radius;</p><p>[NonSerialized] private Double m_area;</p><p>public Circle(Double radius) { m_radius = radius; m_area = Math.PI * m_radius * m_radius;</p><p>}</p><p>... }</p><p>In the preceding code, objects of Circle may be serialized. However, the formatter will serialize the values in the object\u2019s m_radius field only. The value in the m_area field will not be serialized because it has the NonSerializedAttribute attribute applied to it. This attribute can be applied only to a type\u2019s fields, and it continues to apply to this field when inherited by another type. Of course, you may apply the NonSerializedAttribute attribute to multiple fields within a type.</p><p>So, let\u2019s say that our code constructs a Circle object as follows.</p><p>Circle c = new Circle(10);</p><p>Internally, the m_area field is set to a value approximate to 314.159. When this object gets serialized, only the value of the m_radius field (10) gets written to the stream. This is exactly what we want, but now we have a problem when the stream is deserialized back into a Circle object. When deserialized, the Circle object will get its m_radius field set to 10, but its m_area field will be initialized to 0\u2014not 314.159!</p><p>The following code demonstrates how to modify the Circle type to fix this problem.</p><p>[Serializable] internal class Circle {</p><p>private Double m_radius;</p><p>[NonSerialized] private Double m_area;</p><p>public Circle(Double radius) { m_radius = radius; m_area = Math.PI * m_radius * m_radius;</p><p>}</p><p>}</p><p>[OnDeserialized] private void OnDeserialized(StreamingContext context) {</p><p>m_area = Math.PI * m_radius * m_radius; }</p><p>I\u2019ve changed Circle so that it now contains a method marked with the System.Runtime. Serialization.OnDeserializedAttribute custom attribute.2 Whenever an instance of a type is deserialized, the formatter checks whether the type defines a method with this attribute on it and then the formatter invokes this method. When this method is called, all the serializable fields will be set correctly, and they may be accessed to perform any additional work that would be necessary to fully deserialize the object.</p><p>In the preceding modified version of Circle, I made the OnDeserialized method simply calculate the area of the circle by using the m_radius field and place the result in the m_area field. Now, m_area will have the desired value of 314.159.</p><p>In addition to the OnDeserializedAttribute custom attribute, the System.Runtime.Serialization namespace also defines OnSerializingAttribute, OnSerializedAttribute, and OnDeserializingAttribute custom attributes, which you can apply to your type\u2019s methods to have even more control over serialization and deserialization. Here is a sample class that applies each of these attributes to a method.</p><p>[Serializable] public class MyType {</p><p>Int32 x, y; [NonSerialized] Int32 sum;</p><p>public MyType(Int32 x, Int32 y) {</p><p>this.x = x; this.y = y; sum = x + y; }</p><p>[OnDeserializing] private void OnDeserializing(StreamingContext context) {</p><p>// Example: Set default values for fields in a new version of this type }</p><p>[OnDeserialized] private void OnDeserialized(StreamingContext context) {</p><p>// Example: Initialize transient state from fields sum = x + y;</p><p>}</p><p>[OnSerializing] private void OnSerializing(StreamingContext context) {</p><p>2 Use of the System.Runtime.Serialization.OnDeserialized custom attribute is the preferred way of invoking a method when an object is deserialized, as opposed to having a type implement the System.Runtime.Serialization. IDeserializationCallback interface\u2019s OnDeserialization method.</p><p>// Example: Modify any state before serializing }</p><p>[OnSerialized] private void OnSerialized(StreamingContext context) { // Example: Restore any state after serializing</p><p>} }</p><p>Whenever you use any of these four attributes, the method you define must take a single StreamingContext parameter (discussed in the \u201cStreaming Contexts\u201d section later in this chapter) and return void. The name of the method can be anything you want it to be. Also, you should declare the method as private to prevent it from being called by normal code; the formatters run with enough security that they can call private methods.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile207.png\"/><p>Note When you are serializing a set of objects, the formatter first calls all of the objects\u2019 methods that are marked with the OnSerializing attribute. Next, it serializes all of the objects\u2019 fields, and finally it calls all of the objects\u2019 methods marked with the OnSerialized attribute. Similarly, when you deserialize a set of objects, the formatter calls all of the objects\u2019 methods that are marked with the OnDeserializing attribute, then it deserializes all of the object\u2019s fields, and then it calls all of the objects\u2019 methods marked with the OnDeserialized attribute.</p><p>Note also that during deserialization, when a formatter sees a type offering a method marked with the OnDeserialized attribute, the formatter adds this object\u2019s reference to an internal list. After all the objects have been deserialized, the formatter traverses this list in reverse order and calls each object\u2019s OnDeserialized method. When this method is called, all the serializable fields will be set correctly, and they may be accessed to perform any additional work that would be necessary to fully deserialize the object. Invoking these methods in reverse order is important because it allows inner objects to finish their deserialization before the outer objects that contain them finish their deserialization.</p><p>For example, imagine a collection object (like Hashtable or Dictionary) that internally uses a hash table to maintain its sets of items. The collection object type would implement a method marked with the OnDeserialized attribute. Even though the collection object would start being deserialized first (before its items), its OnDeserialized method would be called last (after any of its items\u2019 OnDeserialized methods). This allows the items to complete deserialization so that all their fields are initialized properly, allowing a good hash code value to be calculated. Then, the collection object creates its internal buckets and uses the items\u2019 hash codes to place the items into the buckets. I show an example of how the Dictionary class uses this in the upcoming \u201cControlling the Serialized/ Deserialized Data\u201d section of this chapter.</p><p>If you serialize an instance of a type, add a new field to the type, and then try to deserialize the object that did not contain the new field, the formatter throws a SerializationException with a message indicating that the data in the stream being deserialized has the wrong number of members. This is very problematic in versioning scenarios where it is common to add new fields to a type in a newer version. Fortunately, you can use the System.Runtime.Serialization.OptionalFieldAttribute attribute to help you.</p><p>You apply the OptionalFieldAttribute attribute to each new field you add to a type. Now, when the formatters see this attribute applied to a field, the formatters will not throw the SerializationException exception if the data in the stream does not contain the field.</p><h6>How Formatters Serialize Type Instances</h6><p>In this section, I give a bit more insight into how a formatter serializes an object\u2019s fields. This knowledge can help you understand the more advanced serialization and deserialization techniques explained in the remainder of this chapter.</p><p>To make things easier for a formatter, the FCL offers a FormatterServices type in the System. Runtime.Serialization namespace. This type has only static methods in it, and no instances of the type may be instantiated. The following steps describe how a formatter automatically serializes an object whose type has the SerializableAttribute attribute applied to it.</p><ul style=\"list-style-type: none;\">\n<li><p>1. The formatter calls FormatterServices\u2019s GetSerializableMembers method.</p><p>public static MemberInfo[] GetSerializableMembers(Type type, StreamingContext context);</p>\n<p>This method uses reflection to get the type\u2019s public and private instance fields (excluding any fields marked with the NonSerializedAttribute attribute). The method returns an array of MemberInfo objects, one for each serializable instance field.</p>\n</li>\n<li><p>2. The object being serialized and the array of System.Reflection.MemberInfo objects are then passed to FormatterServices\u2019 static GetObjectData method.</p><p>public static Object[] GetObjectData(Object obj, MemberInfo[] members);</p>\n<p>This method returns an array of Objects where each element identifies the value of a field in the object being serialized. This Object array and the MemberInfo array are parallel. That is, element 0 in the Object array is the value of the member identified by element 0 in the MemberInfo array.</p>\n</li>\n<li><p>3. The formatter writes the assembly\u2019s identity and the type\u2019s full name to the stream.</p></li>\n<li><p>4. The formatter then enumerates over the elements in the two arrays, writing each member\u2019s name and value to the stream.</p></li>\n</ul><p>The following steps describe how a formatter automatically deserializes an object whose type has the SerializableAttribute attribute applied to it:</p><ul style=\"list-style-type: none;\">\n<li><p>1. The formatter reads the assembly\u2019s identity and full type name from the stream. If the assembly is not currently loaded into the AppDomain, it is loaded (as described earlier). If the assembly can\u2019t be loaded, a SerializationException exception is thrown and the object cannot be deserialized. If the assembly is loaded, the formatter passes the assembly identity information and the type\u2019s full name to FormatterServices\u2019 static GetTypeFromAssembly method.</p><p>public static Type GetTypeFromAssembly(Assembly assem, String name);</p>\n<p>This method returns a System.Type object indicating the type of object that is being deserialized.</p>\n</li>\n<li><p>2. The formatter calls FormatterServices\u2019s static GetUninitializedObject method.</p><p>public static Object GetUninitializedObject(Type type);</p>\n<p>This method allocates memory for a new object but does not call a constructor for the object. However, all the object\u2019s bytes are initialized to null or 0.</p>\n</li>\n<li><p>3. The formatter now constructs and initializes a MemberInfo array as it did before by calling the FormatterServices\u2019s GetSerializableMembers method. This method returns the set of fields that were serialized and that need to be deserialized.</p></li>\n<li><p>4. The formatter creates and initializes an Object array from the data contained in the stream.</p></li>\n<li><p>5. The reference to the newly allocated object, the MemberInfo array, and the parallel Object array of field values is passed to FormatterServices\u2019 static PopulateObjectMembers method.</p></li>\n</ul><p>public static Object PopulateObjectMembers( Object obj, MemberInfo[] members, Object[] data);</p><p>This method enumerates over the arrays, initializing each field to its corresponding value. At this point, the object has been completely deserialized.</p><h6>Controlling the Serialized/Deserialized Data</h6><p>As discussed earlier in this chapter, the best way to get control over the serialization and deserialization process is to use the OnSerializing, OnSerialized, OnDeserializing, OnDeserialized, NonSerialized, and OptionalField attributes. However, there are some very rare scenarios where these attributes do not give you all the control you need. In addition, the formatters use reflection internally and reflection is slow, which increases the time it takes to serialize and deserialize objects. To get complete control over what data is serialized/deserialized or to eliminate the use of reflection,</p><p>your type can implement the System.Runtime.Serialization.ISerializable interface, which is defined as follows.</p><p>public interface ISerializable {</p><p>void GetObjectData(SerializationInfo info, StreamingContext context); }</p><p>This interface has just one method in it, GetObjectData. But most types that implement this interface will also implement a special constructor that I\u2019ll describe shortly.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile208.png\"/><p>Important The big problem with the ISerializable interface is that after a type implements it, all derived types must implement it too, and the derived types must make sure that they invoke the base class\u2019s GetObjectData method and the special constructor. In addition, after a type implements this interface, it can never remove it because it will lose compatibility with the derived types. It is always OK for sealed types to implement the ISerializable interface. Using the custom attributes described earlier in this chapter avoids all of the potential problems associated with the ISerializable interface.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile209.png\"/><p>Important The ISerializable interface and the special constructor are intended to be used by the formatters. However, other code could call GetObjectData, which might then return potentially sensitive information, or other code could construct an object that passes in corrupt data. For this reason, it is recommended that you apply the following attribute to the GetObjectData method and the special constructor.</p><p>[SecurityPermissionAttribute(SecurityAction.Demand, SerializationFormatter = true)]</p><p>When a formatter serializes an object graph, it looks at each object. If its type implements the ISerializable interface, then the formatter ignores all custom attributes and instead constructs a new System.Runtime.Serialization.SerializationInfo object. This object contains the actual set of values that should be serialized for the object.</p><p>When constructing a SerializationInfo, the formatter passes two parameters: Type and System.Runtime.Serialization.IFormatterConverter. The Type parameter identifies the object that is being serialized. Two pieces of information are required to uniquely identify a type: the string name of the type and its assembly\u2019s identity (which includes the assembly name, version, culture, and public key). When a SerializationInfo object is constructed, it obtains the type\u2019s full name (by internally querying Type\u2019s FullName property) and stores this string in a private field. You can obtain the type\u2019s full name by querying SerializationInfo\u2019s FullTypeName property. Likewise, the constructor obtains the type\u2019s defining assembly (by internally querying</p><p>Type\u2019s Module property followed by querying Module\u2019s Assembly property followed by querying Assembly\u2019s FullName property) and stores this string in a private field. You can obtain the assembly\u2019s identity by querying SerializationInfo\u2019s AssemblyName property.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile210.png\"/><p>Note Although you can set a SerializationInfo\u2019s FullTypeName and AssemblyName properties, this is discouraged. If you want to change the type that is being serialized, it is recommended that you call SerializationInfo\u2019s SetType method, passing a reference to the desired Type object. Calling SetType ensures that the type\u2019s full name and defining assembly are set correctly. An example of calling SetType is shown in the \u201cSerializing a Type As a Different Type and Deserializing an Object As a Different Object\u201d section later in this chapter.</p><p>After the SerializationInfo object is constructed and initialized, the formatter calls the type\u2019s GetObjectData method, passing it the reference to the SerializationInfo object. The GetObjectData method is responsible for determining what information is necessary to serialize the object and adding this information to the SerializationInfo object. GetObjectData indicates what information to serialize by calling one of the many overloaded AddValue methods provided by the SerializationInfo type. AddValue is called once for each piece of data that you want to add.</p><p>The following code shows an approximation of how the Dictionary&lt;TKey, TValue&gt; type implements the ISerializable and IDeserializationCallback interfaces to take control over the serialization and deserialization of its objects.</p><p>[Serializable] public class Dictionary&lt;TKey, TValue&gt;: ISerializable, IDeserializationCallback {</p><p>// Private fields go here (not shown)</p><p>private SerializationInfo m_siInfo; // Only used for deserialization</p><p>// Special constructor (required by ISerializable) to control deserialization [SecurityPermissionAttribute(SecurityAction.Demand, SerializationFormatter = true)] protected Dictionary(SerializationInfo info, StreamingContext context) {</p><p>// During deserialization, save the SerializationInfo for OnDeserialization m_siInfo = info;</p><p>}</p><p>// Method to control serialization [SecurityCritical] public virtual void GetObjectData(SerializationInfo info, StreamingContext context) {</p><p>info.AddValue(\"Version\", m_version); info.AddValue(\"Comparer\", m_comparer, typeof(IEqualityComparer&lt;TKey&gt;)); info.AddValue(\"HashSize\", (m_ buckets == null) ? 0 : m_buckets.Length); if (m_buckets != null) {</p><p>KeyValuePair&lt;TKey, TValue&gt;[] array = new KeyValuePair&lt;TKey, TValue&gt;[Count]; CopyTo(array, 0); info.AddValue(\"KeyValuePairs\", array, typeof(KeyValuePair&lt;TKey, TValue&gt;[]));</p><p>} }</p><p>// Method called after all key/value objects have been deserialized public virtual void IDeserializationCallback.OnDeserialization(Object sender) {</p><p>if (m_siInfo == null) return; // Never set, return</p><p>Int32 num = m_siInfo.GetInt32(\"Version\"); Int32 num2 = m_siInfo.GetInt32(\"HashSize\"); m_comparer = (IEqualityComparer&lt;TKey&gt;)</p><p>m_siInfo.GetValue(\"Comparer\", typeof(IEqualityComparer&lt;TKey&gt;)); if (num2 != 0) {</p><p>m_buckets = new Int32[num2];</p><ul style=\"list-style-type: none;\">\n<li><p>for (Int32 i = 0; i &lt; m_buckets.Length; i++) m_buckets[i] = \u00ad1; m_entries = new Entry&lt;TKey, TValue&gt;[num2]; m_freeList = \u00ad1; KeyValuePair&lt;TKey, TValue&gt;[] pairArray = (KeyValuePair&lt;TKey, TValue&gt;[])</p><p>m_siInfo.GetValue(\"KeyValuePairs\", typeof(KeyValuePair&lt;TKey, TValue&gt;[])); if (pairArray == null)</p>\n<p>ThrowHelper.ThrowSerializationException( ExceptionResource.Serialization_MissingKeys);</p>\n</li>\n<li><p>for (Int32 j = 0; j &lt; pairArray.Length; j++) { if (pairArray[j].Key == null)</p></li>\n</ul><p>ThrowHelper.ThrowSerializationException( ExceptionResource.Serialization_NullKey);</p><p>}</p><p>Insert(pairArray[j].Key, pairArray[j].Value, true);</p><p>} } else { m_buckets = null; } m_version = num; m_siInfo = null;</p><p>Each AddValue method takes a String name and some data. Usually, the data is of a simple value type like Boolean, Char, Byte, SByte, Int16, UInt16, Int32, UInt32, Int64, UInt64, Single, Double, Decimal, or DateTime. However, you can also call AddValue, passing it a reference to an Object such as a String. After GetObjectData has added all of the necessary serialization information, it returns to the formatter.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile211.png\"/><p>Note You should always call one of the overloaded AddValue methods to add serialization information for your type. If a field\u2019s type implements the ISerializable interface, don\u2019t call the GetObjectData on the field. Instead, call AddValue to add the field; the formatter will see that the field\u2019s type implements ISerializable and the formatter will call GetObjectData for you. If you were to call GetObjectData on the field object, the formatter wouldn\u2019t know to create a new object when deserializing the stream.</p><p>The formatter now takes all of the values added to the SerializationInfo object and serializes each of them out to the stream. You\u2019ll notice that the GetObjectData method is passed another parameter: a reference to a System.Runtime.Serialization.StreamingContext object. Most types\u2019 GetObjectData methods will completely ignore this parameter, so I will not discuss it now. Instead, I\u2019ll discuss it in the \u201cStreaming Contexts\u201d section later in this chapter.</p><p>So now you know how to set all of the information used for serialization. At this point, let\u2019s turn our attention to deserialization. As the formatter extracts an object from the stream, it allocates memory for the new object (by calling the System.Runtime.Serialization.FormatterServices type\u2019s static GetUninitializedObject method). Initially, all of this object\u2019s fields are set to 0 or null. Then, the formatter checks if the type implements the ISerializable interface. If this interface exists, the formatter attempts to call a special constructor whose parameters are identical to that of the GetObjectData method.</p><p>If your class is sealed, then it is highly recommended that you declare this special constructor to be private. This will prevent any code from accidentally calling increasing security. If not, then you should declare this special constructor as protected so that only derived classes can call it. Note that the formatters are able to call this special constructor no matter how it is declared.</p><p>This constructor receives a reference to a SerializationInfo object containing all of the values added to it when the object was serialized. The special constructor can call any of the GetBoolean, GetChar, GetByte, GetSByte, GetInt16, GetUInt16, GetInt32, GetUInt32, GetInt64, GetUInt64, GetSingle, GetDouble, GetDecimal, GetDateTime, GetString, and GetValue methods, passing in a string corresponding to the name used to serialize a value. The value returned from each of these methods is then used to initialize the fields of the new object.</p><p>When deserializing an object\u2019s fields, you should call the Get method that matches the type of value that was passed to the AddValue method when the object was serialized. In other words, if the GetObjectData method called AddValue, passing it an Int32 value, then the GetInt32 method should be called for the same value when deserializing the object. If the value\u2019s type in the stream doesn\u2019t match the type you\u2019re trying to get, then the formatter will attempt to use an IFormatterConvert object to \u201ccast\u201d the stream\u2019s value to the desired type.</p><p>As I mentioned earlier, when a SerializationInfo object is constructed, it is passed an object whose type implements the IFormatterConverter interface. Because the formatter is responsible for constructing the SerializationInfo object, it chooses whatever IFormatterConverter type it wants. Microsoft\u2019s BinaryFormatter and SoapFormatter types always construct an instance of the System.Runtime.Serialization.FormatterConverter type. Microsoft\u2019s formatters don\u2019t offer any way for you to select a different IFormatterConverter type.</p><p>The FormatterConverter type calls the System.Convert class\u2019s static methods to convert values between the core types, such as converting an Int32 to an Int64. However, to convert a value between other arbitrary types, the FormatterConverter calls Convert\u2019s ChangeType method to</p><p>cast the serialized (or original) type to an IConvertible interface and then calls the appropriate interface method. Therefore, to allow objects of a serializable type to be deserialized as a different type, you may want to consider having your type implement the IConvertible interface. Note that the FormatterConverter object is used only when deserializing objects and when you\u2019re calling a Get method whose type doesn\u2019t match the type of the value in the stream.</p><p>Instead of calling the various Get methods previously listed, the special constructor could instead call GetEnumerator, which returns a System.Runtime.Serialization.Serialization\u00ad</p><p>InfoEnumerator object that can be used to iterate through all the values contained within the SerializationInfo object. Each value enumerated is a System.Runtime.Serialization. SerializationEntry object.</p><p>Of course, you are welcome to define a type of your own that derives from a type that implements ISerializable\u2019s GetObjectData and special constructor. If your type also implements ISerializable, then your implementation of GetObjectData and your implementation of the special constructor must call the same functions in the base class in order for the object to be serialized and deserialized properly. Do not forget to do this or the objects will not serialize or deserialize correctly. The next section explains how to properly define an ISerializable type whose base type doesn\u2019t implement this interface.</p><p>If your derived type doesn\u2019t have any additional fields in it and therefore has no special serialization/deserialization needs, then you do not have to implement ISerializable at all. Like all interface members, GetObjectData is virtual and will be called to properly serialize the object. In addition, the formatter treats the special constructor as \u201cvirtualized.\u201d That is, during deserialization, the formatter will check the type that it is trying to instantiate. If that type doesn\u2019t offer the special constructor, then the formatter will scan base classes until it finds one that implements the special constructor.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile212.png\"/><p>Important The code in the special constructor typically extracts its fields from the SerializationInfo object that is passed to it. As the fields are extracted, you are not guaranteed that the objects are fully deserialized, so the code in the special constructor should not attempt to manipulate the objects that it extracts.</p><p>If your type must access members (such as call methods) on an extracted object, then it is recommended that your type also provide a method that has the OnDeserialized attribute applied to it or have your type implement the IDeserializationCallback interface\u2019s OnDeserialization method (as shown in the Dictionary example). When this method is called, all objects have had their fields set. However, there is no guarantee to the order in which multiple objects have their OnDeserialized or OnDeserialization method called. So, although the fields may be initialized, you still don\u2019t know if a referenced object is completely deserialized if that referenced object also provides an OnDeserialized method or implements the IDeserializationCallback interface.</p><h6>How to Define a Type That Implements ISerializable When the Base Type Doesn\u2019t Implement This Interface</h6><p>As mentioned earlier, the ISerializable interface is extremely powerful, because it allows a type to take complete control over how instances of the type get serialized and deserialized. However, this power comes at a cost: The type is now responsible for serializing all of its base type\u2019s fields as well. Serializing the base type\u2019s fields is easy if the base type also implements the ISerializable interface; you just call the base type\u2019s GetObjectData method.</p><p>However, someday, you may find yourself defining a type that needs to take control of its serialization, but whose base type does not implement the ISerializable interface. In this case, your derived class must manually serialize the base type\u2019s fields by grabbing their values and adding them to the SerializationInfo collection. Then, in your special constructor, you will also have to get the values out of the collection and somehow set the base class\u2019s fields. Doing all of this is easy (albeit tedious) if the base class\u2019s fields are public or protected, but it can be very difficult or impossible to do if the base class\u2019s fields are private.</p><p>This following code shows how to properly implement ISerializable\u2019s GetObjectData method and its implied constructor so that the base type\u2019s fields are serialized.</p><p>[Serializable] internal class Base {</p><p>protected String m_name = \"Jeff\"; public Base() { /* Make the type instantiable */ }</p><p>}</p><p>[Serializable] internal sealed class Derived : Base, ISerializable {</p><p>private DateTime m_date = DateTime.Now; public Derived() { /* Make the type instantiable*/ }</p><p>// If this constructor didn't exist, we'd get a SerializationException // This constructor should be protected if this class were not sealed [SecurityPermissionAttribute(SecurityAction.Demand, SerializationFormatter = true)] private Derived(SerializationInfo info, StreamingContext context) {</p><p>// Get the set of serializable members for our class and base classes Type baseType = this.GetType().BaseType; MemberInfo[] mi = FormatterServices.GetSerializableMembers(baseType, context);</p><p>// Deserialize the base class's fields from the info object for (Int32 i = 0; i &lt; mi.Length; i++) {</p><p>// Get the field and set it to the deserialized value FieldInfo fi = (FieldInfo)mi[i]; fi.SetValue(this, info.GetValue(baseType.FullName + \"+\" + fi.Name, fi.FieldType));</p><p>}</p><p>}</p><p>// Deserialize the values that were serialized for this class m_date = info.GetDateTime(\"Date\");</p><p>[SecurityPermissionAttribute(SecurityAction.Demand, SerializationFormatter = true)] public virtual void GetObjectData(SerializationInfo info, StreamingContext context) {</p><p>// Serialize the desired values for this class info.AddValue(\"Date\", m_date);</p><p>// Get the set of serializable members for our class and base classes Type baseType = this.GetType().BaseType; MemberInfo[] mi = FormatterServices.GetSerializableMembers(baseType, context);</p><p>}</p><p>// Serialize the base class's fields to the info object for (Int32 i = 0; i &lt; mi.Length; i++) {</p><p>// Prefix the field name with the fullname of the base type info.AddValue(baseType.FullName + \"+\" + mi[i].Name,</p><p>((FieldInfo)mi[i]).GetValue(this)); }</p><p>} public override String ToString() {</p><p>return String.Format(\"Name={0}, Date={1}\", m_name, m_date); }</p><p>In this code, there is a base class, Base, which is marked only with the SerializableAttribute custom attribute. Derived from Base is Derived, which also is marked with the SerializableAttribute attribute and also implements the ISerializable interface. To make the situation more interesting, you\u2019ll notice that both classes define a String field called m_name. When calling SerializationInfo\u2019s AddValue method, you can\u2019t add multiple values with the same name. The preceding code handles this situation by identifying each field by its class name prepended to the field\u2019s name. For example, when the GetObjectData method calls AddValue to serialize Base\u2019s m_name field, the name of the value is written as \u201cBase+m_name.\u201d</p><h6>Streaming Contexts</h6><p>As mentioned earlier, there are many destinations for a serialized set of objects: same process, different process on the same machine, different process on a different machine, and so on. In some rare situations, an object might want to know where it is going to be deserialized so that it can emit its state differently. For example, an object that wraps a Windows semaphore object might decide to serialize its kernel handle if the object knows that it will be deserialized into the same process, because kernel handles are valid within a process. However, the object might decide to serialize the semaphore\u2019s string name if it knows that the object will be deserialized on the same machine but into a different process. Finally, the object might decide to throw an exception if it knows that it will be deserialized in a process running on a different machine because a semaphore is valid only within a single machine.</p><p>A number of the methods mentioned earlier in this chapter accept a StreamingContext. A StreamingContext structure is a very simple value type offering just two public read-only properties, as shown in Table 24-1.</p><ul style=\"list-style-type: none;\">\n<li><p>TABLE 24-1 StreamingContext\u2019s Public Read-Only Properties</p><table border=\"1\">\n<tr>\n<td><p>Member Name</p>\n<p>State</p>\n</td>\n<td><p>Member Type</p>\n<p>StreamingContextStates</p>\n</td>\n<td><h6>Description</h6>\n<p>A set of bit flags indicating the source or destination of the objects being serialized/deserialized</p>\n</td>\n</tr>\n<tr>\n<td><p>Context</p>\n</td>\n<td><p>Object</p>\n</td>\n<td><p>A reference to an object that contains any user-desired context information</p>\n</td>\n</tr>\n</table>\n<p>A method that receives a StreamingContext structure can examine the State property\u2019s bit flags to determine the source or destination of the objects being serialized/deserialized. Table 24-2 shows the possible bit flag values.</p>\n</li>\n<li><p>TABLE 24-2 StreamingContextStates\u2019s Flags</p></li>\n</ul><table border=\"1\">\n<tr>\n<td><p>Flag Name</p>\n<p>CrossProcess</p>\n</td>\n<td><p>Flag Value 0x0001</p>\n</td>\n<td><h6>Description</h6>\n<p>The source or destination is a different process on the same machine.</p>\n</td>\n</tr>\n<tr>\n<td><p>CrossMachines</p>\n</td>\n<td><p>0x0002</p>\n</td>\n<td><p>The source or destination is on a different machine.</p>\n</td>\n</tr>\n<tr>\n<td><p>File</p>\n</td>\n<td><p>0x0004</p>\n</td>\n<td><p>The source or destination is a file. Don\u2019t assume that the same process will deserialize the data.</p>\n</td>\n</tr>\n<tr>\n<td><p>Persistence</p>\n</td>\n<td><p>0x0008</p>\n</td>\n<td><p>The source or destination is a store such as a database or a file. Don\u2019t assume that the same process will deserialize the data.</p>\n</td>\n</tr>\n<tr>\n<td><p>Remoting</p>\n</td>\n<td><p>0x0010</p>\n</td>\n<td><p>The source or destination is remoting to an unknown location. The location may be on the same machine but may also be on another machine.</p>\n</td>\n</tr>\n<tr>\n<td><p>Other</p>\n</td>\n<td><p>0x0020</p>\n</td>\n<td><p>The source or destination is unknown.</p>\n</td>\n</tr>\n<tr>\n<td><p>Clone</p>\n</td>\n<td><p>0x0040</p>\n</td>\n<td><p>The object graph is being cloned. The serialization code may assume that the same process will deserialize the data, and it is therefore safe to access handles or other unmanaged resources.</p>\n</td>\n</tr>\n<tr>\n<td><p>CrossAppDomain</p>\n</td>\n<td><p>0x0080</p>\n</td>\n<td><p>The source or destination is a different AppDomain.</p>\n</td>\n</tr>\n<tr>\n<td><p>All</p>\n</td>\n<td><p>0x00FF</p>\n</td>\n<td><p>The source or destination may be any of the above contexts. This is the default context.</p>\n</td>\n</tr>\n</table><figcaption>Now that you know how to get this information, let\u2019s discuss how you would set this information.</figcaption><p>The IFormatter interface (which is implemented by both the BinaryFormatter and the SoapFormatter types) defines a read/write StreamingContext property called Context. When you construct a formatter, the formatter initializes its Context property so that StreamingContextStates is set to All and the reference to the additional state object is set to null.</p><p>After the formatter is constructed, you can construct a StreamingContext structure using any of the StreamingContextStates bit flags, and you can optionally pass a reference to an object containing any additional context information you need. Now, all you need to do is set the formatter\u2019s Context property with this new StreamingContext object before calling the formatter\u2019s Serialize or Deserialize methods. Code demonstrating how to tell a formatter that you are serializing/deserialzing an object graph for the sole purpose of cloning all the objects in the graph is shown in the DeepClone method presented earlier in this chapter.</p><h6>Serializing a Type As a Different Type and Deserializing an Object As a Different Object</h6><p>The .NET Framework\u2019s serialization infrastructure is quite rich, and in this section, we discuss how a developer can design a type that can serialize or deserialize itself into a different type or object. Below are some examples where this is interesting:</p><p>\u25a0\u25a0 Some types (such as System.DBNull and System.Reflection.Missing) are designed to</p><p>have only one instance per AppDomain. These types are frequently called singletons. If you have a reference to a DBNull object, serializing and deserializing it should not cause a new DBNull object to be created in the AppDomain. After deserializing, the returned reference should refer to the AppDomain\u2019s already-existing DBNull object.</p><p>\u25a0\u25a0 Some types (such as System.Type, System.Reflection.Assembly, and other reflection types like MemberInfo) have one instance per type, assembly, member, and so on. Imagine you have an array where each element references a MemberInfo object. It\u2019s possible that five array elements reference a single MemberInfo object. After serializing and deserializing this array, the five elements that referred to a single MemberInfo object should all refer to a single MemberInfo object. What\u2019s more, these elements should refer to the one MemberInfo object that exists for the specific member in the AppDomain. You could also imagine how this could be useful for polling database connection objects or any other type of object.</p><p>\u25a0\u25a0 For remotely controlled objects, the CLR serializes information about the server object that, when deserialized on the client, causes the CLR to create a proxy object. This type of the proxy object is a different type than the server object, but this is transparent to the client code. When the client calls instance methods on the proxy object, the proxy code internally remotes the call to the server that actually performs the request.</p><p>Let\u2019s look at some code that shows how to properly serialize and deserialize a singleton type.</p><p>// There should be only one instance of this type per AppDomain [Serializable] public sealed class Singleton : ISerializable {</p><p>// This is the one instance of this type private static readonly Singleton s_theOneObject = new Singleton();</p><p>// Here are the instance fields public String Name = \"Jeff\"; public DateTime Date = DateTime.Now;</p><p>// Private constructor allowing this type to construct the singleton private Singleton() { }</p><p>// Method returning a reference to the singleton public static Singleton GetSingleton() { return s_theOneObject; }</p><p>// Method called when serializing a Singleton // I recommend using an Explicit Interface Method Impl. Here [SecurityPermissionAttribute(SecurityAction.Demand, SerializationFormatter = true)] void ISerializable.GetObjectData(SerializationInfo info, StreamingContext context) {</p><p>info.SetType(typeof(SingletonSerializationHelper)); // No other values need to be added</p><p>}</p><p>[Serializable] private sealed class SingletonSerializationHelper : IObjectReference {</p><p>// Method called after this object (which has no fields) is deserialized public Object GetRealObject(StreamingContext context) {</p><p>return Singleton.GetSingleton(); }</p><p>}</p><p>// NOTE: The special constructor is NOT necessary because it's never called }</p><p>The Singleton class represents a type that allows only one instance of itself to exist per AppDomain. The following code tests the Singleton\u2019s serialization and deserialization code to ensure that only one instance of the Singleton type ever exists in the AppDomain.</p><p>private static void SingletonSerializationTest() { // Create an array with multiple elements referring to the one Singleton object Singleton[] a1 = { Singleton.GetSingleton(), Singleton.GetSingleton() }; Console.WriteLine(\"Do both elements refer to the same object? \"</p><p>+ (a1[0] == a1[1])); // \"True\"</p><p>using (var stream = new MemoryStream()) { BinaryFormatter formatter = new BinaryFormatter();</p><p>// Serialize and then deserialize the array elements formatter.Serialize(stream, a1); stream.Position = 0; Singleton[] a2 = (Singleton[])formatter.Deserialize(stream);</p><p>}</p><p>// Prove that it worked as expected: Console.WriteLine(\"Do both elements refer to the same object? \"</p><p>+ (a2[0] == a2[1])); // \"True\" Console.WriteLine(\"Do all elements refer to the same object? \"</p><p>+ (a1[0] == a2[0])); // \"True\" }</p><p>Now, let\u2019s walk through the code to understand what\u2019s happening. When the Singleton type is loaded into the AppDomain, the CLR calls its static constructor, which constructs a Singleton object and saves a reference to it in a static field, s_theOneObject. The Singleton class doesn\u2019t offer any public constructors, which prevents any other code from constructing any other instances of this class.</p><p>In SingletonSerializationTest, an array is created consisting of two elements; each element references the Singleton object. The two elements are initialized by calling Singleton\u2019s static GetSingleton method. This method returns a reference to the one Singleton object. The first call to Console\u2019s WriteLine method displays \u201cTrue,\u201d verifying that both array elements refer to the same exact object.</p><p>Now, SingletonSerializationTest calls the formatter\u2019s Serialize method to serialize the array and its elements. When serializing the first Singleton, the formatter detects that the Singleton type implements the ISerializable interface and calls the GetObjectData method. This method calls SetType, passing in the SingletonSerializationHelper type, which tells the formatter to serialize the Singleton object as a SingletonSerializationHelper object instead. Because AddValue is not called, no additional field information is written to the stream. Because the formatter automatically detected that both array elements refer to a single object, the formatter serializes only one object.</p><p>After serializing the array, SingletonSerializationTest calls the formatter\u2019s Deserialize method. When deserializing the stream, the formatter tries to deserialize a SingletonSerializationHelper object because this is what the formatter was \u201ctricked\u201d into serializing. (In fact, this is why the Singleton class doesn\u2019t provide the special constructor that is usually required when implementing the ISerializable interface.) After constructing the SingletonSerializationHelper object, the formatter sees that this type implements the System. Runtime.Serialization.IObjectReference interface. This interface is defined in the FCL as follows.</p><p>public interface IObjectReference {</p><p>Object GetRealObject(StreamingContext context); }</p><p>When a type implements this interface, the formatter calls the GetRealObject method. This method returns a reference to the object that you really want a reference to now that deserialization of the object has completed. In my example, the SingletonSerializationHelper type has GetRealObject return a reference to the Singleton object that already exists in the AppDomain. So, when the formatter\u2019s Deserialize method returns, the a2 array contains two elements, both of which refer to the AppDomain\u2019s Singleton object. The SingletonSerializationHelper object used to help with the deserialization is immediately unreachable and will be garbage collected in the future.</p><p>The second call to WriteLine displays \u201cTrue,\u201d verifying that both of a2\u2019s array elements refer to the exact same object. The third and last call to WriteLine also displays \u201cTrue,\u201d proving that the elements in both arrays all refer to the exact same object.</p><h6>Serialization Surrogates</h6><p>Up to now, I\u2019ve been discussing how to modify a type\u2019s implementation to control how a type serializes and deserializes instances of itself. However, the formatters also allow code that is not part of the type\u2019s implementation to override how a type serializes and deserializes its objects. There are two main reasons why application code might want to override a type\u2019s behavior:</p><p>\u25a0\u25a0 It allows a developer the ability to serialize a type that was not originally designed to be</p><p>serialized.</p><p>\u25a0\u25a0 It allows a developer to provide a way to map one version of a type to a different version</p><p>of a type.</p><p>Basically, to make this mechanism work, you first define a \u201csurrogate type\u201d that takes over the actions required to serialize and deserialize an existing type. Then, you register an instance of your surrogate type with the formatter telling the formatter which existing type your surrogate type is responsible for acting on. When the formatter detects that it is trying to serialize or deserialize an instance of the existing type, it will call methods defined by your surrogate object. Let\u2019s build a sample that demonstrates how all this works.</p><p>A serialization surrogate type must implement the System.Runtime.Serialization.ISerializationSurrogate interface, which is defined in the FCL as follows.</p><p>public interface ISerializationSurrogate { void GetObjectData(Object obj, SerializationInfo info, StreamingContext context);</p><p>Object SetObjectData(Object obj, SerializationInfo info, StreamingContext context,</p><p>ISurrogateSelector selector); }</p><p>Now, let\u2019s walk through an example that uses this interface. Let\u2019s say your program contains some DateTime objects that contain values that are local to the user\u2019s computer. What if you want to serialize the DateTime objects to a stream but you want the values to be serialized in universal time? This would allow you to send the data over a network stream to another machine in another part of the world and have the DateTime value be correct. Although you can\u2019t modify the DateTime type that ships with the FCL, you can define your own serialization surrogate class that can control how DateTime objects are serialized and deserialized. Here is how to define the surrogate class.</p><p>internal sealed class UniversalToLocalTimeSerializationSurrogate : ISerializationSurrogate {</p><p>public void GetObjectData(Object obj, SerializationInfo info, StreamingContext context) { // Convert the DateTime from local to UTC info.AddValue(\"Date\", ((DateTime)obj).ToUniversalTime().ToString(\"u\"));</p><p>}</p><p>public Object SetObjectData(Object obj, SerializationInfo info, StreamingContext context, ISurrogateSelector selector) { // Convert the DateTime from UTC to local return DateTime.ParseExact(info.GetString(\"Date\"), \"u\", null).ToLocalTime();</p><p>} }</p><p>The GetObjectData method here works just like the ISerializable interface\u2019s GetObject\u00ad</p><p>Data method. The only difference is that ISerializationSurrogate\u2019s GetObjectData method takes one additional parameter: a reference to the \u201creal\u201d object that is to be serialized. In the GetObjectData method above, this object is cast to DateTime, the value is converted from local time</p><p>to universal time, and a string (formatted using universal full date/time pattern) is added to the SerializationInfo collection.</p><p>The SetObjectData method is called in order to deserialize a DateTime object. When this method is called, it is passed a reference to a SerializationInfo object. SetObjectData gets the string date out of this collection, parses it as a universal full date/time formatted string, and then converts the resulting DateTime object from universal time to the machine\u2019s local time.</p><p>The Object that is passed for SetObjectData\u2019s first parameter is a bit strange. Just before calling SetObjectData, the formatter allocates (via FormatterServices\u2019s static GetUninitializedObject method) an instance of the type that the surrogate is a surrogate for. The instance\u2019s fields are</p><p>all 0/null and no constructor has been called on the object. The code inside SetObjectData can simply initialize the fields of this instance by using the values from the passed-in SerializationInfo object and then have SetObjectData return null. Alternatively, SetObjectData could create</p><p>an entirely different object or even a different type of object and return a reference to this new object, in which case, the formatter will ignore any changes that may or may not have happened to the object it passed in to SetObjectData.</p><p>In my example, my UniversalToLocalTimeSerializationSurrogate class acts as a surrogate for the DateTime type, which is a value type. And so, the obj parameter refers to a boxed instance of a DateTime. There is no way to change the fields in most value types (because they are supposed to be immutable) and so, my SetObjectData method ignores the obj parameter and returns a new DateTime object with the desired value in it.</p><p>At this point, I\u2019m sure you\u2019re all wondering how the formatter knows to use this ISerializationSurrogate type when it tries to serialize/deserialize a DateTime object. The following code demonstrates how to test the UniversalToLocalTimeSerializationSurrogate class.</p><p>private static void SerializationSurrogateDemo() { using (var stream = new MemoryStream()) {</p><ul style=\"list-style-type: none;\">\n<li><p>// 1. Construct the desired formatter IFormatter formatter = new SoapFormatter();</p></li>\n<li><p>// 2. Construct a SurrogateSelector object SurrogateSelector ss = new SurrogateSelector();</p></li>\n<li><p>// 3. Tell the surrogate selector to use our surrogate for DateTime objects ss.AddSurrogate(typeof(DateTime), formatter.Context,</p><p>new UniversalToLocalTimeSerializationSurrogate());</p>\n<p>// NOTE: AddSurrogate can be called multiple times to register multiple surrogates</p>\n</li>\n<li><p>// 4. Tell the formatter to use our surrogate selector formatter.SurrogateSelector = ss;</p></li>\n</ul><p>// Create a DateTime that represents the local time on the machine &amp; serialize it DateTime localTimeBeforeSerialize = DateTime.Now; formatter.Serialize(stream, localTimeBeforeSerialize);</p><p>// The stream displays the Universal time as a string to prove it worked stream.Position = 0; Console.WriteLine(new StreamReader(stream).ReadToEnd());</p><p>// Deserialize the Universal time string &amp; convert it to a local DateTime stream.Position = 0; DateTime localTimeAfterDeserialize = (DateTime)formatter.Deserialize(stream);</p><p>} }</p><p>// Prove it worked correctly: Console.WriteLine(\"LocalTimeBeforeSerialize ={0}\", localTimeBeforeSerialize); Console.WriteLine(\"LocalTimeAfterDeserialize={0}\", localTimeAfterDeserialize);</p><p>After steps 1 through 4 have executed, the formatter is ready to use the registered surrogate types. When the formatter\u2019s Serialize method is called, each object\u2019s type is looked up in the set maintained by the SurrogateSelector. If a match is found, then the ISerializationSurrogate object\u2019s GetObjectData method is called to get the information that should be written out to the stream.</p><p>When the formatter\u2019s Deserialize method is called, the type of the object about to be deserialized is looked up in the formatter\u2019s SurrogateSelector and if a match is found, then the ISerializationSurrogate object\u2019s SetObjectData method is called to set the fields within the object being deserialized.</p><p>Internally, a SurrogateSelector object maintains a private hash table. When AddSurrogate</p><p>is called, the Type and StreamingContext make up the key and the ISerializationSurrogate object is the key\u2019s value. If a key with the same Type/StreamingContext already exists, then AddSurrogate throws an ArgumentException. By including a StreamingContext in the key, you can</p><p>register one surrogate type object that knows how to serialize/deserialize a DateTime object to a file and register a different surrogate object that knows how to serialize/deserialize a DateTime object to a different process.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile213.png\"/><p>Note The BinaryFormatter class has a bug that prevents a surrogate from serializing objects with references to each other. To fix this problem, you need to pass a reference to your ISerializationSurrogate object to FormatterServices\u2019s static GetSurrogateForCyclicalReference method. This method returns an ISerializationSurrogate</p><p>object, which you can then pass to the SurrogateSelector\u2019s AddSurrogate method. However, when you use the GetSurrogateForCyclicalReference method, your surrogate\u2019s SetObjectData method must modify the value inside the object referred to by</p><p>SetObjectData\u2019s obj parameter and ultimately return null or obj to the calling method. The downloadable code that accompanies this book shows how to modify the UniversalToLocalTimeSerializationSurrogate class and the SerializationSurrogateDemo method to support cyclical references.</p><h6>Surrogate Selector Chains</h6><p>Multiple SurrogateSelector objects can be chained together. For example, you could have a SurrogateSelector that maintains a set of serialization surrogates that are used for serializing</p><p>types into proxies that get remoted across the wire or between AppDomains. You could also have a separate SurrogateSelector object that contains a set of serialization surrogates that are used to convert Version 1 types into Version 2 types.</p><p>If you have multiple SurrogateSelector objects that you\u2019d like the formatter to use, you must chain them together into a linked list. The SurrogateSelector type implements the ISurrogateSelector interface, which defines three methods. All three of these methods are related to chaining. Here is how the ISurrogateSelector interface is defined.</p><p>public interface ISurrogateSelector { void ChainSelector(ISurrogateSelector selector); ISurrogateSelector GetNextSelector(); ISerializationSurrogate GetSurrogate(Type type, StreamingContext context,</p><p>out ISurrogateSelector selector); }</p><p>The ChainSelector method inserts an ISurrogateSelector object immediately after the ISurrogateSelector object being operated on (\u2018this\u2019 object). The GetNextSelector method returns a reference to the next ISurrogateSelector object in the chain or null if the object being operated on is the end of the chain.</p><p>The GetSurrogate method looks up a Type/StreamingContext pair in the ISurrogateSelector object identified by this. If the pair cannot be found, then the next ISurrogateSelector object in the chain is accessed, and so on. If a match is found, then GetSurrogate returns the ISerializationSurrogate object that handles the serialization/deserialization of the type looked up. In addition, GetSurrogate also returns the ISurrogateSelector object that contained the match; this is usually not needed and is ignored. If none of the ISurrogateSelector objects in the chain have a match for the Type/StreamingContext pair, GetSurrogate returns null.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile214.png\"/><p>Note The FCL defines an ISurrogateSelector interface and also defines a SurrogateSelector type that implements this interface. However, it is extremely rare that anyone will ever have to define their own type that implements the ISurrogateSelector interface. The only reason to define your own type that implements this interface is if you need to have more flexibility over mapping one type to another. For example, you might want to serialize all types that inherit from a specific base class in a special way. The System. Runtime.Remoting.Messaging.RemotingSurrogateSelector class is a perfect example. When serializing objects for remoting purposes, the CLR formats the objects using the RemotingSurrogateSelector. This surrogate selector serializes all objects that derive from System.MarshalByRefObject in a special way so that deserialization causes proxy objects to be created on the client side.</p><h6>Overriding the Assembly and/or Type When Deserializing an Object</h6><p>When serializing an object, formatters output the type\u2019s full name and the full name of the type\u2019s defining assembly. When deserializing an object, formatters use this information to know exactly what type of object to construct and initialize. The earlier discussion about the ISerializationSurrogate interface showed a mechanism allowing you to take over the serialization and deserialization duties for a specific type. A type that implements the ISerializationSurrogate interface is tied to a specific type in a specific assembly.</p><p>However, there are times when the ISerializationSurrogate mechanism doesn\u2019t provide enough flexibility. Here are some scenarios when it might be useful to deserialize an object into a different type than it was serialized as:</p><p>\u25a0\u25a0 A developer might decide to move a type\u2019s implementation from one assembly to a different assembly. For example, the assembly\u2019s version number changes making the new assembly different from the original assembly.</p><p>\u25a0\u25a0 An object on a server that gets serialized into a stream that is sent to a client. When the client processes the stream, it could deserialize the object to a completely different type whose code knows how to remotely invoke method calls to the server\u2019s object.</p><p>\u25a0\u25a0 A developer makes a new version of a type. We want to deserialize any already-serialized</p><p>objects into the new version of the type.</p><p>The System.Runtime.Serialization.SerializationBinder class makes deserializing an object to a different type very easy. To do this, you first define your own type that derives from the abstract SerializationBinder type. In the following code, assume that version 1.0.0.0 of your assembly defined a class called Ver1 and assume that the new version of your assembly defines the Ver1ToVer2SerializationBinder class and also defines a class called Ver2.</p><p>internal sealed class Ver1ToVer2SerializationBinder : SerializationBinder { public override Type BindToType(String assemblyName, String typeName) { // Deserialize any Ver1 object from version 1.0.0.0 into a Ver2 object</p><p>// Calculate the assembly name that defined the Ver1 type AssemblyName assemVer1 = Assembly.GetExecutingAssembly().GetName(); assemVer1.Version = new Version(1, 0, 0, 0);</p><p>// If deserializing the Ver1 object from v1.0.0.0, turn it into a Ver2 object if (assemblyName == assemVer1.ToString() &amp;&amp; typeName == \"Ver1\")</p><p>return typeof(Ver2);</p><p>} }</p><p>// Else, just return the same type being requested return Type.GetType(String.Format(\"{0}, {1}\", typeName, assemblyName));</p><p>Now, after you construct a formatter, construct an instance of Ver1ToVer2SerializationBinder and set the formatter\u2019s Binder read/write property to refer to the binder object. After setting the Binder property, you can now call the formatter\u2019s Deserialize method. During deserialization, the formatter sees that a binder has been set. As each object is about to be deserialized, the formatter calls the binder\u2019s BindToType method, passing it the assembly name and type that the formatter wants to deserialize. At this point, BindToType decides what type should actually be constructed and returns this type.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile215.png\"/><p>Note The SerializationBinder class also makes it possible to change the assembly/type information while serializing an object by overriding its BindToName method, which looks like this.</p><p>public virtual void BindToName(Type serializedType, out string assemblyName, out string typeName)</p><p>During serialization, the formatter calls this method, passing you the type it wants to serialize. You can then return (via the two out parameters) the assembly and type that you want to serialize instead. If you return null and null (which is what the default implementation does), then no change is performed.</p><h6>C H A P T E R 2 5</h6><h2>Interoperating with WinRT Components</h2><h6>In this chapter:</h6><p>CLR Projections and WinRT Component Type System Rules . . 645 Framework Projections . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 649 Defining WinRT Components in C# . . . . . . . . . . . . . . . . . . . . . . . . 658</p><p>Windows 8 comes with a new class library allowing applications to access operating system functionality. The formal name for this class library is the Windows Runtime (WinRT) and its components are accessible using the WinRT type system. WinRT has many of the same goals that the Common Language Runtime (CLR) had when it was first introduced, such as simplifying application development and allowing code implemented in different programming languages to easily interoperate with one another. Specifically, Microsoft supports consuming WinRT components from native C/C++, JavaScript (when using Microsoft\u2019s \u201cChakra\u201d JavaScript virtual machine), as well as C# and Visual Basic via the CLR.</p><p>Figure 25-1 shows the kinds of features exposed by Windows\u2019s WinRT components and the various languages that Microsoft supports to access them. For applications implemented with native C/C++, the developer must compile his or her code for each CPU architecture (x86, x64, and ARM). But Microsoft .NET Framework developers need to compile their code just once into Intermediate Language (IL) and then the CLR compiles that into the native code specific to the host CPU. JavaScript developers actually ship the source code to their application and the \u201cChakra\u201d virtual machine parses the source code and compiles it into native code specific to the host machine\u2019s CPU. Other companies can produce languages and environments that support interoperating with WinRT components too.</p><p>Windows Store Apps and desktop applications can leverage operating system functionality by using these WinRT components. Today, the number of WinRT components that ship as part of Windows is relatively tiny when compared to the size of the .NET Framework\u2019s class library. However, this is by design, because the components are focused on exposing what an operating system does best: abstracting hardware and cross-application facilities to application developers. So, most of the WinRT components expose features, such as storage, networking, graphics, media, security, threading, and so on. Other core language services (like string manipulation) and more complex frameworks (like language integrated query) are not offered by the operating system and are instead provided by the language being used to access the operating system\u2019s WinRT components.</p><p>643</p><table border=\"1\">\n<tr>\n<td><p>HTML &amp; CSS</p>\n</td>\n</tr>\n</table><table border=\"1\">\n<tr>\n<td><p>DirectX or XAML</p>\n</td>\n</tr>\n</table><table border=\"1\">\n<tr>\n<td><p>XAML</p>\n</td>\n</tr>\n</table><table border=\"1\">\n<tr>\n<td><p>C/C++</p>\n</td>\n</tr>\n</table><table border=\"1\">\n<tr>\n<td><p>WinJS</p>\n</td>\n</tr>\n</table><p>CRT C#/VB FCL JS</p><table border=\"1\">\n<tr>\n<td><p>\u201cChakra\u201d</p>\n</td>\n</tr>\n</table><table border=\"1\">\n<tr>\n<td><p>(no runtime; rebuild for x86, x64, ARM)</p>\n</td>\n</tr>\n</table><p>CLR</p><table border=\"1\">\n<tr>\n<td><p>Windows 8</p>\n<p>Application Model</p>\n<p>Devices</p>\n<p>Graphics &amp; Media</p>\n<p>Storage Networking Security Threading</p>\n<p>UI XAML</p>\n<p>WinRT</p>\n</td>\n</tr>\n</table><p>FIGURE 25-1 The kinds of features exposed by Windows\u2019s WinRT components and the various languages that Microsoft supports to access them.</p><p>WinRT components are internally implemented as Component Object Model (COM) components, which is a technology that Microsoft introduced in 1993. At the time, COM was considered a complicated model with a lot of arcane rules and a very tedious programming model. However, there are a lot of good ideas in COM, and over the years, Microsoft has tweaked it in an effort to greatly simplify it. For WinRT components, Microsoft made a very significant tweak: instead of using type libraries to describe a COM component\u2019s API, they now use metadata. That\u2019s right, WinRT components describe their API using the same .NET metadata format (ECMA-335) as was standardized by the ECMA committee. This is the same metadata format I\u2019ve been discussing throughout this whole book.</p><p>Metadata is much richer than type libraries and the CLR already has a complete understanding of metadata. In addition, the CLR has supported interoperating with COM components via Runtime Callable Wrappers (RCWs) and COM Callable Wrappers (CCWs) because its inception. For the most part, this allows languages (like C#) running on top of the CLR to seamlessly interoperate with WinRT types and components.</p><p>In C#, when you have a reference to a WinRT object, you really have a reference to an RCW that internally refers to the WinRT object. Similarly, if you pass a CLR object to a WinRT API, you are really passing a reference to a CCW to the WinRT API and the CCW holds a reference to your CLR object.</p><p>WinRT components have their metadata embedded in files with a .winmd file extension. The WinRT components that ship with Windows have their metadata in the various Windows.*.winmd files, which can be found in the %WinDir%\\System32\\WinMetadata directory. When building an app, you would reference the one Windows.winmd file that the Windows SDK installs here.</p><p>%ProgramFiles(x86)%\\Windows Kits\\8.0\\References\\CommonConfiguration\\Neutral\\Windows.winmd</p><p>A major design goal of the Windows Runtime type system is to enable developers to be successful</p><p>in writing apps by using the technologies, tools, practices, and conventions that are already familiar and well-known to them. In order to achieve this, some WinRT features are projected to the respective development technologies. For .NET Framework developers, there are two kinds of projections:</p><p>\u25a0\u25a0 CLR projections CLR projections are mappings performed implicitly by the CLR, usually related to reinterpreting metadata. The next section focuses on the WinRT Component Type System Rules and how the CLR projects these rules to the .NET Framework developer.</p><p>\u25a0\u25a0 Framework projections Framework projections are mappings performed explicitly in your code by leveraging new APIs introduced into the Framework Class Library. Framework projections are required when the impedance mismatch between the WinRT type system and the CLR\u2019s type system is too great for the CLR to do it implicitly. Framework projections are discussed later in this chapter.</p><h6>CLR Projections and WinRT Component Type System Rules</h6><p>WinRT components conform to a type system similar to how the CLR enforces a type system. When the CLR sees a WinRT type, it usually allows that type to be used via the CLRs normal COM interop technologies. But, in some cases, the CLR hides the WinRT type (by dynamically setting it to private) and then the CLR exposes the type via a different type. Internally, the CLR is looking for certain types (via metadata) and then mapping these types to types in the Framework Class Library. For the complete list of WinRT types that the CLR implicitly projects to Framework Class Library types, see http:// msdn.microsoft.com/en-us/library/windows/apps/hh995050.aspx.</p><h6>WinRT Type System Core Concepts</h6><p>The WinRT type system is not as feature rich as the CLR\u2019s type system. This bulleted list describes the WinRT type system\u2019s core concepts and how the CLR projects them:</p><p>\u25a0\u25a0 File names and namespaces The name of the .winmd file itself must match the name of the namespace containing the WinRT components. For example, a file named Wintellect. WindowsStore.winmd must have WinRT components defined in a Wintellect.WindowsStore namespace or in a sub-namespace of Wintellect.WindowsStore. Because the Windows file system is case insensitive, namespaces that differ by case only are not allowed. Also, a WinRT component cannot have the same name as a namespace.</p><p>\u25a0\u25a0 Common base type WinRT components do not share a common base class. When the CLR projects a WinRT type, it appears as if the WinRT type is derived from System.Object and therefore all WinRT types inherit public methods like ToString, GetHashCode, Equals, and GetType. So, when using a WinRT object via C#, the object will look like it is derived from System.Object, and you can pass WinRT objects throughout your code. You can also call the \u201cinherited\u201d methods such as ToString.</p><p>\u25a0\u25a0 Core data types The WinRT type system supports the core data types such as Boolean, unsigned byte, 16-bit, 32-bit, and 64-bit signed and unsigned integer numbers, single-precision and double-precision floating-point numbers, 16-bit character, strings, and void.1 Like in the CLR, all other data types are composed from these core data types.</p><p>\u25a0\u25a0 Classes WinRT is an object-oriented type system, meaning that WinRT components support data abstraction, inheritance, and polymorphism.2 However, some languages (like JavaScript) do not support type inheritance and in order to cater to these languages, almost no WinRT components take advantage of inheritance. This means they also do not take advantage of polymorphism. In fact, only WinRT components consumable from non-JavaScript languages leverage inheritance and polymorphism. For the WinRT components that ship with Windows, only the XAML components (for building user interfaces) take advantage of inheritance and polymorphism. Applications written in JavaScript use HTML and CSS to produce their user interface instead.</p><p>\u25a0\u25a0 Structures WinRT supports structures (value types), and instances of these are marshaled by value across the COM interoperability boundary. Unlike CLR value types, WinRT structures can only have public fields of the core data types or of another WinRT structure.3 In addition, WinRT structures cannot have any constructors or helper methods. For convenience, the CLR projects some operating system WinRT structures as some native CLR types, which do offer constructors and helper methods. These projected types feel more natural to the CLR developer. Examples include the Point, Rect, Size, and TimeSpan structures all defined in the Windows.Foundation namespace.</p><p>\u25a0\u25a0 Nullable Structures WinRT APIs can expose nullable structures (value types). The CLR projects the WinRT\u2019s Windows.Foundation.IReference&lt;T&gt; interface as the CLR\u2019s System.Nullable&lt;T&gt; type.</p><p>\u25a0\u25a0 Enumerations An enumeration value is simply passed as a signed or unsigned 32-bit integer. If you define an enumeration type in C#, the underlying type must be either int or uint. Also, signed 32-bit integer enums are considered to be discreet values, whereas unsigned 32-bit enums are considered to be flags capable of being OR\u2019d together.</p><p>\u25a0\u25a0 Interfaces A WinRT interface\u2019s members must specify only WinRT-compatible types for</p><p>parameters and return types.</p><p>\u25a0\u25a0 Methods WinRT has limited support for method overloading. Specifically, because JavaScript has dynamic typing, it can\u2019t distinguish between methods that differ only by the types of their parameters. For example, JavaScript will happily pass a number to a method expecting a string. However, JavaScript can distinguish between a method that takes one parameter and a method that takes two parameters. In addition, WinRT does not support operator overload methods and default argument values. Furthermore, arguments can only be marshaled in or</p><ul style=\"list-style-type: none;\">\n<li><p>1 Signed byte is not supported by WinRT.</p></li>\n<li><p>2 Data abstraction is actually enforced, because WinRT classes are not allowed to have public fields.</p></li>\n<li><p>3 Enumerations are also OK, because they are really just 32-bit integers.</p></li>\n</ul><p>out; never in and out. This means you can\u2019t apply ref to a method argument but out is OK. For more information about this, see the \u201cArrays\u201d bullet point in the next list.</p><p>\u25a0\u25a0 Properties WinRT properties must specify only WinRT-compatible types for their data type.</p><p>WinRT does not support parameterful properties or write-only properties.</p><p>\u25a0\u25a0 Delegates WinRT delegate types must specify only WinRT components for parameter types and return types. When passing a delegate to a WinRT component, the delegate object is wrapped with a CCW and will not get garbage collected until the CCW is released by the WinRT component consuming it. WinRT delegates do not have BeginInvoke and EndInvoke methods.</p><p>\u25a0\u25a0 Events WinRT components can expose events by using a WinRT delegate type. Because most WinRT components are sealed (no inheritance), WinRT defines a TypedEventHandler delegate where the sender parameter is a generic type (as opposed to System.Object).</p><p>public delegate void TypedEventHandler&lt;TSender, TResult&gt;(TSender sender, TResult args);</p><p>There is also a Windows.Foundation.EventHandler&lt;T&gt; WinRT delegate type that the CLR projects as the .NET Framework\u2019s familiar System.EventHandler&lt;T&gt; delegate type.</p><p>\u25a0\u25a0 Exceptions Under the covers, WinRT components, like COM components, indicate their status via HRESULT values (a 32-bit integer with special semantics). The CLR projects WinRT values of type Windows.Foundation.HResult as exception objects. When a WinRT API returns a well-known failure HRESULT value, the CLR throws an instance of a corresponding Exceptionderived class. For instance, the HRESULT 0x8007000e (E_OUTOFMEMORY) is mapped to a System.OutOfMemoryException. Other HRESULT values cause the CLR to throw a System. Exception object whose HResult property contains the HRESULT value. A WinRT component implemented in C# can just throw an exception of a desired type and the CLR will convert it to an appropriate HRESULT value. To have complete control over the HRESULT value, construct an exception object, assign a specific HRESULT value in the object\u2019s HResult property, and then throw the object.</p><p>\u25a0\u25a0 Strings Of course, you can pass immutable strings between the WinRT and CLR type systems. However, the WinRT type system doesn\u2019t allow a string to have a value of null. If you pass null to a string parameter of a WinRT API, the CLR detects this and throws an ArgumentNullException; instead, use String.Empty to pass an empty string into a WinRT API. Strings are passed by reference to a WinRT API; they are pinned on the way in and unpinned upon return. Strings are always copied when returned from a WinRT API back to the CLR. When passing a CLR string array (String[]) to or from a WinRT API, a copy of the array is made with all its string elements and the copy is passed or returned to the other side.</p><p>\u25a0\u25a0 Dates and Times The WinRT Windows.Foundation.DateTime structure represents a UTC date/time. The CLR projects the WinRT DateTime structure as the .NET Framework\u2019s System.DateTimeOffset structure, because DateTimeOffset is preferred over the .NET Framework\u2019s System.DateTime structure. The CLR converts the UTC date/time being</p><p>returned from a WinRT to local time in the resulting DateTimeOffset instance. The CLR passes a DateTimeOffet to a WinRT API as a UTC time.</p><p>\u25a0\u25a0 URIs The CLR projects the WinRT Windows.Foundation.Uri type as the .NET Framework\u2019s System.Uri type. When passing a .NET Framework Uri to a WinRT API, the CLR throws an ArgumentException if the URI is a relative URI; WinRT supports absolute URIs only. URIs are always copied across the interop boundary.</p><p>\u25a0\u25a0 IClosable/IDisposable The CLR projects the WinRT Windows.Foundation.IClosable interface (which has only a Close method) as the .NET Framework\u2019s System.IDisposable interface (with its Dispose method). One thing to really take note of here is that all WinRT APIs that perform I/O operations are implemented asynchronously. Because IClosable interface\u2019s method is called Close and is not called CloseAsync, the Close method must not perform any I/O operations. This is semantically different from how Dispose usually works in the .NET Framework. For .NET Framework-implemented types, calling Dispose can do I/O and, in fact, it frequently causes buffered data to be written before actually closing a device. When C# code calls Dispose on a WinRT type however, I/O (like writing buffered data) will not be performed and a loss of data is possible. You must be aware of this and, for WinRT components that wrap output streams, you will have to explicitly call methods to prevent data loss. For example, when using a DataWriter, you should always call its StoreAsync method.</p><p>\u25a0\u25a0 Arrays WinRT APIs support single-dimension, zero-based arrays. WinRT can marshal an array\u2019s elements in or out of a method; never in and out. Because of this, you cannot pass an array into a WinRT API, have the API modify the array\u2019s elements and then access the modified elements after the API returns.4 I have just described the contract that should be adhered to. However, this contract is not actively enforced, so it is possible that some projections might marshal array contents both in and out. This usually happens naturally due to improving performance. For example, if the array contains structures, the CLR will simply pin the array, pass it to the WinRT API, and then unpin it upon return. In effect, the array\u2019s contents are passed in, the WinRT API can modify the contents and, in effect, the modified contents are returned. However, in this example, the WinRT API is violating the contract and this behavior is not guaranteed to work. And, in fact, it will not work if the API is invoked on a WinRT component that is running out-of-process.</p><p>\u25a0\u25a0 Collections When passing a collection to a WinRT API, the CLR wraps the collection object with a CCW and passes a reference to the CCW to the WinRT API. When WinRT code invokes a member on the CCW, the calling thread crosses the interop boundary, thereby incurring a performance hit. Unlike arrays, this means that passing a collection to a WinRT API allows the API to manipulate the collection in place, and copies of the collection\u2019s elements are not being created. Table 25-1 shows the WinRT collection interfaces and how the CLR projects them to</p><p>.NET application code.</p><ul style=\"list-style-type: none;\">\n<li><p>4 This means you can\u2019t have an API like System.Array\u2019s Sort method. Interestingly, all the languages (C, C++, C#, Visual Basic, and JavaScript) support passing array elements in and out, but the WinRT type system does not allow this.</p></li>\n</ul><figcaption>TABLE 25-1 WinRT Collection Interfaces and Projected CLR Collection Types</figcaption><table border=\"1\">\n<tr>\n<td><p>WinRT Collection Type (Windows.Foundation.Collections namespace)</p>\n<p>IIterable&lt;T&gt;</p>\n</td>\n<td><p>Projected CLR Collection Type (System.Collections.Generic namespace)</p>\n<p>IEnumerable&lt;T&gt;</p>\n</td>\n</tr>\n<tr>\n<td><p>IVector&lt;T&gt;</p>\n</td>\n<td><p>IList&lt;T&gt;</p>\n</td>\n</tr>\n<tr>\n<td><p>IVectorView&lt;T&gt;</p>\n</td>\n<td><p>IReadOnlyList&lt;T&gt;</p>\n</td>\n</tr>\n<tr>\n<td><p>IMap&lt;K, V&gt;</p>\n</td>\n<td><p>IDictionary&lt;TKey, TValue&gt;</p>\n</td>\n</tr>\n<tr>\n<td><p>IMapView&lt;K, V&gt;</p>\n</td>\n<td><p>IReadOnlyDictionary&lt;TKey, TValue&gt;</p>\n</td>\n</tr>\n<tr>\n<td><p>IKeyValuePair&lt;K, V&gt;</p>\n</td>\n<td><p>KeyValuePair&lt;TKey, TValue&gt;</p>\n</td>\n</tr>\n</table><p>As you can see from the previous list, the CLR team has done a lot of work to make interoperating between the WinRT type system and the CLR\u2019s type system as seamless as possible so that it is easy for managed code developers to leverage WinRT components in their code.5</p><h6>Framework Projections</h6><p>When the CLR can\u2019t implicitly project a WinRT type to the .NET Framework developer, the developer must resort to explicitly using framework projections. There are three main technologies where framework projections are required: asynchronous programming, interoperating between WinRT streams and .NET Framework streams, and when passing blocks of data between the CLR and WinRT APIs. These three framework projections are discussed in the following three sections of this chapter. Because many applications require the use of these technologies, it is important that you understand them well and use them effectively.</p><h6>Calling Asynchronous WinRT APIs from .NET Code</h6><p>When a thread performs an I/O operation synchronously, the thread can block for an indefinite amount of time. When a GUI thread waits for a synchronous I/O operation to complete, the application\u2019s user interface stops responding to user input, such as touch, mouse, and stylus events, causing the user to get frustrated with the application. To prevent non-responsive applications, WinRT components that perform I/O operations expose the functionality via asynchronous APIs. In fact, WinRT components that perform compute operations also expose this functionality via asynchronous APIs if the CPU operation could take greater than 50 milliseconds. For more information about building responsive applications, see Part V, \u201cThreading\u201d of this book.</p><p>5 To learn even more, go to http://msdn.microsoft.com/en-us/library/windows/apps/hh995050.aspx and then download the CLRandtheWindowsRuntime.docx document.</p><p>Because so many WinRT APIs are asynchronous, being productive with them requires that you understand how to interoperate with them from C#. To understand it, examine the following code.</p><p>public void WinRTAsyncIntro() { IAsyncOperation&lt;StorageFile&gt; asyncOp = KnownFolders.MusicLibrary.GetFileAsync(\"Song.mp3\"); asyncOp.Completed = OpCompleted; // Optional: call asyncOp.Cancel() sometime later</p><p>}</p><p>// NOTE: Callback method executes via GUI or thread pool thread: private void OpCompleted(IAsyncOperation&lt;StorageFile&gt; asyncOp, AsyncStatus status) {</p><p>switch (status) { case AsyncStatus.Completed: // Process result StorageFile file = asyncOp.GetResults(); /* Completed... */ break;</p><p>case AsyncStatus.Canceled: // Process cancellation /* Canceled... */ break;</p><p>}</p><p>case AsyncStatus.Error: // Process exception Exception exception = asyncOp.ErrorCode; /* Error... */ break;</p><p>} asyncOp.Close();</p><p>The WinRTAsyncIntro method invokes the WinRT GetFileAsync method to find a file in the user\u2019s music library. All WinRT APIs that perform asynchronous operations are named with the Async suffix and they all return an object whose type implements a WinRT IAsyncXxx interface; in this example, an IAsyncOperation&lt;TResult&gt; interface where TResult is the WinRT StorageFile type. This object, whose reference I put in an asyncOp variable, represents the pending asynchronous operation. Your code must somehow receive notification when the pending operation completes. To do this, you must implement a callback method (OpCompleted in my example), create a delegate to it, and assign the delegate to the asyncOp\u2019s Completed property. Now, when the operation completes, the callback method is invoked via some thread (not necessarily the GUI thread). If the operation completed before assigning the delegate to the OnCompleted property, then the system will invoke the callback as soon as possible. In other words, there is a race condition here, but the object implementing the IAsyncXxx interface resolves the race for you, ensuring that your code works correctly.</p><p>As noted at the end of the WinRTAsyncIntro method, you can optionally call a Cancel method offered by all IAsyncXxx interfaces if you want to cancel the pending operation. All asynchronous operations complete for one of three possible reasons: the operation runs to completion successfully, the operation is explicitly canceled, or the operation results in a failure. When the operation completes due to any of these reasons, the system invokes the callback method, passes it a reference to the same object that the original XxxAsync method returned, and an AsyncStatus. In my OnCompleted method, I examine the status parameter and either process the result due to the successful completion, handle the explicit cancellation, or handle the failure.6 Also, note that after</p><p>6 The IAsyncInfo interface offers a Status property that contains the same value that is passed into the callback method\u2019s status parameter. Because the parameter is passed by value, your application\u2019s performance is better if you access the parameter as opposed to querying IAsyncInfo\u2019s Status property because querying the property invokes a WinRT API via an RCW.</p><p>processing the operation\u2019s completion, the IAsyncXxx interface object should be cleaned up by calling its Close method.</p><p>Figure 25-2 shows the various WinRT IAsyncXxx interfaces. The four main interfaces all derive from the IAsyncInfo interface. The two IAsyncAction interfaces give you a way to know when the operation has completed, but their operations complete with no return value (their GetResults methods have a void return type). The two IAsyncOperation interfaces also give you a way to know when the operation has completed and allow you to get their return value (their GetResults methods have a generic TResult return type).</p><p>The two IAsyncXxxWithProgress interfaces allow your code to receive periodic progress updates as the asynchronous operation is progressing through its work. Most asynchronous operations do not offer progress updates but some (like background downloading and uploading) do. To receive periodic progress updates, you would define another callback method in your code, create a delegate that refers to it, and assign the delegate to the IAsyncXxxWithProgress object\u2019s Progress property. When your callback method is invoked, it is passed an argument whose type matches the generic TProgress type.</p><table border=\"1\">\n<tr>\n<td><p>IAsyncAction Completed (delegate) GetResults (void)</p>\n</td>\n</tr>\n</table><p>IAsyncInfo Id (UInt32) Status (AsyncStatus) ErrorCode (Exception) Cancel() Close()</p><table border=\"1\">\n<tr>\n<td><p>IAsyncActionWithProgress&lt;TProgress&gt; Completed (delegate) GetResults (void) Progress (delegate)</p>\n</td>\n</tr>\n</table><table border=\"1\">\n<tr>\n<td><p>IAsyncOperation&lt;TResult&gt; Completed (delegate) GetResults (TResult)</p>\n</td>\n</tr>\n</table><table border=\"1\">\n<tr>\n<td><p>AsyncStatus Started Completed, Canceled, Error</p>\n</td>\n</tr>\n</table><table border=\"1\">\n<tr>\n<td><p>IAsyncOperationWithProgress&lt;TResult, TProgress&gt; Completed (delegate) GetResults (TResult) Progress (delegate)</p>\n</td>\n</tr>\n</table><p>FIGURE 25-2 WinRT\u2019s interfaces related to performing asynchronous I/O and compute operations.</p><p>In the .NET Framework, we use the types in the System.Threading.Tasks namespace to simplify performing asynchronous operations. I explain these types and how to use them to perform compute operations in Chapter 27, \u201cCompute-Bound Asynchronous Operations,\u201d and how to use them to perform I/O operations in Chapter 28, \u201cI/O-Bound Asynchronous Operations.\u201d In addition, C# offers the async and await keywords, which allow you to perform asynchronous operations by using a sequential programming model, thereby simplifying your code substantially.</p><p>The following code is a rewrite of the WinRTAsyncIntro method previously mentioned. However, this version is leveraging some extension methods supplied with the .NET Framework, which turn the WinRT asynchronous programming model into the more convenient C# programming model.</p><p>using System; // Required for extension methods in WindowsRuntimeSystemExtensions</p><p>. . . public async void WinRTAsyncIntro() {</p><p>try { StorageFile file = await KnownFolders.MusicLibrary.GetFileAsync(\"Song.mp3\"); /* Completed... */</p><p>} catch (OperationCanceledException) { /* Canceled... */ } catch (SomeOtherException ex) { /* Error... */ }</p><p>}</p><p>What\u2019s happening here is that the use of C#\u2019s await operator causes the compiler to look for a GetAwaiter method on the IAsyncOperation&lt;StorageFile&gt; interface returned from the GetFileAsync method. This interface doesn\u2019t provide a GetAwaiter method, and so, the compiler looks for an extension method. Fortunately, the .NET Framework team has provided in System.Runtime.WindowsRuntime.dll a bunch of extension methods callable when you have one of WinRT\u2019s IAsyncXxx interfaces.</p><p>namespace System {</p><p>public static class WindowsRuntimeSystemExtensions { public static TaskAwaiter GetAwaiter(this IAsyncAction source); public static TaskAwaiter GetAwaiter&lt;TProgress&gt;(this IAsyncActionWithProgress&lt;TProgress&gt;</p><p>source);</p><p>public static TaskAwaiter&lt;TResult&gt; GetAwaiter&lt;TResult&gt;(this IAsyncOperation&lt;TResult&gt; source);</p><p>public static TaskAwaiter&lt;TResult&gt; GetAwaiter&lt;TResult, TProgress&gt;(</p><p>this IAsyncOperationWithProgress&lt;TResult, TProgress&gt; source); }</p><p>}</p><p>Internally, all these methods construct a TaskCompletionSource and tell the IAsyncXxx object to invoke a callback that sets the TaskCompletionSource\u2019s final state when the asynchronous operation completes. The TaskAwaiter object returned from these extension methods is ultimately what C# awaits. When the asynchronous operation completes, the TaskAwaiter object ensures that the code continues executing via the SynchronizationContext (discussed in Chapter 28) that is associated with the original thread. Then, the thread executes the C# compiler generated code, which queries the TaskCompletionSource\u2019s Task\u2019s Result property, which returns the result (a StorageFile in my example), throws an OperationCanceledException in case of cancellation, or throws some other exception if a failure occurred. For an example of how these methods work internally, see the code at the end of this section.</p><p>What I have just shown is the common scenario of calling an asynchronous WinRT API and discovering its outcome. But, in the previous code, I showed how to find out if cancellation occurred, but I didn\u2019t show how to actually cancel the operation. In addition, I didn\u2019t show how to deal with progress updates. To properly handle cancellation and progress updates, instead of having the compiler automatically call one of the GetAwaiter extension methods shown earlier, you would explicitly call one of the AsTask extension methods that the WindowsRuntimeSystemExtensions class also defines.</p><p>namespace System { public static class WindowsRuntimeSystemExtensions { public static Task AsTask&lt;TProgress&gt;(this IAsyncActionWithProgress&lt;TProgress&gt; source, CancellationToken cancellationToken, IProgress&lt;TProgress&gt; progress);</p><p>public static Task&lt;TResult&gt; AsTask&lt;TResult, TProgress&gt;( this IAsyncOperationWithProgress&lt;TResult, TProgress&gt; source, CancellationToken cancellationToken, IProgress&lt;TProgress&gt; progress);</p><p>}</p><p>// Simpler overloads not shown here }</p><p>So now, let me show you the complete picture. Here is how to call an asynchronous WinRT API and fully leverage cancellation and progress for those times when you need these enhancements.</p><p>using System; // For WindowsRuntimeSystemExtensions\u2019s AsTask using System.Threading; // For CancellationTokenSource</p><p>internal sealed class MyClass { private CancellationTokenSource m_cts = new CancellationTokenSource();</p><p>// NOTE: If invoked by GUI thread, all code executes via GUI thread: private async void MappingWinRTAsyncToDotNet(WinRTType someWinRTObj) {</p><p>try { // Assume XxxAsync returns IAsyncOperationWithProgress&lt;IBuffer, UInt32&gt; IBuffer result = await someWinRTObj.XxxAsync(...)</p><p>.AsTask(m_cts.Token, new Progress&lt;UInt32&gt;(ProgressReport)); /* Completed... */</p><p>} catch (OperationCanceledException) { /* Canceled... */ } catch (SomeOtherException) { /* Error... */ }</p><p>}</p><p>private void ProgressReport(UInt32 progress) { /* Update progress... */ }</p><p>public void Cancel() { m_cts.Cancel(); } // Called sometime later }</p><p>I know that some readers would like to understand how these AsTask methods internally convert a WinRT IAsyncXxx into a .NET Framework Task that can ultimately be awaited. The following code shows how the most complicated AsTask method is effectively implemented internally. The simpler overloads are, of course, simpler than this.</p><p>public static Task&lt;TResult&gt; AsTask&lt;TResult, TProgress&gt;( this IAsyncOperationWithProgress&lt;TResult, TProgress&gt; asyncOp, CancellationToken ct = default(CancellationToken), IProgress&lt;TProgress&gt; progress = null) {</p><p>// When CancellationTokenSource is canceled, cancel the async operation ct.Register(() =&gt; asyncOp.Cancel());</p><p>// When the async operation reports progress, report it to the progress callback asyncOp.Progress = (asyncInfo, p) =&gt; progress.Report(p);</p><p>// This TaskCompletionSource monitors the async operation's completion var tcs = new TaskCompletionSource&lt;TResult&gt;();</p><p>// When the async operation completes, notify the TaskCompletionSource // Code awaiting the TaskCompletionSource regains control when this happens asyncOp.Completed = (asyncOp2, asyncStatus) =&gt; {</p><p>switch (asyncStatus) { case AsyncStatus.Completed: tcs.SetResult(asyncOp2.GetResults()); break; case AsyncStatus.Canceled: tcs.SetCanceled(); break; case AsyncStatus.Error: tcs.SetException(asyncOp2.ErrorCode); break;</p><p>} };</p><p>}</p><p>// When calling code awaits this returned Task, it calls GetAwaiter, which // wraps a SynchronizationContext around the Task ensuring that completion // occurs on the SynchronizationContext object's context return tcs.Task;</p><h6>Interoperating Between WinRT Streams and .NET Streams</h6><p>There are many .NET Framework classes that operate on System.IO.Stream-derived types, such as serialization and LINQ to XML. To use a WinRT object that implements WinRT\u2019s IStorageFile or IStorageFolder interfaces with a .NET Framework class that requires a Stream-derived type, we leverage extension methods defined in the System.IO.WindowsRuntimeStorageExtensions class.</p><p>namespace System.IO { // Defined in System.Runtime.WindowsRuntime.dll</p><p>public static class WindowsRuntimeStorageExtensions { public static Task&lt;Stream&gt; OpenStreamForReadAsync(this IStorageFile file); public static Task&lt;Stream&gt; OpenStreamForWriteAsync(this IStorageFile file);</p><p>public static Task&lt;Stream&gt; OpenStreamForReadAsync(this IStorageFolder rootDirectory, String relativePath); public static Task&lt;Stream&gt; OpenStreamForWriteAsync(this IStorageFolder rootDirectory, String relativePath, CreationCollisionOption creationCollisionOption);</p><p>Here is an example that uses one of the extension methods to open a WinRT StorageFile and read its contents into a .NET Framework XElement object.</p><p>async Task&lt;XElement&gt; FromStorageFileToXElement(StorageFile file) { using (Stream stream = await file.OpenStreamForReadAsync()) {</p><p>return XElement.Load(stream); }</p><p>}</p><p>Finally, the System.IO.WindowsRuntimeStreamExtensions class offers extension methods that \u201ccast\u201d WinRT stream interfaces (such as IRandomAccessStream, IInputStream or IOutputStream) to the .NET Framework's Stream type and vice versa.</p><p>namespace System.IO { // Defined in System.Runtime.WindowsRuntime.dll</p><p>public static class WindowsRuntimeStreamExtensions { public static Stream AsStream(this IRandomAccessStream winRTStream); public static Stream AsStream(this IRandomAccessStream winRTStream, Int32 bufferSize);</p><p>public static Stream AsStreamForRead(this IInputStream winRTStream); public static Stream AsStreamForRead(this IInputStream winRTStream, Int32 bufferSize);</p><p>public static Stream AsStreamForWrite(this IOutputStream winRTStream); public static Stream AsStreamForWrite(this IOutputStream winRTStream, Int32 bufferSize);</p><p>} }</p><p>public static IInputStream AsInputStream (this Stream clrStream); public static IOutputStream AsOutputStream(this Stream clrStream);</p><p>Here is an example that uses one of the extension methods to \u201ccast\u201d a WinRT IInputStream to a</p><p>.NET Framework Stream object.</p><p>XElement FromWinRTStreamToXElement(IInputStream winRTStream) { Stream netStream = winRTStream.AsStreamForRead(); return XElement.Load(netStream);</p><p>}</p><p>Note that the \"casting\" extension methods provided by the .NET Framework do a little more than just casting under the covers. Specifically, the methods that adapt a WinRT stream to a .NET Framework stream implicitly create a buffer for the WinRT stream in the managed heap. As a result, most operations write to this buffer and do not need to cross the interop boundary, thereby improving performance. This is especially significant in scenarios that involve many small I/O operations, such as parsing an XML document.</p><p>One of the benefits of using the .NET Framework\u2019s stream projections is that if you use an AsStreamXxx method more than once on the same WinRT stream instance, you do not need to worry that different, disconnected buffers will be created, and data written to one buffer will not be visible in the other. The .NET Framework APIs ensure that every stream object has a unique adapter instance and all users share the same buffer.</p><p>Although in most cases the default buffering offers a good compromise between performance and memory usage, there are some cases where you may want to tweak the size of your buffer to be different from the 16-KB default. The AsStreamXxx methods offer overloads for that. For instance, if you know that you will be working with a very large file for an extended period of time, and that not many other buffered streams will be used at the same time, you can gain some additional performance by requesting a very large buffer for your stream. Conversely, in some network scenarios with low-latency requirements, you may want to ensure that no more bytes are read from the network than were explicitly requested by your application. In such cases, you can disable buffering altogether. If you specify a buffer size of zero bytes to the AsStreamXxx methods, no buffer object will be created.</p><h6>Passing Blocks of Data Between the CLR and WinRT</h6><p>When possible, you should use the stream framework projections as discussed in the previous section, because they have pretty good performance characteristics. However, there may be times when you need to pass raw blocks of data between the CLR and WinRT components. For example, WinRT\u2019s file and socket stream components require you to write and read raw blocks of data. Also, WinRT\u2019s cryptography components encrypt and decrypt blocks of data, and bitmap pixels are maintained in raw blocks of data too.</p><p>In the .NET Framework, the usual way to obtain a block of data is either with a byte array (Byte[]) or with a stream (such as when using the MemoryStream class). Of course, byte array and MemoryStream objects can\u2019t be passed to WinRT components directly. So, WinRT defines an IBuffer interface and objects that implement this interface represent raw blocks of data that can be passed to WinRT APIs. The WinRT IBuffer interface is defined as follows.</p><p>namespace Windows.Storage.Streams {</p><p>public interface IBuffer { UInt32 Capacity { get; } // Maximum size of the buffer (in bytes) UInt32 Length { get; set; } // Number of bytes currently in use by the buffer</p><p>} }</p><p>As you can see, an IBuffer object has a maximum size and length; oddly enough, this interface gives you no way to actually read from or write to the data in the buffer. The main reason for this is because WinRT types cannot express pointers in their metadata, because pointers do not map well to some languages (like JavaScript or safe C# code). So, an IBuffer object is really just a way to pass a memory address between the CLR and WinRT APIs. To access the bytes at the memory address, an internal COM interface, known as IBufferByteAccess, is used. Note that this interface is a COM interface (because it returns a pointer) and it is not a WinRT interface. The .NET Framework team has defined an internal RCW for this COM interface, which looks like the following.</p><p>namespace System.Runtime.InteropServices.WindowsRuntime { [Guid(\"905a0fef\u00adbc53\u00ad11df\u00ad8c49\u00ad001e4fc686da\")] [InterfaceType(ComInterfaceType.InterfaceIsIUnknown)] [ComImport] internal interface IBufferByteAccess {</p><p>unsafe Byte* Buffer { get; }</p><p>Internally, the CLR can take an IBuffer object, query for its IBufferByteAccess interface, and then query the Buffer property to get an unsafe pointer to the bytes contained within the buffer. With the pointer, the bytes can be accessed directly.</p><p>To avoid having developers write unsafe code that manipulates pointers, the Framework Class Library includes a WindowsRuntimeBufferExtensions class that defines a bunch of extension methods, which .NET Framework developers explicitly invoke to help pass blocks of data between CLR byte arrays and streams to WinRT IBuffer objects. To call these extension methods, make sure you add a using System.Runtime.InteropServices.WindowsRuntime; directive to your source code.</p><p>namespace System.Runtime.InteropServices.WindowsRuntime {</p><p>public static class WindowsRuntimeBufferExtensions { public static IBuffer AsBuffer(this Byte[] source); public static IBuffer AsBuffer(this Byte[] source, Int32 offset, Int32 length); public static IBuffer AsBuffer(this Byte[] source, Int32 offset, Int32 length, Int32</p><p>capacity);</p><p>public static IBuffer GetWindowsRuntimeBuffer(this MemoryStream stream); public static IBuffer GetWindowsRuntimeBuffer(this MemoryStream stream, Int32 position,</p><p>Int32 length);</p><p>} }</p><p>So, if you have a Byte[] and you want to pass it to a WinRT API requiring an IBuffer, you simply</p><p>call AsBuffer on the Byte[] array. This effectively wraps the reference to the Byte[] inside an object that implements the IBuffer interface; the contents of the Byte[] array is not copied, so this is very efficient. Similarly, if you have a MemoryStream object wrapping a publicly visible Byte[] array buffer in it, you simply call GetWindowsRuntimeBuffer on it to wrap the reference to the MemoryStream\u2019s buffer inside an object that implements the IBuffer interface. Again, the buffer\u2019s content is</p><p>not copied, so this is very efficient. The following method demonstrates both scenarios.</p><p>private async Task ByteArrayAndStreamToIBuffer(IRandomAccessStream winRTStream, Int32 count) { Byte[] bytes = new Byte[count]; await winRTStream.ReadAsync(bytes.AsBuffer(), (UInt32)bytes.Length, InputStreamOptions.None); Int32 sum = bytes.Sum(b =&gt; b); // Access the bytes read via the Byte[]</p><p>using (var ms = new MemoryStream()) using (var sw = new StreamWriter(ms)) {</p><p>sw.Write(\"This string represents data in a stream\"); sw.Flush(); UInt32 bytesWritten = await winRTStream.WriteAsync(ms.GetWindowsRuntimeBuffer());</p><p>} }</p><p>WinRT\u2019s IRandomAccessStream interface implements WinRT\u2019s IInputStream interface defined as follows.</p><p>namespace Windows.Storage.Streams { public interface IOutputStream : IDisposable {</p><p>IAsyncOperationWithProgress&lt;UInt32, UInt32&gt; WriteAsync(IBuffer buffer); }</p><p>}</p><p>When you call the AsBuffer or GetWindowsRuntimeBuffer extension methods in your code, these methods wrap the source object inside an object whose class implements the IBuffer interface. The CLR then creates a CCW for this object and passes it to the WinRT API. When the WinRT API queries the IBufferByteAccess interface\u2019s Buffer property to obtain a pointer to the underlying byte array, the byte array is pinned, and the address is returned to the WinRT API so it can access the data. The underlying byte array is unpinned when the WinRT API internally calls COM\u2019s Release method on the IBufferByteAccess interface.</p><p>If you call a WinRT API that returns an IBuffer back to you, then the data itself is probably in native memory, and you\u2019ll need a way to access this data from managed code. For this, we\u2019ll turn to some other extension methods defined by the WindowsRuntimeBufferExtensions class.</p><p>namespace System.Runtime.InteropServices.WindowsRuntime {</p><p>public static class WindowsRuntimeBufferExtensions { public static Stream AsStream(this IBuffer source); public static Byte[] ToArray(this IBuffer source); public static Byte[] ToArray(this IBuffer source, UInt32 sourceIndex, Int32 count);</p><p>} }</p><p>// Not shown: CopyTo method to transfer bytes between an IBuffer and a Byte[] // Not shown: GetByte, IsSameData methods</p><p>The AsStream method creates a Stream-derived object that wraps the source IBuffer. With this Stream object, you can access the data in the IBuffer by calling Stream\u2019s Read, Write, and similar methods. The ToArray method internally allocates a Byte[] and then copies all the bytes from the source IBuffer into the Byte[]; be aware that this extension method is potentially expensive in terms of memory consumption and CPU time.</p><p>The WindowsRuntimeBufferExtensions class also has several overloads of a CopyTo method that can copy bytes between an IBuffer and a Byte[]. It also has a GetByte method that retrieves a single byte at a time from an IBuffer and an IsSameData method that compares the contents of two IBuffer objects to see if their contents are identical. For most applications, it is unlikely that you will have a need to call any of these methods.</p><p>I\u2019d also like to point out that the .NET Framework defines a System.Runtime.InteropServices. WindowsRuntimeBuffer class that allows you to create an IBuffer object whose bytes are in the managed heap. Similarly, there is a WinRT component called Windows.Storage.Streams.Buffer that allows you to create an IBuffer object whose bytes are in the native heap. For most .NET Framework developers, there should be no need to use either of these classes explicitly in your code.</p><h6>Defining WinRT Components in C#</h6><p>So far in this chapter, I\u2019ve been focusing on how to consume WinRT components from C#. However, you can also define WinRT components in C# and then these components can be used by native C/C++, C#/Visual Basic, JavaScript, and potentially other languages too. Although this is possible to do, we need to think about the scenarios where this actually makes sense. For example, it makes no</p><p>sense at all to define a WinRT component in C# if the only consumers of the component are other managed languages that run on top of the CLR. This is because the WinRT type system has far fewer features, which make it much more restrictive than the CLR\u2019s type system.</p><p>I also don\u2019t think it makes a lot of sense to implement a WinRT component with C# that could be consumed by native C/C++ code. Developers using native C/C++ to implement their application are probably doing so because they are very concerned about performance and/or memory consumption. They are unlikely to want to take advantage of a WinRT component implemented with managed code, because this forces the CLR to load into their process and thus increases their memory requirements and performance due to the garbage collections and just-in-time compiling of code. For this reason, most WinRT components (like those that ship with Windows itself) are implemented in native code. Of course, there may be some parts of a native C++ app where performance is not so sensitive and, at these times, it may make sense to leverage .NET Framework functionality in order to improve productivity. For example, Bing Maps uses native C++ to draw its UI by using DirectX, but it also uses C# for its business logic.</p><p>So, it seems to me that the sweet spot for C#-implemented WinRT components is for Windows Store app developers who want to build their user interface with HTML and CSS and then use JavaScript as the glue code to tie the UI with business logic code that is implemented in a C# WinRT component. Another scenario would be to leverage existing Framework Class Library functionality (like Windows Communication Foundation) from an HTML/JavaScript app. Developers working with HTML and JavaScript are already willing to accept the kind of performance and memory consumption that comes with a browser engine and may be willing to even further accept the additional performance and memory consumption that comes along with also using the CLR.</p><p>To build a WinRT component with C#, you must first create a Microsoft Visual Studio \u201cWindows Runtime Component\u201d project. What this really does is create a normal class library project; however, the C# compiler will be spawned with the /t:winmdobj command line switch in order to produce a file with a .winmdobj file extension. With this switch specified, the compiler emits some IL code differently than it normally would. For example, WinRT components add and remove delegates to events differently than how the CLR does it, so the compiler emits different code for an event\u2019s add and remove methods when this compiler switch is specified. I\u2019ll show how to explicitly implement an event\u2019s add and remove methods later in this section.</p><p>After the compiler produces the .winmdobj file, the WinMD export utility (WinMDExp.exe) is spawned passing to it the .winmdobj, .pdb, and .xml (doc) files produced by the compiler. The WinMDExp.exe utility examines your file\u2019s metadata, ensuring that your types adhere to the various WinRT type system rules, as discussed at the beginning of this chapter. The utility also modifies the metadata contained in the .winmdobj file; it does not alter the IL code at all. Specifically, the utility maps any CLR types to the equivalent WinRT types. For example, references to the .NET Framework\u2019s IList&lt;String&gt; type are changed to WinRT\u2019s IVector&lt;String&gt; type. The output of the WinMDExp.exe utility is a .winmd file that other programming languages can consume.</p><p>You can use the .NET Framework\u2019s Intermediate Disassembler utility (ILDasm.exe) to inspect the contents of a .winmd file. By default, ILDasm.exe shows you the raw contents of the file. However,</p><p>ILDasm.exe supports a /project command-line switch that shows you what the metadata would look like after the CLR projected the WinRT types into their .NET Framework equivalents.</p><p>The following code demonstrates how to implement various WinRT components in C#. The components leverage many of the features discussed throughout this chapter, and there are a lot of comments throughout the code to explain what is going on. If you need to implement a WinRT component in C#, I\u2019d suggest using the code I show here as a model.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile216.png\"/><p>Important When managed code consumes a WinRT component also written in managed code, the CLR treats the WinRT component as if it were a regular managed component. That is, the CLR will not create CCWs and RCWs, and therefore, it will not invoke the WinRT APIs via these wrappers. This improves performance greatly. However, when testing the component, APIs are not being invoked the way they would be if called from another language (like native C/C++ or JavaScript). So, aside from the performance and memory overhead not being real-life, managed code can pass null to a WinRT API requiring a String without an ArgumentNullException being thrown. And, WinRT APIs implemented in managed code can manipulate arrays that are passed in, and the caller will see the changed array contents when the API returns; normally, the WinRT type system forbids modifying arrays passed into an API. These are just some of the differences that you\u2019ll be able to observe, so beware.</p><p>/****************************************************************************** Module: WinRTComponents.cs Notices: Copyright (c) 2012 by Jeffrey Richter</p><p>******************************************************************************/</p><p>using System; using System.Collections.Generic; using System.Linq; using System.Runtime.InteropServices.WindowsRuntime; using System.Threading; using System.Threading.Tasks; using Windows.Foundation; using Windows.Foundation.Metadata;</p><p>// The namespace MUST match the assembly name and cannot be \"Windows\" namespace Wintellect.WinRTComponents {</p><p>// [Flags] // Must not be present if enum is int; required if enum is uint public enum WinRTEnum : int { // Enums must be backed by int or uint</p><p>None, NotNone</p><p>}</p><p>// Structures can only contain core data types, String, &amp; other structures // No constructors or methods are allowed public struct WinRTStruct { public Int32 ANumber; public String AString;</p><p>public WinRTEnum AEnum; // Really just a 32\u00adbit integer }</p><p>// Delegates must have WinRT\u00adcompatible types in the signature (no BeginInvoke/EndInvoke) public delegate String WinRTDelegate(Int32 x);</p><p>// Interfaces can have methods, properties, &amp; events but cannot be generic. public interface IWinRTInterface {</p><p>// Nullable&lt;T&gt; marshals as IReference&lt;T&gt; Int32? InterfaceProperty { get; set; }</p><p>}</p><p>// Members without a [Version(#)] attribute default to the class's // version (1) and are part of the same underlying COM interface // produced by WinMDExp.exe. [Version(1)] // Class must be derived from Object, sealed, not generic, // implement only WinRT interfaces, &amp; public members must be WinRT types public sealed class WinRTClass : IWinRTInterface {</p><p>// Public fields are not allowed</p><p>#region Class can expose static methods, properties, and events public static String StaticMethod(String s) { return \"Returning \" + s; } public static WinRTStruct StaticProperty { get; set; }</p><p>// In JavaScript 'out' parameters are returned as objects with each // parameter becoming a property along with the return value public static String OutParameters(out WinRTStruct x, out Int32 year) {</p><p>x = new WinRTStruct { AEnum = WinRTEnum.NotNone, ANumber = 333, AString = \"Jeff\" }; year = DateTimeOffset.Now.Year; return \"Grant\";</p><p>} #endregion</p><p>// Constructor can take arguments but not out/ref arguments public WinRTClass(Int32? number) { InterfaceProperty = number; }</p><p>public Int32? InterfaceProperty { get; set; }</p><p>// Only ToString is allowed to be overridden public override String ToString() {</p><p>return String.Format(\"InterfaceProperty={0}\",</p><p>InterfaceProperty.HasValue ? InterfaceProperty.Value.ToString() : \"(not set)\"); }</p><p>public void ThrowingMethod() { throw new InvalidOperationException(\"My exception message\");</p><p>}</p><p>// To throw a specific HRESULT, use COMException instead //const Int32 COR_E_INVALIDOPERATION = unchecked((Int32)0x80131509); //throw new COMException(\"Invalid Operation\", COR_E_INVALIDOPERATION);</p><p>#region Arrays are passed, returned OR filled; never a combination public Int32 PassArray([ReadOnlyArray] /* [In] implied */ Int32[] data) {</p><p>}</p><p>// NOTE: Modified array contents MAY not be marshaled out; do not modify the array return data.Sum();</p><p>public Int32 FillArray([WriteOnlyArray] /* [Out] implied */ Int32[] data) { // NOTE: Original array contents MAY not be marshaled in; // write to the array before reading from it for (Int32 n = 0; n &lt; data.Length; n++) data[n] = n; return data.Length;</p><p>}</p><p>public Int32[] ReturnArray() { // Array is marshaled out upon return return new Int32[] { 1, 2, 3 };</p><p>} #endregion</p><p>// Collections are passed by reference public void PassAndModifyCollection(IDictionary&lt;String, Object&gt; collection) {</p><p>collection[\"Key2\"] = \"Value2\"; // Modifies collection in place via interop }</p><p>#region Method overloading // Overloads with same # of parameters are considered identical to JavaScript public void SomeMethod(Int32 x) { }</p><p>[Windows.Foundation.Metadata.DefaultOverload] // Makes this method the default overload public void SomeMethod(String s) { } #endregion</p><p>#region Automatically implemented event public event WinRTDelegate AutoEvent;</p><p>public String RaiseAutoEvent(Int32 number) { WinRTDelegate d = AutoEvent; return (d == null) ? \"No callbacks registered\" : d(number);</p><p>} #endregion</p><p>#region Manually implemented event // Private field that keeps track of the event's registered delegates private EventRegistrationTokenTable&lt;WinRTDelegate&gt; m_manualEvent = null;</p><p>// Manual implementation of the event's add and remove methods public event WinRTDelegate ManualEvent {</p><p>add { // Gets the existing table, or creates a new one if the table is not yet initialized return EventRegistrationTokenTable&lt;WinRTDelegate&gt;</p><p>.GetOrCreateEventRegistrationTokenTable(ref m_manualEvent)</p><p>.AddEventHandler(value); }</p><p>}</p><p>remove { EventRegistrationTokenTable&lt;WinRTDelegate&gt;</p><p>.GetOrCreateEventRegistrationTokenTable(ref m_manualEvent)</p><p>.RemoveEventHandler(value); }</p><p>public String RaiseManualEvent(Int32 number) { WinRTDelegate d = EventRegistrationTokenTable&lt;WinRTDelegate&gt;</p><p>.GetOrCreateEventRegistrationTokenTable(ref m_manualEvent).InvocationList; return (d == null) ? \"No callbacks registered\" : d(number);</p><p>} #endregion</p><p>#region Asynchronous methods // Async methods MUST return IAsync[Action|Operation](WithProgress) // NOTE: Other languages see the DataTimeOffset as Windows.Foundation.DateTime public IAsyncOperationWithProgress&lt;DateTimeOffset, Int32&gt; DoSomethingAsync() {</p><p>// Use the System.Runtime.InteropServices.WindowsRuntime.AsyncInfo's Run methods to // invoke a private method written entirely in managed code return AsyncInfo.Run&lt;DateTimeOffset, Int32&gt;(DoSomethingAsyncInternal);</p><p>}</p><p>// Implement the async operation via a private method using normal .NET technologies private async Task&lt;DateTimeOffset&gt; DoSomethingAsyncInternal(</p><p>CancellationToken ct, IProgress&lt;Int32&gt; progress) {</p><p>}</p><p>for (Int32 x = 0; x &lt; 10; x++) { // This code supports cancellation and progress reporting ct.ThrowIfCancellationRequested(); if (progress != null) progress.Report(x * 10); await Task.Delay(1000); // Simulate doing something asynchronously</p><p>} return DateTimeOffset.Now; // Ultimate return value</p><p>public IAsyncOperation&lt;DateTimeOffset&gt; DoSomethingAsync2() { // If you don\u2019t need cancellation &amp; progress, use // System.WindowsRuntimeSystemExtensions\u2019 AsAsync[Action|Operation] Task // extension methods (these call AsyncInfo.Run internally) return DoSomethingAsyncInternal(default(CancellationToken), null).AsAsyncOperation();</p><p>} #endregion</p><p>} }</p><p>// After you ship a version, mark new members with a [Version(#)] attribute // so that WinMDExp.exe puts the new members in a different underlying COM // interface. This is required since COM interfaces are supposed to be immutable. [Version(2)] public void NewMethodAddedInV2() {}</p><p>The following JavaScript code demonstrates how to access all of the previous WinRT components and features.</p><p>function () { // Make accessing the namespace more convenient in the code var WinRTComps = Wintellect.WinRTComponents;</p><p>// NOTE: The JavaScript VM projects WinRT APIs via camel casing</p><p>// Access WinRT type's static method &amp; property var s = WinRTComps.WinRTClass.staticMethod(null); // NOTE: JavaScript pass \"null\" here! var struct = { anumber: 123, astring: \"Jeff\", aenum: WinRTComps.WinRTEnum.notNone }; WinRTComps.WinRTClass.staticProperty = struct; s = WinRTComps.WinRTClass.staticProperty; // Read it back</p><p>// If the method has out parameters, they and the return value // are returned as an object\u2019s properties var s = WinRTComps.WinRTClass.outParameters(); var name = s.value; // Return value var struct = s.x; // an 'out' parameter var year = s.year; // another 'out' parameter</p><p>// Construct an instance of the WinRT component var winRTClass = new WinRTComps.WinRTClass(null); s = winRTClass.toString(); // Call ToString()</p><p>// Demonstrate throw and catch try { winRTClass.throwingMethod(); } catch (err) { }</p><p>// Array passing var a = [1, 2, 3, 4, 5]; var sum = winRTClass.passArray(a);</p><p>// Array filling var arrayOut = [7, 7, 7]; // NOTE: fillArray sees all zeros! var length = winRTClass.fillArray(arrayOut); // On return, arrayOut = [0, 1, 2]</p><p>// Array returning a = winRTClass.returnArray(); // a = [ 1, 2, 3]</p><p>// Pass a collection and have its elements modified var localSettings = Windows.Storage.ApplicationData.current.localSettings; localSettings.values[\"Key1\"] = \"Value1\"; winRTClass.passAndModifyCollection(localSettings.values); // On return, localSettings.values has 2 key/value pairs in it</p><p>// Call overloaded method winRTClass.someMethod(5); // Actually calls SomeMethod(String) passing \"5\"</p><p>// Consume the automatically implemented event var f = function (v) { return v.target; }; winRTClass.addEventListener(\"autoevent\", f, false); s = winRTClass.raiseAutoEvent(7);</p><p>// Consume the manually implemented event winRTClass.addEventListener(\"manualevent\", f, false); s = winRTClass.raiseManualEvent(8);</p><p>// Invoke asynchronous method supporting progress, cancelation, &amp; error handling var promise = winRTClass.doSomethingAsync(); promise.then(</p><p>function (result) { console.log(\"Async op complete: \" + result); }, function (error) { console.log(\"Async op error: \" + error); }, function (progress) {</p><p>console.log(\"Async op progress: \" + progress); //if (progress == 30) promise.cancel(); // To test cancelation</p><p>}); }</p><h6>PART V</h6>"}, {"id": "lesson-4", "title": "Threading", "readTime": "5 min read", "subtitle": "", "content": "<ul style=\"list-style-type: none;\">\n<li><p>CHAPTER 26 Thread Basics . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . .669</p></li>\n<li><p>CHAPTER 27 Compute-Bound Asynchronous Operations. . . . .691</p></li>\n<li><p>CHAPTER 28 I/O-Bound Asynchronous Operations . . . . . . . . . .727</p></li>\n<li><p>CHAPTER 29 Primitive Thread Synchronization Constructs. . . .757</p></li>\n<li><p>CHAPTER 30 Hybrid Thread Synchronization Constructs . . . . .789</p></li>\n</ul><h6>C H A P T E R 2 6</h6><h2>Thread Basics</h2><h6>In this chapter:</h6><p>Why Does Windows Support Threads? . . . . . . . . . . . . . . . . . . . . . 669 Thread Overhead . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 670 Stop the Madness. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 674 CPU Trends. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 677 CLR Threads and Windows Threads. . . . . . . . . . . . . . . . . . . . . . . . 678 Using a Dedicated Thread to Perform an Asynchronous Compute-Bound Operation. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 678 Reasons to Use Threads. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 681 Thread Scheduling and Priorities . . . . . . . . . . . . . . . . . . . . . . . . . . 683 Foreground Threads vs. Background Threads. . . . . . . . . . . . . . . 688 What Now?. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 689</p><p>In this chapter, I introduce the basic concepts concerning threads, and I offer a way for developers to conceptualize about them and their use. I\u2019ll explain why Windows introduced the concept of threads, CPU trends, the relationship between common language runtime (CLR) threads and Windows threads, the overhead associated with using threads, how Windows schedules threads, the Microsoft .NET Framework classes that expose thread properties, and much more.</p><p>The chapters in Part V, \u201cThreading,\u201d of this book explain how Windows and the CLR work together to provide a threading architecture. It is my hope that after reading these chapters, you will take away a foundation of knowledge that will allow you to effectively use threads to design and build responsive, reliable, and scalable applications and components.</p><h6>Why Does Windows Support Threads?</h6><p>Back in the early days of computers, operating systems didn\u2019t offer the concept of a thread. In effect, there was just one thread of execution that ran throughout the entire system, which included both operating system code and application code. The problem with having only one thread of execution was that a long-running task would prevent other tasks from executing. For example, in the days of 16-bit Windows, it was very common for an application that was printing a document to stall the entire machine, causing the operating system and all other applications to stop responding. And, sometimes applications would have a bug in them, resulting in an infinite loop that also stopped the entire machine from operating.</p><p>At this point, the end user would have no choice but to reboot the computer by pressing the reset button or power switch. Of course, end users hated doing this (they still do, in fact) because all running applications terminated; more importantly, any data that these applications were processing was thrown out of memory and lost. Microsoft knew that 16-bit Windows would not be a good enough operating system to keep Microsoft relevant as the computer industry progressed, so they set out to build a new operating system to address the needs of corporations and individuals. This new operating system had to be robust, reliable, scalable, and secure, and it had to improve the many deficiencies of 16-bit Windows. This operating system kernel originally shipped in Windows NT. Over the years, this kernel has had many tweaks and features added to it. The latest version of this kernel ships in the latest versions of the Microsoft client and server Windows operating systems.</p><p>When Microsoft was designing this operating system kernel, they decided to run each instance of an application in what is called a process. A process is just a collection of resources that is used by a single instance of an application. Each process is given a virtual address space, ensuring that the code and data used by one process is not accessible to another process. This makes application instances robust because one process cannot corrupt code or data being used by another. In addition, the operating system\u2019s kernel code and data are not accessible to processes; therefore, it\u2019s not possible for application code to corrupt operating system code or data. So now, application code cannot corrupt other applications or the operating system itself, and the whole computing experience is much better for end users. In addition, the system is more secure because application code cannot access user names, passwords, credit card information, or other sensitive information that is in use by another application or the operating system itself.</p><p>This is all well and good, but what about the CPU itself? What if an application enters an infinite loop? Well, if there is only one CPU in the machine, then it executes the infinite loop and cannot execute anything else, so although the data cannot be corrupted and is more secure, the system could still stop responding to the end user. Microsoft needed to fix this problem, too, and threads were the answer. A thread is a Windows concept whose job is to virtualize the CPU. Windows gives each process its very own thread (which functions similar to a CPU), and if application code enters an infinite loop, the process associated with that code freezes up, but other processes (which have their own threads) are not frozen; they keep running!</p><h6>Thread Overhead</h6><p>Threads are awesome because they enable Windows to be responsive even when applications are executing long-running tasks. Also, threads allow the user to use one application (like Task Manager) to forcibly kill an application that appears frozen because it is executing a long-running task. But as with every virtualization mechanism, threads have space (memory consumption) and time (runtime execution performance) overhead associated with them.</p><p>Let\u2019s explore this overhead in more detail now. Every thread has one of each of the following:</p><p>\u25a0\u25a0 Thread kernel object The operating system allocates and initializes one of these data structures for each thread created in the system. The data structure contains a bunch of properties (discussed later in this chapter) that describe the thread. This data structure also contains what is called the thread\u2019s context. The context is a block of memory that contains a set of the CPU\u2019s registers. For the x86, x64, and ARM CPU architectures, the thread\u2019s context uses approximately 700, 1,240, or 350 bytes of memory, respectively.</p><p>\u25a0\u25a0 Thread environment block (TEB) The TEB is a block of memory allocated and initialized in user mode (address space that application code can quickly access). The TEB consumes 1 page of memory (4 KB on x86, x64 CPUs, and ARM CPUs). The TEB contains the head of the thread\u2019s exception-handling chain. Each try block that the thread enters inserts a node in the head of this chain; the node is removed from the chain when the thread exits the try block. In addition, the TEB contains the thread\u2019s thread-local storage data and some data structures for use by Graphics Device Interface (GDI) and OpenGL graphics.</p><p>\u25a0\u25a0 User-mode stack The user-mode stack is used for local variables and arguments passed to methods. It also contains the address indicating what the thread should execute next when the current method returns. By default, Windows allocates 1 MB of memory for each thread\u2019s user-mode stack. More specifically, Windows reserves the 1 MB of address space and sparsely commits physical storage to it as the thread actually requires it when growing the stack.</p><p>\u25a0\u25a0 Kernel-mode stack The kernel-mode stack is also used when application code passes arguments to a kernel-mode function in the operating system. For security reasons, Windows copies any arguments passed from user-mode code to the kernel from the thread\u2019s user-mode stack to the thread\u2019s kernel-mode stack. Once copied, the kernel can verify the arguments\u2019 values, and because the application code can\u2019t access the kernel-mode stack, the application can\u2019t modify the arguments\u2019 values after they have been validated and the operating system kernel code begins to operate on them. In addition, the kernel calls methods within itself and uses the kernel-mode stack to pass its own arguments, to store a function\u2019s local variables, and to store return addresses. The kernel-mode stack is 12 KB when running on a 32-bit Windows system and 24 KB when running on a 64-bit Windows system.</p><p>\u25a0\u25a0 DLL thread-attach and thread-detach notifications Windows has a policy that whenever a thread is created in a process, all unmanaged DLLs loaded in that process have their DllMain method called, passing a DLL_THREAD_ATTACH flag. Similarly, whenever a thread dies, all DLLs in the process have their DllMain method called, passing it a DLL_THREAD_DETACH flag. Some DLLs need these notifications to perform some special initialization or cleanup for each thread created/destroyed in the process. For example, the C-Runtime library DLL allocates some thread-local storage state that is required should the thread use functions contained within the C-Runtime library.</p><p>In the early days of Windows, many processes had maybe 5 or 6 DLLs loaded into them, but today, some processes have several hundred DLLs loaded into them. Right now, on my machine, Microsoft Visual Studio has about 470 DLLs loaded into its process address space! This means that whenever a new thread is created in Visual Studio, 470 DLL functions must get called before the thread is allowed to do what it was created to do. And these 470 functions must be called again whenever a thread in Visual Studio dies. Wow\u2014this can seriously affect the performance of creating and destroying threads within a process.1</p><p>So now, you see all the space and time overhead that is associated with creating a thread, letting it sit around in the system, and destroying it. But the situation gets even worse\u2014now we\u2019re going to start talking about context switching. A computer with only one CPU in it can do only one thing at a time. Therefore, Windows has to share the actual CPU hardware among all the threads (logical CPUs) that are sitting around in the system.</p><p>At any given moment in time, Windows assigns one thread to a CPU. That thread is allowed to run for a time-slice (sometimes referred to as a quantum). When the time-slice expires, Windows context switches to another thread. Every context switch requires that Windows performs the following actions:</p><ul style=\"list-style-type: none;\">\n<li><p>1. Save the values in the CPU\u2019s registers to the currently running thread\u2019s context structure inside the thread\u2019s kernel object.</p></li>\n<li><p>2. Select one thread from the set of existing threads to schedule next. If this thread is owned by another process, then Windows must also switch the virtual address space seen by the CPU before it starts executing any code or touching any data.</p></li>\n<li><p>3. Load the values in the selected thread\u2019s context structure into the CPU\u2019s registers.</p></li>\n</ul><p>After the context switch is complete, the CPU executes the selected thread until its time-slice expires, and then another context switch happens again. Windows performs context switches about every 30 ms. Context switches are pure overhead; that is, there is no memory or performance benefit that comes from context switches. Windows performs context switching to provide end users with a robust and responsive operating system.</p><p>Now, if an application\u2019s thread enters into an infinite loop, Windows will periodically preempt that thread, assign a different thread to an actual CPU, and let this other thread run for a while. This other thread could be Task Manager\u2019s thread and now, the end user can use Task Manager to kill the process containing the thread that is in an infinite loop. When doing this, the process dies and all the data it was working on is destroyed, too, but all other processes in the system continue to run just fine without losing their data. Of course, the user doesn\u2019t have to reset the machine and reboot, so context switches are required to provide end users with a much better overall experience at the cost of performance.</p><p>1 DLLs produced by C# and most other managed programming languages do not have a DllMain in them at all and so managed DLLs will not receive the DLL_THREAD_ATTACH and DLL_THREAD_DETACH notifications improving performance. In addition, unmanaged DLLs can opt out of these notifications by calling the Win32 DisableThreadLibraryCalls function. Unfortunately, many unmanaged developers are not aware of this function, so they don\u2019t call it.</p><p>In fact, the performance hit is much worse than you might think. Yes, a performance hit occurs when Windows context switches to another thread. But the CPU was executing another thread, and the previously running thread\u2019s code and data reside in the CPU\u2019s caches so that the CPU doesn\u2019t have to access RAM memory as much, which has significant latency associated with it. When Windows context switches to a new thread, this new thread is most likely executing different code and accessing different data that is not in the CPU\u2019s cache. The CPU must access RAM memory to populate its cache so it can get back to a good execution speed. But then, about 30 ms later, another context switch occurs.</p><p>The time required to perform a context switch varies with different CPU architectures and speed. And the time required to build up a CPU\u2019s cache depends on what applications are running in the system, the size of the CPU\u2019s caches, and various other factors. So it is impossible for me to give you an absolute figure or even an estimate as to what time overhead is incurred for each context switch. Suffice it to say that you want to avoid using context switches as much as possible if you are interested in building high-performing applications and components.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile217.png\"/><p>Important At the end of a time-slice, if Windows decides to schedule the same thread again (rather than switching to another thread), then Windows does not perform a context switch. Instead, the thread is allowed to just continue running. This improves performance significantly, and avoiding context switches is something you want to achieve as often as possible when you design your code.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile218.png\"/><p>Important A thread can voluntarily end its time-slice early, which happens quite frequently. Threads typically wait for I/O operations (keyboard, mouse, file, network, etc.) to complete. For example, Notepad\u2019s thread usually sits idle with nothing to do; this thread is waiting for input. If the user presses the J key on the keyboard, Windows wakes Notepad\u2019s thread to have it process the J keystroke. It may take Notepad\u2019s thread just 5 ms to process the key, and then it calls a Win32 function that tells Windows that it is ready to process the next input event. If there are no more input events, then Windows puts Notepad\u2019s thread into a wait state (relinquishing the remainder of its time-slice) so that the thread is not scheduled on any CPU until the next input stimulus occurs. This improves overall system performance because threads that are waiting for I/O operations to complete are not scheduled on a CPU and do not waste CPU time; other threads can be scheduled on the CPU instead.</p><p>In addition, when performing a garbage collection, the CLR must suspend all the threads, walk their stacks to find the roots to mark objects in the heap, walk their stacks again (updating roots to objects that moved during compaction), and then resume all the threads. So avoiding threads will greatly improve the performance of the garbage collector, too. And whenever you are using a debugger, Windows suspends all threads in the application being debugged every time a breakpoint is hit and resumes all the threads when you single-step or run the application. So the more threads you have, the slower your debugging experience will be.</p><p>From this discussion, you should conclude that you must avoid using threads as much as possible because they consume a lot of memory and they require time to create, destroy, and manage. Time is also wasted when Windows context switches between threads and when garbage collections occur. However, this discussion should also help you realize that threads must be used sometimes because they allow Windows to be robust and responsive.</p><p>I should also point out that a computer with multiple CPUs in it can actually run multiple threads simultaneously, increasing scalability (the ability to do more work in less time). Windows will assign one thread to each CPU core, and each core will perform its own context switching to other threads. Windows makes sure that a single thread is not scheduled on multiple cores at one time because this would wreak havoc. Today, computers that contain multiple CPUs, hyperthreaded CPUs, or multi-core CPUs are commonplace. But when Windows was originally designed, single-CPU computers were commonplace, and Windows added threads to improve system responsiveness and reliability. Today, threads are also being used to improve scalability, which can happen only on computers that have multiple cores in them.</p><p>The remaining chapters in this book discuss the various Windows and CLR mechanisms that exist so that you can effectively wrestle with the tension of creating as few threads as possible, while still keeping your code responsive and allowing it to scale if your code is running on a machine with multiple cores.</p><h6>Stop the Madness</h6><p>If all we cared about was raw performance, then the optimum number of threads to have on any machine is identical to the number of CPUs on that machine. So a machine with one CPU would have only one thread, a machine with two CPUs would have two threads, and so on. The reason is obvious: if you have more threads than CPUs, then context switching is introduced and performance deteriorates. If each CPU has just one thread, then no context switching exists and the threads run at full speed.</p><p>However, Microsoft designed Windows to favor reliability and responsiveness as opposed to favoring raw speed and performance. And I commend this decision: I don\u2019t think any of us would be using Windows or the .NET Framework today if applications could still stop the operating system and other applications. Therefore, Windows gives each process its own thread for improved system reliability and responsiveness. On my machine, for example, when I run Task Manager and select the Performance tab, I see the image shown in Figure 26-1.</p><p>It shows that my machine currently has 55 processes running on it, and so we\u2019d expect that there were at least 55 threads on my machine, because each process gets at least 1 thread. But Task Manager also shows that my machine currently has 864 threads in it! All these threads end up allocating many megabytes of memory on my machine, which has only 4 GB of RAM in it. This also means that there is an average of approximately 15.7 threads per process, when I should ideally have only 2 threads per process on my dual core machine!</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile219.png\"/><figcaption>Figure 26-1 Task Manager showing system performance.</figcaption><p>To make matters worse, when I look at the CPU Usage, it shows that my CPU is busy 5 percent of the time. This means that 95 percent of the time, these 864 threads have literally nothing to do\u2014they are just soaking up memory that is definitely not being used when the threads are not running. You have to ask yourself: Do these applications need all these threads to do nothing 95 percent of the time? The answer to this question has to be \u201cNo.\u201d Now, if you want to see which processes are the most wasteful, click the Task Manager\u2019s Details tab, add the Threads column, and sort this column in descending order, as shown in Figure 26-2.2</p><p>As you can see here, System has created 105 threads and is using 1 percent of the CPU, Explorer has created 47 threads to use 0 percent of the CPU, Visual Studio (Devenv.exe) has created 36 threads to use 0 percent of the CPU, Microsoft Outlook has created 24 threads to use 0 percent of the CPU, and so on. What is going on here?</p><p>When developers were learning about Windows, they learned that a process in Windows is very, very expensive. Creating a process usually takes several seconds, a lot of memory must be allocated, this memory must be initialized, the EXE and DLL files have to load from disk, and so on. By comparison, creating a thread in Windows is very cheap, so developers decided to stop creating processes and start creating threads instead. So now we have lots of threads. But even though threads are cheaper than processes, they are still very expensive compared to most other system resources, so they should be used sparingly and appropriately.</p><p>2 You add the column by right-clicking an existing column and selecting Select Columns.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile220.png\"/><figcaption>FIGURE 26-2 Task Manager showing details.</figcaption><p>Well, without a doubt, we can say for sure that all of the applications we\u2019ve just discussed are using threads inefficiently. There is just no way that all of these threads need to exist in the system. It is one thing to allocate resources inside an application; it\u2019s quite another to allocate them and then not use them. This is just wasteful, and allocating all the memory for thread stacks means that there is less memory for more important data, such as a user\u2019s document.3</p><p>To make matters worse, what if these were the processes running in a single user\u2019s Remote Desktop Services session\u2014and what if there were actually 100 users on this machine? Then there would be 100 instances of Outlook, all creating 24 threads only to do nothing with them. That\u2019s 2,400 threads each with its own kernel object, TEB, user-mode stack, kernel-mode stack, etc. That is a lot of wasted resources. This madness has to stop, especially if Microsoft wants to give users a good experience when running Windows on netbook computers, many of which have only 1 GB of RAM. Again, the chapters in this part of the book will describe how to properly design an application to use very few threads in an efficient manner.</p><p>3 I just can\u2019t resist sharing with you another demonstration of how bad this situation is. Try this: open Notepad.exe and use Task Manager to see how many threads are in it (you should see 1). Then select Notepad\u2019s File Open menu item to display the common File Open dialog box. After the dialog box opens, look at Task Manager to see how many new threads just got created. On my machine, 31 additional threads are created just by displaying this dialog box! In fact, every application that uses the common File Open or File Save dialog box will get many additional threads created inside it that sit idle most of the time. A lot of these threads aren\u2019t even destroyed when the dialog box is closed!</p><h6>CPU Trends</h6><p>In the past, CPU speeds used to increase with time, so an application that ran slowly on one machine would typically run faster on a newer machine. However, CPU manufacturers are unable to continue the trend of making CPUs faster. When you run CPUs at high speeds, they produce a lot of heat that has to be dissipated. A few years ago, I acquired a newly released notebook computer from a respected manufacturer. This computer had a bug in its firmware that made it not turn the fan on enough; as a result, after running the computer for a while, the CPU and the motherboard melted. The hardware manufacturer replaced the machine and then \u201cimproved\u201d the firmware by making the fan run more frequently. Unfortunately, this had the effect of draining the battery faster, because fans consume a lot of power.</p><p>These are the kinds of problems that the hardware vendors face today. Because CPU manufacturers can\u2019t continuously produce higher-speed CPUs, they have instead turned their attention to making transistors smaller so that more of them can reside on a single chip. Today, we can have a single silicon chip that contains two or more CPU cores. The result is that our software only gets faster if we write our software to use the multiple cores. How do we do this? We use threads in an intelligent fashion.</p><p>Computers use three kinds of multi-CPU technologies today:</p><p>\u25a0\u25a0 Multiple CPUs Some computers just have multiple CPUs in them. That is, the motherboard has multiple sockets on it, with each socket containing a CPU. Because the motherboard must be bigger, the computer case is bigger as well, and sometimes these machines have multiple power supplies in them due to the additional power drain. These kinds of computers have been around for a few decades, but they are not as popular today due to their increased size and cost.</p><p>\u25a0\u25a0 Hyperthreaded chips This technology (owned by Intel) allows a single chip to look like two chips. The chip contains two sets of architectural states, such as CPU registers, but the chip has only one set of execution resources. To Windows, this looks like there are two CPUs in the machine, so Windows schedules two threads concurrently. However, the chip only executes one of the threads at a time. When one thread pauses due to a cache miss, branch misprediction, or data dependency, the chip switches to the other thread. This all happens in hardware, and Windows doesn\u2019t know that it is happening; Windows believes that both threads are running concurrently. Windows does know about hyperthreaded CPUs, and if you have multiple hyperthreaded CPUs in a single machine, Windows will first schedule one thread on each CPU so that the threads are truly running concurrently and then schedule other threads on the already-busy CPUs. Intel claims that a hyperthreaded CPU can improve performance by 10 percent to 30 percent.</p><p>\u25a0\u25a0 Multi-core chips A few years ago, single chips containing multiple CPU cores have entered the scene. As I write this, chips with two, three, and four cores are readily available. Even my notebook computer has two cores in it; our mobile phones now have multiple cores in them too. Intel has even been working on a single chip with 80 cores on it! Wow, this is a lot of computing power! And Intel even has hyperthreaded multi-core chips.</p><h6>CLR Threads and Windows Threads</h6><p>Today, the CLR uses the threading capabilities of Windows, so Part V of this book is really focusing on how the threading capabilities of Windows are exposed to developers who write code by using the CLR. I will explain about how threads in Windows work and how the CLR alters the behavior (if it does). However, if you\u2019d like more information about threads, I recommend reading some of my earlier writings on the topic, such as Windows via C/C++, Fifth Edition, by myself and Christophe Nasarre (Microsoft Press, 2007).</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile221.png\"/><p>Note Back in the early days of the .NET Framework, the CLR team felt that they would someday have the CLR offer logical threads, which did not necessarily map to Windows threads. However, around 2005, this was attempted unsuccessfully, causing the CLR team to give up on the idea. So today, a CLR thread is identical to a Windows thread. However, in the .NET Framework, you see remnants of the attempt. For example, the System. Environment class exposes a CurrentManagedThreadId property, which returns the CLR\u2019s ID for a thread, although the System.Diagnostics.ProcessThread class exposes an Id property that returns Windows\u2019s ID for the same thread. The System.Thread class\u2019s BeginThreadAffintiy and EndThreadAffinity methods were also introduced to deal with the notion that a CLR thread might not map to a Windows thread.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile222.png\"/><p>Note For Windows Store apps, Microsoft has removed some APIs related to threading, because the API encouraged bad programming practices (as discussed in the \u201cStop the Madness\u201d section of this chapter), or because the APIs don't help achieve the goals that Microsoft has set out for Windows Store apps. For example, the whole System.Thread class is not available to Windows Store apps because it has many bad APIs (such as Start, IsBackground, Sleep, Suspend, Resume, Join, Interrupt, Abort, BeginThreadAffinity, and EndThreadAffinity). Personally, I think this is great, and I wish it had happened a long time ago. So, in Chapters 26 to 30, I do discuss some APIs and features that are available for desktop apps but are not available for Windows Store apps. As you read these chapters, you should be able to easily discern why certain APIs are unavailable for Windows Store apps.</p><h6>Using a Dedicated Thread to Perform an Asynchronous Compute-Bound Operation</h6><p>In this section, I will show you how to create a thread and have it perform an asynchronous computebound operation. Although I am going to walk you through this, I highly recommend that you avoid the technique I show you here. And, in fact, this technique is not even possible if you are building a Windows Store app because the Thread class is not available. Instead, you should use the thread pool</p><p>to execute asynchronous compute-bound operations whenever possible. I go into the details about doing this in Chapter 27, \u201cCompute-Bound Asynchronous Operations.\u201d</p><p>However, there are some very unusual occasions when you might want to explicitly create a thread dedicated to executing a particular compute-bound operation. Typically, you\u2019d want to create a dedicated thread if you\u2019re going to execute code that requires the thread to be in a particular state that is not normal for a thread pool thread. For example, explicitly create your own thread if any of the following is true:</p><p>\u25a0\u25a0 You need the thread to run with a non-normal thread priority. All thread pool threads run at normal priority. Although you can change this, it is not recommended, and the priority change does not persist across thread pool operations.</p><p>\u25a0\u25a0 You need the thread to behave as a foreground thread, thereby preventing the application from dying until the thread has completed its task. For more information, see the \u201dForeground Threads vs. Background Threads\u201d section later in this chapter. Thread pool threads are always background threads, and they may not complete their task if the CLR wants to terminate the process.</p><p>\u25a0\u25a0 The compute-bound task is extremely long-running; this way, I would not be taxing the thread</p><p>pool\u2019s logic as it tries to figure out whether to create an additional thread.</p><p>\u25a0\u25a0 You want to start a thread and possibly abort it prematurely by calling Thread\u2019s Abort</p><p>method (discussed in Chapter 22, \u201cCLR Hosting and AppDomains\u201d).</p><p>To create a dedicated thread, you construct an instance of the System.Threading.Thread class, passing the name of a method into its constructor. Here is the prototype of Thread\u2019s constructor.</p><p>public sealed class Thread : CriticalFinalizerObject, ... { public Thread(ParameterizedThreadStart start); // Less commonly used constructors are not shown here</p><p>}</p><p>The start parameter identifies the method that the dedicated thread will execute, and this method must match the signature of the ParameterizedThreadStart delegate.4</p><p>delegate void ParameterizedThreadStart(Object obj);</p><p>Constructing a Thread object is a relatively lightweight operation because it does not actually create a physical operating system thread. To actually create the operating system thread and have it start executing the callback method, you must call Thread\u2019s Start method, passing into it the object (state) that you want passed as the callback method\u2019s argument.</p><p>4 For the record, Thread also offers a constructor that takes a ThreadStart delegate that accepts no arguments and returns void. Personally, I recommend that you avoid this constructor and delegate because they are more limiting. If your thread method takes an Object and returns void, then you can invoke your method by using a dedicated thread or invoke it by using the thread pool (as shown in Chapter 27).</p><p>The following code demonstrates how to create a dedicated thread and have it call a method asynchronously.</p><p>using System; using System.Threading;</p><p>public static class Program { public static void Main() { Console.WriteLine(\"Main thread: starting a dedicated thread \" +</p><p>\"to do an asynchronous operation\"); Thread dedicatedThread = new Thread(ComputeBoundOp); dedicatedThread.Start(5);</p><p>Console.WriteLine(\"Main thread: Doing other work here...\"); Thread.Sleep(10000); // Simulating other work (10 seconds)</p><p>}</p><p>dedicatedThread.Join(); // Wait for thread to terminate Console.WriteLine(\"Hit &lt;Enter&gt; to end this program...\"); Console.ReadLine();</p><p>// This method's signature must match the ParameterizedThreadStart delegate private static void ComputeBoundOp(Object state) {</p><p>// This method is executed by a dedicated thread</p><p>Console.WriteLine(\"In ComputeBoundOp: state={0}\", state); Thread.Sleep(1000); // Simulates other work (1 second)</p><p>}</p><p>// When this method returns, the dedicated thread dies }</p><p>When I compile and run this code, I get the following output.</p><p>Main thread: starting a dedicated thread to do an asynchronous operation Main thread: Doing other work here... In ComputeBoundOp: state=5</p><p>Sometimes when I run this code, I get the following output, because I can\u2019t control how Windows schedules the two threads.</p><p>Main thread: starting a dedicated thread to do an asynchronous operation In ComputeBoundOp: state=5 Main thread: Doing other work here...</p><p>Notice that the Main method calls Join. The Join method causes the calling thread to stop executing any code until the thread identified by dedicatedThread has destroyed itself or been terminated.</p><h6>Reasons to Use Threads</h6><p>There are really two reasons to use threads:</p><p>\u25a0\u25a0 Responsiveness (typically for client-side GUI applications) Windows gives each process its own thread so that one application entering an infinite loop doesn\u2019t prevent the user from working with other applications. Similarly, within your client-side GUI application, you could spawn some work off onto a thread so that your GUI thread remains responsive to user input events. In this example, you are possibly creating more threads than available cores on the machine, so you are wasting system resources and hurting performance. However, the user is gaining a responsive user interface and therefore having a better overall experience with your application.</p><p>\u25a0\u25a0 Performance (for client and server side applications) Because Windows can schedule one thread per CPU and because the CPUs can execute these threads concurrently, your application can improve its performance by having multiple operations executing at the same time in parallel. Of course, you only get the improved performance if and only if your application is running on a machine with multiple CPUs in it. Today, machines with multiple CPUs in them are quite common, so designing your application to use multiple cores makes sense and is the focus of Chapter 27 and Chapter 28, \u201cI/O-Bound Asynchronous Operations.\u201d</p><p>Now, I\u2019d like to share with you a theory of mine. Every computer has an incredibly powerful resource inside it: the CPU itself. If someone spends money on a computer, then that computer should be working all the time. In other words, I believe that all the CPUs in a computer should be running at 100 percent utilization all the time. I will qualify this statement with two caveats. First, you may not want the CPUs running at 100 percent utilization if the computer is on battery power, because that may drain the battery too quickly. Second, some data centers would prefer to have 10 machines running at 50 percent CPU utilization rather than 5 machines running at 100 percent CPU utilization, because running CPUs at full power tends to generate heat, which requires cooling systems, and powering an HVAC cooling system can be more expensive than powering more computers running at reduced capacity. Although data centers find it increasingly expensive to maintain multiple machines, because each machine has to have periodic hardware and software upgrades and monitoring, this has to be weighed against the expense of running a cooling system.</p><p>Now, if you agree with my theory, then the next step is to figure out what the CPUs should be doing. Before I give you my ideas here, let me say something else first. In the past, developers and end users always felt that the computer was not powerful enough. Therefore, we developers would never just execute code unless the end users give us permission to do so and indicate that it is OK for the application to consume CPU resources via UI elements, such as menu items, buttons, and check boxes.</p><p>But now, times have changed. Computers ship with phenomenal amounts of computing power. Earlier in this chapter, I showed you how Task Manager was reporting that my CPU was busy just 5 percent of the time. If my computer contained a quad-core CPU in it instead of the dual-core CPU that it now has, then Task Manager will report 2 percent more often. When an 80-core processor comes out, the machine will look like it\u2019s doing nothing almost all the time. To computer purchasers, it looks like they\u2019re spending more money for more CPUs and the computer is doing less work!</p><p>This is the reason why the hardware manufacturers are having a hard time selling multi-core computers to users: the software isn\u2019t taking advantage of the hardware and users get no benefit from buying machines with additional CPUs. What I\u2019m saying is that we now have an abundance of computing power available and more is on the way, so developers can aggressively consume it. That\u2019s right\u2014in the past, we would never dream of having our applications perform some computation unless we knew the end user wanted the result of that computation. But now that we have extra computing power, we can dream like this.</p><p>Here\u2019s an example: when you stop typing in Visual Studio\u2019s editor, Visual Studio automatically spawns the compiler and compiles your code. This makes developers incredibly productive because they can see warnings and errors in their source code as they type and can fix things immediately. In fact, what developers think of today as the Edit-Build-Debug cycle will become just the Edit-Debug cycle, because building (compiling) code will just happen all the time. You, as an end user, won\u2019t notice this because there is a lot of CPU power available and other things you\u2019re doing will barely be affected by the frequent running of the compiler. In fact, I would expect that in some future version of Visual Studio, the Build menu item will disappear completely, because building will just become automatic. Not only does the application\u2019s UI get simpler, but the application also offers \u201canswers\u201d to the end user, making them more productive.</p><p>When we remove UI components like menu items, computers get simpler for end users. There are fewer options for them and fewer concepts for them to read and understand. It is the multi-core revolution that allows us to remove these UI elements, thereby making software so much simpler for end users that my grandmother might someday feel comfortable using a computer. For developers, removing UI elements usually results in less testing, and offering fewer options to the end user simplifies the code base. And if you currently localize the text in your UI elements and your documentation (like Microsoft does), then removing the UI elements means that you write less documentation and you don\u2019t have to localize this documentation anymore. All of this can save your organization a lot of time and money.</p><p>Here are some more examples of aggressive CPU consumption: spell checking and grammar checking of documents, recalculation of spreadsheets, indexing files on your disk for fast searching, and defragmenting your hard disk to improve I/O performance.</p><p>I want to live in a world where the UI is reduced and simplified, I have more screen real estate to visualize the data that I\u2019m actually working on, and applications offer me information that helps me get my work done quickly and efficiently instead of me telling the application to go get information for me. I think the hardware has been there for software developers to use for the past few years. It\u2019s time for the software to start using the hardware creatively.</p><h6>Thread Scheduling and Priorities</h6><p>A preemptive operating system must use some kind of algorithm to determine which threads should be scheduled when and for how long. In this section, we\u2019ll look at the algorithm Windows uses. Earlier in this chapter, I mentioned how every thread\u2019s kernel object contains a context structure. The context structure reflects the state of the thread\u2019s CPU registers when the thread last executed. After a timeslice, Windows looks at all the thread kernel objects currently in existence. Of these objects, only the threads that are not waiting for something are considered schedulable. Windows selects one of the schedulable thread kernel objects and context switches to it. Windows actually keeps a record of how many times each thread gets context switched to. You can see this when using a tool such as Microsoft Spy++. Figure 26-3 shows the properties for a thread. Notice that this thread has been scheduled 31,768 times.5</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile223.png\"/><figcaption>FIGURE 26-3 Spy++ showing a thread\u2019s properties.</figcaption><p>At this point, the thread is executing code and manipulating data in its process\u2019s address space. After another time-slice, Windows performs another context switch. Windows performs context switches from the moment the system is booted and continues until the system is shut down.</p><p>5 As a side note, you can also see that the thread has been in the system for more than 25 hours, but it actually used less than one second of CPU time, which wastes a lot of resources.</p><p>Windows is called a preemptive multithreaded operating system because a thread can be stopped at any time and another thread can be scheduled. As you\u2019ll see, you have some control over this, but not much. Just remember that you cannot guarantee that your thread will always be running and that no other thread will be allowed to run.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile224.png\"/><p>Note Developers frequently ask me how they can guarantee that their thread will start running within some time period after some event\u2014for example, how can you ensure that a particular thread will start running within 1 ms of data coming from the network? I have an easy answer: You can\u2019t.</p><p>Real-time operating systems can make these promises, but Windows is not a real-time operating system. A real-time operating system requires intimate knowledge of the hardware it is running on so that it knows the latency associated with its hard disk controllers, keyboards, and other components. Microsoft\u2019s goal with Windows is to make it work on a wide variety of hardware: different CPUs, different drives, different networks, and so on. In short, Windows is not designed to be a real-time operating system. Let me also add that the CLR makes managed code behave even less in real time. There are many reasons for this, including just-in-time (JIT) loading of DLLs, JIT compiling of code, and the garbage collector kicking in at unpredictable times.</p><p>Every thread is assigned a priority level ranging from 0 (the lowest) to 31 (the highest). When the system decides which thread to assign to a CPU, it examines the priority 31 threads first and schedules them in a round-robin fashion. If a priority 31 thread is schedulable, it is assigned to a CPU. At the end of this thread\u2019s time-slice, the system checks to see whether there is another priority 31 thread that can run; if so, it allows that thread to be assigned to a CPU.</p><p>As long as priority 31 threads are schedulable, the system never assigns any thread with a priority of 0 through 30 to a CPU. This condition is called starvation, and it occurs when higher-priority threads use so much CPU time that they prevent lower-priority threads from executing. Starvation is much less likely to occur on a multiprocessor machine because a priority 31 thread and a priority 30 thread can run simultaneously on such a machine. The system always tries to keep the CPUs busy, and CPUs sit idle only if no threads are schedulable.</p><p>Higher-priority threads always preempt lower-priority threads, regardless of what the lower-priority threads are executing. For example, if a priority 5 thread is running and the system determines that a higher-priority thread is ready to run, the system immediately suspends the lower-priority thread (even if it\u2019s in the middle of its time-slice) and assigns the CPU to the higher-priority thread, which gets a full time-slice.</p><p>By the way, when the system boots, it creates a special thread called the zero page thread. This thread is assigned priority 0 and is the only thread in the entire system that runs at priority 0. The zero page thread is responsible for zeroing any free pages of RAM in the system when no other threads need to perform work.</p><p>Microsoft realized that assigning priority levels to threads was going to be too hard for developers to rationalize. Should this thread be priority level 10? Should this other thread be priority level 23? To resolve this issue, Windows exposes an abstract layer over the priority level system.</p><p>When designing your application, you should decide whether your application needs to be more or less responsive than other applications that may be running on the machine. Then you choose a process priority class to reflect your decision. Windows supports six process priority classes: Idle, Below Normal, Normal, Above Normal, High, and Realtime. Of course, Normal is the default and is therefore the most common priority class by far.</p><p>The Idle priority class is perfect for applications (like screen savers) that run when the system is all but doing nothing. A computer that is not being used interactively might still be busy (acting as a file server, for example) and should not have to compete for CPU time with a screen saver. Statisticstracking applications that periodically update some state about the system usually should not interfere with more critical tasks.</p><p>You should use the High priority class only when absolutely necessary. You should avoid using the Realtime priority class if possible. Realtime priority is extremely high and can interfere with operating system tasks, such as preventing required disk I/O and network traffic from occurring. In addition, a Realtime process\u2019s threads could prevent keyboard and mouse input from being processed in a timely manner, causing the user to think that the system is completely frozen. Basically, you should have a good reason for using Realtime priority, such as the need to respond to hardware events with short latency or to perform some short-lived task.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile225.png\"/><p>Note To keep the overall system running smoothly, a process cannot run in the Realtime priority class unless the user has the Increase Scheduling Priority privilege. Any user designated as an administrator or a power user has this privilege by default.</p><p>After you select a priority class, you should stop thinking about how your application relates to other applications and just concentrate on the threads within your application. Windows supports seven relative thread priorities: Idle, Lowest, Below Normal, Normal, Above Normal, Highest, and Time-Critical. These priorities are relative to the process\u2019s priority class. Again, Normal relative thread priority is the default, and it is therefore the most common.</p><p>So, to summarize, your process is a member of a priority class and within that process you assign thread priorities that are relative to each other. You\u2019ll notice that I haven\u2019t said anything about priority levels 0 through 31. Application developers never work with priority levels directly. Instead, the system maps the process\u2019s priority class and a thread\u2019s relative priority to a priority level. Table 26-1 shows how the process\u2019s priority class and the thread\u2019s relative priority maps to priority levels.</p><figcaption>TABLE 26-1 How Process Priority Class and Relative Thread Priorities Map to Priority Levels</figcaption><table border=\"1\">\n<tr>\n<td><p>Relative Thread Priority</p>\n</td>\n<td colspan=\"6\"><p>Process Priority Class</p>\n</td>\n</tr>\n<tr>\n<td></td>\n<td><p>Idle</p>\n</td>\n<td><p>Below Normal</p>\n</td>\n<td><p>Normal</p>\n</td>\n<td><p>Above Normal</p>\n</td>\n<td><p>High</p>\n</td>\n<td><p>Realtime</p>\n</td>\n</tr>\n<tr>\n<td><p>Time-Critical</p>\n</td>\n<td><p>15</p>\n</td>\n<td><p>15</p>\n</td>\n<td><p>15</p>\n</td>\n<td><p>15</p>\n</td>\n<td><p>15</p>\n</td>\n<td><p>31</p>\n</td>\n</tr>\n<tr>\n<td><p>Highest</p>\n</td>\n<td><p>6</p>\n</td>\n<td><p>8</p>\n</td>\n<td><p>10</p>\n</td>\n<td><p>12</p>\n</td>\n<td><p>15</p>\n</td>\n<td><p>26</p>\n</td>\n</tr>\n<tr>\n<td><p>Above Normal</p>\n</td>\n<td><p>5</p>\n</td>\n<td><p>7</p>\n</td>\n<td><p>9</p>\n</td>\n<td><p>11</p>\n</td>\n<td><p>14</p>\n</td>\n<td><p>25</p>\n</td>\n</tr>\n<tr>\n<td><p>Normal</p>\n</td>\n<td><p>4</p>\n</td>\n<td><p>6</p>\n</td>\n<td><p>8</p>\n</td>\n<td><p>10</p>\n</td>\n<td><p>13</p>\n</td>\n<td><p>24</p>\n</td>\n</tr>\n<tr>\n<td><p>Below Normal</p>\n</td>\n<td><p>3</p>\n</td>\n<td><p>5</p>\n</td>\n<td><p>7</p>\n</td>\n<td><p>9</p>\n</td>\n<td><p>12</p>\n</td>\n<td><p>23</p>\n</td>\n</tr>\n<tr>\n<td><p>Lowest</p>\n</td>\n<td><p>2</p>\n</td>\n<td><p>4</p>\n</td>\n<td><p>6</p>\n</td>\n<td><p>8</p>\n</td>\n<td><p>11</p>\n</td>\n<td><p>22</p>\n</td>\n</tr>\n<tr>\n<td><p>Idle</p>\n</td>\n<td><p>1</p>\n</td>\n<td><p>1</p>\n</td>\n<td><p>1</p>\n</td>\n<td><p>1</p>\n</td>\n<td><p>1</p>\n</td>\n<td><p>16</p>\n</td>\n</tr>\n</table><p>For example, a Normal thread in a Normal process is assigned a priority level of 8. Because most processes are of the Normal priority class and most threads are of Normal thread priority, most threads in the system have a priority level of 8.</p><p>If you have a Normal thread in a high-priority process, the thread will have a priority level of 13. If you change the process\u2019s priority class to Idle, the thread\u2019s priority level becomes 4. Remember that thread priorities are relative to the process\u2019s priority class. If you change a process\u2019s priority class, the thread\u2019s relative priority will not change, but its priority number will.</p><p>Notice that the table does not show any way for a thread to have a priority level of 0. This is because the 0 priority is reserved for the zero page thread and the system does not allow any other thread to have a priority of 0. Also, the following priority levels are not obtainable: 17, 18, 19, 20, 21, 27, 28, 29, or 30. If you are writing a device driver that runs in kernel mode, you can obtain these levels; a user-mode application cannot. Also note that a thread in the Realtime priority class can\u2019t be below priority level 16. Likewise, a thread in a priority class other than Realtime cannot be above 15.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile226.png\"/><p>Note The concept of a process priority class confuses some people. They think that this somehow means that Windows schedules processes. However, Windows never schedules processes; Windows only schedules threads. The process priority class is an abstract concept that Microsoft created to help you rationalize how your application compares with other running applications; it serves no other purpose.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile227.png\"/><p>Important It is best to lower a thread\u2019s priority instead of raising another thread\u2019s priority. You would normally lower a thread\u2019s priority if that thread was going to execute a longrunning compute-bound task like compiling code, spell checking, spreadsheet recalculations, etc. You would raise a thread\u2019s priority if the thread needs to respond to something very quickly and then run for a very short period of time and go back to its wait state. High-priority threads should be waiting for something most of their life so that they do not affect the responsiveness of the whole system. The Explorer thread that responds to the user pressing the Windows key on the keyboard is an example of a high-priority thread. When the user presses this key, Explorer preempts other lower-priority threads immediately and displays its menu. As the user navigates the menu, Explorer\u2019s thread responds to each keystroke quickly, updates the menu, and then stops running until the user continues navigating the menu.</p><p>Normally, a process is assigned a priority class based on the process that starts it running. And most processes are started by Explorer, which spawns all its child processes in the Normal priority class. Managed applications are not supposed to act as though they own their own processes; they are supposed to act as though they run in an AppDomain, so managed applications are not supposed to change their process\u2019s priority class because this would affect all code running in the process. For example, many ASP.NET applications run in a single process, with each application in its own AppDomain. The same is true for Microsoft Silverlight applications, which run in an Internet browser process, and managed stored procedures, which run inside the Microsoft SQL Server process.</p><p>In addition, a Windows Store app is not able to create additional AppDomains, cannot change its process\u2019s priority class, or any of its threads\u2019 priorities. Furthermore, when a Windows Store app is not in the foreground, Windows automatically suspends all its threads. This serves two purposes. First, it prevents a background app from \u201cstealing\u201d CPU time away from the app the user is actively interacting with. This ensures that touch events like swipes are fast and fluid. Second, by reducing CPU usage, battery power is conserved, allowing the PC to run longer on a single charge.</p><p>On the other hand, your application can change the relative thread priority of its threads by setting Thread\u2019s Priority property, passing it one of the five values (Lowest, BelowNormal, Normal, AboveNormal, or Highest) defined in the ThreadPriority enumerated type. However, just as Windows has reserved the priority level 0 and the real-time range for itself, the CLR reserves the Idle and Time-Critical priority levels for itself. Today, the CLR has no threads that run at Idle priority level, but this could change in the future. However, the CLR\u2019s finalizer thread, discussed in Chapter 21, \u201cThe Managed Heap and Garbage Collection,\u201d runs at the Time-Critical priority level. Therefore, as a managed developer, you really only get to use the five highlighted relative thread priorities listed in Table 26-1.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile228.png\"/><p>Important Today, most applications do not take advantage of thread priorities. However, in the world that I envision, where the CPUs are busy 100 percent of the time doing some kind of useful work, using thread priorities becomes critically important so that system responsiveness is unaffected. Unfortunately, end users have been trained to interpret a highCPU usage number to mean that an application is out of control. In my new world, end users will need to be retrained to understand that high-CPU usage is a good thing\u2014that it actually means that the computer is aggressively processing helpful pieces of information for users. The real problem would be if all the CPUs are busy running threads that are priority level 8 and above, because this would mean that applications are having trouble responding to end user input. Perhaps a future version of Task Manager will take thread priority levels into account when reporting CPU usage; this would be much more helpful in diagnosing a troubled system.</p><p>For desktop apps (non\u2013Windows Store apps), I should point out that the System.Diagnostics namespace contains a Process class and a ProcessThread class. These classes provide the Windows view of a process and thread, respectively. These classes are provided for developers wanting to write utility applications in managed code or for developers who are trying to instrument their code to help them debug it. In fact, this is why the classes are in the System.Diagnostics namespace. Applications need to be running with special security permissions to use these two classes. You would not be able to use these classes from a Silverlight application or an ASP.NET application, for example.</p><p>On the other hand, applications can use the AppDomain and Thread classes, which expose the CLR\u2019s view of an AppDomain and thread. For the most part, special security permissions are not required to use these classes, although some operations are still considered privileged.</p><h6>Foreground Threads vs. Background Threads</h6><p>The CLR considers every thread to be either a foreground thread or a background thread. When all the foreground threads in a process stop running, the CLR forcibly ends any background threads that are still running. These background threads are ended immediately; no exception is thrown.</p><p>Therefore, you should use foreground threads to execute tasks that you really want to complete, like flushing data from a memory buffer out to disk. And you should use background threads for tasks that are not mission-critical, like recalculating spreadsheet cells or indexing records, because this work can continue again when the application restarts, and there is no need to force the application to stay active if the user wants to terminate it.</p><p>The CLR needed to provide this concept of foreground and background threads to better support AppDomains. You see, each AppDomain could be running a separate application and each of these applications would have its own foreground thread. If one application exits, causing its foreground thread to terminate, then the CLR still needs to stay up and running so that other applications continue to run. After all the applications exit and all their foreground threads terminate, the whole process can be destroyed.</p><p>The following code demonstrates the difference between foreground and background threads.</p><p>using System; using System.Threading;</p><p>public static class Program {</p><p>public static void Main() { // Create a new thread (defaults to foreground) Thread t = new Thread(Worker);</p><p>// Make the thread a background thread t.IsBackground = true;</p><p>}</p><p>t.Start(); // Start the thread // If t is a foreground thread, the application won't die for about 10 seconds // If t is a background thread, the application dies immediately Console.WriteLine(\"Returning from Main\");</p><p>private static void Worker() { Thread.Sleep(10000); // Simulate doing 10 seconds of work</p><p>} }</p><p>// The following line only gets displayed if this code is executed by a foreground thread Console.WriteLine(\"Returning from Worker\");</p><p>It is possible to change a thread from foreground to background and vice versa at any time during its lifetime. An application\u2019s primary thread and any threads explicitly created by constructing a Thread object default to being foreground threads. On the other hand, thread pool threads default to being background threads. Also, any threads created by native code that enter the managed execution environment are marked as background threads.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile229.png\"/><p>Important Try to avoid using foreground threads as much as possible. I was brought into a consulting job once where an application just wouldn\u2019t terminate. After I spent several hours researching the problem, it turned out that a UI component was explicitly creating a foreground thread (the default), and that was why the process wouldn\u2019t terminate. We changed the component to use the thread pool to fix the problem, and efficiency improved as well.</p><h6>What Now?</h6><p>In this chapter, I\u2019ve explained the basics about threads, and I hope I\u2019ve made it clear to you that threads are very expensive resources that should be used sparingly. The best way to accomplish this is by using the thread pool. The thread pool will manage thread creation and destruction for you automatically. The thread pool creates a set of threads that get reused for various tasks so your application requires just a few threads to accomplish all of its work.</p><p>In Chapter 27, I will focus on how to use the thread pool to perform compute-bound operations. Then, in Chapter 28, I will discuss how to use the thread pool to perform I/O-bound operations. In many scenarios, you can perform asynchronous compute-bound and I/O-bound operations in such a way that thread synchronization is not required at all. However, there are some scenarios where thread synchronization is required, and the way that the thread synchronization constructs work and the difference between these various constructs are discussed in Chapter 29, \u201cPrimitive Thread Synchronization Constructs,\u201d and Chapter 30, \u201cHybrid Thread Synchronization Constructs.\u201d</p><p>Before ending this discussion, I\u2019d like to point out that I have been working extensively with threads because the first beta version of Windows NT 3.1 was available around 1992. And when .NET was in beta, I started producing a library of classes that can simplify asynchronous programming and thread synchronization. This library is called the Wintellect Power Threading Library, and it is freely downloadable and usable. Versions of the library exist for the desktop CLR, the Silverlight CLR, and the Compact Framework. The library, documentation, and sample code can be downloaded from http://Wintellect.com/PowerThreading.aspx. This website also contains links to a support forum, in addition to videos that show how to use various parts of the library.</p><h6>C H A P T E R 2 7</h6><h2>Compute-Bound Asynchronous Operations</h2><h6>In this chapter:</h6><p>Introducing the CLR\u2019s Thread Pool. . . . . . . . . . . . . . . . . . . . . . . . . 692 Performing a Simple Compute-Bound Operation . . . . . . . . . . . 693 Execution Contexts. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 694 Cooperative Cancellation and Timeout. . . . . . . . . . . . . . . . . . . . . 696 Tasks. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 700 Parallel\u2019s Static For, ForEach, and Invoke Methods . . . . . . 713 Parallel Language Integrated Query . . . . . . . . . . . . . . . . . . . . . . . 717 Performing a Periodic Compute-Bound Operation . . . . . . . . . . 720 How the Thread Pool Manages Its Threads . . . . . . . . . . . . . . . . . 723</p><p>In this chapter, I\u2019ll talk about the various ways that you can perform operations asynchronously. When performing an asynchronous compute-bound operation, you execute it using other threads. Here are some examples of compute-bound operations: compiling code, spell checking, grammar checking, spreadsheet recalculations, transcoding audio or video data, and producing a thumbnail of an image. Certainly, compute-bound operations are common in financial and engineering applications.</p><p>I would say that most applications do not spend the bulk of their time processing in-memory data or performing calculations. You can verify that this is true by opening Task Manager and selecting the Performance tab. If your CPU usage is below 100 percent (which it tends to be most of the time), then the processes you have running are not using all the processing power made available by your machine\u2019s CPU cores. When the CPU usage is less than 100 percent, then some (if not all) of the threads within their processes are not running at all. Instead, these threads are waiting for some input or output operation to occur. For example, these threads are waiting for a timer to come due, waiting for data to be read from or written to a database, web service, file, network, or other hardware device, or waiting for keystrokes, mouse movement, or mouse button clicks. When performing an I/O-bound operation, the Windows device driver has the hardware device do the work for you, and the CPU itself doesn\u2019t execute any threads that happen to exist in the system. Because threads are not running on a CPU, Task Manager indicates that CPU usage is low.</p><p>691</p><p>However, even in applications that are heavily I/O-bound, these applications perform some computation on data that has been received, and parallelizing this computation can greatly improve the application\u2019s throughput. This chapter introduces the common language runtime\u2019s (CLR\u2019s) thread pool and some basic concepts about how it works and how to use it. This information is critically useful, because the thread pool is the core technology that enables you to design and implement responsive and scalable applications and components. Then this chapter shows the various mechanisms available that allow you to perform compute-bound operations via the thread pool.</p><h6>Introducing the CLR\u2019s Thread Pool</h6><p>As stated in the previous chapter, creating and destroying a thread is an expensive operation in terms of time. In addition, having lots of threads wastes memory resources and also hurts performance due to the operating system having to schedule and context switch between the runnable threads. To improve this situation, the CLR contains code to manage its own thread pool. You can think of a thread pool as being a set of threads that are available for your application\u2019s own use. There is one thread pool per CLR; this thread pool is shared by all AppDomains controlled by that CLR. If multiple CLRs load within a single process, then each CLR has its own thread pool.</p><p>When the CLR initializes, the thread pool has no threads in it. Internally, the thread pool maintains a queue of operation requests. When your application wants to perform an asynchronous operation, you call some method that appends an entry into the thread pool\u2019s queue. The thread pool\u2019s code will extract entries from this queue and dispatch the entry to a thread pool thread. If there are no threads in the thread pool, a new thread will be created. Creating a thread has a performance hit associated with it (as already discussed). However, when a thread pool thread has completed its task, the thread is not destroyed; instead, the thread is returned to the thread pool, where it sits idle waiting to respond to another request. Because the thread doesn\u2019t destroy itself, there is no added performance hit.</p><p>If your application makes many requests of the thread pool, the thread pool will try to service all of the requests by using just this one thread. However, if your application is queuing up several requests faster than the thread pool thread can handle them, additional threads will be created. Your application will eventually get to a point at which all of its requests can be handled by a small number of threads, so the thread pool should have no need to create a lot of threads.</p><p>If your application stops making requests of the thread pool, the pool may have a lot of threads in it that are doing nothing. This is wasteful of memory resources. So when a thread pool thread has been idle with nothing to do for some period of time (subject to change with different versions of the CLR), the thread wakes itself up and kills itself to free up resources. As the thread is killing itself, there is a performance hit. However, this probably doesn\u2019t matter, because the thread is killing itself because it has been idle, which means that your application isn\u2019t performing a lot of work.</p><p>The great thing about the thread pool is that it manages the tension between having a few threads, to keep from wasting resources, and having more threads, to take advantage of multiprocessors, hyperthreaded processors, and multi-core processors. And the thread pool is heuristic. If</p><p>your application needs to perform many tasks and CPUs are available, the thread pool creates more threads. If your application\u2019s workload decreases, the thread pool threads kill themselves.</p><h6>Performing a Simple Compute-Bound Operation</h6><p>To queue an asynchronous compute-bound operation to the thread pool, you typically call one of the following methods defined by the ThreadPool class.</p><p>static Boolean QueueUserWorkItem(WaitCallback callBack); static Boolean QueueUserWorkItem(WaitCallback callBack, Object state);</p><p>These methods queue a \u201cwork item\u201d and optional state data to the thread pool\u2019s queue, and then all of these methods return immediately. A work item is simply a method identified by the callback parameter that will be called by a thread pool thread. The method can be passed a single parameter specified via the state (the state data) argument. The version of QueueUserWorkItem without the</p><p>state parameter passes null to the callback method. Eventually, some thread in the pool will process the work item, causing your method to be called. The callback method you write must match the System.Threading.WaitCallback delegate type, which is defined as follows.</p><p>delegate void WaitCallback(Object state);</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile230.png\"/><p>Note The signatures of the WaitCallback delegate, the TimerCallback delegate (discussed in this chapter\u2019s \u201cPerforming a Periodic Compute-Bound Operation\u201d section), and the ParameterizedThreadStart delegate (discussed in Chapter 26, \u201cThread Basics\u201d) are all identical. If you define a method matching this signature, the method can be invoked by using ThreadPool.QueueUserWorkItem, by using a System.Threading.Timer object, or by using a System.Threading.Thread object.</p><p>The following code demonstrates how to have a thread pool thread call a method asynchronously.</p><p>using System; using System.Threading;</p><p>public static class Program {</p><p>public static void Main() { Console.WriteLine(\"Main thread: queuing an asynchronous operation\"); ThreadPool.QueueUserWorkItem(ComputeBoundOp, 5); Console.WriteLine(\"Main thread: Doing other work here...\"); Thread.Sleep(10000); // Simulating other work (10 seconds) Console.WriteLine(\"Hit &lt;Enter&gt; to end this program...\"); Console.ReadLine();</p><p>}</p><p>// This method's signature must match the WaitCallback delegate private static void ComputeBoundOp(Object state) {</p><p>// This method is executed by a thread pool thread</p><p>Console.WriteLine(\"In ComputeBoundOp: state={0}\", state); Thread.Sleep(1000); // Simulates other work (1 second)</p><p>} }</p><p>// When this method returns, the thread goes back // to the pool and waits for another task</p><p>When I compile and run this code, I get the following output.</p><p>Main thread: queuing an asynchronous operation Main thread: Doing other work here... In ComputeBoundOp: state=5</p><p>And, sometimes when I run this code, I get this output.</p><p>Main thread: queuing an asynchronous operation In ComputeBoundOp: state=5 Main thread: Doing other work here...</p><p>The difference in the order of the lines in the output is attributed to the fact that the two methods are running asynchronously with respect to one another. The Windows scheduler determines which thread to schedule first, or it may schedule them both simultaneously if the application is running on a multi-CPU machine.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile231.png\"/><p>Note If the callback method throws an exception that is unhandled, the CLR terminates the process (unless the host imposes its own policy). Unhandled exceptions are discussed in Chapter 20, \u201cExceptions and State Management.\u201d</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile232.png\"/><p>Note For Windows Store apps, the System.Threading.ThreadPool class is not publicly exposed. However, it is used indirectly when you use types in the System.Threading. Tasks namespace (see the \u201cTasks\u201d Section later in this chapter).</p><h6>Execution Contexts</h6><p>Every thread has an execution context data structure associated with it. The execution context includes things such as security settings (compressed stack, Thread\u2019s Principal property, and Windows identity), host settings (see System.Threading.HostExecutionContextManager), and logical call context data (see System.Runtime.Remoting.Messaging.CallContext\u2019s LogicalSetData and LogicalGetData methods). When a thread executes code, some operations are affected by the values of the thread\u2019s execution context settings. This is certainly true for the security settings. Ideally, whenever a thread uses another (helper) thread to perform tasks, the issuing thread\u2019s execution context should flow (be copied) to the helper thread. This ensures that any operations performed by helper thread(s) are executing with the same security settings and host settings. It also</p><p>ensures that any data stored in the initiating thread\u2019s logical call context is available to the helper thread.</p><p>By default, the CLR automatically causes the initiating thread\u2019s execution context to flow to any helper threads. This transfers context information to the helper thread, but it comes at a performance cost, because there is a lot of information in an execution context, and accumulating all of this information and then copying it for the helper thread takes a fair amount of time. If the helper thread then employs additional helper threads, then more execution context data structures have to be created and initialized as well.</p><p>In the System.Threading namespace, there is an ExecutionContext class that allows you to control how a thread\u2019s execution context flows from one thread to another. Here is what the class looks like.</p><p>public sealed class ExecutionContext : IDisposable, ISerializable { [SecurityCritical] public static AsyncFlowControl SuppressFlow(); public static void RestoreFlow(); public static Boolean IsFlowSuppressed();</p><p>// Less commonly used methods are not shown }</p><p>You can use this class to suppress the flowing of an execution context, thereby improving your application\u2019s performance. The performance gains can be quite substantial for a server application. There is not much performance benefit for a client application, and the SuppressFlow method is marked with the [SecurityCritical] attribute, making it impossible to call in some client applications (like Microsoft Silverlight). Of course, you should suppress the flowing of execution context only if the helper thread does not need to access the context information. If the initiating thread\u2019s execution context does not flow to a helper thread, the helper thread will use whatever execution context it last associated with it. Therefore, the helper thread really shouldn\u2019t execute any code that relies on the execution context state (such as a user\u2019s Windows identity).</p><p>Here is an example showing how suppressing the flow of execution context affects data in a thread\u2019s logical call context when queuing a work item to the CLR\u2019s thread pool.1</p><p>public static void Main() { // Put some data into the Main thread's logical call context CallContext.LogicalSetData(\"Name\", \"Jeffrey\");</p><p>// Initiate some work to be done by a thread pool thread // The thread pool thread can access the logical call context data ThreadPool.QueueUserWorkItem(</p><p>state =&gt; Console.WriteLine(\"Name={0}\", CallContext.LogicalGetData(\"Name\")));</p><p>// Now, suppress the flowing of the Main thread's execution context ExecutionContext.SuppressFlow();</p><p>1 The items that you add to the logical call context must be serializable, as discussed in Chapter 24, \u201cRuntime Serialization.\u201d Flowing an execution context that contains logical call context data items can hurt performance dramatically, because capturing the execution context requires serializing and deserializing all the data items.</p><p>// Initiate some work to be done by a thread pool thread // The thread pool thread CANNOT access the logical call context data ThreadPool.QueueUserWorkItem(</p><p>state =&gt; Console.WriteLine(\"Name={0}\", CallContext.LogicalGetData(\"Name\")));</p><p>}</p><p>// Restore the flowing of the Main thread's execution context in case // it employs more thread pool threads in the future ExecutionContext.RestoreFlow();</p><p>... Console.ReadLine();</p><p>When I compile and run the preceding code, I get the following output.</p><p>Name=Jeffrey Name=</p><p>Although this discussion has focused on suppressing the flow of execution context when calling ThreadPool.QueueUserWorkItem, this technique is also useful when using Task objects (discussed in the \u201cTasks\u201d section of this chapter) and is also useful when initiating asynchronous I/O operations (discussed in Chapter 28, \u201cI/O-Bound Asynchronous Operations\u201d).</p><h6>Cooperative Cancellation and Timeout</h6><p>The Microsoft .NET Framework offers a standard pattern for canceling operations. This pattern is cooperative, meaning that the operation that you want to cancel has to explicitly support being canceled. In other words, the code performing the operation that you want to cancel and the code that attempts to cancel the operation must both use the types mentioned in this section. It is nice when long-running compute-bound operations offer cancellation, so you should consider adding cancellation to your own compute-bound operations. In this section, I\u2019ll explain how to accomplish this. But, first, let me explain the two main types provided in the Framework Class Library (FCL) that are part of the standard cooperative cancellation pattern.</p><p>To cancel an operation, you must first create a System.Threading.CancellationTokenSource object. This class looks like this.</p><p>public sealed class CancellationTokenSource : IDisposable { // A reference type public CancellationTokenSource();</p><p>public Boolean IsCancellationRequested { get; } public CancellationToken Token { get; }</p><p>public void Cancel(); // Internally, calls Cancel passing false public void Cancel(Boolean throwOnFirstException);</p><p>... }</p><p>This object contains all the states having to do with managing cancellation. After constructing a CancellationTokenSource (a reference type), one or more CancellationToken (a value type) instances can be obtained by querying its Token property and passed around to your operations that</p><p>allow themselves to be canceled. Here are the most useful members of the CancellationToken value type.</p><p>public struct CancellationToken { // A value type public static CancellationToken None { get; } // Very convenient</p><p>public Boolean IsCancellationRequested { get; } // Called by non\u00adTask invoked operations public void ThrowIfCancellationRequested(); // Called by Task\u00adinvoked operations</p><p>// WaitHandle is signaled when the CancellationTokenSource is canceled public WaitHandle WaitHandle { get; } // GetHashCode, Equals, operator== and operator!= members are not shown</p><p>public Boolean CanBeCanceled { get; } // Rarely used</p><p>public CancellationTokenRegistration Register(Action&lt;Object&gt; callback, Object state,</p><p>Boolean useSynchronizationContext); // Simpler overloads not shown }</p><p>A CancellationToken instance is a lightweight value type because it contains a single private field: a reference to its CancellationTokenSource object. A compute-bound operation\u2019s loop can periodically call CancellationToken\u2019s IsCancellationRequested property to know if the loop should terminate early, thereby ending the compute-bound operation. Of course, the benefit here is that CPU time is no longer being wasted on an operation whose result you know you\u2019re not interested in. Now, let me put all this together with some sample code.</p><p>internal static class CancellationDemo { public static void Main() { CancellationTokenSource cts = new CancellationTokenSource();</p><p>// Pass the CancellationToken and the number\u00adto\u00adcount\u00adto into the operation ThreadPool.QueueUserWorkItem(o =&gt; Count(cts.Token, 1000));</p><p>Console.WriteLine(\"Press &lt;Enter&gt; to cancel the operation.\"); Console.ReadLine(); cts.Cancel(); // If Count returned already, Cancel has no effect on it // Cancel returns immediately, and the method continues running here...</p><p>Console.ReadLine(); }</p><p>private static void Count(CancellationToken token, Int32 countTo) { for (Int32 count = 0; count &lt;countTo; count++) {</p><p>if (token.IsCancellationRequested) { Console.WriteLine(\"Count is cancelled\"); break; // Exit the loop to stop the operation</p><p>}</p><p>} }</p><p>Console.WriteLine(count); Thread.Sleep(200); // For demo, waste some time</p><p>} Console.WriteLine(\"Count is done\");</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile233.png\"/><p>Note If you want to execute an operation and prevent it from being canceled, you can pass the operation the CancellationToken returned from calling CancellationToken\u2019s static None property. This very convenient property returns a special CancellationToken instance that is not associated with any CancellationTokenSource object (its private field is null). Because there is no CancellationTokenSource, no code can call Cancel, and the operation that is querying the special CancellationToken\u2019s IsCancellationRequested property will always return false. If you query CancellationToken\u2019s CanBeCanceled property by using one of these special CancellationToken instances, the property will return false. The property returns true for all other CancellationToken instances obtained by querying a CancellationTokenSource object\u2019s Token property.</p><p>If you\u2019d like, you can call CancellationTokenSource\u2019s Register method to register one or more methods to be invoked when a CancellationTokenSource is canceled. To this method, you pass an Action&lt;Object&gt; delegate, a state value that will be passed to the callback via the delegate, and a Boolean indicating whether or not to invoke the delegate by using the calling thread\u2019s SynchronizationContext. If you pass false for the useSynchronizationContext parameter, then the thread that calls Cancel will invoke all the registered methods sequentially. If you pass true for the useSynchronizationContext parameter, then the callbacks are sent (as opposed to posted) to the captured SynchronizationContext object that governs which thread invokes the callback. The SynchronizationContext class is discussed more in the \u201cApplications and Their Threading Models\u201d section in Chapter 28.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile234.png\"/><p>Note If you register a callback method with a CancellationTokenSource after the</p><p>CancellationTokenSource has already been canceled, then the thread calling Register invokes the callback (possible via the calling thread\u2019s SynchronizationContext if true is passed for the useSynchronizationContext parameter).</p><p>If Register is called multiple times, then multiple callback methods will be invoked. These callback methods could throw an unhandled exception. If you call CancellationTokenSource\u2019s Cancel, passing it true, then the first callback method that throws an unhandled exception stops the other callback methods from executing, and the exception thrown will be thrown from Cancel as well. If you call Cancel passing it false, then all registered callback methods are invoked. Any unhandled exceptions that occur are added to a collection. After all callback methods have executed, if any of them threw an unhandled exception, then Cancel throws an AggregateException with its InnerExceptions property set to the collection of exception objects that were thrown. If no registered callback methods threw an unhandled exception, then Cancel simply returns without throwing any exception at all.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile235.png\"/><p>Important There is no way to correlate an exception object from AggregateException\u2019s InnerExceptions collection to a particular operation; you are basically just being told that some operation failed and the exception type tells you what the failure was. To track down the specific location of the failure will require examining the exception object\u2019s StackTrace property and manually scanning your source code.</p><p>CancellationToken\u2019s Register method returns a CancellationTokenRegistration, which looks like this.</p><p>public struct CancellationTokenRegistration : IEquatable&lt;CancellationTokenRegistration&gt;, IDisposable { public void Dispose(); // GetHashCode, Equals, operator== and operator!= members are not shown</p><p>}</p><p>You can call Dispose to remove a registered callback from the CancellationTokenSource that it is associated with so that it does not get invoked when calling Cancel. Here is some code that demonstrates registering two callbacks with a single CancellationTokenSource.</p><p>var cts = new CancellationTokenSource(); cts.Token.Register(() =&gt; Console.WriteLine(\"Canceled 1\")); cts.Token.Register(() =&gt; Console.WriteLine(\"Canceled 2\"));</p><p>// To test, let's just cancel it now and have the 2 callbacks execute cts.Cancel();</p><p>When I run this code, I get the following output as soon as the Cancel method is called.</p><p>Canceled 2 Canceled 1</p><p>Finally, you can create a new CancellationTokenSource object by linking a bunch of other CancellationTokenSource objects. This new CancellationTokenSource object will be canceled when any of the linked CancellationTokenSource objects are canceled. The following code demonstrates.</p><p>// Create a CancellationTokenSource</p><ul style=\"list-style-type: none;\">\n<li><p>var cts1 = new CancellationTokenSource();</p><ul style=\"list-style-type: none;\">\n<li><p>cts1.Token.Register(() =&gt; Console.WriteLine(\"cts1 canceled\"));</p><p>// Create another CancellationTokenSource var cts2 = new CancellationTokenSource();</p>\n</li>\n<li><p>cts2.Token.Register(() =&gt; Console.WriteLine(\"cts2 canceled\"));</p></li>\n</ul>\n</li>\n</ul><p>// Create a new CancellationTokenSource that is canceled when cts1 or ct2 is canceled var linkedCts = CancellationTokenSource.CreateLinkedTokenSource(cts1.Token, cts2.Token); linkedCts.Token.Register(() =&gt; Console.WriteLine(\"linkedCts canceled\"));</p><p>// Cancel one of the CancellationTokenSource objects (I chose cts2) cts2.Cancel();</p><p>// Display which CancellationTokenSource objects are canceled Console.WriteLine(\"cts1 canceled={0}, cts2 canceled={1}, linkedCts canceled={2}\",</p><p>cts1.IsCancellationRequested, cts2.IsCancellationRequested, linkedCts.IsCancellationRequested);</p><p>When I run the preceding code, I get the following output.</p><p>linkedCts canceled cts2 canceled cts1 canceled=False, cts2 canceled=True, linkedCts canceled=True</p><p>It is often valuable to cancel an operation after a period of time has elapsed. For example, imagine a server application that starts computing some work based on a client request. But the server application needs to respond to the client within two seconds, no matter what. In some scenarios, it is better to respond in a short period of time with an error or with partially computed results as opposed to waiting a long time for a complete result. Fortunately, CancellationTokenSource gives you a way to have it self-cancel itself after a period of time. To take advantage of this, you can either construct a CancellationTokenSource object by using one of the constructors that accepts a delay, or you can call CancellationTokenSource\u2019s CancelAfter method.</p><p>public sealed class CancellationTokenSource : IDisposable { // A reference type public CancellationTokenSource(Int32 millisecondsDelay); public CancellationTokenSource(TimeSpan delay);</p><p>public void CancelAfter(Int32 millisecondsDelay); public void CancelAfter(TimeSpan delay);</p><p>... }</p><h6>Tasks</h6><p>Calling ThreadPool\u2019s QueueUserWorkItem method to initiate an asynchronous compute-bound operation is very simple. However, this technique has many limitations. The biggest problem is that there is no built-in way for you to know when the operation has completed, and there is no way to get a return value back when the operation completes. To address these limitations and more, Microsoft introduced the concept of tasks, and you use them via types in the System.Threading.Tasks namespace.</p><p>So, instead of calling ThreadPool\u2019s QueueUserWorkItem method, you can do the same via tasks.</p><p>ThreadPool.QueueUserWorkItem(ComputeBoundOp, 5); // Calling QueueUserWorkItem new Task(ComputeBoundOp, 5).Start(); // Equivalent of preceding using Task Task.Run(() =&gt; ComputeBoundOp(5)); // Another equivalent</p><p>In the second line of preceding code, I am creating the Task object and then immediately calling Start to schedule the task to run. Naturally, you can create the Task object and then call Start on it later. You could imagine code that creates a Task object and then passes it to some other method that decides when to call Start to schedule the task. Because it is common to create a Task object and then immediately call Start on it, you can call Task\u2019s convenient static Run method as shown on the last line of the preceding code.</p><p>When creating a Task, you call a constructor, passing it an Action or an Action&lt;Object&gt; delegate that indicates the operation that you want performed. If you pass a method that expects an Object, then you must also pass to Task\u2019s constructor the argument that you ultimately want passed to the operation. When calling Run, you can pass it an Action or Func&lt;TResult&gt; delegate indicating the operation you want performed. When calling a constructor or when calling Run, you can optionally pass a CancellationToken, which allows the Task to be canceled before it has been scheduled (see the \u201cCanceling a Task\u201d section later in this chapter).</p><p>You can also optionally pass to the constructor some TaskCreationOptions flags that control how the Task executes. TaskCreationOptions is an enumerated type defining a set of flags that you can bitwise-OR together. It is defined as follows.</p><p>[Flags, Serializable] public enum TaskCreationOptions {</p><p>None = 0x0000,// The default</p><p>// Hints to the TaskScheduler that you want this task to run sooner than later. PreferFairness = 0x0001,</p><p>// Hints to the TaskScheduler that it should more aggressively create thread pool threads. LongRunning = 0x0002,</p><p>// Always honored: Associates a Task with its parent Task (discussed shortly) AttachedToParent = 0x0004,</p><p>// If a task attempts to attach to this parent task, it is a normal task, not a child task. DenyChildAttach = 0x0008,</p><p>}</p><p>// Forces child tasks to use the default scheduler as opposed to the parent\u2019s scheduler. HideScheduler = 0x0010</p><p>Some of these flags are hints that may or may not be honored by the TaskScheduler that is being used to schedule a Task; the AttachedToParent, DenyChildAttach, and HideScheduler flags are always honored, because they have nothing to do with the TaskScheduler itself. TaskScheduler objects are discussed later in the \u201cTask Schedulers\u201d section.</p><h6>Waiting for a Task to Complete and Getting Its Result</h6><p>With tasks, it is also possible to wait for them to complete and then get their result. Let\u2019s say that we have a Sum method that is computationally intensive if n is a large value.</p><p>private static Int32 Sum(Int32 n) { Int32 sum = 0; for (; n &gt; 0; n\u00ad\u00ad)</p><p>checked { sum += n; } // if n is large, this will throw System.OverflowException return sum;</p><p>}</p><p>We can now construct a Task&lt;TResult&gt; object (which is derived from Task), and we pass for the generic TResult argument the compute-bound operation\u2019s return type. Now, after starting the task, we can wait for it to complete and then get its result by using the following code.</p><p>// Create a Task (it does not start running now) Task&lt;Int32&gt; t = new Task&lt;Int32&gt;(n =&gt; Sum((Int32)n), 1000000000);</p><p>// You can start the task sometime later t.Start();</p><p>// Optionally, you can explicitly wait for the task to complete t.Wait(); // FYI: Overloads exist accepting timeout/CancellationToken</p><p>// You can get the result (the Result property internally calls Wait) Console.WriteLine(\"The Sum is: \" + t.Result); // An Int32 value</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile236.png\"/><p>Important When a thread calls the Wait method, the system checks if the Task that the thread is waiting for has started executing. If it has, then the thread calling Wait will block until the Task has completed running. But if the Task has not started executing yet, then the system may (depending on the TaskScheduler) execute the Task by using the thread that called Wait. If this happens, then the thread calling Wait does not block; it executes the Task and returns immediately. This is good in that no thread has blocked, thereby reducing resource usage (by not creating a thread to replace the blocked thread) while improving performance (no time is spent to create a thread and there is no context switching). But it can also be bad if, for example, the thread has taken a thread synchronization lock before calling Wait and then the Task tries to take the same lock, resulting in a deadlocked thread!</p><p>If the compute-bound task throws an unhandled exception, the exception will be swallowed, stored in a collection, and the thread pool thread is allowed to return to the thread pool. When the Wait method or the Result property is invoked, these members will throw a System.AggregateException object.</p><p>The AggregateException type is used to encapsulate a collection of exception objects (which can happen if a parent task spawns multiple child tasks that throw exceptions). It contains an InnerExceptions property that returns a ReadOnlyCollection&lt;Exception&gt; object. Do not confuse the InnerExceptions property with the InnerException property, which the AggregateException class inherits from the System.Exception base class. For the preceding example, element 0 of AggregateException\u2019s InnerExceptions property would refer to the actual System.OverflowException object thrown by the compute-bound method (Sum).</p><p>As a convenience, AggregateException overrides Exception\u2019s GetBaseException method. AggregateException\u2019s implementation returns the innermost AggregateException that is the root cause of the problem (assuming that there is just one innermost exception in the collection).</p><p>AggregateException also offers a Flatten method that creates a new AggregateException, whose InnerExceptions property contains a list of exceptions produced by walking the original AggregateException\u2019s inner exception hierarchy. Finally, AggregateException also provides a Handle method that invokes a callback method for each exception contained in the AggregateException. The callback can then decide, for each exception, how to handle the exception; the</p><p>callback returns true to consider the exception handled and false if not. If, after calling Handle, at least one exception is not handled, then a new AggregateException object is created containing just the unhandled exceptions and the new AggregateException object is thrown. Later in this chapter, I show examples using the Flatten and Handle methods.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile237.png\"/><p>Important If you never call Wait or Result or query a Task\u2019s Exception property, then your code never observes that this exception has occurred. This is not ideal, because your program has experienced an unexpected problem that you are not aware of. To help you detect unobserved exceptions, you can register a callback method with TaskScheduler\u2019s static UnobservedTaskException event. This event is raised by the CLR\u2019s finalizer thread whenever a Task with an unobserved exception is garbage collected. When raised, your event handler method will be passed an UnobservedTaskExceptionEventArgs object containing the unobserved AggregateException.</p><p>In addition to waiting for a single task, the Task class also offers two static methods that allow a thread to wait on an array of Task objects. Task\u2019s static WaitAny method blocks the calling thread until any of the Task objects in the array have completed. This method returns an Int32 index into the array indicating which Task object completed, causing the thread to wake and continue running. The method returns \u00ad1 if the timeout occurs and throws an OperationCanceledException if WaitAny is canceled via a CancellationToken.</p><p>Similarly, the Task class has a static WaitAll method that blocks the calling thread until all the Task objects in the array have completed. The WaitAll method returns true if all the Task objects complete and false if a timeout occurs; an OperationCanceledException is thrown if WaitAll is canceled via a CancellationToken.</p><h6>Canceling a Task</h6><p>Of course, you can use a CancellationTokenSource to cancel a Task. First, we must revise our Sum method so that it accepts a CancellationToken.</p><p>private static Int32 Sum(CancellationToken ct, Int32 n) { Int32 sum = 0; for (; n &gt; 0; n\u00ad\u00ad) {</p><p>// The following line throws OperationCanceledException when Cancel // is called on the CancellationTokenSource referred to by the token ct.ThrowIfCancellationRequested();</p><p>}</p><p>checked { sum += n; } // if n is large, this will throw System.OverflowException</p><p>} return sum;</p><p>In this code, the compute-bound operation\u2019s loop periodically checks to see if the operation has been canceled by calling CancellationToken\u2019s ThrowIfCancellationRequested method. This method is similar to CancellationToken\u2019s IsCancellationRequested property shown earlier in the \u201cCooperative Cancellation and Timeout\u201d section. However, ThrowIfCancellationRequested throws an OperationCanceledException if the CancellationTokenSource has been canceled. The reason for throwing an exception is because, unlike work items initiated with ThreadPool\u2019s</p><p>QueueUserWorkItem method, tasks have the notion of having completed and a task can even return a value. So, there needs to be a way to distinguish a completed task from a canceled task, and having the task throw an exception lets you know that the task did not run all the way to completion.</p><p>Now, we will create the CancellationTokenSource and Task objects as follows.</p><p>CancellationTokenSource cts = new CancellationTokenSource(); Task&lt;Int32&gt; t = Task.Run(() =&gt; Sum(cts.Token, 1000000000), cts.Token);</p><p>// Sometime later, cancel the CancellationTokenSource to cancel the Task cts.Cancel(); // This is an asynchronous request, the Task may have completed already</p><p>try { // If the task got canceled, Result will throw an AggregateException Console.WriteLine(\"The sum is: \" + t.Result); // An Int32 value</p><p>} catch (AggregateException x) {</p><p>// Consider any OperationCanceledException objects as handled. // Any other exceptions cause a new AggregateException containing // only the unhandled exceptions to be thrown x.Handle(e =&gt; e is OperationCanceledException);</p><p>}</p><p>// If all the exceptions were handled, the following executes Console.WriteLine(\"Sum was canceled\");</p><p>When creating a Task, you can associate a CancellationToken with it by passing it to Task\u2019s constructor (as shown in the preceding code). If the CancellationToken gets canceled before the Task is scheduled, the Task gets canceled and never executes at all.2 But if the Task has already been scheduled (by calling the Start method), then the Task\u2019s code must explicitly support cancellation if it allows its operation to be canceled while executing. Unfortunately, while a Task object has a CancellationToken associated with it, there is no way to access it, so you must somehow get the same CancellationToken that was used to create the Task object into the Task\u2019s code itself. The easiest way to write this code is to use a lambda expression and \u201cpass\u201d the CancellationToken as a closure variable (as I\u2019ve done in the previous code example).</p><h6>Starting a New Task Automatically When Another Task Completes</h6><p>In order to write scalable software, you must not have your threads block. This means that calling Wait or querying a task\u2019s Result property when the task has not yet finished running will most likely cause the thread pool to create a new thread, which increases resource usage and hurts performance. Fortunately, there is a better way to find out when a task has completed running. When a task completes, it can start another task. Here is a rewrite of the earlier code that doesn\u2019t block any threads.</p><p>// Create and start a Task, continue with another task Task&lt;Int32&gt; t = Task.Run(() =&gt; Sum(CancellationToken.None, 10000));</p><p>// ContinueWith returns a Task but you usually don't care Task cwt = t.ContinueWith(task =&gt; Console.WriteLine(\"The sum is: \" + task.Result));</p><p>Now, when the task executing Sum completes, this task will start another task (also on some thread pool thread) that displays the result. The thread that executes the preceding code does not block waiting for either of these two tasks to complete; the thread is allowed to execute other code or, if it is a thread pool thread itself, it can return to the pool to perform other operations. Note that the task executing Sum could complete before ContinueWith is called. This will not be a problem because the ContinueWith method will see that the Sum task is complete and it will immediately start the task that displays the result.</p><p>Also, note that ContinueWith returns a reference to a new Task object (which my code placed in</p><p>the cwt variable). Of course, you can invoke various members (like Wait, Result, or even ContinueWith) using this Task object, but usually you will ignore this Task object and will not save a reference to it in a variable.</p><p>I should also mention that Task objects internally contain a collection of ContinueWith tasks. So you can actually call ContinueWith several times using a single Task object. When the task completes, all the ContinueWith tasks will be queued to the thread pool. In addition, when calling ContinueWith, you can specify a bitwise OR\u2019d set of TaskContinuationOptions. The first six flags\u2014None, PreferFairness, LongRunning, AttachedToParent, DenyChildAttach, and</p><p>2 By the way, if you try to cancel a task before it is even started, an InvalidOperationException is thrown.</p><p>HideScheduler\u2014are identical to the flags offered by the TaskCreationOptions enumerated type shown earlier. Here is what the TaskContinuationOptions type looks like.</p><p>[Flags, Serializable] public enum TaskContinuationOptions {</p><p>None = 0x0000,// The default</p><p>// Hints to the TaskScheduler that you want this task to run sooner than later. PreferFairness = 0x0001,</p><p>// Hints to the TaskScheduler that it should more aggressively create thread pool threads. LongRunning = 0x0002,</p><p>// Always honored: Associates a Task with its parent Task (discussed shortly) AttachedToParent = 0x0004,</p><p>// If a task attempts to attach to this parent task, an InvalidOperationException is thrown. DenyChildAttach = 0x0008,</p><p>// Forces child tasks to use the default scheduler as opposed to the parent\u2019s scheduler. HideScheduler = 0x0010,</p><p>// Prevents completion of the continuation until the antecedent has completed. LazyCancellation = 0x0020,</p><p>// This flag indicates that you want the thread that executed the first task to also // execute the ContinueWith task. If the first task has already completed, then the // thread calling ContinueWith will execute the ContinueWith task. ExecuteSynchronously = 0x80000,</p><p>// These flags indicate under what circumstances to run the ContinueWith task NotOnRanToCompletion = 0x10000, NotOnFaulted = 0x20000, NotOnCanceled = 0x40000,</p><p>}</p><p>// These flags are convenient combinations of the above three flags OnlyOnCanceled = NotOnRanToCompletion | NotOnFaulted, OnlyOnFaulted = NotOnRanToCompletion | NotOnCanceled, OnlyOnRanToCompletion = NotOnFaulted | NotOnCanceled,</p><p>When you call ContinueWith, you can indicate that you want the new task to execute only if the first task is canceled by specifying the TaskContinuationOptions.OnlyOnCanceled flag. Similarly, you have the new task execute only if the first task throws an unhandled exception using the TaskContinuationOptions.OnlyOnFaulted flag. And, of course, you can use the TaskContinuationOptions.OnlyOnRanToCompletion flag to have the new task execute only if the first task runs all the way to completion without being canceled or throwing an unhandled exception. By default, if you do not specify any of these flags, then the new task will run regardless of how the first task completes. When a Task completes, any of its continue-with tasks that do not run are automatically canceled. Here is an example that puts all of this together.</p><p>// Create and start a Task, continue with multiple other tasks Task&lt;Int32&gt; t = Task.Run(() =&gt; Sum(10000)); // Each ContinueWith returns a Task but you usually don't care</p><p>t.ContinueWith(task =&gt; Console.WriteLine(\"The sum is: \" + task.Result), TaskContinuationOptions.OnlyOnRanToCompletion);</p><p>t.ContinueWith(task =&gt; Console.WriteLine(\"Sum threw: \" + task.Exception.InnerException), TaskContinuationOptions.OnlyOnFaulted);</p><p>t.ContinueWith(task =&gt; Console.WriteLine(\"Sum was canceled\"), TaskContinuationOptions.OnlyOnCanceled);</p><h6>A Task May Start Child Tasks</h6><p>Finally, tasks support parent/child relationships, as demonstrated by the following code.</p><p>Task&lt;Int32[]&gt; parent = new Task&lt;Int32[]&gt;(() =&gt; { var results = new Int32[3]; // Create an array for the results</p><p>// This tasks creates and starts 3 child tasks</p><ul style=\"list-style-type: none;\">\n<li><p>new Task(() =&gt; results[0] = Sum(10000), TaskCreationOptions.AttachedToParent).Start();</p></li>\n<li><p>new Task(() =&gt; results[1] = Sum(20000), TaskCreationOptions.AttachedToParent).Start();</p></li>\n<li><p>new Task(() =&gt; results[2] = Sum(30000), TaskCreationOptions.AttachedToParent).Start();</p></li>\n</ul><p>// Returns a reference to the array (even though the elements may not be initialized yet) return results;</p><p>});</p><p>// When the parent and its children have run to completion, display the results var cwt = parent.ContinueWith(</p><p>parentTask =&gt; Array.ForEach(parentTask.Result, Console.WriteLine));</p><p>// Start the parent Task so it can start its children parent.Start();</p><p>Here, the parent task creates and starts three Task objects. By default, Task objects created by another task are top-level tasks that have no relationship to the task that creates them. However, the TaskCreationOptions.AttachedToParent flag associates a Task with the Task that creates it so that the creating task is not considered finished until all its children (and grandchildren) have finished running. When creating a Task by calling the ContinueWith method, you can make the continuewith task be a child by specifying the TaskContinuationOptions.AttachedToParent flag.</p><h6>Inside a Task</h6><p>Each Task object has a set of fields that make up the task\u2019s state. There is an Int32 ID (see Task\u2019s read-only Id property), an Int32 representing the execution state of the Task, a reference to the parent task, a reference to the TaskScheduler specified when the Task was created, a reference to the callback method, a reference to the object that is to be passed to the callback method (queryable via Task\u2019s read-only AsyncState property), a reference to an ExecutionContext, and a reference to a ManualResetEventSlim object. In addition, each Task object has a reference to some supplementary state that is created on demand. The supplementary state includes a Cancellation\u00ad</p><p>Token, a collection of ContinueWithTask objects, a collection of Task objects for child tasks that have thrown unhandled exceptions, and more. My point is that although tasks provide you a lot of</p><p>features, there is some cost to tasks because memory must be allocated for all this state. If you don\u2019t need the additional features offered by tasks, then your program will use resources more efficiently if you use ThreadPool.QueueUserWorkItem.</p><p>The Task and Task&lt;TResult&gt; classes implement the IDisposable interface, allowing you to call Dispose when you are done with the Task object. Today, all the Dispose method does is close the ManualResetEventSlim object. However, it is possible to define classes derived from Task and Task&lt;TResult&gt;, and these classes could allocate their own resources, which would be freed in their override of the Dispose method. I recommend that developers not explicitly call Dispose on a Task object in their code; instead, just let the garbage collector clean up any resources when it determines that they are no longer in use.</p><p>You\u2019ll notice that each Task object contains an Int32 field representing a Task\u2019s unique ID. When you create a Task object, the field is initialized to zero. Then the first time you query Task\u2019s read-only Id property, the property assigns a unique Int32 value to this field and returns it from the property. Task IDs start at 1 and increment by 1 as each ID is assigned. Just looking at a Task object in the Microsoft Visual Studio debugger will cause the debugger to display the Task\u2019s ID, forcing the Task to be assigned an ID.</p><p>The idea behind the ID is that each Task can be identified by a unique value. In fact, Visual Studio shows you these task IDs in its Parallel Tasks and Parallel Stacks windows. But because you don\u2019t assign the IDs yourself in your code, it is practically impossible to correlate an ID number with what your code is doing. While running a task\u2019s code, you can query Task\u2019s static CurrentId property, which returns a nullable Int32 (Int32?). You can also call this from Visual Studio\u2019s Watch window or Immediate window while debugging to get the ID for the code that you are currently stepping through. Then you can find your task in the Parallel Tasks/Stacks windows. If you query the CurrentId property while a task is not executing, it returns null.</p><p>During a Task object\u2019s existence, you can learn where it is in its lifecycle by querying Task\u2019s readonly Status property. This property returns a TaskStatus value that is defined as follows.</p><p>public enum TaskStatus { // These flags indicate the state of a Task during its lifetime: Created, // Task created explicitly; you can manually Start() this task WaitingForActivation,// Task created implicitly; it starts automatically</p><p>WaitingToRun, // The task was scheduled but isn\u2019t running yet Running, // The task is actually running</p><p>// The task is waiting for children to complete before it considers itself complete WaitingForChildrenToComplete,</p><p>}</p><p>// A task's final state is one of these: RanToCompletion, Canceled, Faulted</p><p>When you first construct a Task object, its status is Created. Later, when the task is started, its status changes to WaitingToRun. When the Task is actually running on a thread, its status changes</p><p>to Running. When the task stops running and is waiting for any child tasks, the status changes to WaitingForChildrenToComplete. When a task is completely finished, it enters one of three final states: RanToCompletion, Canceled, or Faulted. When a Task&lt;TResult&gt; runs to completion, you can query the task\u2019s result via Task&lt;TResult&gt;\u2019s Result property. When a Task or Task&lt;TResult&gt; faults, you can obtain the unhandled exception that the task threw by querying Task\u2019s Exception property; which always returns an AggregateException object whose collection contains the set of unhandled exceptions.</p><p>For convenience, Task offers several read-only, Boolean properties: IsCanceled, IsFaulted, and IsCompleted. Note that IsCompleted returns true when the Task is in the RanToCompletion, Canceled, or Faulted state. The easiest way to determine if a Task completed successfully is to use code like the following.</p><p>if (task.Status == TaskStatus.RanToCompletion) ...</p><p>A Task object is in the WaitingForActivation state if that Task is created by calling one of these functions: ContinueWith, ContinueWhenAll, ContinueWhenAny, or FromAsync. A Task created by constructing a TaskCompletionSource&lt;TResult&gt; object is also created in the WaitingForActivation state. This state means that the Task\u2019s scheduling is controlled by the task infrastructure. For example, you cannot explicitly start a Task object that was created by calling ContinueWith. This Task will start automatically when its antecedent task has finished executing.</p><h6>Task Factories</h6><p>Occasionally, you might want to create a bunch of Task objects that share the same configuration. To keep you from having to pass the same parameters to each Task\u2019s constructor over and over again, you can create a task factory that encapsulates the common configuration. The System.Threading. Tasks namespace defines a TaskFactory type as well as a TaskFactory&lt;TResult&gt; type. Both of these types are derived from System.Object; that is, they are peers of each other.</p><p>If you want to create a bunch of tasks that return void, then you will construct a TaskFactory. If you want to create a bunch of tasks that have a specific return type, then you will construct a TaskFactory&lt;TResult&gt; where you pass the task\u2019s desired return type for the generic TResult argument. When you create one of these task factory classes, you pass to its constructor the defaults that you want the tasks that the factory creates to have. Specifically, you pass to the task factory the CancellationToken, TaskScheduler, TaskCreationOptions, and TaskContinuationOptions settings that you want factory-created tasks to have.</p><p>Here is some sample code demonstrating the use of a TaskFactory.</p><p>Task parent = new Task(() =&gt; { var cts = new CancellationTokenSource(); var tf = new TaskFactory&lt;Int32&gt;(cts.Token, TaskCreationOptions.AttachedToParent,</p><p>TaskContinuationOptions.ExecuteSynchronously, TaskScheduler.Default);</p><p>// This task creates and starts 3 child tasks var childTasks = new[] {</p><p>tf.StartNew(() =&gt; Sum(cts.Token, 10000)),</p><p>tf.StartNew(() =&gt; Sum(cts.Token, 20000)), tf.StartNew(() =&gt; Sum(cts.Token, Int32.MaxValue)) // Too big, throws OverflowException</p><p>};</p><p>// If any of the child tasks throw, cancel the rest of them for (Int32 task = 0; task &lt; childTasks.Length; task++)</p><p>childTasks[task].ContinueWith( t =&gt; cts.Cancel(), TaskContinuationOptions.OnlyOnFaulted);</p><p>// When all children are done, get the maximum value returned from the // non\u00adfaulting/canceled tasks. Then pass the maximum value to another // task that displays the maximum result tf.ContinueWhenAll(</p><p>childTasks, completedTasks =&gt;</p><p>completedTasks.Where(t =&gt; t.Status == TaskStatus.RanToCompletion).Max(t =&gt; t.Result), CancellationToken.None)</p><p>.ContinueWith(t =&gt;Console.WriteLine(\"The maximum is: \" + t.Result),</p><p>TaskContinuationOptions.ExecuteSynchronously); });</p><p>// When the children are done, show any unhandled exceptions too parent.ContinueWith(p =&gt; {</p><p>// I put all this text in a StringBuilder and call Console.WriteLine just once // because this task could execute concurrently with the task above &amp; I don't // want the tasks' output interspersed StringBuilder sb = new StringBuilder(</p><p>\"The following exception(s) occurred:\" + Environment.NewLine);</p><p>foreach (var e in p.Exception.Flatten().InnerExceptions)</p><p>sb.AppendLine(\" \"+ e.GetType().ToString()); Console.WriteLine(sb.ToString());</p><p>}, TaskContinuationOptions.OnlyOnFaulted);</p><p>// Start the parent Task so it can start its children parent.Start();</p><p>With this code, I am creating a TaskFactory&lt;Int32&gt; object that I will use to create three Task objects. I want to configure the child tasks all the same way: each Task object shares the same CancellationTokenSource token, tasks are considered children of their parent, all continue-with tasks created by the TaskFactory execute synchronously, and all Task objects created by this TaskFactory use the default TaskScheduler.</p><p>Then I create an array consisting of the three child Task objects, all created by calling TaskFactory\u2019s StartNew method. This method conveniently creates and starts each child task. In a loop, I tell each child task that throws an unhandled exception to cancel all the other child tasks that are still running. Finally, using the TaskFactory, I call ContinueWhenAll, which creates a Task that runs when all the child tasks have completed running. Because this task is created with the TaskFactory, it will also be considered a child of the parent task and it will execute synchronously using the default TaskScheduler. However, I want this task to run even if the other child tasks were canceled, so I override the TaskFactory\u2019s CancellationToken by passing in CancellationToken. None, which prevents this task from being cancelable at all. Finally, when the task that processes all</p><p>the results is complete, I create another task that displays the highest value returned from all the child tasks.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile238.png\"/><p>Note When calling TaskFactory\u2019s or TaskFactory&lt;TResult&gt;\u2019s ContinueWhenAll and ContinueWhenAny methods, the following TaskContinuationOption flags are illegal: NotOnRanToCompletion, NotOnFaulted, and NotOnCanceled. And of course, the conve-</p><p>nience flags (OnlyOnCanceled, OnlyOnFaulted, and OnlyOnRanToCompletion) are also illegal. That is, ContinueWhenAll and ContinueWhenAny execute the continue-with task regardless of how the antecedent tasks complete.</p><h6>Task Schedulers</h6><p>The task infrastructure is very flexible, and TaskScheduler objects are a big part of this flexibility. A TaskScheduler object is responsible for executing scheduled tasks and also exposes task information to the Visual Studio debugger. The FCL ships with two TaskScheduler-derived types: the thread pool task scheduler and a synchronization context task scheduler. By default, all applications use the thread pool task scheduler. This task scheduler schedules tasks to the thread pool\u2019s worker threads and is discussed in more detail in this chapter\u2019s \u201cHow the Thread Pool Manages Its Threads\u201d section. You can get a reference to the default task scheduler by querying TaskScheduler\u2019s static Default property.</p><p>The synchronization context task scheduler is typically used for applications sporting a graphical user interface, such as Windows Forms, Windows Presentation Foundation (WPF), Silverlight, and Windows Store applications. This task scheduler schedules all tasks onto the application\u2019s GUI thread so that all the task code can successfully update UI components like buttons, menu items, and so on. The synchronization context task scheduler does not use the thread pool at all. You can get a reference to a synchronization context task scheduler by querying TaskScheduler\u2019s static FromCurrentSynchronizationContext method.</p><p>Here is a simple Windows Forms application that demonstrates the use of the synchronization context task scheduler.</p><p>internal sealed class MyForm : Form { private readonly TaskScheduler m_syncContextTaskScheduler; public MyForm() {</p><p>// Get a reference to a synchronization context task scheduler m_syncContextTaskScheduler = TaskScheduler.FromCurrentSynchronizationContext();</p><p>}</p><p>Text = \"Synchronization Context Task Scheduler Demo\"; Visible = true; Width = 600; Height = 100;</p><p>private CancellationTokenSource m_cts;</p><p>protected override void OnMouseClick(MouseEventArgs e) { if (m_cts != null) { // An operation is in flight, cancel it m_cts.Cancel();</p><p>m_cts = null;</p><p>} else { // An operation is not in flight, start it Text = \"Operation running\"; m_cts = new CancellationTokenSource();</p><p>// This task uses the default task scheduler and executes on a thread pool thread Task&lt;Int32&gt; t = Task.Run(() =&gt; Sum(m_cts.Token, 20000), m_cts.Token);</p><p>// These tasks use the sync context task scheduler and execute on the GUI thread t.ContinueWith(task =&gt; Text = \"Result: \" + task.Result,</p><p>CancellationToken.None, TaskContinuationOptions.OnlyOnRanToCompletion, m_syncContextTaskScheduler);</p><p>t.ContinueWith(task =&gt; Text = \"Operation canceled\", CancellationToken.None, TaskContinuationOptions.OnlyOnCanceled, m_syncContextTaskScheduler);</p><p>} }</p><p>t.ContinueWith(task =&gt; Text = \"Operation faulted\", CancellationToken.None, TaskContinuationOptions.OnlyOnFaulted, m_syncContextTaskScheduler);</p><p>} base.OnMouseClick(e);</p><p>When you click in the client area of this form, a compute-bound task will start executing on a thread pool thread. This is good because the GUI thread is not blocked during this time and can therefore respond to other UI operations. However, the code executed by the thread pool thread should not attempt to update UI components or else an InvalidOperationException will be thrown.</p><p>When the compute-bound task is done, one of the three continue-with tasks will execute. These tasks are all issued against the synchronization context task scheduler corresponding to the GUI thread, and this task scheduler queues the tasks to the GUI thread, allowing the code executed by these tasks to update UI components successfully. All of these tasks update the form\u2019s caption via the inherited Text property.</p><p>Because the compute-bound work (Sum) is running on a thread pool thread, the user can interact with the UI to cancel the operation. In my simple code example, I allow the user to cancel the operation by clicking in the form\u2019s client area while an operation is running.</p><p>You can, of course, define your own class derived from TaskScheduler if you have special task scheduling needs. Microsoft has provided a bunch of sample code for tasks and includes the source code for a bunch of task schedulers in the Parallel Extensions Extras package, which can be downloaded from here: http://code.msdn.microsoft.com/ParExtSamples. Here are some of the task schedulers included in this package:</p><p>\u25a0\u25a0 IOTaskScheduler This task scheduler queues tasks to the thread pool\u2019s I/O threads instead</p><p>of its worker threads.</p><h6>\u25a0\u25a0 LimitedConcurrencyLevelTaskScheduler This task scheduler allows no more than n (a</h6><p>constructor parameter) tasks to execute simultaneously.</p><p>\u25a0\u25a0 OrderedTaskScheduler This task scheduler allows only one task to execute at a time. This</p><p>class is derived from LimitedConcurrencyLevelTaskScheduler and just passes 1 for n.</p><p>\u25a0\u25a0 PrioritizingTaskScheduler This task scheduler queues tasks to the CLR\u2019s thread pool. After this has occurred, you can call Prioritize to indicate that a Task should be processed before all normal tasks (if it hasn\u2019t been processed already). You can call Deprioritize to make a Task be processed after all normal tasks.</p><p>\u25a0\u25a0 ThreadPerTaskScheduler This task scheduler creates and starts a separate thread for each</p><p>task; it does not use the thread pool at all.</p><h6>Parallel\u2019s Static For, ForEach, and Invoke Methods</h6><p>There are some common programming scenarios that can potentially benefit from the improved performance possible with tasks. To simplify programming, the static System.Threading.Tasks. Parallel class encapsulates these common scenarios while using Task objects internally. For example, instead of processing all the items in a collection like this.</p><p>// One thread performs all this work sequentially for (Int32 i = 0; i &lt; 1000; i++) DoWork(i);</p><p>you can instead get multiple thread pool threads to assist in performing this work by using the Parallel class\u2019s For method.</p><p>// The thread pool\u2019s threads process the work in parallel Parallel.For(0, 1000, i =&gt; DoWork(i));</p><p>Similarly, if you have a collection, instead of doing this:</p><p>// One thread performs all this work sequentially foreach (var item in collection) DoWork(item);</p><p>you can do this.</p><p>// The thread pool's threads process the work in parallel Parallel.ForEach(collection, item =&gt; DoWork(item));</p><p>If you can use either For or ForEach in your code, then it is recommended that you use For because it executes faster.</p><p>And finally, if you have several methods that you need to execute, you could execute them all sequentially, like this:</p><p>// One thread executes all the methods sequentially Method1(); Method2(); Method3();</p><p>or you could execute them in parallel, like this.</p><p>// The thread pool\u2019s threads execute the methods in parallel Parallel.Invoke(</p><p>() =&gt; Method1(), () =&gt; Method2(), () =&gt; Method3());</p><p>All of Parallel\u2019s methods have the calling thread participate in the processing of the work, which is good in terms of resource usage because we wouldn\u2019t want the calling thread to just suspend itself while waiting for thread pool threads to do all the work. However, if the calling thread finishes its work before the thread pool threads complete their part of the work, then the calling thread will suspend itself until all the work is done, which is also good because this gives you the same semantics as you\u2019d have when using a for or foreach loop: the thread doesn\u2019t continue running until all the work is done. Also note that if any operation throws an unhandled exception, the Parallel method you called will ultimately throw an AggregateException.</p><p>Of course, you should not go through all your source code replacing for loops with calls to Parallel.For and foreach loops with calls to Parallel.ForEach. When calling the Parallel method, there is an assumption that it is OK for the work items to be performed concurrently. Therefore, do not use the Parallel methods if the work must be processed in sequential order. Also, avoid work items that modify any kind of shared data because the data could get corrupted if it is manipulated by multiple threads simultaneously. Normally, you would fix this by adding thread synchronization locks around the data access, but if you do this, then one thread at a time can access the data and you would lose the benefit of processing multiple items in parallel.</p><p>In addition, there is overhead associated with the Parallel methods; delegate objects have to be allocated, and these delegates are invoked once for each work item. If you have lots of work items that can be processed by multiple threads, then you might gain a performance increase. Also, if you have lots of work to do for each item, then the performance hit of calling through the delegate is negligible. You will actually hurt your performance if you use the Parallel methods for just a few work items or for work items that are processed very quickly.</p><p>I should mention that Parallel\u2019s For, ForEach, and Invoke methods all have overloads that accept a ParallelOptions object, which looks like this.</p><p>public class ParallelOptions{ public ParallelOptions();</p><p>// Allows cancellation of the operation public CancellationToken CancellationToken { get; set; } // Default=CancellationToken.None</p><p>// Allows you to specify the maximum number of work items // that can be operated on concurrently public Int32 MaxDegreeOfParallelism { get; set; } // Default=\u00ad1 (# of available CPUs)</p><p>}</p><p>// Allows you to specify which TaskScheduler to use public TaskScheduler TaskScheduler { get; set; } // Default=TaskScheduler.Default</p><p>In addition, there are overloads of the For and ForEach methods that let you pass three delegates:</p><p>\u25a0\u25a0 The task local initialization delegate (localInit) is invoked once for each task participating in</p><p>the work. This delegate is invoked before the task is asked to process a work item.</p><p>\u25a0\u25a0 The body delegate (body) is invoked once for each item being processed by the various</p><p>threads participating in the work.</p><p>\u25a0\u25a0 The task local finally delegate (localFinally) is invoked once for each task participating in the work. This delegate is invoked after the task has processed all the work items that will be dispatched to it. It is even invoked if the body delegate code experiences an unhandled exception.</p><p>Here is some sample code that demonstrates the use of the three delegates by adding up the bytes for all files contained within a directory.</p><p>private static Int64 DirectoryBytes(String path, String searchPattern, SearchOption searchOption) { var files = Directory.EnumerateFiles(path, searchPattern, searchOption); Int64 masterTotal = 0;</p><p>ParallelLoopResult result = Parallel.ForEach&lt;String, Int64&gt;( files,</p><p>() =&gt; { // localInit: Invoked once per task at start // Initialize that this task has seen 0 bytes return 0; // Set taskLocalTotal initial value to 0</p><p>},</p><p>(file, loopState, index, taskLocalTotal) =&gt; { // body: Invoked once per work item // Get this file's size and add it to this task's running total Int64 fileLength = 0; FileStream fs = null; try {</p><p>fs = File.OpenRead(file); fileLength = fs.Length;</p><p>} catch (IOException) { /* Ignore any files we can't access */ } finally { if (fs != null) fs.Dispose(); } return taskLocalTotal + fileLength;</p><p>},</p><p>taskLocalTotal =&gt; { // localFinally: Invoked once per task at end // Atomically add this task's total to the \"master\" total Interlocked.Add(ref masterTotal, taskLocalTotal);</p><p>});</p><p>return masterTotal; }</p><p>Each task maintains its own running total (in the taskLocalTotal variable) for the files that it is given. As each task completes its work, the master total is updated in a thread-safe way by calling the Interlocked.Add method (discussed in Chapter 29, \u201cPrimitive Thread Synchronization Constructs\u201d). Because each task has its own running total, no thread synchronization is required during the processing of the items. Because thread synchronization would hurt performance, not requiring thread synchronization is good. It\u2019s only after each task returns that masterTotal has to be updated in a thread-safe way, so the performance hit of calling Interlocked.Add occurs only once per task instead of once per work item.</p><p>You\u2019ll notice that the body delegate is passed a ParallelLoopState object, which looks like this.</p><p>public class ParallelLoopState{ public void Stop(); public Boolean IsStopped { get; }</p><p>public void Break(); public Int64? LowestBreakIteration{ get; }</p><p>}</p><p>public Boolean IsExceptional { get; } public Boolean ShouldExitCurrentIteration { get; }</p><p>Each task participating in the work gets its own ParallelLoopState object, and it can use this object to interact with the other task participating in the work. The Stop method tells the loop to stop processing any more work, and future querying of the IsStopped property will return true. The Break method tells the loop to stop processing any items that are beyond the current item. For example, let\u2019s say that ForEach is told to process 100 items and Break is called while processing the fifth item, then the loop will make sure that the first five items are processed before ForEach returns. Note, however, that additional items may have been processed. The LowestBreakIteration property returns the lowest item number whose processing called the Break method. The LowestBreakIteration property returns null if Break was never called.</p><p>The IsException property returns true if the processing of any item resulted in an unhandled exception. If the processing of an item takes a long time, your code can query the ShouldExitCurrentIteration property to see if it should exit prematurely. This property returns true if Stop was called, Break was called, the CancellationTokenSource (referred to by the ParallelOption\u2019s CancellationToken property) is canceled, or if the processing of an item resulted in an unhandled exception.</p><p>Parallel\u2019s For and ForEach methods both return a ParallelLoopResult instance, which looks like this.</p><p>public struct ParallelLoopResult { // Returns false if the operation was ended prematurely public Boolean IsCompleted { get; } public Int64? LowestBreakIteration{ get; }</p><p>}</p><p>You can examine the properties to determine the result of the loop. If IsCompleted returns true, then the loop ran to completion and all the items were processed. If IsCompleted is false and LowestBreakIteration is null, then some thread participating in the work called the Stop method. If IsCompleted is false and LowestBreakIteration is not null, then some thread participating in the work called the Break method and the Int64 value returned from LowestBreakIteration indicates the index of the lowest item guaranteed to have been processed. If an exception is thrown, then you should catch an AggregateException in order to recover gracefully.</p><h6>Parallel Language Integrated Query</h6><p>Microsoft\u2019s Language Integrated Query (LINQ) feature offers a convenient syntax for performing queries over collections of data. Using LINQ, you can easily filter items, sort items, return a projected set of items, and much more. When you use LINQ to Objects, only one thread processes all the items in your data collection sequentially; we call this a sequential query. You can potentially improve the performance of this processing by using Parallel LINQ, which can turn your sequential query into a parallel query, which internally uses tasks (queued to the default TaskScheduler) to spread the processing of the collection\u2019s items across multiple CPUs so that multiple items are processed concurrently. Like Parallel\u2019s methods, you will get the most benefit from Parallel LINQ if you have many items to process or if the processing of each item is a lengthy compute-bound operation.</p><p>The static System.Linq.ParallelEnumerable class (defined in System.Core.dll) implements all of the Parallel LINQ functionality, and so you must import the System.Linq namespace into your source code via C#\u2019s using directive. In particular, this class exposes parallel versions of all the standard LINQ operators such as Where, Select, SelectMany, GroupBy, Join, OrderBy, Skip, Take, and so on. All of these methods are extension methods that extend the System.Linq. ParallelQuery&lt;T&gt; type. To have your LINQ to Objects query invoke the parallel versions of these methods, you must convert your sequential query (based on IEnumerable or IEnumerable&lt;T&gt;) to a parallel query (based on ParallelQuery or ParallelQuery&lt;T&gt;) using ParallelEnumerable\u2019s AsParallel extension method, which looks like this.3</p><p>public static ParallelQuery&lt;TSource&gt; AsParallel&lt;TSource&gt;(this IEnumerable&lt;TSource&gt; source) public static ParallelQuery AsParallel(this IEnumerable source)</p><p>Here is an example of a sequential query that has been converted to a parallel query. This query returns all the obsolete methods defined within an assembly.</p><p>private static void ObsoleteMethods(Assembly assembly) { var query = from type in assembly.GetExportedTypes().AsParallel()</p><p>from method in type.GetMethods(BindingFlags.Public | BindingFlags.Instance | BindingFlags.Static)</p><p>let obsoleteAttrType = typeof(ObsoleteAttribute)</p><p>3 The ParallelQuery&lt;T&gt; class is derived from the ParallelQuery class.</p><p>where Attribute.IsDefined(method, obsoleteAttrType)</p><p>orderby type.FullName</p><p>let obsoleteAttrObj = (ObsoleteAttribute) Attribute.GetCustomAttribute(method, obsoleteAttrType)</p><p>select String.Format(\"Type={0}\\nMethod={1}\\nMessage={2}\\n\", type.FullName, method.ToString(), obsoleteAttrObj.Message);</p><p>}</p><p>// Display the results foreach (var result in query) Console.WriteLine(result);</p><p>Although uncommon, within a query you can switch from performing parallel operations back to performing sequential operations by calling ParallelEnumerable\u2019s AsSequential method.</p><p>public static IEnumerable&lt;TSource&gt; AsSequential&lt;TSource&gt;(this ParallelQuery&lt;TSource&gt; source)</p><p>This method basically turns a ParallelQuery&lt;T&gt; back to an IEnumerable&lt;T&gt; so that operations performed after calling AsSequential are performed by just one thread.</p><p>Normally, the resulting data produced by a LINQ query is evaluated by having some thread execute a foreach statement (as shown earlier). This means that just one thread iterates over all the query\u2019s results. If you want to have the query\u2019s results processed in parallel, then you should process the resulting query by using ParallelEnumerable\u2019s ForAll method.</p><p>static void ForAll&lt;TSource&gt;(this ParallelQuery&lt;TSource&gt; source, Action&lt;TSource&gt; action)</p><p>This method allows multiple threads to process the results simultaneously. I could modify my code earlier to use this method as follows.</p><p>// Display the results query.ForAll(Console.WriteLine);</p><p>However, having multiple threads call Console.WriteLine simultaneously actually hurts performance, because the Console class internally synchronizes threads, ensuring that only one at a time can access the console window. This prevents text from multiple threads from being interspersed, making the output unintelligible. Use the ForAll method when you intend to perform calculations on each result.</p><p>Because Parallel LINQ processes items by using multiple threads, the items are processed concurrently and the results are returned in an unordered fashion. If you need to have Parallel LINQ preserve the order of items as they are processed, then you can call ParallelEnumerable\u2019s AsOrdered method. When you call this method, threads will process items in groups and then the groups are merged back together, preserving the order; this will hurt performance. The following operators produce unordered operations: Distinct, Except, Intersect, Union, Join, GroupBy,</p><p>GroupJoin, and ToLookup. If you want to enforce ordering again after one of these operators, just call the AsOrdered method.</p><p>The following operators produce ordered operations: OrderBy, OrderByDescending, ThenBy, and ThenByDescending. If you want to go back to unordered processing again to improve performance after one of these operators, just call the AsUnordered method.</p><p>Parallel LINQ offers some additional ParallelEnumerable methods that you can call to control how the query is processed.</p><p>public static ParallelQuery&lt;TSource&gt; WithCancellation&lt;TSource&gt;( this ParallelQuery&lt;TSource&gt; source, CancellationTokencancellationToken)</p><p>public static ParallelQuery&lt;TSource&gt; WithDegreeOfParallelism&lt;TSource&gt;( this ParallelQuery&lt;TSource&gt; source, Int32degreeOfParallelism)</p><p>public static ParallelQuery&lt;TSource&gt; WithExecutionMode&lt;TSource&gt;( this ParallelQuery&lt;TSource&gt; source, ParallelExecutionModeexecutionMode)</p><p>public static ParallelQuery&lt;TSource&gt; WithMergeOptions&lt;TSource&gt;( this ParallelQuery&lt;TSource&gt; source, ParallelMergeOptionsmergeOptions)</p><p>Obviously, the WithCancellation method allows you to pass a CancellationToken so that the query processing can be stopped prematurely. The WithDegreeOfParallelism method specifies the maximum number of threads allowed to process the query; it does not force the threads to be created if not all of them are necessary. Usually you will not call this method, and, by default, the query will execute using one thread per core. However, you could call WIthDegreeOfParallelism, passing a number that is smaller than the number of available cores if you want to keep some cores available for doing other work. You could also pass a number that is greater than the number of cores if the query performs synchronous I/O operations because threads will be blocking during these operations. This wastes more threads but can produce the final result in less time. You might consider doing this in a client application, but I\u2019d highly recommend against performing synchronous I/O operations in a server application.</p><p>Parallel LINQ analyzes a query and then decides how to best process it. Sometimes processing a query sequentially yields better performance. This is usually true when using any of these operations: Concat, ElementAt(OrDefault), First(OrDefault), Last(OrDefault), Skip(While), Take(While), or Zip. It is also true when using overloads of Select(Many) or Where that pass a position index into your selector or predicate delegate. However, you can force a query to be processed in parallel by calling WithExecutionMode, passing it one of the ParallelExecutionMode flags.</p><p>public enum ParallelExecutionMode { Default = 0, // Let Parallel LINQ decide to best process the query ForceParallelism = 1 // Force the query to be processed in parallel</p><p>}</p><p>As mentioned before, Parallel LINQ has multiple threads processing items, and then the results must be merged back together. You can control how the items are buffered and merged by calling WithMergeOptions, passing it one of the ParallelMergeOptions flags.</p><p>public enum ParallelMergeOptions { Default = 0, // Same as AutoBuffered today (could change in the future) NotBuffered = 1, // Results are processed as ready AutoBuffered = 2, // Each thread buffers some results before processed FullyBuffered = 3 // Each thread buffers all results before processed</p><p>}</p><p>These options basically give you some control over speed versus memory consumption. NotBuffered saves memory but processes items slower. FullyBuffered consumes more memory while running fastest. AutoBuffered is the compromise in between NotBuffered and FullyBuffered. Really, the best way to know which of these to choose for any given query is to try them all and compare their performance results, or just accept the default, which tends to work pretty well for many queries. See the following blog posts for more information about how Parallel LINQ partitions work across CPU cores:</p><ul style=\"list-style-type: none;\">\n<li><p>\u25a0\u25a0 http://blogs.msdn.com/pfxteam/archive/2009/05/28/9648672.aspx</p></li>\n<li><p>\u25a0\u25a0 http://blogs.msdn.com/pfxteam/archive/2009/06/13/9741072.aspx</p></li>\n</ul><h6>Performing a Periodic Compute-Bound Operation</h6><p>The System.Threading namespace defines a Timer class, which you can use to have a thread pool thread call a method periodically. When you construct an instance of the Timer class, you are telling the thread pool that you want a method of yours called back at a future time that you specify. The</p><p>Timer class offers several constructors, all quite similar to each other.</p><p>public sealed class Timer : MarshalByRefObject, IDisposable {</p><p>public Timer(TimerCallback callback, Object state, Int32 dueTime, Int32 period); public Timer(TimerCallback callback, Object state, UInt32 dueTime, UInt32 period); public Timer(TimerCallback callback, Object state, Int64 dueTime, Int64 period); public Timer(TimerCallback callback, Object state, Timespan dueTime, TimeSpan period);</p><p>}</p><p>All four constructors construct a Timer object identically. The callback parameter identifies the method that you want called back by a thread pool thread. Of course, the callback method that you write must match the System.Threading.TimerCallback delegate type, which is defined as follows.</p><p>delegate void TimerCallback(Object state);</p><p>The constructor\u2019s state parameter allows you to pass state data to the callback method each time it is invoked; you can pass null if you have no state data to pass. You use the dueTime parameter to tell the CLR how many milliseconds to wait before calling your callback method for the very first time. You can specify the number of milliseconds by using a signed or unsigned 32-bit value, a signed 64bit value, or a TimeSpan value. If you want the callback method called immediately, specify 0 for the dueTime parameter. The last parameter, period, allows you to specify how long, in milliseconds, to wait before each successive call to the callback method. If you pass Timeout.Infinite (\u00ad1) for this parameter, a thread pool thread will call the callback method just once.</p><p>Internally, the thread pool has just one thread that it uses for all Timer objects. This thread knows when the next Timer object\u2019s time is due. When the next Timer object is due, the thread wakes up, and internally calls ThreadPool\u2019s QueueUserWorkItem to enter an entry into the thread pool\u2019s queue, causing your callback method to get called. If your callback method takes a long time to execute, the timer could go off again. This could cause multiple thread pool threads to be executing your callback method simultaneously. To work around this problem, I recommend the following: construct the Timer specifying Timeout.Infinite for the period parameter. Now, the timer will fire only once. Then, in your callback method, call the Change method specifying a new due time and again specify Timeout.Infinite for the period parameter. Here is what the Change method overloads look like.</p><p>public sealed class Timer : MarshalByRefObject, IDisposable {</p><p>public Boolean Change(Int32 dueTime, Int32 period); public Boolean Change(UInt32 dueTime, UInt32 period); public Boolean Change(Int64 dueTime, Int64 period); public Boolean Change(TimeSpan dueTime, TimeSpan period);</p><p>}</p><p>The Timer class also offers a Dispose method that allows you to cancel the timer altogether and optionally signal the kernel object identified by the notifyObject parameter when all pending callbacks for the time have completed. Here is what the Dispose method overloads look like.</p><p>public sealed class Timer : MarshalByRefObject, IDisposable { public Boolean Dispose(); public Boolean Dispose(WaitHandle notifyObject);</p><p>}</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile239.png\"/><p>Important When a Timer object is garbage collected, its finalization code tells the thread pool to cancel the timer so that it no longer goes off. So when using a Timer object, make sure that a variable is keeping the Timer object alive or else your callback method will stop getting called. This is discussed and demonstrated in the \u201cGarbage Collections and Debugging\u201d section in Chapter 21, \u201cThe Managed Heap and Garbage Collection.\u201d</p><p>The following code demonstrates how to have a thread pool thread call a method starting immediately and then every two seconds thereafter.</p><p>internal static class TimerDemo { private static Timer s_timer;</p><p>public static void Main() { Console.WriteLine(\"Checking status every 2 seconds\");</p><p>// Create the Timer ensuring that it never fires. This ensures that // s_timer refers to it BEFORE Status is invoked by a thread pool thread s_timer = new Timer(Status, null, Timeout.Infinite, Timeout.Infinite);</p><p>// Now that s_timer is assigned to, we can let the timer fire knowing // that calling Change in Status will not throw a NullReferenceException s_timer.Change(0, Timeout.Infinite);</p><p>Console.ReadLine(); // Prevent the process from terminating }</p><p>// This method's signature must match the TimerCallback delegate private static void Status(Object state) {</p><p>// This method is executed by a thread pool thread Console.WriteLine(\"In Status at {0}\", DateTime.Now); Thread.Sleep(1000); // Simulates other work (1 second)</p><p>// Just before returning, have the Timer fire again in 2 seconds s_timer.Change(2000, Timeout.Infinite);</p><p>} }</p><p>// When this method returns, the thread goes back // to the pool and waits for another work item</p><p>If you have an operation you want performed periodically, there is another way you can structure your code by taking advantage of Task\u2019s static Delay method along with C#\u2019s async and await keywords (discussed extensively in Chapter 28). Here is a rewrite of the preceding code demonstrating this.</p><p>internal static class DelayDemo { public static void Main() { Console.WriteLine(\"Checking status every 2 seconds\"); Status(); Console.ReadLine(); // Prevent the process from terminating</p><p>}</p><p>// This method can take whatever parameters you desire private static async void Status() {</p><p>while (true) { Console.WriteLine(\"Checking status at {0}\", DateTime.Now); // Put code to check status here...</p><p>}</p><p>} }</p><p>// At end of loop, delay 2 seconds without blocking a thread await Task.Delay(2000); // await allows thread to return // After 2 seconds, some thread will continue after await to loop around</p><h6>So Many Timers, So Little Time</h6><p>Unfortunately, the FCL actually ships with several timers, and it is not clear to most programmers what makes each timer unique. Let me attempt to explain:</p><p>\u25a0\u25a0 System.Threading\u2019s Timer class This is the timer discussed in the previous section, and it is the best timer to use when you want to perform periodic background tasks on a thread pool thread.</p><p>\u25a0\u25a0 System.Windows.Forms\u2019s Timer class Constructing an instance of this class tells Windows to associate a timer with the calling thread (see the Win32 SetTimer function). When this timer goes off, Windows injects a timer message (WM_TIMER) into the thread\u2019s message queue. The thread must execute a message pump that extracts these messages and dispatches them to the desired callback method. Notice that all of the work is done by just one thread\u2014the thread that sets the timer is guaranteed to be the thread that executes the callback method. This also means that your timer method will not be executed by multiple threads concurrently.</p><h6>\u25a0\u25a0 System.Windows.Threading\u2019s DispatcherTimer class This class is the equivalent of the</h6><p>System.Windows.Forms\u2019s Timer class for Silverlight and WPF applications.</p><h6>\u25a0\u25a0 Windows.UI.Xaml\u2019s DispatcherTimer class This class is the equivalent of the System.</h6><p>Windows.Forms\u2019s Timer class for Windows Store apps.</p><p>\u25a0\u25a0 System.Timers\u2019s Timer class This timer is basically a wrapper around System.Threading\u2019s Timer class that causes the CLR to queue events into the thread pool when the timer comes due. The System.Timers.Timer class is derived from System.ComponentModel\u2019s Component class, which allows these timer objects to be placed on a design surface in Visual Studio. Also, it exposes properties and events, allowing it to be used more easily from Visual Studio\u2019s designer. This class was added to the FCL years ago while Microsoft was still sorting out the threading and timer stuff. This class probably should have been removed so that everyone would be using the System.Threading.Timer class instead. In fact, I never use the System. Timers.Timer class, and I\u2019d discourage you from using it, too, unless you really want a timer on a design surface.</p><h6>How the Thread Pool Manages Its Threads</h6><p>Now I\u2019d like to talk about how the thread pool code manages worker and I/O threads. However, I don\u2019t want to go into a lot of detail, because the internal implementation has changed greatly over the years with each version of the CLR, and it will continue changing with future versions. It is best to think of the thread pool as a black box. The black box is not perfect for any one application, because it is a general purpose thread-scheduling technology designed to work with a large myriad of applications; it will work better for some applications than for others. It works very well today, and I highly recommend that you trust it, because it would be very hard for you to produce a thread pool that works better than the one shipping in the CLR. And, over time, most applications should improve as the thread pool code internally changes how it manages threads.</p><h6>Setting Thread Pool Limits</h6><p>The CLR allows developers to set a maximum number of threads that the thread pool will create. However, it turns out that thread pools should never place an upper limit on the number of threads in the pool because starvation or deadlock might occur. Imagine queuing 1,000 work items that all block on an event that is signaled by the 1,001st item. If you\u2019ve set a maximum of 1,000 threads, the 1,001st work item won\u2019t be executed, and all 1,000 threads will be blocked forever, forcing end users to terminate the application and lose all their work. Also, it is very unusual for developers to artificially limit the resources that they have available to their application. For example, would you ever start your application and tell the system you\u2019d like to restrict the amount of memory that the application can use or limit the amount of network bandwidth that your application can use? Yet, for some reason, developers feel compelled to limit the number of threads that the thread pool can have.</p><p>Because customers have had starvation and deadlock issues, the CLR team has steadily increased the default maximum number of threads that the thread pool can have. The default maximum is now about 1,000 threads, which is effectively limitless because a 32-bit process has at most 2 GB of usable address space within it. After a bunch of Win32 DLLs load, the CLR DLLs load, the native heap and the managed heap is allocated, there is approximately 1.5 GB of address space left over. Because each thread requires more than 1 MB of memory for its user-mode stack and thread environment block (TEB), the most threads you can get in a 32-bit process is about 1,360. Attempting to create more threads than this will result in an OutOfMemoryException being thrown. Of course, a 64-bit process offers 8 terabytes of address space, so you could theoretically create hundreds of thousands of threads. But allocating anywhere near this number of threads is really just a waste of resources, especially when the ideal number of threads to have is equal to the number of CPUs in the machine. What the CLR team should do is remove the limits entirely, but they can\u2019t do this now because doing so might break some applications that expect thread pool limits to exist.</p><p>The System.Threading.ThreadPool class offers several static methods that you can call to manipulate the number of threads in the thread pool: GetMaxThreads, SetMaxThreads, GetMinThreads, SetMinThreads, and GetAvailableThreads. I highly recommend that you do not call any of these methods. Playing with thread pool limits usually results in making an application perform worse, not better. If you think that your application needs hundreds or thousands of threads, there is something seriously wrong with the architecture of your application and the way that it\u2019s using threads. This chapter and Chapter 28 demonstrate the proper way to use threads.</p><h6>How Worker Threads Are Managed</h6><p>Figure 27-1 shows the various data structures that make up the worker threads' part of the thread pool. The ThreadPool.QueueUserWorkItem method and the Timer class always queue work items to the global queue. Worker threads pull items from this queue using a first-in-first-out (FIFO) algorithm and process them. Because multiple worker threads can be removing items from the global queue simultaneously, all worker threads contend on a thread synchronization lock to ensure that two or more threads don\u2019t take the same work item. This thread synchronization lock can become a bottleneck in some applications, thereby limiting scalability and performance to some degree.</p><table border=\"1\">\n<tr>\n<td><p>Local Queue 1</p>\n<p>Local Queue n</p>\n<p>Global Queue</p>\n<p>Worker Thread 1</p>\n<p>Worker Thread n</p>\n<p>The CLR\u2019s Thread Pool</p>\n</td>\n</tr>\n</table><p>Non-Worker Thread</p><p>FIGURE 27-1 The CLR's thread pool.</p><p>Now let\u2019s talk about Task objects scheduled using the default TaskScheduler (obtained by querying TaskScheduler\u2019s static Default property).4 When a non-worker thread schedules a Task, the Task is added to the global queue. But, each worker thread has its own local queue, and when a worker thread schedules a Task, the Task is added to calling the thread\u2019s local queue.</p><p>When a worker thread is ready to process an item, it always checks its local queue for a Task first. If a Task exists, the worker thread removes the Task from its local queue and processes the item. Note that a worker thread pulls tasks from its local queue by using a last-in-first-out (LIFO) algorithm. Because a worker thread is the only thread allowed to access the head of its own local queue, no thread synchronization lock is required and adding and removing Tasks from the queue is very fast. A side effect of this behavior is that Tasks are executed in the reverse order that they were queued.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile240.png\"/><p>Important Thread pools have never guaranteed the order in which queued items are</p><p>processed, especially because multiple threads could be processing items simultaneously. However, this side effect exacerbates the problem. You must make sure that your application has no expectations about the order in which queued work items or tasks execute.</p><p>If a worker thread sees that its local queue is empty, then the worker thread will attempt to steal a Task from another worker thread\u2019s local queue. Tasks are stolen from the tail of a local queue and require that a thread synchronization lock be taken, which hurts performance a little bit. Of course, the hope is that stealing rarely occurs, so this lock is taken rarely. If all the local queues are empty, then the worker thread will extract an item from the global queue (taking its lock) using the FIFO algorithm. If the global queue is empty, then the worker thread puts itself to sleep waiting for something to show up. If it sleeps for a long time, then it will wake itself up and destroy itself, allowing the system to reclaim the resources (kernel object, stacks, TEB) that were used by the thread.</p><p>4 Other TaskScheduler-derived objects may exhibit behavior different from what I describe here.</p><p>The thread pool will quickly create worker threads so that the number of worker threads is equal to the value pass to ThreadPool\u2019s SetMinThreads method. If you never call this method (and it\u2019s recommended that you never call this method), then the default value is equal to the number of CPUs that your process is allowed to use as determined by your process\u2019s affinity mask. Usually your process is allowed to use all the CPUs on the machine, so the thread pool will quickly create worker threads up to the number of CPUs on the machine. After this many threads have been created, the thread pool monitors the completion rate of work items and if items are taking a long time to complete (the meaning of which is not documented), it creates more worker threads. If items start completing quickly, then worker threads will be destroyed.</p><h6>C H A P T E R 2 8</h6><h2>I/O-Bound Asynchronous Operations</h2><h6>In this chapter:</h6><p>How Windows Performs I/O Operations. . . . . . . . . . . . . . . . . . . . 727 C#\u2019s Asynchronous Functions . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 732 How the Compiler Transforms an Async Function into a State Machine . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 734 Async Function Extensibility . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 738 Async Functions and Event Handlers. . . . . . . . . . . . . . . . . . . . . . . 741 Async Functions in the Framework Class Library . . . . . . . . . . . . 742 Async Functions and Exception Handling . . . . . . . . . . . . . . . . . . 744 Other Async Function Features. . . . . . . . . . . . . . . . . . . . . . . . . . . . 745 Applications and Their Threading Models . . . . . . . . . . . . . . . . . . 748 Implementing a Server Asynchronously. . . . . . . . . . . . . . . . . . . . 751 Canceling I/O Operations . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 751 Some I/O Operations Must Be Done Synchronously . . . . . . . . . 752 I/O Request Priorities. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 754</p><p>The previous chapter focused on ways to perform compute-bound operations asynchronously, allowing the thread pool to schedule the tasks onto multiple cores so that multiple threads can work concurrently, which increases throughput while using system resources efficiently. In this chapter, we\u2019ll focus on performing I/O-bound operations asynchronously, allowing hardware devices to handle the tasks so that threads and the CPU are not used at all. However, the thread pool still plays an important role because, as you\u2019ll see, the thread pool threads will process the results of the various I/O operations.</p><h6>How Windows Performs I/O Operations</h6><p>Let\u2019s begin by discussing how Windows performs synchronous I/O operations. Figure 28-1 represents a computer system with several hardware devices connected to it. Each of these hardware devices has its own circuit board, each of which contains a small, special-purpose computer that knows how to control its hardware device. For example, the hard disk drive has a circuit board that knows how to spin up the drive, seek the head to the right track, read or write data from or to the disk, and transfer the data to or from your computer\u2019s memory.</p><p>727</p><table border=\"1\">\n<tr>\n<td><p>How Windows does Synchronous I/O</p>\n</td>\n</tr>\n<tr>\n<td><p>FileStream fs = new FileStream(...); Int32 bytesRead = fs.Read(...);</p>\n<p>ReadFile(...);</p>\n<p>(Windows I/O Subsystem Dispatcher code) Your thread blocks here!</p>\n<p>Hardware does I/O; No threads involved!</p>\n<p>.NET</p>\n<p>Win32 User-Mode</p>\n<p>Windows Kernel-Mode</p>\n<p>IRP</p>\n<p>RS-232</p>\n<p>NTFS Driver Network DVD-ROM IRP</p>\n<p>Queue</p>\n<p>1</p>\n<p>3</p>\n<p>9</p>\n<p>8</p>\n<p>4 7</p>\n<p>6</p>\n<p>2</p>\n<img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile241.png\"/>\n<img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile242.png\"/>\n<img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile243.png\"/>\n<img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile244.png\"/>\n<p>5</p>\n</td>\n</tr>\n</table><ul style=\"list-style-type: none;\">\n<li><p>FIGURE 28-1 How Windows performs a synchronous I/O operation.</p></li>\n</ul><p>In your program, you open a disk file by constructing a FileStream object. Then you call the Read method to read data from the file. When you call FileStream\u2019s Read method, your thread transitions from managed code to native/user-mode code and Read internally calls the Win32 ReadFile function (#1). ReadFile then allocates a small data structure called an I/O Request Packet (IRP) (#2). The IRP structure is initialized to contain the handle to the file, an offset within the file where bytes will start to be read from, the address of a Byte[] that should be filled with the bytes being read, the number of bytes to transfer, and some other less interesting stuff.</p><p>ReadFile then calls into the Windows kernel by having your thread transition from native/usermode code to native/kernel-mode code, passing the IRP data structure to the kernel (#3). From the device handle in the IRP, the Windows kernel knows which hardware device the I/O operation is destined for, and Windows delivers the IRP to the appropriate device driver\u2019s IRP queue (#4). Each device driver maintains its own IRP queue that contains I/O requests from all processes running on the machine. As IRP packets show up, the device driver passes the IRP information to the circuit board associated with the actual hardware device. The hardware device now performs the requested I/O operation (#5).</p><p>But here is the important part: While the hardware device is performing the I/O operation, your thread that issued the I/O request has nothing to do, so Windows puts your thread to sleep so that it is not wasting CPU time (#6). This is great, but although your thread is not wasting time, it is wasting space (memory), as its user-mode stack, kernel-mode stack, thread environment block (TEB), and other data structures are sitting in memory but are not being accessed at all. In addition, for GUI applications, the UI can\u2019t respond to user input while the thread is blocked. All of this is bad.</p><p>Ultimately, the hardware device will complete the I/O operation, and then Windows will wake up your thread, schedule it to a CPU, and let it return from kernel mode to user mode, and then back to managed code (#7, #8, and #9). FileStream\u2019s Read method now returns an Int32, indicating the actual number of bytes read from the file so that you know how many bytes you can examine in the Byte[] that you passed to Read.</p><p>Let\u2019s imagine that you are implementing a web application and as each client request comes in to your server, you need to make a database request. When a client request comes in, a thread pool thread will call into your code. If you now issue a database request synchronously, the thread will block for an indefinite amount of time waiting for the database to respond with the result. If during this time another client request comes in, the thread pool will have to create another thread and again this thread will block when it makes another database request. As more and more client requests come in, more and more threads are created, and all these threads block waiting for the database to respond. The result is that your web server is allocating lots of system resources (threads and their memory) that are barely even used!</p><p>And to make matters worse, when the database does reply with the various results, threads become unblocked and they all start executing. But because you might have lots of threads running and relatively few CPU cores, Windows has to perform frequent context switches, which hurts performance even more. This is no way to implement a scalable application.</p><p>Now, let\u2019s discuss how Windows performs asynchronous I/O operations. In Figure 28-2, I have removed all the hardware devices except the hard disk from the picture, I introduce the common language runtime\u2019s (CLR\u2019s) thread pool, and I\u2019ve modified the code slightly. I still open the disk file by constructing a FileStream object, but now I pass in the FileOptions.Asynchronous flag. This flag tells Windows that I want my read and write operations against the file to be performed asynchronously.</p><p>To read data from the file, I now call ReadAsync instead of Read. ReadAsync internally allocates a Task&lt;Int32&gt; object to represent the pending completion of the read operation. Then, ReadAsync calls Win32\u2019s ReadFile function (#1). ReadFile allocates its IRP, initializes it just like it did in the synchronous scenario (#2), and then passes it down to the Windows kernel (#3). Windows adds the IRP to the hard disk driver\u2019s IRP queue (#4), but now, instead of blocking your thread, your thread is allowed to return to your code; your thread immediately returns from its call to ReadAsync (#5, #6, and #7). Now, of course, the IRP has not necessarily been processed yet, so you cannot have code after ReadAsync that attempts to access the bytes in the passed-in Byte[].</p><p>Now you might ask, when and how do you process the data that will ultimately be read? Well, when you call ReadAsync, it returns to you a Task&lt;Int32&gt; object. Using this object, you can call ContinueWith to register a callback method that should execute when the task completes and then process the data in this callback method. Or, alternatively, you can use C#\u2019s asynchronous function feature to simplify your code by allowing you to write it sequentially (as you would if you were performing synchronous I/O).</p><table border=\"1\">\n<tr>\n<td><p>How Windows does Asynchronous I/O</p>\n</td>\n</tr>\n<tr>\n<td><p>Completes task with result or exception</p>\n<p>FileStream fs = new FileStream(..., FileOptions.Asynchronous); Task&lt;Int32&gt; task = fs.ReadAsync(\u2026);</p>\n<p>ReadFile(...);</p>\n<p>(Windows I/O Subsystem Dispatcher code) Your thread doesn\u2019t</p>\n<p>block here; it keeps running!</p>\n<p>.NET</p>\n<p>Win32 User-Mode</p>\n<p>Windows KernelMode</p>\n<p>IRP</p>\n<p>NTFS Driver</p>\n<p>The CLR\u2019s Thread Pool Threads can extract completed IRPs from here</p>\n<p>IRP Queue</p>\n<p>1</p>\n<p>3</p>\n<p>7</p>\n<p>6</p>\n<p>4 5</p>\n<p>5</p>\n<p>b</p>\n<p>2</p>\n<p>c</p>\n<img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile245.png\"/>\n<p>a</p>\n</td>\n</tr>\n</table><ul style=\"list-style-type: none;\">\n<li><p>FIGURE 28-2 How Windows performs an asynchronous I/O operation.</p></li>\n</ul><p>When the hardware device completes processing the IRP (a), it will queue the completed IRP into the CLR\u2019s thread pool (b). Sometime in the future, a thread pool thread will extract the completed IRP and execute code that completes the task by setting an exception (if an error occurred) or the result (in this case, an Int32 indicating the number of bytes successfully read) (c).1 So now the Task object knows when the operation has completed and this, in turn, lets your code run so it can safely access the data inside the Byte[].</p><p>Now that you understand the basics, let\u2019s put it all into perspective. Let\u2019s say that a client request comes in, and our server makes an asynchronous database request. As a result, our thread won\u2019t block, and it will be allowed to return to the thread pool so that it can handle more incoming client requests. So now we have just one thread handling all incoming client requests. When the database server responds, its response is also queued into the thread pool, so our thread pool thread will just process it at some point and ultimately send the necessary data back to the client. At this point, we have just one thread processing all client requests and all database responses. Our server is using very few system resources and it is still running as fast as it can, especially because there are no context switches!</p><p>If items appear in the thread pool quicker than our one thread can process them all, then the thread pool might create additional threads. The thread pool will quickly create one thread per CPU</p><p>1 Completed IRPs are extracted from the thread pool by using a first-in-first-out (FIFO) algorithm.</p><p>on the machine. So, on a quad-processor machine, four client requests/database responses (in any combination) are running on four threads without any context switching.2</p><p>However, if any of these threads voluntarily block (by invoking a synchronous I/O operation, calling Thread.Sleep, or waiting to acquire a thread synchronization lock), then Windows notifies the thread pool that one of its threads has stopped running. The thread pool now realizes that the CPUs are undersaturated and creates a new thread to replace the blocked thread. This, of course, is not ideal because creating a new thread is very expensive in terms of both time and memory.</p><p>What\u2019s worse is that the blocked thread might wake up and now the CPUs are oversaturated again and context switching must occur, decreasing performance. However, the thread pool is smart here. As threads complete their processing and return to the pool, the thread pool won\u2019t let them process new work items until the CPUs become exactly saturated again, thereby reducing context switches and improving performance. And if the thread pool later determines that it has more threads in it than it needs, it lets the extra threads kill themselves, thereby reclaiming the resources that these threads were using.</p><p>Internally, the CLR\u2019s thread pool uses a Windows resource called an I/O Completion Port to elicit the behavior that I\u2019ve just described. The CLR creates an I/O Completion Port when it initializes and, as you open hardware devices, these devices can be bound to the I/O Completion Port so that device drivers know where to queue the completed IRPs. If you want to understand more about this mechanism, I recommend the book, Windows via C/C++, Fifth Edition, by myself and Christophe Nasarre (Microsoft Press, 2007).</p><p>In addition to minimal resource usage and reduced context switches, we get many other benefits when performing I/O operations asynchronously. Whenever a garbage collection starts, the CLR must suspend all the threads in the process. Therefore, the fewer threads we have, the faster the garbage collector runs. In addition, when a garbage collection occurs, the CLR must walk all the threads\u2019 stacks looking for roots. Again, the fewer threads there are, the fewer stacks there are, and this also makes the garbage collection faster. But, in addition, if our threads don\u2019t block while processing work items, the threads tend to spend most of their time waiting in the thread pool. So when a garbage collection occurs, the threads are at the top of their stack, and walking each thread\u2019s stack for roots takes very little time.</p><p>Also, when you debug an application, Windows suspends all threads in the debuggee when you hit a breakpoint. Then, when you continue executing the debuggee, Windows has to resume all its threads, so if you have a lot of threads in an application, single-stepping through the code in a debugger can be excruciatingly slow. Using asynchronous I/O allows you to have just a few threads, improving your debugging performance.</p><p>2 This is assuming that other threads are not running on the computer, which is true most of the time, because most computers are running at far less than 100-percent CPU usage. And, even if CPU usage is at 100 percent due to threads lower than priority 8, your application will not have its responsiveness and performance impacted because your application\u2019s thread will just pre-empt the lower priority threads. If other threads are running whose priority interferes with your thread\u2019s priorities, then context switching does occur. This is bad for performance reasons, but it is good for overall application responsiveness reasons. Remember that Windows gives each process at least one thread and performs context switches to ensure that an application whose thread is an infinite loop doesn\u2019t stop other applications\u2019 threads from running.</p><p>And, here\u2019s yet another benefit: let\u2019s say that your application wants to download 10 images from various websites, and that it takes 5 seconds to download each image. If you perform this work synchronously (downloading one image after another), then it takes you 50 seconds to get the 10 images. However, if you use just one thread to initiate 10 asynchronous download operations, then all 10 are being performed concurrently and all 10 images will come back in just 5 seconds! That is, when performing multiple synchronous I/O operations, the time it takes to get all the results is the sum of the times required for each individual result. However, when performing multiple asynchronous I/O operations, the time it takes to get all the results is the time required to get the single worst-performing operation.</p><p>For GUI applications, asynchronous operations offer yet another advantage: the application\u2019s user interface doesn\u2019t hang and remains responsive to the end user. In fact, if you are building a Microsoft Silverlight or Windows Store application, you must perform all I/O operations asynchronously, because the class libraries available to you for performing I/O operations only expose these operations asynchronously; the equivalent synchronous methods simply do not exist in the library. This was done purposely ensuring that these applications can never issue a synchronous I/O operation, thereby blocking the GUI thread making the application nonresponsive to the end user. This forces developers to build responsive applications providing end users a better experience.</p><h6>C#\u2019s Asynchronous Functions</h6><p>Performing asynchronous operations is the key to building scalable and responsive applications that allow you to use very few threads to execute lots of operations. And when coupled with the thread pool, asynchronous operations allow you to take advantage of all of the CPUs that are in the machine. Realizing the enormous potential here, Microsoft designed a programming model that would make it easy for developers to take advantage of this capability.3 This pattern leverages Tasks (as discussed in Chapter 27, \u201cCompute-Bound Asynchronous Operations\u201d) and a C# language feature called asynchronous functions (or async functions, for short). Here is an example of code that uses an async function to issue two asynchronous I/O operations.</p><p>private static async Task&lt;String&gt; IssueClientRequestAsync(String serverName, String message) { using (var pipe = new NamedPipeClientStream(serverName, \"PipeName\", PipeDirection.InOut,</p><p>PipeOptions.Asynchronous | PipeOptions.WriteThrough)) {</p><p>pipe.Connect(); // Must Connect before setting ReadMode pipe.ReadMode = PipeTransmissionMode.Message;</p><p>// Asynchronously send data to the server Byte[] request = Encoding.UTF8.GetBytes(message); await pipe.WriteAsync(request, 0, request.Length);</p><p>3 For developers using a version of the Microsoft .NET Framework prior to version 4.5, my AsyncEnumerator class (that is part of my Power Threading library available on http://Wintellect.com/) allows you to use a programming model quite similar to the programming model that now ships as part of .NET Framework 4.5. In fact, the success of my AsyncEnumerator class allowed me to assist Microsoft in designing the programming model I explain in this chapter. Due to the similarities, it is trivial to migrate code using my AsyncEnumerator class to the new programming model.</p><p>// Asynchronously read the server's response Byte[] response = new Byte[1000]; Int32 bytesRead = await pipe.ReadAsync(response, 0, response.Length); return Encoding.UTF8.GetString(response, 0, bytesRead);</p><p>} // Close the pipe }</p><p>In the preceding code, you can tell that IssueClientRequestAsync is an async function, because I specified async on the first line just after static. When you mark a method as async, the compiler basically transforms your method\u2019s code into a type that implements a state machine (the details of which will be discussed in the next section). This allows a thread to execute some code in the state machine and then return without having the method execute all the way to completion. So, when a thread calls IssueClientRequestAsync, the thread constructs a NamedPipeClient\u00ad</p><p>Stream, calls Connect, sets its ReadMode property, converts the passed-in message to a Byte[] and then calls WriteAsync. WriteAsync internally allocates a Task object and returns it back to</p><p>IssueClientRequestAsync. At this point, the C# await operator effectively calls ContinueWith on the Task object passing in the method that resumes the state machine and then, the thread returns from IssueClientRequestAsync.</p><p>Sometime in the future, the network device driver will complete writing the data to the pipe and then, a thread pool thread will notify the Task object, which will then activate the ContinueWith callback method, causing a thread to resume the state machine. More specifically, a thread will re-enter the IssueClientRequestAsync method but at the point of the await operator. Our method now executes compiler-generated code that queries the status of the Task object. If the operation failed, an exception representing the failure is thrown. If the operation completes successfully, the await operator returns the result. In this case, WriteAsync returns a Task instead of a Task&lt;TResult&gt;, so there is no return value.</p><p>Now, our method continues executing by allocating a Byte[] and then calls NamedPipeClientStream\u2019s asynchronous ReadAsync method. Internally, ReadAsync creates a Task&lt;Int32&gt; object and returns it. Again, the await operator effectively calls ContinueWith on the Task&lt;Int32&gt; object passing in the method that resumes the state machine. And then, the thread returns from IssueClientRequestAsync again.</p><p>Sometime in the future, the server will send a response back to the client machine, the network device driver gets this response, and a thread pool thread notifies the Task&lt;Int32&gt; object, which will then resume the state machine. The await operator causes the compiler to generate code that queries the Task object\u2019s Result property (an Int32) and assigns the result to the bytesRead local variable or throws an exception if the operation failed. Then, the rest of the code in IssueClientRequestAsync executes, returning the result string and closing the pipe. At this point, the state machine has run to completion and the garbage collector will reclaim any memory it needed.</p><p>Because async functions return before their state machine has executed all the way to completion, the method calling IssueClientRequestAsync will continue its execution right after IssueClientRequestAsync executes its first await operator. But, how can the caller know when IssueClientRequestAsync has completed executing its state machine in its entirety? Well, when you mark a method as async, the compiler automatically generates code that creates a Task object when the state machine begins its execution; this Task object is completed automatically when the state</p><p>machine runs to completion. You\u2019ll notice that the IssueClientRequestAsync method\u2019s return type is a Task&lt;String&gt;. It actually returns the Task&lt;String&gt; object that the compiler-generated code creates back to its caller, and the Task\u2019s Result property is of type String in this case. Near the bottom of IssueClientRequestAsync, I return a string. This causes the compiler-generated code to complete the Task&lt;String&gt; object it created and set its Result property to the returned string.</p><p>You should be aware of the following restrictions related to async functions:</p><p>\u25a0\u25a0 You cannot turn your application\u2019s Main method into an async function. In addition, constructors, property accessor methods and event accessor methods cannot be turned into async functions.</p><p>\u25a0\u25a0 You cannot have any out or ref parameters on an async function.</p><p>\u25a0\u25a0 You cannot use the await operator inside a catch, finally, or unsafe block.</p><p>\u25a0\u25a0 You cannot take a lock that supports thread ownership or recursion before an await operator and release it after the await operator. The reason is because one thread might execute the code before the await and a different thread might execute the code after the await. If you use await within a C# lock statement, the compiler issues an error. If you explicitly call Monitor\u2019s Enter and Exit methods instead, then the code will compile but Monitor.Exit will throw a SynchronizationLockException at run time.4</p><p>\u25a0 \u25a0 Within a query expression, the await operator may only be used within the first collection</p><p>expression of the initial from clause or within the collection expression of a join clause. These restrictions are pretty minor. If you violate one, the compiler will let you know, and you can</p><p>usually work around the problem with some small code modifications.</p><h6>How the Compiler Transforms an Async Function into a State Machine</h6><p>When working with async functions, you will be more productive with them if you have an understanding and appreciation for the code transform that the compiler is doing for you. And, I think the easiest and best way for you to learn that is by going through an example. So, let\u2019s start off by defining some simple type definitions and some simple methods.</p><p>internal sealed class Type1 { } internal sealed class Type2 { }</p><ul style=\"list-style-type: none;\">\n<li><p>private static async Task&lt;Type1&gt; Method1Async() {</p><ul style=\"list-style-type: none;\">\n<li><p>/* Does some async thing that returns a Type1 object */</p><p>} private static async Task&lt;Type2&gt; Method2Async() {</p>\n</li>\n<li><p>/* Does some async thing that returns a Type2 object */</p></li>\n</ul>\n</li>\n</ul><p>}</p><p>4 Instead of blocking a thread by having it wait on a thread synchronization construct, you could await the task returned from calling SemaphoreSlim\u2019s WaitAsync method or my own OneManyLock\u2019s AcquireAsync method. I discuss both of these in Chapter 30, \u201cHybrid Thread Synchronization Constructs.\u201d</p><p>Now, let me show you an async function that consumes these simple types and methods.</p><p>private static async Task&lt;String&gt; MyMethodAsync(Int32 argument) { Int32 local = argument; try {</p><p>Type1 result1 = await Method1Async(); for (Int32 x = 0; x &lt; 3; x++) {</p><p>Type2 result2 = await Method2Async(); }</p><p>} catch (Exception) {</p><p>Console.WriteLine(\"Catch\");</p><p>} finally {</p><p>Console.WriteLine(\"Finally\");</p><p>} return \"Done\";</p><p>}</p><p>Although MyMethodAsync seems rather contrived, it demonstrates some key things. First, it is an async function itself that returns a Task&lt;String&gt; but the code\u2019s body ultimately returns a String. Second, it calls other functions that execute operations asynchronously, one stand-alone and the other from within a for loop. Finally, it also contains exception handling code. When compiling MyMethodAsync, the compiler transforms the code in this method to a state machine structure that is capable of being suspended and resumed.</p><p>I took the preceding code, compiled it, and then reverse engineered the IL code back into C# source code. I then simplified the code and added a lot of comments to it so you can understand what the compiler is doing to make async functions work. The following is the essence of the code created by the compiler\u2019s transformation. I show the transformed MyMethodAsync method as well as the state machine structure it now depends on.</p><p>// AsyncStateMachine attribute indicates an async method (good for tools using reflection); // the type indicates which structure implements the state machine [DebuggerStepThrough, AsyncStateMachine(typeof(StateMachine))] private static Task&lt;String&gt; MyMethodAsync(Int32 argument) {</p><p>// Create state machine instance &amp; initialize it StateMachine stateMachine = new StateMachine() {</p><p>// Create builder returning Task&lt;String&gt; from this stub method // State machine accesses builder to set Task completion/exception m_builder = AsyncTaskMethodBuilder&lt;String&gt;.Create(),</p><p>m_state = \u00ad1, // Initialize state machine location m_argument = argument // Copy arguments to state machine fields</p><p>};</p><p>}</p><p>// Start executing the state machine stateMachine.m_builder.Start(ref stateMachine); return stateMachine.m_builder.Task; // Return state machine's Task</p><p>// This is the state machine structure [CompilerGenerated, StructLayout(LayoutKind.Auto)] private struct StateMachine : IAsyncStateMachine {</p><p>// Fields for state machine's builder (Task) &amp; its location public AsyncTaskMethodBuilder&lt;String&gt; m_builder; public Int32 m_state;</p><p>// Argument and local variables are fields now: public Int32 m_argument, m_local, m_x; public Type1 m_resultType1; public Type2 m_resultType2;</p><p>// There is 1 field per awaiter type. // Only 1 of these fields is important at any time. That field refers // to the most recently executed await that is completing asynchronously:</p><ul style=\"list-style-type: none;\">\n<li><p>private TaskAwaiter&lt;Type1&gt; m_awaiterType1;</p></li>\n<li><p>private TaskAwaiter&lt;Type2&gt; m_awaiterType2;</p></li>\n</ul><p>// This is the state machine method itself void IAsyncStateMachine.MoveNext() {</p><p>String result = null; // Task's result value</p><p>// Compiler\u00adinserted try block ensures the state machine\u2019s task completes try {</p><p>Boolean executeFinally = true; // Assume we're logically leaving the 'try' block if (m_state == \u00ad1) { // If 1st time in state machine method,</p><p>m_local = m_argument; // execute start of original method }</p><p>// Try block that we had in our original code try {</p><p>TaskAwaiter&lt;Type1&gt; awaiterType1; TaskAwaiter&lt;Type2&gt; awaiterType2;</p><p>switch (m_state) {</p><p>case \u00ad1: // Start execution of code in 'try' // Call Method1Async and get its awaiter awaiterType1 = Method1Async().GetAwaiter(); if (!awaiterType1.IsCompleted) {</p><p>m_state = 0; // 'Method1Async' is completing // asynchronously m_awaiterType1 = awaiterType1; // Save the awaiter for when we come back</p><p>// Tell awaiter to call MoveNext when operation completes m_builder.AwaitUnsafeOnCompleted(ref awaiterType1, ref this); // The line above invokes awaiterType1's OnCompleted which approximately // calls ContinueWith(t =&gt; MoveNext()) on the Task being awaited. // When the Task completes, the ContinueWith task calls MoveNext</p><p>executeFinally = false; // We're not logically leaving the 'try' // block return; // Thread returns to caller</p><p>} // 'Method1Async' completed synchronously break;</p><p>case 0: // 'Method1Async' completed asynchronously awaiterType1 = m_awaiterType1; // Restore most\u00adrecent awaiter break;</p><p>}</p><p>case 1: // 'Method2Async' completed asynchronously awaiterType2 = m_awaiterType2; // Restore most\u00adrecent awaiter goto ForLoopEpilog;</p><p>// After the first await, we capture the result &amp; start the 'for' loop m_resultType1 = awaiterType1.GetResult(); // Get awaiter's result</p><p>ForLoopPrologue: m_x = 0; // 'for' loop initialization goto ForLoopBody; // Skip to 'for' loop body</p><p>ForLoopEpilog: m_resultType2 = awaiterType2.GetResult(); m_x++; // Increment x after each loop iteration // Fall into the 'for' loop\u2019s body</p><p>ForLoopBody:</p><p>if (m_x &lt; 3) { // 'for' loop test // Call Method2Async and get its awaiter awaiterType2 = Method2Async().GetAwaiter(); if (!awaiterType2.IsCompleted) {</p><p>m_state = 1; // 'Method2Async' is completing asynchronously m_awaiterType2 = awaiterType2; // Save the awaiter for when we come back</p><p>} }</p><p>// Tell awaiter to call MoveNext when operation completes m_builder.AwaitUnsafeOnCompleted(ref awaiterType2, ref this); executeFinally = false; // We're not logically leaving the 'try' block return; // Thread returns to caller</p><p>} // 'Method2Async' completed synchronously goto ForLoopEpilog; // Completed synchronously, loop around</p><p>}</p><p>} catch (Exception) {</p><p>Console.WriteLine(\"Catch\");</p><p>} finally {</p><p>// Whenever a thread physically leaves a 'try', the 'finally' executes // We only want to execute this code when the thread logically leaves the 'try' if (executeFinally) {</p><p>Console.WriteLine(\"Finally\"); }</p><p>} result = \"Done\"; // What we ultimately want to return from the async function</p><p>} catch (Exception exception) {</p><p>// Unhandled exception: complete state machine's Task with exception m_builder.SetException(exception); return;</p><p>} // No exception: complete state machine's Task with result m_builder.SetResult(result);</p><p>If you spend the time to walk through the preceding code and read all the comments, I think you\u2019ll be able to fully digest what the compiler does for you. However, there is a piece of glue that attaches the object being awaited to the state machine and I think it would be helpful if I explained how this piece of glue worked. Whenever you use the await operator in your code, the compiler takes the specified operand and attempts to call a GetAwaiter method on it. This method can be either an instance method or an extension method. The object returned from calling the GetAwaiter method is referred to as an awaiter. An awaiter is the glue I was referring to.</p><p>After the state machine obtains an awaiter, it queries its IsCompleted property. If the operation completed synchronously, true is returned and, as an optimization, the state machine simply continues executing. At this point, it calls the awaiter\u2019s GetResult method, which either throws an exception if the operation failed or returns the result if the operation was successful. The state machine continues running from here to process the result.</p><p>If the operation completes asynchronously, IsCompleted returns false. In this case, the state machine calls the awaiter\u2019s OnCompleted method passing it a delegate to the state machine\u2019s MoveNext method. And now, the state machine allows its thread to return back to where it came from so that it can execute other code. In the future, the awaiter, which wraps the underlying Task, knows when it completes and invokes the delegate causing MoveNext to execute. The fields within the state machine are used to figure out how to get to the right point in the code, giving the illusion that the method is continuing from where it left off. At this point, the code calls the awaiter\u2019s GetResult method and execution continues running from here to process the result.</p><p>That is how async functions work and the whole purpose is to simplify the coding effort normally involved when writing non-blocking code.</p><h6>Async Function Extensibility</h6><p>As for extensibility, if you can wrap a Task object around an operation that completes in the future, you can use the await operator to await that operation. Having a single type (Task) to represent all kinds of asynchronous operations is phenomenally useful because it allows you to implement combinators (like Task\u2019s WhenAll and WhenAny methods) and other helpful operations. Later in this chapter, I demonstrate doing this by wrapping a CancellationToken with a Task so I can await an asynchronous operation while also exposing timeout and cancellation.</p><p>I\u2019d also like to share with you another example. The following is my TaskLogger class, which you can use to show you asynchronous operations that haven\u2019t yet completed. This is very useful in debugging scenarios especially when your application appears hung due to a bad request or a nonresponding server.</p><p>public static class TaskLogger { public enum TaskLogLevel { None, Pending } public static TaskLogLevel LogLevel { get; set; }</p><p>public sealed class TaskLogEntry { public Task Task { get; internal set; }</p><p>}</p><p>public String Tag { get; internal set; } public DateTime LogTime { get; internal set; } public String CallerMemberName { get; internal set; } public String CallerFilePath { get; internal set; } public Int32 CallerLineNumber { get; internal set; } public override string ToString() {</p><p>return String.Format(\"LogTime={0}, Tag={1}, Member={2}, File={3}({4})\",</p><p>LogTime, Tag ?? \"(none)\", CallerMemberName, CallerFilePath, CallerLineNumber); }</p><p>private static readonly ConcurrentDictionary&lt;Task, TaskLogEntry&gt; s_log = new ConcurrentDictionary&lt;Task, TaskLogEntry&gt;(); public static IEnumerable&lt;TaskLogEntry&gt; GetLogEntries() { return s_log.Values; }</p><p>public static Task&lt;TResult&gt; Log&lt;TResult&gt;(this Task&lt;TResult&gt; task, String tag = null, [CallerMemberName] String callerMemberName = null, [CallerFilePath] String callerFilePath = null, [CallerLineNumber] Int32 callerLineNumber = \u00ad1) { return (Task&lt;TResult&gt;)</p><p>Log((Task)task, tag, callerMemberName, callerFilePath, callerLineNumber); }</p><p>public static Task Log(this Task task, String tag = null, [CallerMemberName] String callerMemberName = null, [CallerFilePath] String callerFilePath = null, [CallerLineNumber] Int32 callerLineNumber = \u00ad1) { if (LogLevel == TaskLogLevel.None) return task; var logEntry = new TaskLogEntry {</p><p>Task = task, LogTime = DateTime.Now, Tag = tag, CallerMemberName = callerMemberName, CallerFilePath = callerFilePath, CallerLineNumber = callerLineNumber</p><p>}; s_log[task] = logEntry; task.ContinueWith(t =&gt; { TaskLogEntry entry; s_log.TryRemove(t, out entry); },</p><p>TaskContinuationOptions.ExecuteSynchronously); return task;</p><p>} }</p><p>And here is some code that demonstrates the use of the class.</p><p>public static async Task Go() { #if DEBUG</p><p>// Using TaskLogger incurs a memory and performance hit; so turn it on in debug builds TaskLogger.LogLevel = TaskLogger.TaskLogLevel.Pending;</p><p>#endif</p><p>// Initiate 3 task; for testing the TaskLogger, we control their duration explicitly var tasks = new List&lt;Task&gt; {</p><p>Task.Delay(2000).Log(\"2s op\"), Task.Delay(5000).Log(\"5s op\"),</p><p>Task.Delay(6000).Log(\"6s op\") };</p><p>try { // Wait for all tasks but cancel after 3 seconds; only 1 task should complete in time // Note: WithCancellation is my extension method described later in this chapter await Task.WhenAll(tasks).</p><p>WithCancellation(new CancellationTokenSource(3000).Token);</p><p>} catch (OperationCanceledException) { }</p><p>// Ask the logger which tasks have not yet completed and sort // them in order from the one that\u2019s been waiting the longest foreach (var op in TaskLogger.GetLogEntries().OrderBy(tle =&gt; tle.LogTime))</p><p>Console.WriteLine(op); }</p><p>When I build and run this code, I get the following output.</p><p>LogTime=7/16/2012 6:44:31 AM, Tag=6s op, Member=Go, File=C:\\CLR via C#\\Code\\Ch28\u00ad1\u00adIOOps.cs(332) LogTime=7/16/2012 6:44:31 AM, Tag=5s op, Member=Go, File=C:\\CLR via C#\\Code\\Ch28\u00ad1\u00adIOOps.cs(331)</p><p>In addition to all the flexibility you have with using Task, async functions have another extensibility point: the compiler calls GetAwaiter on whatever operand is used with await. So, the operand doesn\u2019t have to be a Task object at all; it can be of any type as long as it has a GetAwaiter method available to call. Here is an example of my own awaiter that is the glue between an async method\u2019s state machine and an event being raised.</p><p>public sealed class EventAwaiter&lt;TEventArgs&gt; : INotifyCompletion { private ConcurrentQueue&lt;TEventArgs&gt; m_events = new ConcurrentQueue&lt;TEventArgs&gt;(); private Action m_continuation;</p><p>#region Members invoked by the state machine // The state machine will call this first to get our awaiter; we return ourself public EventAwaiter&lt;TEventArgs&gt; GetAwaiter() { return this; }</p><p>// Tell state machine if any events have happened yet public Boolean IsCompleted { get { return m_events.Count &gt; 0; } }</p><p>// The state machine tells us what method to invoke later; we save it public void OnCompleted(Action continuation) {</p><p>Volatile.Write(ref m_continuation, continuation); }</p><p>// The state machine queries the result; this is the await operator's result public TEventArgs GetResult() {</p><p>TEventArgs e; m_events.TryDequeue(out e); return e;</p><p>} #endregion</p><p>// Potentially invoked by multiple threads simultaneously when each raises the event</p><p>public void EventRaised(Object sender, TEventArgs eventArgs) { m_events.Enqueue(eventArgs); // Save EventArgs to return it from GetResult/await</p><p>} }</p><p>// If there is a pending continuation, this thread takes it Action continuation = Interlocked.Exchange(ref m_continuation, null); if (continuation != null) continuation(); // Resume the state machine</p><p>And here is a method that uses my EventAwaiter class to return from an await operator whenever an event is raised. In this case, the state machine continues whenever any thread in the AppDomain throws an exception.</p><p>private static async void ShowExceptions() { var eventAwaiter = new EventAwaiter&lt;FirstChanceExceptionEventArgs&gt;(); AppDomain.CurrentDomain.FirstChanceException += eventAwaiter.EventRaised;</p><p>while (true) {</p><p>Console.WriteLine(\"AppDomain exception: {0}\", (await eventAwaiter).Exception.GetType());</p><p>} }</p><p>And finally, here is some code that demonstrates it all working.</p><p>public static void Go() { ShowExceptions();</p><p>for (Int32 x = 0; x &lt; 3; x++) { try { switch (x) {</p><ul style=\"list-style-type: none;\">\n<li><p>case 0: throw new InvalidOperationException();</p></li>\n<li><p>case 1: throw new ObjectDisposedException(\"\");</p></li>\n<li><p>case 2: throw new ArgumentOutOfRangeException();</p></li>\n</ul><p>}</p><p>} catch { }</p><p>} }</p><h6>Async Functions and Event Handlers</h6><p>Async functions usually have a return type of either Task or Task&lt;TResult&gt; to represent the completion of the function\u2019s state machine. However, it is also possible to define an async function with a void return type. This is a special case that the C# compiler allows to simplify the very common scenario where you want to implement an asynchronous event handler.</p><p>Almost all event handler methods adhere to a method signature similar to this.</p><p>void EventHandlerCallback(Object sender, EventArgs e);</p><p>But, it is common to want to perform I/O operations inside an event handler, for example, when a user clicks a UI element to open a file and read from it. To keep the UI responsive, this I/O should be done asynchronously. Allowing you to write this code in an event handler method that has a void return type requires that the C# compiler allows async functions to have a void return type so you can use the await operator to perform non-blocking I/O operations. When an async function has a void return type, the compiler still generates code to create the state machine but it does not create a Task object because there is no way that one could be used. Because of this, there is no way to know when the state machine of a void-returning async function has run to completion.5</p><h6>Async Functions in the Framework Class Library</h6><p>Personally, I love async functions because they are relatively easy to learn, simple to use, and they are supported by many types in the FCL. It is easy to identify async functions because, by convention, Async is suffixed onto the method\u2019s name. In the Framework Class Library (FCL), many of the types that offer I/O operations offer XxxAsync methods.6 Here are some examples.</p><p>\u25a0\u25a0 All the System.IO.Stream-derived classes offer ReadAsync, WriteAsync, FlushAsync, and</p><p>CopyToAsync methods.</p><p>\u25a0\u25a0 All the System.IO.TextReader-derived classed offer ReadAsync, ReadLineAsync, ReadToEndAsync, and ReadBlockAsync methods. And the System.IO.TextWriter-derived classes offer WriteAsync, WriteLineAsync, and FlushAsync methods.</p><p>\u25a0\u25a0 The System.Net.Http.HttpClient class offers GetAsync, GetStreamAsync, GetByte\u00ad</p><p>ArrayAsync, PostAsync, PutAsync, DeleteAsync, and many more.</p><p>\u25a0\u25a0 All System.Net.WebRequest-derived classes (including FileWebRequest, FtpWebRequest,</p><p>and HttpWebRequest) offer GetRequestStreamAsync and GetResponseAsync methods.</p><p>\u25a0\u25a0 The System.Data.SqlClient.SqlCommand class offers ExecuteDbDataReaderAsync, ExecuteNonQueryAsync, ExecuteReaderAsync, ExecuteScalarAsync, and ExecuteXmlReaderAsync methods.</p><p>\u25a0\u25a0 Tools (such as SvcUtil.exe) that produce web service proxy types also generate XxxAsync</p><p>methods.</p><ul style=\"list-style-type: none;\">\n<li><p>5 For this reason, if you try to mark your program\u2019s entry point method (Main) as async, the C# compiler issues the following error: an entry point cannot be marked with the 'async' modifier. If you put any await operators in your Main method, then your process\u2019s primary thread would return from Main as soon as the first await operator executes. But because code that calls Main can\u2019t get a Task to monitor it and wait for it to complete, the process would just terminate (because you\u2019re returning from Main), and the rest of the code in Main would never execute at all. Fortunately, the C# compiler considers this an error to prevent this from happening.</p></li>\n<li><p>6 WinRT methods follow the same naming convention and return an IAsyncInfo interface. Fortunately, the .NET Framework supplies extension methods that effectively cast an IAsyncInfo to a Task. For more information about using asynchronous WinRT APIs with async functions, see Chapter 25, \u201cInteroperating with WinRT Components.\u201d</p></li>\n</ul><p>For anyone who has been working with earlier versions of the .NET Framework, you may be familiar with some other asynchronous programming models that it offered. There is the programming model that used BeginXxx and EndXxx methods along with an IAsyncResult interface. And there is the event-based programming model that also had XxxAsync methods (that did not return Task objects) and invoked event handler methods when an asynchronous operation completed. These two asynchronous programming models are now considered obsolete and the new model using Task objects is the preferred model.</p><p>While looking through the FCL, you might notice some classes that are lacking XxxAsync methods and instead only offer BeginXxx and EndXxx methods. This is mostly due to Microsoft not having the time to update these classes with the new methods. In the future, Microsoft should be enhancing these classes so that they fully support the new model. However, until they do, there is a helper method that you can use to adapt the old BeginXxx and EndXxx model to the new Task-based model.</p><p>Earlier I showed the code for a client application that makes a request over a named pipe. Let me show the server side of this code now.</p><p>private static async void StartServer() { while (true) { var pipe = new NamedPipeServerStream(c_pipeName, PipeDirection.InOut, \u00ad1, PipeTransmissionMode.Message, PipeOptions.Asynchronous | PipeOptions.WriteThrough);</p><p>// Asynchronously accept a client connection // NOTE: NamedPipServerStream uses the old Asynchronous Programming Model (APM) // I convert the old APM to the new Task model via TaskFactory's FromAsync method await Task.Factory.FromAsync(pipe.BeginWaitForConnection, pipe.EndWaitForConnection,</p><p>null);</p><p>} }</p><p>// Start servicing the client, which returns immediately because it is asynchronous ServiceClientRequestAsync(pipe);</p><p>The NamedPipeServerStream class has BeginWaitForConnection and EndWaitForConnection methods defined, but it does not yet have a WaitForConnectionAsync method defined. Hopefully this method will be added in a future version of the FCL. However, all is not lost, because, as you see in the preceding code, I call TaskScheduler\u2019s FromAsync method, passing into it the names of the BeginXxx and EndXxx methods, and then FromAsync internally creates a Task object that wraps these methods. Now I can use the Task object with the await operator.7</p><p>7 TaskScheduler\u2019s FromAsync method has overloads that accept an IAsyncResult in addition to overloads that accept delegates to the BeginXxx and EndXxx methods. When possible, avoid the overloads that accept an IAsyncResult because they are less efficient.</p><p>For the old event-based programming model, the FCL does not include any helper methods to adapt this model into the new Task-based model. So you have to hand-code it. Here is code demonstrating how to wrap a WebClient (which uses the event-based programming model) with a TaskCompletionSource so it can be awaited on in an async function.</p><p>private static async Task&lt;String&gt; AwaitWebClient(Uri uri) { // The System.Net.WebClient class supports the Event\u00adbased Asynchronous Pattern var wc = new System.Net.WebClient();</p><p>// Create the TaskCompletionSource and its underlying Task object var tcs = new TaskCompletionSource&lt;String&gt;();</p><p>// When a string completes downloading, the WebClient object raises the // DownloadStringCompleted event, which completes the TaskCompletionSource wc.DownloadStringCompleted += (s, e) =&gt; { if (e.Cancelled) tcs.SetCanceled(); else if (e.Error != null) tcs.SetException(e.Error); else tcs.SetResult(e.Result);</p><p>};</p><p>// Start the asynchronous operation wc.DownloadStringAsync(uri);</p><p>// Now, we can the TaskCompletionSource\u2019s Task and process the result as usual String result = await tcs.Task; // Process the resulting string (if desired)...</p><p>return result; }</p><h6>Async Functions and Exception Handling</h6><p>When a Windows device driver is processing an asynchronous I/O request, it is possible for something to go wrong, and Windows will need to inform your application of this. For example, while sending bytes or waiting for bytes to come in over the network, a timeout could expire. If the data does not come in time, the device driver will want to tell you that the asynchronous operation completed with an error. To accomplish this, the device driver posts the completed IRP to the CLR\u2019s thread pool and a thread pool thread will complete the Task object with an exception. When your state machine method is resumed, the await operator sees that the operation failed and throws this exception.</p><p>In Chapter 27, I discussed how Task objects normally throw an AggregateException, and then you\u2019d query this exception\u2019s InnerExceptions property to see the real exception(s) that occurred. However, when using await with a Task, the first inner exception is thrown instead of an AggregateException.8 This was done to give you the programming experience you expect. Also, without this, you\u2019d have to catch AggregateException throughout your code, check the inner exception and either handle the exception or re-throw it. This would be very cumbersome.</p><p>8 For the curious, it is TaskAwaiter\u2019s GetResult method that throws the first inner exception instead of throwing an AggregateException.</p><p>If your state machine method experiences an unhandled exception, then the Task object representing your async function completes due to the unhandled exception. Any code waiting for this Task object to complete will see the exception. However, it is also possible for an async function to have a void return type. In this case, there is no way for a caller to discover the unhandled exception. So, when a void-returning async function throws an unhandled exception, the compiler-generated code catches it and causes it to be rethrown using the caller\u2019s synchronization context (discussed later). If the caller executed via a GUI thread, the GUI thread will eventually rethrow the exception. If the caller executed via a non-GUI thread, some thread pool thread will eventually rethrow the exception. Usually, rethrowing these exceptions causes the whole process to terminate.</p><h6>Other Async Function Features</h6><p>In this section, I\u2019d like to share with you some additional features related to async functions. Microsoft Visual Studio has great support for debugging async functions. When the debugger is stopped on an await operator, stepping over (F10) will actually break into the debugger when the next statement is reached after the operation completes. This code might even execute on a different thread than the one that initiated the operation! This is incredibly useful and simplifies debugging substantially.</p><p>Also, if you accidentally step into (F11), an async function, you can step out (Shift+F11) of the function to get back to the caller; you must do this while on the opening brace of the async function. After you pass the open brace, step out (Shift+F11) won\u2019t break until the async function runs all the way to completion. If you need to debug the calling method before the state machine runs to completion, put a breakpoint in the calling method and just run (F5) to it.</p><p>Some asynchronous operations execute quickly and therefore complete almost instantaneously. When this happens, it is inefficient to suspend the state machine and then have another thread immediately resume the state machine; it is much more efficient to have the state machine just continue its execution. Fortunately, the await operator\u2019s compiler-generated code does check for this. If an asynchronous operation completes just before the thread would return, the thread does not return and instead, it just executes the next line of code.</p><p>This is all fine and good but occasionally, you might have an async function that performs an intensive amount of processing before initiating an asynchronous operation. If you invoke the function via your app\u2019s GUI thread, your user interface will become non-responsive to the user. And, if the asynchronous operation completes synchronously, then your user-interface will be non-responsive even longer. So, if you want to initiate an async function from a thread other than the thread that calls it, you can use Task\u2019s static Run method as follows.</p><p>// Task.Run is called on the GUI thread Task.Run(async () =&gt; {</p><p>// This code runs on a thread pool thread // TODO: Do intensive compute\u00adbound processing here...</p><p>await XxxAsync(); // Initiate asynchronous operation // Do more processing here...</p><p>});</p><p>This code demonstrates another C# feature: async lambda expressions. You see, you can\u2019t just put an await operator inside the body of a regular lambda expression, because the compiler wouldn\u2019t know how to turn the method into a state machine. But placing async just before the lambda expression causes the compiler to turn the lambda expression into a state machine method that returns a Task or Task&lt;TResult&gt;, which can then be assigned to any Func delegate variable whose return type is Task or Task&lt;TResult&gt;.</p><p>When writing code, it is very easy to invoke an async function, forgetting to use the await operator; the following code demonstrates.</p><p>static async Task OuterAsyncFunction() { InnerAsyncFunction(); // Oops, forgot to put the await operator on this line!</p><p>// Code here continues to execute while InnerAsyncFunction also continues to execute... }</p><p>static async Task InnerAsyncFunction() { /* Code in here not important */ }</p><p>Fortunately, when you do this, the C# compiler issues the following warning: Because this call</p><p>is not awaited, execution of the current method continues before the call is completed. Consider applying the 'await' operator to the result of the call. This is nice but on rare occasions, you actually don\u2019t care when InnerAsyncFunction completes, and you do want to write the preceding code and not have the compiler issue a warning.</p><p>To quiet the compiler warning, you can simply assign the Task returned from InnerAsyncFunction to a variable and then ignore the variable.9</p><p>static async Task OuterAsyncFunction() { var noWarning = InnerAsyncFunction(); // I intend not to put the await operator on this line.</p><p>// Code here continues to execute while InnerAsyncFunction also continues to execute... }</p><p>Or, I prefer to define an extension method that looks like this.</p><p>[MethodImpl(MethodImplOptions.AggressiveInlining)] // Causes compiler to optimize the call away public static void NoWarning(this Task task) { /* No code goes in here */ }</p><p>And then I can use it like this.</p><p>static async Task OuterAsyncFunction() { InnerAsyncFunction().NoWarning(); // I intend not to put the await operator on this line.</p><p>// Code here continues to execute while InnerAsyncFunction also continues to execute... }</p><p>One of the truly great features of performing asynchronous I/O operations is that you can initiate many of them concurrently so that they are all executing in parallel. This can give your application</p><p>9 Fortunately, the compiler does not give a warning about the local variable never being used.</p><p>a phenomenal performance boost. I never showed you the code that started my named pipe server and then made a bunch of client requests to it. Let me show that code now.</p><p>public static async Task Go() { // Start the server, which returns immediately because // it asynchronously waits for client requests StartServer(); // This returns void, so compiler warning to deal with</p><p>// Make lots of async client requests; save each client's Task&lt;String&gt; List&lt;Task&lt;String&gt;&gt; requests = new List&lt;Task&lt;String&gt;&gt;(10000); for (Int32 n = 0; n &lt; requests.Capacity; n++)</p><p>requests.Add(IssueClientRequestAsync(\"localhost\", \"Request #\" + n));</p><p>// Asynchronously wait until all client requests have completed // NOTE: If 1+ tasks throws, WhenAll rethrows the last\u00adthrow exception String[] responses = await Task.WhenAll(requests);</p><p>// Process all the responses for (Int32 n = 0; n &lt; responses.Length; n++)</p><p>Console.WriteLine(responses[n]); }</p><p>This code starts the named pipe server so that it is listening for client requests and then, in a for loop, it initiates 10,000 client requests as fast as it possibly can. Each time IssueClientRequestAsync is called, it returns a Task&lt;String&gt; object, which I then add to a collection. Now, the named pipe server is processing these client requests as fast as it possibly can using thread pool threads that will try to keep all the CPUs on the machine busy.10 As the server completes processing each request; each request\u2019s Task&lt;String&gt; object completes with the string response returned from the server.</p><p>In the preceding code, I want to wait until all the client requests have gotten their response before processing their results. I accomplish this by calling Task\u2019s static WhenAll method. Internally, this method creates a Task&lt;String[]&gt; object that completes after all of the List\u2019s Task objects have completed. I then await the Task&lt;String[]&gt; object so that the state machine continues execution after all of the tasks have completed. After all the tasks have completed, I loop through all the responses at once and process them (call Console.WriteLine).</p><p>Perhaps you\u2019d prefer to process each response as it happens rather than waiting for all of them to complete. Accomplishing this is almost as easy by way of Task\u2019s static WhenAny method. The revised code looks like this.</p><p>public static async Task Go() { // Start the server, which returns immediately because // it asynchronously waits for client requests StartServer();</p><p>// Make lots of async client requests; save each client's Task&lt;String&gt; List&lt;Task&lt;String&gt;&gt; requests = new List&lt;Task&lt;String&gt;&gt;(10000);</p><p>10 Fun observation: when I tested this code on my machine, the CPU usage on my 8-processor machine went all the way up to 100 percent, of course. Because all the CPUs were busy, the machine got hotter and the fan got a lot louder! After processing completed, the CPU usage went down and the fan got quieter. Fan volume is a new way of verifying that everything is working as it should.</p><p>for (Int32 n = 0; n &lt; requests.Capacity; n++) requests.Add(IssueClientRequestAsync(\"localhost\", \"Request #\" + n));</p><p>// Continue AS EACH task completes while (requests.Count &gt; 0) {</p><p>// Process each completed response sequentially Task&lt;String&gt; response = await Task.WhenAny(requests); requests.Remove(response); // Remove the completed task from the collection</p><p>} }</p><p>// Process a single client's response Console.WriteLine(response.Result);</p><p>Here, I create a while loop that iterates once per client request. Inside the loop I await Task\u2019s WhenAny method, which returns one Task&lt;String&gt; object at a time, indicating a client request that has been responded to by the server. After I get this Task&lt;String&gt; object, I remove it from the collection, and then I query its result in order to process it (pass it to Console.WriteLine).</p><h6>Applications and Their Threading Models</h6><p>The .NET Framework supports several different kinds of application models, and each application model can impose its own threading model. Console applications and Windows Services (which are really console applications; you just don\u2019t see the console) do not impose any kind of threading model; that is, any thread can do whatever it wants when it wants.</p><p>However, GUI applications, including Windows Forms, Windows Presentation Foundation (WPF), Silverlight, and Windows Store apps impose a threading model where the thread that created a UI element is the only thread allowed to update that UI element. It is common for the GUI thread to spawn off an asynchronous operation so that the GUI thread doesn\u2019t block and stop responding to user input like mouse, keystroke, pen, and touch events. However, when the asynchronous operation completes, a thread pool thread completes the Task object resuming the state machine.</p><p>For some application models, this is OK and even desired because it\u2019s efficient. But for some other</p><p>application models, like GUI applications, this is a problem, because your code will throw an exception if it tries to update UI elements via a thread pool thread. Somehow, the thread pool thread must have the GUI thread update the UI elements.</p><p>ASP.NET applications allow any thread to do whatever it wants. When a thread pool thread starts to process a client\u2019s request, it can assume the client\u2019s culture (System.Globalization.CultureInfo), allowing the web server to return culture-specific formatting for numbers, dates, and times.11 In addition, the web server can assume the client\u2019s identity (System.Security.Principal.</p><p>IPrincipal), so that the server can access only the resources that the client is allowed to access. When a thread pool thread spawns an asynchronous operation, it may be completed by another thread pool thread, which will be processing the result of an asynchronous operation. While this</p><p>11 For more information, see http://msdn.microsoft.com/en-us/library/bz9tc508.aspx.</p><p>work is being performed on behalf of the original client request, the culture and identity needs to \u201cflow\u201d to the new thread pool thread so any additional work done on behalf of the client is performed using the client\u2019s culture and identity information.</p><p>Fortunately, the FCL defines a base class, called System.Threading.SynchronizationContext, which solves all these problems. Simply stated, a SynchronizationContext-derived object connects an application model to its threading model. The FCL defines several classes derived from SynchronizationContext, but usually you will not deal directly with these classes; in fact, many of them are not publicly exposed or documented.</p><p>For the most part, application developers do not need to know anything about the SynchronizationContext class. When you await a Task, the calling thread\u2019s SynchronizationContext object is obtained. When a thread pool thread completes the Task, the SynchronizationContext object is used, ensuring the right threading model for your application model. So, when a GUI thread awaits a Task, the code following the await operator is guaranteed to execute on the GUI thread as well, allowing that code to update UI elements.12 For an ASP.NET application, the code following the await operator is guaranteed to execute on a thread pool thread that has the client\u2019s culture and principal information associated with it.</p><p>Most of the time, having a state machine resume using the application model\u2019s threading model is phenomenally useful and convenient. But, on some occasions, this can get you into trouble. Here is an example that causes a WPF application to deadlock.</p><p>private sealed class MyWpfWindow : Window { public MyWpfWindow() { Title = \"WPF Window\"; }</p><p>protected override void OnActivated(EventArgs e) { // Querying the Result property prevents the GUI thread from returning; // the thread blocks waiting for the result String http = GetHttp().Result; // Get the string synchronously!</p><p>base.OnActivated(e); }</p><p>private async Task&lt;String&gt; GetHttp() { // Issue the HTTP request and let the thread return from GetHttp HttpResponseMessage msg = await new HttpClient().GetAsync(\"http://Wintellect.com/\"); // We never get here: The GUI thread is waiting for this method to finish but this method // can't finish because the GUI thread is waiting for it to finish \u00ad\u00ad&gt; DEADLOCK!</p><p>}</p><p>return await msg.Content.ReadAsStringAsync(); }</p><p>Developers creating class libraries definitely need to be aware of the SynchronizationContext class so they can write high-performance code that works with all application models. Because a lot of class library code is application model agnostic, we want to avoid the additional overhead involved</p><p>12 Internally, the various SynchronizationContext-derived classes get the GUI thread to resume the state machine using methods like System.Windows.Forms.Control.BeginInvoke, System.Windows.Threading.Dispatcher. BeginInvoke, and Windows.UI.Core.CoreDispatcher.RunAsync.</p><p>in using a SynchronizationContext object. In addition, class library developers should do everything in their power to help application developers avoid deadlock situations. To solve both of these problems, both the Task and Task&lt;TResult&gt; classes offer a method called ConfigureAwait whose signature looks like this.</p><p>// Task defines this method: public ConfiguredTaskAwaitable ConfigureAwait(Boolean continueOnCapturedContext);</p><p>// Task&lt;TResult&gt; defines this method: public ConfiguredTaskAwaitable&lt;TResult&gt; ConfigureAwait(Boolean continueOnCapturedContext);</p><p>Passing true to this method gives you the same behavior as not calling the method at all. But, if</p><p>you pass false, the await operator does not query the calling thread\u2019s SynchronizationContext object and, when a thread pool thread completes the Task, it simply completes it and the code after the await operator executes via the thread pool thread.</p><p>Even though my GetHttp method is not class library code, the deadlock problem goes away if I add calls to ConfigureAwait. Here is the modified version of my GetHttp method.</p><p>private async Task&lt;String&gt; GetHttp() { // Issue the HTTP request and let the thread return from GetHttp HttpResponseMessage msg = await new HttpClient().GetAsync(\"http://Wintellect.com/\")</p><p>.ConfigureAwait(false); // We DO get here now because a thread pool can execute this code // as opposed to forcing the GUI thread to execute it.</p><p>return await msg.Content.ReadAsStringAsync().ConfigureAwait(false); }</p><p>As the preceding code shows, ConfigureAwait(false) must be applied to every Task object you await. This is because asynchronous operations may complete synchronously and, when this happens, the calling thread simply continues executing without returning to its caller; you never know which operation requires ignoring the SynchronizationContext object, so you have to tell all of them to ignore it. This also means that your class library code should be application model agnostic.</p><p>Alternatively, I could re-write my GetHttp method as follows so that the whole thing executes via a thread pool thread.</p><p>private Task&lt;String&gt; GetHttp() {</p><p>return Task.Run(async () =&gt; { // We run on a thread pool thread now that has no SynchronizationContext on it HttpResponseMessage msg = await new HttpClient().GetAsync(\"http://Wintellect.com/\"); // We DO get here because some thread pool can execute this code</p><p>}</p><p>return await msg.Content.ReadAsStringAsync(); });</p><p>In this version of the code, notice that my GetHttp method is not an async function; I removed the async keyword from the method signature, because the method no longer has an await operator in it. On the other hand, the lambda expression I pass to Task.Run is an async function.</p><h6>Implementing a Server Asynchronously</h6><p>From talking to many developers over the years, I\u2019ve discovered that very few of them are aware that the .NET Framework has built-in support allowing you to build asynchronous servers that scale really well. In this book, I can\u2019t explain how to do this for every kind of server, but I can list what you should look for in the MSDN documentation.</p><p>\u25a0\u25a0 To build asynchronous ASP.NET Web Forms: in your .aspx file, add \u201cAsync=true\u201d to your page</p><p>directive and look up the System.Web.UI.Page\u2019s RegisterAsyncTask method.</p><p>\u25a0\u25a0 To build an asynchronous ASP.NET MVC controller: derive your controller class from System.Web.Mvc.AsyncController and simply have your action method return a Task&lt;ActionResult&gt;.</p><p>\u25a0\u25a0 To build an asynchronous ASP.NET handler: derive your class from System.Web.HttpTask\u00ad</p><p>AsyncHandler and then override its abstract ProcessRequestAsync method.</p><p>\u25a0\u25a0 To build an asynchronous WCF service: implement your service as an async function and have</p><p>it return Task or Task&lt;TResult&gt;.</p><h6>Canceling I/O Operations</h6><p>In general, Windows doesn\u2019t give you a way to cancel an outstanding I/O operation. This is a feature that many developers would like, but it is actually quite hard to implement. After all, if you make a request from a server and then you decide you don\u2019t want the response anymore, there is no way to tell the server to abandon your original request. The way to deal with this is just to let the bytes come back to the client machine and then throw them away. In addition, there is a race condition hereyour request to cancel the request could come just as the server is sending the response. Now what should your application do? You\u2019d need to handle this potential race condition occurring in your own code and decide whether to throw the data away or act on it.</p><p>To assist with this, I recommend you implement a WithCancellation extension method that extends Task&lt;TResult&gt; (you need a similar overload that extends Task too) as follows.</p><p>private struct Void { } // Because there isn't a non\u00adgeneric TaskCompletionSource class.</p><p>private static async Task&lt;TResult&gt; WithCancellation&lt;TResult&gt;(this Task&lt;TResult&gt; originalTask, CancellationToken ct) {</p><p>// Create a Task that completes when the CancellationToken is canceled var cancelTask = new TaskCompletionSource&lt;Void&gt;();</p><p>// When the CancellationToken is canceled, complete the Task using (ct.Register(</p><p>t =&gt; ((TaskCompletionSource&lt;Void&gt;)t).TrySetResult(new Void()), cancelTask)) {</p><p>// Create a Task that completes when either the original or // CancellationToken Task completes</p><p>Task any = await Task.WhenAny(originalTask, cancelTask.Task);</p><p>}</p><p>// If any Task completes due to CancellationToken, throw OperationCanceledException if (any == cancelTask.Task) ct.ThrowIfCancellationRequested();</p><p>}</p><p>// await original task (synchronously); if it failed, awaiting it // throws 1st inner exception instead of AggregateException return await originalTask;</p><p>Now, you can call this extension method as follows.</p><p>public static async Task Go() { // Create a CancellationTokenSource that cancels itself after # milliseconds var cts = new CancellationTokenSource(5000); // To cancel sooner, call cts.Cancel() var ct = cts.Token;</p><p>}</p><p>try { // I used Task.Delay for testing; replace this with another method that returns a Task await Task.Delay(10000).WithCancellation(ct); Console.WriteLine(\"Task completed\");</p><p>} catch (OperationCanceledException) {</p><p>Console.WriteLine(\"Task cancelled\"); }</p><h6>Some I/O Operations Must Be Done Synchronously</h6><p>The Win32 API offers many functions that execute I/O operations. Unfortunately, some of these methods do not let you perform the I/O asynchronously. For example, the Win32 CreateFile method (called by FileStream\u2019s constructor) always executes synchronously. If you\u2019re trying to create or open a file on a network server, it could take several seconds before CreateFile returns\u2014the calling thread is idle all the while. An application designed for optimum responsiveness and scalability would ideally call a Win32 function that lets you create or open a file asynchronously so that your thread is not sitting and waiting for the server to reply. Unfortunately, Win32 has no CreateFile-like function to let you do this, and therefore the FCL cannot offer an efficient way to open a file asynchronously. Windows also doesn\u2019t offer functions to asynchronously access the registry, access the event log, get a directory\u2019s files/subdirectories, or change a file\u2019s/directory\u2019s attributes, to name just a few.</p><p>Here is an example where this is a real problem. Imagine writing a simple UI control that allows the user to type a file path and provides automatic completion (similar to the common File Open dialog box). The control must use separate threads to enumerate directories looking for files because Windows doesn\u2019t offer any functions to enumerate files asynchronously. As the user continues to type in the UI control, you have to use more threads and ignore the results from any previously spawned threads. With Windows Vista, Microsoft introduced a new Win32 function called CancelSynchronousIO. This function allows one thread to cancel a synchronous I/O operation that is being performed by another thread. This function is not exposed by the FCL, but you can also P/Invoke to</p><p>it if you want to take advantage of it from a desktop application implemented with managed code. I show the P/Invoke signature near the end of this chapter.</p><p>The point I want you to take away though is that many people think that synchronous APIs are easier to work with, and in many cases this is true. But in some cases, synchronous APIs make things much harder.</p><p>Due to all the problems that exist when executing I/O operations synchronously, when designing</p><p>the Windows Runtime, the Windows team decided to expose all methods that perform I/O asynchronously. So, now there is a Windows Runtime API to open files asynchronously; see Windows. Storage.StorageFile\u2019s OpenAsync method. In fact, the Windows Runtime does not offer any APIs</p><p>allowing you to perform an I/O operation synchronously. Fortunately, you can use the C#\u2019s async function feature to simplify your coding when calling these APIs.</p><h6>FileStream-Specific Issues</h6><p>When you create a FileStream object, you get to specify whether you want to communicate using synchronous or asynchronous operations via the FileOptions.Asynchronous flag (which is equivalent to calling the Win32 CreateFile function and passing into it the FILE_FLAG_OVERLAPPED flag). If you do not specify this flag, Windows performs all operations against the file synchronously. Of course, you can still call FileStream\u2019s ReadAsync method, and to your application, it looks as if the operation is being performed asynchronously, but internally, the FileStream class uses another thread to emulate asynchronous behavior; use of this thread is wasteful and hurts performance.</p><p>On the other hand, you can create a FileStream object by specifying the FileOptions.Asynchronous flag. Then you can call FileStream\u2019s Read method to perform a synchronous operation.</p><p>Internally, the FileStream class emulates this behavior by starting an asynchronous operation and then immediately puts the calling thread to sleep until the operation is complete. This is also inefficient, but it is not as inefficient as calling ReadAsync by using a FileStream constructed without the</p><p>FileOptions.Asynchronous flag.</p><p>So, to summarize, when working with a FileStream, you must decide up front whether you intend to perform synchronous or asynchronous I/O against the file and indicate your choice by specifying the FileOptions.Asynchronous flag (or not). If you specify this flag, always call ReadAsync. If you do not specify this flag, always call Read. This will give you the best performance. If you intend to make some synchronous and some asynchronous operations against the FileStream, it is more efficient to construct it using the FileOptions.Asynchronous flag. Alternatively, you can create two FileStream objects over the same file; open one FileStream for asynchronous I/O and open the other FileStream for synchronous I/O. Note that the System.IO.File\u2019s class offers helper methods (Create, Open, and OpenWrite) that create and return FileStream objects. Internally, none of these methods specify the FileOptions.Asynchronous flag, so you should avoid using these methods if you want to create a responsive or scalable application.</p><p>You should also be aware that the NTFS file system device driver performs some operations synchronously no matter how you open the file. For more information about this, see http:// support.microsoft.com/default.aspx?scid=kb%3Ben-us%3B156932.</p><h6>I/O Request Priorities</h6><p>In Chapter 26, \u201cThread Basics,\u201d I showed how setting thread priorities affects how threads are scheduled. However, threads also perform I/O requests to read and write data from various hardware devices. If a low-priority thread gets CPU time, it could easily queue hundreds or thousands of I/O requests in a very short time. Because I/O requests typically require time to process, it is possible that a low-priority thread could significantly affect the responsiveness of the system by suspending high-priority threads, which prevents them from getting their work done. Because of this, you can see a machine become less responsive when executing long-running low-priority services such as disk defragmenters, virus scanners, content indexers, and so on.13</p><p>Windows allows a thread to specify a priority when making I/O requests. For more details about I/O priorities, refer to the white paper at http://www.microsoft.com/whdc/driver/priorityio.mspx. Unfortunately, the FCL does not include this functionality yet; hopefully, it will be added in a future version. However, you can still take advantage of this feature by P/Invoking out to native Win32 functions. Here is the P/Invoke code.</p><p>internal static class ThreadIO { public static BackgroundProcessingDisposer BeginBackgroundProcessing( Boolean process = false) {</p><p>}</p><p>ChangeBackgroundProcessing(process, true); return new BackgroundProcessingDisposer(process);</p><p>public static void EndBackgroundProcessing(Boolean process = false) {</p><p>ChangeBackgroundProcessing(process, false); }</p><p>private static void ChangeBackgroundProcessing(Boolean process, Boolean start) { Boolean ok = process ? SetPriorityClass(GetCurrentWin32ProcessHandle(),</p><p>start ? ProcessBackgroundMode.Start : ProcessBackgroundMode.End) : SetThreadPriority(GetCurrentWin32ThreadHandle(),</p><p>start ? ThreadBackgroundgMode.Start : ThreadBackgroundgMode.End); if (!ok) throw new Win32Exception();</p><p>}</p><p>// This struct lets C#'s using statement end the background processing mode public struct BackgroundProcessingDisposer : IDisposable {</p><p>private readonly Boolean m_process; public BackgroundProcessingDisposer(Boolean process) { m_process = process; } public void Dispose() { EndBackgroundProcessing(m_process); }</p><p>}</p><p>13 The Windows SuperFetch feature takes advantage of low-priority I/O requests.</p><p>// See Win32\u2019s THREAD_MODE_BACKGROUND_BEGIN and THREAD_MODE_BACKGROUND_END private enum ThreadBackgroundgMode { Start = 0x10000, End = 0x20000 }</p><p>// See Win32\u2019s PROCESS_MODE_BACKGROUND_BEGIN and PROCESS_MODE_BACKGROUND_END private enum ProcessBackgroundMode { Start = 0x100000, End = 0x200000 }</p><p>[DllImport(\"Kernel32\", EntryPoint = \"GetCurrentProcess\", ExactSpelling = true)] private static extern SafeWaitHandle GetCurrentWin32ProcessHandle();</p><p>[DllImport(\"Kernel32\", ExactSpelling = true, SetLastError = true)] [return: MarshalAs(UnmanagedType.Bool)] private static extern Boolean SetPriorityClass(</p><p>SafeWaitHandle hprocess, ProcessBackgroundMode mode);</p><p>[DllImport(\"Kernel32\", EntryPoint = \"GetCurrentThread\", ExactSpelling = true)] private static extern SafeWaitHandle GetCurrentWin32ThreadHandle();</p><p>[DllImport(\"Kernel32\", ExactSpelling = true, SetLastError = true)] [return: MarshalAs(UnmanagedType.Bool)] private static extern Boolean SetThreadPriority(</p><p>SafeWaitHandle hthread, ThreadBackgroundgMode mode);</p><p>}</p><p>// http://msdn.microsoft.com/en\u00adus/library/aa480216.aspx [DllImport(\"Kernel32\", SetLastError = true, EntryPoint = \"CancelSynchronousIo\")] [return: MarshalAs(UnmanagedType.Bool)] private static extern Boolean CancelSynchronousIO(SafeWaitHandle hThread);</p><p>And here is code showing how to use it.</p><p>public static void Main () { using (ThreadIO.BeginBackgroundProcessing()) {</p><p>// Issue low\u00adpriority I/O requests in here (eg: calls to ReadAsync/WriteAsync) }</p><p>}</p><p>You tell Windows that you want your thread to issue low-priority I/O requests by calling ThreadIO\u2019s BeginBackgroundProcessing method. Note that this also lowers the CPU scheduling priority of the thread. You can return the thread to making normal-priority I/O requests (and normal CPU scheduling priority) by calling EndBackgroundProcessing or by calling Dispose on the value returned by BeginBackgroundProcessing (as shown in the preceding code via C#\u2019s using statement). A thread can only affect its own background processing mode; Windows doesn\u2019t allow a thread to change the background processing mode of another thread.</p><p>If you want all threads in a process to make low-priority I/O requests and have low CPU scheduling, you can call BeginBackgroundProcessing, passing in true for the process parameter. A process can only affect its own background processing mode; Windows doesn\u2019t allow a thread to change the background processing mode of another process.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile246.png\"/><p>Important As a developer, it is your responsibility to use these new background priorities to allow the foreground applications to be more responsive, taking care to avoid priority inversion. In the presence of intense normal-priority I/Os, a thread running at background priority can be delayed for seconds before getting the result of its I/O requests. If a lowpriority thread has grabbed a thread synchronization lock for which the normal-priority thread is waiting, the normal-priority threads might end up waiting for the backgroundpriority thread until the low-priority I/O requests are completed. Your background-priority thread does not even have to submit I/Os for the problem to happen. So using shared synchronization objects between normal-priority and background-priority threads should be minimized (or eliminated if possible) to avoid these priority inversions where normalpriority threads are blocked on locks owned by background-priority threads.</p><h6>C H A P T E R 2 9</h6><h2>Primitive Thread Synchronization Constructs</h2><h6>In this chapter:</h6><p>Class Libraries and Thread Safety . . . . . . . . . . . . . . . . . . . . . . . . . . 759 Primitive User-Mode and Kernel-Mode Constructs. . . . . . . . . . 760 User-Mode Constructs . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 762 Kernel-Mode Constructs. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 778</p><p>When a thread pool thread blocks, the thread pool creates additional threads, and the time and memory resources required to create, destroy, and schedule threads is very expensive. When many developers see that they have threads in their program that are not doing anything useful, they tend to create more threads in hopes that the new threads will do something useful. The key to building scalable and responsive applications is to not block the threads you have so that they can be used and reused to execute other tasks. Chapter 27, \u201cCompute-Bound Asynchronous Operations,\u201d focused on how to use existing threads to perform compute-bound operations, and Chapter 28, \u201cI/O-Bound Asynchronous Operations,\u201d focused on how to use threads when performing I/O-bound operations.</p><p>In this chapter, I focus on thread synchronization. Thread synchronization is used to prevent corruption when multiple threads access shared data at the same time. I emphasize at the same time, because thread synchronization is all about timing. If you have some data that is accessed by two threads and those threads cannot possibly touch the data simultaneously, then thread synchronization is not required at all. In Chapter 28, I discussed how different sections of async functions can be executed by different threads. Here we could potentially have two different threads accessing the same variables and data. But async functions are implemented in such a way that it is impossible for two threads to access this same data at the same time. Therefore, no thread synchronization is required when code accesses data contained within the async function.</p><p>This is ideal because thread synchronization has many problems associated with it. First, it is tedious and extremely error-prone. In your code, you must identify all data that could potentially be touched by multiple threads at the same time. Then you must surround this code with additional code that acquires and releases a thread synchronization lock. The lock ensures that only one thread at a time can access the resource. If you forget to surround just one block of code with a lock, then the data will become corrupted. Also, there is no way to prove that you have added all your locking code correctly. You just have to run your application, stress-test it a lot, and hope that nothing goes wrong.</p><p>757</p><p>In fact, you should test your application on a machine that has as many CPUs as possible because the more CPUs you have, the better chance that two or more threads will attempt to access the resource at the same time, making it more likely you\u2019ll detect a problem.</p><p>The second problem with locks is that they hurt performance. It takes time to acquire and release a lock because there are additional method calls, and because the CPUs must coordinate with each other to determine which thread will acquire the lock first. Having the CPUs in the machine communicate with each other this way hurts performance. For example, let\u2019s say that you have code that adds a node to the head of a linked list.</p><p>// This class is used by the LinkedList class public class Node {</p><p>internal Node m_next; // Other members not shown</p><p>}</p><p>public sealed class LinkedList { private Node m_head;</p><p>public void Add(Node newNode) { // The following two lines perform very fast reference assignments newNode.m_next = m_head; m_head = newNode;</p><p>} }</p><p>This Add method simply performs two reference assignments that can execute extremely fast. Now, if we want to make Add thread safe so that multiple threads can call it simultaneously without corrupting the linked list, then we need to have the Add method acquire and release a lock.</p><p>public sealed class LinkedList { private SomeKindOfLock m_lock = new SomeKindOfLock(); private Node m_head;</p><p>public void Add(Node newNode) { m_lock.Enter(); // The following two lines perform very fast reference assignments newNode.m_next = m_head; m_head = newNode; m_lock.Leave();</p><p>} }</p><p>Although Add is now thread safe, it has also become substantially slower. How much slower depends on the kind of lock chosen; I will compare the performance of various locks in this chapter and in Chapter 30, \u201cHybrid Thread Synchronization Constructs.\u201d But even the fastest lock could make the Add method several times slower than the version of it that didn\u2019t have any lock code in it at all. Of course, the performance becomes significantly worse if the code calls Add in a loop to insert several nodes into the linked list.</p><p>The third problem with thread synchronization locks is that they allow only one thread to access the resource at a time. This is the lock\u2019s whole reason for existing, but it is also a problem, because blocking a thread causes more threads to be created. So, for example, if a thread pool thread attempts to acquire a lock that it cannot have, it is likely that the thread pool will create a new thread to keep the CPUs saturated with work. As discussed in Chapter 26, \u201cThread Basics,\u201d creating a thread is very expensive in terms of both memory and performance. And to make matters even worse, when the blocked thread gets to run again, it will run with this new thread pool thread; Windows is now scheduling more threads than there are CPUs, and this increases context switching, which also hurts performance.</p><p>The summary of all of this is that thread synchronization is bad, so you should try to design your applications to avoid as much of it as possible. To that end, you should avoid shared data such as static fields. When a thread uses the new operator to construct an object, the new operator returns a reference to the new object. At this point in time, only the thread that constructs the object has a reference to it; no other thread can access that object. If you avoid passing this reference to another thread that might use the object at the same time as the creating thread, then there is no need to synchronize access to the object.</p><p>Try to use value types because they are always copied, so each thread operates on its own copy. Finally, it is OK to have multiple threads accessing shared data simultaneously if that access is readonly. For example, many applications create some data structures during their initialization. Once initialized, the application can create as many threads as it wants; if all these threads just query the data, then all the threads can do this simultaneously without acquiring or releasing any locks. The String type is an example of this: after a String object is created, it is immutable; so many threads can access a single String object at the same time without any chance of the String object becoming corrupted.</p><h6>Class Libraries and Thread Safety</h6><p>Now, I\u2019d like to say a quick word about class libraries and thread synchronization. Microsoft\u2019s Framework Class Library (FCL) guarantees that all static methods are thread safe. This means that if two threads call a static method at the same time, no data will get corrupted. The FCL had to do this internally because there is no way that multiple companies producing different assemblies could coordinate on a single lock for arbitrating access to the resource. The Console class contains a static field, inside which many of its methods acquire and release to ensure that only one thread at a time is accessing the console.</p><p>For the record, making a method thread safe does not mean that it internally takes a thread synchronization lock. A thread-safe method means that data doesn\u2019t get corrupted if two threads attempt to access the data at the same time. The System.Math class has a static Max method implemented as follows.</p><p>public static Int32 Max(Int32 val1, Int32 val2) {</p><p>return (val1 &lt; val2) ? val2 : val1; }</p><p>This method is thread safe even though it doesn\u2019t take any lock. Because Int32 is a value type, the two Int32 values passed to Max are copied into it and so, multiple threads could be calling Max simultaneously, but each thread is working on its own data, isolated from any other thread.</p><p>On the other hand, the FCL does not guarantee that instance methods are thread safe because adding all the locking code would hurt performance too much. And, in fact, if every instance method acquires and releases a lock, then you ultimately end up having just one thread running in your application at any given time, which hurts performance even more. As mentioned earlier, when a thread constructs an object, only this thread has a reference to the object, no other thread can access that object, and no thread synchronization is required when invoking instance methods. However, if the thread then exposes the reference to the object\u2014by placing it in a static field, passing as the state argument to ThreadPool.QueueUserWorkItem or to a Task, and so on\u2014then thread synchronization is required if the threads could attempt simultaneous non-read-only access.</p><p>It is recommended that your own class libraries follow this pattern; that is, make all your static methods thread safe and make all your instance methods not thread safe. There is one caveat to this pattern: if the purpose of the instance method is to coordinate threads, then the instance method should be thread safe. For example, one thread can cancel an operation by calling CancellationTokenSource\u2019s Cancel method, and another thread detects that it should stop what it\u2019s doing by querying the corresponding CancellationToken\u2019s IsCancellationRequested property. These two instance members have some special thread synchronization code inside them to ensure that the coordination of the two threads goes as expected.1</p><h6>Primitive User-Mode and Kernel-Mode Constructs</h6><p>In this chapter, I explain the primitive thread synchronization constructs. By primitive, I mean the simplest constructs that are available to use in your code. There are two kinds of primitive constructs: user-mode and kernel-mode. Whenever possible, you should use the primitive user-mode constructs, because they are significantly faster than the kernel-mode constructs because they use special CPU instructions to coordinate threads. This means that the coordination is occurring in hardware (which is what makes it fast). But this also means that the Windows operating system never detects that a thread is blocked on a primitive user-mode construct. Because a thread pool thread blocked on a user-mode primitive construct is never considered blocked, the thread pool will not create a new thread to replace the temporarily blocked thread. In addition, these CPU instructions block the thread for an incredibly short period of time.</p><p>Wow! All of this sounds great, doesn\u2019t it? And it is great, which is why I recommend using these constructs as much as possible. However, there is a downside\u2014only the Windows operating system kernel can stop a thread from running so that it is not wasting CPU time. A thread running in user mode can be preempted by the system, but the thread will be scheduled again as soon as possible. So, a thread that wants to acquire some resource, but can\u2019t get it, spins in user mode. This potentially</p><p>1 Specifically, the field that both members access is marked as volatile, a concept that will be discussed later in this chapter.</p><p>wastes a lot of CPU time, which would be better spent performing other work or even just letting the CPU go idle to conserve power.</p><p>This brings us to the primitive kernel-mode constructs. The kernel-mode constructs are provided by the Windows operating system itself. As such, they require that your application\u2019s threads call functions implemented in the operating system kernel. Having threads transition from user mode to kernel mode and back incurs a big performance hit, which is why kernel-mode constructs should be avoided.2 However, they do have a positive feature\u2014when a thread uses a kernel-mode construct to acquire a resource that another thread has, Windows blocks the thread so that it is no longer wasting CPU time. Then, when the resource becomes available, Windows resumes the thread, allowing it to access the resource.</p><p>A thread waiting on a construct might block forever if the thread currently holding the construct never releases it. If the construct is a user-mode construct, the thread is running on a CPU forever, and we call this a livelock. If the construct is a kernel-mode construct, the thread is blocked forever, and we call this a deadlock. Both of these are bad, but of the two, a deadlock is always preferable to a livelock, because a livelock wastes both CPU time and memory (the thread\u2019s stack, etc.), whereas a deadlock wastes only memory.3</p><p>In an ideal world, we\u2019d like to have constructs that take the best of both worlds. That is, we\u2019d like a construct that is fast and non-blocking (like the user-mode constructs) when there is no contention. But when there is contention for the construct, we\u2019d like it to be blocked by the operating system kernel. Constructs that work like this do exist; I call them hybrid constructs, and I will discuss them in Chapter 30. It is very common for applications to use the hybrid constructs, because in most applications, it is rare for two or more threads to attempt to access the same data at the same time. A hybrid construct keeps your application running fast most of the time, and occasionally it runs slowly to block the thread. The slowness usually doesn\u2019t matter at this point, because your thread is going to be blocked anyway.</p><p>Many of the CLR\u2019s thread synchronization constructs are really just object-oriented class wrappers around Win32 thread synchronization constructs. After all, CLR threads are Windows threads, which means that Windows schedules and controls the synchronization of threads. Windows thread synchronization constructs have been around because 1992, and a ton of material has been written about them.4 Therefore, I give them only cursory treatment in this chapter.</p><ul style=\"list-style-type: none;\">\n<li><p>2 I\u2019ll show a program that measures the performance later in this chapter, at the end of the \u201cEvent Constructs\u201d section.</p></li>\n<li><p>3 I say that the memory allocated for the thread is wasted because the memory is not being used in a productive manner if the thread is not making forward progress.</p></li>\n<li><p>4 In fact, Christophe Nasarre\u2019s and my book, Windows via C/C++, Fifth Edition (Microsoft Press, 2007), has several chapters devoted to this subject.</p></li>\n</ul><h6>User-Mode Constructs</h6><p>The CLR guarantees that reads and writes to variables of the following data types are atomic: Boolean, Char, (S)Byte, (U)Int16, (U)Int32, (U)IntPtr, Single, and reference types. This means that all bytes within that variable are read from or written to all at once. So, for example, if you have the following class:</p><p>internal static class SomeType { public static Int32 x = 0;</p><p>}</p><p>and if a thread executes this line of code:</p><p>SomeType.x = 0x01234567;</p><p>then the x variable will change from 0x00000000 to 0x01234567 all at once (atomically). Another thread cannot possibly see the value in an intermediate state. For example, it is impossible for some other read to query SomeType.x and get a value of 0x01230000. Suppose that the x field in the preceding SomeType class is an Int64. If a thread executes this line of code:</p><p>SomeType.x = 0x0123456789abcdef;</p><p>it is possible that another thread could query x and get a value of 0x0123456700000000 or 0x0000000089abcdef, because the read and write operations are not atomic. This is called a torn read.</p><p>Although atomic access to variable guarantees that the read or write happens all at once, it does not guarantee when the read or write will happen due to compiler and CPU optimizations. The primitive user-mode constructs discussed in this section are used to enforce the timing of these atomic read and write operations. In addition, these constructs can also force atomic and timed access to variables of additional data types: (U)Int64 and Double.</p><p>There are two kinds of primitive user-mode thread synchronization constructs:</p><p>\u25a0\u25a0 Volatile constructs, which perform an atomic read or write operation on a variable containing</p><p>a simple data type at a specific time</p><p>\u25a0\u25a0 Interlocked constructs, which perform an atomic read and write operation on a variable con-</p><p>taining a simple data type at a specific time</p><p>All the volatile and interlocked constructs require you to pass a reference (memory address) to a variable containing a simple data type.</p><h6>Volatile Constructs</h6><p>Back in the early days of computing, software was written using assembly language. Assembly language is very tedious, because programmers must explicitly state everything\u2014use this CPU register for this, branch to that, call indirect through this other thing, and so on. To simplify programming,</p><p>higher-level languages were introduced. These higher-level languages introduced common useful constructs, like if/else, switch/case, various loops, local variables, arguments, virtual method calls, operator overloads, and much more. Ultimately, these language compilers must convert the high-level constructs down to the low-level constructs so that the computer can actually do what you want it to do.</p><p>In other words, the C# compiler translates your C# constructs into Intermediate Language (IL), which is then converted by the just-in-time (JIT) compiler into native CPU instructions, which must then be processed by the CPU itself. In addition, the C# compiler, the JIT compiler, and even the CPU itself can optimize your code. For example, the following ridiculous method can ultimately be compiled into nothing.</p><p>private static void OptimizedAway() { // Constant expression is computed at compile time resulting in zero Int32 value = (1 * 100) \u00ad (50 * 2);</p><p>// If value is 0, the loop never executes for (Int32 x = 0; x &lt; value; x++) {</p><p>// There is no need to compile the code in the loop because it can never execute Console.WriteLine(\"Jeff\");</p><p>} }</p><p>In this code, the compiler can see that value will always be 0; therefore, the loop will never execute and consequently, there is no need to compile the code inside the loop. This method could be compiled down to nothing. In fact, when JITting a method that calls OptimizedAway, the JITter will try to inline the OptimizedAway method\u2019s code. Because there is no code, the JITter will even remove the code that tries to call OptimizedAway. We love this feature of compilers. As developers, we get to write the code in the way that makes the most sense to us. The code should be easy to write, read, and maintain. Then compilers translate our intentions into machine-understandable code. We want our compilers to do the best job possible for us.</p><p>When the C# compiler, JIT compiler, and CPU optimize our code, they guarantee us that the intention of the code is preserved. That is, from a single-threaded perspective, the method does what we want it to do, although it may not do it exactly the way we described in our source code. However, the intention might not be preserved from a multithreaded perspective. Here is an example where the optimizations make the program not work as expected.</p><p>internal static class StrangeBehavior { // As you'll see later, mark this field as volatile to fix the problem private static Boolean s_stopWorker = false;</p><p>public static void Main() { Console.WriteLine(\"Main: letting worker run for 5 seconds\"); Thread t = new Thread(Worker); t.Start(); Thread.Sleep(5000); s_stopWorker = true; Console.WriteLine(\"Main: waiting for worker to stop\"); t.Join();</p><p>}</p><p>private static void Worker(Object o) { Int32 x = 0; while (!s_stopWorker) x++; Console.WriteLine(\"Worker: stopped when x={0}\", x);</p><p>} }</p><p>In this code, the Main method creates a new thread that executes the Worker method. This Worker method counts as high as it can before being told to stop. The Main method allows the Worker thread to run for five seconds before telling it to stop by setting the static Boolean field to true. At this point, the Worker thread should display what it counted up to, and then the thread will terminate. The Main thread waits for the Worker thread to terminate by calling Join, and then the Main thread returns, causing the whole process to terminate.</p><p>Looks simple enough, right? Well, the program has a potential problem due to all the optimizations that could happen to it. You see, when the Worker method is compiled, the compiler sees that s_stopWorker is either true or false, and it also sees that this value never changes inside the Worker method itself. So the compiler could produce code that checks s_stopWorker first. If s_stopWorker is true, then Worker: stopped when x=0 will be displayed. If s_stopWorker is false, then the compiler produces code that enters an infinite loop that increments x forever. You see, the optimizations cause the loop to run very fast because checking s_stopWorker only occurs once before the loop; it does not get checked with each iteration of the loop.</p><p>If you actually want to see this in action, put this code in a .cs file and compile the code by using C#\u2019s /platform:x86 and /optimize+ switches. Then run the resulting EXE file, and you\u2019ll see that the program runs forever. Note that you have to compile for x86, ensuring that the x86 JIT compiler is used at run time. The x86 JIT compiler is more mature than the x64 JIT compiler, so it performs more aggressive optimizations. The x64 JIT compiler does not perform this particular optimization, and therefore the program runs to completion. This highlights another interesting point about all of this. Whether your program behaves as expected depends on a lot of factors, such as which compiler version and compiler switches are used, which JIT compiler is used, and which CPU your code is running on. In addition, to see the preceding program run forever, you must not run the program under a debugger because the debugger causes the JIT compiler to produce unoptimized code that is easier to step through.</p><p>Let\u2019s look at another example, which has two threads that are both accessing two fields.</p><p>internal sealed class ThreadsSharingData { private Int32 m_flag = 0; private Int32 m_value = 0;</p><p>// This method is executed by one thread public void Thread1() {</p><p>// Note: These could execute in reverse order m_value = 5; m_flag = 1;</p><p>}</p><p>// This method is executed by another thread public void Thread2() {</p><p>}</p><p>// Note: m_value could be read before m_flag if (m_flag == 1)</p><p>Console.WriteLine(m_value); }</p><p>The problem with this code is that the compilers/CPU could translate the code in such a way as to reverse the two lines of code in the Thread1 method. After all, reversing the two lines of code does not change the intention of the method. The method needs to get a 5 in m_value and a 1 in m_flag. From a single-threaded application\u2019s perspective, the order of executing this code is unimportant. If these two lines do execute in reverse order, then another thread executing the Thread2 method could see that m_flag is 1 and then display 0.</p><p>Let\u2019s look at this code another way. Let\u2019s say that the code in the Thread1 method executes in program order (the way it was written). When compiling the code in the Thread2 method, the compiler must generate code to read m_flag and m_value from RAM into CPU registers. It is possible that RAM will deliver the value of m_value first, which would contain a 0. Then the Thread1 method could execute, changing m_value to 5 and m_flag to 1. But Thread2\u2019s CPU register doesn\u2019t see that</p><p>m_value has been changed to 5 by this other thread, and then the value in m_flag could be read from RAM into a CPU register and the value of m_flag becomes 1 now, causing Thread2 to again display 0.</p><p>This is all very scary stuff and is more likely to cause problems in a release build of your program than in a debug build of your program, making it particularly tricky to detect these problems and correct your code. Now, let\u2019s talk about how to correct your code.</p><p>The static System.Threading.Volatile class offers two static methods that look like this.5</p><p>public static class Volatile { public static void Write(ref Int32 location, Int32 value); public static Int32 Read(ref Int32 location);</p><p>}</p><p>These methods are special. In effect, these methods disable some optimizations usually performed by the C# compiler, the JIT compiler, and the CPU itself. Here\u2019s how the methods work:</p><p>\u25a0\u25a0 The Volatile.Write method forces the value in location to be written to at the point of the call. In addition, any earlier program-order loads and stores must occur before the call to Volatile.Write.</p><p>\u25a0\u25a0 The Volatile.Read method forces the value in location to be read from at the point of the call. In addition, any later program-order loads and stores must occur after the call to Volatile.Read.</p><p>5 There are also overloads of Read and Write that operate on the following types: Boolean, (S)Byte, (U)Int16, UInt32, (U)Int64, (U)IntPtr, Single, Double, and T where T is a generic type constrained to \u2018class\u2019 (reference types).</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile247.png\"/><p>Important I know that this can be very confusing, so let me summarize it as a simple rule. When threads are communicating with each other via shared memory, write the last value by calling Volatile.Write and read the first value by calling Volatile.Read.</p><p>So now we can fix the ThreadsSharingData class by using these methods.</p><p>internal sealed class ThreadsSharingData { private Int32 m_flag = 0; private Int32 m_value = 0;</p><p>// This method is executed by one thread public void Thread1() {</p><p>// Note: 5 must be written to m_value before 1 is written to m_flag m_value = 5; Volatile.Write(ref m_flag, 1);</p><p>}</p><p>// This method is executed by another thread public void Thread2() {</p><p>// Note: m_value must be read after m_flag is read if (Volatile.Read(ref m_flag) == 1) Console.WriteLine(m_value);</p><p>} }</p><p>First, notice that we are following the rule. The Thread1 method writes two values out to fields that are shared by multiple threads. The last value that we want written (setting m_flag to 1) is performed by calling Volatile.Write. The Thread2 method reads two values from fields shared by multiple threads, and the first value being read (m_flag) is performed by calling Volatile.Read.</p><p>But what is really happening here? Well, for the Thread1 method, the Volatile.Write call ensures that all the writes above it are completed before a 1 is written to m_flag. Because m_value</p><p>= 5 is before the call to Volatile.Write, it must complete first. In fact, if there were many variables being modified before the call to Volatile.Write, they would all have to complete before 1 is written to m_flag. Note that the writes before the call to Volatile.Write can be optimized to execute in any order; it\u2019s just that all the writes have to complete before the call to Volatile.Write.</p><p>For the Thread2 method, the Volatile.Read call ensures that all variable reads after it start after the value in m_flag has been read. Because reading m_value is after the call to Volatile.Read, the value must be read after having read the value in m_flag. If there were many reads after the call to Volatile.Read, they would all have to start after the value in m_flag has been read. Note that the reads after the call to Volatile.Read can be optimized to execute in any order; it\u2019s just that the reads can\u2019t start happening until after the call to Volatile.Read.</p><h6>C#\u2019s Support for Volatile Fields</h6><p>Making sure that programmers call the Volatile.Read and Volatile.Write methods correctly is a lot to ask. It\u2019s hard for programmers to keep all of this in their minds and to start imagining what other threads might be doing to shared data in the background. To simplify this, the C# compiler has the volatile keyword, which can be applied to static or instance fields of any of these types: Boolean, (S)Byte, (U)Int16, (U)Int32, (U)IntPtr, Single, or Char. You can also apply the volatile keyword to reference types and any enum field as long as the enumerated type has an</p><p>underlying type of (S)Byte, (U)Int16, or (U)Int32. The JIT compiler ensures that all accesses to a volatile field are performed as volatile reads and writes, so that it is not necessary to explicitly call Volatile's static Read or Write methods. Furthermore, the volatile keyword tells the C# and JIT</p><p>compilers not to cache the field in a CPU register, ensuring that all reads to and from the field actually cause the value to be read from memory.</p><p>Using the volatile keyword, we can rewrite the ThreadsSharingData class as follows.</p><p>internal sealed class ThreadsSharingData { private volatile Int32 m_flag = 0; private Int32 m_value = 0;</p><p>// This method is executed by one thread public void Thread1() {</p><p>// Note: 5 must be written to m_value before 1 is written to m_flag m_value = 5; m_flag = 1;</p><p>}</p><p>}</p><p>// This method is executed by another thread public void Thread2() {</p><p>// Note: m_value must be read after m_flag is read if (m_flag == 1)</p><p>Console.WriteLine(m_value); }</p><p>There are some developers (and I am one of them) who do not like C#\u2019s volatile keyword, and they think that the language should not provide it.6 Our thinking is that most algorithms require few volatile read or write accesses to a field and that most other accesses to the field can occur normally, improving performance; seldom is it required that all accesses to a field be volatile. For example, it is difficult to interpret how to apply volatile read operations to algorithms like this one.</p><p>m_amount = m_amount + m_amount; // Assume m_amount is a volatile field defined in a class</p><p>Normally, an integer number can be doubled simply by shifting all bits left by 1 bit, and many compilers can examine the preceding code and perform this optimization. However, if m_amount is a volatile field, then this optimization is not allowed. The compiler must produce code to read m_amount into a register and then read it again into another register, add the two registers together,</p><p>6 By the way, it is good to see that Microsoft Visual Basic does not offer a volatile semantic built into its language.</p><p>and then write the result back out to the m_amount field. The unoptimized code is certainly bigger and slower; it would be unfortunate if it were contained inside a loop.</p><p>Furthermore, C# does not support passing a volatile field by reference to a method. For example, if m_amount is defined as a volatile Int32, attempting to call Int32\u2019s TryParse method causes the compiler to generate a warning as shown here.</p><p>Boolean success = Int32.TryParse(\"123\", out m_amount); // The preceding line causes the C# compiler to generate a warning: // CS0420: a reference to a volatile field will not be treated as volatile</p><p>Finally, volatile fields are not Common Language Specification (CLS) compliant because many languages (including Visual Basic) do not support them.</p><h6>Interlocked Constructs</h6><p>Volatile\u2019s Read method performs an atomic read operation, and its Write method performs an atomic write operation. That is, each method performs either an atomic read operation or an atomic write operation. In this section, we look at the static System.Threading.Interlocked class\u2019s methods. Each of the methods in the Interlocked class performs an atomic read and write operation. In addition, all the Interlocked methods are full memory fences. That is, any variable writes before the call to an Interlocked method execute before the Interlocked method, and any variable reads after the call execute after the call.</p><p>The static methods that operate on Int32 variables are by far the most commonly used methods. I show them here.</p><p>public static class Interlocked { // return (++location) public static Int32 Increment(ref Int32 location);</p><p>// return (\u00ad\u00adlocation) public static Int32 Decrement(ref Int32 location);</p><p>// return (location += value) // Note: value can be a negative number allowing subtraction public static Int32 Add(ref Int32 location, Int32 value);</p><p>// Int32 old = location; location = value; return old; public static Int32 Exchange(ref Int32 location, Int32 value);</p><p>// Int32 old = location; // if (location == comparand) location = value; // return old; public static Int32 CompareExchange(ref Int32 location, Int32 value, Int32 comparand);</p><p>... }</p><p>There are also overloads of the preceding methods that operate on Int64 values. Furthermore, the Interlocked class offers Exchange and CompareExchange methods that take Object, IntPtr, Single, and Double, and there is also a generic version in which the generic type is constrained to class (any reference type).</p><p>Personally, I love the Interlocked methods, because they are relatively fast and you can do so much with them. Let me show you some code that uses the Interlocked methods to asynchronously query several web servers and concurrently process the returned data. This code is pretty short, never blocks any threads, and uses thread pool threads to scale automatically, consuming up to the number of CPUs available if its workload could benefit from it. In addition, the code, as is, supports accessing up to 2,147,483,647 (Int32.MaxValue) web servers. In other words, this code is a great model to follow for your own scenarios.</p><p>internal sealed class MultiWebRequests { // This helper class coordinates all the asynchronous operations private AsyncCoordinator m_ac = new AsyncCoordinator();</p><p>// Set of web servers we want to query &amp; their responses (Exception or Int32) // NOTE: Even though multiple could access this dictionary simultaneously, // there is no need to synchronize access to it because the keys are // read\u00adonly after construction private Dictionary&lt;String, Object&gt; m_servers = new Dictionary&lt;String, Object&gt; {</p><p>{ \"http://Wintellect.com/\", null }, { \"http://Microsoft.com/\", null }, { \"http://1.1.1.1/\", null }</p><p>};</p><p>public MultiWebRequests(Int32 timeout = Timeout.Infinite) { // Asynchronously initiate all the requests all at once var httpClient = new HttpClient(); foreach (var server in m_servers.Keys) {</p><p>m_ac.AboutToBegin(1); httpClient.GetByteArrayAsync(server)</p><p>.ContinueWith(task =&gt; ComputeResult(server, task)); }</p><p>}</p><p>// Tell AsyncCoordinator that all operations have been initiated and to call // AllDone when all operations complete, Cancel is called, or the timeout occurs m_ac.AllBegun(AllDone, timeout);</p><p>private void ComputeResult(String server, Task&lt;Byte[]&gt; task) { Object result; if (task.Exception != null) {</p><p>result = task.Exception.InnerException;</p><p>} else { // Process I/O completion here on thread pool thread(s) // Put your own compute\u00adintensive algorithm here... result = task.Result.Length; // This example just returns the length</p><p>}</p><p>}</p><p>// Save result (exception/sum) and indicate that 1 operation completed m_servers[server] = result; m_ac.JustEnded();</p><p>// Calling this method indicates that the results don't matter anymore public void Cancel() { m_ac.Cancel(); }</p><p>// This method is called after all web servers respond, // Cancel is called, or the timeout occurs private void AllDone(CoordinationStatus status) {</p><p>switch (status) {</p><p>case CoordinationStatus.Cancel: Console.WriteLine(\"Operation canceled.\"); break;</p><p>case CoordinationStatus.Timeout: Console.WriteLine(\"Operation timed\u00adout.\"); break;</p><p>}</p><p>} }</p><p>case CoordinationStatus.AllDone: Console.WriteLine(\"Operation completed; results below:\"); foreach (var server in m_servers) {</p><p>Console.Write(\"{0} \", server.Key); Object result = server.Value; if (result is Exception) {</p><p>Console.WriteLine(\"failed due to {0}.\", result.GetType().Name); } else {</p><p>Console.WriteLine(\"returned {0:N0} bytes.\", result); }</p><p>} break;</p><p>OK, the preceding code doesn\u2019t actually use any Interlocked methods directly, because I encapsulated all the coordination code in a reusable class called AsyncCoordinator, which I\u2019ll explain shortly. Let me first explain what this class is doing. When the MultiWebRequest class is constructed, it initializes an AsyncCoordinator and a dictionary containing the set of server URIs (and their future result). It then issues all the web requests asynchronously one right after the other. It does this by first calling AsyncCoordinator\u2019s AboutToBegin method, passing it the number of requests about to be issued.7 Then it initiates the request by calling HttpClient\u2019s GetByteArrayAsync. This returns a Task and I then call ContinueWith on this Task so that when the server replies with the bytes, they can be processed by my ComputeResult method concurrently via many thread pool threads. After all the web servers\u2019 requests have been made, the AsyncCoordinator\u2019s AllBegun method is called, passing it the name of the method (AllDone) that should execute when all the operations complete and a timeout value. As each web server responds, various thread pool threads</p><p>7 The code would still work correctly if it was rewritten calling m_ac.AboutToBegin(m_requests.Count) just once before the for loop instead of calling AboutToBegin inside the loop.</p><p>will call the MultiWebRequests\u2019s ComputeResult method. This method processes the bytes returned from the server (or any error that may have occurred) and saves the result in the dictionary collection. After storing each result, AsyncCoordinator\u2019s JustEnded method is called to let the AsyncCoordinator object know that an operation completed.</p><p>If all the operations have completed, then the AsyncCoordinator will invoke the AllDone method to process the results from all the web servers. The code executing the AllDone method will be the thread pool thread that just happened to get the last web server response. If timeout or cancellation occurs, then AllDone will be invoked via whatever thread pool thread notifies the AsyncCoordinator of timeout or using whatever thread happened to call the Cancel method. There is also a chance that the thread issuing the web server requests could invoke AllDone itself if the last request completes before AllBegun is called.</p><p>Note that there is a race because it is possible that all web server requests complete, AllBegun is called, timeout occurs, and Cancel is called all at the exact same time. If this happens, then the AsyncCoordinator will select a winner and three losers, ensuring that the AllDone method is never called more than once. The winner is identified by the status argument passed into AllDone, which can be one of the symbols defined by the CoordinationStatus type.</p><p>internal enum CoordinationStatus { AllDone, Timeout, Cancel };</p><p>Now that you get a sense of what happens, let\u2019s take a look at how it works. The Async\u00ad</p><p>Coordinator class encapsulates all the thread coordination logic in it. It uses Interlocked methods for everything to ensure that the code runs extremely fast and that no threads ever block. Here is the code for this class.</p><p>internal sealed class AsyncCoordinator { private Int32 m_opCount = 1; // Decremented when AllBegun calls JustEnded private Int32 m_statusReported = 0; // 0=false, 1=true private Action&lt;CoordinationStatus&gt; m_callback; private Timer m_timer;</p><p>// This method MUST be called BEFORE initiating an operation public void AboutToBegin(Int32 opsToAdd = 1) {</p><p>Interlocked.Add(ref m_opCount, opsToAdd); }</p><p>// This method MUST be called AFTER an operation\u2019s result has been processed public void JustEnded() {</p><p>if (Interlocked.Decrement(ref m_opCount) == 0)</p><p>ReportStatus(CoordinationStatus.AllDone); }</p><p>// This method MUST be called AFTER initiating ALL operations public void AllBegun(Action&lt;CoordinationStatus&gt; callback,</p><p>Int32 timeout = Timeout.Infinite) {</p><p>}</p><p>m_callback = callback; if (timeout != Timeout.Infinite)</p><p>m_timer = new Timer(TimeExpired, null, timeout, Timeout.Infinite); JustEnded();</p><p>private void TimeExpired(Object o) { ReportStatus(CoordinationStatus.Timeout); } public void Cancel() { ReportStatus(CoordinationStatus.Cancel); }</p><p>}</p><p>private void ReportStatus(CoordinationStatus status) { // If status has never been reported, report it; else ignore it if (Interlocked.Exchange(ref m_statusReported, 1) == 0)</p><p>m_callback(status); }</p><p>The most important field in this class is the m_opCount field. This field keeps track of the number of asynchronous operations that are still outstanding. Just before each asynchronous operation is started, AboutToBegin is called. This method calls Interlocked.Add to add the number passed to it to the m_opCount field in an atomic way. Adding to m_opCount must be performed atomically because web servers could be processing responses on thread pool threads as more operations are being started. As web server responses are processed, JustEnded is called. This method calls Interlocked.Decrement to atomically subtract 1 from m_opCount. Whichever thread happens to set m_opCount to 0 calls ReportStatus.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile248.png\"/><p>Note The m_opCount field is initialized to 1 (not 0); this is critically important because it ensures that AllDone is not invoked while the thread executing the constructor method is still issuing web server requests. Before the constructor calls AllBegun, there is no way that m_opCount will ever reach 0. When the constructor calls AllBegun, AllBegun internally calls JustEnded, which decrements m_opCount and effectively undoes the effect of having initialized it to 1. Now, m_opCount can reach 0, but only after we know that all the web server requests have been initiated.</p><p>The ReportStatus method arbitrates the race that can occur among all the operations complet-</p><p>ing, the timeout occurring, and Cancel being called. ReportStatus must make sure that only one of these conditions is considered the winner so that the m_callback method is invoked only once. Arbitrating the winner is done via calling Interlocked.Exchange, passing it a reference to the</p><p>m_statusReported field. This field is really treated as a Boolean variable; however, it can\u2019t actually be a Boolean variable because there are no Interlocked methods that accept a Boolean variable. So I use an Int32 variable instead where 0 means false and 1 means true.</p><p>Inside ReportStatus, the Interlocked.Exchange call will change m_statusReported to 1. But only the first thread to do this will see Interlocked.Exchange return a 0, and only this thread will invoke the callback method. Any other threads that call Interlocked.Exchange will get a return value of 1, effectively notifying these threads that the callback method has already been invoked and therefore it should not be invoked again.</p><h6>Implementing a Simple Spin Lock</h6><p>The Interlocked methods are great, but they mostly operate on Int32 values. What if you need to manipulate a bunch of fields in a class object atomically? In this case, we need a way to stop all threads but one from entering the region of code that manipulates the fields. Using Interlocked methods, we can build a thread synchronization lock.</p><p>internal struct SimpleSpinLock { private Int32 m_ResourceInUse; // 0=false (default), 1=true</p><p>public void Enter() {</p><p>while (true) { // Always set resource to in\u00aduse // When this thread changes it from not in\u00aduse, return if (Interlocked.Exchange(ref m_ResourceInUse, 1) == 0) return; // Black magic goes here...</p><p>} }</p><p>public void Leave() { // Set resource to not in\u00aduse Volatile.Write(ref m_ResourceInUse, 0);</p><p>} }</p><p>And here is a class that shows how to use the SimpleSpinLock.</p><p>public sealed class SomeResource { private SimpleSpinLock m_sl = new SimpleSpinLock();</p><p>public void AccessResource() { m_sl.Enter(); // Only one thread at a time can get in here to access the resource... m_sl.Leave();</p><p>} }</p><p>The SimpleSpinLock implementation is very simple. If two threads call Enter at the same time, Interlocked.Exchange ensures that one thread changes m_resourceInUse from 0 to 1 and sees that m_resourceInUse was 0. This thread then returns from Enter so that it can continue executing the code in the AccessResource method. The other thread will change m_resourceInUse from a 1 to a 1. This thread will see that it did not change m_resourceInUse from a 0, and this thread will now start spinning continuously, calling Exchange until the first thread calls Leave.</p><p>When the first thread is done manipulating the fields of the SomeResource object, it calls Leave, which internally calls Volatile.Write and changes m_resourceInUse back to a 0. This causes the spinning thread to then change m_resourceInUse from a 0 to a 1, and this thread now gets to return from Enter so that it can access SomeResource object\u2019s fields.</p><p>There you have it. This is a simple implementation of a thread synchronization lock. The big potential problem with this lock is that it causes threads to spin when there is contention for the lock.</p><p>This spinning wastes precious CPU time, preventing the CPU from doing other, more useful work. As a result, spin locks should only ever be used to guard regions of code that execute very quickly.</p><p>Spin locks should not typically be used on single-CPU machines, because the thread that holds the lock can\u2019t quickly release it if the thread that wants the lock is spinning. The situation becomes much worse if the thread holding the lock is at a lower priority than the thread wanting to get the lock, because now the thread holding the lock may not get a chance to run at all, resulting in a livelock situation. Windows sometimes boosts a thread\u2019s priority dynamically for short periods of time. Therefore, boosting should be disabled for threads that are using spin locks; see the PriorityBoostEnabled properties of System.Diagnostics.Process and System.Diagnostics.ProcessThread. There are issues related to using spin locks on hyperthreaded machines, too. In an attempt to circumvent these kinds of problems, many spin locks have some additional logic in them; I refer to the additional logic as Black Magic. I\u2019d rather not go into the details of Black Magic because it changes over time as more people study locks and their performance. However, I will say this: The FCL ships with a structure, System.Threading.SpinWait, which encapsulates the state-of-the-art thinking around this Black Magic.</p><h6>Putting a Delay in the Thread\u2019s Processing</h6><p>The Black Magic is all about having a thread that wants a resource to pause its execution temporarily so that the thread that currently has the resource can execute its code and relinquish the resource. To do this, the SpinWait struct internally calls Thread\u2019s static Sleep, Yield, and SpinWait methods. I\u2019ll briefly describe these methods in this sidebar.</p><p>A thread can tell the system that it does not want to be schedulable for a certain amount of time. This is accomplished by calling Thread\u2019s static Sleep method.</p><p>public static void Sleep(Int32 millisecondsTimeout); public static void Sleep(TimeSpan timeout);</p><p>This method causes the thread to suspend itself until the specified amount of time has elapsed. Calling Sleep allows the thread to voluntarily give up the remainder of its time-slice. The system makes the thread not schedulable for approximately the amount of time specified. That\u2019s right\u2014if you tell the system you want a thread to sleep for 100 milliseconds, the thread will sleep approximately that long, but possibly several seconds or even minutes more. Remember that Windows is not a real-time operating system. Your thread will probably wake up at the right time, but whether it does depends on what else is going on in the system.</p><p>You can call Sleep and pass the value in System.Threading.Timeout.Infinite (defined as \u00ad1) for the millisecondsTimeout parameter. This tells the system to never schedule the thread, and it is not a useful thing to do. It is much better to have the thread exit and then recover its stack and kernel object. You can pass 0 to Sleep. This tells the system that the calling thread relinquishes the remainder of its current time-slice, and it forces the system to schedule another thread. However, the system can reschedule the thread that just called Sleep. This will happen if there are no more schedulable threads at the same priority or higher.</p><p>A thread can ask Windows to schedule another thread on the current CPU by calling Thread\u2019s Yield method.</p><p>public static Boolean Yield();</p><p>If Windows has another thread ready to run on the current processor, then Yield returns true and the thread that called Yield ended its time-slice early, the selected thread gets to run for one time-slice, and then the thread that called Yield is scheduled again and starts running with a fresh new time-slice. If Windows does not have another thread to run on the current processor, then Yield returns false and the thread continues its time-slice.</p><p>The Yield method exists in order to give a thread of equal or lower priority that is starving for CPU time a chance to run. A thread calls this method if it wants a resource that is currently owned by another thread. The hope is that Windows will schedule the thread that currently owns the resource and that this thread will relinquish the resource. Then, when the thread that called Yield runs again, this thread can have the resource.</p><p>Yield is a cross between calling Thread.Sleep(0) and Thread.Sleep(1). Thread. Sleep(0) will not let a lower-priority thread run, whereas Thread.Sleep(1) will always force a context switch and Windows will force the thread to sleep longer than one millisecond due to the resolution of the internal system timer.</p><p>Hyperthreaded CPUs really let only one thread run at a time. So, when executing spin loops on these CPUs, you need to force the current thread to pause so that the CPU switches to the other thread, allowing it to run. A thread can force itself to pause, allowing a hyperthreaded CPU to switch to its other thread by calling Thread\u2019s SpinWait method.</p><p>public static void SpinWait(Int32 iterations);</p><p>Calling this method actually executes a special CPU instruction; it does not tell Windows to do anything (because Windows already thinks that it has scheduled two threads on the CPU). On a non-hyperthreaded CPU, this special CPU instruction is simply ignored.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile249.png\"/><p>Note For more information about these methods, see their Win32 equivalents: Sleep, SwitchToThread, and YieldProcessor. You can also learn more about adjusting the resolution of the system timer by looking up the Win32 timeBeginPeriod and timeEndPeriod functions.</p><p>The FCL also includes a System.Threading.SpinLock structure that is similar to my SimpleSpinLock class shown earlier, except that it uses the SpinWait structure to improve performance. The SpinLock structure also offers timeout support. By the way, it is interesting to note that my SimpleSpinLock and the FCL\u2019s SpinLock are both value types. This means that they are lightweight, memory-friendly objects. A SpinLock is a good choice if you need to associate a lock with each item in a collection, for example. However, you must make sure that you do not pass SpinLock instances</p><p>around, because they are copied and you will lose any and all synchronization. And although you can define instance SpinLock fields, do not mark the field as readonly, because its internal state must change as the lock is manipulated.</p><h6>The Interlocked Anything Pattern</h6><p>Many people look at the Interlocked methods and wonder why Microsoft doesn't create a richer set of interlocked methods that can be used in a wider range of scenarios. For example, it would be nice if the Interlocked class offered Multiply, Divide, Minimum, Maximum, And, Or, Xor, and a bunch of other methods. Although the Interlocked class doesn\u2019t offer these methods, there is a well-known pattern that allows you to perform any operation on an Int32 in an atomic way by using Interlocked.CompareExchange. In fact, because Interlocked.CompareExchange has additional overloads that operate on Int64, Single, Double, Object, and a generic reference type, this pattern will actually work for all these types, too.</p><p>This pattern is similar to optimistic concurrency patterns used for modifying database records. Here is an example of the pattern that is being used to create an atomic Maximum method.</p><p>public static Int32 Maximum(ref Int32 target, Int32 value) { Int32 currentVal = target, startVal, desiredVal;</p><p>// Don't access target in the loop except in an attempt // to change it because another thread may be touching it do {</p><p>// Record this iteration's starting value startVal = currentVal;</p><p>// Calculate the desired value in terms of startVal and value desiredVal = Math.Max(startVal, value);</p><p>// NOTE: the thread could be preempted here!</p><p>// if (target == startVal) target = desiredVal // Value prior to potential change is returned currentVal = Interlocked.CompareExchange(ref target, desiredVal, startVal);</p><p>// If the starting value changed during this iteration, repeat } while (startVal != currentVal);</p><p>}</p><p>// Return the maximum value when this thread tried to set it return desiredVal;</p><p>Now let me explain exactly what is going on here. Upon entering the method, currentVal is initialized to the value in target at the moment the method starts executing. Then, inside the loop, startVal is initialized to this same value. Using startVal, you can perform any operation you want. This operation can be extremely complex, consisting of thousands of lines of code. But, ultimately, you must end up with a result that is placed into desiredVal. In my example, I simply determine whether startVal or value contains the larger value.</p><p>Now, while this operation is running, another thread could change the value in target. It is unlikely that this will happen, but it is possible. If this does happen, then the value in desiredVal is based off an old value in startVal, not the current value in target, and therefore, we should not change the value in target. To ensure that the value in target is changed to desiredVal if no thread has changed target behind our thread\u2019s back, we use Interlocked.CompareExchange. This method checks whether the value in target matches the value in startVal (which identifies the value that we thought was in target before starting to perform the operation). If the value in target didn\u2019t change, then CompareExchange changes it to the new value in desiredVal. If the value in target did change, then CompareExchange does not alter the value in target at all.</p><p>CompareExchange returns the value that is in target at the time when CompareExchange is called, which I then place in currentVal. Then, a check is made comparing startVal with the new value in currentVal. If these values are the same, then a thread did not change target behind our thread\u2019s back, target now contains the value in desiredVal, the while loop does not loop around, and the method returns. If startVal is not equal to currentVal, then a thread did change the value in target behind our thread\u2019s back, target did not get changed to our value in desiredVal, and the while loop will loop around and try the operation again, this time using the new value in currentVal that reflects the other thread\u2019s change.</p><p>Personally, I have used this pattern in a lot of my own code and, in fact, I made a generic method, Morph, which encapsulates this pattern.8</p><p>delegate Int32 Morpher&lt;TResult, TArgument&gt;(Int32 startValue, TArgument argument, out TResult morphResult);</p><p>static TResult Morph&lt;TResult, TArgument&gt;(ref Int32 target, TArgument argument, Morpher&lt;TResult, TArgument&gt; morpher) {</p><p>}</p><p>TResult morphResult; Int32 currentVal = target, startVal, desiredVal; do {</p><p>startVal = currentVal; desiredVal = morpher(startVal, argument, out morphResult); currentVal = Interlocked.CompareExchange(ref target, desiredVal, startVal);</p><p>} while (startVal != currentVal); return morphResult;</p><p>8 Obviously, the Morph method incurs a performance penalty due to invoking the morpher callback method. For best performance, execute the operation inline, as in the Maximum example.</p><h6>Kernel-Mode Constructs</h6><p>Windows offers several kernel-mode constructs for synchronizing threads. The kernel-mode constructs are much slower than the user-mode constructs. This is because they require coordination from the Windows operating system itself. Also, each method call on a kernel object causes the calling thread to transition from managed code to native user-mode code to native kernel-mode code and then return all the way back. These transitions require a lot of CPU time and, if performed frequently, can adversely affect the overall performance of your application.</p><p>However, the kernel-mode constructs offer some benefits over the primitive user-mode constructs, such as:</p><p>\u25a0\u25a0 When a kernel-mode construct detects contention on a resource, Windows blocks the losing</p><p>thread so that it is not spinning on a CPU, wasting processor resources.</p><p>\u25a0\u25a0 Kernel-mode constructs can synchronize native and managed threads with each other.</p><p>\u25a0\u25a0 Kernel-mode constructs can synchronize threads running in different processes on the same</p><p>machine.</p><p>\u25a0\u25a0 Kernel-mode constructs can have security applied to them to prevent unauthorized accounts</p><p>from accessing them.</p><p>\u25a0\u25a0 A thread can block until all kernel-mode constructs in a set are available or until any one</p><p>kernel-mode construct in a set has become available.</p><p>\u25a0\u25a0 A thread can block on a kernel-mode construct specifying a timeout value; if the thread can\u2019t have access to the resource it wants in the specified amount of time, then the thread is unblocked and can perform other tasks.</p><p>The two primitive kernel-mode thread synchronization constructs are events and semaphores.</p><p>Other kernel-mode constructs, such as mutex, are built on top of the two primitive constructs. For more information about the Windows kernel-mode constructs, see the book, Windows via C/C++, Fifth Edition (Microsoft Press, 2007) by myself and Christophe Nasarre.</p><p>The System.Threading namespace offers an abstract base class called WaitHandle. The Wait\u00ad</p><p>Handle class is a simple class whose sole purpose is to wrap a Windows kernel object handle. The FCL provides several classes derived from WaitHandle. All classes are defined in the System.Threading namespace. The class hierarchy looks like this.</p><p>WaitHandle</p><p>EventWaitHandle AutoResetEvent ManualResetEvent</p><p>Semaphore Mutex</p><p>Internally, the WaitHandle base class has a SafeWaitHandle field that holds a Win32 kernel object handle. This field is initialized when a concrete WaitHandle-derived class is constructed. In addition, the WaitHandle class publicly exposes methods that are inherited by all the derived classes. Every method called on a kernel-mode construct represents a full memory fence. WaitHandle\u2019s interesting public methods are shown in the following code (some overloads for some methods are not shown).</p><p>public abstract class WaitHandle : MarshalByRefObject, IDisposable { // WaitOne internally calls the Win32 WaitForSingleObjectEx function. public virtual Boolean WaitOne(); public virtual Boolean WaitOne(Int32 millisecondsTimeout); public virtual Boolean WaitOne(TimeSpan timeout);</p><p>// WaitAll internally calls the Win32 WaitForMultipleObjectsEx function public static Boolean WaitAll(WaitHandle[] waitHandles); public static Boolean WaitAll(WaitHandle[] waitHandles, Int32 millisecondsTimeout); public static Boolean WaitAll(WaitHandle[] waitHandles, TimeSpan timeout);</p><p>// WaitAny internally calls the Win32 WaitForMultipleObjectsEx function public static Int32 WaitAny(WaitHandle[] waitHandles); public static Int32 WaitAny(WaitHandle[] waitHandles, Int32 millisecondsTimeout); public</p><p>static Int32 WaitAny(WaitHandle[] waitHandles, TimeSpan timeout); public const Int32 WaitTimeout = 258; // Returned from WaitAny if a timeout occurs</p><p>// Dispose internally calls the Win32 CloseHandle function \u2013 DON\u2019T CALL THIS. public void Dispose();</p><p>}</p><p>There are a few things to note about these methods:</p><p>\u25a0\u25a0 You call WaitHandle\u2019s WaitOne method to have the calling thread wait for the underlying</p><p>kernel object to become signaled. Internally, this method calls the Win32 WaitForSingleObjectEx function. The returned Boolean is true if the object became signaled or false if a timeout occurs.</p><p>\u25a0\u25a0 You call WaitHandle\u2019s static WaitAll method to have the calling thread wait for all the kernel objects specified in the WaitHandle[] to become signaled. The returned Boolean is true if all of the objects became signaled or false if a timeout occurs. Internally, this method calls the Win32 WaitForMultipleObjectsEx function, passing TRUE for the bWaitAll parameter.</p><p>\u25a0\u25a0 You call WaitHandle\u2019s static WaitAny method to have the calling thread wait for any one of the kernel objects specified in the WaitHandle[] to become signaled. The returned Int32 is the index of the array element corresponding to the kernel object that became signaled, or WaitHandle.WaitTimeout if no object became signaled while waiting. Internally, this method calls the Win32 WaitForMultipleObjectsEx function, passing FALSE for the bWaitAll parameter.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile250.png\"/><p>\u25a0\u25a0 The array that you pass to the WaitAny and WaitAll methods must contain no more than 64</p><p>elements or else the methods throw a System.NotSupportedException.</p><p>\u25a0\u25a0 You call WaitHandle\u2019s Dispose method to close the underlying kernel object handle. Internally, these methods call the Win32 CloseHandle function. You can only call Dispose explicitly in your code if you know for a fact that no other threads are using the kernel object. This puts a lot of burden on you as you write your code and test it. So, I would strongly discourage you from calling Dispose; instead, just let the garbage collector (GC) do the cleanup. The GC knows when no threads are using the object anymore, and then it will get rid of it. In a way, the GC is doing thread synchronization for you automatically!</p><p>Note In some cases, when a COM single-threaded apartment thread blocks, the thread can wake up internally to pump messages. For example, the blocked thread will wake to process a Windows message sent from another thread. This is done to support COM interoperability. For most applications, this is not a problem\u2014in fact, it is a good thing. However, if your code takes another thread synchronization lock during the processing of the message, then deadlock could occur. As you\u2019ll see in Chapter 30, all the hybrid locks call these methods internally, so the same potential benefit or problem exists when using the hybrid locks as well.</p><p>The versions of the WaitOne and WaitAll that do not accept a timeout parameter should be pro-</p><p>totyped as having a void return type, not Boolean. The reason is because these methods will return only true because the implied timeout is infinite (System.Threading.Timeout.Infinite). When you call any of these methods, you do not need to check their return value.</p><p>As already mentioned, the AutoResetEvent, ManualResetEvent, Semaphore, and Mutex classes are all derived from WaitHandle, so they inherit WaitHandle\u2019s methods and their behavior. However, these classes introduce additional methods of their own, and I\u2019ll address those now.</p><p>First, the constructors for all of these classes internally call the Win32 CreateEvent (passing</p><p>FALSE for the bManualReset parameter) or CreateEvent (passing TRUE for the bManualReset parameter), CreateSemaphore, or CreateMutex functions. The handle value returned from all of these calls is saved in a private SafeWaitHandle field defined inside the WaitHandle base class.</p><p>Second, the EventWaitHandle, Semaphore, and Mutex classes all offer static OpenExisting</p><p>methods, which internally call the Win32 OpenEvent, OpenSemaphore, or OpenMutex functions, passing a String argument that identifies an existing named kernel object. The handle value returned from all of these functions is saved in a newly constructed object that is returned from the</p><p>OpenExisting method. If no kernel object exists with the specified name, a WaitHandleCannotBeOpenedException is thrown.</p><p>A common usage of the kernel-mode constructs is to create the kind of application that allows only one instance of itself to execute at any given time. Examples of single-instance applications are Microsoft Outlook, Windows Live Messenger, Windows Media Player, and Windows Media Center. Here is how to implement a single-instance application.</p><p>using System; using System.Threading;</p><p>public static class Program { public static void Main() { Boolean createdNew;</p><p>} }</p><p>// Try to create a kernel object with the specified name using (new Semaphore(0, 1, \"SomeUniqueStringIdentifyingMyApp\", out createdNew)) {</p><p>if (createdNew) { // This thread created the kernel object so no other instance of this // application must be running. Run the rest of the application here...</p><p>} else { // This thread opened an existing kernel object with the same string name; // another instance of this application must be running now. // There is nothing to do in here, let's just return from Main to terminate // this second instance of the application.</p><p>} }</p><p>In this code, I am using a Semaphore, but it would work just as well if I had used an EventWaitHandle or a Mutex because I\u2019m not actually using the thread synchronization behavior that the object offers. However, I am taking advantage of some thread synchronization behavior that the kernel offers when creating any kind of kernel object. Let me explain how the preceding code works. Let\u2019s say that two instances of this process are started at exactly the same time. Each process will have its own thread, and both threads will attempt to create a Semaphore with the same string name (\u201cSomeUniqueStringIdentifyingMyApp,\u201d in my example). The Windows kernel ensures that only one thread actually creates a kernel object with the specified name; the thread that created the object will have its createdNew variable set to true.</p><p>For the second thread, Windows will see that a kernel object with the specified name already exists; the second thread does not get to create another kernel object with the same name, although if this thread continues to run, it can access the same kernel object as the first process\u2019s thread. This is how threads in different processes can communicate with each other via a single kernel object. However, in this example, the second process\u2019s thread sees that its createdNew variable is set to false. This thread now knows that another instance of this process is running, and the second instance of the process exits immediately.</p><h6>Event Constructs</h6><p>Events are simply Boolean variables maintained by the kernel. A thread waiting on an event blocks when the event is false and unblocks when the event is true. There are two kinds of events. When an auto-reset event is true, it wakes up just one blocked thread, because the kernel automatically resets the event back to false after unblocking the first thread. When a manual-reset event is true, it unblocks all threads waiting for it because the kernel does not automatically reset the event back to false; your code must manually reset the event back to false. The classes related to events look like this.</p><p>public class EventWaitHandle : WaitHandle { public Boolean Set(); // Sets Boolean to true; always returns true public Boolean Reset(); // Sets Boolean to false; always returns true</p><p>}</p><p>public sealed class AutoResetEvent : EventWaitHandle {</p><p>public AutoResetEvent(Boolean initialState); }</p><p>public sealed class ManualResetEvent : EventWaitHandle {</p><p>public ManualResetEvent(Boolean initialState); }</p><p>Using an auto-reset event, we can easily create a thread synchronization lock whose behavior is similar to the SimpleSpinLock class I showed earlier.</p><p>internal sealed class SimpleWaitLock : IDisposable { private readonly AutoResetEvent m_available;</p><p>public SimpleWaitLock() {</p><p>m_available = new AutoResetEvent(true); // Initially free }</p><p>public void Enter() { // Block in kernel until resource available m_available.WaitOne();</p><p>}</p><p>public void Leave() { // Let another thread access the resource m_available.Set();</p><p>}</p><p>public void Dispose() { m_available.Dispose(); } }</p><p>You would use this SimpleWaitLock exactly the same way that you\u2019d use the SimpleSpinLock. In fact, the external behavior is exactly the same; however, the performance of the two locks is radically different. When there is no contention on the lock, the SimpleWaitLock is much slower than the SimpleSpinLock, because every call to SimpleWaitLock\u2019s Enter and Leave methods forces the calling thread to transition from managed code to the kernel and back\u2014which is bad. But when there is contention, the losing thread is blocked by the kernel and is not spinning and wasting CPU</p><p>cycles\u2014which is good. Note also that constructing the AutoResetEvent object and calling Dispose on it also causes managed to kernel transitions, affecting performance negatively. These calls usually happen rarely, so they are not something to be too concerned about.</p><p>To give you a better feel for the performance differences, I wrote the following code.</p><p>public static void Main() { Int32 x = 0; const Int32 iterations = 10000000; // 10 million</p><p>// How long does it take to increment x 10 million times? Stopwatch sw = Stopwatch.StartNew(); for (Int32 i = 0; i &lt; iterations; i++) {</p><p>x++;</p><p>} Console.WriteLine(\"Incrementing x: {0:N0}\", sw.ElapsedMilliseconds);</p><p>// How long does it take to increment x 10 million times // adding the overhead of calling a method that does nothing? sw.Restart(); for (Int32 i = 0; i &lt; iterations; i++) {</p><p>M(); x++; M();</p><p>} Console.WriteLine(\"Incrementing x in M: {0:N0}\", sw.ElapsedMilliseconds);</p><p>// How long does it take to increment x 10 million times // adding the overhead of calling an uncontended SimpleSpinLock? SpinLock sl = new SpinLock(false); sw.Restart(); for (Int32 i = 0; i &lt; iterations; i++) {</p><p>Boolean taken = false; sl.Enter(ref taken); x++; sl.Exit();</p><p>} Console.WriteLine(\"Incrementing x in SpinLock: {0:N0}\", sw.ElapsedMilliseconds);</p><p>// How long does it take to increment x 10 million times // adding the overhead of calling an uncontended SimpleWaitLock? using (SimpleWaitLock swl = new SimpleWaitLock()) {</p><p>sw.Restart(); for (Int32 i = 0; i &lt; iterations; i++) {</p><p>swl.Enter(); x++; swl.Leave();</p><p>} Console.WriteLine(\"Incrementing x in SimpleWaitLock: {0:N0}\", sw.ElapsedMilliseconds);</p><p>} }</p><p>[MethodImpl(MethodImplOptions.NoInlining)] private static void M() { /* This method does nothing but return */ }</p><p>When I run the preceding code, I get the following output.</p><p>Incrementing x: 8 Fastest Incrementing x in M: 69 ~9x slower Incrementing x in SpinLock: 164 ~21x slower Incrementing x in SimpleWaitLock: 8,854 ~1,107x slower</p><p>As you can clearly see, just incrementing x took only 8 milliseconds. To call empty methods before and after incrementing x made the operation take nine times longer! Then, executing code in a method that uses a user-mode construct caused the code to run 21 (164 / 8) times slower. But now, see how much slower the program ran using a kernel-mode construct: 1,107 (8,854 / 8) times slower! So, if you can avoid thread synchronization, you should. If you need thread synchronization, then try to use the user-mode constructs. Always try to avoid the kernel-mode constructs.</p><h6>Semaphore Constructs</h6><p>Semaphores are simply Int32 variables maintained by the kernel. A thread waiting on a semaphore blocks when the semaphore is 0 and unblocks when the semaphore is greater than 0. When a thread waiting on a semaphore unblocks, the kernel automatically subtracts 1 from the semaphore\u2019s count. Semaphores also have a maximum Int32 value associated with them, and the current count is never allowed to go over the maximum count. Here is what the Semaphore class looks like.</p><p>public sealed class Semaphore : WaitHandle { public Semaphore(Int32 initialCount, Int32 maximumCount); public Int32 Release(); // Calls Release(1); returns previous count public Int32 Release(Int32 releaseCount); // Returns previous count</p><p>}</p><p>So now let me summarize how these three kernel-mode primitives behave:</p><p>\u25a0\u25a0 When multiple threads are waiting on an auto-reset event, setting the event causes only one</p><p>thread to become unblocked.</p><p>\u25a0\u25a0 When multiple threads are waiting on a manual-reset event, setting the event causes all</p><p>threads to become unblocked.</p><p>\u25a0\u25a0 When multiple threads are waiting on a semaphore, releasing the semaphore causes releaseCount threads to become unblocked (where releaseCount is the argument passed to Semaphore\u2019s Release method).</p><p>Therefore, an auto-reset event behaves very similarly to a semaphore whose maximum count is 1. The difference between the two is that Set can be called multiple times consecutively on an auto-reset event, and still only one thread will be unblocked, whereas calling Release multiple times consecutively on a semaphore keeps incrementing its internal count, which could unblock many threads. By the way, if you call Release on a semaphore too many times, causing its count to exceed its maximum count, then Release will throw a SemaphoreFullException.</p><p>Using a semaphore, we can re-implement the SimpleWaitLock as follows, so that it gives multiple threads concurrent access to a resource (which is not necessarily a safe thing to do unless all threads access the resource in a read-only fashion).</p><p>public sealed class SimpleWaitLock : IDisposable { private readonly Semaphore m_available;</p><p>public SimpleWaitLock(Int32 maxConcurrent) {</p><p>m_available = new Semaphore(maxConcurrent, maxConcurrent); }</p><p>public void Enter() { // Block in kernel until resource available m_available.WaitOne();</p><p>}</p><p>public void Leave() { // Let another thread access the resource m_available.Release(1);</p><p>}</p><p>public void Dispose() { m_available.Close(); } }</p><h6>Mutex Constructs</h6><p>A Mutex represents a mutual-exclusive lock. It works similar to an AutoResetEvent or a Semaphore with a count of 1 because all three constructs release only one waiting thread at a time. The following shows what the Mutex class looks like.</p><p>public sealed class Mutex : WaitHandle { public Mutex(); public void ReleaseMutex();</p><p>}</p><p>Mutexes have some additional logic in them, which makes them more complex than the other constructs. First, Mutex objects record which thread obtained it by querying the calling thread\u2019s Int32 ID. When a thread calls ReleaseMutex, the Mutex makes sure that the calling thread is the same thread that obtained the Mutex. If the calling thread is not the thread that obtained the Mutex, then the Mutex object\u2019s state is unaltered and ReleaseMutex throws a System.ApplicationException. Also, if a thread owning a Mutex terminates for any reason, then some thread waiting on the Mutex will be awakened by having a System.Threading.AbandonedMutexException thrown. Usually, this exception will go unhandled, terminating the whole process. This is good because a thread acquired the Mutex and it is likely that the thread terminated before it finished updating the data that the Mutex was protecting. If a thread catches AbandonedMutexException, then it could attempt to access the corrupt data, leading to unpredictable results and security problems.</p><p>Second, Mutex objects maintain a recursion count indicating how many times the owning thread owns the Mutex. If a thread currently owns a Mutex and then that thread waits on the Mutex again, the recursion count is incremented and the thread is allowed to continue running. When that thread calls ReleaseMutex, the recursion count is decremented. Only when the recursion count becomes 0 can another thread become the owner of the Mutex.</p><p>Most people do not like this additional logic. The problem is that these \u201cfeatures\u201d have a cost associated with them. The Mutex object needs more memory to hold the additional thread ID and recursion count information. And, more importantly, the Mutex code has to maintain this information, which makes the lock slower. If an application needs or wants these additional features, then the application code could have done this itself; the code doesn\u2019t have to be built into the Mutex object. For this reason, a lot of people avoid using Mutex objects.</p><p>Usually a recursive lock is needed when a method takes a lock and then calls another method that also requires the lock, as the following code demonstrates.</p><p>internal class SomeClass : IDisposable { private readonly Mutex m_lock = new Mutex();</p><p>public void Method1() { m_lock.WaitOne(); // Do whatever... Method2(); // Method2 recursively acquires the lock m_lock.ReleaseMutex();</p><p>}</p><p>public void Method2() { m_lock.WaitOne(); // Do whatever... m_lock.ReleaseMutex();</p><p>}</p><p>public void Dispose() { m_lock.Dispose(); } }</p><p>In the preceding code, code that uses a SomeClass object could call Method1, which acquires the Mutex, performs some thread-safe operation, and then calls Method2, which also performs some thread-safe operation. Because Mutex objects support recursion, the thread will acquire the lock twice and then release it twice before another thread can own the Mutex. If SomeClass has used an AutoResetEvent instead of a Mutex, then the thread would block when it called Method2\u2019s WaitOne method.</p><p>If you need a recursive lock, then you could create one easily by using an AutoResetEvent.</p><p>internal sealed class RecursiveAutoResetEvent : IDisposable { private AutoResetEvent m_lock = new AutoResetEvent(true); private Int32 m_owningThreadId = 0; private Int32 m_recursionCount = 0;</p><p>public void Enter() { // Obtain the calling thread's unique Int32 ID Int32 currentThreadId = Thread.CurrentThread.ManagedThreadId;</p><p>// If the calling thread owns the lock, increment the recursion count if (m_owningThreadId == currentThreadId) {</p><p>m_recursionCount++; return;</p><p>}</p><p>// The calling thread doesn't own the lock, wait for it m_lock.WaitOne();</p><p>}</p><p>// The calling now owns the lock, initialize the owning thread ID &amp; recursion count m_owningThreadId = currentThreadId; m_recursionCount = 1;</p><p>public void Leave() { // If the calling thread doesn't own the lock, we have an error if (m_owningThreadId != Thread.CurrentThread.ManagedThreadId)</p><p>throw new InvalidOperationException();</p><p>// Subtract 1 from the recursion count if (\u00ad\u00adm_recursionCount == 0) {</p><p>// If the recursion count is 0, then no thread owns the lock m_owningThreadId = 0; m_lock.Set(); // Wake up 1 waiting thread (if any)</p><p>} }</p><p>public void Dispose() { m_lock.Dispose(); } }</p><p>Although the behavior of the RecursiveAutoResetEvent class is identical to that of the Mutex class, a RecursiveAutoResetEvent object will have far superior performance when a thread tries to acquire the lock recursively, because all the code that is required to track thread ownership and recursion is now in managed code. A thread has to transition into the Windows kernel only when first acquiring the AutoResetEvent or when finally relinquishing it to another thread.</p><h6>C H A P T E R 3 0</h6><h2>Hybrid Thread Synchronization Constructs</h2><h6>In this chapter:</h6><p>A Simple Hybrid Lock . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 790 Spinning, Thread Ownership, and Recursion. . . . . . . . . . . . . . . . 791 Hybrid Constructs in the Framework Class Library . . . . . . . . . . 793 The Famous Double-Check Locking Technique . . . . . . . . . . . . . 807 The Condition Variable Pattern. . . . . . . . . . . . . . . . . . . . . . . . . . . . 811 Asynchronous Synchronization. . . . . . . . . . . . . . . . . . . . . . . . . . . . 814 The Concurrent Collection Classes . . . . . . . . . . . . . . . . . . . . . . . . . 818</p><p>In Chapter 29, \u201cPrimitive Thread Synchronization Constructs,\u201d I discussed the primitive user-mode and kernel-mode thread synchronization constructs. From these primitive constructs, all other thread synchronization constructs can be built. Typically, other thread synchronization constructs are built by combining the user-mode and kernel-mode constructs, and I call these hybrid thread synchronization constructs. Hybrid constructs provide the performance benefit of the primitive user-mode constructs when there is no thread contention. Hybrid constructs also use the primitive kernel-mode constructs to provide the benefit of not spinning (wasting CPU time) when multiple threads are contending for the construct at the same time. Because, in most applications, threads are rarely contending for a construct at the same time, the performance improvements can help your application greatly.</p><p>In this chapter, I will first show how hybrid constructs are built from the various primitive constructs. Then, I will introduce you to many of the hybrid constructs that ship with the Framework Class Library (FCL), describe their behavior, and give some insight as to how to use these constructs correctly. I will also mention some constructs that I have created and make available for free in Wintellect\u2019s Power Threading library, which can be downloaded from http://Wintellect.com/PowerThreading.aspx.</p><p>Toward the end of the chapter, I show how to minimize resource usage and improve performance by using the FCL\u2019s concurrent collection classes instead of using some of the hybrid constructs. And finally, I discuss asynchronous synchronization constructs, which allow you to synchronize access to a resource without blocking any threads, thereby reducing resource consumption while improving scalability.</p><p>789</p><h6>A Simple Hybrid Lock</h6><p>So, without further ado, let me start off by showing you an example of a hybrid thread synchronization lock.</p><p>internal sealed class SimpleHybridLock : IDisposable { // The Int32 is used by the primitive user\u00admode constructs (Interlocked methods) private Int32 m_waiters = 0;</p><p>// The AutoResetEvent is the primitive kernel\u00admode construct private readonly AutoResetEvent m_waiterLock = new AutoResetEvent(false);</p><p>public void Enter() { // Indicate that this thread wants the lock if (Interlocked.Increment(ref m_waiters) == 1)</p><p>return; // Lock was free, no contention, just return</p><p>}</p><p>// Another thread has the lock (contention), make this thread wait m_waiterLock.WaitOne(); // Bad performance hit here // When WaitOne returns, this thread now has the lock</p><p>public void Leave() { // This thread is releasing the lock if (Interlocked.Decrement(ref m_waiters) == 0)</p><p>return; // No other threads are waiting, just return</p><p>}</p><p>// Other threads are waiting, wake 1 of them m_waiterLock.Set(); // Bad performance hit here</p><p>public void Dispose() { m_waiterLock.Dispose(); } }</p><p>The SimpleHybridLock contains two fields: an Int32, which will be manipulated via the primitive user-mode constructs, and an AutoResetEvent, which is a primitive kernel-mode construct. To get great performance, the lock tries to use the Int32 and avoid using the AutoResetEvent as much as possible. Just constructing a SimpleHybridLock object causes the AutoResetEvent to be created, and this is a massive performance hit compared to the overhead associated with the Int32 field. Later in this chapter, we\u2019ll see another hybrid construct (AutoResetEventSlim) that avoids the performance hit of creating the AutoResetEvent until the first time contention is detected from multiple threads accessing the lock at the same time. The Dispose method closes the AutoResetEvent, and this is also a big performance hit.</p><p>Although it would be nice to improve the performance of constructing and disposing of a</p><p>SimpleHybridLock object, it would be better to focus on the performance of its Enter and Leave methods because these methods tend to be called many, many times over the object\u2019s lifetime. Let\u2019s focus on these methods now.</p><p>The first thread to call Enter causes Interlocked.Increment to add one to the m_waiters field, making its value 1. This thread sees that there were zero threads waiting for this lock, so the thread gets to return from its call to Enter. The thing to appreciate here is that the thread acquired the lock very quickly. Now, if another thread comes along and calls Enter, this second thread increments m_waiters to 2 and sees that another thread has the lock, so this thread blocks by calling WaitOne using the AutoResetEvent. Calling WaitOne causes the thread to transition into the</p><p>Windows\u2019 kernel, and this is a big performance hit. However, the thread must stop running anyway, so it is not too bad to have a thread waste some time to stop completely. The good news is that the thread is now blocked, and so it is not wasting CPU time by spinning on the CPU, which is what the</p><p>SimpleSpinLock\u2019s Enter method, introduced in Chapter 29, does.</p><p>Now let\u2019s look at the Leave method. When a thread calls Leave, Interlocked.Decrement is called to subtract 1 from the m_waiters field. If m_waiters is now 0, then no other threads are blocked inside a call to Enter and the thread calling Leave can simply return. Again, think about how fast this is: leaving a lock means that a thread subtracts 1 from an Int32, performs a quick if test, and then returns! On the other hand, if the thread calling Leave sees that m_waiters was not 1, then the thread knows that there is contention and that there is at least one other thread blocked in the kernel. This thread must wake up one (and only one) of the blocked threads. It does this by calling Set on AutoResetEvent. This is a performance hit, because the thread must transition into the kernel and back, but this transition occurs only when there was contention. Of course, AutoResetEvent ensures that only one blocked thread wakes up; any other threads blocked on the AutoResetEvent will continue to block until the newly unblocked thread eventually calls Leave.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile251.png\"/><p>Note In reality, any thread could call Leave at any time because the Enter method does not keep a record of which thread successfully acquired the lock. Adding the field and code to maintain this information is easy to do, but it would increase the memory required for the lock object itself and hurt performance of the Enter and Leave methods because they would have to manipulate this field. I would rather have a fast-performing lock and make sure that my code uses it the right way. You\u2019ll notice that events and semaphores do not maintain this kind of information; only mutexes do.</p><h6>Spinning, Thread Ownership, and Recursion</h6><p>Because transitions into the kernel incur such a big performance hit and threads tend to hold on to a lock for very short periods of time, an application\u2019s overall performance can be improved by having a thread spin in user mode for a little while before having the thread transition to kernel mode. If the lock that the thread is waiting for becomes available while spinning, then the transition to kernel mode is avoided.</p><p>In addition, some locks impose a limitation where the thread that acquires the lock must be the thread that releases the lock. And some locks allow the currently owning thread to own the lock</p><p>recursively. The Mutex lock is an example of a lock that has these characteristics.1 Using some fancy logic, it is possible to build a hybrid lock that offers spinning, thread ownership, and recursion. Here is what the code looks like.</p><p>internal sealed class AnotherHybridLock : IDisposable { // The Int32 is used by the primitive user\u00admode constructs (Interlocked methods) private Int32 m_waiters = 0;</p><p>// The AutoResetEvent is the primitive kernel\u00admode construct private AutoResetEvent m_waiterLock = new AutoResetEvent(false);</p><p>// This field controls spinning in an effort to improve performance private Int32 m_spincount = 4000; // Arbitrarily chosen count</p><p>// These fields indicate which thread owns the lock and how many times it owns it private Int32 m_owningThreadId = 0, m_recursion = 0;</p><p>public void Enter() { // If calling thread already owns the lock, increment recursion count and return Int32 threadId = Thread.CurrentThread.ManagedThreadId; if (threadId == m_owningThreadId) { m_recursion++; return; }</p><p>// The calling thread doesn't own the lock, try to get it SpinWait spinwait = new SpinWait(); for (Int32 spinCount = 0; spinCount &lt; m_spincount; spinCount++) {</p><p>// If the lock was free, this thread got it; set some state and return if (Interlocked.CompareExchange(ref m_waiters, 1, 0) == 0) goto GotLock;</p><p>}</p><p>// Black magic: give other threads a chance to run // in hopes that the lock will be released spinwait.SpinOnce();</p><p>// Spinning is over and the lock was still not obtained, try one more time if (Interlocked.Increment(ref m_waiters) &gt; 1) {</p><p>// Still contention, this thread must wait m_waiterLock.WaitOne(); // Wait for the lock; performance hit // When this thread wakes, it owns the lock; set some state and return</p><p>}</p><p>GotLock: // When a thread gets the lock, we record its ID and // indicate that the thread owns the lock once m_owningThreadId = threadId; m_recursion = 1;</p><p>}</p><p>public void Leave() { // If the calling thread doesn't own the lock, there is a bug Int32 threadId = Thread.CurrentThread.ManagedThreadId; if (threadId != m_owningThreadId)</p><p>throw new SynchronizationLockException(\"Lock not owned by calling thread\");</p><p>1 Threads do not spin when waiting on a Mutex object because the Mutex\u2019s code is in the kernel. This means that the thread had to have already transitioned into the kernel to check the Mutex\u2019s state.</p><p>// Decrement the recursion count. If this thread still owns the lock, just return if (\u00ad\u00adm_recursion &gt; 0) return;</p><p>m_owningThreadId = 0; // No thread owns the lock now</p><p>// If no other threads are waiting, just return if (Interlocked.Decrement(ref m_waiters) == 0)</p><p>return;</p><p>}</p><p>// Other threads are waiting, wake 1 of them m_waiterLock.Set(); // Bad performance hit here</p><p>public void Dispose() { m_waiterLock.Dispose(); } }</p><p>As you can see, adding extra behavior to the lock increases the number of fields it has, which increases its memory consumption. The code is also more complex, and this code must execute, which decreases the lock\u2019s performance. In Chapter 29\u2019s \u201cEvent Constructs\u201d section, I compared the performance of incrementing an Int32 without any locking, with a primitive user-mode construct, and with a kernel-mode construct. I repeat the results of those performance tests here and I include the results of using the SimpleHybridlock and the AnotherHybridLock. The results are in fastest to slowest order.</p><p>Incrementing x: 8 Fastest Incrementing x in M: 69 ~9x slower Incrementing x in SpinLock: 164 ~21x slower Incrementing x in SimpleHybridLock: 164 ~21x slower (similar to SpinLock) Incrementing x in AnotherHybridLock: 230 ~29x slower (due to ownership/recursion) Incrementing x in SimpleWaitLock: 8,854 ~1,107x slower</p><p>It is worth noting that the AnotherHybridLock hurts performance as compared to using the SimpleHybridLock. This is due to the additional logic and error checking required managing the thread ownership and recursion behaviors. As you see, every behavior added to a lock impacts its performance.</p><h6>Hybrid Constructs in the Framework Class Library</h6><p>The FCL ships with many hybrid constructs that use fancy logic to keep your threads in user mode, improving your application\u2019s performance. Some of these hybrid constructs also avoid creating the kernel-mode construct until the first time threads contend on the construct. If threads never contend on the construct, then your application avoids the performance hit of creating the object and also avoids allocating memory for the object. A number of the constructs also support the use of a CancellationToken (discussed in Chapter 27, \u201cCompute-Bound Asynchronous Operations\u201d) so that a thread can forcibly unblock other threads that might be waiting on the construct. In this section, I introduce you to these hybrid constructs.</p><h6>The ManualResetEventSlim and SemaphoreSlim Classes</h6><p>The first two hybrid constructs are System.Threading.ManualResetEventSlim and System. Threading.SemaphoreSlim.2 These constructs work exactly like their kernel-mode counterparts, except that both employ spinning in user mode, and they both defer creating the kernel-mode construct until the first time contention occurs. Their Wait methods allow you to pass a timeout and a CancellationToken. Here is what these classes look like (some method overloads are not shown).</p><p>public class ManualResetEventSlim : IDisposable { public ManualResetEventSlim(Boolean initialState, Int32 spinCount); public void Dispose(); public void Reset(); public void Set(); public Boolean Wait(Int32 millisecondsTimeout, CancellationToken cancellationToken);</p><p>public Boolean IsSet { get; } public Int32 SpinCount { get; } public WaitHandle WaitHandle { get; }</p><p>} public class SemaphoreSlim : IDisposable {</p><p>public SemaphoreSlim(Int32 initialCount, Int32 maxCount); public void Dispose(); public Int32 Release(Int32 releaseCount); public Boolean Wait(Int32 millisecondsTimeout, CancellationToken cancellationToken);</p><p>// Special method for use with async and await (see Chapter 28) public Task&lt;Boolean&gt; WaitAsync(Int32 millisecondsTimeout, CancellationToken</p><p>cancellationToken);</p><p>}</p><p>public Int32 CurrentCount { get; } public WaitHandle AvailableWaitHandle { get; }</p><h6>The Monitor Class and Sync Blocks</h6><p>Probably the most-used hybrid thread synchronization construct is the Monitor class, which provides a mutual-exclusive lock supporting spinning, thread ownership, and recursion. This is the most-used construct because it has been around the longest, C# has a built-in keyword to support it, the just-intime (JIT) compiler has built-in knowledge of it, and the common language runtime (CLR) itself uses it on your application\u2019s behalf. However, as you\u2019ll see, there are many problems with this construct, making it easy to produce buggy code. I\u2019ll start by explaining the construct, and then I\u2019ll show the problems and some ways to work around these problems.</p><p>Every object on the heap can have a data structure, called a sync block, associated with it. A sync block contains fields similar to that of the AnotherHybridLock class that appeared earlier in this chapter. Specifically, it has fields for a kernel object, the owning thread\u2019s ID, a recursion count, and a waiting threads count. The Monitor class is a static class whose methods accept a reference to</p><p>2 Although there is no AutoResetEventSlim class, in many situations you can construct a SemaphoreSlim object with a maxCount of 1.</p><p>any heap object, and these methods manipulate the fields in the specified object\u2019s sync block. Here is what the most commonly used methods of the Monitor class look like.</p><p>public static class Monitor { public static void Enter(Object obj); public static void Exit(Object obj);</p><p>// You can also specify a timeout when entered the lock (not commonly used): public static Boolean TryEnter(Object obj, Int32 millisecondsTimeout);</p><p>}</p><p>// I\u2019ll discuss the lockTaken argument later public static void Enter(Object obj, ref Boolean lockTaken); public static void TryEnter(Object obj, Int32 millisecondsTimeout, ref Boolean lockTaken);</p><p>Now obviously, associating a sync block data structure with every object in the heap is quite wasteful, especially because most objects\u2019 sync blocks are never used. To reduce memory usage, the CLR team uses a more efficient way to offer the functionality just described. Here\u2019s how it works: when the CLR initializes, it allocates an array of sync blocks in native heap. As discussed elsewhere in this book, whenever an object is created in the heap, it gets two additional overhead fields associated with it. The first overhead field, the type object pointer, contains the memory address of the type\u2019s type object. The second overhead field, the sync block index, contains an integer index into the array of sync blocks.</p><p>When an object is constructed, the object\u2019s sync block index is initialized to -1, which indicates that it doesn\u2019t refer to any sync block. Then, when Monitor.Enter is called, the CLR finds a free sync block in the array and sets the object\u2019s sync block index to refer to the sync block that was found. In other words, sync blocks are associated with an object on the fly. When Exit is called, it checks to see whether there are any more threads waiting to use the object\u2019s sync block. If there are no threads waiting for it, the sync block is free, Exit sets the object\u2019s sync block index back to -1, and the free sync block can be associated with another object in the future.</p><p>Figure 30-1 shows the relationship between objects in the heap, their sync block indexes, and elements in the CLR\u2019s sync block array. Object-A, Object-B, and Object-C all have their type object pointer member set to refer to Type-T (a type object). This means that all three objects are of the same type. As discussed in Chapter 4, \u201cType Fundamentals,\u201d a type object is also an object in the heap, and like all other objects, a type object has the two overhead members: a sync block index and a type object pointer. This means that a sync block can be associated with a type object and a reference to a type object can be passed to Monitor\u2019s methods. By the way, the sync block array is able to create more sync blocks if necessary, so you shouldn\u2019t worry about the system running out of sync blocks if many objects are being synchronized simultaneously.</p><h6>Managed Heap</h6><ul style=\"list-style-type: none;\">\n<li><p>Object-A Type object ptr</p><p>Sync block index (0) Object\u2019s Instance Fields</p>\n</li>\n<li><p>Object-B Type object ptr</p><p>Sync block index (-1) Object\u2019s Instance Fields</p>\n</li>\n<li><p>Object-C Type object ptr</p></li>\n</ul><p>Sync block index (2) Object\u2019s Instance Fields</p><ul style=\"list-style-type: none;\">\n<li><p>Sync block #0</p></li>\n<li><p>Sync block #1</p><p>CLR\u2019s Array of Sync Blocks</p>\n</li>\n<li><p>Sync block #2</p></li>\n<li><p>Sync block #3</p></li>\n</ul><h6>...</h6><p>Type-T</p><p>Type object ptr Sync block index (3) Type\u2019s Static Fields</p><p>FIGURE 30-1 Objects in the heap (including type objects) can have their sync block indexes refer to an entry in the CLR\u2019s sync block array.</p><p>Here is some code that demonstrates how the Monitor class was originally intended to be used.</p><p>internal sealed class Transaction { private DateTime m_timeOfLastTrans;</p><p>public void PerformTransaction() { Monitor.Enter(this); // This code has exclusive access to the data... m_timeOfLastTrans = DateTime.Now; Monitor.Exit(this);</p><p>}</p><p>}</p><p>public DateTime LastTransaction {</p><p>get { Monitor.Enter(this); // This code has exclusive access to the data... DateTime temp = m_timeOfLastTrans; Monitor.Exit(this); return temp;</p><p>} }</p><p>On the surface, this seems simple enough, but there is something wrong with this code. The problem is that each object\u2019s sync block index is implicitly public. The following code demonstrates the impact of this.</p><p>public static void SomeMethod() { var t = new Transaction(); Monitor.Enter(t); // This thread takes the object's public lock</p><p>// Have a thread pool thread display the LastTransaction time // NOTE: The thread pool thread blocks until SomeMethod calls Monitor.Exit! ThreadPool.QueueUserWorkItem(o =&gt; Console.WriteLine(t.LastTransaction));</p><p>}</p><p>// Execute some other code here... Monitor.Exit(t);</p><p>In this code, the thread executing SomeMethod calls Monitor.Enter, taking the Transaction object\u2019s publicly exposed lock. When the thread pool thread queries the LastTransaction property, this property also calls Monitor.Enter to acquire the same lock, causing the thread pool thread to block until the thread executing SomeMethod calls Monitor.Exit. Using a debugger, you can determine that the thread pool thread is blocked inside the LastTransaction property, but it is very hard to determine which other thread has the lock. If you do somehow figure out which thread has the lock, then you have to figure out what code caused it to take the lock. This is very difficult, and even worse, if you do figure it out, then the code might not be code that you have control over and you might not be able to modify this code to fix the problem. Therefore, my suggestion to you is to always use a private lock instead. Here\u2019s how I\u2019d fix the Transaction class.</p><p>internal sealed class Transaction { private readonly Object m_lock = new Object(); // Each transaction has a PRIVATE lock now private DateTime m_timeOfLastTrans;</p><p>public void PerformTransaction() { Monitor.Enter(m_lock); // Enter the private lock // This code has exclusive access to the data... m_timeOfLastTrans = DateTime.Now; Monitor.Exit(m_lock); // Exit the private lock</p><p>}</p><p>}</p><p>public DateTime LastTransaction {</p><p>get { Monitor.Enter(m_lock); // Enter the private lock // This code has exclusive access to the data... DateTime temp = m_timeOfLastTrans; Monitor.Exit(m_lock); // Exit the private lock return temp;</p><p>} }</p><p>If Transaction\u2019s members were static, then simply make the m_lock field static, too, and now the static members are thread safe.</p><p>It should be clear from this discussion that Monitor should not have been implemented as a</p><p>static class; it should have been implemented like all the other locks: a class you instantiate and call instance methods on. In fact, Monitor has many other problems associated with it that are all because it is a static class. Here is a list of additional problems:</p><p>\u25a0\u25a0 A variable can refer to a proxy object if the type of object it refers to is derived from the System.MarshalByRefObject class (discussed in Chapter 22, \u201cCLR Hosting and AppDomains\u201d). When you call Monitor\u2019s methods, passing a reference to a proxy object, you are locking the proxy object, not the actual object that the proxy refers to.</p><p>\u25a0\u25a0 If a thread calls Monitor.Enter, passing it a reference to a type object that has been loaded domain neutral (discussed in Chapter 22), the thread is taking a lock on that type across all AppDomains in the process. This is a known bug in the CLR that violates the isolation that AppDomains are supposed to provide. The bug is difficult to fix in a high-performance way, so it never gets fixed. The recommendation is to never pass a reference to a type object into Monitor\u2019s methods.</p><p>\u25a0\u25a0 Because strings can be interned (as discussed in Chapter 14, \u201cChars, Strings, and Working with Text\u201d), two completely separate pieces of code could unknowingly get references to a single String object in memory. If they pass the reference to the String object into Monitor\u2019s methods, then the two separate pieces of code are now synchronizing their execution with each other unknowingly.</p><p>\u25a0\u25a0 When passing a string across an AppDomain boundary, the CLR does not make a copy of the string; instead, it simply passes a reference to the string into the other AppDomain. This improves performance, and in theory, it should be OK because String objects are immutable. However, like all objects, String objects have a sync block index associated with them, which is mutable, and this allows threads in different AppDomains to synchronize with each other unknowingly. This is another bug in CLR\u2019s AppDomain isolation story. The recommendation is never to pass String references to Monitor\u2019s methods.</p><p>\u25a0\u25a0 Because Monitor\u2019s methods take an Object, passing a value type causes the value type to get boxed, resulting in the thread taking a lock on the boxed object. Each time Monitor.Enter is called, a lock is taken on a completely different object and you get no thread synchronization at all.</p><p>\u25a0\u25a0 Applying the [MethodImpl(MethodImplOptions.Synchronized)] attribute to a method causes the JIT compiler to surround the method\u2019s native code with calls to Monitor.Enter and Monitor.Exit. If the method is an instance method, then this is passed to these methods, locking the implicitly public lock. If the method is static, then a reference to the type\u2019s type object is passed to these methods, potentially locking a domain-neutral type. The recommendation is to never use this attribute.</p><p>\u25a0\u25a0 When calling a type\u2019s type constructor (discussed in Chapter 8, \u201cMethods\u201d), the CLR takes a lock on the type\u2019s type object to ensure that only one thread initializes the type object and its static fields. Again, this type could be loaded domain neutral, causing a problem. For example, if the type constructor\u2019s code enters an infinite loop, then the type is unusable by all AppDomains in the process. The recommendation here is to avoid type constructors as much as possible or least keep them short and simple.</p><p>Unfortunately, the story gets worse. Because it is so common for developers to take a lock, do some work, and then release the lock within a single method, the C# language offers simplified syntax via its lock keyword. Suppose that you write a method like this.</p><p>private void SomeMethod() { lock (this) {</p><p>// This code has exclusive access to the data... }</p><p>}</p><p>It is equivalent to having written the method like this.</p><p>private void SomeMethod() { Boolean lockTaken = false; try {</p><p>// An exception (such as ThreadAbortException) could occur here... Monitor.Enter(this, ref lockTaken); // This code has exclusive access to the data...</p><p>} finally {</p><p>if (lockTaken) Monitor.Exit(this); }</p><p>}</p><p>The first problem here is that the C# team felt that they were doing you a favor by calling Monitor.Exit in a finally block. Their thinking was that this ensures that the lock is always released no matter what happens inside the try block. However, this is not a good thing. If an exception occurs inside the try block while changing state, then the state is now corrupted. When the lock is exited in the finally block, another thread will now start manipulating the corrupted state. It is better to have your application hang than it is to continue running with a corrupted state and potential security holes. The second problem is that entering and leaving a try block decreases the performance of the method. And some JIT compilers won\u2019t inline a method that contains a try block in it, which decreases performance even more. So now we have slower code that lets threads access corrupted state.3 The recommendation is not to use C#\u2019s lock statement.</p><p>Now we get to the Boolean lockTaken variable. Here is the problem that this variable is trying to solve. Let\u2019s say that a thread enters the try block and before calling Monitor.Enter, the thread is aborted (as discussed in Chapter 22). Now the finally block is called, but its code should not exit the lock. The lockTaken variable solves this problem. It is initialized to false, which assumes that the lock has not been entered into. Then, if Monitor.Enter is called and successfully takes the</p><p>3 By the way, while still a performance hit, it is safe to release a lock in a finally block if the code in the try block reads the state without attempting to modify it.</p><p>lock, it sets lockTaken to true. The finally block examines lockTaken to know whether to call Monitor.Exit or not. By the way, the SpinLock structure also supports this lockTaken pattern.</p><h6>The ReaderWriterLockSlim Class</h6><p>It is common to have threads simply read the contents of some data. If this data is protected by a mutual exclusive lock (like the SimpleSpinLock, SimpleWaitLock, SimpleHybridLock, AnotherHybridLock, SpinLock, Mutex, or Monitor), then if multiple threads attempt this access concurrently, only one thread gets to run and all the other threads are blocked, which can reduce scalability and throughput in your application substantially. However, if all the threads want to access the data in a read-only fashion, then there is no need to block them at all; they should all be able to access the data concurrently. On the other hand, if a thread wants to modify the data, then this thread needs exclusive access to the data. The ReaderWriterLockSlim construct encapsulates the logic to solve this problem. Specifically, the construct controls threads like this:</p><p>\u25a0\u25a0 When one thread is writing to the data, all other threads requesting access are blocked.</p><p>\u25a0\u25a0 When one thread is reading from the data, other threads requesting read access are allowed</p><p>to continue executing, but threads requesting write access are blocked.</p><p>\u25a0\u25a0 When a thread writing to the data has completed, either a single writer thread is unblocked so it can access the data or all the reader threads are unblocked so that all of them can access the data concurrently. If no threads are blocked, then the lock is free and available for the next reader or writer thread that wants it.</p><p>\u25a0\u25a0 When all threads reading from the data have completed, a single writer thread is unblocked so it can access the data. If no threads are blocked, then the lock is free and available for the next reader or writer thread that wants it.</p><p>Here is what this class looks like (some method overloads are not shown).</p><p>public class ReaderWriterLockSlim : IDisposable { public ReaderWriterLockSlim(LockRecursionPolicy recursionPolicy); public void Dispose();</p><p>public void EnterReadLock(); public Boolean TryEnterReadLock(Int32 millisecondsTimeout); public void ExitReadLock();</p><p>public void EnterWriteLock(); public Boolean TryEnterWriteLock(Int32 millisecondsTimeout); public void ExitWriteLock();</p><p>// Most applications will never query any of these properties public Boolean IsReadLockHeld { get; } public Boolean IsWriteLockHeld { get; } public Int32 CurrentReadCount { get; } public Int32 RecursiveReadCount { get; } public Int32 RecursiveWriteCount { get; } public Int32 WaitingReadCount { get; } public Int32 WaitingWriteCount { get; }</p><p>}</p><p>public LockRecursionPolicy RecursionPolicy { get; } // Members related to upgrading from a reader to a writer not shown</p><p>Here is some code that demonstrates the use of this construct.</p><p>internal sealed class Transaction : IDisposable { private readonly ReaderWriterLockSlim m_lock =</p><p>new ReaderWriterLockSlim(LockRecursionPolicy.NoRecursion); private DateTime m_timeOfLastTrans;</p><p>public void PerformTransaction() { m_lock.EnterWriteLock(); // This code has exclusive access to the data... m_timeOfLastTrans = DateTime.Now; m_lock.ExitWriteLock();</p><p>}</p><p>public DateTime LastTransaction {</p><p>get { m_lock.EnterReadLock(); // This code has shared access to the data... DateTime temp = m_timeOfLastTrans; m_lock.ExitReadLock(); return temp;</p><p>} }</p><p>public void Dispose() { m_lock.Dispose(); } }</p><p>There are a few concepts related to this construct that deserve special mention. First, ReaderWriterLockSlim\u2019s constructor allows you to pass in a LockRecursionPolicy flag, which is defined as follows.</p><p>public enum LockRecursionPolicy { NoRecursion, SupportsRecursion }</p><p>If you pass the SupportsRecursion flag, then the lock will add thread ownership and recursion behaviors to the lock. As discussed earlier in this chapter, these behaviors negatively affect the lock\u2019s performance, so I recommend that you always pass LockRecursionPolicy.NoRecursion to the constructor (as I\u2019ve done). For a reader-writer lock, supporting thread ownership and recursion is phenomenally expensive, because the lock must keep track of all the reader threads that it has let into the lock and keep a separate recursion count for each reader thread. In fact, to maintain all this information in a thread-safe way, the ReaderWriterLockSlim internally uses a mutually exclusive spinlock! No, I\u2019m not kidding.</p><p>The ReaderWriterLockSlim class offers additional methods (not shown earlier) that allow a reading thread to upgrade itself to a writer thread. Later, the thread can downgrade itself to a reader thread. The thinking here is that a thread could start reading the data and based on the data\u2019s contents, the thread might want to modify the data. To do this, the thread would upgrade itself from a reader to a writer. Having the lock support this behavior deteriorates the lock\u2019s performance, and I don\u2019t think that this is a useful feature at all. Here\u2019s why: a thread can\u2019t just turn itself from a reader</p><p>into a writer. Other threads may be reading, too, and these threads will have to exit the lock completely before the thread trying to upgrade is allowed to become a writer. This is the same as having the reader thread exit the lock and then immediately acquire it for writing.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile252.png\"/><p>Note The FCL also ships a ReaderWriterLock construct, which was introduced in the Microsoft .NET Framework 1.0. This construct had so many problems that Microsoft introduced the ReaderWriterLockSlim construct in .NET Framework 3.5. The team didn\u2019t improve the ReaderWriterLock construct for fear of losing compatibility with applications that were using it. Here are the problems with the ReaderWriterLock. Even without thread contention, it is very slow. There is no way to opt out of the thread ownership and recursion behaviors, making the lock even slower. It favors reader threads over writer threads, and therefore, writer threads can get queued up and are rarely serviced, which results in denial of service problems.</p><h6>The OneManyLock Class</h6><p>I have created my own reader-writer construct that is faster than the FCL\u2019s ReaderWriterLockSlim class.4 My class is called OneManyLock because it allows access to either one writer thread or many reader threads. The class basically looks like this.</p><p>public sealed class OneManyLock : IDisposable { public OneManyLock(); public void Dispose();</p><p>}</p><p>public void Enter(Boolean exclusive); public void Leave();</p><p>Now I\u2019d like to give you a sense of how it works. Internally, the class has an Int32 field for the state of the lock, a Semaphore object that reader threads block on, and an AutoResetEvent object that writer threads block on. The Int64 state field is divided into five subfields as follows:</p><p>\u25a0\u25a0 Four bits represent the state of the lock itself. The possibilities are 0=Free, 1=OwnedByWriter, 2=OwnedByReaders, 3=OwnedByReadersAndWriterPending, and 4=ReservedForWriter. The other values are not used.</p><p>\u25a0\u25a0 Twenty bits (a number from 0 to 1,048,575) represent the number of reader threads reading</p><p>(RR) that the lock has currently allowed in.</p><p>\u25a0\u25a0 Twenty bits represent the number of reader threads waiting (RW) to get into the lock. These</p><p>threads block on the auto-reset event object.</p><ul style=\"list-style-type: none;\">\n<li><p>4 The code is inside the Ch30-1-HybridThreadSync.cs file that is part of the code that accompanies this book. You can download this code from http://Wintellect.com/Books.</p></li>\n</ul><p>\u25a0\u25a0 Twenty bits represent the number of writer threads waiting (WW) to get into the lock. These</p><p>threads block on the other semaphore object.</p><p>Now, because all the information about the lock fits in a single Int64 field, I can manipulate this field by using the methods of the Interlocked class so the lock is incredibly fast and causes a thread to block only when there is contention.</p><p>Here\u2019s what happens when a thread enters the lock for shared access.</p><p>\u25a0\u25a0 If the lock is Free: Set state to OwnedByReaders, RR=1, Return.</p><p>\u25a0\u25a0 If the lock is OwnedByReaders: RR++, Return.</p><p>\u25a0\u25a0 Else: RW++, Block reader thread. When the thread wakes, loop around and try again. Here\u2019s what happens when a thread that has shared access leaves the lock.</p><p>\u25a0\u25a0 RR--</p><p>\u25a0\u25a0 If RR &gt; 0: Return</p><p>\u25a0\u25a0 If WW &gt; 0: Set state to ReservedForWriter, WW--, Release 1 blocked writer thread, Return</p><p>\u25a0\u25a0 If RW == 0 &amp;&amp; WW == 0: Set state to Free, Return Here\u2019s what happens when a thread enters the lock for exclusive access:</p><p>\u25a0\u25a0 If the lock is Free: Set state to OwnedByWriter, Return.</p><p>\u25a0\u25a0 If the lock is ReservedForWriter: Set state to OwnedByWriter, Return.</p><p>\u25a0\u25a0 If the lock is OwnedByWriter: WW++, Block writer thread. When thread wakes, loop around</p><p>and try again.</p><p>\u25a0\u25a0 Else: Set state to OwnedByReadersAndWriterPending, WW++, Block writer thread. When</p><p>thread wakes, loop around and try again. Here\u2019s what happens when a thread that has exclusive access leaves the lock:</p><p>\u25a0\u25a0 If WW == 0 &amp;&amp; RW == 0: Set state to Free, Return</p><p>\u25a0\u25a0 If WW &gt; 0: Set state to ReservedForWriter, WW--, Release 1 blocked writer thread, Return \u25a0\u25a0 If WW == 0 &amp;&amp; RW &gt; 0: Set state to Free , RW=0, Wake all blocked reader threads, Return Let\u2019s say that there is currently one thread reading from the lock and another thread wants to</p><p>enter the lock for writing. The writer thread will first check to see if the lock is Free, and because it is not, the thread will advance to perform the next check. However, at this point, the reader thread could leave the lock, and seeing that RR and WW are both 0, the thread could set the lock\u2019s state to Free. This is a problem because the writer thread has already performed this test and moved on. Basically what happened is that the reader thread changed the state that the writer thread was accessing behind its back. I needed to solve this problem so that the lock would function correctly.</p><p>To solve the problem, all of these bit manipulations are performed using the technique I showed in the \u201cThe Interlocked Anything Pattern\u201d section from Chapter 29. If you recall, this pattern lets you turn any operation into a thread-safe atomic operation. This is what allows this lock to be so fast and have less state in it than other reader-writer locks. When I run performance tests comparing my OneManyLock against the FCL\u2019s ReaderWriterLockSlim and ReaderWriterLock classes, I get the following results.</p><p>Incrementing x in OneManyLock: 330 Fastest Incrementing x in ReaderWriterLockSlim: 554 ~1.7x slower Incrementing x in ReaderWriterLock: 984 ~3x slower</p><p>Of course, because all reader-writer locks perform more logic than a mutually exclusive lock, their performance can be slightly worse. However, you have to weigh this against the fact that a readerwriter lock allows multiple readers into the lock simultaneously.</p><p>Before leaving this section, I\u2019ll also mention that my Power Threading library (downloadable for free from http://Wintellect.com/PowerThreading.aspx) offers a slightly different version of this lock, called OneManyResourceLock. This lock and others in the library offer many additional features, such as deadlock detection, the ability to turn on lock ownership and recursion (albeit at a performance cost), a unified programming model for all locks, and the ability to observe the run-time behavior of the locks. For observing behavior, you can see the maximum amount of time that a thread ever waited to acquire a lock, and you can see the minimum and maximum amount of time that a lock was held.</p><h6>The CountdownEvent Class</h6><p>The next construct is System.Threading.CountdownEvent. Internally, this construct uses a Manual\u00ad</p><p>ResetEventSlim object. This construct blocks a thread until its internal counter reaches 0. In a way, this construct\u2019s behavior is the opposite of that of a Semaphore (which blocks threads while its count is 0). Here is what this class looks like (some method overloads are not shown).</p><p>public class CountdownEvent : IDisposable { public CountdownEvent(Int32 initialCount); public void Dispose(); public void Reset(Int32 count); // Set CurrentCount to count public void AddCount(Int32 signalCount); // Increments CurrentCount by signalCount public Boolean TryAddCount(Int32 signalCount); // Increments CurrentCount by signalCount public Boolean Signal(Int32 signalCount); // Decrements CurrentCount by signameCount public Boolean Wait(Int32 millisecondsTimeout, CancellationToken cancellationToken);</p><p>}</p><p>public Int32 CurrentCount { get; } public Boolean IsSet { get; } // true if CurrentCount is 0 public WaitHandle WaitHandle { get; }</p><p>After a CountdownEvent\u2019s CurrentCount reaches 0, it cannot be changed. The AddCount method throws InvalidOperationException when CurrentCount is 0, whereas the TryAddCount method simply returns false if CurrentCount is 0.</p><h6>The Barrier Class</h6><p>The System.Threading.Barrier construct is designed to solve a very rare problem, so it is unlikely that you will have a use for it. Barrier is used to control a set of threads that are working together in parallel so that they can step through phases of the algorithm together. Perhaps an example is in order: when the CLR is using the server version of its garbage collector (GC), the GC algorithm creates one thread per core. These threads walk up different application threads\u2019 stacks, concurrently marking objects in the heap. As each thread completes its portion of the work, it must stop waiting for the other threads to complete their portion of the work. After all threads have marked the objects, then the threads can compact different portions of the heap concurrently. As each thread finishes compacting its portion of the heap, the thread must block waiting for the other threads. After all the threads have finished compacting their portion of the heap, then all the threads walk up the application\u2019s threads\u2019 stacks, fixing up roots to refer to the new location of the compacted object. Only after all the threads have completed this work is the garbage collector considered complete and the application\u2019s threads can be resumed.</p><p>This scenario is easily solved using the Barrier class, which looks like this (some method overloads are not shown).</p><p>public class Barrier : IDisposable { public Barrier(Int32 participantCount, Action&lt;Barrier&gt; postPhaseAction); public void Dispose(); public Int64 AddParticipants(Int32 participantCount); // Adds participants public void RemoveParticipants(Int32 participantCount); // Subtracts participants public Boolean SignalAndWait(Int32 millisecondsTimeout, CancellationToken</p><p>cancellationToken);</p><p>public Int64 CurrentPhaseNumber { get; } // Indicates phase in process (starts at 0) public Int32 ParticipantCount { get; } // Number of participants public Int32 ParticipantsRemaining { get; } // # of threads needing to call</p><p>SignalAndWait }</p><p>When you construct a Barrier, you tell it how many threads are participating in the work, and you can also pass an Action&lt;Barrier&gt; delegate referring to code that will be invoked whenever all participants complete a phase of the work. You can dynamically add and remove participating threads from the Barrier by calling the AddParticipant and RemoveParticipant methods but, in practice, this is rarely done. As each thread completes its phase of the work, it should call SignalAndWait, which tells the Barrier that the thread is done and the Barrier blocks the thread (using a ManualResetEventSlim). After all participants call SignalAndWait, the Barrier invokes the delegate (using the last thread that called SignalAndWait) and then unblocks all the waiting threads so they can begin the next phase.</p><h6>Thread Synchronization Construct Summary</h6><p>My recommendation always is to avoid writing code that blocks any threads. When performing asynchronous compute or I/O operations, hand the data off from thread to thread in such a way to avoid the chance that multiple threads could access the data simultaneously. If you are unable to fully</p><p>accomplish this, then try to use the Volatile and Interlocked methods because they are fast and they also never block a thread. Unfortunately, these methods manipulate only simple types, but you can perform rich operations on these types as described in the \u201cThe Interlocked Anything Pattern\u201d section in Chapter 29.</p><p>There are two main reasons why you would consider blocking threads:</p><p>\u25a0\u25a0 The programming model is simplified By blocking a thread, you are sacrificing some</p><p>resources and performance so that you can write your application code sequentially without using callback methods. But C#\u2019s async methods feature gives you a simplified programming model without blocking threads.</p><p>\u25a0\u25a0 A thread has a dedicated purpose Some threads must be used for specific tasks. The best example is an application\u2019s primary thread. If an application\u2019s primary thread doesn\u2019t block, then it will eventually return and the whole process will terminate. Another example is an application\u2019s GUI thread or threads. Windows requires that a window or control always be manipulated by the thread that created it, so we sometimes write code that blocks a GUI thread until some other operation is done, and then the GUI thread updates any windows and controls as needed. Of course, blocking the GUI thread hangs the application and provides a bad end-user experience.</p><p>To avoid blocking threads, don\u2019t mentally assign a label to your threads. For example, don\u2019t create a spell-checking thread, a grammar-checking thread, a thread that handles this particular client request, and so on. The moment you assign a label to a thread, you have also said to yourself that the thread can\u2019t do anything else. But threads are too expensive a resource to have them dedicated to a particular purpose. Instead, you should use the thread pool to rent threads for short periods of time. So, a thread pool thread starts out spell checking, then it changes to grammar checking, and then it changes again to perform work on behalf of a client request, and so on.</p><p>If, in spite of this discussion, you decide to block threads, then use the kernel object constructs if you want to synchronize threads that are running in different AppDomains or processes. To atomically manipulate state via a set of operations, use the Monitor class with a private field.5 Alternatively, you could use a reader-writer lock instead of Monitor. Reader-writer locks are generally slower than Monitor, but they allow multiple reader threads to execute concurrently, which improves overall performance and minimizes the chance of blocking threads.</p><p>In addition, avoid using recursive locks (especially recursive reader-writer locks) because they hurt performance. However, Monitor is recursive and its performance is very good.6 Also, avoid releasing a lock in a finally block because entering and leaving exception-handling blocks incurs a performance hit, and if an exception is thrown while mutating state, then the state is corrupted, and other threads that manipulate it will experience unpredictable behavior and security bugs.</p><ul style=\"list-style-type: none;\">\n<li><p>5 You could use a SpinLock instead of Monitor because SpinLocks are slightly faster. But a SpinLock is potentially dangerous because it can waste CPU time and, in my opinion, it is not sufficiently faster than Monitor to justify its use.</p></li>\n<li><p>6 This is partially because Monitor is actually implemented in native code, not managed code.</p></li>\n</ul><p>Of course, if you do write code that holds a lock, your code should not hold the lock for a long</p><p>time, because this increases the likelihood of threads blocking. In the \u201cAsynchronous Synchronization\u201d section later in this chapter, I will show a technique that uses collection classes as a way to avoid holding a lock for a long time.</p><p>Finally, for compute-bound work, you can use tasks (discussed in Chapter 27) to avoid a lot of the thread synchronization constructs. In particular, I love that each task can have one or more continuewith tasks associated with it that execute via some thread pool thread when some operation completes. This is much better than having a thread block waiting for some operation to complete. For I/O-bound work, call the various XxxAsync methods that cause your code to continue running after the I/O operation completes; this is similar to a task\u2019s continue-with task.</p><h6>The Famous Double-Check Locking Technique</h6><p>There is a famous technique called double-check locking, which is used by developers who want to defer constructing a singleton object until an application requests it (sometimes called lazy initialization). If the application never requests the object, it never gets constructed, saving time and memory. A potential problem occurs when multiple threads request the singleton object simultaneously. In this case, some form of thread synchronization must be used to ensure that the singleton object gets constructed just once.</p><p>This technique is not famous because it is particularly interesting or useful. It is famous because there has been much written about it. This technique was used heavily in Java, and later it was discovered that Java couldn\u2019t guarantee that it would work everywhere. The famous document that describes the problem can be found on this webpage: www.cs.umd.edu/~pugh/java/memoryModel/ DoubleCheckedLocking.html.</p><p>Anyway, you\u2019ll be happy to know that the CLR supports the double-check locking technique just fine because of its memory model and volatile field access (described in Chapter 29). Here is code that demonstrates how to implement the double-check locking technique in C#.</p><p>internal sealed class Singleton { // s_lock is required for thread safety and having this object assumes that creating // the singleton object is more expensive than creating a System.Object object and that // creating the singleton object may not be necessary at all. Otherwise, it is more // efficient and easier to just create the singleton object in a class constructor private static readonly Object s_lock = new Object();</p><p>// This field will refer to the one Singleton object private static Singleton s_value = null;</p><p>// Private constructor prevents any code outside this class from creating an instance private Singleton() {</p><p>// Code to initialize the one Singleton object goes here... }</p><p>// Public, static method that returns the Singleton object (creating it if necessary) public static Singleton GetSingleton() {</p><p>// If the Singleton was already created, just return it (this is fast) if (s_value != null) return s_value;</p><p>Monitor.Enter(s_lock); // Not created, let 1 thread create it if (s_value == null) {</p><p>// Still not created, create it Singleton temp = new Singleton();</p><p>// Save the reference in s_value (see discussion for details) Volatile.Write(ref s_value, temp);</p><p>} Monitor.Exit(s_lock);</p><p>} }</p><p>// Return a reference to the one Singleton object return s_value;</p><p>The idea behind the double-check locking technique is that a call to the GetSingleton method quickly checks the s_value field to see if the object has already been created, and if it has, the method returns a reference to it. The beautiful thing here is that no thread synchronization is required after the object has been constructed; the application will run very fast. On the other hand, if the first thread that calls the GetSingleton method sees that the object hasn\u2019t been created, it takes a thread synchronization lock to ensure that only one thread constructs the single object. This means that a performance hit occurs only the first time a thread queries the singleton object.</p><p>Now, let me explain why this pattern didn\u2019t work in Java. The Java Virtual Machine read the value of s_value into a CPU register at the beginning of the GetSingleton method and then just queried the register when evaluating the second if statement, causing the second if statement to always evaluate to true, and multiple threads ended up creating Singleton objects. Of course, this happened only if multiple threads called GetSingleton at exactly the same time, which in most applications is very unlikely. This is why it went undetected in Java for so long.</p><p>In the CLR, calling any lock method is a full memory fence, and any variable writes you have before the fence must complete before the fence and any variable reads after the fence must start after it. For the GetSingleton method, this means that the s_value field must be reread after the call to Monitor.Enter; it cannot be cached in a register across this method call.</p><p>Inside GetSingleton, you see the call to Volatile.Write. Here\u2019s the problem that this is solving. Let\u2019s say that what you had inside the second if statement was the following line of code.</p><p>s_value = new Singleton(); // This is what you'd ideally like to write</p><p>You would expect the compiler to produce code that allocates the memory for a Singleton, calls the constructor to initialize the fields, and then assigns the reference into the s_value field. Making a value visible to other threads is called publishing. But the compiler could do this instead: allocate memory for the Singleton, publish (assign) the reference into s_value, and then call the constructor. From a single thread\u2019s perspective, changing the order like this has no impact. But what if, after publishing the reference into s_value and before calling the constructor, another thread calls the GetSingleton method? This thread will see that s_value is not null and start to use the Singleton</p><p>object, but its constructor has not finished executing yet! This can be a very hard bug to track down, especially because it is all due to timing.</p><p>The call to Volatile.Write fixes this problem. It ensures that the reference in temp can be published into s_value only after the constructor has finished executing. Another way to solve this problem would be to mark the s_value field with C#\u2019s volatile keyword. This makes the write to s_value volatile, and again, the constructor has to finish running before the write can happen. Unfortunately, this makes all reads volatile, too, and because there is no need for this, you are hurting your performance with no benefit.</p><p>In the beginning of this section, I mentioned that the double-check locking technique is not that interesting. In my opinion, developers think it is cool, and they use it far more often than they should. In most scenarios, this technique actually hurts efficiency. Here is a much simpler version of the Singleton class that behaves the same as the previous version. This version does not use the double-check locking technique.</p><p>internal sealed class Singleton { private static Singleton s_value = new Singleton();</p><p>// Private constructor prevents any code outside this class from creating an instance private Singleton() {</p><p>// Code to initialize the one Singleton object goes here... }</p><p>}</p><p>// Public, static method that returns the Singleton object (creating it if necessary) public static Singleton GetSingleton() { return s_value; }</p><p>Because the CLR automatically calls a type\u2019s class constructor the first time code attempts to access a member of the class, the first time a thread queries Singleton\u2019s GetSingleton method, the CLR will automatically call the class constructor, which creates an instance of the object. Furthermore, the CLR already ensures that calls to a class constructor are thread safe. I explained all of this in Chapter 8. The one downside of this approach is that the type constructor is called when any member of a class is first accessed. If the Singleton type defined some other static members, then the Singleton object would be created when any one of them was accessed. Some people work around this problem by defining nested classes.</p><p>Let me show you a third way of producing a single Singleton object.</p><p>internal sealed class Singleton { private static Singleton s_value = null;</p><p>// Private constructor prevents any code outside this class from creating an instance private Singleton() {</p><p>// Code to initialize the one Singleton object goes here... }</p><p>// Public, static method that returns the Singleton object (creating it if necessary) public static Singleton GetSingleton() {</p><p>if (s_value != null) return s_value;</p><p>// Create a new Singleton and root it if another thread didn't do it first Singleton temp = new Singleton(); Interlocked.CompareExchange(ref s_value, temp, null);</p><p>// If this thread lost, then the second Singleton object gets GC'd</p><p>}</p><p>return s_value; // Return reference to the single object }</p><p>If multiple threads call GetSingleton simultaneously, then this version might create two (or more) Singleton objects. However, the call to Interlocked.CompareExchange ensures that only one of the references is ever published into the s_value field. Any object not rooted by this field will be garbage collected later on. Because, in most applications, it is unlikely that multiple threads will call GetSingleton at the same time, it is unlikely that more than one Singleton object will ever be created.</p><p>Now it might upset you that multiple Singleton objects could be created, but there are many benefits to this code. First, it is very fast. Second, it never blocks a thread; if a thread pool thread is blocked on a Monitor or any other kernel-mode thread synchronization construct, then the thread pool creates another thread to keep the CPUs saturated with work. So now, more memory is allocated and initialized and all the DLLs get a thread attach notification. With CompareExchange, this can never happen. Of course, you can use this technique only when the constructor has no side effects.</p><p>The FCL offers two types that encapsulate the patterns described in this section. The generic System.Lazy class looks like this (some methods are not shown).</p><p>public class Lazy&lt;T&gt; { public Lazy(Func&lt;T&gt; valueFactory, LazyThreadSafetyMode mode); public Boolean IsValueCreated { get; } public T Value { get; }</p><p>}</p><p>This code demonstrates how it works.</p><p>public static void Main() { // Create a lazy\u00adinitialization wrapper around getting the DateTime Lazy&lt;String&gt; s = new Lazy&lt;String&gt;(() =&gt; DateTime.Now.ToLongTimeString(), true);</p><p>}</p><p>Console.WriteLine(s.IsValueCreated); // Returns false because Value not queried yet Console.WriteLine(s.Value); // The delegate is invoked now Console.WriteLine(s.IsValueCreated); // Returns true because Value was queried Thread.Sleep(10000); // Wait 10 seconds and display the time again Console.WriteLine(s.Value); // The delegate is NOT invoked now; same result</p><p>When I run this, I get the following output.</p><p>False 2:40:42 PM True 2:40:42 PM \u00df Notice that the time did not change 10 seconds later</p><p>The preceding code constructed an instance of the Lazy class and passed one of the LazyThreadSafetyMode flags into it. Here is what these flags look like and what they mean.</p><p>public enum LazyThreadSafetyMode { None, // No thread\u00adsafety support at all (good for GUI apps) ExecutionAndPublication // Uses the double\u00adcheck locking technique PublicationOnly, // Uses the Interlocked.CompareExchange technique</p><p>}</p><p>In some memory-constrained scenarios, you might not even want to create an instance of the Lazy class. Instead, you can call static methods of the System.Threading.LazyInitializer class. The class looks like this.</p><p>public static class LazyInitializer { // These two methods use Interlocked.CompareExchange internally: public static T EnsureInitialized&lt;T&gt;(ref T target) where T: class; public static T EnsureInitialized&lt;T&gt;(ref T target, Func&lt;T&gt; valueFactory) where T: class;</p><p>// These two methods pass the syncLock to Monitor's Enter and Exit methods internally public static T EnsureInitialized&lt;T&gt;(ref T target, ref Boolean initialized,</p><p>ref Object syncLock); public static T EnsureInitialized&lt;T&gt;(ref T target, ref Boolean initialized,</p><p>ref Object syncLock, Func&lt;T&gt; valueFactory); }</p><p>Also, being able to explicitly specify a synchronization object to the EnsureInitialized method\u2019s syncLock parameter allows multiple initialization functions and fields to be protected by the same lock.</p><p>Here is an example using a method from this class.</p><p>public static void Main() { String name = null; // Because name is null, the delegate runs and initializes name LazyInitializer.EnsureInitialized(ref name, () =&gt; \"Jeffrey\"); Console.WriteLine(name); // Displays \"Jeffrey\"</p><p>}</p><p>// Because name is not null, the delegate does not run; name doesn\u2019t change LazyInitializer.EnsureInitialized(ref name, () =&gt; \"Richter\"); Console.WriteLine(name); // Also displays \"Jeffrey\"</p><h6>The Condition Variable Pattern</h6><p>Let\u2019s say that a thread wants to execute some code when a complex condition is true. One option would be to let the thread spin continuously, repeatedly testing the condition. But this wastes CPU time, and it is also not possible to atomically test multiple variables that are making up the complex condition. Fortunately, there is a pattern that allows threads to efficiently synchronize their operations based on a complex condition.</p><p>This pattern is called the condition variable pattern, and we use it via the following methods defined inside the Monitor class.</p><p>public static class Monitor { public static Boolean Wait(Object obj); public static Boolean Wait(Object obj, Int32 millisecondsTimeout);</p><p>}</p><p>public static void Pulse(Object obj); public static void PulseAll(Object obj);</p><p>Here is what the pattern looks like.</p><p>internal sealed class ConditionVariablePattern { private readonly Object m_lock = new Object(); private Boolean m_condition = false;</p><p>public void Thread1() { Monitor.Enter(m_lock); // Acquire a mutual\u00adexclusive lock</p><p>// While under the lock, test the complex condition \"atomically\" while (!m_condition) {</p><p>// If condition is not met, wait for another thread to change the condition Monitor.Wait(m_lock); // Temporarily release lock so other threads can get it</p><p>}</p><p>// The condition was met, process the data...</p><p>Monitor.Exit(m_lock); // Permanently release lock }</p><p>public void Thread2() { Monitor.Enter(m_lock); // Acquire a mutual\u00adexclusive lock</p><p>// Process data and modify the condition... m_condition = true;</p><p>// Monitor.Pulse(m_lock); // Wakes one waiter AFTER lock is released Monitor.PulseAll(m_lock); // Wakes all waiters AFTER lock is released</p><p>}</p><p>Monitor.Exit(m_lock); // Release lock }</p><p>In this code, the thread executing the Thread1 method enters a mutual-exclusive lock and then tests a condition. Here, I am just checking a Boolean field, but this condition can be arbitrarily complex. For example, you could check to see if it is a Tuesday in March and if a certain collection object has 10 elements in it. If the condition is false, then you want the thread to spin on the condition, but spinning wastes CPU time, so instead, the thread calls Wait. Wait releases the lock so that another thread can get it and blocks the calling thread.</p><p>The Thread2 method shows code that the second thread executes. It calls Enter to take ownership of the lock, processes some data, which results in changing the state of the condition, and then calls Pulse or PulseAll, which will unblock a thread from its Wait call. Pulse unblocks the longest waiting thread (if any), whereas PulseAll unblocks all waiting threads (if any). However, any unblocked threads don\u2019t wake up yet. The thread executing Thread2 must call Monitor.Exit, allowing the lock to be owned by another thread. Also, if PulseAll is called, the other threads do not unblock simultaneously. When a thread that called Wait is unblocked, it becomes the owner of the lock, and because it is a mutual-exclusive lock, only one thread at a time can own it. Other threads can get it after an owning thread calls Wait or Exit.</p><p>When the thread executing Thread1 wakes, it loops around and tests the condition again. If the condition is still false, then it calls Wait again. If the condition is true, then it processes the data as it likes and ultimately calls Exit, leaving the lock so other threads can get it. The nice thing about this pattern is that it is possible to test several variables making up a complex condition using simple synchronization logic (just one lock), and multiple waiting threads can all unblock without causing any logic failure, although the unblocking threads might waste some CPU time.</p><p>Here is an example of a thread-safe queue that can have multiple threads enqueuing and dequeuing items to it. Note that threads attempting to dequeue an item block until an item is available for them to process.</p><p>internal sealed class SynchronizedQueue&lt;T&gt; { private readonly Object m_lock = new Object(); private readonly Queue&lt;T&gt; m_queue = new Queue&lt;T&gt;();</p><p>public void Enqueue(T item) { Monitor.Enter(m_lock);</p><p>// After enqueuing an item, wake up any/all waiters m_queue.Enqueue(item); Monitor.PulseAll(m_lock);</p><p>Monitor.Exit(m_lock); }</p><p>public T Dequeue() { Monitor.Enter(m_lock);</p><p>// Loop while the queue is empty (the condition) while (m_queue.Count == 0)</p><p>Monitor.Wait(m_lock);</p><p>} }</p><p>// Dequeue an item from the queue and return it for processing T item = m_queue.Dequeue(); Monitor.Exit(m_lock); return item;</p><h6>Asynchronous Synchronization</h6><p>I\u2019m not terribly fond of any of the thread synchronization constructs that use kernel-mode primitives, because all of these primitives exist to block a thread from running, and threads are just too expensive to create and not have them run. Here is an example that hopefully clarifies the problem.</p><p>Imagine a website into which clients make requests. When a client request arrives, a thread pool thread starts processing the client\u2019s request. Let\u2019s say that this client wants to modify some data in the server in a thread-safe way, so it acquires a reader-writer lock for writing. Let\u2019s pretend that this lock is held for a long time. As the lock is held, another client request comes in, so that thread pool creates a new thread for the client request, and then the thread blocks trying to acquire the reader-writer lock for reading. In fact, as more and more client requests come in, the thread pool creates more and more threads. Thus, all these threads are just blocking themselves on the lock. The server is spending all its time creating threads so that they can stop running! This server does not scale well at all.</p><p>Then, to make matters worse, when the writer thread releases the lock, all the reader threads unblock simultaneously and get to run, but now there may be lots of threads trying to run on relatively few CPUs, so Windows is context switching between the threads constantly. The result is that the workload is not being processed as quickly as it could because of all the overhead associated with the context switches.</p><p>If you look over all the constructs shown in this chapter, many of the problems that these constructs are trying to solve can be much better accomplished using the Task class discussed in Chapter 27. Take the Barrier class, for example: you could spawn several Task objects to work on a phase and then, when all these tasks complete, you could continue with one or more other Task objects. Compared to many of the constructs shown in this chapter, tasks have many advantages:</p><p>\u25a0\u25a0 Tasks use much less memory than threads and they take much less time to create and destroy.</p><p>\u25a0\u25a0 The thread pool automatically scales the tasks across available CPUs.</p><p>\u25a0\u25a0 As each task completes a phase, the thread running that task goes back to the thread pool,</p><p>where it can do other work, if any is available for it.</p><p>\u25a0\u25a0 The thread pool has a process-global view of tasks and, as such, it can better schedule these</p><p>tasks, reducing the number of threads in the process and also reducing context switching.</p><p>Locks are popular but, when held for a long time, they introduce significant scalability issues. What would really be useful is if we had asynchronous synchronization constructs where your code indicates that it wants a lock. If the thread can\u2019t have it, it can just return and perform some other work, rather than blocking indefinitely. Then, when the lock becomes available, your code somehow gets resumed, so it can access the resource that the lock protects. I came up with this idea after trying to solve a big scalability problem for a customer, and I then sold the patent rights to Microsoft. In 2009, the Patent Office issued the patent (Patent #7,603,502).</p><p>The SemaphoreSlim class implements this idea via its WaitAsync method. Here is the signature for the most complicated overload of this method.</p><p>public Task&lt;Boolean&gt; WaitAsync(Int32 millisecondsTimeout, CancellationToken cancellationToken);</p><p>With this, you can synchronize access to a resource asynchronously (without blocking any thread).</p><p>private static async Task AccessResourceViaAsyncSynchronization(SemaphoreSlim asyncLock) { // TODO: Execute whatever code you want here...</p><p>await asyncLock.WaitAsync(); // Request exclusive access to a resource via its lock // When we get here, we know that no other thread is accessing the resource // TODO: Access the resource (exclusively)...</p><p>// When done accessing resource, relinquish lock so other code can access the resource asyncLock.Release();</p><p>// TODO: Execute whatever code you want here... }</p><p>The SemaphoreSlim\u2019s WaitAsync method is very useful but, of course, it gives you semaphore semantics. Usually, you\u2019ll create the SemaphoreSlim with a maximum count of 1, which gives you mutual-exclusive access to the resource that the SemaphoreSlim protects. So, this is similar to the behavior you get when using Monitor, except that SemaphoreSlim does not offer thread ownership and recursion semantics (which is good).</p><p>But, what about reader-writer semantics? Well, the .NET Framework has a class called ConcurrentExclusiveSchedulerPair, which looks like this.</p><p>public class ConcurrentExclusiveSchedulerPair { public ConcurrentExclusiveSchedulerPair();</p><p>public TaskScheduler ExclusiveScheduler { get; } public TaskScheduler ConcurrentScheduler { get; }</p><p>// Other methods not shown... }</p><p>An instance of this class comes with two TaskScheduler objects that work together to provide reader/writer semantics when scheduling tasks. Any tasks scheduled by using ExclusiveScheduler will execute one at a time, as long as no tasks are running that were scheduled using the ConcurrentScheduler. And, any tasks scheduled using the ConcurrentScheduler can all run simultaneously, as long as no tasks are running that were scheduled by using the ExclusiveScheduler. Here is some code that demonstrates the use of this class.</p><p>private static void ConcurrentExclusiveSchedulerDemo() { var cesp = new ConcurrentExclusiveSchedulerPair(); var tfExclusive = new TaskFactory(cesp.ExclusiveScheduler); var tfConcurrent = new TaskFactory(cesp.ConcurrentScheduler);</p><p>for (Int32 operation = 0; operation &lt; 5; operation++) { var exclusive = operation &lt; 2; // For demo, I make 2 exclusive &amp; 3 concurrent</p><p>}</p><p>(exclusive ? tfExclusive : tfConcurrent).StartNew(() =&gt; { Console.WriteLine(\"{0} access\", exclusive ? \"exclusive\" : \"concurrent\"); // TODO: Do exclusive write or concurrent read computation here...</p><p>}); }</p><p>Unfortunately, the .NET Framework doesn\u2019t come with an asynchronous lock offering reader-writer semantics. However, I have built such a class, which I call AsyncOneManyLock. You use it the same way that you\u2019d use a SemaphoreSlim. Here is an example.</p><p>private static async Task AccessResourceViaAsyncSynchronization(AsyncOneManyLock asyncLock) { // TODO: Execute whatever code you want here...</p><p>// Pass OneManyMode.Exclusive or OneManyMode.Shared for wanted concurrent access await asyncLock.AcquireAsync(OneManyMode.Shared); // Request shared access // When we get here, no threads are writing to the resource; other threads may be reading // TODO: Read from the resource...</p><p>// When done accessing resource, relinquish lock so other code can access the resource asyncLock.Release();</p><p>// TODO: Execute whatever code you want here... }</p><p>The following is the code for my AsyncOneManyLock.</p><p>public enum OneManyMode { Exclusive, Shared }</p><p>public sealed class AsyncOneManyLock { #region Lock code private SpinLock m_lock = new SpinLock(true); // Don't use readonly with a SpinLock private void Lock() { Boolean taken = false; m_lock.Enter(ref taken); } private void Unlock() { m_lock.Exit(); } #endregion</p><p>#region Lock state and helper methods private Int32 m_state = 0; private Boolean IsFree { get { return m_state == 0; } } private Boolean IsOwnedByWriter { get { return m_state == \u00ad1; } } private Boolean IsOwnedByReaders { get { return m_state &gt; 0; } } private Int32 AddReaders(Int32 count) { return m_state += count; } private Int32 SubtractReader() { return \u00ad\u00adm_state; } private void MakeWriter() { m_state = \u00ad1; } private void MakeFree() { m_state = 0; } #endregion</p><p>// For the no\u00adcontention case to improve performance and reduce memory consumption private readonly Task m_noContentionAccessGranter;</p><p>// Each waiting writers wakes up via their own TaskCompletionSource queued here</p><p>private readonly Queue&lt;TaskCompletionSource&lt;Object&gt;&gt; m_qWaitingWriters = new Queue&lt;TaskCompletionSource&lt;Object&gt;&gt;();</p><p>// All waiting readers wake up by signaling a single TaskCompletionSource private TaskCompletionSource&lt;Object&gt; m_waitingReadersSignal =</p><p>new TaskCompletionSource&lt;Object&gt;(); private Int32 m_numWaitingReaders = 0;</p><p>public AsyncOneManyLock() {</p><p>m_noContentionAccessGranter = Task.FromResult&lt;Object&gt;(null); }</p><p>public Task WaitAsync(OneManyMode mode) { Task accressGranter = m_noContentionAccessGranter; // Assume no contention</p><p>Lock(); switch (mode) {</p><p>case OneManyMode.Exclusive: if (IsFree) { MakeWriter(); // No contention</p><p>} else { // Contention: Queue new writer task &amp; return it so writer waits var tcs = new TaskCompletionSource&lt;Object&gt;(); m_qWaitingWriters.Enqueue(tcs); accressGranter = tcs.Task;</p><p>} break;</p><p>case OneManyMode.Shared: if (IsFree || (IsOwnedByReaders &amp;&amp; m_qWaitingWriters.Count == 0)) { AddReaders(1); // No contention</p><p>} else { // Contention // Contention: Increment waiting readers &amp; return reader task so reader waits m_numWaitingReaders++; accressGranter = m_waitingReadersSignal.Task.ContinueWith(t =&gt; t.Result);</p><p>} break;</p><p>} Unlock();</p><p>return accressGranter; }</p><p>public void Release() { TaskCompletionSource&lt;Object&gt; accessGranter = null; // Assume no code is released</p><p>Lock(); if (IsOwnedByWriter) MakeFree(); // The writer left else SubtractReader(); // A reader left</p><p>if (IsFree) { // If free, wake 1 waiting writer or all waiting readers</p><p>if (m_qWaitingWriters.Count &gt; 0) { MakeWriter(); accessGranter = m_qWaitingWriters.Dequeue();</p><p>} else if (m_numWaitingReaders &gt; 0) { AddReaders(m_numWaitingReaders); m_numWaitingReaders = 0; accessGranter = m_waitingReadersSignal;</p><p>// Create a new TCS for future readers that need to wait m_waitingReadersSignal = new TaskCompletionSource&lt;Object&gt;();</p><p>}</p><p>} Unlock();</p><p>} }</p><p>// Wake the writer/reader outside the lock to reduce // chance of contention improving performance if (accessGranter != null) accessGranter.SetResult(null);</p><p>As I said, this code never blocks a thread. The reason is because it doesn\u2019t use any kernel constructs internally. Now, it does use a SpinLock that internally uses user-mode constructs. But, if you recall from the discussion about SpinLocks in Chapter 29, a SpinLock can only be used when held over sections of code that are guaranteed to execute in a short and finite amount of time. If you examine my WaitAsync method, you\u2019ll notice that all I do while holding the lock is some integer calculations and comparison and perhaps construct a TaskCompletionSource and add it to a queue. This can\u2019t take very long at all, so the lock is guaranteed to be held for a very short period of time.</p><p>Similarly, if you examine my Release method, you\u2019ll notice that all I do is some integer calculations, a comparison and perhaps dequeue a TaskCompletionSource or possibly construct a TaskCompletionSource. Again, this can\u2019t take very long either. The end result is that I feel very comfortable using a SpinLock to guard access to the Queue. Therefore, threads never block when using this lock, which allows me to build responsive and scalable software.</p><h6>The Concurrent Collection Classes</h6><p>The FCL ships with four thread-safe collection classes, all of which are in the System.Collections. Concurrent namespace: ConcurrentQueue, ConcurrentStack, ConcurrentDictionary, and ConcurrentBag. Here is what some of their most commonly used members look like.</p><p>// Process items in a first\u00adin, first\u00adout order (FIFO) public class ConcurrentQueue&lt;T&gt; : IProducerConsumerCollection&lt;T&gt;,</p><p>IEnumerable&lt;T&gt;, ICollection, IEnumerable {</p><p>}</p><p>public ConcurrentQueue(); public void Enqueue(T item); public Boolean TryDequeue(out T result); public Int32 Count { get; } public IEnumerator&lt;T&gt; GetEnumerator();</p><p>// Process items in a last\u00adin, first\u00adout order (LIFO) public class ConcurrentStack&lt;T&gt; : IProducerConsumerCollection&lt;T&gt;,</p><p>IEnumerable&lt;T&gt;, ICollection, IEnumerable {</p><p>}</p><p>public ConcurrentStack(); public void Push(T item); public Boolean TryPop(out T result); public Int32 Count { get; } public IEnumerator&lt;T&gt; GetEnumerator();</p><p>// An unordered set of items where duplicates are allowed public class ConcurrentBag&lt;T&gt; : IProducerConsumerCollection&lt;T&gt;,</p><p>IEnumerable&lt;T&gt;, ICollection, IEnumerable {</p><p>}</p><p>public ConcurrentBag(); public void Add(T item); public Boolean TryTake(out T result); public Int32 Count { get; } public IEnumerator&lt;T&gt; GetEnumerator();</p><p>// An unordered set of key/value pairs public class ConcurrentDictionary&lt;TKey, TValue&gt; : IDictionary&lt;TKey, TValue&gt;,</p><p>ICollection&lt;KeyValuePair&lt;TKey, TValue&gt;&gt;, IEnumerable&lt;KeyValuePair&lt;TKey, TValue&gt;&gt;, IDictionary, ICollection, IEnumerable {</p><p>}</p><p>public ConcurrentDictionary(); public Boolean TryAdd(TKey key, TValue value); public Boolean TryGetValue(TKey key, out TValue value); public TValue this[TKey key] { get; set; } public Boolean TryUpdate(TKey key, TValue newValue, TValue comparisonValue); public Boolean TryRemove(TKey key, out TValue value); public TValue AddOrUpdate(TKey key, TValue addValue,</p><p>Func&lt;TKey, TValue&gt; updateValueFactory); public TValue GetOrAdd(TKey key, TValue value); public Int32 Count { get; } public IEnumerator&lt;KeyValuePair&lt;TKey, TValue&gt;&gt; GetEnumerator();</p><p>All these collection classes are non-blocking. That is, if a thread tries to extract an element when no such element exists, the thread returns immediately; the thread does not block waiting for an element to appear. This is why methods like TryDequeue, TryPop, TryTake, and TryGetValue all return true if an item was obtained and returns false, if not.</p><p>These non-blocking collections are not necessarily lock-free. The ConcurrentDictionary class uses Monitor internally, but the lock is held for a very short time while manipulating the item in the collection. ConcurrentQueue and ConcurrentStack are lock-free; these both internally use Interlocked methods to manipulate the collection. A single ConcurrentBag object internally consists of a mini-collection object per thread. When a thread adds an item to the bag, Interlocked methods are used to add the item to the calling thread\u2019s mini-collection. When a thread tries to extract an element from the bag, the bag checks the calling thread\u2019s mini-collection for the item. If the item is there, then an Interlocked method is used to extract the item. If the thread\u2019s mini-collection doesn\u2019t have the</p><p>item, then a Monitor is taken internally to extract an item from another thread\u2019s mini-collection. We say that the thread is stealing the item from another thread.</p><p>You\u2019ll notice that all the concurrent classes offer a GetEnumerator method, which is typically used with C#\u2019s foreach statement, but can also be used with Language Integrated Query (LINQ). For the ConcurrentStack, ConcurrentQueue, and ConcurrentBag, the GetEnumerator method takes a snapshot of the collection\u2019s contents and returns elements from this snapshot; the contents of the actual collection may change while enumerating over the snapshot. ConcurrentDictionary\u2019s GetEnumerator method does not take a snapshot of its contents, so the contents of the dictionary may change while enumerating over the dictionary; beware of this. Also note that the Count property returns the number of elements that are in the collection at the moment you query it. The count may immediately become incorrect if other threads are adding or removing elements from the collection at the same time.</p><p>Three of the concurrent collection classes, ConcurrentStack, ConcurrentQueue, and ConcurrentBag, implement the IProducerConsumerCollection interface, which is defined as follows.</p><p>public interface IProducerConsumerCollection&lt;T&gt; : IEnumerable&lt;T&gt;, ICollection, IEnumerable { Boolean TryAdd(T item); Boolean TryTake(out T item); T[] ToArray(); void CopyTo(T[] array, Int32 index);</p><p>}</p><p>Any class that implements this interface can be turned into a blocking collection, where threads producing (adding) items will block if the collection is full and threads consuming (removing) items will block if the collection is empty. Of course, I\u2019d try to avoid using blocking collections because their purpose in life is to block threads. To turn a non-blocking collection into a blocking collection, you construct a System.Collections.Concurrent.BlockingCollection class by passing in a reference to a non-blocking collection to its constructor. The BlockingCollection class looks like this (some methods are not shown).</p><p>public class BlockingCollection&lt;T&gt; : IEnumerable&lt;T&gt;, ICollection, IEnumerable, IDisposable { public BlockingCollection(IProducerConsumerCollection&lt;T&gt; collection, Int32 boundedCapacity);</p><p>public void Add(T item); public Boolean TryAdd(T item, Int32 msTimeout, CancellationToken cancellationToken); public void CompleteAdding();</p><p>public T Take(); public Boolean TryTake(out T item, Int32 msTimeout, CancellationToken cancellationToken);</p><p>public Int32 BoundedCapacity { get; } public Int32 Count { get; } public Boolean IsAddingCompleted { get; } // true if CompleteAdding is called public Boolean IsCompleted { get; } // true if IsAddingComplete is true and Count==0</p><p>public IEnumerable&lt;T&gt; GetConsumingEnumerable(CancellationToken cancellationToken);</p><p>}</p><p>public void CopyTo(T[] array, int index); public T[] ToArray(); public void Dispose();</p><p>When you construct a BlockingCollection, the boundedCapacity parameter indicates the maximum number of items that you want in the collection. If a thread calls Add when the underlying collection has reached its capacity, the producing thread will block. If preferred, the producing thread can call TryAdd, passing a timeout (in milliseconds) and/or a CancellationToken, so that the thread blocks until the item is added, the timeout expires, or the CancellationToken is canceled (see Chapter 27 for more information about the CancellationToken class).</p><p>The BlockingCollection class implements the IDisposable interface. When you call Dispose, it calls Dispose on the underlying collection. It also disposes of two SemaphoreSlim objects that the class uses internally to block producers and consumers.</p><p>When producers will not be adding any more items into the collection, a producer should call the CompleteAdding method. This will signal the consumers that no more items will be produced. Specifically, this causes a foreach loop that is using GetConsumingEnumerable to terminate. The following example code demonstrates how to set up a producer/consumer scenario and signal completion.</p><p>public static void Main() { var bl = new BlockingCollection&lt;Int32&gt;(new ConcurrentQueue&lt;Int32&gt;());</p><p>// A thread pool thread will do the consuming ThreadPool.QueueUserWorkItem(ConsumeItems, bl);</p><p>// Add 5 items to the collection for (Int32 item = 0; item &lt; 5; item++) {</p><p>Console.WriteLine(\"Producing: \" + item); bl.Add(item);</p><p>}</p><p>// Tell the consuming thread(s) that no more items will be added to the collection bl.CompleteAdding();</p><p>Console.ReadLine(); // For testing purposes }</p><p>private static void ConsumeItems(Object o) { var bl = (BlockingCollection&lt;Int32&gt;) o;</p><p>// Block until an item shows up, then process it foreach (var item in bl.GetConsumingEnumerable()) {</p><p>Console.WriteLine(\"Consuming: \" + item); }</p><p>}</p><p>// The collection is empty and no more items are going into it Console.WriteLine(\"All items have been consumed\");</p><p>When I execute the preceding code, I get the following output.</p><ul style=\"list-style-type: none;\">\n<li><p>Producing: 0</p></li>\n<li><p>Producing: 1</p></li>\n<li><p>Producing: 2</p></li>\n<li><p>Producing: 3</p></li>\n<li><p>Producing: 4</p></li>\n</ul><ul style=\"list-style-type: none;\">\n<li><p>Consuming: 0</p></li>\n<li><p>Consuming: 1</p></li>\n<li><p>Consuming: 2</p></li>\n<li><p>Consuming: 3</p></li>\n<li><p>Consuming: 4 All items have been consumed</p></li>\n</ul><p>If you run this yourself, the Producing and Consuming lines could be interspersed, but the All items have been consumed line will always be last.</p><p>The BlockingCollection class also has static AddToAny, TryAddToAny, TakeFromAny, and TryTakeFromAny methods. All of these methods take a BlockingCollection&lt;T&gt;[], in addition to an item, a timeout, and a CancellationToken. The (Try)AddToAny methods cycle through all the collections in the array until they find a collection that can accept the item because it is below capacity. The (Try)TakeFromAny methods cycle through all the collections in the array until they find a collection to remove an item from.</p><h6>Index</h6><h6>Symbols</h6><p>.cctor methods. See type constructors</p><p>.NET Framework checking for installation of, 8 extension methods for casting to WinRT, 655 installing, 8 migrating older code, 459 namespace documentation, 101 reflection API, 583 stability improvements with, 34 standardization of, 24 stream projections, 655 version, checking, 8 WinRT interoperability with, 654, 655</p><p>.winmd file extension, 644</p><p>.winmdobj file extension, 659</p><h6>A</h6><p>AbandonedMutexException exception, 785 Abort method, 579, 581</p><p>Thread class, 679 aborting threads, 578 accessibility, 166 accessibility modifiers, 156 accessing resources, 505 accessor methods, 228, 231</p><p>accessibility of, 248 generic, 248 for indexers, 244 inlining, 247 naming, 245 performance of, 247</p><p>AcquireReaderLock method, 577 AcquireWriterLock method, 577</p><p>Action Center. See Windows Action Center Action delegate, 279 Activator class, 594 add-in support, 596 Add methods, arguments for, 236 AddEventHandler method, 603 AddItemHelper method, 501 AddMemoryPressure method, 538 AddParticipant method, 805 AddrOfPinnedObject method, 548 AddToAny methods, 822 AddValue method, 627, 631 administrative control over applications, 62 AggregateException objects, 699 AggregateException type, 702 AIPs (automatically implemented properties), 231 AL.exe, 52, 53</p><p>command-line switches for, 87 publisher policy assemblies, creating, 86</p><p>AllBegun method, 770 AllDone method, 771 Alloc method, 546, 548 ampersands in strings, 608 anonymous function, 410, 411</p><p>body of, 413 modifiers, unnecessary, 410</p><p>anonymous method, referencing instance</p><p>members, 412 anonymous methods, vs. lambda expressions, 413 anonymous types, 237</p><p>declaring properties in, 238 defining, 239 implicitly typed arrays and, 377</p><p>AnotherHybridLock method, 793</p><h6>API for .NET Framework</h6><p>API for .NET Framework, 583 APIs, WinRT, calling, 650 AppDomain type, instance methods, 595 AppDomainManager class, 576 AppDomainMonitorDelta class, 573 AppDomains, 17</p><p>aborting threads, 578 accessing objects across boundaries, 559 application output, 562 ASP.NET applications and, 575 assemblies in, 557 assemblies in, discovering, 602 assemblies using multiple, 558 callback methods, 573 confirming thread transitions between, 565 CreateDomain method overloads, 563 creating, 563 default, 556 definition of, 556 demonstrations of, 563 executable applications and, 574 features of, 556 first-chance exception notifications, 573 foreground/background threads and, 688 friendly names, 563 garbage collection, 571 garbage collection handle tables, 545 graceful aborts, 577 instance fields and, 566, 567 isolation provided by, 557, 558, 564, 566 loading assemblies for specific, 585 loading assemblies into, 564 loading assemblies into multiple times, 586 managed code and, 576 marshaling objects by value, 568 marshaling objects by references, 564 memory usage, monitoring, 524 method calls across boundaries, 566 Microsoft Silverlight applications and, 574 monitoring, 571 passing objects across boundaries, 569 permission set inheritance, 563 proxy types, defining, 564 purpose of, 553 read-only properties in, 572 robust host applications, 577</p><p>singleton types and, 634 SQL Server and, 575 static members and, 567 thread pools. See thread pools threads and, 562 unloading, 477, 494, 566, 570 unloading, as garbage collection trigger, 519 XML web services applications and, 575</p><p>AppDomainSetup object, 563 Append method, 338 AppendChar method, 358 AppendFormat method, 338, 345, 346, 347 AppendLine method, 338 application developers, exception handling</p><p>guidelines, 478, 485 application models, threading models for, 748 application state, serialization/deserialization</p><p>of, 616 application types, 36 ApplicationException type, 466 applications</p><p>add-in support, 596 administrative control, 62 AppDomains and, 574 architecting for events, 250 compiling, 35 configuration files, 62 configuring for assembly download, 45 dynamically extensible, 583, 588 installation complexity, 34 optimizing, 19 single instance, 781</p><p>ApplyPolicy method, 587</p><p>.appx files, 60 architecting applications for events, 250 ArgumentNullException exception, 647 arguments</p><p>commas in, 212 order of evaluation, 210, 211 passing variable number of, 220</p><p>arity, 273</p><p>overloading based on, 286 Array class, 373 array covariance, 379 array initializers, 376 Array type, 380</p><p>ArrayEnumerator objects, 278 arrays, 373. See also vectors</p><p>accessing, 386 accessing elements of, 383 accessing with unsafe code, 386 allocating, 373, 374 allocating on thread stack, 388 casting, 378 casting between types, 379 collection implementation, 381 compiler inferences of, 376 converting, 378 copying, 378, 382 copying elements, 380 covariance, 379 creating, 596 of Decimal values, 383 derivation of, 373, 380 dynamically creating, 383 embedding in structures, 390 implicitly typed, 239, 376, 377 indexes into, 375 initializing elements, 376 inline, 390 internals, 384 jagged, 375 in managed heap, 374 multi-dimensional, 385, 386 non-zero lower bounds, 383 overhead information, 374 passing and returning, 382 of polygons, 375 of reference types, 373 referencing, 376 single-dimensional, 375 static methods for, 380 of tasks, 710 type names, 385 types of, 384 unsafe access, 388 WinRT, 648, 660 zero-based, 374 zero-length, 382</p><p>as operator, 96 AsBuffer method, 658 ASCII encoding, 351</p><p>ASCIIEncoding object, 353 AsOrdered method, 718 ASP.NET</p><p>asynchronous objects, 751 threading model, 748</p><p>ASP.NET applications AppDomains and, 575 processes run by, 687</p><p>assemblies 32-bit and 64-bit, 8 administrative control, 62 Assembly Linker and, 52 benefits of, 7 characteristics of, 45 combining modules into, 45 compressing, 74 configuration files, 84 configuring download of, 45 creating from managed modules, 7 culture, 59 data files in, 46 default path to, 38 defined, 6, 45 delayed signing, 76 deploying and packaging, 60 different types in, 46 directories for, 63 discovering types in, 590 double copies of, 75 downloading from web, 586 executing code, 11 files required for, 50 friend, 154 global assembly cache, 72 globally deployed, 67 loading into AppDomains, 564 locating, 63, 83 manifest tables, 47 multifile, 46, 48 namespaces and, 101 platform, specifying, 8 preventing execution of, 587 private deployment of, 67 privately deployed, 61 publisher policy controls, 86 referencing, 37, 51, 70, 74, 98</p><h6>assemblies</h6><h6>Assembly Linker</h6><p>assemblies (continued) registering, 74 resource files, adding, 53 satellite, 59 self-describing nature of, 7 serialization and, 616 signing files, 70 statistics, viewing, 44 strongly named, 66, 67, 68, 72 uninstalling from GAC, 73 unloading, 587 used by multiple AppDomains, 558 verification, 76 verification, skipping, 77 version numbers, 58 version resource attributes, 56 version resources in, 54 versioning issues, 597 versioning policies, 587 Visual Studio IDE and, 51 weakly named, 66, 68 Win32 resources, embedding, 54 Windows installer and, 73</p><p>Assembly Linker, 52, 53 assembly loading, 584</p><p>callback methods, 587, 588 embedding dependent DLLs, 588 Load method, 585 for metadata analysis, 586 multiple times into AppDomain, 586 for specific AppDomain, 585 for specific architecture, 585 with specific path name, 586</p><p>AssemblyDef table, 47 AssemblyRef table, 39 AssemblyResolve event, 588 assembly type, 25, 592 AsSequential method, 718 Assert method, 502, 503 associating data, 550 AsStream method, 658</p><p>buffering, 656 Assume method, 502, 503 AsTask method, 653, 654 async, 651 AsyncCoordinator class, 770, 771</p><p>asynchronous compute-bound operations,</p><p>693. See compute-bound asynchronous operations</p><p>calling, 693 initiating, 700 Windows scheduler and, 694</p><p>asynchronous exceptions, 495 asynchronous flag, 729 asynchronous functions, 732</p><p>await operator and, 746 awaiters and, 738 compiler warnings, turning off, 746 debugging, 745 event handlers and, 741 examples of, 735 exception handling and, 744 extensibility of, 738 in Framework Class Library, 742 identifying, 733 initiating from non-caller thread, 745 restrictions on, 734 as state machines, 734 stepping into and out of, 745 Task object and, 733 thread synchronization and, 757 unhandled exceptions and, 745 WebClient and, 744</p><p>asynchronous I/O operations, 729 asynchronous functions and, 732 canceling, 751 concurrent function of, 746 debuggers and, 731 event handlers and, 742 FileStream objects and, 753 garbage collection and, 731 GUI responsiveness and, 732 ignoring SynchronizationContext object, 750 incomplete, showing, 738 performance benefits, 731 Task type for, 738 thread priorities, 754</p><p>asynchronous lambda expressions, 746 asynchronous operations, 650, 691 asynchronous server implementation, 751 asynchronous thread synchronization, 814 asynchonous WinRT APIs, 649</p><p>AsyncOneManyLock class, 816 Attribute class, 424 attributes. See also custom attributes</p><p>setting, 368 AttributeUsage attribute, 428 automatically implemented properties, 231 auto-reset events, 782, 784 AutoResetEvent method, 786 await, 651 await operator, 652 awaiters, 738, 740</p><h6>B</h6><p>background threads vs. foreground, 688 backing fields, 230 Backus-Naur Form grammar for type</p><p>names, 592 Barrier class, 805 base-64 encoding and decoding, 356 Base class, 296, 312</p><p>in WinRT, 645 base types, vs. interfaces, 312 BeginBackgroundProcessing method, 755 BeginWaitForConnection method, 743 BinaryFormatter object, 614 binary namespace, 614 binary operators, 192</p><p>nullable types and, 444 BinaryReader type, 350 BinaryWriter type, 350 binding</p><p>early vs. late, 589 handles for, 608</p><p>BindToName method, 641 BindToType method, 641 Bing maps, 659 bit flags, 367</p><p>converting numeric values into, 370 enumerated types for, 368 numeric equivalents, 369 setting, 368</p><p>bitmaps, garbage collection of, 538 BlockCopy method, 380 blocking collections, 820</p><p>blocking threads avoiding, 705, 805 countdown for, 804 reasons for, 806</p><p>BlockingCollection class, 820 blocks</p><p>catch, 455, 457, 480, 482 finally, 455, 456, 478, 497, 535 try, 454, 497</p><p>bool type, 112 boxing, nullable types, 447 boxing value methods, 135 boxing value types, 124 changing fields, 136 efficiency of, 130 example code, 128</p><p>Break method (Parallel class), 716 breakpoints, AIPs and, 232 buffering</p><p>AsStream methods and, 656 WinRT, 658</p><p>buffering LINQ query results, 720 byte array method, garbage collection</p><p>and, 548 byte arrays, 656 byte order mark (BOM), 353 byte type, 112</p><h6>C</h6><p>C# asynchronous functions, 732 is operator, 95 optimizing code, 14 question marks in, 443 using directive, 98 WinRT component definitions in, 658</p><p>C# compiler event implementation, 256 language translation done by, 763 output of, 9 platform command-line switch, 8, 10 primitive types and, 114 referencing types, 35 response files, 36 unsafe code, 17</p><h6>C# compiler</h6><h6>C++ compiler, managed/unmanaged code and</h6><p>C++ compiler, managed/unmanaged code and, 6</p><p>caching threads, 673 weak references and, 550</p><p>callback functions, 391. See also delegates unmanaged, 391 uses for, 391</p><p>callback methods, 249. See also lambda</p><p>method for AppDomains, 573 assembly loading and, 587, 588 calling, 393 disposing of, 699 exceptions thrown by, 698 invoking, 399 lambda expressions, 410 referencing local parameters, 413 registering after cancellation, 698 returning values with, 408 syntax for, 409</p><p>calling back static methods, 394 calling methods, 163</p><p>across AppDomain boundaries, 566 calling type constructors, 188 callvirt instruction, 163 CAN-DO relationships, 312 Cancel method, 760 canceling operations, 696</p><p>after set time, 700 cooperatively, 696 invoking methods after, 698 preventing, 698 canceling tasks, 704 canceling WinRT APIs, 653 CancellationToken objects</p><p>associating with tasks, 705 passing as closure variables, 705</p><p>CancellationTokenSource objects, 696</p><p>creating, 699 CancellationTokenSource type, 704 CancellationToken value type, 697 CannotUnloadAppDomainException</p><p>exception, 571 carriage returns, 322 casting arrays, 378</p><p>casting, Char type to numeric value, 319 casting types</p><p>between objects, 93 dynamic, 145 implicit vs. explicit, 114 with is/as operators, 95 primitive, 114</p><p>catch blocks, 455, 457, 482. See also finally</p><p>blocks; try blocks constrained execution regions and, 475 exceptions thrown by, 457 overuse of, 480 redundant, warning for, 459 stack trace, obtaining, 461</p><p>catch types, 455</p><p>variable names after, 456 CCCheck.exe (Code Contract Checker), 503 CCDocGen.exe (Code Contract Document</p><p>Generator), 504 CCRefGen.exe (Code Contract Reference</p><p>Assembly Generator), 504 CCWs (COM Callable Wrappers), 644 CERs. See constrained execution regions (CERs</p><p>chaining delegates, 400, 404</p><p>C# support for, 404 ChainSelector method, 639 char type, 112, 317</p><p>casting to numeric value, 319 converting numeric types to, 319 instance methods, 318 static methods, 318</p><p>character arrays, 337 methods for, 338</p><p>characters, 317. See also text elements converting to bytes, 353 examining in strings, 333 expanding, 327 lowercase/uppercase, changing, 318 numeric equivalent, returning, 318</p><p>checked operators, 116 child tasks, 707 circuit boards, 727 class constraint, 287 class constructors. See type constructors class inheritance. See inheritance class libraries, thread synchronization and, 759</p><p>class library developers, exception handling guidelines, 478, 485</p><p>classes defining, 168, 276 generic collection, 125 inheritance, 296 partial, 159 as reference types, 119 sealed, 168 security, 168 static, 158 WinRT, 646</p><p>cleanup code, 478. See also catch blocks; finally</p><p>blocks; try blocks; garbage collection construction of, 479 Dispose method, 535, 537 exceptions thrown by, 457 guaranteeing, 497 for native resources, 533</p><p>Clear method, 337 CLI (Common Language Infrastructure), 24 Clone method, 335 cloneable variables, 301 cloning strings, 335 cloning, via serialization, 615 Close method, 648 closed types, 272 CloseHandle method, 780 CLR (Common Language Runtime) code language vs. behavior, 25 code location for, 554 common language aspect of, 152 compilers for, 4 defined, 3 demonstration of, 104 field layout, 123 functions performed by, 471 headers, 5 hosting, 554 interoperability with unmanaged code, 30 loading, 8, 556 namespaces and, 98 nullable type support, 447 passing blocks of data to WinRT, 656 setting host managers, 555</p><h6>CollectionCount method</h6><p>shutting down, as garbage collection</p><p>trigger, 519 type references, resolving, 80 type safety, 95 version, checking, 8 versioning in, 161 WinRT, mapping types to, 659</p><p>CLR projections, 645 WinRT component type system rules</p><p>and, 645 CLRCreateInstance function, 554 CLRVer.exe, 8 CLS (Common Language Specification), 26</p><p>example code using, 27 exceptions, 458 rules overview, 28</p><p>Code Contract Checker (CCCheck.exe), 503 Code Contract Document Generator Tool</p><p>(CCDocGen.exe), 504 Code Contract Reference Assembly Generator (CCRefGen.exe), 504</p><p>code contracts, 475, 498 checking, 503, 504 Contract class, 498 defining, 501 exceptions, 500 runtime function, 502 tools for maximizing, 499 turning on features for, 500 violation of, 500</p><p>code explosion, 183, 277 code optimization, 14, 763</p><p>Volatile class and, 765 code pages, specifying, 352 code, program order, 765 code reuse. See generics codeBase elements, 45, 84 Collect method, 519, 522, 523 collection classes</p><p>generic, 125, 270 thread-safe, 818</p><p>collection initializers, 235 collection objects, serializing/deserializing, 622 collection types, WinRT, 648 CollectionCount method, 524</p><h6>collections</h6><p>collections, 260 adding items to, 236 array implementation of, 381 maximum number in, 821 non-blocking, into blocking, 820 producer/consumer scenario, 821 WinRT, 648</p><p>ColorMatrix class, 244 COM Callable Wrappers (CCWs), 644 COM components, WinRT components as, 644 Combine method, 400 command-line switches, response files, 36 Common Language Specification. See CLS (Common Language Specification)</p><p>Common Type System. See CTS (Common Type System) compacting memory in garbage collection, 509, 521</p><p>Compare method, comparing strings, 325 CompareExchange method, 769, 776, 810 CompareInfo</p><p>class, methods in, 329 object, 326, 327 property, 327</p><p>CompareOptions type, 324 CompareTo method, 135, 311, 318 comparing strings, 323 comparisonType argument, 324 CompilationRelaxations attribute, 330 CompilerGenerated attribute, 410 compilers</p><p>code optimization by, 763 functions performed by, 470 managed, exception handling by, 492</p><p>compiling source code, 3 CompleteAdding method, 821 Component Object Model (COM). See COM</p><p>objects Component Software Processing (CSP), 160 components</p><p>overview of, 160 servicing, 161</p><p>composability of object-oriented programming, 470 compressing, assemblies, 74</p><p>compute-bound asynchronous operations, with threads, 678</p><p>compute-bound operations, 691, 693 canceling, 696 canceling after set time, 700 cooperative cancellation, 696 initiating, 700 periodic, 720 preventing cancellation of, 698</p><p>ComputeResult method, 770 Concat method, 129, 222 concatenating strings, 322 ConcurrentBag class, 818 concurrent classes, 818 ConcurrentDictionary class, 818 ConcurrentExclusiveSchedulerPair class, 815 ConcurrentQueue class, 818 ConcurrentStack class, 818 condition variable pattern, 811 conditional attribute classes, 440 ConditionalWeakTable class, 551 configuration files, 62, 84</p><p>components of, 84 locating, 64 publisher policy controls and, 86 as separate assembly files, 87</p><p>ConfigureAwait method, 750 Console class, 345, 759 constants</p><p>constraints on, 175 defined, 151, 175 metadata and, 175 for non-primitive types, 175</p><p>const-ness, unsupported, 224 ConstrainedCopy method, 380 ConstrainedExecution namespace, 528 constrained execution regions (CERs), 475, 494 constraints</p><p>constructor, 289 generics and, 285 primary, 287 reference types as, 287 secondary, 288 on type parameters, 287</p><p>constructing type instances, 594 constructor constraints, 289</p><p>constructor method, 43, 181, 184, 187, 396 calling default, 186 creating types without, 182 default, 181 defining multiple, 182 defining on value types, 185 for delegates, 398 inheritance, 181 parameterless, 186 virtual methods and, 182</p><p>ConstructorInfo object, 595 constructors</p><p>defining, 595 for custom attribute classes, 428 named parameters, 424 parameterless, 235 positional parameters, 424 for serialization/deserialization, 628 Task objects and, 701 thread, 679</p><p>context structures, 683 context switching, 672, 683</p><p>in asynchronous synchronization, 814 avoiding, 673 I/O operations and, 731 performance issues, 672, 673</p><p>contexts, thread, 671 ContinueWhenAll method, 710 ContinueWith method, 705, 707, 729, 733 Contract class, 475 contract reference assemblies, 504 ContractFailed event, 500 ContractInvariantMethod attribute, 502 ContractReferenceAssembly attribute, 504 contra-variant reference types, 395 contra-variant type parameters, 279</p><p>in and out keywords, 280 convenient syntax, 183 conversion constructors, 195 conversion operator methods, 195</p><p>constructing, 197 implicit and explicit, 196, 197 metadata for, 197 parameters for, 197 support for, 198</p><p>conversion operators, defined, 152</p><h6>CreateEvent method</h6><p>Convert class, 628 Convert method, 355 Convert type, 319, 356 ConvertIList method, 289 cooperative cancellation, 696</p><p>unhandled exceptions and, 698</p><p>Copy method, 335 Array class, 378, 382 vs. BlockCopy method, 380</p><p>copy operation, and unboxing types, 127 copying</p><p>arrays, 378, 382 strings, 335</p><p>CopyTo method, 335, 338, 658 CoreClr.dll, 574 cores, multiple. See multiple cores CorFlags.exe, 9 corrupted</p><p>memory, 506 object state, avoiding, 228 state exceptions (CSEs), 489 corruption. See state corruption CountdownEvent class, 804 Counter method, 393, 394</p><p>delegate chaining and, 403 country identifiers, 326 covariant</p><p>in and out keywords, 280 reference types, 395 type parameters, 279</p><p>CPU virtualization, 670. See also threads CPUs. See also multiple cores</p><p>aggressive utilization of, 682 analyzing usage of, 682 heat dissipation and, 677, 681 high usage, as good thing, 688 hyperthreaded, 677 low usage of, 691 multiple cores, 677 power utilization, optimal, 681 trends for, 677</p><p>CreateDelegate method, 417 CreateDomain method, AppDomain overloads</p><p>of, 563 CreateEvent method, 780</p><h6>CreateFile method, synchronous execution of</h6><p>CreateFile method, synchronous execution</p><p>of, 752 CreateInstanceAndUnwrap method, 564 CreateInstanceFrom method, 595 CreateInstance method, 383</p><p>Activator class, 594 AppDomain class, 595 Array class, 596</p><p>CreateMemoryResource notification, 519 CreateMutex method, 780 CreateProcess function, 557 CreateSemaphore method, 780 critical regions, 577 CriticalFinalizer object, 528 CriticalHandle class, 531 CSC.exe, 36 CSC.rsp, 36 CSP containers for public/private keys, 78 CTS (Common Type System), 24 CUI (Console User Interface) applications, 36 culture setting in assemblies, 59 CultureInfo class, 318 CultureInfo type, 326, 342 CurrentCulture property, 318, 326 CurrentUICulture property, 326 custom attributes, 421. See also attributes</p><p>applying multiple times, 427 attribute objects, constructing, 431 AttributeUsage attribute, 428 class inheritance, 425, 428 conditional attribute classes, 440 constructors, 428 creating, 425 C# syntax for, 422 data types for, 428 defining attribute class, 425 defining classes for, 428 definition of, 424 derivation of, 424 detecting in code, 430 detecting in code, without executing, 437 detecting specific, 432 discovering, 602 enumerated types, 427 examples of, 422 as logical state containers, 425</p><p>matching against each other, 434 multiple, applying, 424 named parameters, 424 positional parameters, 424 prefixes, 423 public constructors, 427 security concerns, 437 serialization and, 429 suffix, omitting, 424, 425 syntax for, 424 targets for, 423 Visual Basic .NET syntax for, 423</p><p>CustomAttributeData class, 437 CustomAttributeExtensions class, 430</p><h6>D</h6><p>data collections, LINQ queries on, 717 data encapsulation, 228 data types, for custom attribute classes, 428.</p><p>See types DataContractSerializer class, 612 datatips, 339 date formatting, 341 dates and times, in WinRT, 647 DateTime</p><p>structure, 647 type, 349</p><p>DateTimeFormatInfo type, 343 DateTimeOffset structure, 647 deadlocks, 750, 761 debuggable attribute, 511 debuggers</p><p>generics and, 271 asynchronous I/O operations and, 731 threads and, 673</p><p>debugging</p><p>and garbage collections, 510 asynchronous functions, 745 exceptions, 490</p><p>decimal type, 112, 118 declaring delegates, 392 declaring methods, for variable number of</p><p>arguments, 220 declaring properties, 231 in anonymous types, 238</p><p>Decoder class, 356 Decoding</p><p>bytes, 356 strings, 356</p><p>Decrement method, 791 decrypting SecureStrings, 360 dedicated threads, 679 default AppDomain, 556 default properties. See parameterful properties DefaultMember attribute, 246 DefaultParameterValue attribute, 212 defining</p><p>classes, 168 fields, without initialization, 184 properties, 230 types, 251</p><p>definition tables in metadata, 39 Delay method, 722 delayed signing, 76 Delegate class, 397 delegate fields, 257 delegate types</p><p>extension methods for, 202 serializing, 618 WinRT, 647</p><p>delegates, 249. See also callback functions adding/removing, 604 arrays, returning each reference in, 405 callback method, invoking, 399 calling back instance methods, 395 calling back static methods with, 394 calling private members with, 394 chaining, 400, 404 chaining, C# support for, 404 chaining, result variable and, 404 class defined by, 396, 397 constructors, 398 contravariant type parameters, 279 covariant type parameters, 279 creating, 392 defining, 408 definitions for, 407 extension methods and, 203 garbage collection, 401 generic, 278, 407 initialization of, 398</p><h6>Display method</h6><p>invariant type parameters, 279 invoking dynamically, 417 MulticastDelegate class, 397 reflection and, 416 removing from chains, 403 syntax, 408 syntax shortcuts for, 409, 410, 413 for tasks, passing, 715 unregistering, 260</p><p>Delete method, exceptions thrown by, 532 delete operator, 93 DeleteAsync method, 742 deploying assemblies, 60</p><p>privately, 79 derived class, 311, 312 derived types, preconditions and, 502 deserialization</p><p>of application state, 616 of collection objects, 622 controlling, 619 controlling data, 624 definition of, 611 formatter consistency, 615 ISerializable interface, 625 matching serialization method, 628 into object graphs, 615 of objects, as different objects, 633 overriding assembly/type, 640 quick start for, 613 setting up, 628 of singleton types, 633 streaming contexts for, 631 surrogates for, 636 type bindings, 641 of type instances, 624</p><p>Deserialize method, 635 DeserializeFromStream method, 615 deserializing objects, 182 diagnostics namespace, 688 dictionary type, serialization, 626 direct memory accesses, 386 DisableOptimizations flag, 511 disambiguating types, 99 DispatcherTimer classes, 723 DispatcherUnhandledException event, 488 Display method, 283</p><h6>Dispose method</h6><p>Dispose method, 260, 358, 512, 533, 648, 699 avoiding, 535 calling explicitly, 537 in finally block, 535 native resource cleanup and, 534 on Task objects, 708 WaitHandle class, 780</p><p>distributed applications, unhandled exceptions</p><p>in, 488 DllMain method, 671 DLLs</p><p>interoperability of, 34 performance issues and, 672 thread-attach and thread-detach</p><p>notifications, 671 double-check locking, 807 double type, 112 downloading assemblies from web, 586 DumpBin.exe, 9 dynamic expressions, as object types, 147 dynamic keyword, 214 dynamic type, 112, 144</p><p>example of, 145 vs. var keyword, 146</p><p>dynamically extensible applications, 583 building, 588 Managed Extensibility Framework (MEF), 598</p><p>DynamicAttribute method, 145 DynamicInvoke method, 417 dynamification, 147</p><h6>E</h6><p>early binding, 589 encoding</p><p>explicitly constructing types, 353 methods, 355 stateless, 356</p><p>Encoding class, 352 properties, 352</p><p>encodings, 350</p><p>property values, 354 EndBackgroundProcessing, 755 EndsWith method, comparing strings, 325 EndWaitForConnection method, 743 EndXxx method, 481</p><p>EnsureCapacity method, 337 EnsureInitialized method, 811 EnsureSufficientExecutionStack method, 489 Enter method, 790</p><p>overloading, discouraged, 476 Monitor class, 795, 797</p><p>Enum class, 362 Enum type, 364 enumerated type, 361</p><p>bit flags, defining with, 368 classes derived from, 362 converting symbols into, 366 converting values into flags, 370 declaring, 363 defined symbols in, 370 defining, 367 displaying all values for, 365 extension methods for, 202 formatting, 364 IsDefined method and, 371 mapping to strings, 364 methods, adding, 371 with multiple symbols, 364 parameter validation, 366 as primitive types, 363 serializing, 618 structure of, 362 symbols as constant fields, 362, 363 underlying types for, 363 user interface display of, 365</p><p>enumerating types, 588 enumeration types, in WinRT, 646 ephemeral garbage collector. See garbage</p><p>collection epilogue code, 102 equality operators, nullable types and, 444 Equals method, 139, 238, 332, 338, 355</p><p>overriding, 140, 141 properties of, 141 System.Object, 92</p><p>error messages, 157 errors. See exceptions; exception handling escalation policy, 577 event handlers</p><p>asynchronous functions and, 741 class naming convention, 251</p><p>code contract violations and, 500 method signatures, 741 void return type, 253</p><p>event log, viewing unhandled exceptions</p><p>in, 485 EventArgs class, 251 EventAwaiter class, 741 EventDef table, 39 EventHandler delegate type (WinRT), 647 EventHandlersStore type, 262 EventInfo type, 604 events, 249, 782</p><p>_add prefix, 257 architecting applications for, 250 auto-reset, 784 compiler implementation of, 256 defined, 24, 152 delegates, 249 delegates, adding/removing, 604 designing types to expose, 250 explicitly implementing, 260 members, defining, 252 metadata on, 604 notification for, 253 notifications, unregistering, 260 Object type, 252 as primitive kernel-mode thread</p><p>synchronization constructs, 778 raising, in thread-safe way, 254 types for listening, 258 typing the return parameter, 253 typing the sender parameter, 252 unregistering, 260 unregistering interest from objects, 257 WinRT, 647</p><p>EventSet class, 262 EventWaitHandle class, 780 Exception class, 460 exception-derived types, hierarchy of, 593 exception handling</p><p>assuming exceptions will occur, 470 asynchronous functions and, 744 backing out of partial operations, 482 best practices, 478 catch blocks, 455, 480</p><h6>exceptions</h6><p>for class library developers vs. application</p><p>developers, 478, 485 CLS objects, 458 defined, 453 differing implementations of, 492 example code for, 453 exceptions thrown by, 457 finally blocks, 456, 478 guidelines, 478, 485 for high error rate methods, 493 language for, 457 localizing string messages, 467 managed compilers and, 492 mechanics of, 453 migrating code for, 459 non-CLS objects, 458 notifications for, 456 performance issues, 492 problems with, 477 programming culture and, 473 reflection and, 485 Reliability Monitor problem signatures, 487 reliability vs. productivity, 469 resetting exception starting point, 462 stack tracing, 461, 462, 463 state corruption and, 474 state corruption, mitigating, 475 System.Exception type, 460 testing with Performance Monitor, 493 thread synchronization locks and, 476 try blocks, 454 TryParse method and, 494 viewing exception objects, 455 Windows Error Reporting, 488</p><p>Exception objects, avoiding throwing, 466 exceptions</p><p>adding types to Visual Studio debugger, 491 asynchronous, 495 catching all, when to, 482 catching specific, when to, 483 class derivation of, 469 for code contract violations, 500 in critical regions, 577 debugging, 490 defined, 452 defining custom classes for, 467</p><h6>Exceptions dialog box (Visual Studio)</h6><p>exceptions (continued) designing for, 453 FCL-defined classes, 463 locating source of, 484 maintaining method contracts after, 483 potential sources of, 471 processing, 573 recovering gracefully from, 481 re-throwing, 480, 482 re-throwing different exception, 483 serializable, 467 System.Exception type properties, 460 throwing, 466 types, 463, 466 unhandled, 474, 480, 485, 488 versioning issues, 466 WinRT and, 647</p><p>Exceptions dialog box (Visual Studio), 490 Exchange method, 769 executable applications, AppDomains and, 574 executable files. See also PE (portable</p><p>executable) files 32-bit vs. 64-bit, 9 compiling, 35</p><p>ExecuteCodeWithGuaranteedCleanup</p><p>method, 497 ExecuteDbDataReaderAsync method, 742 ExecuteNonQueryAsync method, 742 ExecuteReaderAsync method, 742 ExecuteScalarAsync method, 742 ExecuteXmlReaderAsync method, 742 executing assembly code, 11 execution context, 694</p><p>controlling, 695 ExecutionContext class, 695 helper threads and, 695 performance issues and, 695 suppressing, 695</p><p>Exit method, 574</p><p>Monitor class, 795, 813 expanding characters, 327 ExpandoObject class, 242 explicit interface method implementations</p><p>(EIMIs), 303, 308, 310 explicit methods, state corruption and, 476 explicit type casts, 114</p><p>explicitly implementing events, 260 ExportedTypesDef table, 47 exposing events, designing types for, 250 exposing state, 168 expressions, casting to dynamic, 145 Extensible Storage Engine, 477 extension attribute, 203 extension methods, 198, 201, 362</p><p>advantages of, 199 declaring, 200 defining, 255 for delegate types, 202 displaying list of, 200 for enumerated types, 202 for interface types, 202 LINQ technology, 202 multiple, avoiding, 201 for passing data between CLR and</p><p>WinRT, 657 performance issues, 201 rules and guidelines for, 200 versioning and, 201</p><p>ExtensionAttribute class, 203</p><h6>F</h6><p>factories, task, 709 FailFast method, 477, 480 Family and Assembly accessibility, 157 FaxMsg method, 259 FCL (Framework Class Library), 22</p><p>exceptions defined by, 463 generics in, 270 hybrid constructs in, 793 thread safety, 759 unhandled exception documentation, 488 WinRT supported types, 645</p><p>FCL types vs. primitive types, 113 with corresponding primitive types, 112</p><p>FeedbackToConsole method, 394 FeedbackToFile method, 396 FeedbackToMsgBox method, 394 field layout for types, 123 FieldDef table, 39 FieldGetter method, 566</p><p>FieldInfo objects, converting to RunTimeFieldHandle, 609</p><p>fields backing, 230 in boxed value types, changing, 136 defined, 24, 151, 177 defining, 178 defining, without initialization, 184 encapsulating, 228 initializing, 182, 187 initializing, with convenient syntax, 183 inline initialization, 179 modifiers for, 177 private, 168, 228, 231 FieldSetter method, 566 file extensions</p><p>for WinRT components, 644</p><p>.winmdobj, 659 file names, WinRT, 645 file versioning, difficulties with, 66 FileAttributes type, 367 FileDef table, 47, 81 FileNotFoundException exception, 584 FileStream</p><p>class, 532 I/O operations and, 728 objects, asynchronous and synchronous</p><p>operations with, 753 type, 525, 537</p><p>finalization, 525, 542 garbage collection and, 544 list, 542 freachable queue and, 544 Finalize method, 525, 526, 542 CLR thread dedicated to, 544 inherited, 543 memory leaks and, 526 overriding, 479 similarity to destructor, 526 System.Object, 92</p><p>finally blocks, 455, 456. See also catch blocks;</p><p>try blocks as callback methods, 497 constrained execution regions and, 475</p><h6>FxCopCmd.exe</h6><p>Dispose method in, 535 exceptions thrown by, 457 liberal use of, 478 try blocks and, 457</p><p>FirstChanceException event, 573 fixed statement, 548 Flags attribute, 368 FlagsAttribute class, 430 Flatten method, 703 float type, 112 FlushAsync method, 742 For method (Parallel class), 713, 714 ForAll method, 718 forcing garbage collection, 522, 539 ForEach method (Parallel class), 713, 714 foreground threads</p><p>forcing, 679 vs. background, 688</p><p>Format method, 344, 364 overloading, 344 vs. ToString method, 364</p><p>FormatterConverter type, 628 FormatterServices type, 623, 628 Formatting, strings, 346 Forms namespace, Timer class, 723 Framework Class Library. See FCL (Framework</p><p>Class Library) asynchronous functions in, 742 types. See FCL types</p><p>framework projections, 645, 649 freachable queue, 543</p><p>finalization list and, 544 naming of, 544</p><p>Free method, 548 friend assemblies, 154</p><p>modules in, 156 friendly names, 563 FromAsync method, 743 FromBase64String method, 356 FromCurrentSynchronizationContext</p><p>method, 711 Func delegate, 279, 408 functions, callback. See callback functions FxCopCmd.exe, 480</p><h6>GAC (global assembly cache)</h6><h6>G</h6><p>GAC (global assembly cache), 72 installing into, 72 permissions required for, 73 uninstalling assemblies from, 73</p><p>GACUtil.exe, 72 Windows install switch, 73</p><p>garbage collection, 507 algorithm, 507 asynchronous I/O operations and, 731 for bitmaps, 538 byte array method and, 548 Collect method, 523 compacting phase, 509 concurrent vs. non-concurrent mode, 521 debugging and, 510 dependency issues, 537 Dispose method, 512 vs. Dispose method, 535 finalized objects and, 544 Finalize method and, 526 forcing, 522, 539 generational, 513 generational, assumptions made by, 514 generations and, 513 handle tables, 545, 547 idle threads and, 517 large objects and, 519 latency modes, 522 lease managers, 567 marking phase, 508 memory budgeting and, 517 memory compacting, 521 memory usage, monitoring, 524 monitoring, 524 native resources and, 538 notifications for, 518, 523 object lifetime and, 510 object lifetime, controlling manually, 545 OutOfMemory exceptions, 510 performance goals, 515 pinning objects, 548</p><p>reference counting algorithms, 508 resurrected objects, 544 roots and, 509 server mode, 520 symbols for, 522 threads and, 673 Timer objects and, 721 triggers for, 519 for types wrapping native resources, 525 on unloaded AppDomains, 571 weak references and, 550 workstation mode, 520</p><p>GCCollectionMode enumerated type, 523 GCHandle type, 545</p><p>explicit, 548 GCHandleType type, 546 GCLatencyMode property, 521 GCNotification class, 518 GCSettings class, 521 GCWatch method, 551 generation,1, 2, 513, 514, 516</p><p>growth of, 516 promotions to, 515</p><p>generic arguments, limiting types, 285 collection classes, 125, 270 contravariant and covariant type</p><p>parameters, 281 delegates, 278, 407 interfaces, 277, 278, 297, 303 list algorithm, 266 namespace, 313 vs. non-generic versions, 304 type identity, 275 type parameters, 305</p><p>generic methods, 265, 281 example of, 282 overriding, 286 static, 270 type inference and, 283</p><p>generic type variables as operands, 292 casting, 290</p><p>comparing to each other, 292 comparing with null, 291 setting to default value, 291</p><p>generic types, 265 constructing, 596 defining, 273 inheritance and, 274 out and ref parameters, 282 parameters, 266 reference, 269 type arguments, 266 type objects for, 272 using directive and, 276</p><p>generics backwards compatibility, 270 benefits of, 267 constraints, 285 explicit declarations, 281 in and out keywords, 281 in FCL, 270 infrastructure of, 271 interface constraints and, 305 members and, 284 overview of, 265 performance gain with, 267, 268 verifiability, 284, 290 variance and, 280</p><p>GetAssemblyName method, 586 GetAsync method, 742 GetAvailableThreads method, 724 GetAwaiter method, 652, 738, 740 GetBaseException method, 703 GetByteArrayAsync method, 742, 770 GetBytes method, 353, 356, 658 GetCharCount method, 353, 356 GetChars method, 356 GetCustomAttribute method, 430, 431, 437 GetDecoder method, 356 GetDomain method, 562 GetEncoder method, 356 GetEncoding method, 352 GetEnumerator method, 820 GetEnumUnderlyingType method, 363</p><h6>GetWindowsRuntimeBuffer method</h6><p>GetEnumValues method, 364 GetFileAsync method, 650 GetFormat method, 342, 347 GetGenericArguments method, 602 GetHashCode method, 142, 164, 238, 355</p><p>algorithms for, 143 overriding, 142 System.Object, 92 GetHttp method, 750 GetInvocationList method, 405 GetLowerBound method, 383 GetMaxByteCount method, 353 GetMaxCharCount method, 353 GetMaxThreads method, 724 GetMetaObject method, 148 GetMinThreads method, 724 GetNumericValue method, 318 GetObjectData method, 623, 626, 630, 635</p><p>ISerializationSurrogate interface, 637 GetParameters method, 602 GetPreamble method, 355 GetRealObject method, 635 GetRequestStreamAsync method, 742 GetResponseAsync method, 742 GetResult method, 738 GetRuntime function, 554 GetSingleton method, 809 GetStreamAsync method, 742 GetString method, 355 GetTextElementEnumerator method, 333 GetTotalMemory method, 524 GetType method, 93, 135, 591, 608</p><p>calling via nullable type, 448 overloads, 591 System.Object, 92</p><p>GetTypeInfo method, 592 GetUnderlyingType method, 363 GetUnicodeCategory method, 317 GetUninitializedObject method, 624, 628, 637 GetUpperBound method, 383 GetValue method, 364, 603 GetVersionInfo method, 54 GetWindowsRuntimeBuffer method, 658</p><h6>global assembly cache (GAC)</h6><p>global assembly cache (GAC). See GAC (global</p><p>assembly cache) globally deployed assemblies, 67, 72 GlobalMemoryStatusEx function, 550 graceful aborts, 577 GUI (graphical user interface) applications, 36</p><h6>H</h6><p>handle-recycling exploits, 531 handle tables, 545, 547 HandleCollector class, 539 handles, 529, 531 HasFlag method, 368 hash codes, 142</p><p>algorithms for, 143 persisting values, avoiding, 143 read-only property for, 238</p><p>hashes, 70 helper types, 169 hidden files</p><p>determining, 368 setting attribute for, 368</p><p>High priority class, 685 high surrogate code value, 333 host applications, retrieving threads, 578 Host SDK assemblies, 597 hosting, 553</p><p>CLR (Common Language Runtime), 554</p><p>hybrid constructs, 761, 789, 805 Barrier class, 805 CountdownEvent class, 804 in Framework Class Library (FCL), 793 hybrid locks, 790 ManualResetEventSlim class, 794 Monitor class, 794 OneManyLock class, 802 ReaderWriterLockSlim class, 800 SemaphoreSlim class, 794 sync blocks, 794</p><p>hybrid locks, 790 performance issues, 790 performance of, 793 with spinning, ownership, and recursion, 791</p><p>hyperthreaded CPUs, 677</p><h6>I</h6><p>IAsyncAction interfaces, 651 IAsyncOperation interface, 651, 652 IAsyncXxx method, conversion into Task</p><p>method, 654 IAsyncXxxWithProgress interfaces, 651 IBuffer interface, 656 IBufferByteAccess interface, 656, 658 IClosable interface, 648 ICLRMetaHost interface, 554 ICollection interface, arrays and, 381 IComparable interface, 298, 308 IConvertible interface, 319 IConvertible method, 310 IDisposable interface, 358, 533, 534, 648, 708</p><p>BlockingCollection class and, 821 Idle priority class, 685 Idle thread priority level, 687 IEnumerable interface, arrays and, 381 IFormatProvider</p><p>interface, 340, 341, 343, 344 parameter, 348</p><p>IFormattable interface, 340, 344 IFormatter interface, 614, 632 ignoring response files, 38 IInputStream interface, 655, 657 IL</p><p>code defined, 5 converting into CPU instructions, 104 intellectual property and, 19 verification and, 16</p><p>ILDasm.exe, 40, 259, 659 IList interface, arrays and, 381 implicit type casts, 114 implicitly typed arrays, 239, 376 with anonymous types, 377</p><p>implicitly typed local variables, 237 arrays and, 377 incomplete asynchronous operations,</p><p>displaying, 738 Increment method, 791 IndexerName attribute, 245 indexers. See parameterful properties</p><p>accessing, 245 accessor methods, 244</p><p>compiling, 244 naming, 245 parameters for, 244</p><p>IndexOf method, 199, 329 INEFFICIENT symbol, 132 inherit parameter, 432 inheritance, 295</p><p>for interfaces, 297 of interfaces, 298 multiple, 295 Object class and, 296 of preconditions, 502</p><p>inherited member conflicts, 170 InitializeLifetimeServices method, 567 initializers, 235 initializing</p><p>collections, 235 objects, 106, 235</p><p>initializing fields, 182, 187 inline, 179 with convenient syntax, 183</p><p>inline arrays, 390 inline field initialization, 179 inlining code, 247 InnerExceptions collection, 699 Insert method, 338 InsertAt method, 358 instance constructors, 181, 184</p><p>calling explicitly, 185 creating types without, 182 default, 181 defined, 151 defining multiple, 182 inheritance, 181 virtual methods and, 182</p><p>instance fields</p><p>AppDomains and, 566 performance and, 567 serializing, 619</p><p>instance methods, 163, 595 calling, 199 calling back, with delegates, 395 for Char type, 318 thread safety, 760 volatile keyword, 767</p><p>int type, 112</p><h6>Intermediate Disassembler utility</h6><p>Int32 field, in Task objects, 708 Int32 variables, static methods for, 768 intellectual property</p><p>IL code and, 19 NGen.exe and, 20</p><p>IntelliSense, 200 interface keyword, 297 interface methods</p><p>calling, 302 calling with nullable types, 448 strongly typed, 304 for value types, 319</p><p>interface types, constraints for, 288 interfaces</p><p>vs. base types, 312 boxing and, 306, 308 collection-related, 313 constraining generic type parameters to, 305 defined, 296 examples of, 297 generic, 297, 303 generic vs. non-generic versions, 304 inheritance, 297, 298 items comprising, 296 methods, calling, 300 multiple implementations of, 305 naming conventions, 297 non-generic, 308 as parameter types, 306 public methods, 299 with same methods, 307 static members and, 297 String type and, 300 type parameters, 305 type safety and, 309 value types and, 301 variables of, 300 versioning, 313 virtual methods and, 299 visibility/accessibility, 297 WinRT, 646</p><p>Interlocked class, 768, 769, 773, 776, 803,</p><p>805, 810 interlocked constructs, 762, 768 Intermediate Disassembler utility.</p><p>See ILDasm.exe</p><h6>InternalsVisibleTo attribute</h6><p>InternalsVisibleTo attribute, 155 InteropServices namespace, 424, 531 InvalidOperationException exception, 587 InvariantCulture property, 342 invariant type parameters, 279 Invoke method, 396, 403, 595, 603</p><p>Parallel class, 714 invoking type members, 603 I/O Completion Ports, 731 I/O operations, 727</p><p>asynchronous, 729, 731, 746 canceling, 751 context switching and, 731 event handlers and, 742 ignoring SynchronizationContext object, 750 IRPs in, 728 synchronous, 752 thread blocking and, 731 thread priorities, 754 wasted resources and, 728 I/O Request Packets (IRPs), 728 IOException exception, 532 IOTaskScheduler, 712 IOutputStream interface, 655 IProducerConsumerCollection interface, 820 IRandomAccessStream interface, 655, 657 is operator, 95 IS-A relationship, 312 IsCancellationRequested property, 698 IsDefined method, 366, 367, 430, 431</p><p>for bit flag-enumerated types, 371 IsEnumDefined method, 366 ISerializable interface, 625, 629, 630</p><p>defining type for, 630 special constructor, 629</p><p>ISerializationSurrogate interface, 640 type, 637</p><p>IsInterned method, 330 IsPrefix method, 329 IsServerGC property, 521 IssueClientRequestAsync method, 733, 734, 747 IsSuffix method, 329 IsTransparentProxy method, 565, 569 ISurrogateSelector interface, 639</p><h6>J</h6><p>jagged arrays, 375 JIT compiler, 12</p><p>calling methods, 106 optimizing, 15 performance hit from, 14, 16 unsafe code and, 18 JITCompiler function, 12 Join method, 680 JustEnded method, 771</p><h6>K</h6><p>kernel constructs, when to use, 806 kernel-mode constructs, 760, 761, 778, 784</p><p>vs. user-mode constructs, 778, 784 kernel-mode stacks, 671 kernel objects, 671 key/value pairs, hash codes, 142 keywords, vs. type names, 113</p><h6>L</h6><p>lambda expression, 410, 411 vs. anonymous methods, 413 appropriate use of, 416 asynchronous, 746 benefit of, 413 in separate method, 414</p><p>language identifiers, 326 language settings for assemblies, 59 large objects, 519 LastIndexOf method, 329 late binding, 589 Lazy class, 810 lazy initialization, 807 LazyInitializer class, 811 LazyThreadSafetyMode flags, 811 ldstr instruction, 321 lease managers, 567 Leave method, 790 letter casing, 318 LimitedConcurrencyLevelTaskScheduler, 712</p><p>linked lists data types in, 274 inheritance and, 274</p><p>LINQ queries buffering and merging, 720 parallel processing, 719 preserving operation order, 718 processing, 719 processing data from, 718 sequential and parallel, 717 stopping prematurely, 719</p><p>LINQ technology, 202 namespace, 717 anonymous types and, 239 Parallel language integrated query, 717 querying data collections with, 717 standard operators, 717</p><p>LINQ to Objects queries, 717 List class (generic), 266 Listeners, adding delegates to list, 257 literal</p><p>strings. See strings types, 114 livelocks, 761 Load method (AppDomain class), 585 Load method (Assembly class), 585, 616</p><p>overloads of, 584 serialization and, 617</p><p>LoadFile method, 586 LoadFrom method, 586</p><p>serialization and, 617 loading assemblies, from URLs, 586.</p><p>See assembly loading LoadLibrary function, 10 local parameters, callback method references</p><p>to, 413 local variables, implicitly typed, 212 locating assemblies, 63 lock, 799</p><p>scalability issues, 814 locking threads, double-check locking, 807 lockTaken variable, 799 long type, 112, 113 low surrogate code value, 333 lowercase/uppercase, converting between, 318 LowLatency garbage collection mode, 521</p><h6>MEF (Managed Extensibility Framework)</h6><h6>M</h6><p>Machine.config file, 64 Main method, 43, 764 maintaining state, 482 MakeByRefType method, 608 MakeGenericType method, 596 managed applications, prioritization of, 687 managed code. See IL code</p><p>AppDomains and, 576 C++ compiler and, 6 escalation policy, 577 performance concerns, 15 performance of, vs. unmanaged, 15 reducing processes with, 17 state corruption and, 477 WinRT components as, 660</p><p>Managed Debugging Assistants (MDAs), 538 Managed Extensibility Framework (MEF), 598 managed heap</p><p>allocating resources from, 506 arrays in, 374 example of, 507 garbage collection and, 507</p><p>managed modules, 4 combining into assemblies, 7 components of, 5 metadata in, 5</p><p>manifest tables, 47, 49 ManifestResourceDef table, 47 manual-reset events, 782 ManualResetEventSlim class, 794, 804 marking objects in garbage collection, 508 Marshal class, 360 MarshalByRefObject class, 564, 567, 599, 798 MarshalByValType type, 568 marshaling objects by value, 568 Match method, 434 Math class, 759 Maximum method, 776 Max method, 759 MaxValue field, 317 MBCS (Multi-Byte Character Set), converting</p><p>Unicode to, 350</p><p>MDAs (Managed Debugging Assistants), 538 MEF (Managed Extensibility Framework), 598</p><h6>member accessibility</h6><p>member accessibility, 156, 166 MemberInfo class, 602 type, 599</p><p>MemberRef table, 40 MemberwiseClone method, 182</p><p>System.Object, 92 memcpy function, 378 memory</p><p>addresses, passing between CLR and</p><p>WinRT, 656 allocating, 506, 507, 513 compacting, in garbage collection, 509, 521 corruption, 506 garbage collection and, 507 inefficient allocation, 676 leaks, 506, 510, 526 limitations of, 506 monitoring, 524 OutOfMemory exception, 510 pressure methods, 539 reclaiming, with garbage collection, 517</p><p>MemoryStream class, 656 object, 614</p><p>merging LINQ query results, 720 metadata, 5</p><p>assembly, 586 components of, 38 constants and, 175 for conversion operator methods, 197 definition tables in, 39 for events, 604 examining, 431 in human-readable form, 40 identical format of, 152 manifest tables, 47, 49 on properties, 603 property definitions in, 231 reference tables, 39 strings in, 332 tokens, 50 uses for, 5 WinRT, 644, 659</p><p>method parameters, passing, 214 MethodDef table, 39</p><p>MethodInfo method, 417 objects, converting to</p><p>RuntimeMethodHandle, 609</p><p>methods, 162 accessor, 228, 231, 247, 248 anonymous types and, 240 callback, 249 calling, 102, 103, 163 calling for the first time, 11 calling for the second time, 14 compiling once only, 277 conflict resolution, 170, 171 contract, maintaining when exception</p><p>thrown, 483 conversion operator, 195 declaring as constants, 224 declaring return types, 223 defined, 24, 152 for encoding classes, 355 for enumerated types, 371 examples of, 163 explicit, and state corruption, 476 explicit interface method implementations</p><p>(EIMIs), 303, 308, 310 extension, 198, 255 generic, 265, 270, 281, 283, 286 generic and non-generic, 284 instance constructors, 181, 184 interface, calling, 302, 448 invoking after removal from delegate</p><p>chain, 255 marking as async, 733 nonvirtual vs. virtual, 165 operator overload, 191 out and ref parameters, 282 overloading, arity-based, 286 overrides, 172 overriding, 299 parameters. See parameters parameter types, 214 partial, 204 passing variable number of arguments</p><p>to, 220 performance issues, 167 polymorphic, 166</p><p>public, 299 referencing, 35 reliability contract documentation, 496 reliable, 497 return types, 395 splitting specific and generic, 283 static, 106 tables, 301 thread-safe, 759 translating input into events, 256 tuple types and, 241 type constructors, 187 type parameters, defining, 282 virtual, 133, 163, 169 virtual, changing to override, 172 virtual instance, 108 WinRT, 646, 650</p><p>MethodWithReturn method, 568 Microsoft Silverlight applications, AppDomains</p><p>and, 574 Microsoft Spy++, 683 Microsoft SQL Server, AppDomains and, 575 Microsoft Visual Studio, Checked setting, 117 Microsoft Windows NT kernel, 670 migrating code from .NET Framework</p><p>versions, 459 MinValue field, 317 ModuleDef table, 39, 81 ModuleRef table, 39, 81 modules</p><p>building types into, 35 combining into assemblies, 45 in friend assemblies, 156</p><p>Monitor class, 476, 794 commonly used methods, 795 example code, 796 passing reference to type object into, 798 problems with, 798 as static class, avoiding, 798</p><p>monitoring AppDomains, 571 memory usage, 524 object lifetimes, 546</p><p>MPGO.exe, 21 MSCorEE.dll file, 554 MSCorLib.dll, 35, 558</p><h6>newobj instruction</h6><p>MSCorWks.dll, 554 MSIExec.exe, 61 MulticastDelegate class, 397</p><p>non-public fields, 398 multi-dimensional arrays, 385, 386 multifile assemblies, 46, 48 multiple cores, 677. See also CPUs</p><p>processing power of, 682 taking advantage of, 682 thread starvation on, 684 UI element removal and, 682 user productivity and, 682</p><p>multiple inheritance, 295. See also interfaces multithreaded operating system, 684 MultiWebRequest class, 770 Mutex</p><p>class, 780, 785 lock, 792 type, 525 mutexes, 785</p><h6>N</h6><p>naked constraints, 288 named parameters, 209, 424</p><p>arguments, order of evaluation with, 210 rules and guidelines, 211</p><p>NamedPipeServerStream class, 743 namespaces, 97</p><p>aliases, creating, 99 assemblies and, 101 creating, 100 WinRT, 645 native C++, 659 native resources</p><p>cleanup code for, 534 fixed in number, 538 garbage collection and, 525, 538 wrapping, 530, 532</p><p>NetworkStream object, 355 new operator, 92, 506</p><p>delete operator and, 93 newline characters, 322 NewLine property, 322 NewMailEventArgs object, 253 newobj instruction, 321</p><h6>NGen.exe</h6><p>NGen.exe, 16, 19</p><p>intellectual property and, 20 nonstatic events, 152 Normal flag, 546, 547 notifications</p><p>for exceptions, 456 for garbage collection, 518, 523 for thread attachment/detachment, 671 unregistering interest in, 260</p><p>null</p><p>comparing generic type variables with, 291 initializing locally typed variables with, 213 in WinRT, 647</p><p>nullable types, 288, 441 boxing, 447 C# support for, 443 calling GetType via, 448 calling interface methods with, 448 CLR support for, 447 null-coalescing operator and, 446 operators and, 444 overloaded operators and, 445 performance and, 445 unboxing, 448</p><p>nullable value types, 646 number types, parsing strings into, 348 NumberFormatInfo type, 343 Numbers, vs. enumerated types, 361 NumberStyles parameter, 348 numeric types, 117. See also types</p><p>recognized formats, 340</p><p>NumTimesWordAppearsEquals method, 331 NumTimesWordAppearsIntern method, 332</p><h6>O</h6><p>obfuscator utilities, 19 Object class, 296, 362</p><p>inheritance, 296 object equality, 139 checking, 140 properties of, 141</p><p>object graphs, serializing, 614 serializing multiple, 615 object handles, wrapping, 778</p><p>object identity, checking, 140 object lifetime, controlling manually, 545 object-oriented programming (OOP), 160 ObjectDisposedException exception, 534 ObjectHandle type, 595 ObjectInvariant method, 502 objects</p><p>allocating and initializing, 106 casting between types, 93 converting between types, 195 corrupted state, avoiding, 228 creating, 92, 106, 596 Equals method, 139 formatting, 344 GetHashCode method, 142 hash codes, 142 initializers, 235 invariants, 498, 501 marshaling by reference, 564 type, 23, 112</p><p>OnDeserialization method, 629 OneManyLock class, 802</p><p>performance results, 804 OneManyResourceLock class, 804 OnNewMail method, 254 OnThreadException virtual method, 488 op_Addition method, 193 open types, 272 OpenAsync method, 753 OpenExisting methods, 780 operands, generic type variables as, 292 operating system instability, reasons for, 34 OperationCanceledException exception, 652 operations</p><p>canceling, 696 canceling after set time, 700 canceling, invoking methods after, 698 cooperative cancellation, 696 preventing cancellation of, 698</p><p>operator overload methods, 191 calling internally, 194 CLR handling of, 191 for core numeric types, 193 example of, 192 naming rules, 194</p><p>operators, list of, 193 overloads defined, 152 parameters for, 191 support for, 191 unsupported, 193</p><p>operators binary, 192 checked and unchecked, 116 null-coalescing, 446 and nullable types, 444 unary, 192</p><p>op_Explicit method, 197 op_Implicit method, 197 OptimizedAway method, 763 optimizing code, 14, 277, 763</p><p>Volatile class and, 765 optional attribute, 212 optional parameters, 209</p><p>arguments, order of evaluation with, 210 rules and guidelines, 211</p><p>OrderedTaskScheduler, 713 ordinal equality check, 329 out keyword</p><p>overloading methods based on, 216 vs. ref keyword, 215</p><p>out parameter, 282 OutOfMemory exception, 510 overflows, checked and unchecked, 115 overhead fields, 795 overloading</p><p>arity-based, 286 nullable types and, 445</p><p>overloading operators. See operator overload</p><p>methods override methods, 172 overriding methods, 299</p><p>generic, 286 ownership, of thread locks, 791</p><h6>P</h6><p>packaging assemblies, 60 Parallel class, 713, 714</p><p>performance issues, 714 when to use, 714</p><h6>ParseExact method</h6><p>Parallel LINQ, 717 buffering and merging, 720 parallel processing queries, 719 partitions across CPU cores, 720 preserving operation order, 718 sequential and parallel queries, 717 stopping queries prematurely, 719</p><p>parallel queries, 717 ParallelEnumerable class, 717, 719 ParallelLoopResult instance, 716 ParallelLoopState object, 716 ParallelOptions object, 714 ParamArray attribute, 221 ParamDef table, 39 parameterful properties, 227, 242</p><p>accessing from unsupported languages, 247 selecting, 246</p><p>ParameterInfo object, 602 parameterless constructors, 186, 235 parameterless properties, 227 parameters</p><p>declaring as constants, 224 declaring types, 214, 223 default values, 210, 212 default values, changing, 211 for indexers, 244 named, 209 optional, 209 out keyword vs. ref keyword, 215 params keyword, 221 passing by reference, 215 passing via method, 214 types, in methods, 214 types, interfaces as, 306 validation, 366 variables, 211 weak vs. strong, 223</p><p>params keyword, 221 Parse method, 348, 366, 370</p><p>exception handling for, 493 overloading, 349 parameters, 349 performance and, 350 skipping leading space, 348</p><p>ParseCombiningCharacters method, 333 ParseExact method, 349</p><h6>parsing</h6><p>parsing dates and times, 349 strings, 348</p><p>partial keyword, 159 partial methods, 204 efficiency of, 205 overriding types with, 205 private keyword and, 207 return type for, 207 rules and guidelines, 207 runtime performance and, 206 sealed classes and, 205 in Visual Studio, 206</p><p>passing arrays, 382 passing parameters, via reference, 215 path names, loading assemblies with, 586 pbytes local variable, 549 PDB (Program Database) files, 15 PE (portable executable) files</p><p>manifest, 47 command-line switches for compiling, 47 components of, 38 embedding resource files into, 53</p><p>PE32 (+) files, 6 (+) headers, 38 PE32 headers, 5 platform version and, 9</p><p>PerfMon.exe, 524 performance concerns, 15 performance counters, 524 performance issues</p><p>binding handles and, 608 context switching and, 672, 673 DLLs and, 672 double-check locking and, 808 exception handling and, 492 execution contexts and, 695 hyperthreading and, 677 instance fields and, 567 I/O operations and, 731 of methods with variable number of</p><p>arguments, 222 monitoring with Task Manager, 675 Parallel class and, 714 reflection and, 589</p><p>tasks and, 708 thread pools and, 692 threads and, 670, 674, 681 thread synchronization and, 758 thread synchronization locks and, 790</p><p>Performance Monitor, testing exception</p><p>handling with, 493 PerfView, 525 periodic compute-bound operations, 720 PEVerify.exe, 18 Pinned flag, 546 pinning objects, 548 points of failure, sources of, 471. See also</p><p>exceptions; exception handling policy controls, 86 polygons, arrays of, 375 polymorphism, WinRT components and, 646 pooling strings, 332 positional parameters, 424 PostAsync method, 742 postconditions, 498, 501</p><p>execution of, 502 side-effect-free members, 501</p><p>Power Threading library, 804 preamble, 353 preconditions, 498, 501</p><p>inheritance of, 502 side-effect-free members, 501</p><p>preemptive multithreaded operating</p><p>system, 684 PrepareConstrainedRegions method, 496 primary constraints, 287 primitive thread sychronization constructs, 760</p><p>kernel-mode, 761, 778, 784 interlocked, 768 user-mode, 762 volatile, 766</p><p>primitive types, 111 checked and unchecked, 115 comparing to each other, 292 defining constants with, 175 dynamic, 144 as enumerated types, 363 vs. FCL types, 113 with FCL types, 112 parameter default values, 211</p><p>primitives, overflows and, 115 prioritizing threads, 684, 754</p><p>by application, 687 classes for, 685 High priority, 685 Idle priority class, 685 Idle priority level, 687 importance of, 688 mapping priority levels, 686 priority boosting, 774 process priority class, 686 raising and lowering priority, 687 Realtime class, 685 relative priority classes, 685 Time-Critical priority level, 687 for Windows Store apps, 687 zero page thread, 684, 686 PrioritizingTaskScheduler, 713 priority</p><p>inversion, 756 thread, 679</p><p>private assemblies, 67</p><p>strongly named, 79 private fields, 168, 228, 231 private keys. See public and private keys privately deployed assemblies, 61 private methods</p><p>partial methods as, 207 serialization and, 622 type constructors, 188 private thread locks, 797 problem signatures, 487 Process class, 688 processes</p><p>creating, expensive nature of, 675 creating, vs. creating threads, 675 defined, 670 example of, 558 isolation, 557 process priority class, 686 reducing, with managed code, 17 terminating, 477, 574 threads per, 674 wasteful, identifying, 675</p><h6>public and private keys</h6><p>processor architecture, loading assemblies</p><p>for, 585 ProcessThread class, 688 productivity vs. reliability in error handling, 469 program order, 765 Program type, 43 prologue code, 102 properties</p><p>accessor accessiblity, 248 accessor methods, 231 automatically implemented, 231 backing fields, 230 behavior and, 248 compiler results, 244 confusion caused by, 232 C# support for, 231 declaring, 231 declaring, in anonymous types, 238 defined, 24, 152 defining, 230 defining intelligently, 232 example of, 229 generic accessor methods, 248 in metadata, 231 metadata on, 603 parameterful, 227, 242, 246 parameterless, 227 vs. public fields, 425 returning different values, 233 thread synchronization and, 233 in Tuple types, 241 Visual Studio debugger and, 234 when to use, 233</p><p>PropertyDef table, 39 PropertyInfo type, 603 public and private keys</p><p>for assemblies, 67. See also strongly, named</p><p>assemblies creating, 69 in CSP containers, 78 delayed signing, 76 in hardware devices, securing, 78 hashing to reduce space, 71 for publisher policy assemblies, 87</p><h6>public</h6><p>public constructors, defining, 195 fields, vs. properties, 425 methods, interfaces and, 299 types, 25, 154</p><p>publisher policy controls, 86 publisherPolicy elements, 85 publishing values, 808 Pulse method, 813 PulseAll method, 813 purpose of book, xxiii PutAsync method, 742</p><h6>Q</h6><p>qsort function, 391 quantums, 672</p><p>ending early, 673 queries, LINQ. See LINQ queries QueryMemoryResource notification, 519 question marks, in C# code, 443 queues, thread-safe, 813 QueueUserWorkItem method, 409, 693,</p><p>700, 724</p><h6>R</h6><p>raising events, in thread-safe way, 254 Rational type, 195 RCWs (Runtime Callable Wrappers), 644 Read method, 753</p><p>in I/O operations, 728 Read method (Volatile class), 765, 767, 768 read-only files, setting attribute for, 368 ReadAsync method, 729, 733, 742, 753 ReadBlockAsync method, 742 ReaderWriterLock class, 802 reader-writer locks, 801, 804 ReaderWriterLockSlim class, 800 ReadLineAsync method, 742 readonly fields, 177</p><p>defining, 178 ReadToEndAsync method, 742 real-time operating systems, 684</p><p>Realtime priority class, 685 Rectangles, constructing, 185 recursive locks, avoiding, 806 recursive thread locks, 786 ref keyword</p><p>overloading methods based on, 216 vs. out keyword, 215</p><p>ref parameter, 282 reference counting, 508 ReferenceEquals method, 140, 331 References, checking, 157 reference tables in metadata, 39 reference tracking, 508 reference type variables, setting to null, 288 reference types, 118, 320</p><p>arrays of, 373 classes as, 119 converting value types into, 125 example code, 121 examples of, 119 field layout, 123 generic, 269 null-coalescing operator and, 446 overlapping with value types, 124 performance issues, 119 as primary constraints, 287 ref keyword vs. out keyword, 215 serializing, 618 sharing code, 277 swapping, 218, 219 with type constructors, 188 vs. value types, 122</p><p>referencing assemblies, 70, 98 strongly named assemblies, 74 types, 80</p><p>reflection, 141, 148, 430 discovering type members, 599 exception handling and, 485 object model, discovering, 602 performance issues, 589 serialization and, 614 type discovery with, 590, 604 type safety and, 589</p><p>reflection API for .NET Framework, 583 reflection members, generics and, 271 reflection methods, 432 Reflection namespace, 431, 588 reflection types, 588, 589</p><p>hierarchy of, 600 ReflectionOnlyGetType method, 591 ReflectionOnlyLoadFrom method, 587 ReflectionOnlyLoad method, 587 regional settings, 326 Register method</p><p>CancellationToken class, 699 CancellationTokenSource class, 698</p><p>RegisterForFullGCNotification method, 523 registering assemblies, 74 relational operators, nullable types and, 444 relative thread priority classes, 685 ReleaseMutex method, 785 ReliabilityContract attribute, 496 Reliability Monitor</p><p>problem signatures, 487 terminated applications, viewing in, 487</p><p>reliability vs. productivity in error handling, 469 Remove method, 260, 338, 403 RemoveAt method, 358 RemoveEventHandler method, 603 RemoveMemoryPressure method, 538 RemoveParticipant method, 805 Replace method, 338 ReportStatus method, 772 ResetAbort method, 579, 580 resolving types, 593 resource files, adding to assemblies, 53 resource leaks, 531 resources, accessing, 505 response files, 36</p><p>ignoring, 38 resurrected objects, 544 return types, discovering information on, 602 returning arrays, 382 reverse engineering, protecting against, 19 roots, 508</p><p>in garbage collection, 509 Run method, 745</p><p>Runtime</p><p>binders, 148 handles, 608</p><p>Runtime Callable Wrappers (RCWs), 644 RuntimeWrappedException class, 458</p><h6>S</h6><p>safe places, 580 SafeHandle class, 527 derivation of, 528 SafeHandle namespace, 529, 530, 531 SafeWaitHandle class, 529 satellite assemblies, 59 sbyte type, 112 scalability, threads and, 674 scheduling tasks, 711</p><p>reader/writer semantics, 815 scheduling threads, 683, 684 sealed classes, 168</p><p>partial methods and, 205 secondary constraints, 288 SecureString class, 357, 358 decrypting objects, 360 FCL support for, 358 passing objects as a password, 358</p><p>secure strings, 357 security, class, 168 SecurityCritical attribute, 489, 695 security settings, thread pools and, 694 Semaphore class, 780, 784 semaphores, 784</p><p>vs. auto-reset events, 784 as primitive kernel-mode thread</p><p>synchronization constructs, 778 SemaphoreSlim class, 794, 815 sequential queries, 717 serialization, 589</p><p>AddValue method and, 627 of application state, 616 assemblies and, 616 cloning with, 615 of collection objects, 622</p><h6>serialization</h6><h6>SerializationBinder class</h6><p>serialization (continued) controlling, 619 controlling data, 624 converting types, 628 defining type for, 630 definition of, 611 formatter consistency, 615 of instance types, 619 ISerializable interface, 625, 630 matching deserialization method, 628 of multiple object graphs, 615 namespace, 623</p><p>.NET Framework support for, 612 of object graph, 614 of object set, 622 overriding assembly/type, 641 quick start for, 613 reflection and, 614 security and, 619 of singleton types, 633 special constructor type, 628 streaming contexts for, 631 surrogate selector chains, 639 surrogates for, 636 of type instances, 623 of types, 617, 618 of types, as different types, 633 to XML stream, 612, 614 when to avoid, 619</p><p>SerializationBinder class, 640 SerializationException exception, 615, 616,</p><p>618, 622 SerializationInfo object, 625, 628 Serialize method, 614 SerializeToMemory method, 614 Server garbage collection mode, 520 Servers, asynchronous implementation, 751 servicings, 161 SetAt method, 358 SetMaxThreads method, 724 SetMinThreads method, 724, 726 SetType method, 626, 635 SetValue method, 603 shared data, Parallel class and, 714</p><p>shim DLL, 554 short type, 112 SignalAndWait method, 805 signed data types, 117 signing files, 70 Silverlight applications</p><p>AppDomains and, 574 processes run by, 687</p><p>Silverlight, threading model, 748 SimpleHybridLock method, 793 SimpleSpinLock method, 773 SimpleWaitLock method, 782, 784 simplifying applications, 682 SimulateNewMail method, 256 single-dimensional arrays, 375 single-instance applications, 781 Singleton class, 809 singleton types, serialization of, 633 Sleep method, 774 SN.exe, 68</p><p>delayed signing with, 77 Soap namespace, 614 SoapFormatter</p><p>class, 614 object, 614</p><p>Software Publisher\u2019s Digital ID for</p><p>Authenticode, 488 SomeMethod method, 565, 566 SOS Debugging Extension, 525 source code</p><p>compiling, 3 splitting types between, 159 protection, 267 source control, 159 spin locks, 773. See also thread synchronization spin loops, thread yielding and, 775 SpinLock method, 775 SpinLocks, 818 spinning threads, with hybrid locks, 791 SpinWait method, 774 Spy++, 683 SQL Server, AppDomains and, 575 stackalloc statement, 388 StackOverflowExceptions, 497</p><p>stack space, 102</p><p>checking for, 489 stack trace, 461, 462 complete, 462 constructing, 462 interpreting, 463</p><p>StackTrace property, 461 Start method (Thread class), 679 StartNew method, 710 StartsWith method, comparing strings, 325 startup time, improving, 19, 21 starvation, thread, 684 state corruption</p><p>destroying, 476 exception handling and, 474 FailFast method, 480 managed code and, 477 mitigating, 475 terminating process for, 477</p><p>state machines, returning threading model, 749 stateless encoding and decoding, 356 statements, checked and unchecked, 116 static classes, 158 static constructors. See type constructors</p><p>defining on generic types, 273 static events, 152 static fields, 177 defining, 178 initializing, 189 memory leaks and, 510</p><p>static members interfaces and, 297 MarshalByRefObject class and, 567</p><p>static methods for arrays, 380 calling, 106 calling back, with delegates, 394 calling operator overload methods with, 194 for Char type, 318 extension attribute, 203 Finalize method and, 526 for Int32 variables, 768 thread safety, 759, 760 volatile keyword, 767</p><p>StaticDelegateDemo method, 394 Stop method (Parallel class), 716 streaming contexts for serialization, 631 StreamReader type, 350 streams, 656 StreamWriter type, 350, 537</p><p>finalization and, 538</p><p>String class hash table, accessing, 330 methods, 338 sealed, 323</p><p>string interning, 329 String objects. See strings</p><p>immutability of, 569</p><p>String type, 112, 320 constructing, 320 interfaces and, 300 method overloads defined by, 325 as primitive type, 320</p><p>StringBuilder class, 198, 345 creating objects, 336 members, 337 methods, 338</p><p>StringBuilder type, 336 StringComparer class, 329 StringComparison type, 324 StringInfo, 333 strings</p><p>base-64 encoding/decoding, 356 changing case of characters in, 325 character arrays, 337 characters, examining, 333 cloning, 335 comparing, 323 concatenating, 322 constructing, 320 constructing efficiently, 336 converting to bytes, 353 converting Unicode to MBCS, 350 copying, 335 custom formatter, 345 date formatting, 341 deleting securely, 358 encodings, 350</p><h6>strings</h6><h6>Strong Name utility (SN.exe)</h6><p>strings (continued) formatting, 344, 346 formatting multiple objects into, 344 immutability of, 323 interning, 329 mapping enumerated types to, 364 in metadata, 332 number formatting, 341 as object representation, 339 parsing, 348 pooling, 332 secure, 357 in source code, 321 uses for, 324 verbatim, 322 WinRT, 647</p><p>Strong Name utility (SN.exe), 68 strongly named assemblies</p><p>characteristics of, 66 components of, 68 compressing, 74 creating, 67 deploying globally, 72 deploying privately, 79 obtaining key for, 68 referencing, 74 security of, 76 tamper resistant nature of, 75 unique identification of, 67 verification, 76</p><p>struct constraint, 287 Structured Exception Handling (SEH), 453.</p><p>See also exceptions; exception handling structures. See value types Substring method, 335 SubstringByTextElements method, 333 SuppressFlow method, 695 SuppressMessage attribute, 440 SurrogateSelector objects, 639 surrogate types for serialization, 636 surrogates for Unicode elements, 333 suspending threads, 774 sync blocks, 794</p><p>array, 795 index, 105 initialization of, 795</p><p>optimizing, 795 public indexes, 797 relationships, 795 type objects and, 795</p><p>synchronization context task schedulers, 711 SynchronizationContext class, 749 synchronizing threads. See thread</p><p>synchronization synchronous I/O operations, 752. See I/O operations</p><p>FileStream objects and, 753 System Monitor, 524 System namespace, 22, 23 System.Collections.Generic namespace, 270 System.Decimal type, 118, 198 System.Diagnostics.ConditionalAttribute</p><p>attribute, 206</p><p>System.Exception class, catching, state corruption and, 474 type, 460</p><p>SystemException type, 466 System.InvalidCastException type, 95 System.Object type, 26, 91</p><p>delete operator, 93 new operator, 92 protected methods, 92 public methods, 92</p><p>System.Reflection.AssemblyName class, 68 System.Runtime.ProfileOptimization class, 16 System.Text namespace, 97 System.Type object, type object pointer, 110 SZ arrays, 375</p><h6>T</h6><p>TakeFromAny methods, 822 Task class</p><p>vs. Barrier class, 814 vs. thread synchronization, 814</p><p>Task Manager, 670, 675 monitoring performance with, 675</p><p>Task objects, 701 arrays of, 710 Boolean properties for, 709 fields in, 707 ContinueWith task collections, 705</p><p>creation flags for, 701 CurrentId property, 708 disposing of, 708 factory for, 709 Int32 fields in, 708 statuses of, 708 Status property, querying, 708 supplementary state, 707 WaitingForActivation state, 709</p><p>TaskFactory type, 709, 710 TaskLogger class, 738 tasks, 700</p><p>arrays of, 710 canceling, 704 CancellationToken objects, associating</p><p>with, 705 child tasks, 707 composition of, 707 constructors and, 701 creation options, 701 deadlock situations, 702 delaying, 722 delegates, passing, 715 exceptions, detecting, 703 exceptions thrown by, 704 performance issues and, 708 reader/writer semantics, 815 returning void with, 709 scheduling to run, 701 starting automatically on other task</p><p>completion, 705 state, 707 supplementary state, 707 synchronization context task schedulers, 711 task factories, 709 task schedulers, 711, 712 vs. thread synchronization, 807 unhandled exceptions thrown by, 702 unique IDs for, 708 waiting for completion, 702 Tasks namespace, 651, 700, 709 TaskScheduler objects, 711 TaskScheduler type, defining classes derived</p><p>from, 712 TEB (thread environment block), 671 temporary variables, 284</p><h6>thread synchronization</h6><p>terminated applications, viewing in Reliability</p><p>Monitor, 487 terminating process, 477, 574 text elements. See also characters</p><p>examining in strings, 333 TextElementEnumerator method, 333 thread blocking, I/O operations and, 731 Thread class, 678 thread environment block (TEB), 671 thread kernel objects, 671 thread locks, asynchronous functions and, 734 thread pools, 692</p><p>calling methods asynchronously, 693 child tasks, 707 continuing tasks in, 705, 707 deadlock situations, 724 delaying tasks, 722 execution contexts, 695 function of, 692 heuristic nature of, 692 limiting number of threads in, 724 loop state management, 716 parallel performance of, 713 parallel processing, exceptions thrown</p><p>when, 716 performance and, 692 processing order, 725 queuing in, 693 scheduling method calls, 720 scheduling tasks in, 712 security settings and, 694 sequential method execution, 713 thread management in, 723 thread synchronization locks and, 725 worker thread management, 724 work items in, 693</p><p>thread prioritization, 754 thread-safe</p><p>collection classes, 818 queues, 813</p><p>thread safety, 759 thread synchronization</p><p>async functions and, 757 asynchronous, 814 auto-reset events and, 782 avoiding, 759</p><h6>ThreadAbortException exception</h6><p>thread synchronization (continued) blocking, avoiding, 805 blocking countdown, 804 class libraries and, 759 condition variable pattern, 811 constructs, 805 double-check locking, 807 drawbacks of, 757 exception handling and, 476 hybrid, 790 hybrid constructs. See hybrid constructs livelocks and deadlocks, 761 lock, 189, 799 locks, 476, 725 lock scalability issues, 814 m_opCount field, 772 Mutex objects, 786 parallel execution, 805 performance issues, 758 primitive constructs, 760 priority boosting, disabling, 774 private locks, 797 properties and, 233 reader-writer locks, 801, 804 recursive locks, 786 recursive locks, avoiding, 806 specifying synchronization object, 811 spin lock addition logic, 774 spin locks, 773 tasks vs., 807 yielding threads when, 775</p><p>ThreadAbortException exception, 580 ThreadException event, 488 threading models, 748 Threading namespace, 695, 720, 778</p><p>DispatcherTimer class, 723 Timer class, 723</p><p>ThreadPerTaskScheduler, 713 ThreadPool class, 693</p><p>for Windows Store apps, 694</p><p>threads, 102 in 16-bit Windows, 669 aborting, 578, 679 allocating local variables, 105</p><p>asynchronous compute-bound operations</p><p>with, 678 avoiding, 674 blocking, avoiding, 705 caching of, 673 CLR and Windows interoperability, 678 constructors for, 679 contexts, 671 context structures, 683 context switching, 672, 673, 683 creating, vs. creating processes, 675 debuggers and, 673 dedicated, 679 defined, 670 DLL thread-attach/thread-detach</p><p>notifications, 671 early implementation of, 669 ending early, 673 execution contexts, 694, 695 forcibly killing, 670 foreground, 679 foreground vs. background, 688 garbage collection and, 673 gracefully aborting, 577 High priority class, 685 Idle priority class, 685 Idle priority level, 687 inefficient memory allocation and, 676 kernel-mode stacks, 671 labeling, avoiding, 806 lowering priority, 687 minimizing number of, 676 number per process, 674 overhead, 670 performance issues, 674, 681 prioritizing, 684, 685, 688 priority levels, mapping, 686 priority, non-normal, 679 properties, viewing, 683 Realtime priority class, 685 reasons for using, 681 relative priorities, 685 responsiveness of, 681 safe places, 580</p><p>scalability, 674 scheduling, 683, 684 starting, 679 starvation, 684 suspending, 774 Time-Critical priority level, 687 time-slices, 672 user-mode stacks, 671 Windows Store apps, APIs removed for, 678 yielding, 775 zero page, 684, 686</p><p>ThreadsSharingData class, 766, 767 ThrowIfCancellationRequested method, 704 throwing exceptions, 466. See also exceptions;</p><p>exception types Time-Critical thread priority level, 687 time-slices, 672</p><p>ending early, 673 Timer class, 720, 723 Timer objects, garbage collection of, 721 Timers namespace, Timer class, 723 ToArray method, 658 ToLowerInvariant method, 318 ToObject method, 367 torn reads, 762 ToSingle method, 310 ToString method, 134, 238, 318, 335, 337, 339,</p><p>364, 365, 569 number formatting, 341 output formatting, 369 output of, 364 overriding, 339 parameterless, 340 parameters, 340 vs. Format method, 364 System.Object, 92</p><p>ToString, null for format string, 341 ToUpperInvariant method, 318 Transaction class, 797 TransactionScope class, 475 triggering garbage collections, 519 try blocks, 454. See also catch blocks; finally</p><p>blocks as callback methods, 497 constrained execution regions and, 475</p><h6>type parameters</h6><p>finally blocks and, 457 threads and, 671</p><p>TryParse method, 366, 370, 493 exception handling and, 494</p><p>Tuple class, 241 types, 237, 240 properties and, 241</p><p>type arguments, 266 type constructors, 187</p><p>calling, 188 calling in order, avoiding, 189 default, 187 defined, 151 defining, 188 defining inside value types, avoiding, 188 initializing singleton objects with, 189 mutual references in, 189 naming convention for, 190 private, 188 thread synchronization lock, 189 unhandled exceptions, 189</p><p>type definitions, example of, 153 type inference</p><p>generic methods and, 283 overview of, 283</p><p>type object pointer, 105 Type objects, 591</p><p>converting to RuntimeTypeHandle, 609 converting to TypeInfo objects, 592 for generic types, 272 obtaining, 591 obtaining from type names at compile</p><p>time, 592 open, creating, 272 sync blocks and, 795</p><p>type parameters, 266 constraining to interfaces, 305 constraints, 287, 288 constructor constraints for, 289 contra-variant and covariant, 279 generic, 281 naming conventions, 266 overriding methods and, 286 variance and, 280</p><h6>type-safe languages</h6><p>type-safe languages, 144 type safety, 219, 267</p><p>interfaces and, 308, 309 protecting, 277 reflection and, 589</p><p>type variables, generic, 290, 291, 292 TypeDef table, 39 TypeInfo class, 602 TypeInfo object, 592</p><p>querying, 593 TypeInfo objects, 592 typeless instructions, 17 typeof operator, 429, 592 TypeRef table, 40 types, 24. See also CTS (Common Type System)</p><p>across assemblies, 597 as add-ins, 596 aliases, creating, 99 ambiguous references, 99 anonymous, 237, 238, 239 arity, 273 assembly, 25 Backus-Naur Form grammar for, 592 building into modules, 35 casting between, 93 casting with is/as operators, 95 catch, 456 common properties and methods, 602 constructing instance of, 594 converting, 628 converting between, 195 creating objects for, 596 data fields in, 105 declaring, 214 defining, 35, 105, 251 disambiguating, 99 discovering members, 599, 604 dynamically constructing, 590 enumerated, 361 enumerating, 588 example definition of, 28 exception, 463, 466 exception-derived, hierarchy of, 593 exposing events with, 250 external, 35</p><p>field layout, 123 general format, 341 generic, 265, 266, 269, 274, 276, 596 generic identity, 275 generic interfaces and, 278 help documentation for, 310 helper, 169 inferring from initializing expression, 212 initializing static fields, 189 invoking members, 603 vs. keywords, 113 limiting, for generics, 285 linked lists, 274 listening for events, 258 literal, 114 locations of, 81 member accessibility, 156 members of, 24, 151 method tables, 301 moving between assemblies, 83 namespaces, 97, 99 naming conventions, 273 nullable, 441, 443, 445 overloading, arity-based, 286 partial, 159 performance issues, 167 primitive, 111 properties, obtaining, 593 public, 25, 154 querying, 600 recognized formats, 340 reference, 118 referenced assemblies, 98 referencing, 35 reflection, 588, 589, 600 resolving, 593 resolving references, 80 restricting access to, 25 returning name of, 339 for runtime handles, 608 safety, 95 serializing, 617, 618, 620, 623 signed, 117 singleton, 633 surrogate, for serialization, 636</p><p>System.Object derivation, 91 Tuple, 237, 240 unique names, 99 unloaded, 191 value, 118, 277 visibility, 154, 156, 166 Visual Studio tooltips for, 214 WinRT, 646 interoperability, 649 without instance constructors, 182</p><h6>U</h6><p>UI elements, removing, 682 uint type, 112 ulong type, 112 unary operators, 192</p><p>nullable types and, 444</p><p>unboxing nullable types, 448 value methods, 135</p><p>unboxing value types, 124, 126 efficiency of, 130 example code, 128 field copying and, 127 unchecked operators, 116 unhandled exceptions, 474, 480, 485 asynchronous functions and, 745 in distributed applications, 488 FCL documentation, 488 from type constructors, 189 UnhandledException event, 488 UnhandledExceptionFilter event, 488 Unicode characters, 333</p><p>converting to MBCS, 350 Unicode code values, 317 UnicodeCategory type, 317 Unload method, 566, 570 unloaded types, 191 unloading</p><p>AppDomains, 477, 494, 566, 570 assemblies, 587 as garbage collection trigger, 519</p><p>unmanaged C++, 224</p><h6>value types</h6><p>unmanaged callback functions, 391 unmanaged code</p><p>C++ compiler and, 6 CLR interoperability with, 30 performance of, vs. managed, 15 UnobservedTaskException event, 703 Unregister method, 260 unsafe code, 17 unsafe modifier, 388 UnsafeLoadFrom method, 586 Unwrap method, 595 uppercase/lowercase, converting between, 318 Uri type, 648 URIs, WinRT, 648 user-defined attributes. See custom attributes user-mode constructs, 760, 762</p><p>vs. kernel-mode constructs, 784 vs. primitive-mode constructs, 778</p><p>user-mode stacks, 671 ushort, 112 using directive, generic types and, 276 using statement, 536</p><ul style=\"list-style-type: none;\">\n<li><p>UTF-7 encoding, 351</p></li>\n<li><p>UTF-8 encoding, 351 UTF-16 encoding, 351 UTF-32 encoding, 351</p></li>\n</ul><h6>V</h6><p>value methods, boxing and unboxing, 135 value types, 118, 441</p><p>boxing and unboxing, 124, 126, 128 calling virtual methods, 133 changing fields in, 136 complexity of, 138 constructors, 184 converting to reference types, 125 default constructors, 186 defining, 132 defining constructor methods on, 185 defining type constructors in, avoiding, 188 example code, 121 examples of, 119 field layout, 123 immutability of, 138</p><h6>ValueType class</h6><p>value types (continued) initializing fields, 187 interface methods for, 319 interfaces and, 301 native code for, 277 nullable, 441, 443, 646 out keyword vs. ref keyword, 215 overlapping with reference types, 124 vs. reference types, 122 serializing, 618 with type constructors, 188 unboxing, and field copying, 127 verifiability, 186 virtual methods, calling, 165 when to use, 121 WinRT, 646</p><p>ValueType class, 362 var keyword, 237</p><p>vs. dynamic, 146 vs. dynamic keyword, 214</p><p>variables cloneable, 301 implicitly typed, 212, 237 initializing with null, 213 of interface types, 300 temporary, 284</p><p>variance, restrictions on, 280 vectors, 375. See also arrays verbatim strings, 322 verification</p><p>of generics, 284 generics and, 290 IL and, 16 skipping, 18</p><p>verifying assemblies, 76</p><p>skipping, 77 VeriSign IDs, 488 version</p><p>numbers, 58 resources, 54 attributes, 56</p><p>versioning, 161, 167 assemblies and, 587, 597 extension methods and, 201 interfaces and, 313 virtual methods and, 169</p><p>virtual instance methods, 108 virtual methods, 163, 165</p><p>calling, 133 call instruction for, 165 constructor methods and, 182 interfaces and, 299 minimizing, 166 performance of, 167 security of, 168 versioning and, 169 visibility, type, 154, 166 Visual Studio</p><p>debugger, and properties, 234 exception debugging, 490 exceptions, adding to debugger, 491 Exceptions dialog box, 490 IDE, adding assemblies with, 51 Intellisense, 200 partial methods in, 206 publishing applications with, 61 speed of, due to multiple cores, 682 target platform, setting, 8 tooltips, viewing, 214 Windows Runtime Component project, 659</p><p>volatile class, 765, 767, 768, 773, 805, 808 fields, 177 keyword, 767</p><p>volatile constructs, 762, 766 C# support for, 767 drawbacks of, 767</p><p>Volatile.Read method, 254</p><h6>W</h6><p>Wait method, 702 WaitAll method, 703, 779 WaitAny method, 703, 779 WaitAsync method, 815 WaitCallBack delegate type, 693 WaitForConnectionAsync method, 743 WaitHandle class, 778 WaitOne method, 779 Weak flag, 546, 549 weak references, 550, 551</p><p>weakly named assemblies, 66, 68</p><p>private-only deployment, 67 WeakReference class, 549 WeakTrackResurrection flag, 546, 549 WebClients, asynchronous functions and, 744 web-downloading assemblies, 586 Web Forms, asynchronous, 751 WhenAll method, 747 WhenAny method, 747 Windows, as preemptive multithreaded</p><p>operating system, 684 Windows Action Center, viewing unhandled</p><p>exceptions in, 486 Windows Error Reporting, 488 Windows Forms, threading model, 748 Windows Installer service, 61 Windows NT kernel, 670 Windows Presentation Foundation (WPF),</p><p>threading models, 748 Windows Runtime Component project in Visual Studio, 659 Windows scheduler, asynchronous operations and, 694</p><p>Windows Store apps, 36 C#-implemented WinRT components, 659 threading APIs removed, 678 threading model, 748 thread prioritization and, 687 WinRT components, 643</p><p>WindowsRuntimeBuffer class, 658 WindowsRuntimeBufferExtensions class,</p><p>657, 658 WindowsRuntime directive, 657 WindowsRuntimeStorageExtensions class, 654 WindowsRuntimeSystemExtensions class, 653 Windows RunTime (WinRT), 31. See WinRT WinMDExp.exe, 659 WinRT</p><p>arrays, 648, 660 asynchronous APIs, calling, 649, 653 base type, 645 canceling APIs, 653 classes, 646 collections, 648 core concepts, 645</p><h6>Wintellect Power Threading Library</h6><p>dates and times, 647 delegate types, 647 enumeration types, 646 events, 647 exceptions, 647 explicitly calling methods, 648 features exposed by, 643 file names and namespaces, 645 framework projections, 649 IAsyncXxx interfaces, 651 IAsyncXxx method conversion into .NET</p><p>Framework Task method, 654 IBuffer interface, 656 IClosable/IDisposable interfaces, 648 interfaces, 646 JavaScript example code, 664 mapping CLR types to, 659 memory addresses, passing to CLR, 656 metadata, 659 methods, 646, 650</p><p>.NET interoperability, 654 nullable value types, 646 passing blocks of data to CLR, 656 properties, 647 StorageFile, converting to .NET XElement, 655 strings, 647 structures, 646 type system, 646 type system interoperability, 649 URIs, 648 value types, 646 WinRT components CLR projections and, 645 as COM components, 644 defining in C#, 658 FCL types and, 645 file extensions, 644 as managed components, 660 metadata used by, 644 polymorphism and, 646 type system, 645 for Windows Store apps, 659</p><p>WinRTAsyncIntro method, 650, 652 Wintellect Power Threading Library, 690</p><h6>WithCancellation method</h6><p>WithCancellation method, 719, 751 WithMergeOptions method, 720 work items in thread pools, 693 Worker method, 764 Workstation garbage collection mode, 520 WoW64 technology, 9 wrapping native resources, 530, 532 Write method, 131, 345</p><p>exceptions thrown by, 534 Write method (Volatile class), 765, 767, 773, 808 WriteAsync method, 742 WriteLineAsync method, 742 WriteLine method, 130, 131, 138, 164, 345, 385</p><h6>X</h6><p>Xaml namespace, DispatcherTimer class, 723 XML configuration files. See configuration files XML elements, converting, 195 XML web wervices applications, AppDomains</p><p>and, 575 XmlSerializer class, 612</p><h6>Y</h6><p>Yield method, 775</p><h6>Z</h6><p>zero-based arrays, 374 zero-length arrays, 382 zero page threads, 684, 686</p><h6>About the Author</h6><p>JEFFREY RICHTER is a cofounder of Wintellect (http://www. Wintellect.com/), a training and consulting company dedicated to helping companies produce better software faster. Jeffrey has written or cowritten many books about Microsoft .NET Framework and Win32 programming, including CLR via C#, Fourth Edition (Microsoft Press, 2012), Windows via C/C++, Fifth Edition (Microsoft Press, 2007), and Programming Server-Side Applications</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile253.png\"/><p>for Microsoft Windows 2000 (Microsoft Press, 2000). Jeffrey was a contributing editor for MSDN Magazine, where he has written numerous feature articles and has been the Win32 Q&amp;A colum-</p><p>nist, .NET Q&amp;A columnist, and Concurrent Affairs columnist. Jeffrey also speaks at various trade conferences worldwide, including Wintellect\u2019s Devscovery, VSLive!, and Microsoft\u2019s TechEd and Professional Developers Conference.</p><p>Jeffrey has consulted for many companies, including AT&amp;T, DreamWorks, General Electric, Hewlett-Packard, IBM, and Intel. His code has shipped in many Microsoft products, among them Microsoft Visual Studio, Microsoft Office, and various versions of Windows. Jeffrey consulted with the .NET Framework team for eight years and maintains an ongoing close relationship with that team as well as the Windows team. In fact, Jeffrey worked with Microsoft to design a new asynchronous programming model that ships with .NET Framework 4.5. This model is similar to what Jeffrey made available with his Power Threading Library since 2005.</p><p>On the personal front, Jeffrey holds both airplane and helicopter pilot licenses, though he never gets to fly as often as he\u2019d like. He is also a member of the International Brotherhood of Magicians and enjoys showing friends sleight-of-hand card tricks from time to time. Jeffrey\u2019s other hobbies include music (especially jazz and progressive rock from the 1970s), drumming, model railroading, and karate. He also enjoys traveling and theater. He lives in Kirkland, Washington, with his wife, Kristin, and his two sons, Aidan and Grant.</p><h5>Expert Consulting and Training</h5><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile254.png\"/><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile255.png\"/><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile256.png\"/><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile257.png\"/><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile258.png\"/><p>John Robbins Jeffrey Richter Jeff Prosise</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile259.png\"/><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile260.png\"/><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile261.png\"/><figcaption>Come to Wintellect for your consulting and training needs. Wintellect\u2019s expert consulting and training is led by John Robbins, Jeffrey Richter, and Jeff Prosise, along with a team of industry leaders. Let Wintellect\u2019s most respected and sought after experts consult, train and deliver excellence to your company and projects.</figcaption><p>Visit wintellect.com to learn more about our services.</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile262.png\"/><h5>If you like the book, you\u2019ll love the training Know how. Anytime, anywhere.</h5><p>Wintellect has provided QUALITY in-person training for over a decade to some of the largest companies in the world.</p><p>Wintellect is proud to now offer another high-quality option to accommodate your ever-changing training needs.</p><h6>Introducing....</h6><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile263.png\"/><p>Your new on demand training solution</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile264.png\"/><p>WintellectNow.com</p><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile265.png\"/><img alt=\"\" src=\"CLR via C# (.NET 4.5), 4th Edition 2012 (\u043e\u0440\u0438\u0433\u0438\u043d\u0430\u043b)_images/imageFile266.png\"/><p>What do you think of this book?</p><p>We want to hear from you! To participate in a brief online survey, please visit:</p><h6>microsoft.com/learning/booksurvey</h6><p>Tell us how well this book meets your needs\u2014what works effectively, and what we can do better. Your feedback will help us continually improve our books and learning resources for you.</p><p>Thank you in advance for your input!</p>"}]}];
